# Invisible Scene Editor — status

> Design: [docs/design/invisible-editor.md](../design/invisible-editor.md) · Guide: [docs/tools/invisible-editor.md](../tools/invisible-editor.md) · Agent: `.claude/agents/invisible-components.md` (component-focused; not a registered spawnable agent)
>
> Companion tool: [Component Editor status](./component-editor.md) — this editor **places** component instances; that tool **authors** the `ComponentDef`s.

**One-line state:** Built — the full 10-step build plan + Templates/Import (§7) are on `main`, registered (`editor` tool) and documented; ⏳ **many editor render paths are code-only / not browser-verified**, and new/reordered non-`background` screens still don't drive the shipped game generically for the reference games.

## Current state

Launcher-native, full-page WebGL/canvas layout editor at `/editor` (auth + `editor`-tool gated; SSR off, `load` runs server-side). Project-centric: loads the active project's `scenes.json`, asset library (atlases/spines/sheets/effects), components, and its game-type template. Layout is written to R2 at `editor/<client>/<project>/scenes.json` on a debounced autosave; the engine fetches it at boot (`GET /api/editor/doc`, `EDITOR_DOC_SECRET`-gated) and renders it via `<LayoutScene>`, falling back to the checked-in `editor-scenes.ts` fixture.

Shipped capabilities on `main`:

- **Screens** — game + HUD screen lists (reorder within group, rename, hide, duplicate, delete); per-screen coordinate **space** (`game` / `standard` / `canvas` / `background` cover-fit); per-screen **"Always on top"** tick (`Scene.alwaysOnTop` — opt OUT of list layering and pin to the fixed top band; `TOP` badge on the row); per-screen **"Zoom with anticipation"** tick (`Scene.zoomWithAnticipation`, game-space only — the screen zooms + pans in lockstep with the reel-anticipation camera toward the same reel centre; engine-layout `registerSceneCameraTransform` bridge + `LayoutScene` wrap, off-by-default/identity ⇒ parity; see `docs/design/reel-anticipation.md` phase 7); scaffold from a **game kind** (§21), **import composed reference** (§7.2), add-missing-screens, "Save as new game kind".
- **Canvas** — real-texture rendering (spine previews as a live skeleton; FX render **live particles** as an overlay following pan/zoom), zoom/pan/Fit, multi-select, transform handles (move/scale/rotate), snap-to-node guides, undo/redo, copy/cut/paste/duplicate.
- **Library placement** — Text / Rect / Reel elements, atlas pages + manifest regions, spines (+ `_shared/`, upload), sheet regions, **Invisible FX** effects (place free or attach-to-rig; particle atlas ships automatically), and **Invisible Flipbook** clips (2026-08-25 — a `flipbook` node carrying a `clipId` + placement, with per-placement `fps`/`loop` overrides; the canvas **plays** it in place rather than chipping it, because a clip is atlas frames in order and not a WebGL emitter. The clip's sheet ships automatically. See [flipbook status](flipbook.md)).
- **Per-region drag/thumbnail/overlay** (§B18) — each sheet/atlas-manifest row expands to individually-draggable region thumbnails (`RegionThumb.svelte`); selected node gets a floating item toolbar (`EditorItemOverlay.svelte`) with delete/lock/anchor/z-order.
- **Properties** — transform/tint/**blend**/text/spine anim + slot fill; **background cover** (fit — cover / contain / fit-width / fit-height — plus cover scale and align x/y, all per layoutType, 2026-09-14); node actions (Convert to reel grid / parametric button / Edit as component). Reel-grid **symbol size (× cell)** (`reelGrid.symbolSizeRatios`) is authored here and bakes on the normal scene bake.
- **Device layouts** — per-layoutType overrides — transform (position/scale/size/anchor/rotation/alpha/tint/**blendMode**/visibility), **background cover fit + cover scale** (2026-09-14), **plus per-ratio TEXT STYLE and COMPONENT PARAMS** (2026-08-10): editing a text-style field or an instance param (e.g. a Text Box's `fontSize`) while a non-base bucket is active writes a sparse `overrides[layoutType].style` / `.params`, so e.g. a headline can shrink its real font size in portrait without a stretchy scale. Per-layoutType **Canvas Size** (the MAIN box), seeded from the game type's reference with a "Match game box" warning/fix. The runtime ADOPTS this box (`setAuthoredMainSizesMap`, 2026-07-24 — see `docs/status/engine.md`), so it is WYSIWYG; a drifted box now only moves the coded parts laid out relative to it (the reel board), not the authored nodes.
- **Authorable layout profiles** (2026-08-11) — the bucket set itself is now author-defined, not the hardcoded `desktop/tablet/landscape/portrait`. Game Settings → **Layout** opens a modal (shared `LayoutProfileEditor`) to set each bucket's resolution/aspect, its window-selection rule (ratio + shorter-side bounds), reorder, add/remove, rename, and pick the fallback — e.g. give Desktop and Landscape one box, or add an ultrawide bucket. A **pipeline-wide default** is authored in Admin → Settings (`app_settings.layoutProfileDefault`); a project inherits it unless it overrides (stored sparse on `LayoutDoc.layoutProfile`, source badge shows which). The device bar, preview frame, and every per-bucket enumeration derive from the active profile (`constants-shared/layoutProfile.ts` — `DEFAULT_LAYOUT_PROFILE` reproduces the legacy four exactly, so existing docs are byte-identical). Runtime selection + the STANDARD frame box are profile-driven (`utils-layout` `selectBucket`/`setAuthoredLayoutProfile`); the publish bundle bakes the effective profile onto the doc. Base bucket generalised from `'desktop'` to the profile fallback (`baseLayoutType`). Design: `docs/design/layout-profiles.md` (to write). ⏳ **not browser-verified** (needs the live launcher + a project); offline parity + custom-profile logic proven in `packages/engine-layout/scripts/test-layout-profile-parity.mjs`.
- **Flow owns visibility** (standing rule, 2026-07-03) — the per-screen and per-instance "Shows during" gates were removed; a screen/instance's visibility now comes from Invisible Flow, not a Scene-Editor field.
- **Generic doc-driven scene mounting** (§20.1, PR #67) — an author's new Scene Editor screen ships without hardcoding it in `Game.svelte` (`engine-layout/genericMountScenes.ts` + Flow Phase 4 `activeScreenTakeover`), parity-gated.
- **Templates** — a template-authoring mode (tag nodes with `slotId`, choose game type, save `editor/templates/<gameType>.json`) + slot/asset-issue warnings on save (§7).
- **Pipeline** — layout-doc bake + **editor-art export** (`deploy/editor-art/`, per-sheet namespaced) travel the export→deploy→bake→pull→register chain.

## Open items / next

1. **New / reordered non-`background` screens don't yet drive the shipped reference games.** They mount scenes by hardcoded id in fixed code order, so a brand-new empty/HUD screen or a reorder changes the **editor preview** but not the built game (only `background`-space scenes mount generically today; Flow Phase 4 covers exclusive-screen takeover). Wiring the runtime to render doc scenes generically by doc order is the next engine step.
2. ✅ ~~**FILLED reference layout for `ways`**~~ — DONE 2026-08-20. `cluster` / `scatter` remain engine-skeleton by decision: the owner is not building those templates, and both would need the tumble/cascade mechanic the shared runtime has never had (`tumbleBoard`, `updateTumbleWin`, `updateGlobalMult`, plus `updateGrid`/`boardMultiplierInfo`).
3. **HUD parity gaps** — the corner logo/game-name containers still ignore `scale` in-game (rotation now ships), so scaling those two corner texts in the editor won't ship yet.
4. **Animated / book-event content stays coded** — symbols, win-line draws, count-ups mount via the engine `mount`/`bind` escape hatch; the editor only places their anchor and has no in-editor book-event playback (verify animated behaviour in the live game).

## Blocked (owner / external)

- **`EDITOR_DOC_SECRET`** must be set on the launcher for the live-doc fetch to serve; unset ⇒ endpoint refuses and games fall back to `editor-scenes.ts`.
- **Engine submodule bump required for shipped games** — Book of Borut and other shipped games vendor the engine as a submodule, so editor/engine parity fixes only reach them after the submodule is bumped and the game rebuilt (owner-owned).
- **Live-verify** — interactive feel (undo/redo, copy/paste, multi-select) and several render paths build clean and type-check, but the auth-gated canvas makes automated interaction testing hard; owner confirms live.

## Recent changes

- 2026-09-17 — **`lighten` joins the blend modes — the one Screen was supposed to be.**
  Owner-reported against real art (warm light shafts over blue water): `screen` washed the colour
  out. It does, by construction — `1-(1-b)(1-s)` pushes bright areas toward white, so a warm shaft
  `(240,200,120)` over water `(20,80,160)` lands at `(241,216,205)`, near-white. `lighten` is a
  per-channel `max`, so the same pair lands at `(240,200,160)` and stays warm. Dark areas still
  disappear, which was the part Screen got right.
  - Free to add: the Pixi advanced-blend registration shipped with `overlay` (2026-09-17), and
    `lighten` is in that same set, so this is the type, the list, the label, the three surface
    readers and a docs line.
  - **Verified in-game** the same way `overlay` was — probe node, forced render (the browser pane
    freezes rAF), pipe `_filterHash` reports `['lighten']`, so the filter really resolves rather
    than silently falling back to `normal`. Screen-vs-Lighten screenshots on the same node show
    the washed-out/preserved-colour difference directly.
  - Gates: `pnpm lint`, `check:undefined-names`, both app builds green.
  - **Not a bug report to chase:** `overlay` was working; it is keyed on the BACKDROP (multiply
    where what is behind is dark, screen where it is light), so over a dark game background it
    darkens — the opposite of "dark parts of my art disappear". That is a source-keyed ask, which
    is `screen`/`lighten` territory.

- 2026-09-17 — **`overlay` joins the blend modes, and the registration that makes it real.**
  Added to `engine-layout/blendMode.ts` (type + list + label + all three surface readers), so the
  editor dropdown, the 2D canvas and the WebGL overlays pick it up with no further wiring —
  Canvas2D `globalCompositeOperation` and CSS `mix-blend-mode` both support `overlay` natively.
  - **The game needed one more thing.** `overlay` is one of Pixi's ADVANCED blend modes: a filter
    that reads the backdrop, registered only by `import 'pixi.js/advanced-blend-modes'`. Without
    that import Pixi accepts `blendMode = 'overlay'` on the node and silently renders it as
    `normal` — no warning, no error. `<InitialiseApplication>` now does the import beside the
    existing `pixi.js/ktx2` one.
  - **Proven by A/B, both sides with a forced render** (the browser pane freezes rAF, so a reading
    taken without `renderer.render()` is meaningless — two earlier readings were, and looked like
    evidence): registration OFF ⇒ the pipe's `_filterHash` is `[]` and the node does not blend,
    while still reporting `blendMode: 'overlay'`; registration ON ⇒ `['overlay']`. Each side ran on
    a freshly restarted dev server, because a side-effect import stays registered across an HMR
    reload and made the first A/B look like a no-op.
  - **Correction to the 2026-09-16 entry's rationale:** it said the advanced modes have "no
    `mix-blend-mode` equivalent". That was wrong — CSS and Canvas2D support `overlay`,
    `soft-light`, `hard-light`, `color-dodge`, `color-burn`, `difference`, `exclusion`,
    `saturation`, `color` and `luminosity`. The real constraint was always the Pixi-side
    registration, which is now paid: the remaining ten are a dropdown-list change away, and the
    three-way intersection is 16 modes wide.
  - **Cost:** the import registers all 22 advanced blends (~92 KB of unminified source). Filters
    are constructed on demand, so a game that places none pays the bundle and nothing else.
  - Gates: `pnpm lint`, `check:undefined-names`, `apps/lines` build all green; `svelte-check`
    unchanged against the same-worktree baseline. ⏳ `/editor` still not browser-verified.

- 2026-09-16 — **Photoshop-style blend modes on placed art (sprite / spine / flipbook / FX),
  previewed exactly.** `BaseNode.blendMode` + `NodeOverride.blendMode` (so it is per-layoutType like
  `tint`), resolved by `resolveTransform` into `ResolvedTransform.blendMode` and passed by
  `<LayoutNodeView>` to `<Sprite>` / `<SpineProvider>` / `<Flipbook>` and to the effect's wrapper
  `<Container>` — the wrapper, so an emitter blends as ONE glow (pixi inherits `groupBlendMode`)
  rather than per particle sprite. Four modes (`normal` / `add` / `multiply` / `screen`), defined
  once in `engine-layout/blendMode.ts` with a reader per surface (`pixiBlendMode`,
  `canvasCompositeOp`, `cssBlendMode`).

  - **Four modes, not PixiJS's thirty**, by decision: these are the ones that exist natively on all
    three surfaces (PixiJS, Canvas2D, CSS `mix-blend-mode`), so every mode the dropdown offers
    renders IDENTICALLY in the editor and the game. Pixi's advanced modes are filter-backed backdrop
    reads with no `mix-blend-mode` twin for the WebGL-overlay path, so offering them would have
    shipped a preview that lies.
  - **The preview had to change shape to be honest.** The editor is not one canvas: per scene it
    stacks a 2D canvas (sprites/flipbooks) + a `spine-webgl` overlay + a Pixi text overlay + a Pixi
    FX overlay, and each scene group had its own `z-index`. Both facts break blending —
    `ctx.globalCompositeOperation` on a scene's transparent canvas can only see that scene's art,
    an `add` applied INSIDE a transparent WebGL overlay is a no-op, and a positioned `z-index`
    forms a stacking context that isolates blending to the group. So the canonical case (an
    additive glow on the base-game screen lifting the BACKGROUND screen's art) showed nothing in the
    editor while the game showed the glow.
  - **Fix: blend on the ELEMENT, not inside the surface.** `EditorCanvas` now draws each scene as
    one canvas per **blend RUN** (consecutive top-level nodes sharing a mode), each carrying the
    mode as CSS `mix-blend-mode`; runs rather than one layer per mode so draw order is untouched,
    and a scene that blends nothing yields exactly ONE run — today's single canvas, parity. The
    spine/FX overlays gained `nodeFilter` + `blend` props and mount one EXTRA layer per non-normal
    mode a scene uses (none when nothing blends, so the WebGL context count is unchanged for the
    common scene). `.scene-group` lost its `z-index` (DOM order already orders the groups, and
    positioned `z-index:auto` still paints above the non-positioned base canvas and below the
    rider/HUD layers), and `.wrap` gained `isolation: isolate` to bound blending to the editor
    surface — it already had `overflow:hidden`, so nothing that used to escape now can't.
  - **Nested** nodes (inside a container / component instance) and the shared HUD canvas cannot have
    an element of their own, so they blend inline via `globalCompositeOperation`; `elementCarriesBlend`
    keeps the two paths from applying the mode twice.
  - **Ship chain: nothing to wire.** `bake-editor-doc.mjs` embeds `doc` verbatim, so `blendMode`
    travels with the node (rule 8 does not bite — this adds no asset class).
  - **Verified:** in-game with a temporary probe node on `apps/lines` (mock RGS, port 7788) — the
    live Pixi sprite reported `blendMode: 'add'` / `localBlendMode: 'add'` while every sibling read
    `inherit`, and `add` vs `multiply` rendered visibly differently over the cave backdrop. The
    editor's compositing model was proven in a standalone harness reproducing the exact DOM/CSS
    stack, A/B against the old `z-index` stack: blended layers composite against the scene beneath
    in the new stack and render flat/opaque in the old one. `svelte-check` clean for the changed
    files (the repo's pre-existing errors are untouched); `apps/launcher-api` and `apps/lines` both
    build. ⏳ **the `/editor` page itself is not browser-verified** (auth-gated; needs an owner
    sign-in) — the dropdown, the run splitting on a real doc, and the per-layoutType override are
    code-only so far.
- 2026-09-16 — **A correctly deployed atlas was served as the OLD art in `/editor`, `/symbols` AND
  `/flipbook` — the stale-deploy guard rejected the right page.** Owner flow: generate an atlas in
  Flipbook 🎬 video mode → re-pack it in the Atlas Maker → deploy. The deployed atlas is correct;
  every launcher tool that resolves regions kept drawing the previous art, and **a hard browser
  reload did not fix it** — which is what identifies it as server-side, not a client cache.
  - **Verified against live R2** (`invisible_wall/test6`, read-only probe):

    | object | mtime | pixels |
    | --- | --- | --- |
    | `manifests/atlas_manifest_S_New_Squid_Idle.json` | 09:55:47Z | declares `atlas` 2047x1173 |
    | `deploy/sprites/S_New_Squid_Idle/….webp` | 09:50:20Z | **2047x1173** |
    | `sheets/S_New_Squid_Idle/….png` | 08:37:51Z | 1934x1612 |

    The manifest's own declared page size matches the DEPLOY exactly. The source page is the
    Flipbook video mode's original, written once and never touched by the Atlas Maker's re-pack.
  - **Cause: the 2026-07-14 guard used the wrong evidence.** `editorRegions.ts findDeployedPage`
    rejected a deployed page whenever the manifest was newer than it (09:50 < 09:55), so
    `resolvePageKey` fell through to `atlas.source_image_path` — a page of a DIFFERENT SIZE, so
    fresh rects were cropped at meaningless coordinates. **A manifest mtime is not a proxy for
    "the geometry changed":** a manifest is re-saved by a metadata edit, an auto-seed, or the Atlas
    Maker's own `_refresh_manifest_from_r2` writeback, none of which move a rect. And because the
    fallback it chose was OLDER STILL than the page it rejected, the guard was strictly worse than
    no guard at all.
  - **Fix: reject the deploy only when the fallback is better evidence.** New pure predicate
    `deployedPage.ts isDeployedPageStale(deployedModified, sourceModified, manifestModified)` —
    stale iff the manifest is newer than the deploy **and** the SOURCE page is itself newer than the
    deploy (i.e. an un-shipped re-pack is what wrote it). A source page older than the deploy, or
    missing from R2 (legacy manifests carry a local Windows path in `source_image`), is not better
    evidence, so the deploy stands. The un-shipped-re-pack case the guard was built for — the
    "new region shows up but the image is blank" bug — behaves exactly as before. Cost is one
    `headObject` on the source key, paid ONLY in the suspicious case (the cheap manifest-vs-deploy
    test gates it), so the hot path is unchanged.
  - Pinned by `pnpm -C apps/launcher-api check:deployed-page` (now 16 assertions), **mutation-
    verified**: restoring the mtime-only rule fails exactly the three new cases (the live one, the
    missing-source one, and the same-second one). The institutional reasoning + the R2 numbers live
    in the `isDeployedPageStale` doc comment, because the rule reads like something to "simplify"
    straight back.
  - **Not the same thing as `/flipbook`'s new ↻ Refresh from R2** (same day, [flipbook
    status](flipbook.md)): that button fixes IN-SESSION staleness in a tab left open, and was
    written believing the server was clean. It is a real gap; it was not this report's cause.
  - **Deployed 2026-09-16** — the launcher rolled out on `ed7bf11a` (which carries this commit): Railway reports `Success - app.invisiblewall.org`, and the live build stamp at `/_app/version.json` decodes to 10:38:54Z, minutes after the merge. ⏳ **Still not live-verified** — a re-check of the same sheet is what remains. The deploy proves the guard is running, not that it picks the right page for the owner's actual manifest.

- 2026-09-14 — **A background cover can be fitted on ONE axis, aligned off-centre, and both per screen layout** (owner: *“specify if I fit using X or Y … overwritable per screen layout … I should be able to change the anchor, right now this option is not doing anything”*). Three things in one seam. (a) **`fit` gained `width` / `height`.** `cover`/`contain` pick the driving axis FROM the aspect ratio, so which axis they pin flips as the window ratio crosses the art's — there was no way to say “always span the window's width, crop/gap vertically”. The two new values pin the uniform fit scale to that axis. (b) **The anchor now means something on a cover node.** It was dead — every cover path overwrote it with `0.5` — so the crop was always taken from the centre. The fit is still COMPUTED centred; `coverTransform` then slides the fitted art within the overflow (CSS `object-position` semantics: `0` = left/top edge, `1` = right/bottom) and returns that as the centre to draw at, so there is exactly one alignment formula and the art keeps its own 0.5 pivot everywhere. Parity is free: every editor spawn already writes `anchor {0.5,0.5}`, and an absent anchor defaults to it ⇒ zero slide. (c) **Per-layoutType overrides** — `NodeOverride` gains `fit` + `coverScale` (stretch and alignment already ride the existing per-layout `scale`/`anchor`), and all four canonical readers take the active `layoutType`, resolving that bucket before the base. **That also fixed a latent bug:** `backgroundCoverStretch` read only the BASE `scale`, so a per-ratio stretch override was silently ignored by every cover path. The Properties “Background” section now carries **fit** (4 options) / **cover scale** / **align x** / **align y**, each with the standard overridden-dot + × reset in a non-base ratio. Wired through every cover surface so the preview can't disagree with the game: runtime `LayoutNodeView` (sprite / flipbook / componentInstance covers, and the SPINE cover — sized by `<SpineProvider fit>`, so its alignment comes from the shared `coverAnchorOffset` against the skeleton dims it is sized from), the coded `<Background>` (same helper, per rig key), `EditorCanvas` (both cover paths) and `EditorSpineLayer` (background/coverFit + the `cover`-placement preview art). pixi-svelte's `spineSizeScale` learned the same two axis fits. A per-axis fit on a `preview.art` bind anchor also had to map to the COVER placement in `resolveAnchorPreviewArt` — only `'cover'` did, so `width`/`height` would have demoted the full-bleed background to a centred overlay; that resolver is now layoutType-aware for the same reason (a per-ratio fit override could otherwise flip the GAME to a cover while the editor kept previewing a centred overlay). **The componentInstance cover is threaded inside `coverBoxTransform`, not at its call sites** — #611 gave that path a second entry point (the editor-baked `node.coverBox`, which is the path every re-saved doc takes), so an anchor applied to one `return` would align the same backdrop differently depending on whether the editor had baked it. `tools/bg-scene-spike/coverBox.ts` — the parity guard for exactly that pair of surfaces — gained the anchor + per-axis fit as inputs, because a formula parameter it does not exercise is a parameter it no longer guards.

  **Verified:** the RUNTIME half is browser-verified against a real doc — `fit: 'cover'` fitted the background rig at scale 0.47958 and `fit: 'width'` at 0.41831 (= 1280 ÷ the rig's authored 3059.92, read off the live Pixi tree); anchor `{0,0}` vs `{1,1}` visibly pinned a half-size contain to the top-left then the bottom-right; and a per-bucket override beat a deliberately wrong base. The EDITOR half is ⏳ **not browser-verified** (auth-gated) — owner confirms live. Gates on the rebased tree: eslint, `check:undefined-names`, the flow-vocabulary check, both `lines` and `launcher-api` builds, `test-cover-fit.mjs`, the bg-scene spike, and `packages/engine-layout/scripts/test-cover-axis-anchor.mjs` (33 assertions over the real module: the axis fits, the alignment, the offset helper, override precedence).

  **SHIPPED** (2026-09-14): merged as `6e59e0fe` (#659) and released to the shared `_runtime/lines` bundle — R2 `release.json` reads `status:'released'`, `commit:6e59e0fe`, and the test server re-hydrated it (that file is served from the server's in-memory copy, so seeing the new sha at `games.invisiblewall.org/<key>/release.json` is itself the proof the `POST /refresh` took). The served bundle carries the new formula literally (`"contain"?Math.min(` twice — engine-layout and pixi-svelte — and the minified `align` helper `C/2+(.5-h)*(u-C)`). **Production parity measured, not assumed:** a live lines-runtime game booted on that bundle puts its background rig at exactly the window centre with the exact cover scale at two very different aspect ratios — 1280×720 → `(640, 360)` at `0.479584` (= 720 ÷ 1501.3, height-driven), 1280×2772 → `(640, 1386)` at `1.8464` (= 2772 ÷ 1501.3) — uniform on both axes, so a doc that authors no anchor and no per-axis fit renders bit-for-bit as before. The launcher redeployed at 15:02 UTC, so the new fit / align controls are live in the panel.

- 2026-09-14 — **A flipbook inside a component can be rebound per placement, closing the cue story’s last asymmetry.** The runtime already remapped a flipbook cue through `cueSignalOverrides` (the field is keyed by NODE id, not by kind), but nothing could author it: the rebind rows live inside the "Spine (this placement)" panel, which iterates `instanceSpineNodes`. That list was NOT widened — it also feeds the spine MANIFEST prefetch (`neededSpineKeys`) and the button-state panel, and a clip has neither a spine bundle nor interaction states, so widening it would have prefetched nothing and offered hover/press animations for a frame animation. Instead a sibling `instanceFlipbookCueNodes` derived (flipbook nodes in the def root that CARRY cues — an un-cued one would render an empty heading) backs a new **Flipbook (this placement)** panel holding just the "Driven by signal" rows, each labelled by the clip the cue swaps to exactly as the spine rows are labelled by the animation. The writer needed no change at all: `onSetInstanceCueSignal` is keyed by `(nodeId, origSignal)` and was already kind-agnostic, and nothing prunes `cueSignalOverrides` against a node list, so an existing override cannot be dropped by this. Now two placements of one component — a character used on the base game and again on the free-spin screen — can react to different moments. ⏳ Not browser-verified (auth-gated); `launcher-api` builds clean, `svelte-check` at 66 = the baseline exactly, every cue harness unregressed.

- 2026-09-14 — **"Plays on signal" is on FLIPBOOK nodes too, where a cue picks a CLIP.** Owner-reported with a screenshot: their character had no cue block at all, because it is a `flipbook` node and the block lived only in the SPINE section. A flipbook section now carries its own, a sibling of the spine one in every respect that matters — same gate (`componentSignals.length > 0 || cueSceneMode`), same `.bind-grid.cue-row` markup, same free-text signal in `/editor` vs the declared-signal dropdown in `/components`, same "Declare a signal on this component" fallback. Parallel `addFlipbookCue`/`updateFlipbookCue`/`removeFlipbookCue` helpers rather than generalised ones, so the spine path cannot drift. Two deliberate differences: the payload field is a **clip** picker — reusing the node’s own `clipList`/`clipOf` source, label and `(missing)` affordance, so there is one clip list on the panel, not two — and there is **no "fire signal on complete"** in either mode, because `<Flipbook>` exposes no completion hook. The cue’s **loop** is a tri-state select (`clip default (loop|once)` / `loop` / `play once`), NOT a checkbox, mirroring the node’s own loop control: an unset cue loop means "inherit the target clip’s answer", which an unchecked box would draw as the phantom default "off" — ticking then un-ticking could never return to inherit, and "play once" over a clip authored looping would be unauthorable (the same phantom-default trap the Flow inspector’s bool editor already fixed). The mechanism + parity argument is in [engine status](engine.md). ⏳ Not browser-verified (auth-gated); `launcher-api` builds clean and `svelte-check` matches the baseline exactly (66). The per-instance rebind half followed on 2026-09-14 (below).

- 2026-09-09 — **"Plays on signal" now renders in the Scene Editor, where the signal name is FREE TEXT.** The spine cue block existed but was gated on `componentSignals.length > 0`, and `/editor` never passes that prop (it defaults to `[]`; only `/components/+page.svelte` passes it) — so the entire cue authoring surface was dead code here, and giving a placed character a reaction meant first promoting it to a reusable component. The gate widens to `componentSignals.length > 0 || cueSceneMode`, keyed off the EXISTING `componentMode` prop (the file's established discriminator, used 15+ times), so the split is by editor, not by a new flag. The two modes differ on purpose: in the **Component Editor** the component DECLARES its signals, so `signal` stays a closed `<select>` over `componentSignals` and a cue may also set "fire signal on complete" — `/components` is byte-identical. In the **Scene Editor** there is no declaring component, so `signal` is a free-text input (`e.g. characterSpin`) matched at runtime on the open component-signal bus, and "fire signal on complete" is HIDDEN because there is no per-instance fired-signal bus out there to fire it on. `addCue`/`updateCue`/`removeCue` needed no signature change (`addCue`'s seed `componentSignals[0]?.key ?? ''` already degrades to `''`), and the `{:else if componentMode}` "declare a signal first" branch still reaches exactly its old case. The name typed here is the same name a Flow **Fire Cue** node broadcasts — the launcher harvests it into that project's cue vocabulary ([flow status](flow.md)) — and the `loop` tick on a cue now actually holds the animation, which it did not before ([engine status](engine.md) has the mechanism + the parity argument). ⏳ **Not browser-verified** (auth-gated); `launcher-api` builds clean and no new typecheck error is attributable to the block.

- 2026-09-08 — **Symbol overflow now reaches the ANIMATED states, and a latch that made spins worse than before the feature is gone.** Same-day follow-up to the knob below, from the owner's report that the padding held for the resting board but not for `intro` / `explosion` / the spin. On the authoring side nothing changes — the two fields are the same two fields — but what they now cover is: a symbol's **Land / Win / Explosion / Intro** art spills too, on the reel board and on the swap-in-place overlay, because all four animate with the symbol on its seat. Only genuine TRAVEL stays clipped (the roll, and a cascade's fall and drain), which is the only case the reel window is load-bearing for. The tool guide's "only once every reel has stopped" line was accordingly wrong and is rewritten. Engine detail — the latched mask `y`, the pre-spin hole in the gate, and the overlay's own transit counter — in [engine status](engine.md).

- 2026-09-08 — **A symbol drawn bigger than its cell no longer has to be cut off at the board edge.** Reported with a screenshot: a squid clipped flat across the top row, mermaids clipped across the bottom. The board is masked to its reel window and always has been — that mask is what hides a rolling strip — so any art that overhangs its cell is trimmed at the window, and there was no knob for it. The `reelGrid` node gains **overflow X / overflow Y** (px, blank = 0), edited beside board nudge; the mask grows outward by that much. Two decisions carry the feature. It grows the **clip only** — no cell, no seat and no board box moves, so a board gains room without gaining size; and it is spent **only while every reel is `stopped`** (`boardOverflow` in `gameState.svelte.ts`), which is what makes "only the pictures that landed" literal rather than approximate. That gate is not a nicety: `SymbolWrap` culls a symbol when its CENTRE leaves the window, so a cell entering from above is drawn while it is still half outside and the mask is precisely what hides that half — widen it mid-spin and half a symbol blinks into view above the board on every cell, every reel, for the length of the spin. It is all reels rather than each reel for itself because a uniform board is masked by ONE rectangle over all of them, so growing it for a landed reel 0 would uncover a still-rolling reel 4; a staggered or anticipated stop therefore opens up at the settle. The cascade overlay (`TumbleBoard`) mounts the same `BoardMask` **without** the flag — its symbols cross the same edge on the way in, so it has a spin's problem, not a settled board's. A STEPPED board's compound ring grows the same way at each column's own top/bottom and at the board's two outer sides, but **not** at the boundaries between columns: that exact tiling is what keeps a tall neighbour from covering the notch beside a short column. The canvas previews it — symbol art clips to the grown window and the extra room is drawn as a dashed outline — and `reelGridGeometry` reports the pair so the 2D canvas and the WebGL spine layer keep reading one geometry. No storage change was needed (`normalizeNode` is pass-through). Parity is the whole risk here, since `apps/lines` is the shared `_runtime/lines` bundle: `scripts/verify-symbol-overflow.mjs` (3.7k assertions) pins that an un-authored or unusable value (0, negative, NaN, Infinity, a string, absent) is byte-identical at every layer including the stepped ring, that the gate holds for `spinning` and `bouncing` on every single reel, and that the editor's preview grows by the same authored px the mask does — asserted against the shipped expressions, with `BoardMask`'s four rect props lifted out of the markup and evaluated rather than transcribed. ⏳ **Not browser-verified** (needs the live launcher + a project doc); builds clean and every reel-grid fixture still passes. Guide: [the tool guide](../tools/invisible-editor.md#symbol-overflow--room-for-art-that-spills-past-the-reel).
- 2026-09-08 — **A background screen now frames the same overlay in the game as in the editor.** Owner-reported: a `background (cover-fit)` Big Win screen shipped with its background cropped differently and its overlays out of place. Three separate causes, told apart. (1) **The real bug — the two surfaces measured a `componentInstance`'s cover box from different inputs.** An instance on a background screen cover-fits the window as ONE unit, sized by the union of its def's content; the editor (`componentInstanceContentBox`) measures art it has already rendered — spine setup-pose bounds from the WebGL overlay, rendered glyph boxes, clip frame rects, a nested instance's own union — and never skips a child, while the runtime (`componentDesignSize`) is fed an `intrinsic` that sizes only a SPRITE and skips every spine / text / flipbook / nested instance. A spine-and-text overlay therefore measured far smaller in the game, and one with no sprite child measured `null`, at which point `bgComponent` gave up and the instance rendered at its **raw authored x/y with no cover at all**. Fixed by baking the number rather than re-deriving it: new `BaseNode.coverBox` (the union's `minX`/`minY`/`width`/`height` in the instance's local space), written by an `EditorCanvas` effect from the box the editor actually previewed, read first by `LayoutNodeView`'s `bgComponent`. It lives on the **instance**, not the `ComponentDef`, because params change what a component draws (text length, the chosen spine) so two instances of one def legitimately have two different unions. `normalizeNode` is PASS-THROUGH, so it persists with no storage change — unlike a `ComponentDef` field, which `normalizeComponent`'s whitelist would have silently dropped. BASE bucket only (a per-ratio child override makes the union bucket-dependent and the runtime reads one box), written silently so a bucket switch does not look like an edit, and the `160x100` "could not measure" fallback is refused rather than baked. Absent `coverBox` ⇒ the old walk, byte-identical (parity). (2) **A second, latent divergence in the same space:** a non-cover node (text / rect / bare container) on a background screen fell through to the editor's GAME-space mapping (`mainToWorld` + `mainScale`) while the game draws it at raw window pixels — `LayoutScene` wraps only `game`/`standard` in a `<MainContainer>`. The editor now uses the shared `anchoredPosition` there, the same fix `canvas` space already got in that function. (3) **Not a bug, and worth stating so it stops being reported as one:** cover targets the LIVE WINDOW in the game (`canvasSizes()` = `innerWidth`/`innerHeight`) and the layout BUCKET in the editor, so a window whose aspect differs from the bucket's crops differently by design — the author's lever is a bucket that matches the real window. Covered offline by `tools/bg-scene-spike/coverBox.ts` (`pnpm --filter bg-scene-spike run coverbox`, 5/5), which pins cause (1) as an assertion so a future attempt to teach `componentDesignSize` about spines fails loudly instead of leaving two mechanisms that disagree. ⏳ Not browser-verified — needs the launcher deploy (to re-bake by saving the screen) and a runtime publish.
- 2026-09-03 — **The spine preview loader uses ONE runtime, loaded once.** `loadSpineRuntime()` no longer takes a line: it always loads the vendored 4.2 runtime (the line `spine-pixi-v8` runs in the game) and `getActiveRuntime()` returns the object captured at load instead of reading `window.spine` live. The old per-line loading enforced "first line wins" only after a script had FINISHED loading, so two lines requested before either arrived both injected and the last to finish owned the global — skeletons built by one runtime were posed with the other's `Physics` token (`physics is undefined`) and drawn by the other's `SceneRenderer`. Seen as blank spine cells on a cold /symbols load. The two 4.1 built-ins (anticipation, reelhouse) parse and pose under 4.2 — guarded by `scripts/check-builtin-spines.mjs`. The `static/spine/vendor/spine-webgl-4.1.js` file stays for the R2-hosted Spine Viewer.
- 2026-09-02 — **Reel-cell spine previews fit the rig's box where its header puts it.** `measureSpineBounds` returns the authored `skeleton.x/y` corner (via `authoredSpineBox`) instead of assuming an origin-centred canvas, matching the game's new `<SpineProvider centreBox>`. Only Rigger rigs with an off-centre Bounds frame move; placed spine NODES are untouched (origin-at-position). See [rigger status](rigger.md).
- 2026-09-02 — **The instance-param panel shows the value the game RUNS, including a def's `defaultInstanceParams` seed.** Found live on `test6` right after #557 stopped the engine force-mounting background-space screens: the splash now booted to BLACK. Its `loadingBar` instance carried only `tapToContinue: true`, yet `resolveComponentParams` applies `LOADING_BAR_DEF.defaultInstanceParams` (`completeOnLoaded: true`) at RUNTIME — so the moment assets finished loading the bar completed the held `showContainer{awaitComplete}`, the flow's `hideContainer` ran, and the tap surface vanished before anyone could tap (the always-on engine copy had been masking this). Meanwhile `EditorProperties.svelte` read the RAW `node.params` for the boolean checkbox + the `<details open>` of "Tap to continue" / "On loaded", so the panel drew "On loaded" UNCHECKED while the game ran it — the panel stating the opposite of the game, the very trap the checkbox comment warned about. Fix: `instanceParamEffective(node, param)` = explicit value ◁ `instanceComponent.defaultInstanceParams[key]` ◁ `param.default`, used by the boolean field and both shared-toggle groups; the "On loaded" copy now says the Loading Bar has it ON by default and to untick it for a tap-gated splash. No runtime change — the author simply sees the truth and can clear it (an explicit `false` wins over the seed). Same day, the seed itself was dropped (see [flow status](flow.md)): the Loading Bar now seeds only Tap to continue, so no owner action was needed on test6.
- 2026-09-01 — **A HUD Readout's BACKGROUND is per-instance art now, not one def-wide coloured rectangle** (owner: "I do not have a possibility to expose the image that is used as background, so I can change it per instance"). Why it was missing: the readout's `Background` is a **coded part** (`bind: HudTicker`), and a `bind` node gets `bind.props` fields — never the sprite panel's **Bind to param** section, which only renders for `kind:'sprite'` nodes. So the only background knobs were `texture`/`tint`/outline/radius on the **def** (shared by every instance of it), and `texture` feeds `UiSprite`, which in this engine is a rounded `Rectangle` that IGNORES it — i.e. there was no way to put an image behind a readout at all, per-instance or otherwise. Fixed the way the Free-Spin Counter's `frameImage` did it: `HUD_READOUT_DEF` gains a collapsible **Background** group of per-instance params — **image** (`kind:'image'` ⇒ the scoped region picker), **tint**, **width**, **height** — and `HudTicker` reads them off the SAME param context the Caption/Value parts read their style from, drawing a real `<Sprite>` (scoped `<assetKey>::<region>` key + bare-name `fallbackKey`, resolved as `LayoutNodeView` does) in place of the tile when one is picked. Unset ⇒ the coded tile, byte-identical (parity) — so balance / win / bet can each carry their own art without forking the def. The Scene Editor previews it through a new **data-driven** catalog declaration (`TileImageBinding` on `BOUND_COMPONENT_DEFAULTS.HudTicker`) that NAMES the param keys the canvas reads, so `EditorCanvas` hardcodes no component id; the stand-in chip now honours the size + tint too. Fixed alongside: that chip stood in at a **made-up 240px** while the game paints ≈603 — the real tile box is now the exported `HUD_TILE_WIDTH`/`HUD_TILE_HEIGHT`, shared by the def preview, the catalog and the param fallbacks. Ship chain is the generic one: an `image`-kind param value is already collected by `editorArtExport`, and `loadComponent` runs `mergeBuiltinCodedParams`, so a FROZEN saved `hudReadout` both gains the controls and gets its atlas exported. `engine-layout` / `lines` / `launcher-api` build clean; an offline fixture over the real modules checks the def↔catalog key agreement, the frozen-def merge, instance-param resolution and the scoped-ref split. Known gap: a width/height override moves the DRAW but not the instance's selection frame (`nodeBox`/`naturalSize` are not threaded instance params). ⏳ Not browser-verified — needs the launcher deploy for the picker, and a **runtime publish** (+ engine-submodule bump for shipped games) for the game to draw it.

- 2026-08-28 — **The Art bounds box measured pointers against the wrong element**, so its drag drifted by the stage's 1px border — the miniature of the fault that put `/flipbook`'s box 562px from its art on the same day. Both editors now nest the thumbnail and the overlay in one exactly-sized wrapper, and the fit maths they shared by copy is now one function (`$lib/boundsFit.ts`) with an offline fixture that fails if the overlay is ever re-parented to the stage. Detail in [flipbook status](flipbook.md).

- 2026-08-28 — **A sprite region can now declare the BOX it is sized by — the Rigger's Bounds, for art.** Selecting a `sprite` node shows an **Art bounds** section: a region preview with the shared `BoundsBox` drag frame, plus Fit / Centre / Clear and exact x/y/w/h. Everything that draws that region then sizes, anchors and cover-fits by the box rather than by whatever rect the packer produced — including a box SMALLER than the art, which is the point (the art overflows instead of being cropped, so a symbol is sized by the part that reads while a flourish hangs outside the cell; the sprite answer to a spine rig whose canvas covers invisible FX). Three placement decisions carry the weight: it edits the **art**, not the placement (keyed `<assetKey>::<region>`, applies everywhere the region draws, and the panel says so); it is stored in its OWN doc (`editor/art-bounds.json`) rather than in the sheet manifest, because a packer rewrites manifests and because altering a region's trim there re-bases the coordinate space every frozen `.irig` mesh was authored against; and it ships as **trim, not as a new asset class** — `editorArtExport` folds each box into the `sourceSize`/`spriteSourceSize` of the TexturePacker JSON it already writes, which is exactly where PIXI reads a declared box from, so the game gains no code and rule 8 has nothing to strand. The canvas repaints on a box edit through a single `artBoundsVersion()` counter (the boxes themselves stay in a NON-reactive map, for the reason `clipsById` does: `draw()` reads it and several effects call `draw()` synchronously). Placed flipbook nodes also gained `direction` / `mirror X` / `mirror Y` overrides beside fps/loop, and the canvas previews the placement's own values. Full story in [flipbook status](flipbook.md); the box arithmetic has exactly one implementation (`applyClipBounds`) shared by the runtime, both editors and the export.

- 2026-08-27 — **A symbol bound to an Invisible Flipbook clip drew an amber marker on the board, in every project.** The reel preview only ever knew two symbol kinds: a `spine` static goes to the WebGL layer, and everything else was assumed to be a sprite whose `assetKey` IS the frame name. A `flipbook` cell is neither — its `assetKey` holds the clip's PRIMARY SHEET (a manifest key), and only `clipId` names the frames — so `findRegion(assetKey, assetKey)` missed and every such cell fell to the placeholder. Nothing was wrong with the binding, the clip or the ship chain: the game played it correctly the whole time, and a Borut-style project (sprite + spine symbols only) never showed a gap, so the hole only opened once a project actually authored flipbook symbols. `symbolStatics` now carries `clipId`, and `drawReelGrid` resolves a flipbook cell through the SAME `flipbookFrame(clipClockMs)` a placed `flipbook` node uses — so board symbols animate in step with every other clip on screen. The playback clock's gate had the matching hole (it looked only for a placed `flipbook` NODE), so a board-only project would have frozen on frame 0; it now admits a `reelGrid` with clip-bound symbols, mirroring how `sceneHasSpine` admits one with spine-bound symbols. Same shape as the 2026-08-18 spine-cell fix, and the same lesson: a symbol-kind union widened in `symbolsStorage` has to be walked to every surface that switches on it.

- 2026-08-25 — **The Library gained a Flipbooks section, so a clip can finally be placed on a screen.** Full detail in [flipbook status](flipbook.md); the editor-side headline is that the doc normalizer's accepted node-kind set now DERIVES from `engine-layout`'s exported `LAYOUT_NODE_KINDS` instead of a hand-copied list. That list is the quietest landmine in this tool: a kind missing from it is dropped on save — drawn on the canvas, erased by the round-trip, with the launcher's type-check-free `vite build` green the whole way. Guarded offline by `pnpm --filter flipbook-spike run scene-node`.

- 2026-08-21 — **The Spines palette's shared tier is no longer just the boot mark — the engine's whole Spine set (29 bundles) is in `_shared/spines/`.** `engine-loader`, `engine-transition`, `engine-bigwin`, `engine-anticipation`, `engine-reelhouse-glow`, `engine-foreground[-feature]`, `engine-buy-button`, `engine-fs-*`, `engine-global-multiplier`, `engine-cluster-pay`, `engine-tumble-*`, `engine-win-meter-explosion`, plus the symbol spines — so a new project can place real animation before commissioning any. Split ONE BUNDLE PER SKELETON because the palette addresses a bundle by FOLDER plus animation name and the resolver takes the folder's first `skeletons.json` entry; shipping upstream's shared-atlas folders whole would have published skeletons that could never resolve. Canonical listing + the page-duplication caveat now live in [the tool guide](../tools/invisible-editor.md#the-shared-spine-library) (this editor's export dedups identical pages via `PageStore`, so placing several costs one page — the Symbols export does not). Full story in [symbols.md](symbols.md).

- 2026-08-21 — **A spine bound from `_shared/spines/` previewed in the editor and shipped nothing, in silence.** `editorArtExport` resolved bundles through `loadSkeletonIndex`, which reads the shared index ONLY when the project has none of its own — so any project that owns a `spines/skeletons.json` (every project that has used the Rigger) found no entry for a shared bundle, and `exportSpineBundle` returns `null` on a missing entry rather than warning. Classic "shows in the editor ≠ it ships": `listProjectAssets` lists `_shared/spines/` and `resolveBundlePrefix` falls back to it, so authoring worked end to end and only the export dropped out. Now goes through `loadSkeletonIndexWithShared()` (project-first concat, the same tie-break the file resolution applies), which is the reasoning `bootSplashExport` had already hand-rolled for itself. Surfaced while seeding the shared `engine-explosion` bundle — full story in [symbols.md](symbols.md).

- 2026-08-21 — **"Perspective (advanced)" is SHAPE only now; the board's behaviour moved to Game Config → Reel behaviour.** The `swap symbols in place` tick, the `swap style` picker and the `column stagger` box (#407 / #425) are gone from this panel. A `reelGrid` node is authored **per `layoutType`**, so having them here let the schema express a board that rolled in portrait and swapped in landscape, or swept at two speeds depending on the phone. `farScale` and `vanishX` stay — they genuinely are per-ratio decisions. The section now points at where the behaviour lives. Nothing authored the removed fields yet, so there is nothing to migrate; `verify-reelgrid-perspective-roundtrip.mjs` asserts they are gone from `ReelGridPerspective` AND from `resolveReelGridPerspective`, which matters because `normalizeNode` is PASS-THROUGH and would happily carry a stale `swapInPlace` out of an old doc forever. Full story in [game-config.md](game-config.md).

- 2026-08-21 — **The board preview can converge, and it is asserted to agree with the game seat-for-seat** (#415, perspective board mode phase 3; engine phases 0–2 in #407/#410, tiles in #421). `reelGridGeometry()` mirrors the engine's perspective model and is the ONLY place that does — it is the one definition the 2D board preview and the WebGL spine layer both read, which is why a sprite symbol and a spine symbol land on the same seat; a consumer re-deriving the scale ramp in its own drawing code would be a second definition and the two layers would drift. Seats now carry their row's `scale` and box size; the geometry carries the board `outline` and an axis-aligned `clip`. Each CELL stays an axis-aligned rectangle (every cell in a row shares that row's scale, so a row contracts uniformly about the vanishing point) — it is the BOARD that is a trapezoid; the clip is the outline's OUTER bound, because the game's `BoardMask` over-extends by a whole cell and clipping to the trapezoid would crop back-row art the live game shows. A "Perspective (advanced)" properties section (`farScale` / `vanishX` / `swapInPlace`) writes sparsely through `markDirty()` → `canvasRedrawNonce`, so an untouched node serialises exactly as before. **The editor/game agreement is asserted, not assumed:** `scripts/verify-reel-grid-geometry.mjs` derives the affine map from `boardLayout()`'s real pivot and checks 307580 seats through it (worst relative error 8.79e-14; flat parity by `Object.is`). The `cellW/2` vs `cellSize/2` term that LOOKS like an editor/game divergence on non-square cells is not one — it is cancelled exactly by the pivot's `((reels−1)/2)·columnExtraLocal`, and its Y twin by `(rows/2)·(rowPitchLocal − SYMBOL_SIZE)` against the game's `gapY/2` origin. **The `build`-is-not-a-typecheck gotcha bit and was caught:** a first pass dropped `cx`/`cy` from `ReelGridSeat`, `vite build` was fully green, and `svelte-check` reported 7 errors across all three consumers. ⏳ **NOT browser-verified** — a panel edit autosaves 1.2s later over a `scenes.json` that had no backup, so verifying live on a real project was a destructive act; the scene-doc backup (open PR, deliberately held) lands first. ⚠️ While writing this up: `docs/tools/invisible-editor.md`'s "Symbol size on the reel" section still documents the REMOVED `symbolSizeRatios` control — stale, needs a grounded pass. ⚠️ Pre-existing type error in the save path, ships green because the build does not typecheck: `editorStorage.ts:190` assigns `DOC_VERSION` (2) to `LayoutDoc.version`, which is still typed `1`.

- 2026-08-20 — **Game Settings gains the per-project boot splash** — this game's own spine mark, shown after the admin-owned engine mark. `GameSettings.bootLoader` (bundle + animation + background), picked from the project's spine library. NOTE `normalizeGameSettings` is a strict whitelist: the field was silently dropped on save until it was threaded there — any future `GameSettings` field needs the same. Full story in [engine.md](engine.md).

- 2026-08-20 — **`ways` gets a FILLED reference layout — and the "needs real ways art" blocker recorded a day earlier was simply wrong.** `referenceLayouts/ways.ts` returned the bare engine skeleton, so ways scaffolded art-less and never appeared in "Import composed reference". The recorded reason for leaving it that way was missing art; checking rather than assuming showed `apps/ways` and `apps/lines` ship **byte-identical** `reelsFrame` atlases — same eight frames, same source rectangles for `frame_bg.png` / `frame_edge.png` / `Frame_FSCounter.png` — on top of the same 5×3@120 board (`engine-game` `SYMBOL_SIZE = 120`, `REEL_PADDING = 0.53`). There was never any missing art. So ways needs no second generator and no new assets: `defaultLayout` stopped hardcoding `'lines'` (a small `REFERENCE_TYPES` spec drives it), `waysReferenceLayout()` is now `defaultLayout('ways')`, and the kind is registered `filled: true`. The ONE difference the generator makes is dropping the `specialBook` scene — the expanding-symbol reveal is a Book-of mechanic and a ways game has none, the same cut the `/flow-v2` starter seed makes when it omits the `specialBook` container. `apps/lines` carries that scene because it IS the Book-of reference game, even though `standardTemplate` doesn't declare it (slots constrain, they don't forbid). **Parity is the load-bearing check** — `defaultLayout('lines')` backs `apps/lines`' shipped `fallbackEditorScenes`, and it is byte-identical across all six `DefaultLayoutOptions` combinations (buttons / transition / freeSpinOverlays / winInstance and the all-on case). New offline guard `pnpm --filter bg-scene-spike run waysreference` (11 assertions: ways is filled + importable, carries the same art keys, omits `specialBook`, is otherwise byte-identical to the lines doc, lines keeps its scene, and an unknown game type still THROWS rather than silently returning lines); `select` + `boardglow` unregressed; `engine-layout` / `launcher-api` / `lines` build clean. Editor-only — no runtime publish. ⏳ Not browser-verified (no ways project exists yet).
- 2026-08-20 — **`ways` is a first-class authoring kind: a real slot template instead of no template at all.** `TEMPLATES` (`engine-layout/templates/index.ts`) held only `lines` + `bookOf`, so a ways project resolved NO `GameTemplate` — the editor surfaced no named drop targets and the launcher had nothing to seed or validate against. Ways needs the same screen set (same `_runtime/lines` bundle, same coded components, same 5×3 board — `apps/ways` `SYMBOL_SIZE = 120`, `BOARD_DIMENSIONS = 5×3`); its whole difference from lines is how it PAYS, which the Game Config's `winModel` declares and a screen/slot schema does not describe. So rather than copy 136 lines that could only drift, the scene set moved to `standardTemplate(gameType)` (`templates/standard.ts`, a FRESH object per call so one type's template can't be mutated through another's) and `lines`/`ways` are one line each on top of it; `bookOf` keeps its own literal because its SCREENS genuinely differ (it adds `freegame`). Verified against the real modules via tsx: `lines` and `bookOf` serialize byte-identically to their pre-refactor values, and `ways` equals `lines` apart from `gameType` (8 scenes, board 5×3@120). Editor-only — no runtime publish.
- 2026-08-18 — **The board's SPINE symbols now draw on the canvas instead of reading "spine"** (owner: "on the reel I still can't see the selected spine — only the placeholder box with the text"). `drawReelGrid` drew each cell's Static binding on the **2D** canvas, which can blit a sprite frame but not pose a skeleton — so every spine-bound symbol fell back to the amber marker labelled `spine`, while the WebGL layer that already draws every other spine in the scene simply had no idea the board existed. Fix, in three parts. (1) **One geometry resolver**: `reelGridGeometry` (`editorCanvas.helpers.ts`) now owns the cell boxes + symbol seats (count from the Game Config, `boardNudge`, the reel/row LEAD and per-cell ALIGNMENT); the 2D canvas and `EditorSpineLayer` both read it, so a sprite symbol and a spine symbol land on the identical seat by construction. (2) **The spine layer collects the board**: one render target per spine-bound cell — for a top-level board and one nested in a container/instance alike, via the same `composeWorldMatrix` ancestor chain the nested-spine path uses — CONTAIN-fit into the seat and auto-looping the cell's `animationName` (what the Static state shows on an idle board in-game). A CODED default cell resolves through its `previewKey`, the same key the Symbols grid loads, so shared-atlas symbol bundles resolve. Full-cell contain is the right size because the game's `cell × SYMBOL_SPINE_FILL` (0.5) box and `SYMBOL_SPINE_LOAD_SCALE` (2) are a tuned pair that nets out to one cell. (3) The 2D canvas keeps the marker only until the bundle reports ready (`readySpineKeys`), exactly like a spine NODE's placeholder, and `sceneHasSpine` now mounts the layer for a scene whose only spine work is the board. Also de-duplicated: the natural-bounds measure the Symbols grid used moved to `editorSpine.client.ts` as `measureSpineBounds` (measured once per bundle, cached on the entry) — and it brings the `.set()`-implementing probe objects with it, so a rig with no authored skeleton size (a Rigger `.irig`) now measures instead of silently falling back to a 100×100 box. `SpineSkeletonData` gained its real `width`/`height` fields, retiring a cast. **VERIFIED LIVE** (app.invisiblewall.org/editor, Book of Borut Remake, read-only): the board that showed six `spine` markers now renders the character rigs + the scatter, and five symbol bundles load (`R_Character_Bull`, `R_TEST_Cowgirl`, `R_Character_Cowboy`, `R_Scatter`, `R_Board`).
- 2026-08-17 — **Localized text ran past its frame: auto-fit now works on a WIDTH-only box, and the Info Bar exposes the text-box knobs** (owner: a translated line spilling out of the message plaque). Two gaps. (1) `autoFitFontSize` / `<TextBox>` bailed out unless the box had a `height` — so a one-line banner, where height is meaningless, could never shrink; both now treat a missing box height as unbounded and fit on WIDTH alone (height still constrains when present — no regression). (2) The overflowing text was the **Info Bar**, not a Text Box: `INFO_BAR_DEF`'s message node had no box bindings at all, so there was nothing to switch on. The §text-box params + bindings are now declared ONCE (`TEXT_BOX_LAYOUT_BINDINGS` / `TEXT_BOX_LAYOUT_PARAMS` in `builtinComponents.ts`) and shared by Text Box and Info Bar, which also gains `padding`. `mergeBuiltinCodedParams` retrofits both onto already-saved defs, so an existing project's frozen `infoBar` picks them up with no re-author and no version bump (proven offline against a real published bundle). `isTextBoxInstance` (canvas resize handles → box params) now also requires the def to be TEXT-ONLY, so a component that draws art too — the Info Bar's plaque sprite — keeps its normal scale handles instead of snapping the artwork to scale 1. **To use:** select the Info Bar (or any Text Box), set **box width** to the frame's inner width and tick **auto-fit font to box**. `engine-layout` + `launcher-api` build clean; behaviour verified in an offline Node fixture. **SHIPPED** (2026-08-17): merged as `4a17ed6a` (#329), runtime released to the shared `_runtime/lines` bundle (R2 `release.json` → `status:'released'`, `commit:4a17ed6a`; `games.invisiblewall.org/bookofborutremake/` serves `bundle.qzXYtiyS.js`), and the Book-of-Borut-Remake engine submodule pin advanced from `b4edf30` (99 commits behind) to `4a17ed6a`. ⏳ NOT browser-verified (auth-gated editor) — owner confirms live. **Note the knobs are opt-in:** shipping this changes nothing on screen until an author sets the Info Bar's **box width** and ticks **auto-fit**.
- 2026-08-11 — **On-canvas toolbar fixes: controls no longer buried by canvas content, and the device tabs can never overlap the action buttons.** Two bugs surfaced by the shell re-layout. (1) `.canvas-actions` (undo/redo · Reload art · FX · Bounds) had NO `z-index`, so the canvas art layers (scene groups + HUD reach `z-index:1001` in the same stacking context — `.wrap` is `position:relative` with `z-index:auto`, so it creates no isolating context) painted over it and the buttons vanished behind the art. (2) The device bar (`CanvasModeBar`, floated top-CENTRE, `z-index:2000`) and the top-RIGHT actions were independently-positioned floats that collided on a narrow canvas / with large text. Fix: unified both into ONE flex row `.canvas-top` (`position:absolute; top/left/right; z-index:2000; flex-wrap:wrap`) owned by `EditorCanvas` — device bar at the left, actions right-aligned (`margin-left:auto`); when the row is too tight the actions WRAP to a second line instead of overlapping. `CanvasModeBar` gained an `inline` prop (drops its absolute float so the parent row lays it out) and is passed into `EditorCanvas` via a new `modeBar?: Snippet` prop; the standalone `<CanvasModeBar>` sibling in `+page.svelte` is retired. Visual change: the device bar moves from top-centre to the top-left end of the shared row (the price of the no-overlap guarantee). `.hint` (bottom-left) also got `z-index:2000` so it isn't buried either. `launcher-api` builds clean; Prettier clean. ⏳ NOT browser-verified (auth-gated editor) — owner confirms live.
- 2026-08-11 — **Editor shell re-layout (cosmetic, owner-requested): left = Screens-only tree, right = tabbed, roomier top bar.** No behaviour/data change — pure UI reorg of `editor/+page.svelte`. (1) **Left column is now just the Screens list**; the old left tab bar (Library / Outline / Template / Components) is gone. The **active screen expands inline to its outline** (node tree + template slots) tree-view style — the `EditorOutline` now renders per-active-row inside the Screens list (new `.screen-outline` li + a scrollable `.left-body`), replacing the separate Outline tab. (2) **Right (Properties) column is now tabbed** `Properties | Library [| Template]`: Properties = the old right-panel content (Screen space, node Properties, Canvas Size, Game Settings); **Library** = Elements + the atlas/spine/sheet library + **Components as its own `PanelSection`** (folded in `EditorComponentPanel`, so the Components tab is retired from the left); **Template** appears only in Template-editor mode (owner picked "3rd right tab"). (3) **Top bar de-crammed** like the Component Editor — `ToolTopBar` `meta()` now carries only the scene/asset counters; the save pill + buttons, slot/asset warnings, the Template-editor toggle (+ game-type select / Save template), and component status moved into a dedicated wrap-friendly `.editor-bar` action row below the chrome. Persisted UI state key switched `leftTab`→`rightTab` (properties/library only; template is mode-gated, not restored). `launcher-api` builds clean; Prettier clean. ⏳ NOT browser-verified (auth-gated editor) — owner confirms live at app.invisiblewall.org/editor.
- 2026-08-10 — **Per-screen-ratio overrides now cover TEXT STYLE and COMPONENT PARAMS, not just transform** (owner-requested: "an override for text parameters between screen ratios … change text size between the ratios"). The device-layout override system (`NodeOverride` + `node.overrides[layoutType]`, resolved reactively by `resolveTransform`) only carried flat TRANSFORM fields — so a text node could be repositioned/rescaled per ratio but its real `fontSize` (and every other text-style field), and every **component-instance param** (how the owner actually authors text — the shared `textBox` instance's `fontSize`/`boxWidth`/`align`/`fill` are _params_, not transform fields), could not vary by ratio. Added two sparse, additive fields to `NodeOverride`: `style?: Partial<TextStyle>` (raw text nodes) and `params?: Record<string, unknown>` (any componentInstance param — kept GENERIC per the owner's "everything generic" rule, not text-only). Two shared pure helpers hold the merge so editor + runtime agree: `resolveOverrideTextStyle(node, layoutType)` (`resolveTransform.ts`) and `resolveLayoutInstanceParams(node, layoutType)` (`componentParams.ts`). **Runtime:** `<LayoutNodeView>`'s `resolvedStyle` folds the per-ratio style under its bound-param overrides (a param-bound field still wins — vary it via the PARAM override); `<ComponentInstance>` overlays the per-ratio params as REACTIVE getters keyed on the override-union keys, defined before the live-feed getters so a count-up `value`/spin `label` still wins, and reading the current layoutType from `getContextLayout()` so a device ROTATION re-resolves them — while the init-stable structural reads (`source`/`action`/`tapEnabled`/cues, which read `staticParams` directly) are untouched, so structure never varies by ratio. **Editor:** the canvas (`EditorCanvas.expandInstance` + spine-readiness scan) and text overlay (`EditorTextLayer`) resolve instance params through `resolveLayoutInstanceParams` and text style through `resolveOverrideTextStyle`, so the preview at the active device layout matches the game; the Properties panel routes edits to the active ratio's override in override mode (instance params via `onSetInstanceParam` → `overrides[layoutType].params`; raw text style via `textStyleBucket`/`pruneTextStyleOverride`) and DISPLAYS the resolved value (`instanceParamValue` / `styleField`), with an amber "editing the {ratio} ratio" hint in the Params and Text sections. Stroke/dropShadow stay base-only for v1 (nested-object fields, rare per-ratio); the runtime shallow-merges them if authored. **Parity:** every field is sparse + additive — a doc with no per-ratio style/params is byte-identical (the helpers return the base ref/copy, the runtime adds no getters). Verified by an offline esbuild+Node fixture over the real `engine-layout` modules (12/12: merge precedence, undefined-skip, same-ref parity, e2e through `resolveComponentParams`); `engine-layout` (svelte-package `.d.ts`), `launcher-api`, and `lines` all build clean. Ships to the editor on the launcher deploy; the **game** needs a runtime publish (+ engine-submodule bump for shipped games) to render per-ratio style/params — the schema field rides in the doc automatically, the reader code does not.
- 2026-07-31 — **A Text Box COMPONENT INSTANCE's drag now resizes the box, not the scale (the real fix)** (owner: "the bounding box is still resizing text … on or off no difference"). The whole point of the text-box work never reached the owner's actual elements because a Text Box is a `componentInstance` — its resize handles ran `writeScale` (stretching the whole component, pixelating the bitmap font), while the box-resize path only ever applied to RAW text nodes. Now `applyScale` detects a Text Box instance (its def exposes a `boxWidth` param) and its corner-drag writes the `boxWidth`/`boxHeight` PARAMS instead of scaling: the current displayed scale is folded into the box and the instance scale reset to 1, so the box is the single size authority and the font renders crisp at its `fontSize` (`isTextBoxInstance` + `setInstanceBox` in `EditorCanvas`). Also **reverted the align→anchor experiment** — it shifted the text against a selection box that didn't move with it, so it looked broken; alignment is now purely WITHIN the box (horizontal via `style.align` across the wrap width, vertical via placement padding), and the box is placed by the node anchor so the selection frame and render agree. Fixed the editor overlay's `buildBitmap` to pass `wordWrap`/`wordWrapWidth` so a boxed BITMAP font actually aligns in the editor (it silently ignored them before). `launcher-api` builds clean (runtime `lines.js` chunk compiles in it). ⏳ Being verified live via claude-in-chrome before declaring done.
- 2026-07-31 — **Text-box `align`/`verticalAlign` now work WITHOUT a box (map to the anchor)** (owner: "the alignment still does nothing … and is separated from the canvas alignment grid"). The first component-param pass only aligned text INSIDE an explicit box, so setting `align: right` with no box width did nothing — the common case. Now `alignToAnchorX`/`verticalAlignToAnchorY` map the alignment to the text's ANCHOR (left⇒0, center⇒0.5, right⇒1; top/middle/bottom⇒0/0.5/1), the SAME semantic as the canvas anchor grid — so `right` right-aligns the text at its position immediately, box or no box; inside a box the box is placed by that anchor AND the lines align within it, so the two agree. Wired through `LayoutNodeView` (`textAnchor`, used by the plain/boxed/numeric-readout paths) and `EditorTextLayer` (effective anchor in both paths). `align`/`verticalAlign` params lost their `left`/`top` defaults so an existing Text Box with no alignment keeps its authored anchor (parity); labels clarified (box width only needed for wrapping/auto-fit). Also confirmed the earlier "empty font list" was a **deploy transient** — verified live via claude-in-chrome that `/api/editor/fonts` returns the 3 project fonts and the dropdown is fully populated. Builds clean; align→anchor + box math unit-tested (9/9). ⏳ Live-verify the alignment render owed.
- 2026-07-31 — **The Text Box COMPONENT gets the box model too (align + box sizing on live count-up readouts)** (owner follow-up: "I hard refresh but see no difference"). The prior text-box work targeted raw `kind:'text'` nodes, but the owner authors all text through the shared **`textBox` component instance** with a live value `source` — which renders through the numeric `ParamReadoutText` path and exposed NO align/box params, so nothing changed for the elements they actually use. Fix: `TEXT_BOX_DEF` gains per-instance params **align / verticalAlign / boxWidth / boxHeight / autoFit**, bound on the inner text node (`style.align`, `style.verticalAlign`, `width`, `height`, `autoFit`); `LayoutNodeView` resolves those bound box fields and threads them into BOTH the numeric branch (`ParamReadoutText`, now box-aware — renders through `<TextBox>` when boxed, so a count-up value aligns/sizes in a box) and the plain `<TextBox>` branch; the editor overlay (`EditorTextLayer`) resolves the same bound box + align/verticalAlign per target, and `componentInstanceContentBox` reflects the bound box so the instance selection frame matches the boxed render. Crucially, a **saved/pinned** `textBox` snapshot would surface the new params but wire them to nothing (the frozen root has no bindings) — so `mergeBuiltinCodedParams` now ALSO unions each node's `paramBindings` from the coded twin (additive, author wins), not just the params list. Unset `boxWidth` ⇒ auto-size ⇒ byte-identical to a plain readout (parity); existing instances unaffected until an author sets a box width. `launcher-api` + `lines` build clean; an offline fixture over the real modules verifies the merge (saved def gains 5 params + inner box bindings, idempotent) and that instance box params resolve to the inner node (19/19). Editor gets it on the launcher deploy; the **game** needs a runtime publish (+ engine-submodule bump for shipped games) to render boxed readouts. ⏳ Not browser-verified.
- 2026-07-31 — **Text nodes are now a real TEXT BOX** (owner-reported three ways: the selection box never resized with the text; `align` did nothing; and dragging a handle stretched/pixelated the glyphs instead of resizing the font). Root causes: (a) `editorCanvas.helpers.nodeBox` returned a HARDCODED `{160×28}` for every text node — the frame could never track the real text; (b) PIXI `align` only positions lines _within a wrap width_, so a single line with no box has nothing to align to → right-align looked identical to left; (c) the corner handles wrote `node.scale` (like a sprite), stretching the text (and a bitmap font pixelates). **Fix — a box model, shared by runtime + editor via ONE pure helper** (`engine-layout/textBoxLayout.ts`: `hasTextBox` · `textBoxStyleOverrides` · `textBoxPlacement` · `autoFitFontSize`, unit-tested offline). `TextNode` gains `width`/`height` (the box, surfaced through `resolveTransform` so it takes per-layoutType overrides) + `autoFit`; `TextStyle` gains `verticalAlign`. When a `width` is set the glyphs wrap to it and align inside (`align` horizontally, `verticalAlign` vertically), and the resize handles change the BOX — never `scale` — so nothing stretches; `autoFit` shrinks the font until the wrapped block fits the height. Runtime: new `TextBox.svelte` (measures via `onresize`, still renders through `<CatalogText>` so the bitmap-vs-web font decision is unchanged) mounted by `LayoutNodeView` only when a box exists (box-less text = byte-identical parity). Editor: `EditorTextLayer` applies the SAME box layout and reports each text node's MEASURED glyph size back (`onMeasuredChange` → `textMeasured`), so an auto-size node's selection frame hugs the real text; `EditorCanvas.applyScale` writes `width`/`height` for a text node; Properties gains **box width/height** (blank = auto), **vertical align**, and an **auto-fit** toggle. Text default anchor is now top-left (0) everywhere (matches how the overlay + pixi already draw box-less text), so adding a box never jumps the node. Both `launcher-api` + `lines` build clean; box-math verified by an offline fixture (8/8). ⏳ NOT browser-verified on the auth-gated editor — owner to confirm live.
- 2026-07-28 — **A builtin component's CODED default spine now gets real animation/slot/bone dropdowns in Properties** (owner-reported on the Free-spin intro: the `intro spine bundle` / `intro animation` / `idle animation` / `number slot` params rendered as free-text boxes instead of pickers). Root cause: the editor sources spine metadata ONLY from R2 project spines (`data.assets.spines`) or a live canvas render, and resolves it via `EditorProperties.resolveSpineAssetKey` → `spines.find(name)?.key`. A builtin component's default spine (`fsIntroNumber`/`fsOutroNumber`/`fsIntro`/`bigwin`) is a CODED bundle shipped inside the game — no R2 presence, and `boundComponentCatalog.resolveAnchorPreviewArt` can't render it live (its bind preview also looks up `assets.spines`), so `spineMetaFor` returned `undefined` → the `spineAnimation`/`spineSlot`/`spineBone` branch fell through to a free-text `<input>` (design intent was "a _custom_ project rig populates the dropdowns; the coded default degrades to free-text"). **Fix — ship the coded bundles' name lists with the engine, mirroring the built-in REGION + built-in SPINE precedents** (`builtinRegions.ts`, `editorSpine.client.ts`'s `BUILTIN_SPINES`): new **generated** registry `engine-layout/src/lib/builtinSpineMeta.generated.ts` (`BUILTIN_SPINE_META`, 29 spines) produced by `scripts/gen-builtin-spines.mjs` — it scans the reference game's coded bundles (`apps/lines/static/assets/spines/*/index.ts`), maps spine NAME→skeleton JSON (both `spine`-prop single + `spines:{}` multi shapes), and extracts animations/skins/slots/bones. Hand-written companion `builtinSpineMeta.ts` exports `SpineBundleMeta`, `builtinSpineMeta(name)`, `BUILTIN_SPINE_NAMES`. `EditorProperties`: `resolveSpineAssetKey` returns a synthetic `builtin:<name>` key for a coded bundle; `spineMetaFor` resolves that key from the registry; the meta-prefetch `$effect` skips `builtin:` keys (nothing to fetch); the `spine`-kind picker lists the coded bundles as `[coded]`-tagged options so a default is a real, pickable value. A project spine of the same name still WINS (project key resolved first; deduped from the picker). Generic off `builtinSpineMeta()` — no per-component/per-spine id logic, so ANY builtin component's coded spine benefits. Wired into `engine-layout` `build` + a pre-commit `--check` guard (staging a coded bundle or the registry re-verifies freshness); the generated file is `.prettierignore`d so its string-compare `--check` stays stable. **Editor-only** — the runtime already registers these coded spines itself; no runtime release, no deploy-chain change. Verified: generator + `--check` pass; an esbuild+Node fixture over the real `engine-layout` modules resolves all 4 builtin defaults with correct name lists (`fsIntroNumber` → anims `[idle,intro]`, slots incl. `slot_number`); `pnpm --filter launcher-api build` bundles the editor page clean (launcher has no type-check). NOT browser-verified — the auth-gated editor needs the owner's logged-in session to confirm the dropdowns render.
- 2026-07-23 — **A spine bundle referenced by a componentInstance `spine`-kind PARAM now SHIPS to the runtime** (owner-reported: the Win Overlay's `winSpine` pointed at a Rigger `.irig` `R_WinScreen` rendered in the editor but not in the game — the win showed the count-up number but the authored spine never appeared). Root cause in the live-assets export: `collectArtRefs` (`apps/launcher-api/src/lib/server/editorArtExport.ts`) walked spine bundles from placed `spine` NODES only (`node.assetKey` → `refs.spineKeys`); for `componentInstance` nodes it collected `image`-kind params but NEVER `spine`-kind params. So a spine a component references BY PARAM (`winSpine`, the free-spin visuals' `introSpine`/`outroSpine`) was omitted from `editorArt.spines` — the editor drew it (reads R2 manifests directly) but the game had no such bundle registered, so `<SpineProvider key={winSpine}>` loaded nothing. Fix (generic off `ComponentParam.kind === 'spine'`, def defaults + instance overrides — mirrors the existing `image`-param collection): collect the bundle NAMES into a new `refs.spineNames`, reconstruct each into a project-rooted assetKey (`${SUB.spines}/<name>/`) for the SHARED `exportSpineBundle` path (which resolves project→`_shared` and renames a Rigger `.irig` skeleton to `.json`), dedup by the registration key, and register under the plain bundle NAME — the exact value `<SpineProvider key={…}>` looks up (nodes reach the same name via `resolveSpineKeysForGame`; a param already stores the name). A coded/game-bundled default (`bigwin`/`fsIntroNumber`, no R2 bundle) resolves to nothing and is skipped (the game registers it itself) ⇒ parity for any doc with no custom spine param. **Reaches the online game via the launcher deploy alone** — `runtimeBundle.ts` runs `exportEditorArt` FRESH on every `GET /api/editor/runtime` fetch (the `?runtime=1` live path), so the online remake re-exports + ships `R_WinScreen` on its next fetch; no runtime release, no engine change (the game already registers `editorArt.spines`). Verified: `pnpm --filter launcher-api build` bundles clean (launcher has no type-check). NOT fixture- or browser-verified. Known edge: the synthetic assetKey is project-rooted, so a param pointing at a `_shared/spines/` bundle relies on `resolveBundlePrefix`'s shared fallback inside `exportSpineBundle` — untested; a project-scoped rig (the reported case) is the covered path.
- 2026-07-23 — **A componentInstance's spine-animation param dropdowns (`spineAnimation`/`spineSlot`/`spineBone`) now populate for a CUSTOM authored rig** (owner-reported on the new Win Overlay component: pointing `winSpine` at a Rigger `.irig` `R_WinScreen` showed a free-text box, not a dropdown, for the per-tier/shared animation cues — while the SAME `.irig` as a plain spine node DID show a dropdown). Two causes, both fixed: (1) the Scene Editor's editor-PREVIEW for a bound-component spine used the FIXED catalog bundle (`BOUND_COMPONENT_DEFAULTS.WinVisual.preview.bundle = 'bigwin'`), never the authored param, so a custom rig never rendered on the canvas ⇒ no live `SpineMeta` ⇒ dropdown fell back to free-text; (2) `EditorProperties.neededSpineKeys` scanned only component-authoring-mode draft params, so a SELECTED instance's own animation params never triggered the `/api/editor/spine/meta` static prefetch either. Fix: new generic `instancePreviewSpineBundle(def, params)` in `engine-layout/boundComponentCatalog.ts` returns the def's FIRST `kind:'spine'` param value (no per-id logic ⇒ `win`/`freeSpinIntroVisual`/`freeSpinOutroVisual` all benefit); `resolveAnchorPreviewArt` takes an optional `previewSpineBundle` tried FIRST (unset/typo/not-in-project ⇒ catalog default, parity). Both canvas layers thread it: `EditorSpineLayer.svelte` renders the authored rig for a nested spine-preview bind (new `bindSpineTarget`, gated on the enclosing def declaring a spine param ⇒ every other nested bind byte-identical) so it publishes live meta AND the author sees real win art; `EditorCanvas.svelte` mirrors the readiness key. `EditorProperties.neededSpineKeys` now also scans `authorParams` (the static-meta belt-and-suspenders). No server change needed — `.irig` correctly indexes as `format:'json'`; it was just never requested. Editor-only (preview + meta); the game runtime is untouched (no runtime release — ships via launcher auto-deploy). Verified: `engine-layout` + `launcher-api` build clean; an esbuild+Node fixture over the real modules confirms authored `winSpine`→its key, default→`bigwin`, not-in-project→`bigwin` fallback. `svelte-check` unavailable in-workspace ⇒ the 3 `.svelte` edits reviewed by hand (arities + `SpineRenderTarget` shape check out). NOT browser-verified on the auth-gated canvas — owner confirms live.
- 2026-07-20 — **The engine's built-in UI regions are now resolvable everywhere** (owner-reported: the live `bookofborutremake` console logs `4 placed region(s) are in NO shipped atlas and will render blank: Frame_FSCounter.png, progressBar.png, progressBarBackground.png, progressBarFrame.png`; "I think they should be found by the template when we create a new project!"). Investigated the runtime FIRST: those four are the coded defaults of `freeSpinCounter` (`frameImage` + its real sprite node) and `loadingBar`/`loadingIntro` (`imageBackground`/`imageProgress`/`imageFrame`), and **every** game app already ships + registers their sheets (`static/assets/sprites/{reelsFrame,progressBar}` in `game/assets.ts`, `progressBar` with `preload:true`), so the frames ARE in `loadedAssets` under their bare names. Confirmed on the LIVE game, not by inference: `GET /bookofborutremake/assets/sprites/progressBar/progressBar.json` → **200** with all three frames, and the served bundle still carries `progressBar:{type:"sprites",…,preload:!0}` resolving to that exact URL. **So the warning was a FALSE POSITIVE and the art was never missing at runtime** — `editorArtExport`'s dangling-region guard only knows R2 atlases, so it flagged engine-shipped art as dangling on every project, sending authors to re-pack an atlas that was never the problem. What WAS genuinely broken is the **editor**: `EditorCanvas.findRegion` resolves a bare region name only against the PROJECT's atlases, so built-in regions could never resolve and drew a placeholder. **Fix — vendored engine art behind a builtin key, mirroring the built-in SPINE precedent** (`editorSpine.client.ts`'s `BUILTIN_SPINES`, which exists for exactly this "ships as a LOCAL game asset ⇒ not in R2" reason) rather than pushing the art into each project's atlas: pushing it through R2 would fork one engine asset into N project copies, need the whole export→deploy→bake→pull→register chain (rule 8) for art that already ships, and still leave existing projects broken until each re-baked. New `engine-layout/builtinRegions.ts` is the ONE registry (`BUILTIN_SHEETS`, `BUILTIN_REGION`, `isBuiltinRegion`, `builtinSheetIdForRegion`, `builtinSheetKey`); the defs + `boundComponentCatalog` now read their literals FROM it, so a renamed frame can't leave a def pointing at nothing (the drift that produced this report). The launcher vendors a copy of both sheets at `static/builtin/sheets/<id>/`; `fetchRegions` resolves a `builtin:<id>` key from the vendored manifest (authoritative for rects — the registry only routes name→sheet), and the page path is launcher-static so `regionAssetUrl`/`ensureImage` pass it through instead of the R2-gated proxy. The canvas consults built-ins **after** the project scan, so a project atlas packing the same name still overrides the engine default. The export guard now excludes built-in regions. Verified with an esbuild+Node fixture over the real `engine-layout` dist (18/18): registry ≡ the frames the vendored sheets actually pack, all 7 non-empty image defaults + the sprite node + the catalog preview resolve as built-in, `builtin:` key round-trip, and the dangling filter keeping only a genuine miss. `launcher-api` + `lines` build clean; the rebuilt game bundle still emits the same four literals and the same sheet registrations (**parity**). **Existing projects are fixed by the launcher deploy alone** — `missing` is computed server-side at bake/runtime-assemble, so `bookofborutremake` stops logging the warning with no runtime release and no author action. Diagnostic text left as-is deliberately: with the guard corrected it now fires only for genuinely dangling regions, where "re-pack the atlas, or re-pick the frame" IS the right remedy.
- 2026-07-17 — **A `game`-space node carrying a `screenAnchor` rendered off-screen in the game while the editor drew it in place** (owner-reported: "I added 2 FX to the same screen but only the one on the left is showing"; moving it changed nothing, and other FX placed outside the canvas worked fine). `screenAnchor` pins a node to a window edge and is defined ONLY for `canvas`-space scenes (`types.ts` — "Window-edge anchor for `canvas`-space scenes"). The **editor always honoured that** (`editorCanvas.helpers.childLocalTransform` + `EditorCanvas.nodeTransform` fold it in only `if (space === 'canvas')`); **`LayoutNodeView` applied it in EVERY space**. So `bookofborutremake`'s `F_Sideline_Star_left` (`n_sms88r5z`, in the `game`-space "Free-spin counter" screen, `screenAnchor {x:1,y:0}`, `x:1410`) rendered at `canvasWidth + 1410` = **x≈3330 on a 1920 canvas** — 1400px off the right edge — while the editor drew it at 1410. Its mirror twin had no `screenAnchor` and rendered fine, which is what made it look like a duplicate/publish bug. Nudging x could never help: x was only ever an _offset from the far canvas edge_. **Fix:** one shared `anchoredPosition(transform, space, w, h)` in `engine-layout/resolveTransform.ts`, now used by the runtime AND both editor surfaces — the rule previously existed in **three copies**, which is exactly how they drifted. Verified with a fixture over the two REAL star nodes (3330.7 → 1410.7) plus canvas-space PARITY against the real `hudCorners` logo/game-name pins, the `loading` scene and the `basegameOverlays` Transition instance (all `space:'canvas'`, all unchanged). Shipped to `_runtime/lines`; the `"canvas"` gate is confirmed live in the served bundle. ⏳ On-screen confirmation owed (the screen only mounts during free spins).
- 2026-07-17 — **A free-spin counter can finally count DOWN** (owner-reported: "the counter is counting up instead of down — I would like control over that"). The only free-spin readout an author could bind was `freeSpins`, whose feed hardcodes the composed string `` `${freeSpinCounterCurrent} OF ${freeSpinCounterTotal}` `` and whose `current` is set ascending (`updateFreeSpinCounter` does `amount + 1`) — so no amount of Flow wiring could reverse it, and the direction was an engine decision the author had no say in. Added the two HALVES as bindable numbers (`ENGINE_PARAM_CATALOG` + `registerComponentValues`): **`freeSpinsRemaining`** (counts DOWN to 0) and **`freeSpinsCurrent`** (counts UP from 1), so an authored counter picks its own direction and layout instead of inheriting "X OF Y". Both read the new `apps/lines/src/game/freeSpinCounterValues.ts`, which the flow's existing `$engine.freeSpinsRemaining`/`freeSpinsTotal` engine reads now call too — a genuine single home, not a copied expression, so a readout and a flow branch cannot disagree about how many spins are left. **Finite-guarded**, because the `stateUi` fields behind them are not trustworthy numbers: the effects that write them assert `payload.total as number` over an `unknown` flow payload, so a mis-authored pin writes `undefined` (and `undefined + 1` writes `NaN`) straight past a field declared `0` — and `Math.max(NaN, 0)` is `NaN`, i.e. the literal text "NaN" rendered at the player. The authoring cause is now caught by the flow validator's `signal-cross-event`; the `as number` assertions at the effect boundary are deliberately left LOUD rather than coerced (masking them would hide the authoring bug), but a player-facing readout degrades to a sane number. `freeSpins`/`freeSpinsWon` untouched (parity). Needs a runtime publish to render in-game.
- 2026-07-17 — **An unset boolean param drew unchecked while the game read `true`** (found while diagnosing the book-reveal panel: "symbol follows bone rotation" showed OFF though its default is `true`). The instance-params panel read `checked={Boolean(node.params?.[p.key])}` — the RAW value, ignoring the def default — while every other kind in that panel falls back to it (selects render `(default: x)`, texts placeholder it) and the runtime resolves `booleanParam(k) ?? <prop default>`. So for any default-TRUE param the panel stated **the opposite of what the game did**, and an author "turning it on" was a no-op against a value already on. Now unset ⇒ show `p.default === true`; an explicit `false` still reads as unchecked, so ticking/unticking keeps writing explicit values. Verified with a fixture over the real def across `{}` / `{followRotation:false}` / `{followRotation:true}`: the old expression mismatched the runtime on 4 combos (both `followRotation`/`followScale` when unset), the new one matches on all. Only `freeSpinIntroSymbolReveal` ships default-true booleans today, so no other panel changes. Editor-only; no runtime publish.
- 2026-07-17 — **Dropdowns instead of free-text for values the tools already know** (owner-reported: "why do I have to type it? sounds counter productive since everything is already authored"). Three findings behind one complaint, on the `freeSpinIntroSymbolReveal` panel. (a) **`symbolBone` rendered as a text box** — the `spineBone` kind IS a dropdown and its render arm always handled it, but `EditorProperties`'s `neededSpineKeys` prefetch listed only `spineAnimation`/`spineSlot`, so a bone param never fetched manifest meta and silently fell through to the free-text `{:else}` whenever the canvas hadn't published LIVE meta (marker-only preview, or a rig that never settles) — the exact gap the manifest fallback exists to close. Added `spineBone` to the prefetch. (b) **`symbolState` was declared `kind:'string'`** so free text was all the panel could render — for a CLOSED set the Symbols tool next door renders as fixed grid columns. New `symbolState` param kind + a `<select>` of `SYMBOL_STATES` labelled with the same `STATE_LABELS` the grid uses (so a state reads identically in both tools), keeping the spine dropdowns' `(custom)` passthrough so an existing typed value never silently vanishes. (c) **The real one — `componentStorage`'s `PARAM_KINDS` allowlist was missing `spineBone`**, so saving a forked copy of the def STRIPPED `symbolBone` outright (latent author-data loss). Its own comment documents this exact bug happening once before with `spine`/`spineAnimation`/`spineSlot`; nobody extended it when `spineBone` shipped. Fixed structurally rather than by hand: `COMPONENT_PARAM_KINDS` is now a VALUE in `engine-layout/types.ts` that the `kind` union derives from, and the validator imports it — a copy can't drift if there's nothing to copy. A `Record`-keyed compile-time guard was tried FIRST and rejected on evidence: **the launcher has no type-check at all** (`build` is a bare `vite build`, no `svelte-check` dep), proven by deleting a key and watching the build still pass — so a compile-time-only guard would not have caught (a) or (c) either. Same DRY pass collapsed `SYMBOL_STATES`, which existed in THREE hand-synced copies (`apps/lines/game/types.ts`, `server/symbolsStorage.ts`, the browser-side `symbols.client.ts`) — all now re-export `engine-layout/symbolStates.ts`, the one home both the launcher and the games can import. Verified offline with an esbuild+Node fixture over the real modules (the `dist` is unrunnable directly — extensionless imports, `gotcha_symbols_publish_silent_double_fail`): all 11 def params now KEPT where the old allowlist dropped `symbolBone`, every state labelled; `launcher-api` + `lines` build clean. **No runtime publish needed** — nothing at runtime branches on a param KIND (the runtime reads param VALUES, and all three affected kinds stay plain strings); the kinds are editor input hints, so this ships with the launcher deploy alone. ⏳ Live-verify pending: the panel needs an author to open a `freeSpinIntroSymbolReveal` instance against a real rig.
- 2026-07-16 — **"Behind the reels" tick — an overlay screen can finally mount UNDER the board** (owner-reported: "background_left/background_right are overlay screens, placed under the reel in the Scene Editor, but they render on top in the game"). This was the REAL bug behind the layering report (the takeover fix below is a separate, latent one — `bookofborutremake` is v2-driven, so its takeover never mounts). The whole list-ordered band starts at `LAYER_BAND_BASE = 100` and the reel board is engine-owned at the **implicit z 0**, so EVERY list-ordered screen sat above the reels: no list position could express "behind the board", and the `basegame` row was inert for layering. Dragging an overlay above it looked right and did nothing. Fix: sparse `Scene.behindReels?: boolean` + a new `LAYER_BAND_BEHIND = -500` band (doc-index-ordered, so under-reel screens keep their list order). Making room meant re-banding the backdrop: authored `space:'background'` scenes -10 → `LAYER_BAND_BACKGROUND = -1000`, and the coded `<Background>` (which emitted its own `-3..-1` children straight into the root) is now WRAPPED at `LAYER_BAND_BACKGROUND_CODED = -900` — relative order unchanged, so it's parity for every game, but it opens a real gap between the backdrop and the board. Editor: a "Behind the reels" tick in Properties (mutually exclusive with "Always on top" — ticking one clears the other) + an `UNDER` badge on the row; whitelisted in `normalizeScene`. Full map, low→high: authored bg (-1000) → coded bg (-900) → behind-reels (-500+i) → **board (0)** → list band (100+i) → pinned (9000+i) → engine gates (10000). Verified against the live doc: Background 2 / BackgroundRight land at -499/-498 — under the board, in front of the backdrop, list order preserved. ⏳ Needs a runtime publish.
- 2026-07-16 — **Custom kinds + game-type templates are conflict-guarded too** (Phase 1 cont.).
  Both live on GLOBAL `_shared/` keys, so no project lease can ever cover them. A **kind's id is
  derived from the author's chosen NAME**, so two authors picking the same name silently replaced
  each other's kind — now `ifNoneMatch` → 409 → "already exists, overwrite?" (the Font Maker's
  id-collision contract, but enforced by R2 rather than an in-request check that a race defeats,
  cf. [[bug_font_maker_id_collision]]). Templates are re-saved rather than created, so they CAS on
  an ETag; `/api/editor/template` GET now returns `{ template, etag }`, and switching game type
  re-adopts the new object's etag — a save must guard the object it's about to write, not the one
  the page happened to load with.
- 2026-07-16 — **Autosave no longer clobbers a concurrent author** (Phase 1 of
  [multi-user-concurrency](../design/multi-user-concurrency.md)). The editor serialized the WHOLE
  doc and PUT it unconditionally every 1.2 s, so of two people on a project the later writer erased
  _everything_ the other did — not just the conflicting nodes. `saveDoc` stamped `updatedAt` and
  never read it back. Now the loader returns the doc's ETag, the client sends it on every save, and
  a stale one answers **409** instead of overwriting: local work is kept, `dirty` stays true,
  autosave PAUSES (else it would 409 every 1.2 s), and the save pill offers **Reload theirs** /
  **Overwrite with mine** — losing someone's edits is now a decision, not an accident. The three
  flush-before-navigate hops (component editor) refuse to leave when the flush didn't land, instead
  of silently discarding the doc. **VERIFIED live** (owner, 2026-07-16): normal autosave unaffected,
  and a two-tab test on one project raises the conflict banner with the loser's edits intact.
  (Repeated autosaves passing is the stronger signal of the two — each save rotates the ETag, so a
  client that failed to re-adopt it would 409 on its _second_ save, not its first.)
- 2026-07-16 — **A flow-active screen now layers from the Screens list + an "Always on top" tick** (owner-reported: "new authored screens moved in the screen layering still render on top in the game"). Every screen path read its z from the doc `scenes[]` index (`docLayerZIndex` = `LAYER_BAND_BASE + index`) — **except** the flow's ACTIVE-screen takeover, which `Game.svelte` pinned at the hard-coded `LAYER_BAND_TAKEOVER` (9000) constant. So a **persistent** flow-active screen was stuck above everything with no way to re-layer it and no clue why: on `bookofborutremake` the author's "Progress Bar" (`s_q9iw9aqf`) is the v1 flow's `initial: true` screen ⇒ takeover ⇒ z=9000, while its list position said z=103. Diagnosed from the LIVE doc + the live Pixi stage (`__PIXI_APP__.stage` children read `z=105`/`106` = correct `100+index`, `sortableChildren:true` — proving the editor, the save path and the z math were all fine, and only the takeover bypassed them). **Fix:** new sparse `Scene.alwaysOnTop?: boolean` + a single resolver `sceneLayerZIndex(scenes, id)` (`layerOrder.ts`) = ticked ⇒ `LAYER_BAND_TAKEOVER`, else `docLayerZIndex`, else `undefined`; EVERY mount path now uses it (takeover — now `$derived`, not a const — plus `extraScenes`, `authoredHud`, hud/overlays/specialBook, and the flow-v2 container re-stamp), so one screen has ONE z regardless of which path mounts it. Exposed in the editor as an **"Always on top"** checkbox in a screen's Properties (with a live "layer N of M — set by the Screens list" readout) and a **`TOP`** badge on the Screens-list row, so the pin is visible instead of hidden in code. Whitelisted in `normalizeScene` (else silently dropped on save — the known `gotcha_scene_field_whitelist_normalizescene` class). **PARITY:** no stored doc carries the new field, so `LayoutDoc.version` went 1→2 with an idempotent read-time backfill (`backfillAlwaysOnTop`) that pins the screens the engine used to pin implicitly — the `loading` ROLE + the legacy `freeSpinIntro`/`freeSpinOutro`/`bigWin` ids — because **the boot splash is takeover-mounted, not coded** (`Game.svelte`'s `<LoadingScreen>` is gone), so without it the splash drops to its list position and paints under the HUD. Matching is role-OR-id: real docs carry `role:'basegame'` as noise, so an `!scene.role` guard misses `freeSpinOutro`. An author's OWN screens are deliberately not backfilled (a persistent flow-active screen is exactly what was wrongly pinned); on v2 a missing tick is a real choice, so the backfill never re-pins. Pinned screens resolve to `LAYER_BAND_TAKEOVER + index` (not a flat 9000) so they keep their list order; `hudZIndex`'s `Math.min` now pins the whole chrome if either HUD scene is ticked (it used to swallow the tick). Verified against the live `bookofborutremake` doc (Progress Bar 9000→103; splash 9005 + outro 9008 stay above the HUD at 112). Round-blocking engine gates (`LAYER_BAND_TOP`) are unaffected. ⏳ Needs a runtime publish to reach the live game.
- 2026-07-16 — **Free-spin board glow is authorable** (owner-requested). The pink reel-house glow behind the reels was coded-only: a `reelhouse` spine in `BoardFrame.svelte` playing a hard-coded `_start→_idle→_exit` chain, its colour baked into the atlas and its size/position literals (`SPINE_SCALE`, `POSITION_ADJUSTMENT`) — `editor-scenes.ts` explicitly held it out of editor v1 scope. Now there's a **`boardGlow` scene** (reference layout + `linesTemplate`) carrying a `BoardFrame` bind anchor, and a `hasAuthoredBoardGlow`/`boardGlowScene` gate in `backgroundScenes.ts` that **mirrors `hasAuthoredBackground`/`hasAuthoredBookReveal` exactly**: real art in the scene ⇒ the coded spine is suppressed and the authored scene mounts in the BELOW-reel slot (before the board's `<MainContainer>`, where the coded `zIndex:-1` spine drew); anchor-only ⇒ the coded glow renders unchanged (**parity**). Enter/exit ride two new `ENGINE_SIGNAL_CATALOG` signals (`boardGlowShow`/`boardGlowHide`) wired in `registerComponentSignals` off the EXISTING `boardFrameGlow*` emitter events — so an authored glow component's spine cues get the same timing as the coded chain, whether or not a flow owns `freeSpinTrigger`/`freeSpinEnd`. **TIMING needed no engine change at all** — `boardFrameGlowShow`/`boardFrameGlowHide` were already declared flow-v2 cues, so a `fireCue` node has always driven them (an earlier `showBoardGlow`/`hideBoardGlow` action pair was shipped then reverted as duplicate API: `ab937bb`/`c18032a`). Verified `tools/bg-scene-spike/boardGlow.ts` (`pnpm --filter bg-scene-spike run boardglow`, 10/10) — incl. the anchor-only PARITY trap, and a double-mount guard that scrapes the real `RESERVED_SCENE_IDS` out of `Game.svelte` (un-reserved, `extraMountScenes` would mount the scene a second time ON TOP of the reels; mutation-tested to confirm the assertion actually fails). ⏳ **Live-verify pending** — no project has authored a `boardGlow` scene yet; needs art + bake + a runtime publish.
- 2026-07-15 — HUD Readout **per-text align (v2→v3):** per-instance `captionAlign`/`valueAlign` dropdowns (left/center/right) in the Caption/Value groups drive the text anchor AND an x-offset of ±`alignWidth/2` (a shared param defaulting to the coded ticker width ≈603) so a readout text left/right-aligns to its background box with NO manual positioning. Unset align inherits the node's manual transform anchor (back-compat). Wired through `HudCaption`/`HudValue` (`x={offsetX}` + anchor) and mirrored in the editor 2D preview (`readoutAlign()` in `EditorCanvas`). Custom-width backgrounds override `alignWidth`. ([detail in history](../history.md)).
- 2026-07-15 — **Component Editor repaint + anchor-aware readout preview.** (a) The Component Editor (`/components`) reused `EditorCanvas` but passed NO `redrawNonce` and NO `onDirty` to `EditorProperties`, so a transform/anchor/param edit mutated the draft but never repainted ("nothing changes when I change anchors"). Added an `editNonce` bumped on every panel edit (`onDirty` + the inline `onSetInstanceParam`) and fed to the canvas — mirrors the Scene Editor's nonce. (b) `drawHudChip`'s decomposed readout caption/value text ignored the node ANCHOR (hardcoded `textAlign:'center'` at the origin) while the game honours it (`HudCaption`/`HudValue` render `<CatalogText anchor={transform.anchor}>`) — so left/right-aligning a readout text previewed wrong. Now draws left-aligned + shifted by `measuredWidth × anchor.x` / `size × anchor.y`, WYSIWYG for any fractional anchor. ([detail in history](../history.md)).
- 2026-07-15 — HUD Readout **per-text style params (v1→v2)**: the Caption and Value texts can now be styled independently via collapsible `Caption`/`Value` groups (`captionFill`/`captionFontSize`/`captionFontFamily` + `value*`), each inheriting the SHARED `fill`/`fontSize`/`fontFamily` when unset — so older instances are byte-identical. Wired through the coded `HudCaption`/`HudValue` parts and the editor's 2D chip preview. A **saved def is a frozen snapshot** and would hide the new coded params — so `componentStorage` (`listComponents` + `loadComponent`) now runs `mergeBuiltinCodedParams()`: it additively unions the code-defined params of ANY built-in-backed def (never removing/overriding author params, no version bump), so an old saved `hudReadout` in project/shared R2 gains the new controls automatically. ([detail in history](../history.md)).
- 2026-07-15 — Properties-panel edits now repaint the canvas live: `markDirty()` bumps `canvasRedrawNonce` (the redraw effects only track a field whitelist + the nonce, never per-node `params`/`bind.props`/`transform`). Also: removed the redundant `center` v-align/h-align option (identical to the default `centre` at runtime — only `bottom`/`left`/`right` deviate), and implemented horizontal align in the editor `standardToWorld` preview (was vertical-only). ([detail in history](../history.md)).
- 2026-07-14 — Editor regions: mtime guard so a re-packed atlas's NEW region no longer shows as an EMPTY image (manifest newer than deployed page ⇒ fall back to source page) ([detail in history](../history.md)). **Narrowed 2026-09-16** — the manifest-vs-deploy test alone rejected correct pages; see the entry at the top of this section.
- 2026-07-13 — Bone-ridden stand-in symbol preview for the free-spin symbol reveal ([detail in history](../history.md)).
- 2026-07-08 — Invisible FX: LIVE particle preview on the Scene Editor canvas + place effects as an `effect` node ([detail in history](../history.md)).
- 2026-07-03 — Removed the per-screen and per-instance "Shows during" gates entirely (Flow owns visibility, standing rule) ([detail in history](../history.md)).
- 2026-07-03 — Reel grid: split the conflated `reelPadding` into three honest, independent knobs (engine + editor) ([detail in history](../history.md)).
- 2026-07-01 — Background `componentInstance` cover-fit shipped to `_runtime/lines` ([detail in history](../history.md)).
