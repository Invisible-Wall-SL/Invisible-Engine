# Invisible Flow — status

> Design: [invisible-flow-v2.md](../design/invisible-flow-v2.md) (+ [schema](../design/invisible-flow-v2-schema.md), [v1 plan](../design/invisible-flow.md), [flow-driven-game](../design/flow-driven-game.md)) · Guide: [docs/tools/flow.md](../tools/flow.md) · Agent: `.claude/agents/invisible-flow.md`

**One-line state:** Shipped and live — `/flow-v2` is the only editor, the v1 runtime + doc format + bake still power the game, and the flow-driven-game engine story (loading→tap→basegame, win-branch, condition guards, generic takeover mount, HUD replace) is complete on `main`; **`bookofborutremake` IS v2-driven in production** (`flowV2DrivesScreens === true`).

## Current state

- **Editor — `/flow-v2` only.** Unreal-style Blueprint graph (exec + typed data pins) over one game, at `apps/launcher-api/routes/(app)/flow-v2/`, backed by `packages/engine-flow-v2`. Palette projected from the template vocabulary (`BOOK_OF_VOCAB`), pins derived from node refs, connect-time type checking, live Validation panel, deterministic headless Preview (ordered effect/cue/delay/show/hide timeline, 1×/2× speed), Collapse-to-Function (global shared library) and Collapse-to-Group, drag-off-pin contextual spawner. Auto-saves (no Save button) to R2 `editor/flow-v2.json` via `POST /api/flow-v2/save` (flow-gated). **v1 `/flow` editor retired 2026-07-13** (route + `/api/flow/save` + v1-editor-only helpers deleted).
- **v1 runtime + doc format + bake still LIVE (load-bearing).** `engine-flow`, `flowExport.ts`/`exportEditorFlow` + `/api/editor/export-flow`, `flowVocabularies.ts` (the `/fx` tool derives its emitter vocab from it), and the `apps/lines/src/game/flow*` interpreter were deliberately KEPT — v2 storage/export, the FX tool, and the live runtime layer on these shared `flow*` primitives. The runtime dispatches a screen/event through the interpreter when a FlowDoc declares it, else falls through to coded mounting + `bookEventHandlerMap` (byte-identical to `main`).
- **Full ship chain exists.** `/flow-v2` author → R2 → `flowV2Export.ts` (`deploy/flow-v2.json`) → bake → bundle slot → game `bakedFlowV2Doc()`. v1 doc bake path also intact.
- **flow-driven-game engine — Phases 1–4 + 6.1–6.3 + 8 on `main`** (merged 2026-06-30, `bdc849f`; see [design](../design/flow-driven-game.md)):
  - **P1** loading splash is a Flow screen; tap fires its `complete` edge to `basegame`.
  - **P2** big-win / free-spin-intro promoted to exclusive Flow screen nodes (`bigWin`/`freeSpinIntro`) reached by guarded `bookEvent` transitions, returning via tap `complete`; small wins stay feed-driven overlays.
  - **P3** revived the `condition` trigger — bounded `$engine.*` reader + a `Game.svelte` `$effect` re-evaluates condition edges on state change. Every trigger kind (bookEvent / complete-tap / condition) is live.
  - **P4** generic `activeScreenTakeover` — any authored non-`basegame`/non-`loading` active screen mounts its backing scene as a top-layer takeover over the persisting board (retires the §20.1 hardcoded-mount limitation).
  - **P6.1–6.3** universal `action`/`visibleSource` bindings tray, UI-vs-schema controls (`visibleFor`/`screenAnchor`/custom `options`), per-instance cue-signal rebinding.
  - **P8** action→intent pins (Spin) SHIPPED. Container events fire from real button presses; Game Signals source node; intent-command actions for every standard HUD button.
- **HUD full-replace** — `hud_*` screens at the HUD z-band replace coded UI (`hasAuthoredHud` suppresses coded `<UI>`).
- **Text Message node** (2026-08-10) — a `textMessage` node kind (`packages/engine-flow-v2`): a presentation LEAF that draws one localized line of text at a normalized `place` on the game canvas. It carries its own `text` (the editable default AND the localization key), so the string is auto-harvested into `/localization` (a read-only **Flow messages** section, `origin:'flow'`) and travels inside the already-baked `bundle.flowV2` (no new baked class). Visibility is OR-ed from two drivers: a reactive **state-gate** `visibleWhile` (`idle`/`spinning`/`freeSpins`/`always`) — the sustained-state case flow events can't express — and the flow-driven `show`/`hide` exec inlets + an optional `autoHideMs`. A **`placement`** field (added 2026-08-10) picks WHERE it draws: `'infoBar'` (default) routes the text through the game's shared `stateMessage`/`showMessage` slot — the exact single slot win toasts use, so it reads like every other in-game message (needs the project's HUD to carry a message/info-bar component); `'anchor'` draws an independent overlay at the normalized `place`. The seed's "Good luck" uses `infoBar`; "Click spin button to start" uses `anchor`. Anchor messages render through `<CatalogText>` (added 2026-08-10) and default to the game's HUD font (`proxima-nova`); `style.font` picks any project font (the inspector's Font dropdown reuses the Scene Editor's `fetchFontCatalog`), resolved web-vs-bitmap via the boot font catalog. Rendered game-side by `apps/lines/src/components/FlowV2Messages.svelte` (a `<Text>` per visible message inside `<MainContainer>`, gate = `stateXstateDerived.isIdle()`/`isPlaying()`/`stateGame.gameType==='freegame'`). The driven seed ships two editable defaults — "Click spin button to start" (`visibleWhile:'idle'`) and "Good luck" (flow-shown off `reveal` + `autoHideMs:1200`). **Live-verified in `apps/lines` (`?flowV2=lines`):** idle prompt shows at idle + hides on spin; "Good luck" flashes on reveal + auto-hides. Editor: a **Presentation** palette group + inspector (text / visible-while / anchor / auto-hide / style).
- **Generic `showMessage` effect** (2026-07-14) — Flow-authored Info Bar / transient toast producer, wired into the `winInfo` choreography in both v1 and v2 reference docs; fall-through unaffected.
- **Inspector bool literal is now a tri-state select** (2026-08-07) — a `bool` data-in used to render a bare checkbox that drew the phantom default (`false`) as an unchecked box. For an optional pin whose effect default is `true` (`enableAnticipationMode`'s `zoom`/`greyOut`), an untouched pin looked "off" while the game ran it "on" (`boolOr(payload, true)`), so unchecking never stored `false` (no `onchange`). The bool editor now mirrors the enum/number fix: `— default —` placeholder when unset + explicit `on`/`off` options, so picking a value is the only way to display one and it always commits. `NodeInspector.svelte` `dataSourceEditor`.
- **Ships via runtime bundle vs main merge:** the launcher/editor UI ships on Railway from a `main` merge; the flow **engine** reaches a live online game only via a `publish-runtime-bundle.mjs` (`_runtime/lines`) + `/refresh`, and standalone Book of Borut only via an `engine` submodule bump.
- **`bookofborutremake` IS v2-driven in production** (verified live 2026-07-16, off the running game — this **corrects** a long-standing "no shipped game runs an authored FlowDoc yet" claim that was false and load-bearing for scoping decisions). `window.__IE_FLOW_V2__.ownsEvent('load')` ⇒ `true` (so `flowV2DrivesScreens === true`), `.ordered()` ⇒ `[{id:'loading',sceneId:'loading',z:105}]`. It owns via **wired `gameSignals` exec pins** — `load`, `tapToStart`, `winInfo`, `freeSpinEnd`, `setExpandingSymbol` — and has **zero `event` nodes**, so ownership resolves through `flowOwnsSignal`, NOT through `event`-node refs. It does **not** author `freeSpinIntro` (that scene isn't in the doc) and does **not** own `setWin`.
  - **Gotcha when reading a v2 doc:** the graph schema is `{nodes, exec, data}` — there is **no `edges` field**. `graph.edges || []` reads as "the flow is inert" and has produced wrong claims twice. Use `flowOwnsSignal` / `__IE_FLOW_V2__`.
- **⏳ Caveats:** the cutover-track runtime dispatch / book-event ownership are landing incrementally; verify a project's authored flow against the live game (baked data masks dev-only bugs).

## Open items / next

1. **Free-spin outro count-up split ✅ DONE (FS-7 outro, 2026-07-28).** The LOAD-BEARING core of `FreeSpinOutroGate` (count-up `WinCountUpProvider` + `freeSpinOutroState` publish + baked coin fountain) is extracted as a HEADLESS `FreeSpinOutroDriver`. When the `freeSpinOutro` scene is author-rebuilt (`hasAuthoredFreeSpinOutro`) the engine mounts ONLY the driver (no dim/press/sprites) — the authored screen owns dim/tap/hold/count-text/art; else the full `FreeSpinOutroGate` stays (Borut byte-identical). The driver is the sole `freeSpinOutroCountUp` subscriber (`resolveFreeSpinOutroMount` guarantees exactly one across both bands + Borut's composer), self-resolving the hold on count-up completion under v2 (mirrors `WinGate`) or transferring to the screen's Complete pin under v1 `ownsOutro`. New authorable surfaces: `freeSpinOutroTotalWin` value source (live count-up text), `freeSpinOutroBigWin`/`freeSpinOutroSmallWin` signals (big/small art via `hiddenUntilSignal`), `freeSpinOutroCountUpComplete` signal (arm a tap/prompt only after the count-up via `tapArmAfterSignal`/`hiddenUntilSignal`). The driver emits NO coin fountain (owner decision — the #138 baked-fountain plumbing was removed; authors place their own FX/particle/spine) and supports HOLD-to-fast-forward (pointer or Space held ⇒ the count-up accelerates via `WinCountUpProvider`'s opt-in `speedScale`). Verified `flow-spike run fs7outro`. **Still pending for gate retirement:** every shipped game v2-driven for the steps it uses.
2. **Then: retire the coded gates** (`FreeSpinIntroGate`/`FreeSpinOutroGate`/`BookRevealGate`/`FreeSpinIntroFlowGate` + `freeSpinOwnership`). `Scene.gate` is already gone (2026-07-16); these are the remainder. Hard cut — gated on (1) and on every shipped game being v2-driven for the steps it uses.
3. **Authoring-time hold safety.** A `showContainer{awaitComplete}` on a container with **no** `tapToContinue` overlay and no reachable `hideContainer`/`complete:<id>` hangs the round forever — nothing catches it today. Feed a `containerTaps` map into `PinContext` the way `deriveContainerEvents` already feeds `containerEvents`, and add a `hold-without-release` validator error (+ a `tap-without-hold` info). Fires only where the map actually resolved (the `scope.ts` "never guess" rule).
4. ✅ ~~**flow-driven-game Phase 5**~~ — **DONE (owner-confirmed 2026-08-04).** Real `bigWin`/`freeSpinIntro`/`loading` backing scenes were authored, baked, and shipped; a game runs a real FlowDoc end-to-end. The acceptance proof is met.
5. **Phase 9** — value dataflow pins → value edges (the symmetric other half of Phase 8; mirrors its shape).
6. **Phase 6 remainder** — `value`/`signal` universal binding (needs a def node to consume them), `def.slots` / component-`space` UI.
7. **Phase 7** — behaviour/timeline layer + open catalog (the long tail).
8. Author the `showMessage` action node into the live `winInfo` forEach + ship the runtime bundle.
9. Editor gaps: containers + `templateId` are data-only (not UI-editable); cue `await` flag not authorable; function Entry/Result signature fixed once created.

## Blocked (owner / external)

- ✅ ~~**Ship a real game on a FlowDoc**~~ — DONE (owner-confirmed 2026-08-04): a game runs a real
  authored FlowDoc end-to-end. Phase 5 is closed.

## Recent changes

- 2026-08-05 — **Buy-feature card fixes: press payload threaded, duplicate `onSelect` pin deduped, card text wraps.** Three fixes to the flow-shown buy menu (`showContainer(buyFeature)`): **(1) press payload** — `apps/lines/Game.svelte`'s `registerFlowPress` callback took only `(componentId, action)`, DROPPING the third `payload` arg the `<Repeater>` passes (`{ betModeKey: <card key> }`). So even a correctly-routed card press armed the flow's `selectBetMode` with `undefined` — the buy committed no bet mode. Now forwards `payload` into `resolveFlowV2Press` (a button passes none ⇒ byte-identical). **(2) duplicate pin** — a `repeater` projects its canonical fused `<id>.onSelect` (WITH a `betModeKey` data-out), but its item component (`featureCard`) also declares a `select` signal that the component-signal projection could surface as a SECOND, payload-less `<id>.onSelect` — two pins on the Show node, and wiring the payload-less one silently breaks the press. Fixed in two places: `deriveContainerEvents` (`engine-flow-v2/containerEvents.ts`) now de-dupes decls by id, PREFERRING the payload-carrying one (repeater's canonical wins regardless of author order); the `/flow-v2` server projection (`+page.server.ts`) now explicitly suppresses a repeater item's `select` signal (surfacing any OTHER item signals for a richer card). Distinct component signals (confirm/cancel) untouched — parity. **(3) text wrap** — `FEATURE_CARD_DEF` (`engine-layout/builtinComponents.ts`) title + description text nodes gained `wordWrap`/`wordWrapWidth: FEATURE_CARD_WIDTH - 40` (mirrors `CONFIRM_DIALOG_DEF`'s message), so long copy wraps inside the card instead of overflowing. Verified: `flow-spike run v2repeaterselect` (new section 8: repeater+item-`select` fuse to ONE canonical pin; distinct signals survive) + `v2buyflow`/`v2containerevents`/`v2fullscreen`/`v2drivenseed`/`v2buyconfirmvalues`/`v2confirmgate` unregressed; `engine-flow-v2` typecheck + `engine-layout`/`launcher-api`/`apps/lines`/`cluster` builds green; Prettier clean. **Clickability caveat (needs a live look):** the card press path is byte-identical between the imperative `<BuyFeatureScreen>` and the flow `<FlowV2Mount>` mount (same `<LayoutScene>`; the card is an `eventMode:"static"` hit surface with `onSelect` wired; `getFlowPress` is registered + routes when owned) — so a card that clicks imperatively but not under the flow is being SWALLOWED by a full-screen interactive surface mounted ABOVE the buy container (a `tapToContinue`/celebration overlay from another flow-mounted container, or the buy container's scene-order z sitting under an interactive sibling). That is a FlowDoc/z concern (cf. `gotcha_flowv2_celebration_screens_mounted_at_boot_disable_slam`), fixed by authoring, not an engine defect. **Engine change on `main` — reaches an online/remake game only on a runtime-bundle republish + `engine` submodule bump.**
- 2026-08-04 — **Buy-flow-in-Flow Phase 3 Step 6 — reusable `ConfirmGatedAction` (a doc-builder, NOT a new primitive).** The confirm-gate shape (Show(confirm) → `onConfirm ▶ <action> ▶ Hide(confirm)`; `onCancel ▶ Hide(confirm) ▶ Show(prompt)`) is now a reusable unit: `buildConfirmGate({ idPrefix, promptContainerId, confirmContainerId, confirmComponentId, onConfirmedActionRef, pos? })` returns TOP-LEVEL `{ nodes, exec, entry, showConfirmId, onConfirmedId }` a doc author splices into `doc.graph` (`packages/engine-flow-v2/src/builders/confirmGate.ts`, exported from the barrel). **Mechanism decision — builder, NOT a `group` (runtime evidence):** a `group` IS flattened (`flattenGroups`) before the runtime walk, so a grouped `showContainer`'s `onConfirm`/`onCancel` WOULD still dispatch (`runContainerEvent` reads the flattened graph). BUT the game-side ownership predicates — `flowOwnsContainerEvent`/`flowOwnsSignal`/`flowScreenDrivingStatus` — read `doc.graph` RAW (un-flattened), because they are the pure static reads a game calls to SUPPRESS the coded press; a `showContainer` buried in a `group.body` is INVISIBLE to `flowOwnsContainerEvent`, so the coded confirm/cancel press would NOT be suppressed and would DOUBLE-FIRE (breaks ownership/parity). Making the predicate flatten would change ownership semantics (out of scope), and a `group` is INLINE + non-reusable by design (§5.2) anyway. So the gate stays top-level. **Seed refactor (pure):** `drivenSeed.ts`'s `BOOK_OF_DRIVEN_SEED_DOC` buy subgraph now builds its confirm-gate portion via `buildConfirmGate` (pre-gate `event buyBonus ▶ Show(buyFeature) ▶ selectBetMode ▶ Hide(buyFeature)` stays inlined + wires `hideFeature.exec ▶ gate.entry`); behavior IDENTICAL step-for-step (internal node ids changed — nothing references them). **Reuse proof:** new `pnpm --filter flow-spike run v2confirmgate` instantiates the gate TWICE (the buy gate + a synthetic `demo` gate: different containers + `openSettings` onConfirmed) in ONE doc — disjoint ids, each dispatches ONLY its own confirm/cancel, top-level ownership true for BOTH, ghost not owned — the second needed ZERO new engine code, just a second builder call. No new engine primitive; no change to runtime dispatch/ownership semantics; default imperative path + coded apps untouched (only the opt-in seed doc + the builder). Verified: `v2confirmgate`, `v2buyflow`/`v2buyconfirmvalues`/`v2drivenseed` unregressed, `engine-flow-v2` typecheck + `engine-layout`/`apps/lines`/`cluster` (inert) builds green. **Engine change on `main` — reaches an online/remake game only on a runtime-bundle republish + `engine` submodule bump; not needed for this headless step.**
- 2026-08-04 — **Buy-flow-in-Flow Phase 3 Step 5b — the flow-shown confirm dialog now renders the picked mode's copy, not the def-default labels (closes the Step-5 gap).** The `buyConfirm` scene shown by `showContainer` in flow mode fed the `confirmDialog` instance nothing — no `<Repeater>` prop, no `<ConfirmDialog>` mount instance-binding — so its `engineProvided` params (`title`/`message`/`confirmLabel`/`cancelLabel`/`imageKey`) fell through to `CONFIRM_DIALOG_DEF`'s literal node text. New NODE-ID-keyed feed: `registerInstanceValues` / `getInstanceValueSource` / `InstanceValueSource` (`packages/engine-layout/src/lib/registerInstanceValues.ts`, exported from the index) — the MAP sibling of `registerComponentValues` (keyed by source name) / `registerRepeaterSources` (list). `<ComponentInstance>` consults it as the LOWEST-precedence `engineProvided` supplier: `engineValues` PROP (Repeater) `??` instance-binding (imperative `<ConfirmDialog>`) `??` **this node-id feed (flow)** `??` def default — the resolver's new `else if (instanceValueSource)` branch runs ONLY when neither prop nor binding is present, so the imperative path is byte-identical (the source is never consulted while a mount supplies values) and coded apps are inert. Getters fall back to the def default per missing key (`instanceValues?.[key] ?? staticParams[key]`), so a partial/pre-emit source never blanks a param. State-coupled registration lives in `components-ui-html` (`registerBuyFeature.svelte.ts`, alongside the `featureCards` repeater source — `engine-layout` stays state-free): a runes store keyed to the seeded `confirm-dialog` node id, mirroring `<BuyBonusConfirm>`'s `values` derivation (per-mode `stateBonusDerived.selectedBetModeData()` + `stateI18nDerived.translate` + `i18nDerived.confirm/cancel`), reactive on `stateBonus.selectedBetModeKey`. **Scoping:** keyed by NODE id (the one seeded `confirm-dialog` instance, exported as `CONFIRM_DIALOG_NODE_ID` from `confirmScene.ts`), NOT by the reusable `confirmDialog` COMPONENT id — a second `confirmDialog` placed elsewhere (Step 6) registers no source under its own node id and is untouched. Node id is the tightest key the render-time seam exposes; a scene that copies the same node id would collide (documented on the registry). Verified headlessly: new `pnpm --filter flow-spike run v2buyconfirmvalues` (registry scoping incl. component-id-miss control; per-mode source replay + re-emit on select; the EXACT precedence resolver over the REAL `CONFIRM_DIALOG_DEF.params` — flow reads the source, imperative byte-identical + source-never-consulted, prop wins, partial falls back), `v2buyflow`/`v2drivenseed` unregressed, `engine-flow-v2` typecheck + `engine-layout`/`apps/lines` + `cluster` (inert) builds green. **Live check owed:** that a bound text/sprite node PAINTS the resolved value (paramBinding→node-literal fall-through is a render step, outside the headless harness). **Engine change on `main` — reaches an online/remake game only on a runtime-bundle republish + `engine` submodule bump.**

- 2026-08-04 — **Buy-flow-in-Flow Phase 3 Steps 4+5 — the buy sequence authored end-to-end in the reference flow (opt-in).** Step 4: (a) a `confirmDialog` now projects fused container-event pins `<id>.onConfirm` / `<id>.onCancel` — GENERIC over a component def's declared `signals` (new `componentSignalConfiguredEvents` in `engine-flow-v2/containerEvents.ts`, no confirm/cancel name special-casing), and the `/flow-v2` server projection (`+page.server.ts`) emits one decl per BUILTIN def's signals in addition to the universal `action` binding. (b) the confirm/cancel PRESS routes through the flow when owned: `<ComponentInstance>`'s `setComponentPress` seam now consults `getFlowPress()?.(node.id, name)` first — owned ⇒ flow ALONE, else the Phase-2 `instanceBindingContext` action (the imperative `<ConfirmDialog>`'s `onConfirm`/`onCancel`) runs EXACTLY as today. The shared `firePress` short-circuit is untouched. Step 5: the buy subgraph is authored into `BOOK_OF_DRIVEN_SEED_DOC` (`drivenSeed.ts`, the doc `apps/lines` loads via `?flowV2=lines`/`__IE_FLOW_V2_LINES__`/baked — NOT the default; default apps/lines + all coded apps are byte-identical): `event buyBonus ▶ Show(buyFeature)`; `buy-feature-cards.onSelect(betModeKey) ▶ selectBetMode ▶ Hide(buyFeature) ▶ Show(buyConfirm)`; `confirm-dialog.onConfirm ▶ commitBuyBonus ▶ Hide(buyConfirm)`; `confirm-dialog.onCancel ▶ Hide(buyConfirm) ▶ Show(buyFeature)` (a SECOND show node — an exec-in takes one predecessor; ownership is keyed by componentId so a re-shown card still routes to the first node's wired `onSelect`). Owning `buyBonus` makes `routeActionThroughFlow` dispatch to the flow so the imperative `stateModal` buy modal never opens and the imperative `<BuyFeatureScreen>`/`<ConfirmDialog>` mounts stay suppressed. Validation callers of the driven seed (`seed-flow-v2.ts`, `flowV2DrivenSeed.ts`) now pass the exported `BOOK_OF_DRIVEN_SEED_CONTAINER_EVENTS` map so the fused-pin edges resolve as real endpoints. **KNOWN GAP (flagged) — ✅ CLOSED 2026-08-04 (Step 5b, see below):** in flow mode the flow-shown `buyConfirm` scene rendered the confirm dialog's DEF-DEFAULT labels (`Title`/`Message`/`Confirm`/`Cancel`), not the per-mode dynamic copy — the imperative `<ConfirmDialog>` mount that fed those engineProvided values via `instanceBindingContext` is bypassed when the flow shows the scene directly. Functionally complete (select→confirm→commit/cancel all work); wiring the dynamic copy needed a flow-side value-source for the dialog's engineProvided fields — now delivered. Verified headlessly: `pnpm --filter flow-spike run v2buyflow` (full sequence: buyBonus→carried key→selectBetMode→confirm→commitBuyBonus / cancel→back to select, ownership + suppression + both press gates, over the REAL `BOOK_OF_DRIVEN_SEED_DOC`+`BOOK_OF_VOCAB`), `v2repeaterselect`/`v2drivenseed`/`v2containerevents` unregressed, `engine-flow-v2` typecheck + `engine-layout`/`apps/lines`/`cluster` builds green. **Engine change on `main` — reaches an online/remake game only on a runtime-bundle republish + `engine` submodule bump; not needed for this headless step.**

- 2026-08-04 — **`durationMs` now measures coded-`bind` animations — the Transition wipe's real length reaches a wired Delay instead of 0.** The `showContainer.durationMs` pin measured `0` for a Transition overlay because its wipe is a coded component mounted behind a `bind` (`TRANSITION_DEF`'s inner container binds `Transition` → `TransitionAnimation.svelte`, which plays the preloaded `transition` skeleton's `animation` clip), and `sceneAnimationDurationMs`'s walk only handled `spine`/`effect`/`container`/`componentInstance` nodes — never `bind` (a `BaseNode` field on any kind). A Delay wired to that pin with no literal floor held `0` → HideContainer tore the scene down on mount → "the wipe/FX stopped playing." Fix (the declare≠implement seam, same as `bookEventHandlerMap`): `SceneDurationResolvers` gains an optional `boundComponentMs?(component)` and the walk calls `resolvers.boundComponentMs?.(node.bind.component)` before the kind switch (`packages/engine-layout/src/lib/sceneDuration.ts`); `apps/lines/src/game/flowV2Runtime.svelte.ts` supplies it — `Transition` → `spineClipMs('transition', 'animation')`, which resolves against the loaded (boot-preloaded) skeleton to the real clip length. Now `durationMs`→Delay reports the true wipe length. Optional resolver ⇒ `flow-spike` and any other caller are untouched. **Engine change on `main` — reaches the online/remake runtime only on a runtime-bundle republish + `engine` submodule bump.** NOT browser-verified (auth-gated editor; the scene doc lives in R2).

- 2026-08-04 — **flow-driven-game Phase 5 closed (owner-confirmed).** Real
  `bigWin`/`freeSpinIntro`/`loading` backing scenes authored, baked, and shipped; a game runs a real
  FlowDoc end-to-end — the acceptance proof for the flow-driven-game story.

- 2026-08-03 — **Delay `ms` is now FAIL-SAFE when wired — an unmeasurable duration pin falls back to the authored literal floor instead of collapsing the hold to 0.** Fixes a live regression: wiring the new `showContainer.durationMs` pin into a Delay on a runtime that couldn't resolve it (the shipped bundle predated the pin, OR the pin measured 0 — spine not loaded / clip-name mismatch / no animated node) made the Delay resolve `ms` to `undefined`/`NaN`/`0`, so the guard fired the Delay INSTANTLY and the shown Spine/FX screen was torn down the moment it mounted ("Spine and FX stopped playing"). Now the `delay` case routes through a new `resolveDelayMs(graph, node, scope)` (`packages/engine-flow-v2/src/runtime.ts`): a WIRED `ms` that resolves finite and `> 0` wins; anything else (an old runtime with no pin case → `0`, or a pin that couldn't measure → `0`) falls back to the node's OWN authored literal `ms` (the floor), then a final non-finite guard → `0`. An UNWIRED Delay is byte-identical (it already read the literal); compute-driven dynamic delays (finite, `> 0`) are unaffected. So authoring `durationMs`→Delay is safe even before the runtime ships the pin — the screen holds for your literal floor rather than vanishing. Verified: new `pnpm --filter flow-spike run delayfailsafe` (measurable pin drives the delay; unmeasurable pin + floor → floor; ABSENT env hook = old-runtime analogue → floor; unmeasurable + no floor → 0; unwired unchanged) PASS; `v2runtime`/`v2hold`/`sceneduration` unregressed; `engine-flow-v2` typecheck clean.
- 2026-08-03 — **`showContainer` nodes gained a `durationMs` OUTPUT data-pin — wire a screen's real animation length into a Delay instead of guessing ms.** A `showContainer` node now derives a `durationMs` data-out (type `ms`, label "Animation duration (ms)"); connect it to a Delay's `ms` and the beat holds for exactly the shown scene's longest animation. Runtime-computed (spine duration is only known once the skeleton loads): the interpreter reads it via a new optional `FlowV2Env.containerAnimationMs?(containerId)` hook (`packages/engine-flow-v2/src/runtime.ts`), returned by `resolveDataOut` for the `durationMs` pin only (`?? 0`). The value comes from a new PURE calculator `sceneAnimationDurationMs(scene, resolvers)` (`packages/engine-layout/src/lib/sceneDuration.ts`, exported from the index): it walks the scene's nodes — descending into `container` children and expanding `componentInstance` defs (cycle-guarded) — and returns the MAX wall-clock ms across spine nodes (max over `defaultAnimation` + `cues[].animation`) and effect nodes, `0` when nothing is measurable. The doc records only NAMES, so the durations are injected as resolvers: `apps/lines/src/game/flowV2Runtime.svelte.ts` wires `spineClipMs` (loaded `SkeletonData.findAnimation(name).duration × 1000`, with the `EffectLayer` bundle-folder fallback), `effectMs` (baked `EffectDoc` → max over layers of `trigger.duration ?? emitterSecondsToWallMs(config.emitterLifetime)`, reusing the engine-fx 0.00234 emit-speed scale — NOT reinvented), and `resolveComponent` (`getComponent`). **Parity: the pin is opt-in** — an un-loaded/un-baked/unknown asset is skipped (→ 0), a Delay fed 0 doesn't wait, and a Delay with a literal `ms` is untouched (`resolveDataIn` reads the data edge first, literal fallback otherwise). `ms` out → `ms`/`int` in is already legal (`types-check.ts`, no change). Verified offline: new `pnpm --filter flow-spike run sceneduration` (max-selection incl. spine default+cues; container + componentInstance expansion; self-referential-def cycle guard terminates; empty scene + all-unmeasurable ⇒ 0) PASS; `engine-flow-v2`/`engine-fx` typecheck clean; `sceneDuration.ts` isolated strict `tsc` clean. **Engine change on `main` — reaches the Book of Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned), and only once a `durationMs`→Delay wire is authored.** NOT browser-verified (auth-gated editor + WebGPU runtime).
- 2026-07-31 — **Count-up interaction is now PER-INSTANCE authorable + gained a tap-to-skip mode (was a hardcoded, always-on hold).** The WIN overlay + free-spin-outro count-up hold-to-fast-forward (added 2026-07-29, hardcoded) is now TWO independent, author-controlled toggles PER COUNT-UP NODE (owner direction: "should be per instance, not per project"), and a second interaction mode was added. The toggles are optional BOOL params `holdToSpeedUp` + `tapToSkip` on the `freeSpinOutroCountUp` AND `winUpdate` action nodes (`BOOK_OF_VOCAB.actions`, `packages/engine-flow-v2/src/reference/bookOf.ts`) — so they show in that node's Inspector. `holdToSpeedUp` = hold a press/Space to fast-forward; `tapToSkip` = a quick tap/press jumps straight to the FINAL total (the provider's existing `finishCountUp` slam). Data path: the param → the effect broadcast payload (`flowEffects.ts` `freeSpinOutroCountUp`/`winUpdate` add `holdToSpeedUp: payload.x === true`) → the emitter event (source types in `Win.svelte`/`FreeSpinOutro.svelte`, regen'd into `emitterVocabulary.ts` via `node scripts/gen-flow-vocabulary.mjs`) → the driver (`WinGate`/`FreeSpinOutroDriver` read `emitterEvent.holdToSpeedUp ?? false` into `$state`). **UNSET ⇒ OFF** (both), matching the Inspector's unchecked checkbox — no phantom-default mismatch (an existing outro/win node loses the old auto-hold until the box is ticked; honest + trivially restored). The two byte-identical inline hold blocks were extracted into ONE shared `apps/lines/src/components/CountUpInteraction.svelte` (canvas-space rect + `<OnHotkey Space>`) that COMPOSES the toggles — tap-only ⇒ skip on the down edge (instant); hold-only ⇒ accelerate on down, stop on release; both off ⇒ renders nothing. **Both-on gesture (reworked after an owner live-test — the first cut used "skip only if released < 200ms", which read most real taps as holds so tap-to-skip never fired):** the hold now ACTIVATES only after `HOLD_ACTIVATE_MS` (300ms) — a release before that is unambiguously a tap → skip (and never accelerated); a press that outlives it is a hold → speed up, release returns to normal (no skip). Per-input activation timers cleared on release/`onDestroy`. `WinCountUpProvider` UNCHANGED (already exposed `speedScale` + `finishCountUp`); hold off ⇒ driver passes `speedScale={undefined}` (original single-tween path). Only active on the flow path (`!codedPressOwned` / the driver mounts); the coded fallback keeps `PressToContinue`. No bake change (v2 embeds the doc as-stored; the params ride the node). **Follow-up (same day) — DOUBLE-COUNT-UP fix so tap-to-skip snaps the visible number:** the engine count-up (`WinCountUpProvider`) already animates the count and responds to skip, but an authored count Text Box with `countUp: true` bound to `winCountUpAmount`/`freeSpinOutroTotalWin` ran its OWN ~500ms `ParamReadoutText` tween on top — which ignored the skip, so on tap the number kept easing to the total instead of snapping. Fixed generically via a new `ValueSource.selfAnimated` flag (`registerComponentValues.ts`): a source that is ITSELF a live tween sets it (the two count-up sources in `Game.svelte` now pass `selfAnimated: true`); `ComponentInstance` forwards it as the `valueSelfAnimated` param; `LayoutNodeView` then SUPPRESSES a readout's own `countUp` when set (`countUp && valueSelfAnimated !== true`), so the readout snaps to the live value each frame — the count-up still looks smooth (the source animates it) AND a tap-to-skip / round-slam that snaps the source shows the final total instantly, regardless of the author's `countUp` tick. Plain (non-animated) sources are unaffected (parity). Verified: `engine-flow-v2` typecheck clean, `flow-spike run vocab`/`fs7outro`/`fs7win` PASS, `apps/lines` dev boot clean. **Engine change on `main` — reaches the Book of Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned).**
- 2026-07-31 — **The WIN overlay is a proper Flow-v2 tap-to-dismiss container — OWNERSHIP-based + NAME-AGNOSTIC (revised from the first scene-id-keyed cut, owner direction).** An author names their win container ANYTHING, authors their OWN count Text Box + their own cue-driven animations, and the engine mounts only the load-bearing headless count-up behind it. The TRIGGER is `ownsEvent('setWin')` (read like `flowV2DrivesScreens = ownsEvent('load')`, exposed in `Game.svelte` as `flowOwnsSetWin`), NOT a scene named `bigWin`. `apps/lines/src/game/winOwnership.ts` `resolveWinMount({flowV2DrivesScreens, flowOwnsSetWin, basegameOverlaysHasCodedWinGate})` ⇒ `'driver'`/`'gate'`/`null`, guaranteeing EXACTLY ONE `winUpdate` subscriber under v2: non-flow ⇒ `null` (parity); coded `Win`/`WinGate` in `basegameOverlays` ⇒ `null` (it already subscribes — mirrors `main`'s `!basegameOverlaysHasCodedWinGate` gate, so a non-owning game is byte-identical); owns `setWin` ⇒ `'driver'` (`<WinGate headless>`, dim dropped — author owns dim/text/art/tap); does NOT own `setWin` ⇒ `'gate'` (full coded gate; the coded `setWin` handler broadcasts `winShow`/`winUpdate` — today's Borut remake path). KEPT from the first cut (already name-agnostic): the `winCountUpComplete` component signal + `winState.countUpComplete` latch, `WinGate`'s `headless` prop, the `Win.svelte` `EmitterEventWin` entry, the `ENGINE_SIGNAL_CATALOG` signal entry. ADDED: (1) `winCountUpAmount` value source (`Game.svelte` `registerComponentValues` + `ENGINE_PARAM_CATALOG`, the LIVE count-up tween — author binds their own Text Box `source: 'winCountUpAmount'`, mirrors `freeSpinOutroTotalWin`); (2) `flowV2` handle exposes `awaitTargets` (`awaitCompleteContainerIds(doc)`). REMOVED: the scene-id machinery (`hasAuthoredWin`/`bigWinHasCodedWinGate`/`WIN_SCREEN_ID`/`bigWin`-scene inspection). **Celebration-lock finding + fix:** the spin-button lock (`Game.svelte` `celebrationLock.win`) keyed on `activeScreenIds.has('bigWin')` — a hardcoded name. The authored container's `tapToContinue`→`PressToContinue` locks via `continuePressCount` only AFTER the tap ARMS (post `winCountUpComplete`); during the count-up window before that, an arbitrarily-named container would NOT lock the button. Fixed name-agnostically: under a driven flow the lock also holds while ANY `awaitComplete`-held container is shown (`winAwaitTargets ∩ activeScreenIds`) — `celebrationLock.{intro,outro,win}` are read only through `hasCelebrationOverlay` (ORs the three), so OR-ing into `.win` is behaviour-identical; the coded/v1 path (`flowV2DrivesScreens` false) keeps only the `bigWin` id check ⇒ byte-identical. **Parity:** with no flow owning `setWin` (today's Borut remake + `apps/lines`) the full `'gate'` mounts exactly as `main`. Verified `pnpm --filter flow-spike run fs7win` (exactly-one-subscriber across all 8 ownership combos + a Borut-remake parity control + tap-arm gating + latch order-independence), `fs7outro`/`v2hold` unregressed, `engine-layout` build clean, `apps/lines` svelte-check 0 errors, Prettier clean. **Engine change on `main` — reaches the Book of Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned), and only once the flow OWNS `setWin` + a win container is authored.**
- 2026-07-30 — **Container completion is now ORDER-INDEPENDENT (the mount-model twin of the #149 signal race).** Live on `bookofborutremake` a fully-authored free-spin outro hung the round if the player tapped in the ~300ms window between the outro driver ARMING the tap (`freeSpinOutroCountUp` done → broadcast `freeSpinOutroCountUpComplete`) and the `showContainer{awaitComplete}` node REGISTERING its hold: the tap dispatcher's `mount.complete('freeSpinOutro')` found no hold, dropped the completion silently, and the later `awaitComplete('freeSpinOutro')` then waited forever (screen never hides; the one-shot `tapToContinue` already spent). Fix: `createContainerMountModel` (`packages/engine-flow-v2/src/mount.ts`) now LATCHES a `complete(id)` that arrives with no hold registered, so a later `awaitComplete(id)` resolves immediately and consumes it (mirrors #149's seed-on-subscribe). **Scoping (the whole risk):** the latch is restricted to containers a `showContainer{awaitComplete}` node actually targets — a new pure `awaitCompleteContainerIds(doc)` (`runtime.ts`), passed as the model's 3rd arg by `flowV2Runtime.svelte.ts`. So the tap dispatcher's top-down `complete()` probe can NOT spuriously latch a persistent container (basegame/hudBar, never an await target → `complete` returns `false` and the scan continues as before); a real target latches + returns `true` so the dispatcher stops at it. This also preserves the `complete:<id>` HANDOFF path (handoff-shape containers are shown WITHOUT `awaitComplete`, so they're not latchable). The latch is CLEARED on a fresh `show(id)` (not-shown→shown) and any `hide(id)`, so a stale completion can't pre-resolve a future round's hold; a redundant re-`show` of an already-shown id preserves a legitimate latch (the two-`showContainer` authoring case). Omitting the 3rd arg ⇒ no latching (byte-identical legacy behaviour) so the headless recorder / pure harnesses never deadlock. Verified: `pnpm --filter flow-spike run v2hold` (new section 7 — complete-before-await resolves; stale latch cleared by hide/show; redundant-show preserves; persistent-container not spuriously consumed; helper reads the graph; no-target legacy path unchanged) + `v2mount`/`v2drivenseed`/`v2halfon` green + `engine-flow-v2` typecheck clean. **Engine RUNTIME change on `main` — reaches online games only via a `Runtime release` (runtime=lines) + `/refresh`, and standalone Borut on an `engine` submodule bump.** Did NOT trigger the release.
- 2026-07-29 — **A `tapToContinue` overlay's DIM now follows the authored layer order** (was always painted on top). The hoisted tap surface is SPLIT: the invisible hit area + prompt still always draw on top (so a tap anywhere dismisses the screen and the prompt stays visible), but the visible **dim backdrop draws BEHIND the scene content when the tap node has any node painted after it** (content the author placed ABOVE it in the outline) — so an authored celebration screen (e.g. a free-spin retrigger) shows over its own dim instead of being covered by it. A **topmost** tap node keeps the dim in front — byte-identical to before. A `game`/`standard` dim is canvas-space and can't interleave with the scaled `MainContainer` nodes, so it goes wholly behind or wholly in front; the decision is `tapDimBehind` (`tapToContinue.ts`), rendered by `LayoutScene`, with the dim drawn by the engine (`ComponentInstance` passes the game tap component `dimAlpha:0` so it isn't double-drawn). Touches `tapPortalContext.ts` (dim/tap split), `LayoutScene.svelte`, `ComponentInstance.svelte`, `tapToContinue.ts`. Verified `node scripts/test-tap-to-continue.mjs` (5 layer-order cases + no param regression) + `engine-layout` build. **Engine change on `main` — reaches the Borut remake on a runtime-bundle republish.**
- 2026-07-29 — **FS-4 free-spin RETRIGGER ("won +N extra free spins") is now authorable online.** The `freeSpinRetrigger` book event (payload `extraFs`, `total`) already arrives end-to-end — the facade emits it when 3+ scatters land mid free spin (`stakeFacade.ts`) — but was silently dropped by a coded no-op and inert in Flow. FS-4: (1) surfaced `freeSpinRetrigger` to the v2 vocabulary (`BOOK_OF_VOCAB.events` in `packages/engine-flow-v2/src/reference/bookOf.ts`), so `gameSignals` now projects an `onFreeSpinRetrigger` exec-out + typed `extraFs`/`total` data-outs an author wires; (2) reconciled the seam — `freeSpinRetrigger` is promoted from an always-stripped seam to a first-class optional free-spin STEP in `FREE_SPIN_STEPS` (`freeSpinOwnership.ts`; owned/stripped per-step like intro/counter/outro; new `ownsRetrigger`), and the reserved LAYER edge now fires on the real `freeSpinRetrigger` event (was the phantom `retrigger`, `flowDoc.ts`). An owner authors `onFreeSpinRetrigger → showContainer(freeSpinRetrigger, {awaitComplete})` + an `extraFs` "+N" readout + a `tapToContinue` overlay to FREEZE the free-spin sequence until the player taps; un-authored ⇒ the coded no-op (present nothing) — parity, so Book of Borut stays byte-identical. Deferred (additive): the FS-3 `freeSpinRetrigger` spine-cue signal (`ENGINE_SIGNAL_CATALOG` + a runtime broadcast). Verified offline: new `flow-spike run fs4` (pin projection + runtime authoring chain + owned/stripped seam parity), plus `fs1`/`fs2`/`v2gamesignals`/`v2vocab` green and `engine-flow-v2` typecheck clean. **Engine change on `main` — reaches the Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned), and only once a retrigger screen is actually authored.**
- 2026-07-29 — **FS-7 outro follow-up: driver no longer emits coins, a count-up-complete signal arms the tap, and hold-to-fast-forward.** Three refinements to the authored-outro path (owner-directed): **(1)** the baked coin fountain was REMOVED from `FreeSpinOutroDriver` (owner: "driver never emits coins") — the `<WinCoins>` block + `coins` derivation are gone, and the whole #138 coin plumbing (`OutroCoinsConfig` + `freeSpinOutroState.coins`; the `showCoins`/`coinsX`/`coinsY`/`coinsLevel` params + publisher on `FreeSpinOutroVisual`; the `boundComponentCatalog` entries) was deleted in favour of author-placed fountains. The fallback `FreeSpinOutroGate` keeps its OWN hardcoded `<WinCoins>` UNCHANGED (byte-identical). **(2)** New component-scoped fired signal `freeSpinOutroCountUpComplete` — the driver broadcasts a new payload-less `freeSpinOutroCountUpComplete` emitter event the moment `startCountUp()` resolves (natural, slammed, or fast-forwarded; once per outro); registered in `ENGINE_SIGNAL_CATALOG` + `registerComponentSignals` exactly like the big/small-win signals, so an author sets `tapArmAfterSignal: 'freeSpinOutroCountUpComplete'` (or `hiddenUntilSignal`) to make the tap/prompt appear only after the count. **(3)** HOLD-to-fast-forward — while a pointer (canvas-space surface mounted only during the count-up so it never blocks the authored tap) OR the Space key (`OnHotkey` off the global `EnableHotkey` broadcast) is held, the count-up runs 6× faster and eases back on release. True continuous acceleration, not a snap: `WinCountUpProvider` gained an opt-in `speedScale` prop (re-targets the svelte `Tween` on speed change, owns completion via a value-reached watch since `Tween.set` drops the aborted promise); default/omitted ⇒ the original single-tween path, so the WIN overlay + fallback gate are byte-identical (only the driver passes it). Verified offline: `flow-spike run fs7outro` (adds section D — the complete signal arms tap + reveals prompt), `pixi-svelte`/`engine-layout`/`engine-flow-v2` builds+typecheck clean, `apps/lines` svelte-check 0 errors. **Engine change on `main` — reaches the Book of Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned).**
- 2026-07-28 — **FS-7 free-spin OUTRO is fully authorable from primitives (headless driver split).** The outro reaches the same authoring depth as the intro + book reveal: an author rebuilds it entirely (own spine via SpineCue, live count text, big/small art, own tap/hold) while the engine keeps only the load-bearing count-up. New `apps/lines/src/components/FreeSpinOutroDriver.svelte` = the extracted headless core of `FreeSpinOutroGate` (subscribe `freeSpinOutro*` → run `WinCountUpProvider` → publish `countUpAmount`/`winLevelData` to `freeSpinOutroState` → emit the toggleable baked coin fountain) with NO dim / press / coded sprites. `FreeSpinOutroGate` kept byte-identical as the un-authored fallback. `Game.svelte` routes via `resolveFreeSpinOutroMount` (the single decision guaranteeing exactly one `freeSpinOutroCountUp` subscriber): v2 author-rebuilt (`hasAuthoredFreeSpinOutro`) ⇒ driver at the container band (self-resolves the hold on count-up done, mirrors `WinGate`; the authored `tapToContinue`+`showContainer{awaitComplete}` owns the tap-hold like the intro); v2 driven-seed/Borut composer ⇒ full gate/composer unchanged; v1 `ownsOutro` ⇒ driver at the top band transferring the hold to the screen's Complete pin (mirrors `FreeSpinIntroFlowGate`). New authorable surfaces: `freeSpinOutroTotalWin` value source (`ENGINE_PARAM_CATALOG`, currency-formatted live count-up), `freeSpinOutroBigWin`/`freeSpinOutroSmallWin` signals (`ENGINE_SIGNAL_CATALOG` + `registerComponentSignals`, fired off the same `freeSpinOutroCountUp` broadcast, filtered by `winLevelData.type`), `showCoins`/`coinsX`/`coinsY`/`coinsLevel` on `FreeSpinOutroVisual` (fountain KEPT baked in the driver per decision 2, published to `freeSpinOutroState.coins`). `ComponentInstance` now also subscribes to registered signals named by a `hiddenUntilSignal`/`tapArmAfterSignal` gate (not only spine cues), so plain art gates on a win-level signal. Verified offline: `flow-spike run fs7outro` (routing/parity + exactly-one-subscriber across all 12 combos + win-level reveal), `test-signal-gates.mjs`, `apps/lines` svelte-check 0 errors, `engine-layout`/`pixi-svelte`/`engine-flow-v2` builds clean. **Engine change on `main` — reaches the Book of Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned).**
- 2026-07-28 — **A custom overlay can now sequence "spine intro plays → then text + tap appear → idle loops," authored in the editor.** Owner-reported building a CUSTOM free-spin-intro component (a placed spine rig + a "10 Free Spin" text + tap): the intro→idle handoff and the auto-`enter` cue already worked, but there was **no way to gate the text or the tap on the intro FINISHING** — the one-shot's completion was swallowed by the spine's idle hand-off and never surfaced, so text rendered on mount and the tap armed on mount (a tap during the intro closed it instantly). Built one generic **author-named, component-scoped fired-signal bus** (no hardcoded ids — the author types one name, e.g. `introDone`, and wires three fields to it): (1) `SpineCue.completeSignal` — a spine one-shot fires the named signal the moment it completes (new `oncomplete` on `pixi-svelte`'s `SpineTrack`, attached only for a NON-looping primary, wired through `LayoutNodeView`'s existing intro→idle handoff); (2) `BaseNode.hiddenUntilSignal` — a node stays hidden until the named signal has fired for the instance (the `{#if transform.visible}` gate becomes `&& revealed`, mirroring the `spineVisible` gate); (3) `tapArmAfterSignal` (a `TAP_TO_CONTINUE_PARAMS` entry) — the tap surface isn't mounted until the named signal fires (taps pass through before). `ComponentInstance` owns the per-instance bus (a `$state` fire-count map, reset on fresh mount so a re-entered free-spin screen re-hides + re-arms) and records `enter` (visible edge), every game signal (so a gate can key on `win`/etc.), and any spine `completeSignal`; descendants read it via the new `componentFiredSignalsContext`. Pure gate helpers extracted to `signalGates.ts` (single source of truth for "fired ⇒ open") so the two consumers can't disagree. Authoring UI in the Scene/Component editor: per-cue "fire signal on complete" (non-looping cues only), a per-node "hidden until signal", and "Arm tap after signal" (renders generically from `TAP_TO_CONTINUE_PARAMS`). **Parity: every field unset ⇒ byte-identical to today** (no provider ⇒ gates default OPEN, no `SpineTrack` listener attached, nothing fired) — no existing game changes. New node/cue fields survive save/load unchanged (`normalizeNode` trusts kind+id; `identityComponentRoot` passes children by reference — no whitelist edit needed). Verified offline: `signalGates.mjs` fixture (22/22, incl. an end-to-end mount→enter→introDone→reveal+arm micro-scenario) + updated `tap-to-continue` fixture; `pixi-svelte` / `engine-layout` / `launcher-api` builds all clean. NOT browser-verified (auth-gated editor + runtime). **Engine change on `main` — reaches the Book of Borut remake only on a runtime-bundle republish + `engine` submodule bump (owner-owned).**
- 2026-07-27 — **`openPayTable` is now a flow action, so an authored HUD menu can reach the symbol pay table.** Reported live: a menu's `onGameRules` pin wired correctly, but the "symbols and what they pay" page was gone. Root cause: the flow exposed `openGameRules`/`openSettings` but **no `openPayTable`** — the coded `payTable` modal existed (`Game.svelte`) yet had no flow route, so a flow-driven menu (which replaces the coded HUD) left the `payTable` modal unreachable via its `onPayTable` pin. Fix (mirrors `openGameRules` exactly): added the `openPayTable` command + `payTable` intent to `BOOK_OF_VOCAB` (`engine-flow-v2/reference/bookOf.ts`) so the action shows in the `/flow-v2` palette; `openPayTable → payTable` in `INTENT_COMMANDS` (`flowV2Runtime.svelte.ts`); a `payTable` case in `invokeHostIntent` + a shared `doOpenPayTable` reused by the coded button (`Game.svelte`). Authors wire `onPayTable → openPayTable`, restoring the two-button split (Paytable → symbols+payouts, Game Rules → rules). Also **led the shared `gameRules` `InfoOverlay` with GAME RULES** (was PAYLINES) so the Game Rules button lands on the rules; paylines is page 2. Verified: `engine-flow-v2` typecheck + `apps/lines` build green. Shipped via [PR #95](https://github.com/Invisible-Wall-SL/Invisible-Engine/pull/95) — reaches online games only on a runtime release; standalone Borut on an `engine` submodule bump.
- 2026-07-23 — **Half-on flow guard: a flow with `showContainer` nodes but no `load` ownership no longer silently hides the game (the Test1 missing-reel bug).** Live forensics on Test1 (owner): the authored flow reported `ownsEvent('load') === false` (so `flowV2DrivesScreens` is false and the coded/v1 path owns screens) yet `ownsEvent('tapToStart') === true` with a `tapToStart → showContainer(basegame)` chain. Because a `showContainer` only reaches `activeScreenIds` when `flowV2DrivesScreens` is true (`Game.svelte:1406`), basegame never entered the active set — and the flow's ownership of the `tapToStart` screen-lifecycle signal suppressed the coded loading→tap→basegame transition — so the engine board `<Board/>` (gated on `isBasegameActive`, `Game.svelte:1535/832`) never mounted. A "half-on" dead state: screen-driving intent without the `load` that makes it work. **Guard (safety net):** new pure `flowScreenDrivingStatus(doc)` + `SCREEN_LIFECYCLE_SIGNALS` in `engine-flow-v2/runtime.ts` classify the posture (`halfOn` = has `showContainer`/`hideContainer` nodes AND does not own `load`). `createLinesFlowV2` (`flowV2Runtime.svelte.ts`) detects it, shouts one loud `console.error` naming the fix, and neutralizes the handle's `ownsEvent` for the screen-lifecycle signals (`load`, `tapToStart`) so they fall through to the coded screen path (reel mounts) — **book-event ownership (reveal/winInfo/…) is untouched**. The single seam is the `ownsEvent` definition site, so every consumer (`flowV2DrivesScreens`, the book path, `routeActionThroughFlow`) sees one consistent decision. **Deliberately NOT half-on:** owning `tapToStart` alone for audio (the seeded reference does this) — only a container node signals intent to mount a screen, so a book-events-only flow is never flagged. Verified offline over the real modules: `tools/flow-spike/flowV2HalfOn.ts` (`pnpm --filter flow-spike run v2halfon`, 20/20, incl. the audio false-positive control + a raw-ownership control proving the guard is the only thing flipping `tapToStart`); `engine-flow-v2` typecheck + `apps/lines` svelte-check green; `v2gamesignals`/`v2lifecycle`/`v2runtime` harnesses unregressed.
  - **FIX 2 (fully-driven default seed) — BUILT (⏳ needs live owner verification before trust).** Superseded the scope-deferral above: owner live-inspected `bookofborutremake`'s runtime flow (`gameSignals.load` wired ⇒ `flowV2DrivesScreens` true; 19 `showContainer` + 9 `hideContainer` nodes; `freeSpinOutroCountUp` wired + shipping), proving a driven flow works end-to-end. New `packages/engine-flow-v2/src/reference/drivenSeed.ts` — `BOOK_OF_DRIVEN_SEED_DOC` + `freshBookOfDrivenSeedDoc()`: owns `load` (wired `gameSignals.load`), shows `loading`, transitions loading→game on **`complete:loading`** (the loading-bar `completeOnLoaded` auto-advance path) AND `tapToStart` (the tap path; also unlocks `bgm_main`), mounting `basegame` + `hudBar` + `hudCorners` + `specialBook` + `freeSpinCounter`/`Intro`/`Outro`, and wires every canonical `BOOK_OF_CHOREO` book event off its gameSignals pin. **Overlay-visibility model:** overlays are mounted once on start and their internal visibility is toggled by the book-event cues their bound components already subscribe to — mirrors the coded path, avoids the show-then-cue race, and covers the whole show set trivially (`basegameOverlays` renders unconditionally so it is deliberately NOT shown). **Uses ONLY canonical scaffold ids** (no Borut custom ids). **Decision (b):** the driven doc is a SEPARATE seed used only by the launcher seed path (`loadFlowV2DocForEditor` → `freshBookOfDrivenSeedDoc`); `apps/lines`' `LINES_FLOW_V2_DOC` stays the book-events-only `BOOK_OF_REFERENCE_DOC` (unchanged — no apps/lines behavior change). HUD buttons are NOT flow-wired (canonical bind-based `hudBar` buttons carry no `action` param → no fused pins; their coded `onpress` fires when mounted, so Spin works via parity). Verified offline: `tools/flow-spike/flowV2DrivenSeed.ts` (`pnpm --filter flow-spike run v2drivenseed`, 35/35 — `drivesScreens`+`!halfOn`, canonical-only show set covering basegame+HUD+free-spins, `validateFlowDoc` 0 errors, and a runtime `load`→`complete:loading` mount check leaving the game shown); `engine-flow-v2` typecheck + `apps/lines`/`launcher-api` svelte-check show no new errors. **⏳ Live-verify owed (runtime release + fresh-project test): the free-spin OUTRO COUNT-UP** — `freeSpinEnd` fires `freeSpinOutroCountUp` whose subscriber historically lived in the coded `FreeSpinOutroGate` (suppressed under a driven flow); Borut renders it, but whether the CANONICAL `freeSpinOutro` scene's bound `FreeSpinOutro` component carries the count-up is unproven headlessly.
- 2026-07-23 — **A fresh project could not save its flow, and opened on a throwaway sample.** `/flow-v2` conflated "no stored `flow-v2.json`" with "no project bound": the loader returned `doc: null` (client fell back to the built-in `SAMPLE_DOC`) and `hasProject = data.doc !== null` gated autosave OFF, so `markDirty()` early-returned and NOTHING ever POSTed — a chicken-and-egg deadlock where the editor only saved once a doc already existed. Every never-seeded project (e.g. a newly created one) showed the sample, silently dropped edits, and reloaded back to the sample. Read-key == write-key throughout (`<client>/<project>/editor/flow-v2.json`), so it was never a key mismatch. Fix: (a) **lazy-seed** — the reference book-of flow was promoted from `apps/lines` into `engine-flow-v2` (`reference/starterDoc.ts`: `BOOK_OF_REFERENCE_DOC` + `freshBookOfFlowDoc()`; `apps/lines/flowV2Doc.ts` now re-exports it as `LINES_FLOW_V2_DOC`), and a NEW editor-only read `loadFlowV2DocForEditor` returns a deep clone of it (never null) with `seeded: true` + the correct etag (null→create, malformed→overwrite). The bake/export + FX consumers KEEP the null-returning `loadFlowV2Doc`, so §7 fall-through is intact — an un-authored project still bakes nothing and falls through to coded handlers. (b) **save gate** — `hasProject` now derives from the resolved project scope (`data.projectKey`), not doc presence, so the first edit arms the debounce and creates the R2 object (`ifNoneMatch:'*'` via the null `docEtag`); subsequent saves CAS on the returned etag. (c) the "sample · not saved" pill became **"New — unsaved"** until the first autosave lands, then normal "Saved". `SAMPLE_DOC`/`SAMPLE_CONTAINER_EVENTS` deleted (dead); `LIBRARY` fallback kept. So a new project opens on a real, editable, saveable loading→tap→basegame→win flow that drives the game once saved. Verified: `engine-flow-v2` `typecheck` green; `launcher-api` `svelte-check` shows ZERO new errors in the 5 touched files (the remaining flow-v2 errors — `graphOps.ts`, `CommentNode`, `FlowCanvasV2`, `+page.svelte:103` `activeFn` `$derived` narrowing — are byte-identical on HEAD); `launcher-api` build bundles `starterDoc.js`.
- 2026-07-17 — **`data-in-shadowed` was unfixable from the UI — now unreachable by construction.** Fallout from the wiring fix above: once the owner replaced their literals with real data wires, three `data-in-shadowed` warnings appeared and could not be cleared. The inspector renders a wired data-in as a bare `wired` tag with NO source editor (`NodeInspector`), so the stale `literal 0` / `accessor $index` underneath was invisible AND uneditable — the panel reported a problem the UI gave no way to fix, on a graph that was by then completely correct (the warning is honest: the edge wins, so the stored source is merely dead). Two fixes: (a) `addDataEdgeIn` now re-stamps the target pin `{kind:'wire'}` when it carried a literal/accessor — dragging a wire onto a pin that already had one is the ONE way an author creates the shadow, so this makes it unreachable going forward; (b) the wired row surfaces a **"Clear dead literal/accessor"** button when a stale source exists, for the docs that already carry one. Verified over the real modules (a Node fixture composing `addDataEdge` + `validateFlowDoc` on the owner's exact node shape): raw edge append ⇒ `['data-in-shadowed']`, `addDataEdge` ⇒ `[]` with both pins `{kind:'wire'}` and the data edges intact.
- 2026-07-17 — **A Game Signals data wire only resolves on its OWN event's chain — now an error, and `$trigger` is finally authorable.** Reported live as "the free-spin counter shows NaN on the first spin, then counts from the second". Root cause: the ONE `gameSignals` node surfaces EVERY event's data pins at once, so wiring `onUpdateFreeSpin.total` into a node on the `freeSpinTrigger` chain LOOKS authored — but `resolveDataOut` reads a signal pin as `scope.trigger[<field>]` (the FIRING event's payload, whatever pin it was dragged from) and `runEvent` seeds exactly one trigger per dispatch, so it resolves `undefined` on every dispatch that reaches the target. `updateFreeSpinCounter` then does `amount + 1` ⇒ **NaN**. The coded `updateFreeSpin` handler was silently repairing it on the next spin (`flowOwnsSignal` needs an **exec** edge — data edges don't count — so pulling a signal's data without wiring its exec leaves the event un-owned), which is what made it look like a timing bug.
  - **`signal-cross-event`** (error, `validate.ts`): a `gameSignals` data-out wired into a node that node's chain never runs on. New `eventOwners(graph)` pass walks exec from every entry (an `event` node's `ref`; each wired `gameSignals` exec-out pin, whose pin id IS the event name) to resolve each node's owning event set. Message names both events and both fixes ("feed the pin from 'x', or move the node onto the 'y' chain"). The runtime cannot catch this — only the graph knows which chain a node sits on, and the interpreter is deliberately parity-safe (unresolved ⇒ `undefined`, never a throw).
  - **`$trigger`/`$context` added to the inspector's accessor dropdown.** The schema has carried `{ on: 'trigger' }` + `{ on: 'context' }` and the runtime has resolved both since Phase 4, but `NodeInspector` hardcoded only `$item`/`$index`/`$engine`/`$input` — so the payload of the event you're authoring was **unreachable from the UI**, which is what pushed authors toward cross-event wires and literals in the first place. `$trigger.<member>` gets the accessor twin of the wire check (reuses `accessor-unresolved`, names the owning event's real fields).
  - **A `showContainer`'s fused container-event pins are their OWN roots, not a continuation** — the subtlety that makes a naive "walk every exec edge" wrong, and which code review caught before this shipped. `runContainerEvent` starts a FRESH run at the pin's target and seeds `trigger` with the COMPONENT EVENT's payload, so attributing the mounting event to that chain produced BOTH a false positive (`$trigger.total` on a tap chain — a real `onTap` field — reported unresolved, i.e. a valid graph called broken, in exactly the pattern the new dropdown invites) and a false negative (a `freeSpinTrigger.totalFs` wire into that chain — the real NaN bug — reported clean). `eventOwners` now re-roots each container-event pin under its own decl id, which is never a vocab event name — so the wire check still fires there while `$trigger.<member>` stays unjudged (the component event's payload isn't in the template vocabulary).
  - **Never guess** holds throughout: an orphan chain (no entry reaches it) is judged by neither check; `$trigger.<member>` is only judged when EVERY owning trigger is a declared vocab event; and it is only TYPE-checked from an unambiguous single owner. Known, deliberate under-reports are documented on `eventOwners` (a node reachable from two chains passes if either matches; a `gameSignals` chain shadowed by a same-named `event` node is attributed an owner it never runs under).
  - Pinned by `tools/flow-spike/flowV2SignalScope.ts` (`pnpm --filter flow-spike run v2signalscope`, 17/17). Every validator assertion is paired with a **runtime** one first — the cross-event wire really does resolve `total: undefined`, the same graph wired from its own event resolves `total: 10`, the tap chain really does resolve `total: 7` — so each check is pinned against proven behaviour, not a theory, and the FP/FN pair above are permanent controls. Full v2 + fs harness suite re-run: no regressions (`v2choreo`'s stale `boardWithAnimateSymbols` assertion fails identically before AND after — unrelated, see its own task).
- 2026-07-17 — **Optional vocab params + the inspector's phantom default.** Authoring the new `cameraEffect` node reported two `unfilled-data-in` errors for `kind` and `blocking` even though the inspector showed `shake` and an unchecked box. Two independent bugs, both pre-existing and general:
  - **The vocabulary could not say a param is optional**, so `unfilled-data-in` fired on EVERY declared param — flagging the author for doing exactly what the param's own help invites. `enableSequentialReelStop` ("leave both unset ⇒ the coded constants") had the same latent error. New `ParamDecl.optional` → carried to `Pin.optional` by `derivePins` → skipped by the validator (§2 anti-drift: derived from the vocab, never stored on the node). Marked on `cameraEffect.durationMs/intensity/blocking` + `enableSequentialReelStop.gaps/speeds`.
  - **The inspector rendered a default it never stored.** `sourceFor` falls back to `defaultLiteral(pin.dataType)` so a fresh pin isn't blank, but only `onchange` commits — so ACCEPTING the default stores nothing and the pin stays unfilled. It bites hardest where the default IS the wanted value: re-picking an enum's first member (`shake`) or leaving a bool unchecked fires no change event, so **there was no interaction that could save it**. `durationMs`/`intensity` only escaped because the user typed `1`. An unset pin now renders honestly — enum ⇒ a disabled `— choose —`/`— default —` placeholder, numeric ⇒ empty with a `default`/`required` placeholder — instead of a value the doc doesn't have. Affects every enum param (`setSpecialSymbol.symbol`, `soundMusic.name`), not just the new node.
  - Also fixed: `derivePins` **dropped every action/cue param's `description`**, so `ParamDecl.description`'s own docstring ("surfaced as the pin's editor tooltip via the derived `Pin.doc`") was false for params — only event payloads carried it. Params now pass `description` + `optional` through one `paramIn` helper, and the Inputs list renders the doc as a `title` tooltip.
  - Pinned by `tools/flow-spike/flowV2OptionalParams.ts` (`pnpm --filter flow-spike run v2optional`, 6/6) — with CONTROLS, because the risk in this fix is a blanket mute: a bare `setWinBookEventAmount` must STILL report its required `amount`, and a wrong-typed optional literal must still report `literal-type` (optional means "may be absent", never "unchecked when present").
- 2026-07-17 — **`constants-shared` is now Node-resolvable (`exports` map + `type: module`).** It had neither, so its subpath imports resolved only under bundlers that guess extensions; plain Node/tsx could not load them, and Node treated the `.ts` as CommonJS so named ESM imports failed. This was invisible until `bookOf.ts` imported a `constants-shared/camera` VALUE — which **broke every flow-spike harness that loads `BOOK_OF_VOCAB`** while the launcher + game builds stayed green (Vite resolves it fine). The spikes had been quietly working around it for months ("constants-shared deps tsx can't resolve at runtime — import only TYPES"); that workaround is no longer needed. Verified: the full spike suite is back to its exact pre-change baseline (the same 3 unrelated harnesses fail before and after), and `launcher`/`lines`/`cluster` all build clean — `cluster` because the `./particleConfig` directory subpath is the one case the `./*` → `./*.ts` mapping doesn't cover and it needed its own entry.
- 2026-07-16 — **One home for tap-to-continue: `Scene.gate` retired + the last two ungated coded tap surfaces suppressed under v2.** The owner reported it was "confusing and difficult to track" that tap-to-continue could be activated from **two** editors. Root cause: **three** parallel copies of the same dim/prompt knobs. `Scene.gate` (`dimColor`/`dimAlpha`/`hidePrompt`) was the orphan — **no editor UI anywhere**, matched to a screen by a `visibleSource` string, feeding only the three coded gates, and under v2 two of its three read sites were already dead. Removed the field (`engine-layout/types.ts`), `normalizeGate` + its `normalizeScene` call (`editorStorage.ts`), and the three `$derived` reads + prop pass-throughs (`Game.svelte`). The remaining coded gates keep their own inlined defaults (`0x000000`/`0.5`/prompt shown) — no replacement knob invented, and their now-unpassed `Props` were deleted rather than left dead (the gates are never scene-bound, so nothing else could feed them). **`tapDimColor`/`tapDimAlpha`/`tapShowPrompt` on the overlay instance are now the single home for the gate look.** Also suppressed under `flowV2DrivesScreens`: `BookRevealGate` (was mounted unconditionally) and `Win.svelte`'s `PressToContinue` (was ungated) — under v2 the authored container's `tapToContinue` overlay owns dim+prompt+tap and `showContainer{awaitComplete}` owns the hold. Dropping Win's press cannot hang the round: its `winUpdate` `waitForResolve` is always resolved by the sibling `OnMount` (`startCountUp` → 300 ms → `oncomplete`), so the tap only ever SKIPPED the count-up.
  - **Honest scope — what this does NOT fix.** `BookRevealGate` provably never drew a pixel: `bookRevealGateShow` is **broadcast nowhere in the repo** (it exists only in the emitter vocabulary + its own subscriber), so `show` stayed `false` and `FadeContainer` (`{#if alpha.current > 0}`) rendered nothing. Gating it is dead-weight hygiene, **not** a black-overlay fix. The **only hardcoded black overlay that can still draw under v2 is `Win.svelte`'s big-win dim** (`CanvasSizeRectangle backgroundAlpha={0.5}`, gated on `isBigWin`), reached via the coded `setWin` handler — which the remake does **not** own and which, unlike `freeSpinTrigger`, carries no `getFlowV2()` guard. Left deliberately: it is win _presentation_, not a tap gate, and removing it would silently change big-win presentation. A black overlay seen on the **loading** screen is most likely the **authored** `tapDimAlpha` on that container's own tap overlay (author data — now editable in exactly one place).
  - **Not done here (each needs its own reviewed change):** the outro count-up split (`FreeSpinOutroGate` still solely owns `WinCountUpProvider` + `WinCoins` + the `freeSpinOutroState` publisher, so the `freeSpinOutroCountUp` action silently no-ops under v2), and the Phase-D gate deletion — deleting the gates before that split would strand the count-up for every non-v2 game (`apps/lines` dev, standalone Borut).
- 2026-07-16 — **Flow-v2 autosave is conflict-guarded, and the shared function library most of all**
  (Phase 1 of [multi-user-concurrency](../design/multi-user-concurrency.md)). Both the project
  FlowDoc and `_shared/flow-v2/functions.json` were unconditional whole-doc PUTs on an 800 ms
  autosave. The **library is the worse one**: its key is GLOBAL, so a Phase 2 project lease can
  never protect it and the ETag is the whole fix — and because it's read at page load and written
  at autosave, its read-modify-write window is the entire editing session. Two authors on unrelated
  projects each adding a function meant the second save erased the first's outright. Both now CAS
  on an ETag → 409 → autosave pauses, edits kept, Reload/Overwrite offered. Also closed a
  **cross-project** hole: `/api/flow-v2/save` resolves the project from the SESSION while the page
  resolves it from `?project=`, so a tab could hold project X's doc and save it onto Y — silently
  before, and (once conditional) disguised as an ordinary conflict inviting "overwrite", which
  would have destroyed an unrelated project. The client now sends its `projectKey` and a mismatch
  is **refused** (`409 scope-mismatch`, not forceable). ⏳ **Live-verify owed** (see editor status).
- 2026-07-16 — **Validation catches loop-scope authoring bugs at authoring time** (two live crashes on `bookofborutremake` that only surfaced at runtime). Both had the same root: the validator mapped bare `derivePins(node, ctx)` over the graph, so it saw neither the loop a node sits in nor what an incoming data EDGE carries. New `packages/engine-flow-v2/src/scope.ts` — `deriveGraphPins(graph, ctx)` resolves both (an exec walk from each `forEach`'s `body` pin gives each node its enclosing loop; the loop's element type comes from whatever feeds its `in`, edge-first, matching the runtime's `resolveDataIn` precedence) and threads them into `derivePins` as the new `PinScope` (`{ item, wiredIns }`). Consequences: (1) a `forEach.item` fed by wire (`gameSignals.winInfo.wins → forEach.in`, the normal way) is now TYPED, so wiring it into a typed pin (`item` (a `Win`) → `showWinLine.positions` (`list<Position>`) → `o.map is not a function`) reports `type-mismatch` AND is rejected by the connect gate — `/flow-v2` now derives its canvas pins, inspector pins and drag gate from the same `deriveGraphPins` the panel uses, so the drop gate and the panel can't disagree; (2) `$item.<member>` is checked against the scope struct, so a typo (`$item.pisition` on a `Win` → `undefined is not iterable` → the awaited action never resolves → the round hangs forever) reports `accessor-unresolved` naming the struct + its valid fields; an accessor that resolves is also type-checked against its pin. New `data-in-shadowed` warning code (+ panel glyph) for a data-in fed by BOTH an edge and a stored literal/accessor — the edge silently wins at runtime, which is what made bug 1 read as "my accessor is ignored". **Never guess** is the governing rule: every scope-dependent check fires only where the scope actually resolved (`$trigger`/`$input`/orphan-chain scopes stay accepted structurally), so nothing valid is flagged — verified headlessly against the user's real saved doc (the `pisition` typo fires; nothing else new) and against `LINES_FLOW_V2_DOC`, `SAMPLE_DOC` + `StaggerStop` (0 issues before AND after). Cyclic/self-feeding graphs are bounded (loop stack keyed walk + recursion guards), resolving to "unknown" rather than hanging.
- 2026-07-16 — **Fullscreen — BUILT (the SDK never had it).** Stake shipped the `jurisdiction.disabledFullscreen` permission flag but no mechanic: on Stake the casino wrapper owned the button, so the game only needed to know if it was allowed. (`OnPressFullScreen` in `components-layout` is unrelated — a canvas-sized transparent press target.) Now: `stateFullscreen.svelte.ts` in `state-shared` (toggle + `isFullscreenSupported` + an `active` flag driven ONLY by `fullscreenchange`, since Esc exits without touching the button), `fullscreen` in `ENGINE_ACTION_CATALOG` (→ projects an `onFullscreen` pin), `toggleFullscreen` book-of vocab command + `INTENT_COMMANDS` → the `fullscreen` intent, and the `Game.svelte` handler (`active` = real browser state; `disabled` when unsupported or `disabledFullscreen` — the first consumer of that jurisdiction block). **Authoring rule: wire `onFullscreen` DIRECTLY to `toggleFullscreen`** — browsers reject `requestFullscreen` outside a user gesture, and a `delay` (or any awaiting node) ahead of it spends the gesture. That the interpreter reaches `env.effect` synchronously from the press is load-bearing but invisible in the source, so it's pinned by `tools/flow-spike/flowV2Fullscreen.ts` (`pnpm --filter flow-spike run v2fullscreen`, 6/6, incl. a delay CONTROL proving the test can fail). **VERIFIED live** — authored on a real button and confirmed working in a real browser (2026-07-16); shipped in `_runtime/lines` at `184d6ad`. (The Browser pane cannot test it — it blocks fullscreen by Permissions-Policy, `TypeError: Permissions check failed` — so real-browser is the only check.) ⏳ The iframe `allow="fullscreen"` guard in `isFullscreenSupported` remains spec-correct but untested: it only matters once a casino embeds the game, and the pane reports `fullscreenEnabled: true` regardless, so it's no oracle.
- 2026-07-16 — `menuClose` HUD action, so an authored submenu can CLOSE itself. A submenu was already authorable with no engine change (any Scene-Editor screen auto-syncs into `doc.containers`, so `<menuButton>.onMenu → showContainer(Submenu)` works and suppresses the coded `stateUi.menuOpen` drawer), but the submenu's close button had no catalog action to bind → no pin → nothing to wire into `hideContainer`. Added `menuClose` to `ENGINE_ACTION_CATALOG` + label "submenu close" (`componentCatalog.ts`) and registered its handler in `Game.svelte` (`doCloseMenu`, routed via `routeActionThroughFlow` + an `invokeHostIntent` arm), so it projects an `onMenuClose` pin like every other action. Unwired it closes the coded drawer (parity). The editor's `action` dropdown renders from the LIVE catalog, so it appears on existing pinned button defs without a re-save.
- 2026-07-15 — **SHIPPED to `_runtime/lines`** (bookofborutremake verified live on `bundle.DXkuqJkp.js`).
- 2026-07-15 — `onTapToStart` fires from the TAP (refined). Initial "Option B" fired `tapToStart` from `dispatchFlowV2Complete` (only when a tap RESOLVED a screen complete — a held container / `complete:<id>`), so a flow that drives the start purely off the Game Signals `onTapToStart` pin (loading screen NOT held) never fired it, and the tap did nothing. Also `completeActiveScreen` is invoked by the feed-triggered `completeOnLoaded` (auto-advance, no tap), which must not fire it. Now `fireTapToStartOnce()` fires from the genuine player tap in `TapToContinue.svelte.onTap`, decoupled from the complete path — so it runs regardless of held-ness, and never on `completeOnLoaded`. Same one-shot latch (reset per boot in `setFlowV2`); later free-spin gate taps don't re-fire. Verified `tools/flow-spike/flowV2TapToStart.ts` (11/11, incl. the tap↔complete decoupling).
- 2026-07-15 — `onTapToStart` pin made live + music is flow-driven ("Option B"). The runtime only ever dispatched `load`, so the Game Signals `onTapToStart` pin was dead; now it fires (see the refinement above). `Sound.svelte` no longer auto-plays `bgm_main` at boot when a v2 flow drives screens (gated on the canonical `flowV2DrivesScreens()` doc-level predicate in `flowV2Runtime.svelte.ts` — a synchronous mirror of Game.svelte's `ownsEvent('load')` decision, usable before the handle exists); the reference `LINES_FLOW_V2_DOC` wires `onTapToStart → soundMusic(bgm_main)`. Non-flow / v1 / v2-not-driving paths are byte-identical (music still boots as before).
- 2026-07-15 — sound-cue `name` inputs are DROPDOWNS. `soundMusic`/`soundOnce`/`soundLoop`/`soundStop` cue params are now enum-typed (`MusicName`/`SoundEffectName`/`SoundName`), so the `/flow-v2` inspector offers the game's real sound names instead of free text. The enum values are codegen'd from `apps/lines/src/game/sound.ts` into `packages/engine-flow-v2/src/reference/soundEnums.generated.ts` — regenerate after adding a sound: `pnpm gen:flow-v2-sounds` (`:check` gates staleness). Authoring-fidelity only.
- 2026-07-15 — Node pins carry authored help. Optional `description` on `EventDecl`/`ParamDecl` (vocab) → derived onto `Pin.doc` → a theme-styled hover card in `FlowV2Node.svelte` (replaces the plain `title`). Authored for all 13 book-of Game Signals + their payload fields in `BOOK_OF_VOCAB`; the same `doc` also surfaces on standalone `event` nodes. Purely additive/optional — pins without a `description` keep the plain label tooltip.
- 2026-07-14 — Generic `showMessage` Flow effect (Info Bar / toast producer), authored into both reference docs ([detail in history](../history.md)).
- 2026-07-13 — v1 Flow editor teardown: `/flow` route retired, v2 is the only editor ([detail in history](../history.md)).
- 2026-07-10 — flow-v2 flatten re-namespaces colliding group-body node ids (fixes press-Spin → free-spin-intro); shipped via `_runtime/lines` ([detail in history](../history.md)).
- 2026-07-09 — flow-v2 generic overlay round-block HOLD + container z re-stamped from `docLayerZIndex`; drag-off-pin contextual spawner ([detail in history](../history.md)).
- 2026-07-08 — flow-v2 cutover track (parity + translator + game-side dispatch), Collapse-to-Group, Game Signals node, container events as exec-out pins, intent-command HUD actions ([detail in history](../history.md)).
