# Engine & games — status

> Design: [flow-driven-game](../design/flow-driven-game.md) · [live-assets](../design/live-assets.md) · [games-deploy](../design/games-deploy.md) · Agent: `.claude/agents/engine-pixi-svelte.md`

**One-line state:** Shipped — the shared runtime (`runtime:lines`, built from `apps/lines` + `packages/*`) carries every scoped feature on `main`; the `apps/{lines,cluster,ways,scatter,number-picker,price}` are dev/reference games, and `lines`/`bookOf`/`ways` have rich (art-bearing) reference scene sets.

## Current state

What runs on `main` today (this is the ENGINE side — the runtime + reference games, not a launcher tool). Online games run the SHARED `_runtime/lines` bundle; `apps/lines` is its source.

- **Symbol sizing** — sprites are plain `contain`-fit to the cell by their native art aspect; SPINE symbols contain-fit the rig's authored box (`skeleton.{x,y,width,height}` — the Rigger's Bounds frame), CENTRED on that box via `<SpineProvider centreBox>` (2026-09-02), to `SYMBOL_SIZE × SYMBOL_SPINE_FILL` (spine-only shrink constant, `apps/lines/src/game/constants.ts`, started 0.5). The old `sizeRatios` param + the Scene Editor "Symbol size (× cell)" control were removed from the render equation (size comes from the ART, not a number; oversize spines are fixed by tightening the rig bounds in the Rigger). Cross-origin alpha-trim of sprite textures was tried and reverted (tainted `getImageData` — don't retry).
- **Persistent background scene (coded path only)** — without a screen-driving flow, a `space:'background'` scene renders behind the game for the whole session (`engine-layout/backgroundScenes.ts`), cover-fit as one group so multi-part backgrounds keep their real size + animate (`ComponentInstance` cover path). Under a v2 flow that drives the screens it is an ordinary container the flow shows/hides — the space is only the coordinate frame (2026-09-02, see [flow status](flow.md)).
- **Per-node cover-fit (`node.coverFit`)** — a cover-capable node in a normal `canvas`-space (FLOW-GATED) scene can opt into true cover-fit to the window via a `coverFit` boolean on `BaseNode` (`engine-layout/types.ts`), reusing the exact `background` cover math (`coverTransform` + `backgroundCoverScale`/`backgroundFit`/`scale` stretch) WITHOUT the always-on persistent mounting of `space:'background'` — so a free-spin intro screen can host a full-screen stretched animation the flow still shows/hides. **Which kinds cover is ONE exported list, not a condition repeated at each gate:** `isCoverFitKind` (sprite / spine / flipbook) and its texture-measured subset `isCoverArtKind` (sprite / flipbook) in `coverTransform.ts` — every gate below reads them, because when the list was spelled out per-gate the `flipbook` kind reached none of them. Runtime gate `isCanvasCoverFit`/`isCover` in `LayoutNodeView.svelte` (a `coverFit` spine is force-CENTRED on the canvas via `spineCoverCenter`, matching the sprite cover + the editor — it ignores authored x/y, since a flow screen places it anywhere); editor previews it WYSIWYG (`EditorCanvas.nodeTransform` + `isBackgroundCover`, `EditorSpineLayer` `coverFit` target, `+page.svelte` `isBackgroundCoverSelected`); authored via a "Cover / full-screen fill" toggle in `EditorProperties.svelte` (canvas-space cover-capable kinds only) that reveals the existing cover-scale/fit/stretch controls. Additive — `coverFit` unset is byte-identical, `background` scenes unchanged. Verify offline: `node packages/engine-layout/scripts/test-cover-fit.mjs`. Persistence: node fields pass through `editorStorage.normalizeNode` + `identityComponentRoot`'s child-preserving copy untouched (no allowlist), and `markDirty` always bumps the canvas redraw nonce, so no whitelist change was needed. Reaching a live game needs a runtime release + reconcile.
- **Bitmap-font layout text** — `LayoutNodeView` renders `<BitmapText>` for authored bitmap fonts via `registerFontCatalog`; coded HUD/label text honors authored bitmap fonts through the shared `engine-layout/CatalogText` component (one source, canvas `<Text>` + `<BitmapText>` can't drift). `pixi-svelte`'s `<BitmapText>` applies three font guards before pixi sees the text (`sanitizeBitmapText.ts`): a synthesised space glyph + unbaked-glyph sanitisation (the two black-screen guards) and `normalizeBitmapFontMetrics`, which corrects a descriptor that under-reports its glyph box so `fontSize` means the same thing for every font (the bundled `silver` declares 97 for 199px glyphs). The launcher's `EditorTextLayer` calls the same normaliser so the editor canvas can't drift from the game.
- **Component value/defaults + HUD readouts (B1–B4)** — the component value/defaults system + the live Balance/Win/Bet readouts now render as `hudReadout` component INSTANCES in `apps/lines` (B4.1–B4.4 flip, shipped 2026-06-08: `readoutNode()` emits `componentInstance` nodes, `LayoutEditable` mounts them + suppresses the coded snippets via `coveredSources`; `hudScenes({readouts:true})` is on for lines, off → coded `UiLabel*` binds for Borut/editor-picker = parity). Remaining tail: the **Borut mirror (B4.6)** — a submodule bump — and owner live-verify.
- **Default-HUD fallback (Phase 1, 2026-08-06)** — a project seeded with the legacy CODED `hudBar` (`UiLabel*`/`UiButton*` `bind` nodes) rendered BLANK under a flow-v2-driven game: those names aren't registered bound components (only the coded `<UI>` drew them, from the scene positions), and `de772aec` suppresses the coded `<UI>` under `flowV2DrivesScreens` so `<FlowV2Mount>` mounts them as empty containers. Fix: `normalizeHudScenes(doc)` (engine-layout) rewrites each legacy `hudBar` node → its parametric `componentInstance` (`hudReadout`/`button`) at doc load (`loadEditorScenes`), preserving the transform byte-for-byte via `labelBindToInstance`/`buttonBindToInstance`; the parametric HUD renders through the generic mounter in BOTH paths. Runs in memory (no re-save) so every old project self-heals; strict no-op for docs already parametric or with no `hudBar` (`scripts/verify-hud-fallback.mts` = 14/14). The parametric HUD is the go-forward default; the coded `<UI>` HUD is slated for retirement. Phase 1 shipped to `main` + runtime-released 2026-08-06 (PR #252), verified live on `test2` (bottom bar back). **Phase 2 (corners) — DONE (unmerged):** `HudGameName`/`HudLogo` are now REGISTERED bound components (`components-ui-pixi`), so under a flow-v2-driven game `<FlowV2Mount>` mounts the reserved `hudCorners` scene and `LayoutNodeView` resolves those binds (they mounted as EMPTY containers before — the logo/game-name twin of the Phase-1 blank bar, the open item from the 2026-07-24 double-mount fix below). The coded `<UI>` `gameName`/`logo` snippets now render through the SAME two components (one home, no drift); `hudCorners` is a RESERVED scene so no double-mount in the coded path. Phase 2 SHIPPED + runtime-released (PR #255), verified live on `test2` (clock + game name + logo back). **Phase 2b — DONE (unmerged):** the persist + reset half, in the launcher/editor (auto-deploys on push to `main`, no runtime release). (a) `editorStorage.normalizeDoc` now runs `normalizeHudScenes` — same migrate-on-read shape as `backfillAlwaysOnTop` — so the editor SHOWS the flow-v2-safe parametric HUD and the next save PERSISTS the migrated doc (and `/api/editor/doc` serves it migrated to the runtime too, before `applyHudGameNameDefault`). (b) the editor "↻ Refresh HUD layer" action → "↻ Reset HUD to default", now emitting `hudScenes({ readouts: true, buttons: true })` (was `readouts` only, which re-seeded coded `UiButton*` binds that go blank under flow-v2). Verified: `launcher-api` build green. Remaining: Phase 3 = client-scoped default HUD layer.
- **Per-sheet editor-art namespacing** — editor-art regions are scoped `<assetKey>::<region>` so a new sheet no longer shadows an older same-named region.
- **Generic doc-driven scene mounting (Editor §20.1)** — an author's new Scene Editor screen (custom id, non-background space) ships in the game without being hardcoded in `Game.svelte`: `engine-layout/genericMountScenes.ts` `extraMountScenes(scenes, reservedIds)` renders them in doc order behind a parity-gated `{#each}` (empty reserved-set ⇒ byte-identical). FlowDoc-authored screens are folded into the reserved set to avoid double-mount.
- **Buy-feature (Select-Feature) screen — authored, data-driven** — the buy-bonus SELECT menu can render in-canvas from config instead of the HTML modal. A `featureCard` builtin component (`engine-layout/builtinComponents.ts`) is the per-card prefab (panel + icon + title/description/price/button, with the variable parts as `engineProvided` params + a `select` signal). A new `repeater` layout node (`engine-layout/types.ts` `RepeaterNode` + `Repeater.svelte`) stamps one component instance per item of a named data source, resolved through `registerRepeaterSources` (the list sibling of `registerComponentValues`), laid out row/grid; its `componentId` rides the bake/pull chain via `collectComponentIds`. `apps/lines` registers the `featureCards` source from `stateMeta.betModeMeta` (non-`default` bet modes), feeding each card its values + an `onSelect` that preserves the coded HTML `BonusCards` press contract (`stateBonus.selectedBetModeKey` + `buyBonusConfirm`). **Authorable online:** the Scene Editor has a Repeater palette element, an inspector (data source / component / row·grid·gap·columns) and a labelled canvas placeholder, round-tripped via `editorStorage`'s `NODE_KINDS`. **It is now the DEFAULT buy-bonus SELECT surface for every game — no flag, and the HTML select modal is DELETED** (`ModalBuyBonus`/`BonusCards`/`BonusCard` + the orphaned `BonusContentWrap*` are gone from `components-ui-html`; only the HTML CONFIRM step `ModalBuyBonusConfirm` remains, pending Phase 2). The shared, state-agnostic `<BuyFeatureScreen>` (`engine-layout/BuyFeatureScreen.svelte`; props `open`/`onDismiss`/`zIndex`/`scene`) mounts the scene as a canvas takeover; `defaultBuyFeatureScene()` (`engine-layout/buyFeatureScene.ts`) is the code-fallback, seeded into every reference layout (lines, `engineSkeleton` for ways/cluster/scatter, bookof), so a fresh project of any type ships an authorable page. The state wiring is `registerBuyFeature()` in `components-ui-html` (the home of `stateBonus`; `engine-layout` never imports `state-shared`) — it registers the `featureCard` def + the `featureCards` source; each app calls it once and mounts `<BuyFeatureScreen open={stateModal.modal?.name === 'buyBonus'} …>`, keying visibility DIRECTLY on the modal state (no redirect effect). A card press advances to `buyBonusConfirm`, a backdrop tap clears the modal. Card COUNT = number of non-default bet modes; per-card icon is the bet mode's editor-art key. **The CONFIRM step is also in-canvas now (Phase 2 done):** a generic, reusable `confirmDialog` component (`builtinComponents.ts` — panel + title + message + confirm/cancel buttons, all `engineProvided`; `confirm`/`cancel` signals) rendered by a shared state-agnostic `<ConfirmDialog>` (twin of `<BuyFeatureScreen>`), seeded as the `buyConfirm` scene. Two-button routing added `BaseNode.pressAction` + a component-press context (`componentActionsContext`) so N named buttons route to N callbacks — the generalization of Phase 1's single `onSelect`; a mount injects per-instance values + actions via `instanceBindingContext` (single-instance sibling of `<Repeater>`'s per-item props). `<BuyBonusConfirm>` (`components-ui-html`) supplies the buy wiring, preserving the deleted `ModalBuyBonusConfirm` contract exactly (`activeBetModeKey` → `bet` for buy / infinity limits for activate → close; cancel/backdrop → back to select). The HTML `ModalBuyBonusConfirm` is DELETED — the buy flow is fully in-canvas. **Deferred:** a bet-mode/dialog image URL doesn't preload/paint in pixi yet (binding is correct); Phase 3 = author the select→confirm→buy/cancel flow in the Flow editor (reusable for any confirm-gated action). Slices: #207 (repeater+card) + #208 (editor authoring + scene) + #214 (default/delete-HTML select) + Phase 2 (ConfirmDialog / delete-HTML confirm). **Per-card param overrides (branch `feat/per-card-param-overrides`, unmerged):** the config can now vary ANY param of the SHARED card component PER bet mode — `BetModePresentation.cardParams` (a generic `Record<string, string|number|boolean>`) rides `resolveBetModes` → `BetModeData.cardParams` → the repeater item's `values`. The core engine change is in `ComponentInstance.svelte`: the per-item value resolver's gate dropped from `param.engineProvided` to `param.key in engineValues`, so a per-card `panelImage`/`spineKey`/`buttonImage`/tint wins over the component's authored default while a param absent from the map keeps it (parity; the `<ConfirmDialog>` binding is unchanged since only its own engineProvided keys are `in` the map). Art/spine keys named in `cardParams` ride the ship chain via `betModeCardParamRefs` + `editorArtExport` (classified by the resolved card def's param kind; default `featureCard` seeded when a mode overrides without a custom card). `/config` gained a per-mode "Card graphics" editor (typed inputs for the card's authorable params). So one `featureCard` renders visually-distinct cards without a component per card. Verified: game-config typecheck, engine-layout/lines/launcher-api/cluster builds, an offline fixture (normalize round-trip + resolver precedence). Needs a runtime release to reach live games. **Buy-feature/confirm SCENE ROLES (branch `feat/buy-feature-scene-role`, unmerged):** `Scene.role` gained `'buyFeature'`/`'buyConfirm'` (`sceneRole.ts` + `buyFeatureSceneId`/`buyConfirmSceneId` helpers), so an owner can TAG any authored scene as the buy SELECT / CONFIRM screen — the old lookup was hardcoded id `buyFeature`/`buyConfirm`, which existing projects (editor mints random ids; those scenes are seeded only into fresh docs) never had, so a tagged feature-select scene fell through to role `basegame` and rendered as an always-on base-game overlay. `Game.svelte` now resolves the buy scenes ROLE→id→seeded-fallback (parity) and adds the role-resolved ids to `reservedSceneIds`, so a role-tagged scene mounts ONLY via its `<BuyFeatureScreen>`/`<BuyBonusConfirm>` takeover (gated on the buy modal) — never double-mounted by the generic overlay path. Editor Role dropdown + the `editorStorage` save whitelist gained both roles (the whitelist is load-bearing — without it the role is silently dropped on save). A `standard`-space role-tagged buy scene renders correctly (`<LayoutScene>` wraps it in `<MainContainer standard>`). Verified: engine-layout/lines/cluster/launcher-api builds + an 11-assertion offline fixture (role→id→default precedence + role-tagged scenes excluded from generic mount over the real modules). Needs a runtime release + the owner tagging their scene, then a live re-check.
- **Buy CHARGE follows the SELECTED mode's cost (branch `feat/fix-buy-cost-per-mode`)** — the in-canvas buy menu DISPLAYED each card's price correctly (`betAmount × mode.costMultiplier`, `registerBuyFeature`) but every card was CHARGED a fixed premium (100× = Gold Rush's cost) regardless of the tapped card. Root cause: the Play4Fun facade's book-path bet body collapsed all buy modes to a single `isBuy` boolean (`context: [isBuy ? 1 : 0, betPerLine]`, `packages/rgs-translator-eagaming/src/engineFacade.ts`), and the book mock (`scripts/mock-rgs-server-book.mjs`, which `services/test-server` imports = Borut's live RGS) hardcoded `total = betPerLine × NUM_LINES × (isBuy ? 100 : 1)` — so the cost multiplier the card showed never reached the charge. Fix: the engine publishes each mode's `costMultiplier` to `globalThis.__IE_BET_MODES__` from `syncBetModeMeta` (`apps/lines/src/game/betModeMeta.ts` — the decoupled-global bridge, mirror of `publishWinLevelsToFacade`, coupled to the buy MENU since both come from that one sync); the facade sends that per-mode multiplier as `context[0]` (0 = normal spin); the mock uses `context[0]` AS the cost multiplier (`total = betPerLine × NUM_LINES × cost`). So the debit now equals the card's displayed price (High Noon 25 / Bull Chase 50 / Gold Rush 100 at $1 bet). The imperative + flow buy paths share `commitBuyBonus`/the same bet, so both are fixed identically; a normal spin still sends `context[0] = 0` → 1× (parity). Verified: rgs-translator-eagaming typecheck + `apps/lines` build green; an offline fixture (`scripts/verify-buy-cost.mts`, run with tsx) drives the REAL book mock over HTTP (per-cost charge = 100/2500/5000/10000 cents) AND the REAL facade `requestBet` (context[0] = 25/50/100 per mode, 0 for BASE) — all 8 assertions pass. Live re-check: a real buy per card against the mock; the standalone `bookofborut` bundle needs an engine-submodule bump, the remake rides the shared runtime + a `services/test-server` deploy.
- **Win-line renderer in the SHARED engine** — ported from the standalone Book of Borut into `apps/lines` (`806d6cf`): `WinLine.svelte` (Graphics polyline, optional layered-stroke glow, optional `svelte/motion` animated draw, bitmap win-amount text), resolved from the sparse `bundle.symbols.winLine` (`bakedWinLineConfig()`/`bakedWinLineEnabled()`, runtime→baked precedence). Default-ON, so every `runtime:lines` game draws it unless disabled in `/symbols`. **Winning SYMBOLS keep animating on the resting board** (`apps/lines/src/game/winSymbolCycle.ts`, default ON): once a round's book is fully presented the game re-lights that spin's winning cells through the same `animateSymbols` leaf the round used, until the next bet stops it. Symbols ONLY — the line and its stamped amount are never replayed. Authored via the doc-level `winCycle` in `/symbols` (its own section, independent of the win-line toggle).
- **SPIN HOLD — a mid-round pause the spin button releases** (2026-08-17, off by default). New shared primitive in `state-shared` (`stateUi.spinHoldActive` + `armSpinHold`/`releaseSpinHold`/`cancelSpinHold`, read as `hasSpinHold()`): the INVERSE of `celebrationLock`/`unskippablePresentationActive`, which make the button inert — this makes it LIVE mid-round, keys it `spin_default`, and routes its press to release the hold instead of betting or slamming. All four decisions stay in the one chokepoint `utils-shared/spinStop` (`getSpinButtonKey` / `getSpinPressSound` — a hold-releasing press gets the bet whoosh, not the stop cue / `runSpinOrSlamStop` / the new `isSpinButtonSpinning`, which also DE-DUPLICATES the rolling-frame derivation that `ButtonBetProvider` and Game.svelte's parametric `spin` action had each written separately), so the Space hotkey, the flow `spin` action and an invoked intent inherit it for free. Its first consumer is the free-spin between-spins hold (`apps/lines/src/game/freeSpinHold.ts`, `winCycle.holdAfterBigWin` — **detail in `docs/status/symbols.md`**), but the primitive is game-agnostic: any presentation that wants to park a book on a player press can arm it. `playBet`'s `finally` cancels a stranded hold alongside the other belt-and-braces latch resets.
- **Book-of scatter is a pure trigger (no pay)** — the scatter/book match now ONLY triggers the free-spins feature; it no longer pays out (`cf1db93`). This is BOOK-OF-ONLY: the change lives in the book mock RGS (`scripts/mock-rgs-server-book.mjs`, which `services/test-server` imports), which emits the scatter win with `pay: 0` (kept as a zero-pay win so the scatter symbols still glow and the trigger count/positions still reach the client; `SCATTER_PAY` is now only the qualifying-count gate 3/4/5). Covers the buy-bonus first spin and natural base-game triggers; the retrigger path never credited the scatter pay. The plain `lines` mock (`scripts/mock-rgs-server.mjs`) is untouched and still pays scatters anywhere — it has no free-spin trigger. Paired info-bar note: on `freeSpinTrigger` the engine shows **"N Scatters award N Free Spins"** (count from `bookEvent.positions`, award from `bookEvent.totalFs`; `apps/lines/src/game/bookEventHandlerMap.ts`, `showMessage` info line). That handler is shared `apps/lines` code, so a hypothetical non-book game that emitted `freeSpinTrigger` would also show the message (harmless/accurate) — but the no-pay behaviour is strictly the book mock's. Verified locally by driving a buy round against the mock (scatter `pay=0`, `spinTrigger`/`enterBonus` still fire). Reaching the online Borut remake needs a runtime release + a `services/test-server` deploy.
- **Full-screen camera effects** — `shake` / `flash` / `zoomPunch` / `chromaticWobble`, authored from Flow as ONE `cameraEffect` action with a `kind` dropdown (adding a flourish must not grow the palette an entry each). Implemented engine-side in `pixi-svelte/src/lib/cameraEffects.ts` against the **`Application.stage`** — `<App>`'s children are flat z-banded siblings, so the stage is the only transform that moves board + HUD + overlays together (it moves `<DebugStage>` too; accepted). Effects **compose**: each contributes a per-frame sample and one compositor applies the sum, so overlapping effects can't fight over `stage.position`; the resting transform is captured up front and restored exactly when the last effect drains, and the ticker callback is removed at rest (zero idle cost). `intensity` is a **multiplier of a per-kind reference** (24px / 8% / 0.85 alpha / 12px), not pixels — capped at 4. `blocking` (default false) picks whether the interpreter awaits it. Kinds live in `constants-shared/camera` and are read by BOTH the vocab enum and the implementation, so the editor can't offer a kind the game would no-op. Off unless authored (no FlowDoc references it yet ⇒ byte-parity).
- **Free-spin sequential reel stop** — a timing-only `sequential` spin mode (reels settle consecutively in free spins, no anticipation glow/SFX). Off by default (byte-parity when unused); Flow-toggleable via `enableSequentialReelStop`/`disableSequentialReelStop` effects with per-reel `gaps`/`speeds` (`list<float>`). Engine plumbing added a `'sequential'` `SpinType` member across `utils-slots`. **TURBO WINS over it** (`createEnhanceBoardSpin`): sequential stop is pacing the feature imposes, turbo is the player asking for pacing to be dropped, so a held turbo takes the reel back to the global `fast` spin type instead of the free-spin mode silently overriding it.
- **Reel anticipation — client-computed MODE (Phase 2 of `docs/design/reel-anticipation.md`)** — the server-driven `anticipation[]` path is DELETED (the field on every game's `BookEventReveal`, the facade's `anticipation: reels.map(()=>0)` emit, the `createEnhanceBoardSpin` read, and the `Anticipation(s).svelte` server overlays + their `Game.svelte` mounts across `lines/ways/scatter/price/cluster`; story fixtures stripped of `anticipation:[…]`). Replaced by a CLIENT calculator: `createEnhanceBoardSpin.spin` takes an optional `computeArming(reelIndex) → {level, tier}|null`; armed reels drive the MIGRATED HOLD (the `anticipated` spinType → `reelPaddingMultiplierAnticipated`, unchanged) and set new `reelState.anticipationLevel`/`anticipationTier` (replacing `anticipating`; inert on cascading reels). `apps/lines/src/game/anticipation.ts` builds a `createLinesReach` from the FINAL board + config (paylines/paytable/wild/scatter/`numLines`, feature-trigger count from the scatter row's `occurs`, big thresholds via `gameConfig.activeBigTierThresholds()`) and arms on `stateGame.anticipationMode` (default OFF) with `anticipationConfidence` (`possible`=max bound / `guaranteed`=min) + `minAnticipateReel` (2). **OFF by default ⇒ byte-parity** (no policy ⇒ the whole block is skipped; the spin decision is identical to the old server-zero case — proven by a node reasoning check). **Sequential-reel-stop preserved**: with the server flag gone the sequential path's `!isAnticipated` is now `!isArmed`, which is `true` for every reel when the mode is off, so shipped sequential behaviour is byte-identical (turbo still wins). **SHIPPED 2026-08-05** (PR #229 → `main`, runtime-released to `_runtime/lines`; release.json `commit` = `94a35ce8`, `status: released`). Full stack landed: Phase 3 presentation (`Anticipation.svelte` per-reel overlay spine stack + `Anticipations.svelte` grey-out dim rects + `AnticipationCamera.svelte` zoom, escalating by `AnticipationTier` big→mega→massive via `anticipationPresentation.ts`); Phase 4 Flow authoring (`enableAnticipationMode`/`disableAnticipationMode` effects + `/flow-v2` palette in `bookOf.ts`, payload `confidence`/`minAnticipateReel`/`greyOut`/`zoom`, per-screen swappable); Phase 5 `/symbols` "Reel anticipation" panel authoring the per-tier FX + overlay spine through the full export→bake→pull→register chain, with the coded `ANTICIPATION_TIER_FX` kept as the un-authored fallback (`resolveTierFx`/`resolveAnticipationSpineKey` — the single choke point). Still OFF by default ⇒ byte-parity. Verified: builds green, reachability fixture + schema round-trip pass, code-reviewer clean, playtester confirmed arming math + overlay/grey-out mount in the Pixi scene graph — the animated hold (reels holding + zoom easing) still needs a foreground tab to eyeball (rAF frozen headless). Standalone `bookofborut` (own bundle) needs an engine-submodule bump to receive it; the remake rides the shared runtime. Design + full plan: `docs/design/reel-anticipation.md`. **Follow-up (branch `engine/anticipation-dynamic-tiers`, not yet shipped):** the hardcoded big/mega/massive tier triple is now GENERIC — the tiers MIRROR the config's big-win tiers. `AnticipationTier`/`ReelAnticipationArming.tier` widened to an opaque `string` alias; `gameConfig.activeBigTiers()` is the source (`winLevels` `type==='big'`, ascending, `{alias,name,threshold}`); arming stamps the reached tier's ALIAS; `ANTICIPATION_TIER_FX`+`tierForLevel`+`TIER_RANK` replaced by a coded RAMP `codedTierFx(rank,count)` interpolated across the configured big tiers; `resolveTierFx(alias)` merges the alias-keyed authored override over the ramp; `activeMaxTier()` returns the highest active reel's alias. Baked/schema `anticipation.tiers` is now an alias-keyed record; the `/symbols` panel renders one FX column per config big tier (via `resolveBigTiers()` in `+page.server.ts`). See `docs/status/symbols.md`.
- **Stacked-picture reel mode (LINES, visuals-only)** — the KA-Gaming "tall symbol" look: a contiguous vertical run of the same eligible symbol on a settled reel draws ONE tall picture (the symbol's new `stacked` state art, sprite/spine/flipbook) over the run, cropped to the top `runLength ÷ naturalHeight` and top-aligned. Per-symbol natural heights + eligible set in `constants.ts#STACKED_PICTURE` (H1=2…W=5). Client-inferred from the board (no protocol change). `stackedPictureRuns()`/`stackedCoverage()` `$derived` scans on `stateGame.svelte.ts`; `StackedPicture(s).svelte` render + rect-mask crop, mounted in the resting board container; `ReelSymbol` hides covered cells (no doubling). New `stacked` member in `engine-layout` `SYMBOL_STATES` (+ `LINES_SYMBOL_STATES` gates the `/symbols` column to lines); `getSymbolInfo` falls `stacked`→`static` unauthored. Flow-toggled via `enableStackedPictures`/`disableStackedPictures` (payload `symbols`/`highPayOnly`/`minRun`; `bookOf.ts` vocab). **OFF by default ⇒ byte-parity.** Test data: `scripts/mock-rgs-server.mjs` `STACKED=1` deal (high-symbol runs + full 5-tall WILD) + `WILD→'W'` in the lines facade. Verified: Node scan fixture 12/12 (owner example 3/4, WILD 3/5 & 5/5, 3×3/10×10 scale) + live Pixi scene structure (Container + stretched Sprite + rect mask). Follow-ups: real tall art + `/symbols` `stacked` authoring, wild-substitution runs, win-math, a real RGS emitting stacked boards. Design: `docs/design/stacked-picture-mode.md`.
- **Three-knob reel grid** — the conflated `reelPadding` was split into three orthogonal `ReelGridNode` knobs, all default-to-identity (byte-parity): reel/row **LEAD** (`reelPadding`/`rowPadding`, seats the cluster), seat **ALIGNMENT** (`symbolAlignX/Y`, art within its cell), board **NUDGE** (`boardNudgeX/Y`, px offset). Editor `drawReelGrid` mirrors the game math exactly (editor↔game < 1e-12 px).
- **Slam stop (mid-spin quick stop)** — ALWAYS ON, no config toggle. A press while a round runs snaps the reels to the already-resolved result AND fast-forwards the post-reveal presentation (count-ups jump to the final total, win lines draw complete, player-gated holds release) so the machine returns to idle promptly. The token is scoped to the **SPIN**: in `apps/lines` the re-arm hangs off `updateFreeSpin`, the FIRST event of every free spin (not the `reveal` — the counter update leads the spin it labels, so re-arming at the reveal left the next spin's counter presented under the previous spin's tripped token), so a press slams only the spin that is rolling and the feature then resumes at full pace — normal roll, normal anticipation, normal count-up — until the next press. (Base-game rounds have a single reveal, never re-arm, and keep the round-scoped behaviour. The other five dev games are still round-scoped: their bonus mechanics are cascades, and the owner's direction was about the Book-of feature.) During **autoplay** the first press slams the current spin only; a **second** press cancels the remaining sequence (autoplay is therefore always cancellable, in at most two presses). Built on a new `utils-shared/skipToken` primitive + the shared `utils-shared/spinStop` press/key helper (which de-duplicates the spin/stop logic that was copied verbatim into `ButtonBetProvider` and `Game.svelte`). Skipping never DROPS a beat: every book event still runs, fully awaited, and applies its effect at final value — only the time is removed. **Two presentations are carved out as UNSKIPPABLE** (`apps/lines/src/game/unskippablePresentation.ts`): the book / expanding-symbol reveal (`setExpandingSymbol`) and the free-spin intro (`freeSpinTrigger`) run to completion, because their visual is a spine/`.irig` timeline that a released wait leaves playing detached over the next spin. Their player-gated tap holds still release on a slam (a slam makes the spin button inert, and it swallows the tap). **A slam is not a blank:** a slammed spin that WON keeps a minimum win display — the lit win symbols and the per-win info message, ~600ms each — while the win line and the amount count-up stay fully skipped. **A slam NEVER skips a CELEBRATION** (owner direction 2026-07-30): the big-win overlay (`setWin` at a `big` win level), the free-spin **outro** (`freeSpinEnd`) and the **retrigger** flourish (`freeSpinRetrigger`) must play in full — a slam snaps the reels, it does not fast-forward the show. These are NOT added to the UNSKIPPABLE set (that keeps the token TRIPPED and only stretches cues, so it can't protect the outro's player-gated count-up or a `showContainer{awaitComplete}` hold, both released-on-skip to dodge the slam hang). Instead the slam token is **RE-ARMED right before a celebration** (`startsCelebration` in `unskippablePresentation.ts`, applied by `runBookEventPresentation` on the same seam as the per-spin re-arm), so it presents on a FRESH token — byte-identical to a round nobody slammed (cues wait in full, player-gated holds wait for the real tap, nothing can hang because the hang only exists while the token is tripped). ORDINARY wins keep fast-forwarding — only a `big`-level `setWin` re-arms, small/medium wins still slam. Covers coded, v1-flow and v2-flow (the seam wraps all three), so the flow-v2 Borut remake gets it too after a `runtime:lines` release. **The INTRO's scatter-match gap** (owner report 2026-07-30): the free-spin intro's scatter-symbols match animation plays at the START of `freeSpinTrigger`, BEFORE the intro screen mounts, so the screen-driven celebration lock is still open and the button is a live STOP — a slam there tripped the token and collapsed the intro's player-gated `freeSpinIntroUpdate` hold. Fixed two ways: (a) `freeSpinTrigger` was added to `startsCelebration` so the re-arm clears an UPSTREAM reel-roll slam; (b) `runBookEventPresentation` now mirrors its unskippable `depth` (0↔1 edges) into `stateUi.unskippablePresentationActive`, and `spinStop.isCelebrationLocked()` ORs `hasUnskippablePresentation()` — so the button is inert for the WHOLE unskippable window (scatter match + intro + book reveal), stopping a slam WITHIN the window. `playBet`'s `finally` force-clears the flag (belt-and-braces vs a stuck lock). Verified at token/flag/button level (game-playtester): re-arm clears an upstream trip, window-lock suppresses `roundSkip.skip()` while true, nested resume-path replay doesn't clear the outer window early, ordinary wins unaffected, idle flag false after round. **The press has its own SOUND:** a mid-round press broadcasts `soundPressStop` (`sfx_btn_stop`) instead of the bet whoosh — ⚠️ currently a STAND-IN region, see open item 5. **Celebration lock:** during a non-skippable celebration — free-spin **intro**, free-spin **outro**, or the **win panel** (WinGate = big-win presentation) — the spin button goes fully inert (it can't slam-skip the celebration); it renders as a greyed STOP. Read at the single chokepoint `utils-shared/spinStop` via `hasCelebrationOverlay()` (`getSpinButtonKey` → `stop_disabled` key; `runSpinOrSlamStop` early-returns as belt-and-braces for the flow `spin` action / Space hotkey / intent callers; `isSpinButtonDisabled` helper de-dupes the disabled check across `ButtonBetProvider`, the authored HUD `spin` action + Space hotkey in `Game.svelte`). ⚠️ **Signal source matters (learned the hard way, live-debugged on the remake):** the lock does NOT read the `stateUi.*Show` flags, and does NOT subscribe to the `*Show` EMITTER cues either. A **flow-v2** authored game (the Book-of-Borut remake) mounts its intro/outro via `showContainer` and NEVER broadcasts `freeSpinIntroShow`/`winShow` — confirmed by driving the live game: the intro screen was up for its full duration with every `*Show` flag false and no cue logged, so both a flag-read AND a cue-subscription were dead there. The lock is instead driven off the flow's **ACTIVE SCREENS**: `apps/lines` Game.svelte runs an `$effect` setting `stateUi.celebrationLock.{intro,outro,win}` from `activeScreenIds ⊇ {freeSpinIntro, freeSpinOutro, bigWin}` (the engine's canonical flow screen ids; `freeSpinIntro`/`freeSpinOutro` confirmed present live for the whole celebration). `hasCelebrationOverlay()` reads that latch. At the `spinStop` chokepoint the lock is `isCelebrationLocked() = hasCelebrationOverlay() || hasContinuePress()` — the `hasContinuePress()` term catches a big win presented as a HUD count-up + tap (no `bigWin` screen mounted on the remake), so its tap-to-continue phase also locks. The disabled button's canvas rect still swallows a tap in its footprint (Pixi `eventMode` stays `static`) — accepted small dead-zone. ⏳ Residual: a big-win **count-up before its tap** (no screen, `continuePress` not yet armed) stays slammable on a game with no `bigWin` screen — verify/tune if the remake shows a distinct big-win count-up.
- **Signal-subscription re-entrancy loop (fixed)** — a slammed free-spin **outro** could crash the reactive cycle with `effect_update_depth_exceeded` (Svelte 5), which aborts the update pass so downstream nodes freeze and the screen reads as broken. Root cause: `ComponentInstance.svelte`'s signal-subscription `$effect` subscribes to registered signal sources, and a source may EMIT SYNCHRONOUSLY on subscribe — the seed-on-subscribe latch (`freeSpinOutroCountUpComplete` fires `run()` immediately when `freeSpinOutroState.countUpComplete` is already true, the fix for the instant-count-up race). That emission runs the callback INSIDE the subscription effect, and `fireComponentSignal` READS + WRITES `firedSignals[key]` → the read leaks as a dependency, the write re-invalidates the effect, it re-subscribes/re-seeds/re-writes → infinite loop. It only surfaced on a SLAM, where the outro count-up completes in the SAME tick the outro-visual instance (tap armed on `freeSpinOutroCountUpComplete`) mounts + subscribes, so the latch is already set. Fix: wrap the subscription SETUP in `untrack(...)` (the effect has no legitimate reactive deps — `signalToTargets`/`gateSignals`/the registry are init-stable — so it should subscribe exactly once; mirrors `LayoutScene`'s tap-portal guard). Untrack suppresses dependency TRACKING, never the writes/notifications, so later emissions still fire cues + update `hiddenUntilSignal`/`tapArmed`. Reproduced deterministically headless (forced trigger + retrigger via the book mock, then Space-spam to slam the outro) and confirmed gone across 600+ slams / 24 free-spin rounds. Guard: `node packages/engine-layout/scripts/test-signal-subscribe-untrack.mjs`. Reaching the online Borut remake needs a `runtime:lines` release.
- **Live-asset pipeline** — a game asset only ships when it travels the full chain **export → `deploy/` → bake (embed index in bundle) → pull (mirror into `static/assets/`) → runtime register** (CLAUDE.md rule 8; `docs/design/live-assets.md`). Exported sheets are content-versioned (`sheetVersion` hashes region geometry + source-page ETag) so a re-packed atlas busts the cache automatically; exporters emit a `missing[]` dangling-binding guard. "Shows in the editor" ≠ "ships" — the editor reads R2 directly.
- **How the runtime ships** — an engine/runtime change reaches online games ONLY via a runtime-bundle publish, NOT a `main` merge. **Preferred: the `Runtime release` GitHub Action** (`.github/workflows/runtime-release.yml`) — a manual `workflow_dispatch` ("Run workflow", or `gh workflow run runtime-release.yml -f runtime=lines`) that does the whole chain with the R2 keys held in GitHub Actions secrets, so a release needs no keys on a dev machine. Game Maker's Publish CANNOT do it: `publishGame` only points a game's manifest at the prebuilt `_runtime/<id>` and "never rebuilds". The equivalent manual chain: rebuild the `engine-layout` **dist** first (stale-dist trap), then `PUBLIC_RGS_TRANSPORT=play4fun pnpm --filter lines build`, then `node apps/launcher-api/scripts/publish-runtime-bundle.mjs lines apps/lines/build`, then `POST https://games.invisiblewall.org/refresh`. Shipped standalone games (e.g. Book of Borut) instead bump their `engine` git submodule to a `main` commit (owner-owned). Standalone Borut keeps its OWN `src/game/*` copies, so game-side wiring (flags/effects/constants) must be MIRRORED there — the submodule only delivers `packages/*` engine code.

⏳ Live-verify caveats still open: `SYMBOL_SPINE_FILL`'s live size judgement (0.5 tunable); the z-order of author extra-scenes (currently above the HUD) is a live tuning call; the win-line line-draw and sequential-stop timing were shipped + reachability-verified but not auto-verified in a live spin (owner confirms by forcing the win / triggering free spins).

## Open items / next

0. **Game-type templates — SHIPPED and live** (#355, #357; runtime `1f1639e`). A game type is now
   data + a shared runtime rather than an app fork. `packages/engine-game` holds the extracted
   engine layer (context / board state / config resolution inverted behind factories);
   `GameConfigDoc.winModel` (lines · ways · cluster · scatter) states how a game pays and is
   authored in `/config`; the engine reads it through `activeWinModel()` and adapts — payline
   colour and the payline diagram stand down off-lines, the paytable divisor follows the model
   (ways = the WAYS count, 243 on a 5×3), and anticipation stands down because its reachability is
   per-payline. `ways` ships a config default and a mock RGS protocol (a `winModel` option on the
   lines mock, not a fork).

   **Every game type shares ONE runtime bundle** (id `lines` — historical, it means "the shared
   engine runtime"). A per-type bundle would be built from `apps/<id>`, which are vanilla upstream
   and cannot consume a runtime bundle at all, so it would break the very project it was meant to
   serve. Revisit only for a type needing bespoke COMPILED code — `cluster`s tumble board is the
   first real candidate. Harnesses: `pnpm check:ways`, `game-config-spike run doc` / `run winmodel`,
   `pnpm check:path-imports`.

   **⚠️ Open:**

   1. **The client still does not EVALUATE a non-lines win, and no ways project has been played
      end-to-end yet** — the honest Phase D gate. The model is READ (surfaces adapt) but the engine
      presents whatever the RGS reports, so `warnOnGameConfigIssues` warns at boot that a declared
      ways/cluster/scatter game is being played as lines.
   2. `apps/ways` strips are **cosmetic, evenly weighted — NOT a math export**. Fine as a client
      default; not fine shipped for money.
   3. `cluster` still has empty-placeholder `paddingReels`, so it ships no config default.
   4. A game only ever ships an **authored** config, so setting a project game type without
      pressing Save in `/config` leaves it playing as lines. See [tools/game-config](../tools/game-config.md).

1. **Game-type templates — Phase A extraction: A0–A5 have all landed; the remainder is small and
   named.** Plan: [design/game-type-templates.md](../design/game-type-templates.md).
   `packages/engine-game` now holds the type-agnostic engine layer — 7 leaf modules, the context
   (inverted, not moved), `createGameState()`, `createGameConfig()`, the `constants` split and 10
   components. `apps/lines/src/game/stateGame.svelte.ts` is 75 lines of composition + re-export and
   `gameConfig.ts` is 45, with every call site untouched. `getPaylines`/`getNumLines`/`paylineColor`
   moved across unchanged so the relocation stayed provable, and Phase C/D then turned them into the
   win-model arms they were left in place for (item 0 above, which also owns what Phase D still
   owes).

   **What is genuinely left:** `utils` and `symbolMap`, which need `editor-scenes`/`assets` seams —
   and they gate most of the remaining component queue. **Re-scoped:** ~13 of the components still
   in `apps/lines` are lines/book mechanic components that should NEVER move, so a raw file count
   overstates the remainder. Reference scene sets for a type other than `lines`/`bookOf` are Phase E
   (a game type as an authoring KIND), not Phase A — they belong to [status/editor](editor.md) and
   [status/flow](flow.md), and subsume the old "reference layouts for `ways`/`cluster`/`scatter`"
   item.

   **The gate, and what it cost to build.** The fingerprint is exact only for **type-only** slices
   (a real file move reorders rollup's graph and reshuffles minified names), so relocation slices
   are verified with **`scripts/bundle-modules.mjs`** (sourcemap build → compare the module set +
   per-module content hashes); a string-literal comparison **cannot** work on this bundle (lodash
   regexes containing quotes desync any scanner, template literals embed renamed identifiers) —
   that was tried and deleted. A2 set the precedent that a slice changing runtime **wiring** (not
   just relocating code) gets booted against the mock RGS, not just module-set-checked. Two gotchas
   worth not rediscovering: a dependency scan pattern of `[a-zA-Z.]*` **silently excludes digits**,
   hiding every `flowV2*` import; and `sed -i` over a glob rewrites line endings in ALL matched
   files, which git normalizes away but the **sourcemap-based module check does not** — it reported
   27 phantom changed modules until the untouched files were restored.

   **Per-slice detail worth keeping.** **A1** — recomputed from the real import graph, `utils.ts`
   and `symbolMap.ts` are NOT leaves (they reach `bookEventHandlerMap`, `flowEffects`,
   `../editor-scenes`) and `constants.ts` was mixed, so all three needed splitting or a later slice.
   **A2** — the context could NOT be relocated, because four things in it are app-bound
   (`eventEmitter` is typed by a union assembled from the COMPONENTS, mechanic events included, and
   components rely on that precise typing; `stateApp` from this game's assets + baked editor art;
   `stateLayout` from this game's ratios/sizes; plus `stateGame`/`i18nDerived`). The keystone is a
   dependency **inversion** — `createGameContext()` owns the wiring, `apps/lines/src/game/context.ts`
   is a thin composition root — which is **Phase B's mechanic contract arriving early**, for state
   and events rather than the win model; Phase A therefore cannot fully precede Phase B, and the
   owner approved pulling B forward. **A3** — `getGameContext()` is typed by declaration merging
   (the app declares `interface GameContext extends LinesContext`, the only direction available
   since a package must never import from an app). Only **10** components moved, not the planned 44:
   that number counted whether a component mentions the MECHANIC and ignored what each IMPORTS, the
   set must be closed under sibling imports (`Win` looks clean but pulls `WinGate`/`WinVisual`), and
   `BoardFrame`/`Effects` import `../editor-scenes` while `TapToContinue` imports
   `flowV2InterpreterHolder`. **A3.5** — engine feel knobs (`SYMBOL_SIZE`, `SYMBOL_SPINE_FILL`,
   `REEL_PADDING`, `SYMBOL_DIM_TINT`, `INITIAL_SYMBOL_STATE`, `SPIN_OPTIONS_DEFAULT`/`FAST`) →
   package; this game's `STACKED_PICTURE` / `SYMBOL_INFO_MAP` / `SCATTER_LAND_SOUND_MAP` stay. The
   halves had no cross-references, so the cut was clean. **Hard constraint to preserve:** the app's
   `constants.ts` now has ZERO imports and must keep it that way —
   `apps/launcher-api/scripts/publish-symbol-defaults.mjs` imports it STANDALONE under Node
   type-stripping to read `SYMBOL_INFO_MAP`, and the `engine-game` barrel re-exports `.svelte`
   components that type-stripping cannot parse, so a value import from there would silently break
   the publish. Seven dead exports were deleted rather than moved (moving them would have created
   dead API in a new package): `MOTION_BLUR_VELOCITY`, `zIndexes`, `BACKGROUND_RATIO`,
   `PORTRAIT_BACKGROUND_RATIO`, the three `*_MAIN_SIZES` — note `FreeSpinAnimation.svelte` has a
   LOCAL `BACKGROUND_RATIO` with a different value, and the main sizes are duplicated as literals
   inside `stateLayout.ts`, which is what actually feeds the layout. **A4** — the board/reel
   machinery (lattice, spin profiles, anticipation, stacked runs, win-dim) is in the package; this
   game's grid/layout/sounds are injected as `deps`. **Correction worth keeping:** the earlier "~95%
   generic" figure counted MECHANIC mentions and does not predict extractability — the real coupling
   was six app deps plus `onSymbolLand`, which hardcodes this game's `'S'`/`'W'` ids and cue names
   (it stayed in the app). It did NOT unblock the component queue: of the 11 component importers,
   six import only `stateGame` itself, which package components already reach via `getGameContext()`,
   and most of those are book/expanding mechanic components that **stay in the app by design**. The
   reason to do it is that the board machinery is what a `ways`/`cluster` build would otherwise fork.
   Two implementation notes: types that depend on factory internals (`Reel`, `ReelSymbol`,
   `MultiplierSymbol`, `StackedPictureRun`) are **derived from the factory's return type** rather
   than re-declared, so they cannot drift from `buildBoard`; and the real risk was array identity,
   not typing — `enhancedBoard` closes over `stateGame.board` at module init and `rebuildBoard`
   splices rather than reassigns, so the factory must be called exactly once at app module scope.
   **A5** — the `runtime → baked → compiled` resolution, its memo, the win-tier ladder, the board
   grid and the config warnings all moved to `createGameConfig()`; only the two SOURCES are injected
   (`bakedGameConfig()` and the compiled `./config`). App file: 462 → 45 lines, 23 functions
   re-exported, 21 consumers untouched. Cleanest seam in Phase A — the file was already type-agnostic
   apart from its two inputs, and the `__IE_SERVER_CONFIG__`/`__IE_WIN_LEVELS__` globals needed
   nothing because they are deliberately decoupled bridges to the Play4Fun facade.

2. ✅ ~~**B4 — HUD migration**~~ — **DONE for `apps/lines` (shipped 2026-06-08).** Live Balance/Win/Bet readouts render as `hudReadout` component instances behind the parity gate. Remaining: the **Borut mirror (B4.6)** submodule bump + owner live-verify.
3. **Three-knob reel grid + Borut mirror tail** — the reel-grid split (2026-07-03) was NOT yet published to `_runtime/lines` at the time of writing and not mirrored to Borut's engine submodule; verify it rode a later runtime publish.
4. **Author the sequential-reel-stop / flow effect nodes** into a target game's FlowDoc online — the mode is capability-only until authored; mirror the game-side wiring into standalone `bookofborut`.
5. **⚠️ Author the REAL `sfx_btn_stop` slam sound — the shipped one is a placeholder.** There is no new audio file: the key is mapped to the SAME sprite region as `sfx_btn_general` (`[284000, 58.66]`, the short UI click) in all five audio apps' `static/assets/audio/sounds.json`. It is audibly distinct from the 1s spin whoosh, which is the whole point of the change, but it is NOT a purpose-made slam cue. To finish: re-cut the audiosprite with a real slam sound and re-point the region in each app's `sounds.json` — **no code change needed**. A game whose audiosprite predates the key falls back to the spin cue (`sound.hasSound()`), so a stale manifest degrades rather than going silent.
6. **Camera effects — author + tune live.** The four kinds are capability-only until a FlowDoc drops a `cameraEffect` node (a win/free-spin trigger is the obvious first beat). The reference strengths were picked by eye, not on a real game: the numeric verification proves the mechanic, NOT that a 24px shake or an 8% punch feels right — expect one tuning pass. Needs a runtime publish to reach online games, and the game-side bridge mirrored into standalone `bookofborut`.
7. **Perspective board mode — PHASES 0–4 SHIPPED to `main` (#407 `b81171cc`, #410 `80deba43`, #421 `eea3c773`, #415 `4a518067`); phase 5 (ship) unbuilt. STILL INERT — nothing authors it yet, so no online game is affected.** A converging, non-rectangular board whose symbols SWAP IN PLACE instead of rolling, driven by the "Knights of the Golden Label" concept art. The mechanic half already existed: the cascade (#375) and multiplier-collect (#377). **What is built:** `getSymbolSeat(reel, row) → {x, y, scale}` in `packages/engine-game/src/game/gameState.svelte.ts` is the ONE per-cell seat, asked by every per-cell call site; a `ReelGridPerspective` block on `ReelGridNode` (`farScale`, `vanishX` — **SHAPE ONLY**, see the behaviour note below) plus a sibling `tileRegion`, resolved by `resolveReelGridPerspective` / `resolveReelGridTileArt` beside the `spin`/`anticipation` resolvers; the model (row scale ramping `farScale`→1, columns contracting toward `vanishX`, y as the running sum of compressed pitches); `SymbolWrap.scale`; `boardWindowHeight()` as the ONE definition of the visible board height; back-to-front draw order; the `swapInPlace` mode switch (`presentReveal`, shared by the coded handler AND the flow-v2 `revealBoard` effect, branches to a swap presentation through the tumble overlay, and the four reel-shaped behaviours stand down through the off-path each already had); **three swap STYLES** — `dropIn` (the whole board falls at once), `columnCascade` (#425: the resting board drains column by column, left to right, each column refilling as it empties, via a new `tumbleBoardDrain` cue and a PER-COLUMN scoped `tumbleBoardInit`, with `columnStaggerMs` as the ONE knob covering both readings of "left to right" — short ⇒ a wave, longer than a column ⇒ strictly sequential, `0` ⇒ all at once), and `emerge` (2026-08-27: NOTHING travels — each symbol is placed on its own seat at `duration: 0` and plays a new authored `intro` symbol state there, so the arrival ANIMATION is the whole presentation; `columnStaggerMs` sweeps it, defaulting to `0` where the cascade defaults to 140 ms. It is the one style that answers “the symbols rise out of the water”, and it is NOT reachable by shortening a fall — a fall that lands in 1 ms is still a fall, and its `land` beat still fires AFTER the movement rather than instead of it. Full story in [game-config.md](game-config.md) and [symbols.md](symbols.md)); a `clearBoard` opener for the drop-in (every visible cell plays its authored `explosion` state and leaves first, reusing the two cascade steps the drop-in omits — no new cues); the editor mirror in `reelGridGeometry()` + a "Perspective (advanced)" properties section; and ground TILES stamped from the lattice with a per-tile win highlight. **Seven traps found, all now load-bearing knowledge:** (a) **flat must be an EARLY RETURN, not a `scale` of 1** — `vanishX + (getSymbolX(reel) − vanishX) * 1` subtracts and adds back, which ROUNDS (63.6 → 63.599999999999994); (b) **`props.row` indexes the PADDED strip** while `getSymbolY`'s domain is the lattice row (the reel homes at `−pitch`, so strip _i_ rests at `getSymbolY(i−1)`); (c) **`scale = 1` is not a no-op in Pixi 8** — see [[gotcha_pixi_identity_prop_is_not_a_noop]]; (d) **`swapInPlace` is INDEPENDENT of `farScale`** and must NOT be gated on `boardPerspective()` (a flat board that swaps is a legal authored config; gating them together makes it silently do nothing — there is a regression test); (d2) **the board's BEHAVIOUR does not belong on the `reelGrid` node at all** — 2026-08-21, owner call. A `reelGrid` node is authored PER `layoutType`, so `swapInPlace` / `swapStyle` / `columnStaggerMs` living there let the schema express a board that ROLLED IN PORTRAIT AND SWAPPED IN LANDSCAPE, or swept at two speeds depending on the phone. All three moved to the Invisible Game Config's `reelBehaviour` block, read through a new `deps.reelBehaviour` on `createGameState` and authored in `/config` → **Reel behaviour**; the perspective block is shape only now. Nothing had authored them, so there was nothing to migrate — but the fields are asserted GONE from both the type and the resolver, because `normalizeNode` is pass-through and would carry a stale one out of an old doc forever. Full story in [game-config.md](game-config.md); (e) **the design's drop-in sequence had a hole** — "the cascade minus the explode step" leaves the old board in the survivor layer with nothing to filter it out, so the combined column is twice as tall and `land` fires on the off-screen half; hence `tumbleBoardInit.keepBase`, and "no survivors" is one EMPTY COLUMN PER REEL, not an empty array; (f) **the editor's `cellW/2` vs the game's `cellSize/2` is NOT a divergence** — it is cancelled exactly by `boardLayout()`'s pivot recentring (`((reels−1)/2)·columnExtraLocal`), and the Y twin by `(rows/2)·(rowPitchLocal − SYMBOL_SIZE)` against the game's `gapY/2` origin; both terms are load-bearing; (g) **RULE 8's broken link was `collectArtRefs`** — a per-node-kind whitelist (sprite/spine/componentInstance only), so a tile would look perfect in the editor and ship as a MISSING FRAME. Fixed via `addImageRef`; the other four links are free because the tile rides the existing `editorArt` sheets. **Verification is offline and mutation-tested throughout** (the validate-the-contract-offline discipline; every fixture slices the REAL shipped source so it cannot rot): `scripts/verify-symbol-seat.mjs` (265905), `verify-swap-in-place-mode.mjs` (**533**, on a virtual clock — sweep order, drain-before-refill per column, the stagger actually changing the overlap, the clear step, the tile layer never blinking out, and — part 9 — the emerge style proved to move NOTHING by a ledger of every `Tween.set` duration the reveal asks for, with the shipped drop-in in the same run as the control that shows the ledger can tell travel apart), `verify-reel-grid-geometry.mjs` (307580 — asserts the editor seat EQUALS the engine seat through an affine map derived from `boardLayout()`'s own pivot, worst relative error 8.79e-14), `verify-board-tiles.mjs` (58638), `verify-reelgrid-perspective-roundtrip.mjs` (13). What they do NOT cover: **nothing renders** — no phase has been verified in a browser — and only event ORDER is tested, never timing. **Phase 5 (ship) — DONE for the ONLINE games, 2026-08-21.** It needs no manual step any more: `.github/workflows/runtime-release.yml` auto-fires on any push to `main` touching `apps/lines/**` or `packages/**`, so an engine merge publishes `test_server/_runtime/lines` and pokes the test server itself. Released at `dcaaf84` (#435) — 163 files / 57.6 MB, `release.json` stamped `status='released'`, refresh took on attempt 7 after the usual 502 flapping while the server re-hydrates. **Verified in the LIVE bundle, not just from a green tick**: `games.invisiblewall.org/bookofborutremake/_app/immutable/bundle.*.js` contains `reelBehaviour` (a string only this merge introduces — #425 used `swapStyle`/`columnStaggerMs`), and the game boots (Pixi 8.8.1 webgl2, canvas up, 9 stage children, no crash; the `?k=`-less fallback-layout warning is expected when loading the game URL directly). **Still owed:** a runtime release + reconcile for the `emerge` style (merged 2026-08-27 — the auto-release fires on any `main` push touching `apps/lines/**`, so this lands with the merge), and the `engine` submodule bump for STANDALONE Book of Borut — the auto-release does not reach it. Nothing else to deliver until a game authors the mode. **Open decisions for the owner:** (1) **per-`layoutType` overrides** — the design said decide "before phase 1 rather than retrofitting" and we are now past phase 4; `spin`/`anticipation` are not per-ratio so `perspective` follows them as a plain node-level block, and retrofitting after tiles are stamped against the lattice is the expensive version; (2) ~~tiles unmount during a cascade~~ **CLOSED by #425** — the overlay now draws the same ground layer, so it no longer blinks out for the length of a swap. The double-draw guard OBSERVES the reel board's own `boardShow`/`boardHide` rather than assuming the two cues flip in one Svelte batch, because both are authorable Broadcast cues and a doc CAN hold the overlay and the reels on screen together (two coplanar tile layers with alpha would darken the ground); the fixture exercises that case directly; (3) **`symbolAlignY = 1` (feet-on-tile) moves the TILE down with the character**, because the seat folds the alignment — pinning the tile to the cell BOX while the art rides the alignment would be a second, independent knob. Owner decisions (2026-08-20): no roll; the per-symbol win dim (`SYMBOL_DIM_TINT` through `SymbolWrap`) is enough since it touches no geometry — a DIFFERENT feature from the anticipation grey-out, which stands down with the roll. Perspective SPINNING reels are explicitly out of scope. Plan: `docs/design/perspective-board-mode.md`.
8. **Stepped grids — SHIPPED to `main` (#445 `553848a4`) and runtime-released, 2026-08-24. A board whose columns are different heights (`3/4/5/4/3` diamond, `2/3/4/5/6` ramp).** `numRows` has been a PER-REEL array since the config schema was written and its doc comment always claimed "so a stepped grid is expressible", but only the MATH ever read it that way (`activeWaysCount`, the per-reel payline bounds check, `gameProfile`'s `boardText`) — every renderer and dealer collapsed it to `Math.max(...)`. So a non-uniform config **saved, validated and shipped** while the board drew a rectangle against it: the paytable pricing 720 ways over a 5×5 board, the RGS dealing five rows into a three-row column — the client board diverging from the scored board, reachable by typing a number into `/config`. The feature is mostly about REMOVING that disagreement. **What is built:** `resolveGrid` (`packages/game-config/src/grid.ts`) is the ONE resolver both halves read — `rowsForReel` + `rowOffsetForReel` (in rows, FRACTIONAL, so a 4-row column centred in a 5-row box lands on the half-cell stagger that makes a diamond read as a diamond); a new optional `gridAlign` (`center`|`top`|`bottom`) stored only when it departs from the default AND the grid is actually stepped; per-column clip windows (`ReelColumn`) + a per-column cull in `SymbolWrap`; the offset applied on ONE axis but TWICE (into the reel's `symbolLead` so it travels while rolling, and into the resting seat via `rowSeatIndex`) because `ReelSymbol` picks between those two y sources every frame; ragged dealing end to end (`/config` → `mockContract` → manifest → test-server → mock, each hop sending `rowsPerReel` only when the columns differ), with the mock DECLARING what it dealt (`config.window.rowsPerReel`) and the facade clamping PER COLUMN against that declaration; `standardPaylines`/`coversAllRows` taught per-column heights (a board-wide coverage test can never be satisfied by a stepped grid, so the server would regenerate lines forever); the editor preview seating each column from the SAME `resolveGrid` output; `ROWS=3,4,5,4,3` on the mock CLI. **`stepped` is the parity gate** — false for every board that exists, and every consumer early-returns its EXISTING path on it, not an equivalent one. **Traps:** (a) a short column is pushed DOWN, so its padding row lands INSIDE the board-wide window — culled against the board it would be DRAWN, a phantom symbol above a short column on the unmasked animate layer; hence the per-column cull, which is correctness, not precision; (b) the rolling y and the resting seat have NEVER been bit-equal (a uniform board already drifts ~2.8e-14 between them today, because the two expressions associate differently), so that one assertion is a 1e-9 TOLERANCE while everything else in these fixtures is `Object.is` — the failure it guards misplaces a column by half a row, fifteen orders of magnitude clear of the noise; (c) the stepped-vs-PERSPECTIVE conflict CANNOT be checked in the config validator — perspective lives on the reelGrid NODE, not in the config doc, so a check there would silently never fire; it is in `reelGridWarnings`, the one surface that sees both. **Verification:** `scripts/verify-stepped-grid.mjs` (229, new — including `clampBoardToGrid` SLICED from the real facade) + a stepped section in `verify-symbol-seat.mjs` (now 274,644), negative-tested. The two load-bearing parity assertions: the same seed dealt with `rows: 3` and `rows: [3,3,3,3,3]` gives BYTE-IDENTICAL responses (the RNG stream is untouched — otherwise every existing game's deal would shift the day this shipped), and a uniform seat is still literally `getSymbolX`/`getSymbolY`. **Verified live** in `apps/lines` + mock by reading the Pixi scene graph directly (the browser pane would not composite, so a screenshot would have proved less): a 3/4/5/4/3 config draws five masked column containers holding 3/4/5/4/3 symbols at row offsets 1/0.5/0/0.5/1; reverting to 5×3 returns ONE mask on the board container with all 15 symbol containers as its direct children at y 60/180/300 — the flat structure unchanged, not merely equivalent. **Cascade + stepped WORKS**, and the first pass was wrong about why it might not: the tumble overlay does NOT seat falling replacements against the board's row count — every cascade seat already goes through `getSymbolSeat(reelIndex, …)`, a drain drops by the COLUMN's own length, `combineTumbleReel` is length-agnostic, and the mock refills exactly what it removed per reel. What WAS board-wide is the same pair the reel board fixed: the CLIP (the resting cascade layer is clipped by the board-wide `BoardMask`, so a short column's replacements — stacked deliberately ABOVE its window waiting to fall — sit inside the bounding box and would be DRAWN hanging above it, and its drained symbols would park below it instead of leaving) and the CULL (`TumbleSymbol` handed `SymbolWrap` no `reelIndex`). `TumbleBoardBase` now wraps its columns in the same `ReelColumn`; grouping by COLUMN is safe where grouping by ROW is not, because a symbol never changes column mid-cascade, so the object keying that keeps a falling symbol's `symbolY` Tween alive across `tumbleBoardRemoveExploded` is untouched. `cascadeBoard.fixture.ts` now drives a ramp (2/3/4/5/6) and a diamond (3/4/5/4/3) beside its original rectangle — 937 assertions over 291 tumble steps, real mock + real facade + real board rule. **Perspective + stepped COMPOSES**, and the fix was to change how a stepped board is CLIPPED rather than to reconcile two paint orders. The first design wrapped each column in its own container + mask, which forces a COLUMN-major scene graph while perspective paints ROW-major (so a front-row character covers the row behind it) — mutually exclusive. The clip is now ONE compound mask whose geometry is the UNION of the per-column windows (`boardMaskColumns`), needing no grouping at all: the child list stays flat, both `BoardBase` branches are untouched, `ReelColumn` is deleted, and `BoardBase`/`TumbleBoardBase` are byte-identical to `main` again. Two traps: the columns must TILE rather than overlap (an overlapping polygon lets a tall neighbour cover the notch beside a short column, and a symbol scrolling through it would be drawn where the board does not exist); and under perspective each column must be sampled at EVERY ONE OF ITS ROW BOUNDARIES rather than drawn as a four-corner trapezoid — a trapezoid interpolates its edges linearly in Y while the contraction is linear in the ROW and y is a quadratic sum of compressed pitches, and the two disagree enough mid-column that a cell's centre lands inside its NEIGHBOUR's polygon. Both were caught by the fixture, not foreseen; `verify-symbol-seat.mjs` now point-in-polygon-tests the mask across 3 perspective settings x 4 stepped shapes x 15 grids, probing from the SEAT path rather than from the polygon's own vertices (the first version probed the ring and a deliberate one-pitch shift of every interior boundary sailed through it). **Verified live and quantitatively** via `renderer.extract` pixel readback: a 3/4/5/4/3 config paints column spans of 2.98/3.95/5.00/3.94/2.92 rows at offsets 1.03/0.54/0/0.63/1.10 — the diamond with its half-row stagger — while a reverted 5x3 puts the mask back on the `Rectangle` path (`fill`+`stroke`, not the compound `fill`) with 15 symbols at y 60/180/300. **WAYS on a stepped board — a real bug, found by closing the gap note rather than by trusting it.** The note said the ways reach was "correct by construction" since `createWaysReach` reads each column's height off the board it is handed. True — but the board it was HANDED was wrong: `buildAnticipationArming` sliced the padded reveal to the BOUNDING BOX (`reel.slice(1, 1 + y)` with `y = max(numRows)`), so a 3-row column (5 padded cells) kept its bottom PADDING row — four cells in a three-cell column. Nothing downstream could detect it: a ways pay is a product over the per-reel counts DIVIDED by the ways count and both move together, so the round silently pays the wrong multiple (3x4x5x4x3 = 720 ways against 3125 if every column is counted as five), and for `lines` an off-screen symbol can complete a run. Fixed to slice by `grid.rowsForReel(reelIndex)` — the identical number on a uniform board. `anticipationWaysReach.fixture.ts` gained a ragged-board section: bounds monotone, converging on the real win, plus the load-bearing one — padding the short columns out to the bounding box provably CHANGES the win, so the slice cannot regress silently. The rest of the family was already safe and is worth recording as such: `bookEventHandlerMap` and `flowEffects` walk the visible rows behind a `row < symbols.length - 1` guard, which bounds them by the column's own strip rather than by the board. **Open:** no full ROUND has been driven in a browser (the pane will not composite, which throttles the animation clock so a round never settles and a second spin never arms — a single spin does fire on spacebar), so the cascade is covered by the fixture plus SOURCE-level assertions that the per-column window is wired into both symbol renderers (`verify-stepped-grid.mjs` §6). **Released to the online games**: the auto `Runtime release` workflow fired on the merge and went green (build → publish `_runtime/lines` → refresh the test server). Verified in the LIVE bundle rather than from the green tick — `games.invisiblewall.org/bookofborutremake/_app/immutable/bundle.CENqRIJc.js` contains `gridAlign`, `rowsPerReel` and `rowOffsetForReel`, three markers only this merge introduces. And verified INERT there, which is the parity claim that actually protects a live game: the remake's board draws 15 symbols at y 60/180/300 behind a mask whose instructions are `fill`+`stroke` (the untouched `Rectangle` path, NOT the compound `poly`+`fill`) at 840x360 — the same geometry as before the merge. (Loading that URL bare still shows the RGS handshake error; it carries no `sessionID`/`rgs_url`, which is the documented behaviour for a direct load and unrelated.) **Still owed:** the `engine` submodule bump for STANDALONE Book of Borut — the auto-release does not reach it. Plan: `docs/design/stepped-grid.md`.

## Blocked (owner / external)

- **Shipped-game submodule bumps** — Book of Borut (and other shipped games) bump their engine submodule + republish on the owner's cadence; per-game-engine branches are owner-merged.
- **Live-verify of shipped runtime changes** — win-line draw, sequential stop timing, spine symbol size, and author extra-scene z-order need an in-browser confirmation the headless build can't give.

## Recent changes

- 2026-09-02 — **A spine symbol is centred on its authored BOX, not its skeleton origin.** `<SpineProvider centreBox>` (pixi-svelte) pivots a cell-fitted rig on the centre of `skeleton.{x,y,width,height}` — load-scale-aware and y-flipped (`spineBoxPivot`) — and `SymbolSpineMain`, `StackedPicture` and the paytable `InfoOverlay` opt in. Every Spine-editor rig we ship centres that box on the origin, so they are byte-identical; a Rigger rig's box (the measured setup-pose extent, or a hand-dragged frame) is not, and the game used to size it right and place it wrong. Full story in [rigger status](rigger.md). Placed scene spines are untouched (origin-at-position, matching the Scene Editor). Ships to online games via the automatic runtime release.
- 2026-09-01 — **A win bound to SPRITE art lasted one frame, so most wins — and their amount text — were never seen** (`WIN_BEAT_MIN_MS` in `symbolBeat.ts`, applied in `Board.svelte`'s `boardWithAnimateSymbols`). Owner report: "the win amount text in the middle doesn't show when the reels tumble and I get a second win". The tumble was incidental. `SymbolSprite` reports `oncomplete` from an `$effect` the moment its art changes, so a `sprite` bound to the `win` state resolves the beat in the same tick — and everything scoped to that beat is torn down with it, including the win's stamped amount text. Measured on the live `test6` at full frame rate, one cascading spin: `winInfo L2 -> 2ms` (sprite, no text), `winInfo H4 -> 961ms` (flipbook, "€0.05" drawn at board centre), `winInfo H3 -> 2ms` (sprite, no text). Eight of that project's ten symbols were sprite-bound, so eight wins in ten flashed for a single frame — which is also what the earlier "not every win is showing up correctly" report was. The win beat is now bounded at BOTH ends: `WIN_BEAT_CAP_MS` (4000) remains the runaway guard, and `WIN_BEAT_MIN_MS` (650 — the same length and the same reasoning as the stacked-cell `STACKED_WIN_HOLD_MS` one branch away) is the floor. It runs CONCURRENTLY with the animation (`Promise.all`), so authored art longer than the floor still sets the pace and is unchanged — verified 2ms→655ms, 961ms→970ms, 2000ms→2005ms, never-reports→4007ms (still capped). A sprite bound to `win` is a legitimate authoring choice; it simply carries no duration of its own, so the engine supplies one. **Engine-wide by owner direction** — it re-times sprite wins on every game including the shipped Book of Borut (instant → 650ms). Reaches online games automatically: `.github/workflows/runtime-release.yml` auto-releases the `lines` runtime on any push to `main` touching `apps/lines/**` or `packages/**`.

- 2026-08-31 — **A project's authored symbol dictionary is now what the mock deals AND what it pays.** Owner report on the live `test6` (ways): the symbols set in `/config` do not match the ones in `/symbols` or the ones on the board. Measured over 15 live rounds — config declares 11, has 9 on its strips, and exactly 7 were ever dealt. Two independent causes, fixed together because either alone is useless. (1) **The wire vocabulary could not express three of them.** The captured Play4Fun set has seven line symbols to the engine's ten, so `linesMapping` covered `H1–H4`/`L1`/`L2`/`L5` and `H5`/`L3`/`L4` had NO server name — `projectLineSymbols` filters that table down and can never add, so a config that put `H5` or `L3` on its strips was silently never dealt them. `PIC8`/`PIC9`/`PIC10` are appended for exactly those three (`EXTENDED_LINE_SYMBOLS` in the mock, reachable ONLY through an explicit project pool); `PIC1–PIC7` keep their verified meaning, so Hot Fruits and Book of Borut are untouched and a real server simply never sends the new names. (2) **The project's paytable travelled for the `scatter` model alone**, so every lines/ways/cluster game paid captured Hot Fruits values whatever `/config` authored — and the newly-mapped symbols had no price row at all, since `PAY_TABLE` has none. `symbolPaytable` now rides for every model and every evaluator prefers it per-symbol (`payRowOf`), falling back to the mock's own table. Two follow-ons the change forced: the evaluators iterated the module-level `LINE_SYMBOLS` rather than the instance pool (invisible while a pool could only be a SUBSET, wrong the moment it can contain a name that list lacks) — they now take `poolOf(opts)`; and an authored paytable is bet-multipliers, routinely fractional (`test6` prices `L1` at `0.4`), so a 3-of-a-kind at a 1-cent stake paid `0.4` cents straight into `session.balance` — `payCents` rounds every payout with a one-cent floor, inert for the integer captured table. The `config`/`spinStart` events also advertise the real pool + price list instead of the module defaults. **Verified:** `node scripts/cascade-chain.fixture.mjs` gained cases driving a pool that needs the extended names (dealt, paid at authored multipliers, and the wire quoting them) and a cluster game on a SPARSE authored paytable; a byte-parity harness confirms lines/ways/cluster/scatter/wild/stacked games with no project pool and no project paytable are IDENTICAL to before (a project that STATES a pool legitimately differs: its `config`/`spinStart` events now advertise that pool and its price list rather than the module defaults, while boards, wins and payouts are unchanged); and `pnpm --filter game-config-spike run scattercrosscheck` / `wayscrosscheck` — the harnesses that assert the mock scores exactly what the RTP verifier scores — pass, which is what keeps the payout rounding at the mock's wire boundary rather than inside the shared evaluators. Simulated against `test6`'s real contract: `H5` and `L3` now deal and pay, zero fractional payouts. **⚠️ DEPLOY ORDER:** the launcher auto-deploys from `main` and will start publishing pools containing `PIC8/9/10`, which an un-updated test server would deal and an un-updated runtime could not render — deploy `services/test-server` and cut a **runtime release** together with the merge, not after it.

- 2026-08-31 — **The win meter now climbs with every win instead of jumping once at the end of the round** (`advanceWinMeter`, `apps/lines/src/game/utils.ts`). Owner report on the live `test6` (ways + tumble): a cascade narrates win after win and the Win box reads 0.00 for the whole chain. Every `winInfo` already carries `totalWin` — the round's payout INCLUDING that win, accumulated by the Play4Fun facade's `runningTotal` across a spin's several paying lines AND across every board a cascade scores — and nothing consumed it: `stateBet.winBookEventAmount` was written only by `setTotalWin`, at `gameEnd`, so the meter snapped to the total after the board had already settled. The advance sits at the `dispatchBookEvent` seam beside `recordWinCycleWins`, for the same reason: a flow-owned `winInfo` never reaches the coded handler map, and the meter must behave the same on a flow-driven game as on a coded one. Forward-only (`playBet` zeroes it per round), so a book that omits `totalWin` cannot walk it backwards; `setTotalWin` still assigns the closing figure. Side effect worth having: `winBookEventAmount` now genuinely holds the accumulated feature total DURING a free-spin outro, which is what the `totalWin` value source in `Game.svelte` already claimed. Verified offline — `cascadeCollect.fixture.ts` gained "the climbing meter lands on the round total" (1059 assertions green). **Needs a runtime release** to reach online games.

- 2026-08-31 — **A losing spin on a ways game tumbled anyway, exploding symbols that never paid** (`nativeCascade` in `scripts/mock-rgs-server.mjs`, `cascadeEnabledFor` in `services/test-server/server.mjs`). Same owner report, same game. The mock decides whether a dead spin still tumbles from `nativeCascade`, which asked WHICH WIN MODEL is in play (`cluster`/`scatter`) — a question that stopped being the right one the moment a project could author `cascade: true` in its own Game Config. A `ways` game that declared a tumble therefore fell through to the DEMO pass, which picks the most COMMON symbol on the board and blows every one of its cells away, twice, paying nothing. Measured against the live `test6` RGS: **20 of 20 dead spins tumbled**, each reading to the player as a win that paid nothing. The question is now WHO TURNED THE CASCADE ON, and the test server — the side that knows — answers it: `cascadeEnabledFor` returns `{ on, demo }` and passes `cascadeDemo` alongside `cascade`, with only the `CASCADE_GAMES` / `CASCADE=1` env override marked as a demo. An authored or protocol-implied cascade is the game's MECHANIC and no longer fires on a spin that paid nothing. Regression held by `node scripts/cascade-chain.fixture.mjs`, which gained a ways case ("an authored cascade chains" + "a dead spin never tumbles"; 1213 assertions green) and now expresses the demo path as `cascadeDemo: true`. **Needs a `services/test-server` deploy** — it is the mock RGS every launcher-hosted game plays against.

- 2026-08-27 — **The big win could not be dismissed once the count-up landed — two hold-and-tap surfaces on one presentation, and the mask runs only the top one.** Owner report on the live remake: the progressive tier-stepping works, but the tap after the total lands does nothing and the overlay sits there. Cause is the premise `#456` rested on. It arms an engine-side hold on the landing tap (`winState.awaitingDismiss`, so the total is readable instead of flashing past in the 300ms settle) and mounts a dismiss press for it, reasoning that a container owning the tap would have absorbed the landing tap already — so a hold can only exist where the gate owns the press. That is false for the arming a win container actually uses: `tapArmAfterSignal: 'winCountUpComplete'`. Its tap surface does not exist DURING the count-up — which is _why_ tier-stepping reaches `CountUpInteraction` at all — so the landing tap arms the gate's hold, and the authored tap then registers a flush later, ON TOP of the gate's dismiss press. `<ContinuePressMask>` runs the top-registered press alone, so every later tap went to the container, where it only latched a completion the flow had not reached yet, while the one press that could release the gate sat underneath it unreachable. The overlay ignored the player for the full `DISMISS_HOLD_CAP_MS` (10s), then concluded on the cap and vanished at once as the flow consumed the latch.

  - **Where the flow already holds, the gate stands down.** The remake's flow authors the same beat itself — `winUpdate` → `showContainer{awaitComplete}` → `hideContainer` — so the hold and the dismiss tap were always there; `#456` added a second, rival copy on top. `winState.flowHoldsPresentation` is the name-agnostic "a `showContainer{awaitComplete}` container is on screen" test the celebration lock already computes (`winAwaitTargets` ∩ shown), published from the same `Game.svelte` effect and read by both of `WinGate`'s landing taps. The lock keeps ORing the coded `bigWin` id; the gate deliberately does not — a v1/coded `bigWin` screen is a mounted scene that holds nothing, so folding the id in would suppress the gate's hold on exactly the path that needs it. A flow that authors no hold is untouched: the gate holds and dismisses as `#456` shipped it.
  - **Either surface can now end the hold** (`releaseWinDismissHold`, called from `TapToContinue`). Belt-and-braces under the stand-down: it makes the two presses unable to deadlock each other whichever way the registration order falls. It sets the same latch `dismissNow` does, so the outro stays skipped on a deliberate dismiss (`#295`), and leaves `awaitingDismiss` set so `WinAnimation`'s `holdOutro` does not start an exit clip in the beat the overlay is leaving.
  - Diagnosed offline against the LIVE artifacts rather than by playing for a big win: the deployed `release.json` commit (which contains `#456`), and the remake's `/api/editor/runtime` doc — its wired `setWin` Game Signals pin (⇒ `resolveWinMount` = the headless `driver`), its `showContainer{awaitComplete}` on the win container, and that container's `tapArmAfterSignal`. Pinned in `packages/engine-game/fixtures/winTapLand.fixture.ts` (`node --experimental-strip-types …`), which now models the ignored-tap window and asserts the four wiring halves against source — the timeline model alone cannot see them. ⏳ **Not browser-verified**: reproducing it needs a real big win on the live remake.
  - **Engine change on `main`** — reaches the online remake only on a `runtime:lines` republish.

- 2026-08-26 — **A project's own sounds now travel the full asset chain into a running game** (`engine-layout/bakedSounds.ts`, `EnableSound.svelte`, `LoadedAudio.format`). The catalog becomes one audio BANK per file, appended after the shipped audiosprite so a same-named project sound overrides it. Detail in [status/sound](sound.md).

- 2026-08-26 — **The sound player holds a LIST of audio banks, not one `Howl`** (`packages/utils-sound`). The precondition for a project having sounds of its own; last-wins on name, one `Howl` per bank, per-bank unload. Detail in [status/sound](sound.md).

- 2026-08-26 — **A placed FLIPBOOK clip now cover-fits like every other background art node.** Owner report: a clip dropped on a `background (cover-fit)` screen did not fill the window in the game, and stayed drag-resizable in the editor — unlike the sprite/spine sitting next to it. Cause was not the cover math but its GATE: five separate places each spelled out the cover-capable kinds as `sprite | spine` (+ `componentInstance` for the background auto-cover), so the `flipbook` kind — added later — reached none of them. A `background`-space clip fell through the editor's `nodeTransform` to the plain `game`-space mapping and through `LayoutNodeView`'s `bg` derive to its authored transform, i.e. it was treated as an ordinary sized node in both surfaces. Fix: the kind list is now the exported `isCoverFitKind` / `isCoverArtKind` pair in `coverTransform.ts` and every gate reads it (runtime `isCanvasCoverFit` + `bgTexture`/`bg`; editor `nodeTransform` ×2, `isBackgroundCover`, `isBackgroundCoverSelected`, `canCoverFit`). The clip measures its FIRST frame — the frame `EditorCanvas.naturalSize` already sizes off, so preview == game, and a cover recomputed per frame would pulse the backdrop at `fps`. `flipbookCoverRef` resolves that frame with the same scoped→bare precedence `resolveClipFrames` uses. Additive: the sprite/spine/componentInstance paths are byte-identical and the "Cover / full-screen fill" toggle now also offers itself to a clip on a flow-gated `canvas` screen. **An existing background clip that was hand-scaled to fit must have its `scale.x`/`scale.y` reset to 1** — under the cover path `node.scale` is the free per-axis STRETCH multiplied onto the fitted cover, so a 2.1× hand-scale now over-covers by 2.1×. Verify offline: `node packages/engine-layout/scripts/test-cover-fit.mjs` (now asserts the kind membership directly, so a future kind cannot be half-added again) and `pnpm --filter flipbook-spike run cover`. Reaching a live game needs a runtime release + reconcile; the editor half ships with the launcher.

- 2026-08-25 — **`<AnimatedSprite>` stopped itself one frame in, so every flipbook in the engine rendered a STILL FRAME.** PIXI's `textures` setter ends in `gotoAndStop(0)`, and `propsSyncEffect` re-assigns EVERY prop whenever ANY tracked prop changes — so the first unrelated change after mount (alpha, x, a resize, a `loadedAssets` update) froze playback, permanently, because the `play` effect only re-runs when `play` changes. `textures` is now assigned only on a real frame-list change (a CONTENT comparison — the upstream array is a `$derived` rebuilt every recompute), carrying playback across it. Affects BOTH flipbook consumers: scene `flipbook` nodes and Symbols cells. Extracted decision + fixture: `pixi-svelte/src/lib/animatedSpriteFrames.ts`, `node --experimental-strip-types packages/pixi-svelte/fixtures/animatedSpriteFrames.fixture.ts` (it reproduces the old still frame as well as pinning the fix). Diagnosed in the LIVE game rather than from the code — see [flipbook status](flipbook.md) for the reading, including the hidden-tab trap that made the first diagnosis wrong. **Needs a runtime release** to reach online games.

- 2026-08-25 — **`LayoutNode` gained a `flipbook` kind** — a placed Invisible Flipbook clip, mounted by `LayoutNodeView` as `<Flipbook>` in the shape of the `sprite` branch (size folded into `width`/`height`, per-placement `fps`/`loop` overriding the clip's own). `resolveTransform`'s SIZED + TINTED lists, `sceneDuration`'s walk (through the `flipbookMs` seam that was wired ahead of it, now taking the placement's `loop`), and the new `flipbookCycleMs` in `registerFlipbooks` are the rest of the engine half. Additive: a doc with no `flipbook` node is byte-identical. Also new here: **`LAYOUT_NODE_KINDS`**, an exported runtime list of the union's kinds — the launcher's doc normalizer now derives its accepted-kind set from it instead of hand-copying, which is what stops a future kind being silently dropped on save. **Reaching online games needs a runtime release** (the reader is in `packages/*`); standalone game repos need an engine submodule bump only — no per-game `src/` mirror. Authoring UI, editor playback and the full story: [flipbook status](flipbook.md) · [editor status](editor.md). Fixture: `pnpm --filter flipbook-spike run scene-node`.

- 2026-08-25 — **26 of the 53 sounds in every game's audiosprite had never been played by any code path, the cascade pop loudest among them — and none of them were bindable without an engine edit.** Owner report: the tumble explosion is silent, and the sounds for it are visibly there in the Flow editor's library. Both halves were true, and they were the same bug seen from two ends. The Flow dropdown is generated from `apps/lines/src/game/sound.ts`'s `SoundName` union (`soundEnums.generated.ts`), which lists NAMES; the wiring was a hardcoded string literal at each presentation beat, spread over ten files. So a moment with no literal was silent forever, and it LOOKED bound. `tumble_win_1…5` — a five-rung escalation ladder cut for exactly the cascade pop — has shipped in the audiosprite since the fork with nothing playing a rung of it; `tumble_win_5` was not even in the type union. An audit of the sprite against the runtime code found the same for the whole multiplier family, `sfx_reel_stop_2…5` (every reel played `_1`), `sfx_symbols_landing`, `sfx_royals_landing`, `sfx_scatter_reveal` and `sfx_scatter_win`.

  - **The unit of authorship is now a SLOT** — a named presentation moment — declared once in a catalogue (`packages/game-config/src/sounds.ts`): `tumbleExplosion`, `reelStop`, `symbolLand`, `royalLand`, `scatterLand`, `wildLand`, `wildExplode`. The ENGINE owns when a slot fires, the CONFIG owns what it plays, and the Invisible Symbols doc owns a per-symbol exception. Every slot ships a coded default, so a project that authors nothing gets the full sound set — **the default is inverted from every other optional block in this schema**, and deliberately: elsewhere "absent" means "what the engine did before", and here that was _nothing_, so defaulting to the old behaviour would ship the fix switched off.
  - **The catalogue stays LIVE.** `/config` stores only DEPARTURES (`normalizeSounds` drops a binding equal to the default), so a project that opens the panel, looks, and saves does not freeze its ladder to that day's copy — the same freeze the shared compiled config already inflicted on every online game once.
  - **Ladders** are indexed by a number the firing site supplies and CLAMP at the top: the cascade STEP for `tumbleExplosion` (reset each `reveal`), the REEL INDEX for `reelStop`, the running scatter count for `scatterLand`. A six-reel board plays rung 5 twice rather than restarting the climb or falling silent.
  - **Picture vs royal is decided by PAYTABLE RANK, never by symbol id** (`landSlotForSymbol`): at-or-below the median of the paying dictionary is a royal. `H*`/`L*` is a convention of the templates we ship, not a contract — a math team's `CHERRY`/`BELL` set would fall off a prefix test in silence. The same routing answers "is this a scatter" for the scatter counter, replacing a hardcoded `'S'`; `SCATTER_LAND_SOUND_MAP` is deleted.
  - **The pop fires once per cascade STEP, not per exploding cell**, and stands down for the board CLEAR. `tumbleBoardExplode` serves two moments that look identical — the cascade removing cells that paid, and `clearOutgoingSymbols` emptying the board before a drop-in reveal — and in the second nothing has won. The clear only runs under a `reveal`, so "is the event being dispatched right now a `tumbleBoard`" separates them exactly, with no new field on the cue and nothing for a flow author to pass. That rule lives in `apps/lines/src/game/cascadeSoundStep.ts`, import-free so it is exercisable offline; `trackCascadeStep` is called from `dispatchBookEvent`, the one seam all three dispatch paths cross (the same reason `recordWinCycleWins` lives there) — a flow-owned `tumbleBoard` never reaches the coded handler map.
  - Files: new `game-config/src/sounds.ts` + `sounds` on `GameConfigDoc`; `engine-game` `activeSounds()` + a `sounds` dep on `createGameState` driving the reel-stop ladder; new `apps/lines/src/game/soundBindings.ts` (the one resolution point, and the one cast between authored `string` names and the compile-time `SoundEffectName` union); `symbolSounds` through the Symbols doc → export → bake → `bakedSymbolSounds()`. `tumble_win_5` added to the union and the Flow enums regenerated.
  - **Verified live** (`apps/lines` + mock RGS, `Howl.prototype.play` instrumented in a real Chrome tab): one spin now plays `sfx_reel_stop_1,2,3,4,5` in order plus `sfx_royals_landing`, `sfx_symbols_landing` and `sfx_scatter_stop_1` — four of those had never played at all, and the ladder previously played rung 1 five times. Plus 36 offline claims in `packages/game-config/sounds.fixture.ts`, 12 in `apps/lines/src/game/cascadeSoundStep.fixture.ts`, and 13 doc-contract claims in `apps/launcher-api/scripts/check-sound-bindings.ts` (`pnpm --filter launcher-api check:sound-bindings`, which asserts the SAVE path's whitelist rebuild rather than only the Zod parse — the launcher build type-checks nothing). ⏳ **The cascade pop itself is not browser-verified**: no mock emits `tumbleBoard`, and `apps/lines` does not cascade. Its gate is covered by the fixture; the audible check belongs on a cascading project.
  - **Engine change on `main`** — reaches an online/remake game only on a `runtime:lines` republish + `engine` submodule bump.

- 2026-08-25 — **A published build shipped tens of MB of atlas pages no atlas referenced, because the puller's prune skipped the one subtree that churns every export.** `pull-project-assets.mjs` mirrors R2 `deploy/` into `static/assets/` and then prunes the export-generated subtrees back to the listing — but the list was `editor-art`, `editor-fonts`, `editor-symbols`, `effects`, `clips`, and NOT `_pages`. `PageStore` writes every shared atlas page content-addressed (`_pages/<hash>.<ext>` + its `.ktx2` twin), so a re-pack or a re-encode lands the page at a NEW hash and R2 drops the old one — while the superseded LOCAL copy simply stayed. Nothing else removes it: `_pages/` is untracked, so the launcher's pre-build `git reset --hard origin/main` leaves it, and `adapter-static` then copies all of `static/` into `build/` verbatim. Stale pages accumulated one generation per pull and every one of them shipped.

  **`_pages` and `_boot` joined `GENERATED_SUBTREES`.** Both are 100% export-written (`PageStore`, `bootSplashExport`) and neither is ever committed by a game, so converging them on the deploy listing is the same safe operation the other five already get — and the mirror runs BEFORE the prune, so the current generation lands first. Measured on the Book of Borut Remake checkout: the prune goes from 27.3 MB (17 files) to 84.8 MB (56 files) — **57.5 MB of dead pages that used to ship**.

  Worth knowing about the rest of the tree: this closes a stale-file leak, NOT a general "only ship what the game uses" guarantee. Reachability filtering exists for editor art, symbols and FX effects (and Rollup tree-shakes the JS), but the puller mirrors `deploy/` verbatim, fonts export the whole catalog, flipbook clips ship in full by design, and anything committed under a game's `static/` ships whether or not the game mounts it. The 🗜 Optimize pass trims format twins and spare audio formats but deliberately only REPORTS art referenced by no descriptor.

- 2026-08-25 — **The tap on the LAST big-win tier dismissed the presentation instead of landing on it, so the player never saw the amount they had just tapped to see.** Owner report on the live remake, and the other half of the `#282` tap grammar: mid-chain a tap means "next tier, faster"; on the FINAL tier there is nothing left to step to, so it meant "slam and go". `#295` had deliberately armed the outro-skip there — the LAST tap used to sit through the full tier outro before hiding — which left the whole appearance of the total to the fixed `roundSkip.wait(300)` settle. The number arrived and vanished in the same beat.

  **A tap on the final tier now only LANDS.** `resolveWinTap` (new, `packages/engine-game/src/game/winEscalation.ts`, beside `tierHasExit`) is the one rule: step while a next tier exists, otherwise land — and `hold` on the land is simply whether a chain is presenting. On a hold the gate sets `winState.awaitingDismiss`, `WinAnimation` keeps the final tier's idle looping instead of flipping to its outro (a new `holdOutro`, which returns BEFORE `chainConcluded` so the collapse still runs untouched if the hold lapses), and `concludePresentation` waits. The DISMISS press then behaves exactly as `#295` made it: it sets `escalationOutroComplete`, so the overlay hides instantly with the outro skipped. Two taps, each doing one thing.

  **The hold is bounded, by a cap sized for a human rather than for art.** `waitForEscalationOutro` now picks `DISMISS_HOLD_CAP_MS` (10000) over `ESCALATION_OUTRO_CAP_MS` (4000) when it is waiting on a person, and skips the mis-authored-outro warning in that case — a lapsed hold is a player who walked away, not a broken rig. Ten seconds is far longer than reading a number takes and still short of the tier idle's own 12000ms loop.

  **Headless (`resolveWinMount` ⇒ `'driver'`, the flow owning `setWin` — what the remake and `test6` run) holds too**, which is the only reason this reaches a real game; the post-count-up `<PressToContinue hidePrompt>` mounts for it while the hold is up. That is not a second tap surface competing with an authored container: a container's own `tapToContinue` registers a continue press, which mounts the canvas-top `<ContinuePressMask>` and absorbs the tap wherever it lands — so where a container owns the tap, `CountUpInteraction` never sees one, no tap ever lands the count, and no hold is ever armed. Only a presentation whose taps actually reach the gate can hold, which is exactly the presentation the gate can also dismiss.

  **And the other half of a step was dead on every project that authors its config.** Found while verifying the above, on `test6`: a step is two things — the walk jumps to the tapped tier's ART, and the COUNT is sought to that tier's amount — and the second half had never run on an authored game. `WinVisual` derives each tier's seek target as `threshold × BOOK_AMOUNT_MULTIPLIER` off the presentation object, but `tierToWinLevelData` (the authored-config projection, `game/gameConfig.ts`) did not carry `threshold` through, so `?? 0` gave `escalationBoundaries: [0,0,0,0,0]`. Nothing failed and nothing logged: `jumpTo` clamps FORWARD ONLY, so seeking to 0 is a no-op — the tap stepped the tier art while the number carried on counting from wherever it was. The coded `winLevelMap` rows carry a threshold, which is exactly why the un-authored dev games never showed it and only real projects did. `tierToWinLevelData` now carries it, and `WinLevelData.threshold`'s doc no longer says a runtime-built tier omits it.

  **Verified:** `packages/engine-game/fixtures/winTapLand.fixture.ts` (new, `node --experimental-strip-types …`) drives a 5-tier chain through the real extracted decision imported from source and asserts BEFORE/AFTER — the old landing tap leaves the total on screen for **300ms**, the new one holds it until the dismiss tap; stepping is identical tap-for-tap with the hold on or off; a deliberate dismiss still skips the outro; an abandoned hold concludes on the dismiss cap; and an un-escalating win is byte-identical (`{kind:'land', hold:false}` — a plain win has no tiers and nothing to look at, so holding it would only make small wins sticky). For the seek target it models `jumpTo`'s forward-only clamp — a dropped threshold zeroes every boundary and leaves the count at 4891, a carried one snaps it to 8000 — and then asserts the WIRE against the SOURCE (`scripts/verify-stepped-grid.mjs` §6's technique), because the projection is a closure inside `createGameConfig` and the derivation is a Svelte template: neither can be imported and executed from Node, and re-dropping the field would leave every data-level assertion green. Negative-tested — deleting `threshold: tier.threshold` fails the fixture. `apps/lines` builds, the touched files lint clean, and the pre-existing `winEscalationOutro` fixture still passes. **NOT verified in a browser:** the escalation path was reached live in `apps/lines` against the mock (a 100x MAX-WIN spin, `escalationActive` with 5 tiers, taps stepping the walk 0→1→2→3) but the final-tier tap itself was never exercised — the automation browser leaves the tab hidden, so rAF is throttled to ~1 frame per 10s and the count-up never runs unattended. One live spin on the remake would close it.

- 2026-08-24 — **A big win stepped its tiers but would not dismiss: the escalation waited on an outro `complete` that could never arrive, with no tap surface left to cut it short.** Owner report on the Book of Borut remake. The engine had not regressed — `release.json` showed the live shared bundle at `main` HEAD, and `CountUpInteraction.svelte` / `WinAnimation.svelte` were byte-identical to the `#295` state verified working on 2026-08-11. The cause was in the authored data hitting two holes in the engine at once.

  **Hole 1 — an outro authored as the idle never restarts the track, so the round waits out the idle instead.** The remake points every tier's outro at that tier's own idle clip (`bigOutro: Tier1_Idle`, `superwinOutro: Tier2_Idle`, `megaOutro: Tier3_Idle`) — what an author does when the rig has no exit and the dropdown insists on a name. `<SpineTrack>` applies by VALUE (`shouldApplySpineAnimation`: `animationName !== track.animationName`), so the idle→outro switch was a no-op: the running entry kept going and its `complete` arrived at the end of the OLD cycle. Measured on the reference `bigwin` rig, that is not a rounding error — every `*_win_exit` is **467ms** and every `*_win_idle` is **12000ms**. The gate sat blocked for a full idle cycle after the number had landed.

  Two fixes, because they are two different mistakes. `WinAnimation` now passes `<SpineTrack>` a **re-apply token** (`stepIndex * 3 + phase`, injective over the walk) so any transition restarts the track even when the next phase names the clip already playing — this also fixes the general case of two adjacent tiers sharing a clip, where the second tier silently never played. And the "is there an exit at all" rule moved to **`packages/engine-game/src/game/winEscalation.ts`** (`tierHasExit`), which now treats an outro equal to that tier's idle as NO exit, exactly like the empty name the old guard already covered: the chain reports done immediately and leaves the idle on screen until the overlay hides, rather than pretending an exit is playing.

  **Hole 2 — the flow path had no dismiss surface after the count-up.** `WinGate` mounts `<CountUpInteraction>` only `{#if !countUpCompleted}` and suppresses the coded `<PressToContinue>` under v2 (`codedPressOwned`), so between the count landing and the outro finishing there was NOTHING to tap — `dismissNow()` (`#295`) existed but was wired only to the press that never mounts there. It now mounts `<PressToContinue hidePrompt>` in that window when `!headless`, which is the ownership line `resolveWinMount` already draws (headless ⇒ the flow owns `setWin` and the authored container owns dim/art/tap). `PressToContinue` rather than another `CountUpInteraction` deliberately: it registers with `registerContinuePress`, so the canvas-top `<ContinuePressMask>` mounts and absorbs the tap wherever the pointer rests — including over live HUD chrome, which hit-tests above the overlay and would otherwise swallow it (the `#400` narrowing). `hidePrompt` keeps the coded `MM_pressanywhere` sprite off an authored screen, which was the only reason the press was suppressed there.

  **And the wait itself is now bounded.** `waitForEscalationOutro` races `ESCALATION_OUTRO_CAP_MS` (4000) and warns when it blows — the same runaway-guard doctrine `game/symbolBeat.ts` applies to the win beat, for the same reason: it BLOCKS THE ROUND. Sized off the art like `WIN_BEAT_CAP_MS`: ~8.5× a real 467ms exit, still a third of an idle cycle. The race settles through one guarded `settle()` so the loser cannot resolve twice, cannot warn about an outro that did land, and cannot leave the watcher `$effect.root` running for the session on every capped win.

  **Verified:** `packages/engine-game/fixtures/winEscalationOutro.fixture.ts` (`node --experimental-strip-types …`) drives the remake's real authoring through both extracted decisions, imported from source, and asserts BEFORE/AFTER — the old path blocks **12000ms** and never re-applies the same-named outro, the new one blocks **0ms** with the walk itself unchanged; a rig with real exits plays byte-identical animations and still ends on its **467ms** exit; and a shared-clip tier that the old track skipped entirely now plays. `apps/lines` builds, `engine-game` lints clean, the touched files lint + prettier clean, the pre-existing `spineTrackReplay` fixture still passes. Live in `apps/lines` against the mock: boots clean and **no `effect_update_depth_exceeded`** — the `#396` failure class these new effects could have reintroduced (`chainConcluded` is a plain `let`, not `$state`, for exactly that reason).

  **NOT live-verified end to end.** The Browser pane reports `document.hidden` even when fronted, so rAF is frozen, `FadeContainer`'s alpha tween never opens, and the win overlay's count-up cannot run — the same wall `#442` documented. The flow-path branch (Hole 2) additionally needs a v2 doc that drives screens, i.e. the remake's project token. Worth one confirming mega win on the remake after the runtime release.

  **The authoring is still worth fixing too:** the remake's tiers should point their outro at a real exit clip (or leave it empty). The engine now behaves correctly either way, but an authored exit is what makes the celebration actually play out.

- 2026-08-24 — **`createInterruptible().add()` could strand its promise forever — a latent stuck win presentation / reel.** `packages/utils-shared/interruptible.ts` passed an `async` function as a `Promise` executor (ESLint `no-async-promise-executor`). A rejection from `targetToWait()` inside an async executor is NOT forwarded to `reject` — it became an unhandled rejection and the returned promise never settled, so every caller awaiting it hung until (and unless) an `interrupt()` happened to arrive. The four call sites — `WinCountUpProvider` (count-up + the value-reached settle), `OnHotkey` (hold-to-fast-forward), `createReelForSpinning` (slide-down), `createReelForCascading` (fall-in delay) — all simply continue the sequence afterwards and none catches, so the failure mode was a permanently frozen presentation. Fixed by dropping the async executor and chaining: the wait now ALWAYS settles, a throwing target settles as `{ interrupted: false }` (the wait is over; nothing cut it short) and the error is `console.error`'d rather than swallowed. **Rejecting was considered and rejected**: no call site catches, so a rejection would leave the sequence equally stuck, only louder. `targetToWait` is still invoked SYNCHRONOUSLY inside `add()`, so animation timing is unchanged. Behaviourally inert today — no current target can reject (`waitForTimeout` and svelte `Tween.set` never do) — this is a guard for the next one that can. Verified: clean under `no-async-promise-executor`, prettier + `tsc --strict` clean, a 5-case harness over the compiled helper (normal / interrupt / async rejection / sync throw / late-interrupt-after-settle) with the old code proven to HANG on the rejection case, `pnpm --filter lines build` green, and a live A/B in `apps/lines` against the mock RGS (stock vs fixed: identical — bet placed, balance debited, zero console errors).

  **Written 2026-08-19 and never merged** — it sat on an un-PR'd branch until a branch audit found it.

- 2026-08-24 — **The swap-in-place fixture had stopped asserting anything: it crashed on a slice marker before its first check, so the cascade contract it exists to protect was unguarded on `main`.** `node scripts/verify-swap-in-place-mode.mjs` threw `could not find "\tconst CASCADE_BEAT_CAP_MS ="`. Nothing was renamed — the constant was EXTRACTED. #442 moved the race and the cap into `game/symbolBeat.ts` (`TRANSIT_BEAT_CAP_MS`), and the marker went with it.

  **Two extractions had broken it, and the loud one hid the quiet one.** Re-pointing the marker got the slice evaluating again and immediately surfaced the earlier breakage: #443's `PAD_ROWS_ABOVE` had left the component for `game/tumbleBoardLayout.ts` too, so the slice referenced an undefined binding. The fixture had therefore been dead since #443, and #442 only changed which line it died on.

  Both modules are now SLICED rather than restated — `awaitSymbolBeat` + `TRANSIT_BEAT_CAP_MS`, and `PAD_ROWS_ABOVE` + `combineTumbleReel` — so the fixture still drives the shipped race against the shipped 650ms and stacks each column by the shipped rule. `combineTumbleReel` especially: a fixture holding its own copy of that rule would assert the settle contract cell for cell against a board the shipped one no longer produces, which is exactly the bug #443 fixed. All five names joined the declared-names guard, and the annotation guard grew `T` for the generic.

  **Two checks were genuinely wrong and were updated, not silenced.** They encoded the pre-#443 stacking (`adding` at index 0); the shipped column is `[pad, …refills…, …survivors…, pad]`. They now walk it top-down and pin the pad's position, which is the fact that regressed.

  **412 checks, exit 0 — and mutation-tested, since a fixture that only stops throwing proves nothing.** Renaming `TRANSIT_BEAT_CAP_MS` or `awaitBeat` at the source fails loudly with the named marker; `PAD_ROWS_ABOVE = 2` fails 2 checks; reverting `combineTumbleReel` to `[...adding, ...base]` fails the two stacking checks. The other six `verify-*.mjs` fixtures were run and all pass — this was the only rotted one.

- 2026-08-21 — **The win beat awaited an animation that a symbol is not obliged to play, so an ordinary paying spin could freeze the round outright.** `Board.svelte`'s `boardWithAnimateSymbols` set each lit cell to `win` and awaited its `oncomplete` with no bound. But a symbol only reports completion when its state actually PLAYS something: `SymbolSprite` fires from an `$effect` on `symbolInfo`, `Symbol.svelte` renders NOTHING when the state resolves to no art (`missingArt`), `SymbolWrap` mounts nothing for a seat out of frame or on the other layer, and a spine whose bound `animationName` is absent — or loops — never fires `complete`. Any one of those turns the beat's `Promise.all` into a promise that never settles: `winInfo` never finishes, the spin button stays disabled, and the game reads as frozen. Only a slam released it, and only incidentally — `awaitCue` races the round-skip token, which is the sole thing that bounded the wait.

  **Reproduced live**, running `apps/lines` against the mock: a 4-of-a-kind held its lit frames and the `$100.00` stamp on screen with `Win $0.00` and STOP still enabled, indefinitely.

  The fix is the guard the cascade already had. `TumbleBoard` raced its explode/land beats against `CASCADE_BEAT_CAP_MS` and `MultiplierBoard` raced its collect against a second, separately-declared copy of the same 650ms — while the win beat, the one every online game hits on every paying spin, had none. All three now go through **`apps/lines/src/game/symbolBeat.ts`**: one `awaitSymbolBeat(arm, capMs)` race, and the caps named by ROLE rather than per-component — `TRANSIT_BEAT_CAP_MS` (650) for a beat that is a step on the way somewhere, `WIN_BEAT_CAP_MS` (4000) for the win animation itself. `Board.svelte`'s existing `STACKED_WIN_HOLD_MS` stays as it was: a covered cell provably mounts no `<Symbol>`, so it holds a fixed beat rather than racing one.

  **Why the win cap is 4000 and not 650.** A cap short enough to trim an authored win animation would silently re-time every shipped game, and this ships in the shared `_runtime/lines` bundle. Measured across the reference symbol spines (`static/assets/spines/symbols{,2,3}`, 51 animations): the longest of any kind is exactly **2000ms** — every picture/royal win, every multiplier pay, the wild — and `timeScale` is only ever 1 or 2, so turbo makes them faster, never slower. 4000ms is double the longest authored beat. A truncated animation is the worse failure of the two: silent, and on every paying spin. A blown guard is loud, rare and diagnosable.

  **Verified against the loaded module in the browser:** a beat whose animation reports at 2000ms resolves in **2012ms** (the animation wins — pacing is unchanged), and a cell that can never report resolves in **4018ms** instead of never. The running game logged 14 real win beats through the same helper, each bounded, where before each was a hang. `apps/lines` builds; eslint + prettier clean.

- 2026-08-21 — **After a tumble the client was showing a DIFFERENT board from the one the server scored, so the next step's win frames landed on symbols that never paid.** Owner reported it on a ways game (`test3`): a cascade pays, the refills fall, and the frames that come back sit on mismatched symbols on non-adjacent reels, with a row that looks like it belongs to the previous spin still in the grid. This is the SAME report that was closed on 2026-08-21 as the 12-step cap (below) — that entry says "the positions were correct", and they were: every step's cells were checked against the board **the server** produced. Nobody ever checked the board the CLIENT ended up with.

  **The cause is one line of stacking.** `tumbleBoardCombined` merged the two cascade layers as `[...adding, ...base]`. A cascade never explodes a padding row — the server only indexes visible cells — so BOTH pads survive into `base`, and stacking the refills on top of the whole thing left the column as `[…new…, pad, …survivors…, pad]` with the top pad stranded in the middle. The refills sit one row too high, the survivors one row too low, and the stranded pad is a stale duplicate of the pre-tumble top row sitting inside the visible window. It only shows on reels that actually lost cells, which is why it reads as "some wins wrong, some missing" rather than as a board-wide shift. Two more failures rode along on it: the slide aimed the refills one row too high (pushing the topmost out of the window), and `tumbleBoardSlideDown`'s "visible rows only" guard (`index > 0 && index < length - 1`) skipped a real symbol's `land` and played one for the pad.

  The rule and the padding contract it turns on now live in one place, `apps/lines/src/game/tumbleBoardLayout.ts` — `PAD_ROWS_ABOVE` plus `combineTumbleReel`, which splices the refills in BELOW the top pad. `TumbleBoard`'s seat offset reads the same constant, so the two cannot drift apart again. A column with no survivors (the drop-in / column-cascade reveals, `keepBase: false`) reduces to the adding layer exactly as before.

  **It is a plain TS module on purpose: a rune-bearing `.svelte.ts` cannot be imported by a Node fixture, and that is precisely why this shipped wrong for months.** `cascadeBoard.fixture.ts` now drives the real mock, the real facade (`padReel` + the +1 row shift) and the real `combineTumbleReel` over 60 ways rounds, walking each round IN BOOK ORDER and asserting, at every `winInfo` after a tumble, that the client is showing the symbol the server says paid there — plus that a step never changes the board's shape and that no win lands on a padding row. **179 assertions across 100 tumble steps, green; 29 of them FAIL on the old stacking**, so it is a regression test rather than a description. A whole-board parity sweep over 525 steps put it beyond doubt: the old rule was wrong on **525 of 525**, the new one on none. Only one line is restated in the fixture (the exploded-cell filter, which lives inside a Svelte component); everything either side of it is the shipped code.

  eslint + prettier clean. **Needs a runtime release** — it is client-side, so it reaches the online games via the auto-release on merge to `main`, not via a test-server deploy.

- 2026-08-21 — **A scatter pay dimmed the WHOLE board and lit none of it.** Owner reported a cluster game going dark on a win with no win overlay. The dim is real and correct machinery (`/symbols` → win cycle → "Dim non-winning symbols"): it lights the paying cells and darkens the rest. The paying cells were empty.

  **The mock stripped them.** `evaluateScatters` was the ONLY evaluator that wrapped its cells — `context: { positions }` where `evaluateClusters` / `evaluateWays` / `evaluateScatterPays` and the whole book mock emit a bare array. The facade's `winPositions` reads `Array.isArray(context)` then `context.payline`, so the wrapper matched neither and every SCAT win reached the client with `positions: []`.

  **The wrapper was load-bearing, which is why it survived.** It was chosen so `payingCells` — the cells a cascade blows up — would skip the scatters, and its comment said exactly that: "the SCAT trigger win carries neither shape, so it contributes nothing". It did the job. It just did it by making the win unreadable to everyone, the client included. The exclusion now lives in `payingCells` keyed off `mode === 'scatter' && what === 'SCAT'` — what the win IS, not what its context looks like — and the positions go out as a bare array.

  **Three things were broken at once, and only one was visible.** With no positions: the win-dim set was empty, so on a scatter-ONLY paying spin `setWinDim(true, {})` darkened all 36 cells and lit zero; `boardWithAnimateSymbols` got no cells, so no symbol entered its `win` state and no highlight drew; and `scatterTriggerPositions` was empty, so a free-spin TRIGGER animated no scatters either. A cluster game surfaced it first because scatter-only paying spins are ordinary there and there is no payline to carry the set — on a lines game a line win almost always covered for it.

  **Guarded engine-side too:** `refreshWinDim` now returns instead of activating on an empty lit set. Dimming 100% of the board is never a correct win presentation, whatever sent the win.

  Verified offline against the real mock over the real reader: pre-fix, 8/8 scatter rounds arrived with 0 positions and 7 of them would have darkened the entire board; post-fix all 8 carry their cells. Cascade safety re-checked over 93 tumble steps — no SCAT cell is ever blown off the board. **Reaches the live test server on the next deploy from `main`;** the `refreshWinDim` guard needs a Runtime release.

- 2026-08-21 — **The cascade's 12-step cap was shaping the game, so a chain that would have settled came to rest still paying.** Owner reported the win frames looking wrong after a tumble: frames on mixed symbols, on a board that had stopped. The positions were correct — every step's cells were verified against the board that step produced — and the cause was the cap. A cascade ends when a board pays nothing; a cap low enough to be hit ends it somewhere else, and the somewhere-else is the one state a cascade must never rest in.

  **The measurement that settled it:** a mock configured 8x8 with `minCount 4` runs **40 tumbles and then settles on its own**. The old cap cut that off at 12 — truncating a chain that had a natural end, three times too early. So the bound was never protecting against runaway chains in any real sense; it was cutting normal ones short.

  `CASCADE_MAX_STEPS = 12` becomes **`CASCADE_RUNAWAY_GUARD = 200`**: a bound that stops an infinite loop and nothing else. Reaching it is no longer reported as a truncation but as what it is — a MATH problem — naming `scattermath` as the tool that measures it. A cap cannot fix broken math, it can only hide it, and hiding it is what cost a day of hunting a presentation bug that was a config bug.

  **The guard path is now tested**, which matters because it is the one branch that only runs when a config is broken and is therefore the most likely to rot unnoticed: `cascade-chain.fixture.mjs` stands up a mock with two symbols on a 64-cell board and a threshold of 4 — every refill pays again, forever — and asserts the round TERMINATES (200 steps, ~30ms) rather than looping. A mock that loops there takes the test server down for every game on it.

  The verifier keeps its OWN, lower bound (`RUNAWAY_AT = 25`) and says so: following the engine's 200 would spend 200 tumbles per spin on exactly the configs it exists to condemn, and report "average chain 200" where "these never settle" is the useful sentence. On the committed template: 1.13 tumbles average, 0.00% still paying at 25. On `test5`: 24.62 average, **88.98%** still paying at 25.

  700 chain assertions green; eslint clean. **Reaches the live test server on the next deploy from `main`;** no runtime release is needed (the mock is server-side).

- 2026-08-21 — **A symbol with no art took the whole board down, and the multiplier work walked straight into it.** Owner's live `test5` threw `Cannot read properties of undefined (reading 'static')` from inside a render and the reels vanished. Cause: `getSymbolInfo` indexes `map[rawSymbol.name][state]` with no guard, so a name the symbol map has never heard of throws mid-render — and an exception thrown while rendering unmounts the board, so the player loses the REELS over one cell.

  **The gap is reachable by ordinary authoring**, which is what makes this worth fixing rather than avoiding. `getActiveSymbolInfoMap` merges the coded map with the baked overrides as a UNION of both key sets, so a config can name a symbol — a multiplier, a new picture — and have it dealt long before anyone binds art for it in `/symbols`. `warnOnGameConfigIssues` has always REPORTED this at boot as an error; it was detected and not survivable, which is the worst of both.

  **Now it renders nothing and warns once** (naming the symbol), and `Symbol.svelte` takes a `missingArt` branch before the `{:else}` arm — which is the SPINE renderer, so falling through would have handed it an undefined bundle: the same crash one layer down. A value the symbol carries (a multiplier) still draws, because that is the part the player needs to read.

  **What put a nameless symbol on the board was #411's own gate.** It asked the Game Config whether a multiplier symbol was declared and IN PLAY, and stopped there. `test5` declares `M` on its strips with no art behind it, so every `MULT` cell the collect fixture dealt was a cell the project could not draw. The publish gate now also requires the symbols doc to bind `M.static` — the exact state a resting board renders and the exact one that threw. The engine no longer crashes either way, but dealing an invisible symbol is still wrong: a blank cell that pays is worse than no cell at all.

  **The lesson is about the SHAPE of the gate.** "Is this a multiplier game?" was answered from the math contract alone, when the question the mock actually needed answered was "can this project SHOW a multiplier?" — a fact that lives in a different doc. Config-declares and art-exists are two different truths, and a feature that deals a symbol needs both. Verified: `svelte-check` reports no errors in `utils.ts`, `Symbol.svelte` or any `getSymbolInfo` consumer (the 182 it does report are pre-existing `static/assets/**` debt); `apps/lines` and `launcher-api` both build clean. ⏳ Reaches the live game on the Runtime release that merging this fires.

- 2026-08-21 — **The cascade only tumbled ONCE, so a board that still paid just sat there.** Owner spun the live `test5`, screenshotted the settled board and counted it: 10 miners and 8 pickaxes on a 6x5 grid with `minCount: 8`. Two more wins, and nothing happened. Correct reading — `cascadeSteps` exploded the dealt board's winners and stopped, and its own comment said so ("the mock does not re-evaluate the refilled board").

  **That was a defect, not a simplification — but only since yesterday.** The one-shot was right while the cascade was a presentation fixture: the mock paid PAYLINES and could not score a cluster or scatter board at all, so re-scoring would have blown up cells nothing paid on, which is the one thing the fixture was careful never to do. `evaluateClusters` / `evaluateScatterPays` landed on 2026-08-20 and made the refusal obsolete the same day. The lesson worth keeping: a deliberate limitation outlives the reason for it silently, because the comment explaining it still reads as sound.

  **Now it chains.** `evaluatePayWins` is the spin's win-model switch, lifted out so the loop cannot drift from it (a second implementation of "what pays" is how a mock stops describing the game), and `cascadeSteps` runs evaluate → explode → refill until a board pays nothing, capped at `CASCADE_MAX_STEPS` (12) with a WARNING rather than a silent truncation. The free-spin trigger is deliberately NOT re-scored: whether scatters landing mid-cascade retrigger is game math no capture has answered, and quietly saying yes would change how often the bonus fires.

  **Each step carries the wins of the board it PRODUCED**, not the ones it destroyed — which is what lets the facade narrate each payout on the board it belongs to. The dealt board's wins ride the ordinary spin flush; every later board's wins are flushed straight after the tumble that revealed it, reusing `flushWins` so `runningTotal` climbs across the whole chain instead of resetting per step. Getting this backwards would draw step 3's win frame over the board step 1 was still showing — the #404 failure, one layer up.

  **A dead spin no longer tumbles on a cluster/scatter game.** The most-common-symbol fallback explodes cells nothing paid on; that is fine as a DEMO for a lines game whose cascade was forced on via `CASCADE_GAMES` (the reason the fixture exists), and wrong for a game whose cascade is its mechanic — a real tumble game whose spin paid nothing just sits there. Gated on `nativeCascade`, mirroring `CASCADE_PROTOCOLS` in the test server. Those steps are marked `demo: true` and carry the round's running total rather than 0, because a SCAT-trigger-only win names no cells, cascades nothing, and would otherwise step the meter backwards.

  **The round pays the whole chain**, and the multiplier collect multiplies the whole chain rather than just the dealt board — which is also what makes that beat feel like a payoff.

  Verified: **745 mock-level assertions** over three configurations (a 6x5 `minCount: 8` scatter matching the owner's board, a lines demo, and a scatter with the cascade off) pinning that every chain ENDS on a board that pays nothing, that `runningWin` accumulates step by step, that a step only exists because its predecessor paid, that each reel refills exactly what it lost, and that `gameEnd` pays the chain (times the collect). 60 scatter rounds produced 20 multi-step chains, max depth 3, and **zero** tumbles on a losing spin. Plus the facade fixture extended to **1048 assertions**, adding that nothing pays after the final tumble (which is WHY the chain stopped) and that the win meter never steps back mid-chain. ⚠️ This materially changes the mock's payout distribution — chained wins pay far more than single ones — and cascade RTP is chain-dependent with nothing in the repo verifying it. Still a test approximation, just a far more realistic-looking one.

- 2026-08-21 — **Scatter's multiplier collect has a wire at last — and the facade path that carried it had never once been executed.** #377 shipped the collect PRESENTATION (`MultiplierBoard`, the `boardMultiplierInfo` handler, the six cues) against nothing: no mock emitted the event and no facade case translated one, so the beat was unreachable in exactly the way the cascade overlay was before #385. Now it runs end to end.

  **The mock lands multipliers where a scatter game actually lands them — in the REFILLS.** A cascade removes the winners and drops new cells in; some of those carry a value. Landing them on the initial reveal instead would have collected before anything tumbled, which is not the mechanic. When the chain ends, `collectStep` reads the board the cascade FINISHED on and emits `multiplierCollect`. Reading the final board rather than accumulating as it goes is what makes the event agree with `multiplierBoardInit`, which re-reads the settled board rather than trusting the positions it was sent — two independent reads of the same thing, so a disagreement would be a visible bug rather than a silent one.

  **Three gates, all facts rather than flags:** the win model is `scatter`, the cascade is on (no tumble ⇒ nowhere for a multiplier to land), and the PROJECT declares a multiplier symbol IN PLAY — `special_properties` contains `multiplier` and the symbol is on a strip. That last one reuses the `projectWild` shape exactly, and exists so a project with no multiplier art never has blank cells dealt at it. The committed `scatter.json` template already ships `M` as a multiplier symbol ON the strips, so a project seeded from it collects with nothing to configure.

  **The wire encodes the value ON the cell** (`MULT:5`), because the board is a `string[][]` and a refill moves rows: a side table of positions would have to be kept in step with every drop and would desync the first time one moved. ⚠️ Ours, not captured — same standing as `tumbleStep`, labelled at both ends, and `MULT` is declared in the boot `config` only when the fixture can deal it so the facade's unknown-symbol warning stays meaningful everywhere else.

  **The round pays what the beat announced.** `roundWin` is now separate from `totalWin` (what the wins add up to) so the two cannot drift — the meter, `gameEnd`, `gameRoundOver` and the balance all read the one number. On the facade side `runningTotal` moves with it, in BOOK-EVENT units rather than cents; that counter is fixed-point bet-multipliers and assigning cents to it would have ended the round on a figure ~10,000x wrong while every internal consistency check still passed.

  **The real finding: `packages/rgs-translator-eagaming` had no test harness at all.** #385 recorded its own translation as "prettier- and eslint-clean but never executed" — `apps/lines` does not import the package, so no build compiles those cases. `cascadeCollect.fixture.ts` closes it by standing the REAL mock on a port and driving the REAL public facade API (`requestAuthenticate` → `requestBet`) against it over 40 rounds, asserting on `round.state`: **512 assertions**, covering that a `MULT:5` cell arrives as a mapped name plus a value (never as a symbol literally called `MULT:5`), that `boardMult` is the sum of what it collected, that `totalWin` keeps the tumbleWin×boardMult RATIO through the cents→engine conversion (a skipped conversion stays self-consistent, so only the ratio catches it), that positions are shifted into PADDED board space, and that the collect falls after the last tumble and before the round total. No hand-written event stream anywhere in it — the two ends cannot agree in a fixture and disagree in production.

  Also verified: 40-spin mock-level probe (26 collects; multipliers in refills only, never on the reveal; `gameEnd`/`gameRoundOver` paying the multiplied total), a scatter project WITHOUT a declared multiplier symbol collecting nothing and never being dealt one, a cluster project ignoring the flag, and the #409 cascade truth table unregressed. ⏳ **Not browser-verified** — the beat needs a project with `M` art authored in `/symbols`; with none, the cells fly but render nothing.

- 2026-08-21 — **A scatter game did not tumble, and the reason was that we asked an env var to know what the game type already says.** Owner spun the live `test5` (scatter) and reported the defining behaviour missing: winners should leave and the symbols above should fall into the gap. Probing the live RGS settled it in one spin — `bet → gameStart → spinStart → playedSpin → gameEnd → gameRoundOver`, a `PIC6 count=9 mode=scatterPays pay=24` win, and **zero `tumbleStep` events** on winning and losing spins alike (the fixture's no-win fallback would have emitted some either way). So the mock's scatter EVALUATOR was working perfectly — `evaluateScatterPays` priced a count-anywhere win off the project's own table — and nothing cascaded because `CASCADE_GAMES` still named only `test4`.

  **The gate was the wrong shape, not mis-set.** `CASCADE_GAMES` was built (#385) to stop one process cascading the shipped Book of Borut, and per-game was the right answer to _that_ question. But a `cluster`/`scatter` game IS a tumble game — a cascade is its mechanic, not a fixture — so requiring an env var per project meant every new game of those types shipped visibly broken and waited for someone to remember. **`CASCADE_PROTOCOLS = {cluster, scatter}` now decides by default**, which keeps every bit of the safety the allowlist was for: `lines` and `book` still cannot cascade by accident, with no env set at all. `CASCADE_GAMES` survives as the override for the protocols that do not cascade on their own — the way to give a lines game the tumble overlay. A future tumble type is one entry in that set.

  **And the project can now say so itself**, which no env-only gate could express: `cascade` on the Game Config doc, authored in `/config` ("Winners tumble"), synced into the test-server manifest at publish, and read ahead of the protocol default. See [status/game-config.md](game-config.md) for the schema half.

  **Precedence, most specific first:** the project's authored `cascade` → the protocol default → `CASCADE_GAMES`. Only an EXPLICIT departure travels into the manifest — sending the resolved boolean for every project would have pinned every unauthored game and silently killed the env override.

  Verified end to end against the REAL `services/test-server/server.mjs` in `TEST_SERVER_LOCAL` mode, 8 spins per game across a six-game fixture: `scatter` and `cluster` tumble with no env set, `lines` and `book` do not, a scatter authored `cascade:false` does not, and a lines game authored `cascade:true` does. Plus 26 offline assertions (`packages/game-config/cascade.fixture.ts`) over the real normalizer, pinning that the field is stored ONLY when it departs from its type's default — the parity trap that would otherwise add a field to every existing doc. `check:game-config-defaults` reports all three committed templates byte-identical; `launcher-api` builds clean; `check:undefined-names` green over 857 files; eslint clean.

  **Still owed on the scatter side:** `boardMultiplierInfo` — the multiplier-collect beat — has a handler in the shared runtime and **no emitter anywhere**, neither the mock nor the facade. Scatter phases 1–2 shipped the presentation; that beat's wire was never written, so a scatter game tumbles but does not yet collect. ⏳ The `/config` control is compiled but NOT browser-verified.

- 2026-08-21 — **The engine is ours by name now: Stake branding is gone from code, comments and docs.** The fork still spoke its origin everywhere — a `stakeFacade.ts`, a `Stake-shaped` facade, `play4FunToStake`/`stakeToPlay4Fun`, `StakeBetRequest`/`StakeBetResponse`, `STAKE_AMOUNT_MULTIPLIER`, a `[stake-facade]` console prefix, Storybook viewports keyed `stake`, and ~150 comments calling this "the Stake Engine". All renamed to our own vocabulary (`engineFacade.ts` / `engine-facade.ts`, `play4FunToEngine`, `EngineBetRequest`, `ENGINE_AMOUNT_MULTIPLIER`, `[engine-facade]`).

  **The load-bearing part is what was NOT renamed, because "stake" carries three unrelated meanings here.** (1) The WAGER — `totalStake`, `interimStake`, `finalStake`, "total stake" — is the money vocabulary the RGS consistency gates assert on; renaming it would have broken the very checks that catch a client/server payout disagreement. (2) Live EXTERNAL references stay literal: `StakeEngine/web-sdk` is our actual `upstream` remote, and `engine.stake.com` / `stakeengine.github.io/math-sdk` / `stake.us` are real links that renaming turns dead. (3) The literal names of DELETED artifacts — `LoaderStakeEngine`, `stake-engine-loader.gif` — are kept inside backticks in the boot-splash migration note above, because a game repo carrying a frozen copy of `apps/lines/src` still imports them and needs to find that note by searching the OLD name.

  One honest origin line stays in `README.md`, `CLAUDE.md` and `docs/ONBOARDING.md`: this began as a fork of the Stake Engine web SDK. Upstream is MIT, so attribution is the correct posture, not a leftover. Also dropped a dead upstream vestige — `svelte.plugin.css.globals` in both `.vscode/settings.json` files pointed at `stake-kit`, a package in no `package.json` and no `node_modules`.

  **Two traps worth recording for the next sweep of this kind.** A blanket `sed -i` over every tracked file rewrites files it does not match, normalising line endings into a 1,775-file phantom diff — and it CORRUPTS binaries whose extensions are not excluded (`.ac3`, `.m4a`, `.wasm`, `.gz`, `.ico` were all hit and had to be restored from HEAD). Separately, `docs/design/invisible-editor.md` has MIXED CRLF/CR/LF terminators, so any line-based tool rewrites all 1,248 of its lines for a one-line edit; it needs a byte-exact replace instead.

  Verified: `check:path-imports`, `check:undefined-names` (855 files), `check:ways` and `eslint` all green; `apps/lines` builds with `PUBLIC_RGS_TRANSPORT=play4fun` and the renamed facade both resolves (all six exports) and ships in the built bundle (`[engine-facade]` present in the JS); launcher builds. **NOT swept:** `scripts/build-*.py`, three dated Italian client-deliverable PDF builders whose text describes the historical Stake `lines` → Hot Fruits comparison — rewriting those would falsify a delivered document.

- 2026-08-20 — **A cascade game replayed its win line on the board the cascade had already destroyed — a frame drawn around symbols that never paid.** Reported on the online `test4` cluster game: the win registers, the winners explode, new symbols drop, and then the win line comes BACK, tracing cells that now hold something else entirely.

  **The replay, not the round, was drawing it.** `winSymbolCycle` is the resting-board narration: once the whole book has been presented it loops the round's recorded wins, drawing each win's line before lighting its symbols. It replays by POSITION, and its only invalidation seam was `reveal` — correct for a spinning game, where the board only changes on a new spin. A cascade changes the board MID-BOOK: `tumbleBoard` blows the paying cells out and drops new symbols into those exact seats, so by the time the cycle starts, every recorded win describes a board that no longer exists. `recordWinCycleWins` now clears on `tumbleBoard` as well as `reveal` (wins + the win-celebration dim, the same two clears). What survives is exactly the wins that landed AFTER the last cascade step — which for a chain that ends because nothing more pays is nothing at all, so the settled board simply rests. That is the correct read for a cascade: the resting board is not the board that paid.

  **The mock's cascade fixture was exploding the wrong cells, which is what made it unreadable.** `cascadeSteps` (`scripts/mock-rgs-server.mjs`, `CASCADE_GAMES`) picked the most COMMON line symbol on the board and blew that up twice, ignoring the spin's actual wins — so the frame stayed around survivors while unrelated symbols fell out from under it. Not cosmetic: the client narrates a win over the cells it was told paid. When the spin pays, step 1 now removes exactly this spin's paying cells (flat `{reel,row}` context for cluster/ways/scatter-pays; the leftmost `occurs` reels of a payline context — the same slice the client lights) and the chain stops there, because the mock does not re-evaluate the refilled board and a second step could only explode cells nothing paid on. The old most-common pass survives as the NO-WIN fallback, which is the only reason the fixture exists. Still not a protocol claim — `tumbleStep` is ours.

  **Verified live, A/B, on `apps/lines` against the cascade mock.** Traced `winLineShow`/`winLineHide` against the book-event dispatch. Before: `reveal → winInfo → winLineShow(5pts) → winLineHide → tumbleBoard → setWin → setTotalWin → finalWin → winLineShow(5pts, IDENTICAL points)` — the reported bug, the same line redrawn over the tumbled board. After: the trailing `winLineShow` is gone and nothing follows `finalWin`. Mock side checked across all three win models (lines / ways / cluster): exploding cells == paying cells, one step, per-reel refill counts == per-reel gaps, and dead spins still cascade. `check-undefined-names` clean, eslint clean, prettier clean.

- 2026-08-20 — **Two boot/win console warnings on the remake, one noise and one a real authoring gap: an unset asset key was reported as "missing", and a win tier had no way to name the count slot on its OWN spine.**

  **`Spine/Sprite: key "" is not found in loadedAssets` — noise.** The four missing-asset diagnostics (`Sprite`, `SpineProvider`, `SpriteSheet`, `Particles`) fired for an EMPTY key. An unset key is not a missing asset — it is a node the author has not pointed at anything yet, which the editor treats as legitimate ("an empty `spineKey` ⇒ no rig resolves ⇒ nothing renders"). Nothing renders either way, so the error only buried the real missing keys under noise no boot could ever clear. All four now gate on a shared `hasAssetKey` in `pixi-svelte/missingAsset.ts` — ONE rule, four readers, rather than the same test re-derived per component.

  **`[SpineSlot] no slot "slot_win_count"` — a real gap.** `WinVisual` resolves the big-win spine PER TIER (`<alias>Spine` ?? the config tier's `spineKey` ?? the shared `winSpine`) but applied ONE shared `slotName` to every tier. A tier pointing at its own rig almost never repeats the base bundle's slot name, and there was no per-tier knob to say so — so the count silently failed to mount on that tier and the warning was a dead end. Added `<alias>Slot` (`kind:'spineSlot'`, `spineParam:'<alias>Spine'`) to `winTierPresentationParams`, resolved in `WinVisual` as `<alias>Slot` ?? the shared `slotName` — the exact shape the spine already resolves by, on both the escalation-chain and single-tier paths. Empty ⇒ the shared value ⇒ parity, and `mergeBuiltinCodedParams` unions it onto pinned instances so no version bump is needed. The editor's dropdown lists the TIER spine's slots, falling back to `winSpine`'s when the tier spine is unset (`effectiveSpineBundle` already did this for the animation dropdowns).

  **And the warning itself now names the slots the skeleton DOES expose** (truncated at 20 — the rig checked live carries 54). Without them the author cannot see which name to type, which is why this one sat in the console instead of being fixed.

  **Verified:** `apps/lines` builds clean; `pixi-svelte` lints clean (the `engine-layout` errors are pre-existing, in files this does not touch); prettier clean. The Spine API the new warning uses (`skeleton.slots` → `slot.data.name`, and `findSlot` returning falsy for an unknown name) confirmed against a live 54-slot rig in the running game. **NOT verified end to end on the remake** — it needs the owner's session to boot, so the remaining `slot_win_count` warning there clears only once an author picks that tier's real slot in the new dropdown; the engine now shows them which names are available.

- 2026-08-20 — **CI now fails on an identifier that does not exist — the one type error `vite build` can never catch.** This closes the gap the free-spin hang exposed earlier today: an app's `build` is a bare `vite build`, esbuild TRANSPILES per file without type-checking, and typescript-eslint leaves `no-undef` off because it defers to a compiler nothing was running. So `#373`'s renamed-away `linePay` merged green, auto-released to every online game, and killed the first free spin of every Book-of round.

  **Narrow on purpose, which is what makes it need no baseline.** `scripts/check-undefined-names.mjs` runs `tsc --noResolve` over all 855 workspace `.ts` files and reports only `TS2304` / `TS2552` / `TS18004` — "this name does not exist". `--noResolve` is the trick: nothing loads across module boundaries, so the check is immune to the two things that make a full `tsc` unusable here (the ambient `*.svelte` shim, which shows plain `tsc` only a default export so every `import type { Props } from './X.svelte'` reports a phantom error; and unresolved JSON/asset imports). An import STATEMENT still declares its names, so a genuine dangling identifier is still caught. The repo is CLEAN under this rule today, so any hit is new and real — no baseline file, no ratchet.

  **Ambient names are seeded, not stubbed.** `--noResolve` also means no declarations for globals, so the program includes the repo's own `.d.ts` files (they declare `__IE_DEBUG__`) plus Svelte's REAL `types/index.d.ts` for the runes. A hand-written rune shim was the obvious shortcut and is wrong: a typo'd rune (`$stat`) is exactly what this should catch, and a shim would declare it away.

  **The gate's own first version had the bug it exists to prevent.** Checking 855 files needs more than Node's default heap; `tsc` OOM'd, printed nothing the filter recognised, and the script reported a clean pass — while silently missing the very regression it was written for. Caught by re-introducing `#373`'s exact diff and watching it pass. Fixed with `--max-old-space-size=8192` AND a hard exit-status check: `tsc` exits 0/1/2, so anything else means the check did not COMPLETE and must fail. Re-verified the same way (2 hits with the regression in, 0 with it out).

  **It found a real one immediately:** `packages/utils-sound/src/createSound.svelte.ts` called `new Howl({…})` while importing only `Howler`. It happened to work because howler's bundle assigns the global as a side effect of the import that IS there — a runtime coincidence, not a contract. Now imported properly.

  **A full type-check is still owed, and the debt is measured rather than guessed.** `apps/lines/tsconfig.json` no longer OOMs the compiler (it excluded only `dist`/`build`/`node_modules`, while `config-ts/base.json` sets `allowJs`/`checkJs`, so the multi-megabyte bundles under `.svelte-kit/output` were being type-checked); it now excludes build output, and `tsc -p apps/lines` completes. What it reports: 159 errors outside `node_modules` — **114 of them from `static/`** (asset-glue index files vite compiles with its own plugins), and of the 36 in `src/`, 14 are `.svelte`/module-resolution artifacts and **22 are genuine code-level type errors**. Most of the rest is configuration, not code: `resolveJsonModule` is off and the app does not extend `.svelte-kit/tsconfig.json`, so `$app/*` never resolves. `svelte-check` (not installed) is the other half and is bigger — the last measurement in this file records cluster 140 / price 153 / scatter 138 / ways 121 / number-picker 26. Gating any of that needs a baseline first, which is why it is NOT in this change.

- 2026-08-20 — **The count-up skip fix (#396) shipped an infinite `$effect` loop — reverted, and fixed at the actual root instead: a disabled button was still eating the pointer.** After the Runtime release the live Borut remake threw `Uncaught Error: effect_update_depth_exceeded` on a loop the moment a win count-up mounted (right after the `slot_win_count` SpineSlot warnings), on every flush.

  **The loop.** `<CountUpInteraction>`'s new `$effect` called `registerSkipGesture(...)`, whose body is `stateUi.skipGestureCount += 1`. `+=` READS the counter, and that read happened inside the effect's tracking scope — so the effect depended on the very `$state` it wrote. Run → +1 → self-invalidate → cleanup −1 → run → +1 → … The pre-existing `registerContinuePress` never had this: its counter bump lives in `PressToContinue`'s script BODY, not in an effect, and the registry itself is a plain array. **The lesson generalises — a register-in-an-`$effect` helper must not touch `$state` with a read-modify-write.**

  **The root cause the registry was working around is one line.** A disabled button kept `eventMode: 'static'`, so it still WON the hit test and swallowed the press — every handler early-returns on `disabled`, so it just did nothing with it. That is the whole reason a pointer parked on the spin/turbo button made a celebration unskippable: the overlay's own full-screen tap surface (`PressToContinue`, `CountUpInteraction`) sits BELOW the HUD and never saw the gesture. Fixed at every place a press surface decides it is inert — the three button paths `components-pixi/Button.svelte` (coded), `components-ui-pixi/ButtonFrame.svelte` (the parametric bound part) and `engine-layout/ComponentInstance.svelte` (the authored art-button path), plus the three bet-value tap targets `LabelBet` / `HudValue` / `HudReadout`, which go inert mid-spin and so were live-swallowing throughout the very window a celebration runs in. All now `eventMode={disabled ? 'none' : 'static'}`. `'none'` skips the container AND its children in the hit test, so the press falls through to whatever the overlay put under the chrome. No canvas-top mask, no gesture registry, no cross-component state.

  `<ContinuePressMask>` stays as it was before #396 (press-to-continue only) — it is load-bearing for authored screens and untouched by this.

  **Verified live** (`apps/lines` on the mock, real Pixi tree): with a Space-hold raising `stateBet.isSpaceHold`, all six surfaces flip `static` → `none` and none is left `not-allowed` while still hit-testing; the two buttons that stay live keep `static`. Console clean — no `effect_update_depth_exceeded`.

  **Watch for:** a disabled button no longer blocks what is BEHIND it. During a celebration that is the point. At idle there is no full-canvas tap surface mounted, so nothing catches a fall-through press — but a future full-screen surface under the HUD would now receive taps aimed at greyed chrome.

- 2026-08-20 — **Free spins died on the first reveal — `#373` renamed a local and left the Book-of reach reading the old name.** Owner triggered the feature on the live Borut remake: the intro played, the first free spin dealt, and the reels never stopped. Console: `Uncaught ReferenceError: linePay is not defined` thrown out of the `reveal` book event, so `playBookEvent` never resolved and the round hung with no way forward. Cause is a rename that missed one call site — `#373` (the ways-reach change, merged the same morning) replaced `buildReach`'s `const linePay` with `symbolPay` and dropped `const numLines = getNumLines()` in favour of calling it inline, but the **`bookReach`** block below still passed both as shorthand properties (`linePay,` / `numLines,`). Both now point at nothing.

  **Why only free spins, and only Book-of.** `bookReach` is built solely when `stateGame.specialSymbol` is set — the round's expanding symbol, assigned at feature start and cleared at `freeSpinEnd`. Base game never evaluates that branch, and neither does a non-Book-of game, so every spin outside the feature ran clean and the break looked like it appeared from nowhere. It also needs `anticipationMode` on, which the remake has.

  **Why the build was green.** `apps/lines`' `build` script is bare `vite build` — esbuild transpiles per file and never type-checks, and eslint's `no-undef` is off under typescript-eslint (it defers to the compiler). `tsc` catches it instantly: `TS18004: No value exists in scope for the shorthand property 'linePay'`, and the same on `numLines`. Nothing in CI ran `tsc`, so a hard runtime crash merged with a green tick. **Closed the same day** by the `no undefined identifiers` CI gate — see the entry below.

  **Blast radius, because this is the shared runtime.** Merging to `main` auto-fires the Runtime release workflow on any `apps/lines/**` or `packages/**` change, which re-points EVERY online game at the new bundle — so `#373` reached the remake within minutes of merging and stayed broken until this. Verified: `tsc` clean on the file, the built client bundle now carries `linePay` only as a property name and a `t.linePay(...)` member call with `numLines:O$()` (the `getNumLines` call) beside it — no free identifier left; `apps/lines` builds clean.

- 2026-08-20 — **`scatter` gets an evaluator too, and the boot warning is gone — all four win models are honoured end to end.** New `evaluateScatterPays` in the mock: every symbol pays on COUNT ANYWHERE, `minCount` from the project's declared win model, wilds substituting, positions as the flat `{reel,row}` list the facade already reads. **Unlike cluster, this does NOT approximate the payout.** A scatter game prices by count (8 → 3, 9 → 7.5, 10 → 15, 13+ → 60) and the sample config already carries exactly that table, while the mock's own is keyed by payline RUN LENGTHS (3/4/5) — clamping a count of 12 into a 5-run row would make every scatter win pay the same number, degenerate enough to be useless. So the project's table travels: `projectSymbolPaytable` maps it into the mock's server vocabulary through the same in-play gate + `linesMapping` translation `projectLineSymbols` uses, rides the manifest grid as `symbolPaytable`, and is shape-checked on arrival (a malformed table would leave every win silently unpriced). Priced against the TOTAL stake, matching `payoutDivisor` returning 1 for the model. **A bug the harness caught:** the first lookup was exact-match with a `Math.min(count, max)` clamp, so against a SPARSE table (`{8,9,10,13}`) a count of 11 resolved to an unpriced row and paid NOTHING — a win visible on the board scoring zero. Paytable rows are THRESHOLDS, so it now takes the highest tier at or below the count; that also does the clamping the fallback table needs. **Naming hazard worth knowing:** `evaluateScatters` already existed and is a different thing — the SCAT feature symbol that triggers free spins, which runs for every win model. The new one is the win MODEL, where an ordinary H1 pays because eight of them landed. Wired like cluster (`MockProtocol`, `protocolFor`, `projectGrid`, the test server's protocol allowlist — the same allowlist that silently forced `'lines'` during the cluster work). **The boot warning is REMOVED**, not narrowed: there is no declared-but-unhonoured model left. The comment says what to restore if a fifth is ever added, and records why this one had to be watched — it spent a while claiming every client surface fell back to line behaviour, which had stopped being true. Verified: 9-assertion evaluator harness (count-anywhere, threshold, exact tier pricing, non-adjacency, wild gating, sparse-table tier, fallback), then end to end through the REAL `server.mjs` — `PIC1 count=8 pay=300` (mult 3 × 100 stake), `PIC6 count=9 pay=120` (1.2 × 100), positions matching. Both apps build; eslint clean.

- 2026-08-20 — **A cluster game is PLAYABLE against the test server — and "no runtime for cluster" turned out to be the wrong diagnosis.** Owner asked why cluster still had no runtime when what they need is a working test setup, not a shipped one. Checking rather than repeating the warning: **the client was already correct for cluster** — `payoutDivisor` → 1, `winLineShapeFor` → `'cells'`, anticipation stands down, and the cascade overlay mounts. The ONLY thing making a cluster project play as lines was that the **mock had no cluster evaluator**, so it paid paylines. That is a test-server gap, not an engine one, and the boot warning claiming "every client surface falls back to line behaviour" was simply wrong. New `evaluateClusters` (`scripts/mock-rgs-server.mjs`): a flood fill per candidate symbol — per-symbol rather than one board partition, because a wild can join clusters of DIFFERENT symbols at once — honouring `minCluster` and `orthogonal`/`diagonal` adjacency straight from the project's declared win model, with positions as the flat `{reel,row}` list `engineFacade`'s `winPositions` already reads for a non-payline win. Wired end to end: `MockProtocol` += `'cluster'`, `protocolFor('cluster')`, `projectGrid` now runs for cluster (it previously bailed on `protocol !== 'lines'` AND on having no paylines — a cluster game has none, so it would have silently fallen back to the shared lines grid), the manifest grid carries `minCluster`/`adjacency`, and the test server's protocol allowlist accepts it. **⚠️ The payout is a TEST APPROXIMATION**: the mock's paytable is keyed by payline run lengths (3/4/5) and a cluster is 5+ cells, so size is CLAMPED to the largest priced row. That is enough to exercise presentation and is explicitly not a cluster paytable — real pricing is by cluster size and needs a math export, the same line held on the ways strips. Corollary worth knowing when configuring a test project: a cluster smaller than the lowest priced run never pays, so `minCluster` below the paytable's floor reads as "clusters don't work" (my own first test tripped on exactly that). The boot warning now names only `scatter`, and says what is actually true — "has no EVALUATOR … so the board is dealt LINE wins" — rather than claiming the client surfaces fall back. Verified: 9-assertion evaluator harness (L-shaped group, minCluster honoured, orthogonal vs diagonal grouping, wild substitution gated on a declared wild paytable), then end to end through the REAL `server.mjs` with a cluster manifest — `mode=cluster`, groups of 6 and 7 cells, positions matching. Both apps build; eslint clean.

- 2026-08-20 — **The cascade could deadlock the round — every symbol beat is now bounded.** Owner spun the live `test4` cascade: symbols dropped and refilled correctly, then the spin button would not re-arm — intermittently. Cause: `tumbleBoardExplode` and `tumbleBoardSlideDown` awaited each symbol's `oncomplete` unconditionally, and a symbol only reports completion when its state actually ANIMATES. `SymbolSprite` fires `oncomplete` from an `$effect` gated on `symbolInfo` CHANGING, so a symbol whose `explosion`/`land` state resolves to the same art it is already showing — the normal case for a project that authored neither, which `test4` has not — never reports at all. The `broadcastAsync` never settles, the book event never finishes, the round never ends. **Intermittent because the mock explodes whichever symbol is most common that spin**, so whether it hung depended on which symbol's art happened to differ. Fixed by RACING every beat against a 650ms cap (`CASCADE_BEAT_CAP_MS`, and `COLLECT_BEAT_CAP_MS` in `MultiplierBoard`, which had the identical unbounded wait in `multiplierBoardAnimate` and was fixed at the same time before anyone hit it): an authored animation still drives the timing because it resolves first, and an unauthored one costs a bounded beat instead of hanging forever. The slide-down also force-settles `symbolState` to `static` after the race, since a cap win would otherwise park the cell on `land` — a symbol frozen mid-animation once the cascade ends. Same reasoning as `Board.svelte`'s `STACKED_WIN_HOLD_MS` (a covered cell mounts no `<Symbol>` at all — different cause, identical failure), which is the precedent this mirrors. **This is the class of bug only a real spin finds:** the contract harness, the byte-identity checks and the mock-level verification all passed while the round deadlocked, because none of them run the presentation. `apps/lines` builds clean; eslint clean. ⏳ Needs a republish of `test4` to verify the fix live.

- 2026-08-20 — **The old vendor logo is gone: the boot splash is now a pipeline spine, in two tiers.** Every game opened on `stake-engine-loader.gif` (1.3 MB, copied into all six `apps/*/static/`) via `LoaderStakeEngine`, then on `LoaderExample`'s "Add Your Loader" placeholder — both hardcoded in each `+layout.svelte`. Replaced by `BootSplashSequence` + `LoaderSpine` (`components-shared`), which play **spine** marks produced by our own tools: an ENGINE mark (global, admin-owned) then the GAME's own (per project). No new asset class — both are ordinary spine bundles read out of the roots that already exist, `_shared/spines/<bundle>` and `<client>/<project>/spines/<bundle>`, and `resolveBundlePrefix` already falls back project→shared, so ONE resolution path serves both tiers.

  **Storage mirrors the tiers, deliberately.** The engine mark is named by `app_settings.bootSplashEngine` (admin-only write, `/admin` → Settings) and resolves through a NEW `resolveSharedBundlePrefix` / `exportSpineBundle({forceShared})` — the project-first resolver would have let a project owning a same-named bundle silently shadow the engine's own mark, which is the one thing the admin tier exists to prevent. The game mark is `GameSettings.bootLoader` on the editor doc (Scene Editor → Game Settings), and resolves project-then-shared for both its files and its `skeletons.json` entry.

  **Why a `deploy/_boot/` mirror rather than the runtime bundle:** the splash paints before `/api/editor/runtime` resolves, so it cannot be TOLD where its art is. `bootSplashExport.ts` mirrors both bundles to fixed stems (`_boot/engine`, `_boot/game`) and writes `_boot/boot.json` beside them; the splash resolves everything off `assetBase`, which is the page-relative `assets/` when baked and the launcher's PATH-form `/api/deploy/f/<token>/…/` in runtime mode — both preserve file extensions, which `Assets.load` needs to pick a spine parser. Runs inside `ensureDeployExports`, so Publish and the live assemble share the one export path. Deliberately does NOT use the shared `_pages/` store (`editorArtExport` prunes that against its own written set and would delete a boot page) and prunes only `_boot/`.

  **Ordering was the real finding.** `apps/lines` paints `#ie-boot` from the HTML shell at `z-index: 99999` and holds it until every asset loads; the old gif loaders sat UNDER it at `999` and burned their 2s timers unseen. So the vendor mark was already invisible online and only ever showed in the five dev games (whose `app.html` has no shell) — the "replace the logo" job was half-done by an overlay nobody had connected it to. Running a spine ANIMATION under that overlay would be pointless the same way, so `__ieBoot` gained `whenDone(cb)` and the sequence waits on it: progress bar → engine mark → game mark → game. Hosts with no shell start immediately.

  **Fail-open throughout, because this is the boot path.** No index, an empty index, a renamed bundle, a missing animation, no WebGL — each skips its tier and calls `oncomplete`; nothing here can stall a game. A named-but-unresolvable bundle logs a warning rather than failing a publish. `LoaderSpine` stands up its own `Application` (the game's does not exist yet) pinned to `preference:'webgl'` (WebGPU inits but paints nothing on iOS/Pixel) and **destroys it on teardown** — the GL context is capped and the game's renderer wants one moments later. An animation name that no longer resolves falls back to the first clip rather than the setup pose, which renders empty.

  **One trap caught by the offline fixture, not the build:** `normalizeGameSettings` in `editorStorage.ts` is a strict whitelist, so `bootLoader` round-tripped away on every save — the field would have appeared to save in the editor and never reached R2. 14 offline assertions over the REAL modules now cover `normalizeBootSplashRef` (blank/`..`/absolute rejected, trimming, nested bundles) and that round-trip, including "settings holding only a bootLoader isn't dropped as all-defaults". `launcher-api` + all six games build clean.

  **Migration note:** `LoaderStakeEngine` / `LoaderExample` are DELETED from the `components-shared` barrel (`LoaderBase`/`LoaderImage` stay — still generic and exported). A game repo carrying a frozen copy of `apps/lines/src` still imports them, so its next engine-submodule bump needs the `+layout.svelte` reconciled (`gotcha_game_repo_app_src_drift_from_apps_lines`). ⏳ **Not browser-verified** — needs a spine published to `_shared/spines/` and a publish; nothing is configured yet, and with both tiers unset boot is byte-identical to today minus the two gif splashes.

- 2026-08-20 — **The cascade fixture is per-GAME, because a global flag would have cascaded Book of Borut.** #385 shipped the fixture reading `process.env.CASCADE` inside the mock. One Invisible Test Server process serves EVERY game, so setting that env — the literal next step for seeing the cascade — would have made the shipped `bookofborutremake` tumble on every spin. Caught before enabling it, not after. Now `CASCADE_GAMES` is a comma-separated allowlist of game keys (`CASCADE_GAMES=test4`, or `*` for all, unset ⇒ nobody), read by the SERVER, which passes an explicit boolean per game into its mock. The mock's env read survives only for the standalone CLI, where there is one game and no ambiguity — and `opts.cascade ?? env` means an explicit `false` from the server always wins over a stray env. Verified by running two mocks side by side as the server configures them: `test4` emits tumble steps, `bookofborutremake` emits none. **To see the cascade:** set `CASCADE_GAMES=test4` on the test-server service, spin, and note the cells play their authored `explosion` state — a project with none authored in `/symbols` will show them vanish rather than explode.

- 2026-08-20 — **The cascade overlay was unreachable — mounted now, plus an opt-in cascade to drive it.** Owner asked how to see `TumbleBoard`; the answer was that you could not, and not because of a flag. #375 put `TumbleBoard`/`MultiplierBoard` in `registerBoundComponents` and stopped there — that registry only declares that a scene node MAY bind a component, and nothing did, so neither ever mounted, never subscribed to its cues, and the `tumbleBoard` handler broadcast into nothing. `SpecialBook` works because the reference layout carries a `specialBook` scene with a `bind` anchor; the cascade had no equivalent. **Two independent reasons nothing rendered, and #375 only reported the second** (no RGS sends `tumbleBoard`). The vocab harness passed throughout because it tests the CONTRACT — that the palette declares these surfaces — not that anything mounts; byte-identity and contract tests cannot catch "right component, never mounted". Fixed by mounting both DIRECTLY beside `<Board />` inside the `reelStack()` snippet, which is what they are (the tumble stands in for the reels between `boardHide`/`boardShow`; the collect flies multipliers to board centre) — and by REMOVING them from `registerBoundComponents`, since a bind anchor plus a coded mount would double-subscribe and play every cue twice (the `gotcha_coded_ui_doubles_hud_under_flowv2` shape). Neither needs placement: both position off `boardLayout()`. Both render nothing until their cue, so an un-cascading game is byte-identical with them mounted. **Something to look at:** the mock gains a `CASCADE=1` presentation fixture (same idiom as `FORCE_TRIGGER`/`BIG_WIN`/`STACKED`, off by default) emitting two deterministic `tumbleStep` events per spin — explode the most common symbol, refill from above — and `engineFacade` translates them to `tumbleBoard`, shifting rows by the `BOARD_PADDING_ROWS` the reveal adds. ⚠️ **`tumbleStep` is NOT a captured protocol.** Every other case in that switch was verified against a real Play4Fun session; this one is ours, invented so the presentation has input, gated off, and labelled at both ends — a real provider's cascade should REPLACE it, not be bent to fit. Verified: the mock emits 2 steps with per-reel refill counts matching the explosion counts, and with `CASCADE` unset emits none (parity). **NOT verified: the facade case is prettier/eslint-clean but never executed** — `apps/lines` does not import `engine-facade`, so no build compiles it, and the translator package has no test harness. First live cascade will exercise it. Also unverified live: the games host (`games.invisiblewall.org`) was 502ing across all four games during this work.

- 2026-08-20 — **The multiplier-collect mechanic lands too — scatter phases 1–2.** Scatter is the cascade PLUS one beat: multipliers landed during a tumble play their win state in place, fly to the board centre, and combine into a board multiplier. So it cost roughly a third of cluster, and `SCATTER_VOCAB` is built **ON** `CLUSTER_VOCAB` rather than beside it — the two share the whole cascade, and expressing that as inheritance is what stops the tumble surfaces being declared twice and drifting. New surfaces are exactly `boardMultiplierInfo` + six cues. **Two things came out of the reference rather than being written.** (1) `stateGame.multiplierBoard` and its `MultiplierSymbol` type were ALREADY in the shared board state — flagged as dead scaffolding when the cascade landed, they turn out to be scatter's model, lifted by the Phase A extraction and left waiting for these components, so nothing new had to be added to hold it. (2) The reference tested `rawSymbol.name === 'M'` to decide what collects — a hardcoded symbol id that would silently collect NOTHING in a project that named its multiplier anything else; the port tests `RawSymbol.multiplier !== undefined`, the property the value actually rides on. Also simplified deliberately: `MultiplierSymbol` renders through the ordinary `Symbol` state machine instead of the reference's bespoke `SymbolSpineMain`/`SymbolSpineBackground` pair, so its art is whatever `/symbols` authored for the `win` state rather than a second art path no tool reaches; and the beat's totals ride the game's ordinary win meter rather than a `tumbleWinAmount`/`multiplierTotal` display stack of its own. Fixed while porting: the upstream `boardMultiplierInfo` type declared `tumbleWin: 400; boardMult: 5; totalWin: 2000` — example values pasted in as LITERAL types — now plain numbers. Verified: `flowV2Vocab` over four templates, asserting in both directions that scatter INHERITS the cascade (so a later rebase onto the standard palette could not silently drop the beat it is mostly made of), adds exactly the collect surfaces, takes nothing away, and declares `M` where cluster does not; `v2drivenseed`/`v2runtime`/`v2mount`/`v2buyflow` unregressed; `gen-flow-vocabulary --check` green at 55 events / 16 book events; both apps build; eslint clean. **Same blockers as cluster** — no RGS sends `boardMultiplierInfo`, and the cascade wire has never been captured. One step better off: scatter ships a committed config default where cluster's upstream strips are empty. ⏳ Not browser-verified. **Engine change on `main` — reaches an online game only on a runtime republish + `engine` submodule bump.**

- 2026-08-20 — **The cascade (tumble) mechanic is in the shared runtime — cluster Phases 1–2.** Reverses the 2026-08-19 scope-out, which was right that a cascade is a board MECHANIC rather than a template and wrong about what that costs. Two findings did it: **(1) the tumble is an OVERLAY** — `TumbleBoard` mounts for the cascade and unmounts again (`boardHide` → show → explode → remove → slide → `boardSettle` → `boardShow`), so a game that never tumbles never mounts it and is unaffected; **(2) `explosion` was already an authorable symbol state** in `SYMBOL_STATES`, ungated, so the cascade's defining animation is authored in `/symbols` like any other state and the mechanic needed no symbol tooling. Landed: `stateTumble.svelte.ts` (the `base`/`adding` two-layer model — kept apart so `removeExploded` can filter survivors without touching symbols still falling), `TumbleBoard`/`TumbleBoardBase`/`TumbleSymbol` in `apps/lines` (which IS `_runtime/lines`, the same reason `SpecialBook` lives there rather than in `engine-game` — the tumble depends on the game's own `Symbol`/`SymbolWrap`, one of the Phase A slices still unextracted), three book events + handlers, seven cues, and a registered `/flow-v2` `cluster` vocabulary. **The one thing that did NOT port** is the geometry: the reference game seats symbols at `(index + 0.5) * SYMBOL_SIZE`, which this runtime outgrew when the reel grid became authorable, so **`getSymbolY` was added beside `getSymbolX`** as the shared resting-seat expression `createReelForSpinning` already computes — a cascade must drop a symbol on the SAME seat a settled reel would give it, and matching a constant would have held only until the first project resized its grid. Also fixed on the way past: keying `TumbleBoardBase`'s inner `{#each}` by the symbol OBJECT rather than its index, because `removeExploded` shifts every surviving index and index-keying would tear down the very `symbolY` Tween the slide is about to animate (the reference implementation has the un-keyed version). Verified: `flowV2Vocab` 25/25 over three templates incl. cluster adds EXACTLY the cascade surfaces and no book-of mechanic, and the book-of reveal flow is REJECTED against the cluster vocab; `v2drivenseed`/`v2runtime`/`v2mount`/`v2buyflow` unregressed; `gen-flow-vocabulary --check` green at 49 events / 15 book events; `apps/lines` + `launcher-api` build clean; eslint clean. **A cluster game still cannot be published** — no RGS sends `tumbleBoard` (the wire would be invented, not transcribed: there is no capture of a cascade game), and `apps/cluster` ships empty `paddingReels` so there is no config default to seed from. ⏳ Not browser-verified. **Engine change on `main` — reaches an online/remake game only on a runtime republish + `engine` submodule bump.**

- 2026-08-20 — **A cinematic could not be skipped while the pointer rested on the spin button — an inert button still eats the click.** Free-spin intro/outro and the big win put a full-screen tap-to-continue overlay up, and #153/#163 locked the spin button so a press could not slam past it. But locking only kills the press BODY: the button keeps `eventMode: 'static'`, so it stays the hit target, and `PressToContinue`'s own full-screen rect paints at the OVERLAY's z — below the HUD. A tap over the button therefore did nothing at all (button inert, overlay never reached) and the player had to move the pointer off the HUD to skip. Turbo was worse: not locked at all, so a tap there toggled turbo under a full-screen overlay instead of dismissing it. Fixed by inverting who masks whom. New `<ContinuePressMask>` (`engine-game`) mounts ONCE in `Game.svelte` at a new `LAYER_BAND_INPUT_MASK` (11_000, the one band above `LAYER_BAND_TOP`) and, while any press is live, covers the canvas and runs the NEWEST registered press body — so the overlay masks the chrome, a tap anywhere skips, and every HUD button (menu, buy, bet steppers, autoplay) goes untappable for exactly as long as the overlay is up, without per-button pointer surgery. The handler registry lives beside `stateUi.continuePressCount` (`registerContinuePress` / `runTopContinuePress`), which `PressToContinue` now feeds alongside the count it already kept; newest-wins mirrors what the per-overlay rects did by paint order across a fade-out/fade-in overlap. Nothing mounts when no press is live ⇒ the HUD is byte-unchanged the rest of the time. Greying rode along as the AFFORDANCE: `isCelebrationLocked()` is now exported from `utils-shared/spinStop` and the turbo button (coded + parametric) greys on the same read the spin button uses, so a dead button looks dead. Verified live on `apps/lines` + the book mock: turbo greys with spin the moment the lock raises, and the mask band mounts empty until a press exists. **Reaching online games needs a Runtime release** (they run the shared `_runtime/lines` bundle).

- 2026-08-20 — **Reel anticipation now runs on a ways game — the one place the client actually evaluates a board.** "Non-lines win evaluation in the client" sounded larger than it is: the client never DETERMINES wins (it receives `winInfo` with positions from the RGS), so the only surface that scores a board is reel anticipation, where `createLinesReach` answers "could this reel still complete a paying run". #357 made ways stand DOWN there rather than feed line maths a board it does not fit. New `createWaysReach` (`utils-slots/anticipationReach.ts`) closes it, and it is a genuinely different walk rather than lines math minus the paylines: (1) there is no row to walk — a reel either holds the symbol or it does not, and the locked prefix carries a running PRODUCT of per-reel counts; (2) symbols do not compete — a payline pays its single best interpretation, ways pays every completed run at once, so it SUMS across symbols exactly as `evaluateWays` does; (3) the optimistic bound must maximise `mult(r) x ways(r)` over the reachable lengths, because a longer run is not automatically worth more (`mult(r)` can be absent at some `r` while the ways product keeps growing) — the line walker can safely assume full width, this cannot. The divisor is the product of each reel's VISIBLE row count read off the board, so a stepped grid counts correctly (mirrors `activeWaysCount`). **`triggerBounds` turned out to be model-INDEPENDENT** and was extracted to a shared helper: a scatter triggers on a count anywhere, never along a line, which is why "tease the 3rd scatter" needed no ways variant at all. `anticipation.ts` picks the reach by `activeWinModel()`; `cluster`/`scatter` still stand down through the empty payline list, now stated in a comment so it doesn't read as an accident. **The boot warning was narrowed** — it claimed a ways game "is being played as LINES", which stopped being true across #357/#360 and this change, so it now fires only for `cluster`/`scatter`. Verified: new `node packages/utils-slots/anticipationWaysReach.fixture.ts` — bounds narrow monotonically AND converge on the board's REAL payout (computed independently in the fixture, since a reach that converges on the wrong number still passes every monotonicity check), a run broken among locked reels cannot be rescued, the ways product is honoured, trigger-reach matches lines; the existing lines fixture is unchanged and still passes after the shared-helper extraction; `apps/lines` builds clean; eslint clean. ⏳ Not browser-verified — anticipation is opt-in per flow and no ways project exists yet. **Engine change on `main` — reaches an online/remake game only on a runtime-bundle republish + `engine` submodule bump.**

- 2026-08-20 — **The game-type-templates plan is finally readable: `docs/design/game-type-templates.md` now exists.** It was cited as the authoritative numbered plan from ~14 code comments and both status files, and had never been written — so rule 7's "read the registered next step" pointed at nothing. Reconstructed from the shipped work (#355 / #357 / #360, plus the `engine/ways-authoring-kind` branch for Phase E), with a section per named phase — A0–A5, B, C, D, E — so every citation resolves. It also records the owner decision of 2026-08-19 that **`cluster` / `scatter` templates will NOT be built**: both need the tumble/cascade mechanic the shared runtime has never carried (`tumbleBoard`, `updateTumbleWin`, `updateGlobalMult`, plus `updateGrid` for cluster and `boardMultiplierInfo` for scatter), which makes them a board mechanic rather than a template; their win models stay declared and correctly priced. Open item 1 above was rewritten at the same time: its headline still said Phase A was IN PROGRESS with only A0/A1 landed, which its own body (A2–A5) and `packages/engine-game` on disk both contradicted.

- 2026-08-19 — **A ways win drew four disconnected vertical dashes instead of a win line — the cluster heuristic can't tell a ways win from a Book-of expansion.** `WinLine.svelte` has always had two shapes, picked by a `columnWin` flag that `winLineColumnWinFor` computed as "does any reel pay more than one cell". That is exactly right for the case it was written for (a **lines** game whose special symbol expands to fill whole reels — Book of Borut — where a connected polyline traces an unreadable criss-cross zig-zag) and exactly wrong for **ways**, where a duplicate reel is the normal case, not the exception. Project `test3` (`winModel: {type:'ways', direction:'ltr', minKind:3}`, 5×3, zero paylines) paid hearts on reels 1-4 with reel 4 contributing two cells — 2 ways — and the flag flipped, so the line rendered as one short bar per paying cell. The shape is now decided by the **declared win model**, not by counting reels: `winLineShapeFor()` returns `'reels'` for `ways`, `'cells'` for `cluster`/`scatter`, and for `lines` keeps the duplicate-reel heuristic (so the shipped Book-of expansion is byte-unchanged). The new `'reels'` shape merges a reel's winning cells into ONE bar spanning topmost→bottommost ± half a cell — a ways win pays by whole-reel participation, so the reel is the unit, and cells on the same reel join even when they are NOT adjacent. Reels are keyed by the point's `x` (`getSymbolX(reel)`, one exact value per reel), so the grouping is lossless without threading reel indices through the emitter event. `columnWin?: boolean` on `winLineShow` becomes `shape?: WinLineShape` — not in any FlowDoc or emitter vocabulary, so nothing authored changes — and all three dispatch sites (coded handler, `showWinLine` effect, resting win cycle) share the one helper as before. Both bar shapes still skip the head-trace reveal and the full-payline underlay, and still stamp the amount ONCE centred over the winning columns. `svelte-check` 0 errors. **Ships to online games with a Runtime release** (they run the shared `_runtime/lines` bundle).

- 2026-08-19 — **Every button in a fully authored HUD threw on press — the whole parametric HUD was inert.** `<ButtonFrame>`'s `onpress` read the param context from inside its Pixi `onpointerup` callback (`getComponentParams()['onpress']`), and `getComponentParams()` is `getContext`, which Svelte 5 permits only during component initialisation. So the press threw `lifecycle_outside_component` _before_ reaching either the Flow v2 route or the coded fallback — flow-owned or not, the button was dead. It read as "the flow isn't wired" rather than as a crash because the button still rendered, hovered and painted its states correctly (every other call site in the file sits inside `$derived`, which runs in the reactive context and was fine). The coded HUD never hit it — `ButtonBetProvider` and friends don't go through `<ButtonFrame>` — so this only bit projects driving a fully authored HUD, which is why it survived this long. Fix: hoist the params object to init; `<ComponentInstance>` provides a stable object whose engine-provided keys are live getters, so holding the reference keeps every read reactive. Verified by stash/un-stash against a real authored HUD (project `test3`, 7 buttons) on one dev server — before: all 7 threw and the bet never moved; after: none threw and the coded `increase` body ran. Then verified LIVE after the Runtime release: all 7 press clean, and the authored buy chain runs end-to-end (BUY BONUS ▶ `buyFeature`; card press ▶ `selectBetMode` ▶ `buyConfirm`; cancel ▶ back to `buyFeature`). #358.

- 2026-08-19 — **Game-type templates, Phase A0: `packages/engine-game` + a build gate that can actually prove "unchanged".** Phase A relocates ~80% of `apps/lines/src` into a shared package so a game type becomes a mechanic rather than an app fork; the invariant is that `lines` keeps emitting identical code at every slice. That claim needed a check, and the obvious one was worthless as-is: **two builds of an untouched tree produce different bundles** (`B-2n2O0P` → `BVP2bHQi`). Three sources, one root — the `ie-build-stamp`'s wall-clock `builtAt` changes every build, which changes the content hash, which changes the content-hashed filename the bundle embeds _self-referentially_; SvelteKit's per-build random app id (`__sveltekit_dhfji1`) is the third. New `scripts/bundle-fingerprint.mjs` normalizes exactly those three, after which two builds of an unchanged tree are bit-identical (verified over three runs). Baseline for `lines` at `9a3cfa97`: bundle `5adc7bc7bfcc44a1`. The A0 slice itself is deliberately **type-only** — `types.ts` splits, with the agnostic vocabulary (`SymbolName`, `RawSymbol`, `Position`, `SymbolState`, `SymbolCellInfo`, `SymbolInfoMap`) moving to the package and `BetMode`/`GameType` staying in the app (bound to the compiled config, shared vocabulary with the RGS). Types are erased at build, so a correct move MUST be bit-identical — it is, which proves the package resolves _and_ that the gate reports nothing when nothing changed. Package is source-entry (`main` → `index.ts`, no build step), modelled on `components-pixi` not `engine-layout`, because a pre-built `dist/` is what lets a game ship stale engine code. Note for later slices: `pnpm lint` (exits 2 for existing packages too — the eslint 9 flat-config migration was never done) and `tsc` (~53k pre-existing errors, no `svelte-check` installed) are NOT usable gates here; the fingerprint is.

- 2026-08-18 — **One home for the engine's coded UI strings, so the Localization tool can see them.** `BALANCE`/`WIN`/`BET`, the menu + info-page entries, the bet menu, the settings and autoplay modals and the info page's rules copy all already localized by source-as-key (every one goes through `stateI18nDerived.translate`), but the literals were inlined across two `i18nDerived` maps and `apps/lines`' `infoManifest` — invisible to `/localization`, so there was no row to translate and the chrome stayed in the source language for any language the code catalogs ship no entry for (`en`/`zh` only). New `engine-layout/uiText.ts`: `UI_TEXT` (the catalog keys, semantic-name → source literal), `UI_INFO_RULES` (the default info-page rules), and `collectUiTextStrings()` — the launcher harvests the SAME registry the components render from, so the two cannot drift. Literals unchanged, so no existing translation is orphaned. Two behaviour fixes rode along: `HudReadout` rendered an authored bet-mode `betAmountLabel` RAW while its `HudCaption`/`LabelBet` siblings translated it, and the social-build relabels (`SPIN`/`PLAY BONUS`) bypassed `translate()` entirely — both now localize. `pnpm build --filter=lines...` green. Detail in [localization.md](localization.md).

- 2026-08-18 — **The runtime fetch aborted at the instant the response arrived, silently serving STALE BAKED data on ~half of boots.** `RUNTIME_ATTEMPT_TIMEOUT_MS` was 30s, set against a then-measured ~17-19s assemble. Measured 2026-08-18 on Book of Borut Remake (183 KB bundle): TTFB **29.6s / 33.1s / 34.2s** over three fetches — the cap sat INSIDE the response spread. The retry usually rescued it (the endpoint single-flights, so attempt 2 joins the run in flight), which is why it read as "flaky" rather than broken; the only symptom was `live data fetch failed (signal timed out) — retry 1/2`. A boot that lost the coin flip is not merely "a bit stale": measured against the baked bundle via a temporary probe, `winText.toast.symbolAsImage` is `false`, `bakedSymbolNames()` is empty so a symbol renders as its raw id (`H4`), and the win-text template is the English default — several unrelated-looking wrongnesses at once on a game that looks healthy. Cap 30s→**60s**; budget 45s→**90s** and now a REAL deadline (each attempt clamps to the budget remaining, so a hang is bounded at exactly 90.0s instead of overrunning to ~121s). New near-miss warning: a SUCCESSFUL attempt spending >60% of its cap logs what to raise, so the next rot announces itself before it starts failing. Verified on a fake-clock harness over the patched control flow (34s → succeeds attempt 1; 50s → succeeds + warns; 502×2 → retries then succeeds; 401 → fails fast; hang → bounded 90.0s); `pnpm --filter lines build` green. **Still open: the ~30s assemble itself**, which every live-data boot pays.

- 2026-08-17 — **Win text lost its `{amount}`/`{count}`/`{symbolName}` — regression from the localization fix, same day.** `registerEditorTextLocalization`'s resolver returned `stateI18nDerived.translate(key)`, i.e. `i18n._()`. A runtime message compiler is installed, so `_()` evaluates the message as ICU — and with **no `values`** it substitutes EMPTY for every `{token}`. Win text is deliberately localize-THEN-interpolate (`formatWinText`), so the template's placeholders were consumed before the engine could fill them: `"You win {amount} with {count} {symbolName}"` rendered as a bare **"Vinci con"**. It also killed the inline symbol IMAGE, which keys off the interpolated `{symbolName}`. Latent until the catalog actually resolved — which is exactly what the fix above enabled, and it would have hit English too. The resolver now returns the RAW catalog string (falling back to `translate()` for a non-string value); plain strings are byte-identical either way. Verified live: `"Vinci 10,00  con 5"`.
- 2026-08-17 — **Currency renders its SYMBOL wherever the symbol is unambiguous.** `Intl`'s default (`currencyDisplay: 'symbol'`) prints the ISO code whenever CLDR judges the glyph ambiguous for the locale — Italian gives `€` and `£` but `1234,50 USD`, because a bare `$` doesn't say WHICH dollar. Forcing `narrowSymbol` globally wins back `R$`/`zł`/`₹`/`₺` but collapses USD/CAD/AUD/NZD/MXN/ARS/CLP into one indistinguishable `$`. So `numberToCurrencyString` now takes the narrow symbol **only when nothing else claims it** — uniqueness COMPUTED from `Intl.supportedValuesOf('currency')`, not a hand-kept table that would rot, cached per (locale, currency) and skipped entirely on a runtime without `supportedValuesOf`. In `it`: BRL `BRL`→`R$`, PLN→`zł`, CZK→`Kč`, HUF→`Ft`, RON→`lei`, TRY→`₺`, UAH→`₴`, INR→`₹`, IDR→`Rp`, VND→`₫`, ZAR→`R`, NGN→`₦`, NOK→`NKr`; USD/CAD/AUD/NZD/MXN/ARS/CLP/JPY/KRW/CNY unchanged, and `en` is unchanged throughout. **Caveat that bit immediately:** a BITMAP font only renders glyphs in its charset. Book of Borut's four fonts carry 94–95 ASCII glyphs — `$` yes, `€`/`£`/`¥` no — so a non-ASCII symbol renders as nothing where the ISO code used to render fine. Rebuild affected fonts in Font Maker with the `currency` preset (`0123456789.,$€£¥¢+- `) or a custom charset before selling a title in such a currency.

- 2026-08-17 — **A project's translations never reached the game — two independent bugs, both fixed.** Symptom: a fully-translated project rendered English in every locale while NUMBERS localised correctly (`5000,00 €` under `lang=it`), with no error anywhere. (1) **The merged catalog was snapshotted before the runtime bundle existed.** `apps/lines/src/i18n/messagesMap/index.ts` built `const messagesMap = mergeMessagesMaps([…, bakedLocalizationMessagesMap()])` at MODULE scope, but that getter only returns live strings once `prepareRuntimeBundle()` has run — and that is `+layout.ts`'s `load()`, which SvelteKit runs AFTER importing the route's modules. So it always captured the empty baked map and the fetched translations were dropped. Every other runtime-bundle consumer is a lazy function, which is why localization alone was affected. Now `getMessagesMap()`, called from `+layout.svelte` (`<LoadI18n>`) and `Game.svelte` (`registerEditorTextLocalization`) — both after `load()`. Deliberately NOT memoised: a cache re-introduces the same class of bug the moment something calls it early. (2) **Authored component `label` params were never run through the text resolver.** A parametric HUD readout carries its caption as `params.label` (`"Balance"`), which the Localization tool harvests as a key — but `resolveLocalizedText` only covered text nodes and `textBox.text`, so the three coded consumers (`HudReadout`, `HudCaption`, `ButtonLabel` in `components-ui-pixi`) rendered the literal. Resolved at the consumer rather than in `resolveComponentParams`, so a frozen/saved component def can't shadow the fix and enum-ish string params (`source`, `variant`, `icon`) are untouched. Verified live against the real Book of Borut Remake runtime bundle: `lang=it` → `Saldo` / `Vincita` / `Puntata` / `Fai clic sul pulsante di giro per iniziare`; `lang=en` byte-identical to before. **Reaching online games needs a Runtime release + republish.** Related: `/localization` ships REVIEWED strings only — an unreviewed translation reaches an `ie_authoring=1` launch but never a player or the bake.

- 2026-08-17 — **`?currency=` is now read, and OVERRIDES the RGS's currency.** New `stateUrlDerived.currency()` (`packages/state-shared/src/stateUrl.svelte.ts`) + `Authenticate.svelte` taking `launchCurrency || authenticateData.balance.currency`. Previously the launch param was declared in the `Key` union but had no getter, so every game formatted amounts with whatever the RGS echoed — always `USD` from the mock and from the Play4Fun facade. The picker that drives it lives in the launcher; see [launcher.md](launcher.md) for the full entry.

- 2026-08-11 — **Authorable layout profiles — the layout-bucket set is now author-defined, not the hardcoded four.** New `constants-shared/layoutProfile.ts` defines a `LayoutProfile` (ordered buckets, each with `id`/`label`/`box`/`rule{minRatio,maxRatio,minSide,maxSide}`/`stacked`) + `selectBucket`/`resolveBucketBox`/`normalizeLayoutProfile`. `DEFAULT_LAYOUT_PROFILE` reproduces the legacy `desktop/tablet/landscape/portrait` decision tree EXACTLY (proven byte-identical across a device grid in `packages/engine-layout/scripts/test-layout-profile-parity.mjs`), so a game shipping no profile is unchanged. `utils-layout/createLayout`: `layoutType()`/`isStacked()` now derive from `selectBucket(activeProfile)`; new `setAuthoredLayoutProfile()`/`getActiveLayoutProfile()` (twin of `setAuthoredMainSizesMap`); the STANDARD frame box is profile-driven via `resolveBucketBox` (crash-safe fallback for an author-added bucket a coded game has no box for). `LayoutType` widened to `string`; `LayoutDoc` gained optional `layoutProfile`. Authored in Admin (pipeline default, `app_settings.layoutProfileDefault`) + the Scene Editor (per-project override, sparse on the doc) — see `docs/status/editor.md`. The publish bundle bakes the effective profile onto the doc; `apps/lines/src/editor-scenes.ts` adopts it at boot via `setAuthoredLayoutProfile(doc.layoutProfile)`. Reaches online games via a **Runtime release + republish**. NOT browser-verified (needs the live launcher). Files: `packages/constants-shared/{layoutProfile.ts,layout.ts}`, `packages/utils-layout/src/createLayout.svelte.ts`, `packages/engine-layout/src/lib/{types.ts,index.ts}`, `packages/game-spec/src/schema.ts`, `apps/lines/src/editor-scenes.ts`.
- 2026-08-10 — **Per-layoutType overrides now carry TEXT STYLE + COMPONENT PARAMS at runtime** (see `docs/status/editor.md` for the full write-up). `NodeOverride` gained sparse `style?: Partial<TextStyle>` + `params?: Record<string, unknown>`; `<LayoutNodeView>`'s `resolvedStyle` folds the per-ratio style (`resolveOverrideTextStyle`) under bound-param overrides, and `<ComponentInstance>` overlays per-ratio params as REACTIVE getters (keyed on the override-union keys, read `getContextLayout().layoutType()` so a device rotation re-resolves; defined before the live-feed getters so `value`/`label` still win; the init-stable structural reads that use `staticParams` directly are untouched). Additive + sparse ⇒ a doc with no per-ratio override is byte-identical (parity). Reaches online games via a **Runtime release + republish** (schema rides in the doc; the reader code ships with the runtime). Files: `packages/engine-layout/src/lib/{types.ts, resolveTransform.ts, componentParams.ts, LayoutNodeView.svelte, ComponentInstance.svelte}`.
- 2026-08-10 — **Flow v2 §6.3 in-game TEXT MESSAGE overlay — GAME-SIDE RENDER (`apps/lines`).** The schema/interpreter/handle were already built (`packages/engine-flow-v2` `TextMessageNode`; `flowV2Runtime.svelte.ts` now exposes `textMessages` + reactive `messageShown`). Added the Pixi render: new `apps/lines/src/components/FlowV2Messages.svelte` renders one localized `<Text>` per authored `textMessage` node at its normalized `place`, INSIDE `<MainContainer>` (design box `mainLayout.width`×`height`, read via `getContextLayout()` — so a place of {0.5,0.5} is dead-centre and lands identically across layouts). Visibility per node = `gateMatches(node.visibleWhile) || flow.messageShown(node.id)` — the OR serves BOTH use cases with one rule: a state-gated prompt ("Click spin to start", `visibleWhile:'idle'`) shows while its round-phase gate is on, a flow-driven prompt ("Good luck", bare `show` exec + `autoHideMs`) shows while the interpreter's reactive `SvelteSet` flag is raised. `gateMatches`: `idle`→`stateXstateDerived.isIdle()`, `spinning`→`isPlaying()`, `always`→true, `none`/undefined→false, and **`freeSpins`→`stateGame.gameType === 'freegame'`** — the game MODE flag (set by the freeSpin book events, cleared to `basegame` when the feature ends), chosen over `stateUi.freeSpinCounterShow` because the counter's a HUD-visibility flag the intro/outro celebration screens flip off MID-feature. Text is run through `resolveLocalizedText` (localize-then-render); style defaults to readable white 40px `proxima-nova` with a dark stroke + drop shadow, overridable by `node.style.size`/`.color`. Wired into `Game.svelte` near `<FlowV2Mount>`, mounted UNGATED by `flowV2DrivesScreens` (a book-events-only flow can still author messages); the handle is mirrored into a `flowV2Handle` `$state` at load, alongside `flowV2Containers`. Independent of the shared single-slot `stateMessage` HUD toast. Renders nothing when no v2 flow / no message nodes (parity). Verified: `pnpm --filter lines build` green; `svelte-check apps/lines` **0 errors** (2 pre-existing tsconfig warnings only); Prettier clean on the new file. NOT browser-verified (needs a flow authoring a `textMessage` node). Reaches online games only via a `Runtime release (runtime=lines)` + republish; standalone `bookofborut` picks up the `packages/*` half on an engine submodule bump but must mirror the `apps/lines/src/components/{FlowV2Messages.svelte, Game.svelte}` wiring into its own frozen `src/` copy. Files: `apps/lines/src/components/FlowV2Messages.svelte` (new), `apps/lines/src/components/Game.svelte`.
- 2026-08-03 — **Fix + polish: fast-forward / tap-to-skip and win-tier escalation.** The count-up (`WinGate` `WinCountUpProvider`, accelerated by `holdToSpeedUp` / slammed by `tapToSkip`) runs on a SEPARATE clock from the spine idle-completes that advance the escalation chain (`WinAnimation`). When the count-up finished mid-chain the gate concluded ~300ms later while the tiers were still walking ⇒ the escalation was cut off. Three parts: (1) **SMOOTH RAMP** — while the player HOLDS to fast-forward, `WinAnimation` runs the tier intro/idle spines at the gate's live `interactionSpeedScale` (published as `winState.escalationSpeedScale`, applied as the `SpineTrack`/`TrackEntry.timeScale` via `propsSyncEffect`), so the tiers visibly accelerate in lockstep with the count-up; release ⇒ 1×. Only the HOLD drives it (a `tapToSkip` slam stays instant). (2) **COLLAPSE SAFETY NET** — if the count-up still completes before the (now-faster) chain reaches the final tier, `WinAnimation` jumps `stepIndex` to the last step + plays its outro (`animationState='outro'`), so the player lands on the biggest tier's art as it exits. (3) The GATE defers conclusion until that outro finishes — `WinGate.concludePresentation()` awaits `winState.escalationOutroComplete` on the escalation path (`escalationActive`, published by `WinVisual`) before resolving the round-block, routed from BOTH the count-up `OnMount` and the coded `PressToContinue` post-count-up tap. Coordination = `winState` latches (`escalationActive`/`escalationOutroComplete`/`escalationSpeedScale`) + a `WinAnimation.onOutroComplete` callback; empty-outro safety fires completion immediately (a mis-authored tier can't hang the now-awaiting gate); per-win rewind + `interactionSpeedScale` reset on `winShow` (no stale fast-start on a repeat win). NON-escalating single-tier path (no `chain`) byte-identical: ramp/collapse/rewind/outro-wait all guarded on `escalating`, `timeScale` resolves to 1 (= spine default), and the un-escalating gate concludes as before incl. tap-to-slam. Files: `apps/lines/src/game/winState.svelte.ts`, `components/{WinAnimation,WinVisual,WinGate}.svelte`. `lines` build + `svelte-check apps/lines` (0 errors). Escalation is config-authored (not the dev default), so live-verify is owner-owed. Ships in `_runtime/lines` ⇒ needs a **Runtime release + republish**.
- 2026-08-03 — **Desktop launcher build broke: `pixi-svelte` `svelte-package` fails (`No Lingui config found` → `src\app.html does not exist`).** The game build (`pnpm --filter "<game>^..." run build`) died at `pixi-svelte`'s `svelte-package`. Root cause: `svelte-package` only loads the app-level `vite.config.js` (`config-vite` → `sveltekit()` + `lingui()`) when it has to **preprocess a `<style>` block** — and #181 added `MemoryHud.svelte`, the FIRST styled `.svelte` in `pixi-svelte/src/lib`, newly triggering that path. It then hit two pieces of standard `svelte-package`-library scaffolding pixi-svelte never had: no `lingui.config` (every other `config-vite` consumer ships one → `lingui()` factory aborts) and no `src/app.html` (SvelteKit `sync` aborts). In-repo turbo cache masked it; the game repo runs `svelte-package` cold. **Fix ([PR #192](https://github.com/Invisible-Wall-SL/Invisible-Engine/pull/192)):** add `packages/pixi-svelte/lingui.config.ts` (re-exports `config-lingui`) + `packages/pixi-svelte/src/app.html` (minimal skeleton; `svelte-package` only packages `src/lib`, so it never enters `dist`). Same class as the `new-game.mjs` bug (4) below. Verified: clean `pnpm run build` in pixi-svelte green; `pnpm --filter lines... run build` (dist wiped) builds end-to-end. Reaches the desktop launcher only after the game repo's engine submodule pin advances to this commit.
- 2026-08-03 — **Win-tier presentation split (config = structure, `win` component = presentation).** The big-win presentation (spine bundle / intro-idle-outro / duration / sfx-bgm) moved off the `/config` panel onto the `win` component, per tier keyed by ALIAS, generated from the active config's big tiers. Engine: `engine-layout` `winComponentDef`/`winTierPresentationParams`/`DEFAULT_WIN_TIERS` (built-in `WIN_DEF` regenerated); `WinVisual` resolves per-tier animation/spine from live params (alias-keyed, replaced `TIER_PREFIX`, **closed the single-tier `spineKey` gap** — `activeSpine = <alias>Spine ?? winLevelData.spineKey ?? winSpine`); duration/sound bridge out-of-tree via `bakedWinPresentationParams()` → `publishWinPresentation()` (Game.svelte boot) → `gameConfig.withWinPresentation` overlay on `activeWinLevelData`/`ByAlias`/`Chain`. Launcher: `/editor` server passes `winTiers`, client `withConfigWinTiers` rebuilds the def; `/config` panel slimmed to name/threshold/escalation. Un-authored ⇒ byte-identical (empty overlay + `DEFAULT_WIN_TIERS` = the coded `winLevelMap` big rows + live params all empty). Verified: `game-config` typecheck/spike, `engine-layout`/`lines`/`launcher-api` builds, `svelte-check apps/lines` 0 errors. Launcher render + live spin owner-verify-owed. **Canvas preview landed** (focused-param → live spine preview of the tier's bundle + animation, `EditorProperties`→`EditorCanvas`→`EditorSpineLayer`, preview-only). **Reaches online games only via a `Runtime release (runtime=lines)` + republish** (the reading code ships in `_runtime/lines`); standalone Borut needs an engine submodule bump + mirroring `apps/lines/src` win wiring. See `docs/status/game-config.md`.
- 2026-07-30 — **Free-spin OUTRO hang on a real large win under a v2-driven, author-rebuilt outro (double `freeSpinOutroCountUp` subscriber).** On the Book of Borut remake (online `_runtime/lines`, v2 flow owns `load`, author-rebuilt `freeSpinOutro`) the `freeSpinEnd` chain HUNG at `action freeSpinOutroCountUp` — the awaited `broadcastAsync` never resolved, so it never reached `showContainer{awaitComplete}`/`exitFreeSpinOutro`; on-screen the outro count text (`freeSpinOutroTotalWin` ← `freeSpinOutroState.countUpAmount`) rose, DROPPED, then rose again at a different speed. Root cause: `resolveFreeSpinOutroMount` (`apps/lines/src/game/freeSpinOwnership.ts`) computed `top: ctx.ownsOutro ? 'driver-transfer' : …` — reading v1 `ownsOutro` BEFORE `flowV2DrivesScreens`. `resolveFlowOwnsFreeSpins` reads the v1 `bakedFlowDoc`, which on a MIGRATED v2 game can COEXIST with the v2 doc and still report `ownsOutro` true (the outro screen + `freeSpinEnd` edge survive in the stale v1 doc). That mounted a SECOND `<FreeSpinOutroDriver holdUntilComplete>` (top band) alongside the v2 `band` `driver` ⇒ TWO `freeSpinOutroCountUp` subscribers (each returns a pending `waitForResolve` from `broadcastAsync`'s `Promise.all`) + TWO `WinCountUpProvider`/`OutroStatePublisher` instances fighting over `freeSpinOutroState.countUpAmount` (the count drop / double-speed). The v2 `band` driver self-resolves, but the v1 `holdUntilComplete` driver waits for an active-set `true→false` edge that never fires under a v2 `showContainer` outro ⇒ `Promise.all` never resolves ⇒ the round hangs (the "invariant" the fs7 test ASSUMED — it SKIPPED the `flowV2DrivesScreens && ownsOutro` combo as "impossible by construction" — was never enforced in `resolveFreeSpinOutroMount`). Fix: `top: ctx.flowV2DrivesScreens ? null : ctx.ownsOutro ? 'driver-transfer' : 'gate'` — v2 forces `top` null UNCONDITIONALLY (v1 `ownsOutro` is inert under v2, now enforced not assumed), so exactly one `freeSpinOutroCountUp` subscriber for EVERY combination. Headless guard: `tools/flow-spike/fs7Outro.ts` B no longer skips that combo (all 16 combos assert one subscriber) + a REGRESSION spot-check; it FAILS on the old ternary (`{band:'driver', top:'driver-transfer'}` = 2) and PASSES on the fix (`pnpm --filter flow-spike run fs7outro`). Reaches the online remake ONLY via a `Runtime release (runtime=lines)` (NOT triggered here); standalone `bookofborut` picks it up on an engine submodule bump.
- 2026-07-30 — **Ambient board smoke fades out under a full-screen tap-to-continue overlay** (retrigger celebration, free-spin intro/outro gates). The signal is `stateUi.continuePressCount > 0` (bumped/decremented by `PressToContinue.svelte`). Two coverage paths: **(1) coded atmosphere** — `apps/lines/src/components/Background.svelte`'s drifting `dust` spine (on both `foregroundAnimation` + `foregroundFeatureAnimation`) is split into its own `FadeContainer`s gated on `show && showDust` where `showDust = stateUi.continuePressCount === 0`, so the dust fades for the duration of the gate and the static `idle` background is untouched; no tap overlay ⇒ `showDust` true ⇒ byte-identical to before (dust always played). **(2) authored atmosphere** — a new visibility source **`tapOverlayIdle`** (`VISIBILITY_SOURCE_KEYS` + label in `packages/engine-layout/src/lib/componentCatalog.ts`, registered in `apps/lines/src/components/Game.svelte` as `tapOverlayIdle: boolSource(() => stateUi.continuePressCount === 0)`) lets an author bind an authored background-smoke node's `visibleSource` to it so it hides during any tap celebration (the coded `Background` is suppressed when authored background art exists, so the authored path is the only lever for an authored spine). Additive — an author who never binds it, and any game with no tap overlay active, is byte-identical. Reaches online games via a runtime release (`runtime:lines`); standalone `bookofborut` picks the engine-layout catalog entry up on a submodule bump, and must mirror the `Background.svelte` dust split + the `Game.svelte` registration into its own `src/*` copies.
- 2026-07-29 — **FS-7 outro follow-up (engine-side): count-up-complete signal + opt-in `WinCountUpProvider` speed-scale; outro coin knobs removed.** Detail in [docs/status/flow.md](flow.md); the engine-side pieces: `ENGINE_SIGNAL_CATALOG` gains `freeSpinOutroCountUpComplete` (`componentCatalog.ts`), fired via a new payload-less emitter event the outro driver broadcasts on count-up completion — an author arms a tap/prompt with `tapArmAfterSignal`/`hiddenUntilSignal`. `WinCountUpProvider` (`components-pixi`) gains an OPT-IN `speedScale` prop for continuous hold-to-fast-forward acceleration: it re-targets the svelte `Tween` on speed change and owns completion via a value-reached watch (because `Tween.set` aborts the previous task without resolving its promise); OMITTED ⇒ the original single-tween path, so the WIN overlay + fallback outro gate are byte-identical (only the lines outro driver passes it). The outro coin-fountain knobs on `boundComponentCatalog.ts` `FreeSpinOutroVisual` (`showCoins`/`coinsX`/`coinsY`/`coinsLevel`) + their `OutroCoinsConfig`/`freeSpinOutroState.coins` plumbing were REMOVED (owner: the driver never emits coins; authors place their own). The separate WIN-visual `showCoins` (`builtinComponents.ts`/`WinVisual`) is UNAFFECTED. Reaches the Book of Borut remake on a runtime-bundle republish + `engine` submodule bump.
- 2026-07-28 — **FS-7 free-spin OUTRO authoring parity (engine-side surfaces).** The Flow feature (headless `FreeSpinOutroDriver` split, full-primitive outro authoring) is logged in [docs/status/flow.md](flow.md); the engine-side additions it needed: `ENGINE_PARAM_CATALOG` gains `freeSpinOutroTotalWin` (the live count-up value source) and `ENGINE_SIGNAL_CATALOG` gains `freeSpinOutroBigWin`/`freeSpinOutroSmallWin` (`componentCatalog.ts`); `ComponentInstance.svelte`'s per-instance fired-signal bus now also subscribes to registered signals named by a `hiddenUntilSignal`/`tapArmAfterSignal` gate (not only spine cues), so plain art can gate on a game signal — parity-safe (no gate ⇒ no new subscription); `boundComponentCatalog.ts` `FreeSpinOutroVisual` gains the coin-fountain knobs (`showCoins`/`coinsX`/`coinsY`/`coinsLevel`). Additive across every game (byte-parity when unused). Reaches the Book of Borut remake on a runtime-bundle republish + `engine` submodule bump.
- 2026-07-27 — **An authored `win` Overlay renders WYSIWYG on ANY screen space (the residual position/size mismatch after the earlier size fix).** Owner report: after the `WinAnimation` natural-size fix landed, the game STILL drew the win overlay smaller AND offset to the upper-left of the board vs the Scene Editor (which shows it board-centred at full size). Root cause was a **coordinate-space mismatch driven by the host screen's SPACE**, not the two BASE sizes (that was the prior fix). The `win` componentInstance sits on a **`canvas`-space** screen ("Base game overlays"), but `BOUND_COMPONENT_DEFAULTS.WinVisual` declares `space:'game', placement:'boardCentre'` — it's a **board-relative** overlay. A `game`-space screen wraps its nodes in `<MainContainer>` (main→window scale + centre, the SAME mapping the reel board uses), so board and overlay scale together and editor==game at any window. A `canvas`-space screen has **no `<MainContainer>`**, so the runtime drew the overlay at **raw window pixels** (`WinVisual` boundToInstance renders `content()` at LOCAL origin, relying on a game-space MainContainer the canvas screen never provides), which does NOT adapt to window size — while the board (game space) scales up with the window via `mainScale`. Net on any window larger than the editor's reference frame: the raw-placed win is smaller than, and above-left of, the scaled-up board. Editor and runtime actually AGREED on the (raw-canvas) formula; they diverged from each OTHER only because the editor's fixed reference frame ≠ the player's window, and canvas space is the one space that doesn't adapt. (Free-spin intro is "perfect" for the same reason inverted: it's a `centre`-**placement** coded gate that self-centres/self-scales to the live canvas, never using raw x/y.) **Fix — one shared notion of "effective space", applied on BOTH surfaces so they can't drift:** new `engine-layout` `hostedComponentSpace(def)` returns the catalog `space` of the FIRST bound component in a componentInstance's def tree (WIN_DEF → `win-anim` bind → `WinVisual` → `'game'`). (a) RUNTIME `LayoutNodeView` componentInstance branch: when the hosted space is `'game'` but the host scene is not `game`/`background`, it re-applies the exact `<MainContainer>` mapping inline (`mainLayout()` centre + scale) to the instance's `x`/`y`/`scale` — a single `<Container>`, so z-order is untouched. (b) EDITOR `EditorCanvas` `nodeTransform`: the same override for a hosted-`game` instance on a non-`game`/`background` scene, mapping via `mainToWorld`/`mainScale` — which the 2D canvas, the spine overlay (`worldTransformOf`=`nodeTransform`) and the selection box all consume, so the preview follows. (c) EDITOR `isGameSpaceNode` gains the same predicate so drag/scale writeback inverts the main mapping and the value round-trips. **Parity:** gated to `hostedComponentSpace === 'game'` AND host scene ≠ `game`/`background`, so a win/free-spin visual on a `game` scene (the reference layouts + the free-spin visuals today) is byte-identical, a `background` scene still cover-fits, and every non-`game`-catalog instance (HUD/buttons/scenery, Background/Transition/LoadingScreen at `canvas`) is untouched. The coded **OFF path** (`bind:Win`, `boundToInstance={false}`) is the Game.svelte mount, never a doc componentInstance — it doesn't hit `LayoutNodeView`'s instance branch and `WinVisual` still self-wraps in `<MainContainer>`; byte-identical. A game not authoring `win` has no such instance. **Free-spin visuals share the risk and are fixed by the SAME generic path** (`FreeSpinIntroVisual`/`OutroVisual`/`ExpandingSymbol`/`FreeSpinIntroSymbolReveal` all declare `space:'game'`): placed on a game scene today ⇒ no change; if ever dropped on a canvas scene they now game-frame automatically. **Verified:** `pnpm --filter engine-layout build` + `pnpm --filter lines build` + `pnpm --filter launcher-api build` green; `svelte-check apps/lines` **0 errors / 0 warnings** (pinned `typescript@5.8.3` + `svelte-check@4.3.0`); Prettier clean. NOT browser-verified. **Not committed / not published** — reaching online games needs a runtime release (`runtime:lines`); the editor half deploys with the launcher; standalone `bookofborut` must bump its engine submodule (the `packages/*` half) — its per-game `src/components/WinVisual.svelte` needed no edit here. Files: `packages/engine-layout/src/lib/boundComponentCatalog.ts` (new `hostedComponentSpace`), `packages/engine-layout/src/lib/LayoutNodeView.svelte` (runtime game-frame override), `apps/launcher-api/src/routes/(app)/editor/EditorCanvas.svelte` (editor `nodeTransform` + `isGameSpaceNode` override).
- 2026-07-27 — **the DESKTOP bake (`bake-editor-doc.mjs`) now embeds flow-v2, so a standalone build of a flow-v2 game drives from the flow like the online runtime does.** Symptom (owner, `bookofborutremakebuild`): the launcher-built standalone rendered the scenes as _placed in the Scene Editor_ — no flow driving, reel not mounted — "as if the flow was never built", while the online `bookofborutremake` (runtime:lines, live fetch) ran the flow correctly. Root cause: two different bake paths had drifted. The ONLINE runtime bundle (`runtimeBundle.ts`) assembles `flow` **and** `flowV2` + `flowV2Library` (via `exportEditorFlowV2`), but the desktop bake `bake-editor-doc.mjs` only ever called `/api/editor/export-flow` (v1) — **zero** flow-v2 handling. The game reads `bakedBundle.flowV2` / `flowV2Library` (`apps/lines/src/editor-scenes.ts` `bakedFlowV2Doc()`/`bakedFlowV2Library()`); the remake authors a v2 flow, so the desktop baked bundle embedded nothing v2 → the v2 interpreter stayed inert → static placement. **Fix, two files:** (a) the `export-flow` endpoint now also calls `exportEditorFlowV2` and returns `flowV2` + `flowV2Library` alongside the v1 `flow` (one bake call covers both; each export self-gates on "authored", so a v1/coded game gets `{}` → byte-identical parity); (b) `bake-editor-doc.mjs` captures those from the response and embeds them in the baked bundle behind the same `...(flowV2 ? …)` parity spread as every other authored block. Reaches a standalone game only after its engine submodule is bumped to this commit + rebuilt; the endpoint half deploys with the launcher. `pnpm --filter launcher-api build` green; bake script `node --check` clean. ⏳ End-to-end verify: rebuild `bookofborutremakebuild` from the launcher (deploy token present) → its baked bundle carries `flowV2` → the standalone drives from the flow.
- 2026-07-24 — **`scripts/new-game.mjs` produced a game that could not build — FIVE bugs, all fixed and verified end-to-end.** Found while standing up a real standalone build for the `test1` project; each one was only reachable after fixing the one before it, so a single smoke-build of the scaffold would have caught the lot years earlier. **(1) Broken tsconfig.** The scaffold wrote `"extends": "./engine/packages/config-ts/svelte.json"`. That file does not exist — `config-ts` ships `base.json` — and `vite:esbuild` fails the build outright on an unresolvable `extends`, so it died on the FIRST module (`src/routes/+layout.ts`) with `failed to resolve "extends"`. Every game the script has ever scaffolded was unbuildable out of the box. Now emits `"extends": "config-ts/base.json"` with the `include: ["."] / exclude: [dist,build,node_modules]` shape every shipped game (`apps/lines`, Book of Borut, HotFruits) already uses — resolved by PACKAGE NAME, since `config-ts` is a `workspace:*` dep. **(2) Dependency drift → invalid compiled JS.** Svelte declares its parser/printer as CARET ranges (`esrap: ^2.0.0`, `acorn: ^8.12.1`, `@sveltejs/acorn-typescript: ^1.0.5`) and a brand-new standalone repo has **no lockfile**, so a fresh install floats them to latest while the monorepo stays on what its committed lockfile resolved (`esrap` 2.0.1 / `acorn` 8.15.0 / `acorn-typescript` 1.0.5). The drift is not cosmetic: **esrap 2.3.0 prints the TypeScript optional-parameter marker into the compiled JS** — `components-ui-pixi/UI.svelte`'s `{#snippet gameName(override?: HudTextOverride)}` came out as `function gameName($payload, override?)`, which is not valid JavaScript, and rollup died with `Expected ',', got '?'`. Identical Svelte version (5.35.1) and byte-identical `svelte/compiler` in both trees — the bug was one layer below it, which is why every config-level comparison (svelte.config/vite.config/pnpm-workspace/tsconfig/plugin list/`ssr.noExternal`) came back identical. The scaffold now emits a `pnpm.overrides` block pinning those three; **re-check them when bumping svelte**. Also added `engine-flow`, `engine-flow-v2`, `engine-fx` to the scaffold's dependency list — `apps/lines` needs all three, so "copy `src/` from an existing game" (what the script's own closing message tells you to do) previously required hand-editing `package.json`. **(3) `svelte` was stale at 5.20.5** while every engine package declares 5.35.1 — and svelte + esrap + acorn are ONE set, so the pins in (2) paired with 5.20.5 blew up on SvelteKit's OWN `error.svelte` with `Not implemented: Program` (an esrap failure). Bumped to 5.35.1 to match the engine. **When bumping svelte, bump the overrides with it.** **(4) No `lingui.config.ts`.** `config-vite` unconditionally registers the `lingui()` plugin, which aborts with `No Lingui config found` when the file is absent; the scaffold never wrote one. Now emits the same one-liner (`import config from 'config-lingui'`) every shipped game has. **(5) No `src/routes/+layout.ts`.** `config-svelte` uses `adapter-static`, which needs `prerender = true`; without it the build got all the way through both vite passes and then died at the adapter with `Encountered dynamic routes`. Now emits `prerender = true` / `ssr = false` (the game is a client-side pixi canvas) / `trailingSlash = 'ignore'`, matching the standalone games. **Verified end-to-end:** scaffold → `pnpm install` → `pnpm build` exits 0 and writes `build/index.html` (59 KB) — previously it failed at the first module. Note for anyone copying `apps/lines/src` into a standalone repo: take it from the **committed `main`** the submodule pins, not the working tree — a dirty engine tree exports symbols (e.g. `resolveSymbolName`) that the pinned engine does not.
- 2026-07-24 (default flip) — **`winCycle.showLine` now defaults ON**, so the resting replay narrates each line the way the spin did (line + stamped amount, then the symbols) unless an author turns it off; `bakedWinCycleConfig()` returns `showLine: c?.showLine ?? true` and the sparse doc keeps only an OFF. Shipped together with the `/symbols` save fix that made the setting persistable at all (see `docs/status/symbols.md`) — until that landed, NO `winCycle` field ever reached R2.
- 2026-07-24 (addition) — **the resting replay can OPTIONALLY redraw the win line + amount again** (`winCycle.showLine`, default OFF ⇒ live behaviour unchanged until authored). Symbols-only was the right default (repeating the per-win narration at rest re-tells a story the player has read), but the owner wanted the pre-change look available as a switch. When on, each pass broadcasts `winLineShow` with that win's `winLinePointsFor`/`winLineTextFor` — awaited, so an animated line finishes tracing and stamps its amount BEFORE the symbols light, the same beat order the spin played — then clears it after. `winLineEnabledForWin` is the shared gate, so the replay inherits the Win-lines toggle and the scatter exclusion for free (a scatter still lights its symbols, just no line). A new `lineOnScreen` flag makes the clear exactly-once and scoped: `stopWinCycle` hides a line THIS cycle drew (a spin press mid-pass can't strand one) and never touches a line the round left. Authored as "Replay the win line too" in the `/symbols` replay section. **Verified in the browser** both ways: default ⇒ animate-only, no line events; `showLine` ⇒ per pass `LINE-show` (amount `- 2026-07-24 — **`scripts/new-game.mjs`produced a game that could not build — FIVE bugs, all fixed and verified end-to-end.** Found while standing up a real standalone build for the`test1`project; each one was only reachable after fixing the one before it, so a single smoke-build of the scaffold would have caught the lot years earlier. **(1) Broken tsconfig.** The scaffold wrote`"extends": "./engine/packages/config-ts/svelte.json"`. That file does not exist — `config-ts`ships`base.json`— and`vite:esbuild`fails the build outright on an unresolvable`extends`, so it died on the FIRST module (`src/routes/+layout.ts`) with `failed to resolve "extends"`. Every game the script has ever scaffolded was unbuildable out of the box. Now emits `"extends": "config-ts/base.json"`with the`include: ["."] / exclude: [dist,build,node_modules]` shape every shipped game (`apps/lines`, Book of Borut, HotFruits) already uses — resolved by PACKAGE NAME, since `config-ts`is a`workspace:\*` dep. **(2) Dependency drift → invalid compiled JS.** Svelte declares its parser/printer as CARET ranges (`esrap: ^2.0.0`, `acorn: ^8.12.1`, `@sveltejs/acorn-typescript: ^1.0.5`) and a brand-new standalone repo has **no lockfile**, so a fresh install floats them to latest while the monorepo stays on what its committed lockfile resolved (`esrap`2.0.1 /`acorn`8.15.0 /`acorn-typescript`1.0.5). The drift is not cosmetic: **esrap 2.3.0 prints the TypeScript optional-parameter marker into the compiled JS** —`components-ui-pixi/UI.svelte`'s `{#snippet gameName(override?: HudTextOverride)}`came out as`function gameName($$payload, override?)`, which is not valid JavaScript, and rollup died with `Expected ',', got '?'`. Identical Svelte version (5.35.1) and byte-identical `svelte/compiler` in both trees — the bug was one layer below it, which is why every config-level comparison (svelte.config/vite.config/pnpm-workspace/tsconfig/plugin list/`ssr.noExternal`) came back identical. The scaffold now emits a `pnpm.overrides`block pinning those three; **re-check them when bumping svelte**. Also added`engine-flow`, `engine-flow-v2`, `engine-fx`to the scaffold's dependency list —`apps/lines`needs all three, so "copy`src/`from an existing game" (what the script's own closing message tells you to do) previously required hand-editing`package.json`. **(3) `svelte`was stale at 5.20.5** while every engine package declares 5.35.1 — and svelte + esrap + acorn are ONE set, so the pins in (2) paired with 5.20.5 blew up on SvelteKit's OWN`error.svelte`with`Not implemented: Program`(an esrap failure). Bumped to 5.35.1 to match the engine. **When bumping svelte, bump the overrides with it.** **(4) No`lingui.config.ts`.** `config-vite`unconditionally registers the`lingui()`plugin, which aborts with`No Lingui config found` when the file is absent; the scaffold never wrote one. Now emits the same one-liner (`import config from 'config-lingui'`) every shipped game has. **(5) No `src/routes/+layout.ts`.** `config-svelte`uses`adapter-static`, which needs `prerender = true`; without it the build got all the way through both vite passes and then died at the adapter with `Encountered dynamic routes`. Now emits `prerender = true`/`ssr = false`(the game is a client-side pixi canvas) /`trailingSlash = 'ignore'`, matching the standalone games. **Verified end-to-end:** scaffold → `pnpm install`→`pnpm build`exits 0 and writes`build/index.html`(59 KB) — previously it failed at the first module. Note for anyone copying`apps/lines/src`into a standalone repo: take it from the **committed`main`** the submodule pins, not the working tree — a dirty engine tree exports symbols (e.g. `resolveSymbolName`) that the pinned engine does not.
.00`, 3 points) → `animate` that line's cells → `LINE-hide`, rotating across lines; and a stop parked mid-pass with the line drawn emits exactly one `LINE-hide`. `tsc --noEmit` over `apps/lines` 0 errors; both builds green.
- 2026-07-24 — **The free-spin intro/outro VISUALS get the same WYSIWYG fix as the win overlay** (same bug class, found while fixing the win overlay below; applied pre-emptively before the owner authors them). `FreeSpinAnimation.svelte` — the shared frame block behind BOTH `FreeSpinIntroVisual` and `FreeSpinOutroVisual` — hardcoded `width={PANEL_SIZES.width}` PLUS an authoring-box offset (`x: 0.5·PANEL.width`, `y: 0.4·PANEL.height`) that applied even under `boundToInstance`, so an authored instance could never match the editor preview (which draws the rig at natural size at the node's transform). Fix mirrors `WinAnimation` exactly: new optional `width?`; `frameWidth = width ?? (boundToInstance ? undefined : PANEL_SIZES.width)` so the coded/OFF composer keeps the historical fit and the AUTHORED path renders NATURAL size; `loadScaleBase={EDITOR_SPINE_LOAD_SCALE}` only on the natural-size path (the engine-bundled `fsIntro` loads at `parser.scale` 2 vs the editor's 1, and Spine leaves `data.width/height` unscaled, so nothing else cancels it); and the authored branch drops the offset + centring pivot entirely so the rig's SKELETON ORIGIN sits on the instance node's origin. **The offset/pivot pair compensated for nothing:** offset (300, 240) vs pivot (300, 234.78) ⇒ net (0, +5.2px), and no sibling reads it — the count spine + caption sprites render INSIDE the frame's `slot_text_placeholder` and ride the RIG, not the container (their `sizes.width * 0.3/0.4` are frame-RELATIVE and were deliberately left alone). **Parity:** OFF path is byte-identical (`frameWidth` resolves to the former `PANEL_SIZES.width`, offsets unchanged, `loadScaleBase` undefined ⇒ factor 1, the `<MainContainer>` wrapper untouched); `apps/{cluster,scatter,ways,price}` have no `FreeSpinAnimation`. The two Visual components needed NO edit — `PANEL_SIZES` is `FreeSpinAnimation`'s own private constant and it already receives `boundToInstance`. Verified: `pixi-svelte` + `engine-layout` + `lines` builds green, `svelte-check apps/lines` 0 errors, Prettier clean. NOT browser-verified. **Known remaining FS divergence (NOT fixed):** the frame rig is hardcoded `key="fsIntro"` for BOTH intro and outro, while the defs' spine params (`introSpine`/`outroSpine`, default `fsIntroNumber`/`fsOutroNumber`) name the COUNT rig — and `instancePreviewSpineBundle` hands the editor the FIRST spine param, so the editor can preview the number rig where the runtime draws the frame rig. Making it unconditional needs a `frameSpine` param listed first on both defs (separate change).
- 2026-07-24 — **An authored `win` Overlay instance now renders at the size + place the Scene Editor previews (WYSIWYG).** Owner report: the `win` componentInstance was resized in the editor so the rig fit nicely; in-game the big-win rig came out WAY too small and offset. Root cause was **two different BASE sizes for the same rig**, with the node's scale multiplying whichever one it landed on. RUNTIME: `apps/lines/src/components/WinAnimation.svelte` hard-coded `<SpineProvider width={boardLayout().width}>`, so the rig was always squeezed to the board width and the instance scale shrank it further. EDITOR: `EditorSpineLayer.svelte`'s `bindSpineTarget` passes the bind node's own `width`/`height` — which the `WIN_DEF` `win-anim` node does not set — so the preview draws the rig at its NATURAL size and the instance transform sizes it. A rig authored ~2500px wide previewed at 2500 and shipped at ~600 ⇒ scaling it down to taste in the editor made the in-game copy tiny. Fix: `WinAnimation` takes an explicit `width?`; `WinVisual.svelte` passes `boardLayout().width` ONLY on the coded/OFF composer path (`bind:Win`, `boundToInstance={false}`) and passes nothing on the authored instance path, so both surfaces size from the same natural base and the node transform is the single knob. **POSITION needed no separate fix — it was the same bug.** Verified the two compose identically: the `win-visual` instance is a `componentInstance` with no `bind`, so the editor's `nodeTransform` never applies the catalog `placement:'boardCentre'` (that only fires for a `bind`/`preview.art` anchor — `resolveAnchorPreviewArt` returns undefined without `node.bind`), and its nested `win-anim` bind composes purely through `composeWorldMatrix`/`childLocalTransform`; the runtime walks the identical chain (`<MainContainer>` → instance `<Container>` → `win-root` → `win-anim` `<Container>` → `WinVisual` at LOCAL origin) and `<SpineProvider>` with no `anchor` pivots at 0, so both put the SKELETON ORIGIN at the same point. What moved was the art: `bigwin`'s skeleton canvas is `x:-200 y:-200 1600×1600` — **not** origin-centred — so scaling about the origin at two different base sizes displaces the visual centre. One base ⇒ one position. **Load-scale reconciliation (the second half, and the trap):** the Spine readers scale skeleton GEOMETRY but copy `skeleton.data.width/height` through UN-scaled (confirmed in `SkeletonJson.js`/`SkeletonBinary.js`), so a bundle's load scale is a bare multiplier that NO sizing path cancels — a width-fit renders at `parser.scale × width`, natural size at `parser.scale × data.width`. Actual numbers: the editor previews EVERY rig at `EDITOR_SPINE_LOAD_SCALE = 1`; an exported `deploy/editor-art` spine is written with `scale: EDITOR_SPINE_LOAD_SCALE` (`editorArtExport.ts`) and read back at that same 1 by `editor-scenes.ts` ⇒ **already at parity (1 ↔ 1)** for every R2 rig, which is the online case. The engine-BUNDLED `bigwin` in `apps/lines/src/game/assets.ts` is **`scale: 2`** ⇒ 2× the editor, so "natural size" would still have meant two things for any game pointing `winSpine` at a bundled rig. Compensated explicitly rather than by changing that (which would halve the OFF path): new `packages/pixi-svelte/src/lib/spineLoadScale.ts` records each bundle's `parser.scale` as `assetLoad.ts` reads it, and `<SpineProvider>` gained an opt-in `loadScaleBase` that divides the difference out (`loadScaleBase / recorded`, resolved under the same key the fallback bundle lookup lands on). `WinAnimation` passes `EDITOR_SPINE_LOAD_SCALE` on the natural-size path only. The constants moved to `packages/engine-layout/src/lib/spineLoadScale.ts` (the package BOTH surfaces already depend on) with `apps/launcher-api/src/lib/spineScale.ts` reduced to a re-export, so the editor preview and the runtime can't drift apart. **Parity:** `loadScaleBase` absent ⇒ factor 1 ⇒ every other `<SpineProvider>` in every game is byte-identical; the OFF composer keeps `boardLayout().width` and therefore its exact shipped size; `apps/{cluster,scatter,ways,price}` have no `WinVisual` at all. **Not fixed here:** `FreeSpinAnimation.svelte` has the same class of mismatch under `boundToInstance` (a hard-coded `width={PANEL_SIZES.width}` plus x/y offsets) — same fix shape, left for a separate change. **Verified:** `pnpm --filter pixi-svelte build` + `pnpm --filter engine-layout build` + `pnpm --filter lines build` + `pnpm --filter launcher-api build` green; `svelte-check apps/lines` **0 errors / 0 warnings** (pinned `typescript@5.8.3` + `svelte-check@4.3.0`); Prettier clean. NOT verified in a browser. **Not committed / not published** — reaching online games needs a runtime release (`runtime:lines`), and the launcher needs a deploy for the re-exported constants; standalone `bookofborut` must mirror the per-game `src/components/{WinAnimation,WinVisual}.svelte` (the `packages/*` half rides an engine submodule bump).
- 2026-07-24 — **The HUD no longer mounts TWICE under a v2 flow that drives the screens.** Owner report: an intermittent "glitch" on the balance text — an extra half-glyph hanging off the end of `$97.00`. Not a font bug: **every HUD readout was rendered twice**. Verified live on `test1` (`games.invisiblewall.org/test1`, shared `_runtime/lines` bundle): the stage carried TWO bands at zIndex 102, each a full copy of the 15-node `hudBar` scene, with two `$100.00` `Text` objects at the identical global x=145, both `anchor {x:0.5,y:0.5}`, w=161. The doc declares ONE `hudBar` scene and the FlowDoc ONE `hudBar` container, so this was the engine mounting it from two places. **Why it read as a glyph artifact:** each copy owns its own `svelte/motion` count-up `Tween` (one via `<ParamReadoutText>`, one via the coded label path), so mid-change the two hold different strings — `$100.00` is 161px wide, `$97.00` ~131px — and centre anchoring makes the wider one overhang the narrower by half the difference on each side. It self-heals once both tweens settle, which is why it looked transient. **Root cause:** `<UI>` renders the `hudBar`/`hudCorners` scenes ITSELF (`<UI hud={{ bar, corners }}>`), and a driven v2 flow mounts those same scenes as containers through `<FlowV2Mount>`. Neither guard on the `<UI>` block caught it — `isHudFlowManaged` reads the **v1** mounter only (`flow?.mounter.authoredScreenIds()`), and `suppressCodedHud` is `hasAuthoredHud()`, deliberately keyed on `hud_*`-prefixed scenes and never the canonical `hudBar`. Its two sibling blocks already carry the right guard (`authoredHud` and `extraScenes` are both `{#if !flowV2DrivesScreens}`, each commented "mounting them here too would double them"); the coded `<UI>` gate never got it. **Fix:** `&& !flowV2DrivesScreens` on `Game.svelte`'s `<UI>` condition, so under a driven flow `<FlowV2Mount>` is the sole scene renderer as documented. **Parity:** a game with no flow, a book-events-only flow, or a half-on flow has `flowV2DrivesScreens` false ⇒ byte-identical. **Verified live** after a `runtime:lines` release (CI published `bundle.7Ja7QjAI.js`, the exact file the game serves): the stage went from 6 `$`-readouts across two z=102 bands to **3 across one**. ⚠️ **Behaviour change under a driven flow — the coded `<UI>` chrome goes with it:** the BUY BONUS button, and the `hudCorners` game-name/logo. The `hudCorners` container still mounts (the flow shows it) but renders EMPTY, because its `bind: { component: 'HudGameName' | 'HudLogo' }` nodes are rendered only by `<UI>`'s `gameName`/`logo` snippets — `boundComponentCatalog` declares their params but no `<LayoutScene>` renderer exists. So a flow-driven game currently has NO way to render those two binds. Menu/auto/turbo/± survive on `test1` because the author already authored them as `Button_Image` instances. **Open:** give `HudGameName`/`HudLogo` (and a buy-bonus bind) a `<LayoutScene>`-side renderer so a driven `hudCorners` paints, or drop them from the reference `hudCorners` scaffold. Reaches online games via a runtime release (`runtime:lines`); standalone `bookofborut`'s `Game.svelte` is a pre-flow-v2 divergent copy with no `flowV2DrivesScreens` and cannot hit this bug.
- 2026-07-24 — **The runtime now scales the DOC's canvas box, so an authored screen can no longer render at a different size/position in the game than in the Scene Editor.** Owner report: "a big difference between the way I have things in the scene editor and the way they display in game — a size difference and a placement difference … the free spin intro / book reveal is perfect". That last clause is the tell: the free-spin intro is a `canvas`-space screen (raw window pixels), and canvas space is the ONE space that never touches the main box. Root cause: two surfaces disagreed about which box a `game`-space node is authored in. The Scene Editor previews against `data.doc.mainSizesMap` (the right panel's **Canvas Size**, editable per layoutType), but the runtime scaled a box **hard-coded** in the game's `stateLayout.ts` (`apps/lines`: desktop 1422×800) — `createLayout` captured it in a closure at module scope and NOTHING anywhere read `doc.mainSizesMap`. `<MainContainer>` still filled the window either way, so a drifted box was silent: every `game`-space node came out scaled by `min(cw,ch / codedBox)` instead of `…/docBox` **and** offset, because a node's position is measured from the box centre (`mainToWorld`) — one wrong number moves and resizes everything at once. A doc can drift trivially: `editorStorage.ts`'s `DEFAULT_MAIN_SIZES` (desktop 1920×1080, landscape 1024×576, …) is nobody's game box, and the Canvas Size field is free-form. Fix: `utils-layout/createLayout.svelte.ts` gained a module-level `$state` authored box + `setAuthoredMainSizesMap()` / `getAuthoredMainSizesMap()`, consulted **only** by `mainLayout` (the game box) and never by `mainLayoutStandard` (the fixed 1920×1080 HUD box every game shares). `apps/lines/src/editor-scenes.ts` publishes it from `loadEditorScenes()`, which is the ONE funnel all four doc sources return through (live runtime bundle / baked freeze / `/api/editor/doc` / bundled fallback), so no source can be missed. Validation is per-layoutType: a non-finite/≤0/missing entry falls back to the coded box rather than collapsing the stage. **Parity:** every reference layout (`lines`, `bookof`, `engineSkeleton`) and the seed script already declare exactly 1422×800 / 1000×1000 / 1600×900 / 800×1422 — identical to the coded map — so a game whose doc box already matches is byte-identical. **Behaviour change for a DRIFTED doc:** the game now renders the box the author previewed, which also re-scales the coded parts laid out relative to it (`boardLayout()` derives from `mainLayout().width/height`, so the reel board stays centred but resizes with the box) — that is the point: the editor becomes the truth. The editor's mismatch warning was reworded to match (it no longer claims "the game will render at a different size"; it now warns that the coded reel board follows YOUR box, not the reference's) — note it lives inside the **collapsed** "Canvas Size" panel, which is why a drifted project can run for a long time unnoticed. **Verified** in a running `apps/lines` (WebGPU, 1280×720): at boot `getAuthoredMainSizesMap()` returns the doc's map and `<MainContainer>` sits at pivot 711/400, scale 0.9 = min(1280/1422, 720/800) — unchanged; calling `setAuthoredMainSizesMap({desktop:960×540,…})` live re-derived it to pivot 480/270, scale 1.3333 = min(1280/960, 720/540), proving the box is doc-driven and reactive. `pnpm --filter lines build` green. Reaches online games via a runtime release (`runtime:lines`); standalone `bookofborut` needs the same two-line wiring in its OWN `editor-scenes.ts` (its `src/game/*` is not delivered by the submodule).
- 2026-07-24 — **Turbo is no longer overridden by the free-spin sequential-stop mode.** Owner report: turbo works in the base game ("the board is spinning faster as it should") and stops working the moment free spins start — "getting overwritten by the new playmode of the free spin". Root cause in `packages/utils-slots/src/createEnhanceBoardSpin.ts`: the global spin type is `stateBet.isTurbo ? 'fast' : 'normal'`, but `forceSequentialStop` (set for the whole feature by the Flow `enableSequentialReelStop` effect) was applied **after** it — `useSequential = forceSequentialStop && !isAnticipated` forced every non-anticipated reel to the `sequential` spinType and `noStop = true`. That discarded turbo twice over: `sequential` is not a `fast` spinType, so `reelState.spinOptions()` fell back to `SPIN_OPTIONS_DEFAULT` (the slow profile) instead of `SPIN_OPTIONS_FAST`, and `noStop: true` also vetoed `generalSpinWith`'s `(!noStop && isTurbo && isSpinning)` instant-land branch in `createReelForSpinning`. Fix: `&& !stateBet.isTurbo` on `useSequential`, so a held turbo returns the reel to the global `fast` type. Anticipation still wins over both (unchanged). No-op for a game that never authors `enableSequentialReelStop` (the mode is off ⇒ `useSequential` was already false) and no-op with turbo off ⇒ byte-parity for the dev games. ⏳ Live-verify with turbo held through a free-spin feature on a game whose FlowDoc authors the effect. Reaches online games via a runtime release (`runtime:lines`); standalone `bookofborut` picks it up on an engine submodule bump.
- 2026-07-24 — **A bitmap font that under-reports its own glyph box no longer ignores `fontSize` (and no longer overlaps its lines).** Owner report on the `/symbols` win-line text: "the font size … is not working, at least not with the default silver font. I put it at 0.25 but is still super big, and is also overlapping the 2 lines". Not a win-line bug — a FONT-DESCRIPTOR bug the call site cannot reach. pixi 8 derives everything about a static bitmap font from two descriptor numbers (verified against `pixi.js@8.8.1` source): `getBitmapTextLayout` draws at `scale = style.fontSize / font.baseMeasurementFontSize` (`<info size>`) and advances each line by `font.lineHeight` (`<common lineHeight>`); `style.lineHeight` is never consulted for bitmap text. The engine's bundled **`silver`** declares `size=97 lineHeight=97` for glyphs that are actually **199** tall — confirmed on the LIVE font object in a running `apps/lines` (`silver: lineHeight=97 base=97 glyphBox=199`, while `gold` 105/105/105, `purple` 177/177/177, `goldblur` 376/376/376 all agree). So at the authored `size: 0.25` (⇒ `fontSize = SYMBOL_SIZE * 0.25 = 30`) silver drew its glyphs **54px** tall (1.8× the requested size — "still super big") while advancing only **30px** per line ⇒ the message and the amount **overlap**. New `normalizeBitmapFontMetrics(font)` in `packages/pixi-svelte/src/lib/sanitizeBitmapText.ts` (sibling of the existing black-screen guards, exported from the package) measures the font's real glyph box (`max(yOffset + texture.frame.height)`) and, when it overflows the declared line box by >10%, rescales `lineHeight`, `baseMeasurementFontSize` and `baseLineOffset` by that factor — restoring the invariant every correct descriptor already satisfies (`lineHeight ≈ tallest glyph`), so text draws at the requested `fontSize` and one line clears the next. Called from `pixi-svelte`'s `<BitmapText>` alongside `ensureBitmapFontSpaceGlyph`/`sanitizeBitmapText` (before the PIXI object is constructed, so the FIRST frame is already correct), and from the launcher's `EditorTextLayer.svelte` bitmap path so the editor canvas and the game agree on what `fontSize` means. Idempotent (per-font `WeakSet`) and a **no-op for a correct descriptor** — gold/goldblur/purple are byte-identical — and for a DYNAMIC font (glyphs minted from a real font file; metrics correct by construction, char set still growing). ⚠️ **Behaviour change for existing silver text:** any authored size tuned against the broken metrics now renders ~1.8× smaller (it finally honours the number) — re-tune those. **Verified:** a Node fixture replicating pixi's exact `getBitmapTextLayout`/`BitmapTextPipe` math over the four real `.xml` descriptors (silver 54.1px glyph / 30px pitch / OVERLAP=true → 26.4px / 30px / OVERLAP=false; the other three unchanged, corrected=false), plus the live browser check above where calling the shipped function on the loaded `silver` font moved it 97/97 → 199/199. `pnpm --filter pixi-svelte build` + `pnpm --filter launcher-api build` green; `tsc --noEmit --strict` clean on the new module. Reaches online games via a runtime release (`runtime:lines`); standalone `bookofborut` picks it up on an engine submodule bump.
- 2026-07-24 (later still, ROOT CAUSE) — **the sequential replay showed only the last line on the SHIPPED games, because the number of `winInfo` events per spin is a property of the SOURCE BOOK.** `recordWinCycleWins` ASSIGNED `wins = bookEvent.wins`, which is right for the reference books (`apps/lines` fixtures put every win in ONE event, `wins: [w1,w2,w3]`) and wrong for the Play4Fun facade the shipped Book of Borut actually runs on: `engineFacade.adaptEventsForEngine`'s `flushWins()` pushes one `winInfo` PER win (`reveal → winInfo×N → setTotalWin`), so each event overwrote the previous and only the LAST line survived. This is why local `apps/lines` testing looked correct while the remake did not — a reminder that a fixture book and the live facade book are different shapes. Fix: `recordWinCycleWins` now ACCUMULATES within a spin (`reveal` still clears), de-duplicated by a `winKey` (symbol|kind|lineIndex|positions) so a book emitting per-line events AND a summary event can't list a line twice. Both book shapes now produce the same rotation. **Verified against LIVE data**, not just the code: eight real `POST /rgs/engine` bets against the book-of mock each returned **3 `spinWin` events** (⇒ 3 `winInfo`s), and in-browser the three-separate-events shape rotates all three groups (`0:1,1:2,2:3` → `0:3,1:2,2:3` → `0:2,1:2,2:2`) with a repeated flush adding no fourth entry. `tsc --noEmit` 0 errors; build green.
- 2026-07-24 (later still) — **the resting replay STEPS THROUGH the paying lines** (owner: "if I have multiple lines … go through all the winning lines sequentially; right now is looping only the last win"). The symbols-only cut lit the deduped UNION of every winning cell each pass, so a three-line spin read as one undifferentiated block. `winSymbolCycle` now keeps one `Position[]` group PER win (`cycleGroups()`, book order, empty groups dropped) and animates them one at a time with the authored gap between each, looping back to the first after the last — a single-win spin is the degenerate one-entry rotation. Note the wins all arrive in ONE `winInfo` event (`wins[]`, each with `meta.lineIndex`), so nothing changed about recording; only the playback shape did. `/symbols` slider relabelled "Gap between lines". **Verified in the browser** (dev + mock RGS, three overlapping lines): the cue log rotates `0:1,1:2,2:3` → `0:3,1:2,2:3` → `0:2,1:2,2:2` → back to the first, ~435ms apart, ZERO `winLineShow`/`winLineHide`, nothing after `stopWinCycle()`. `tsc --noEmit` over `apps/lines` 0 errors; both builds green.
- 2026-07-24 (later, OWNER CORRECTION) — **the resting replay is SYMBOLS ONLY.** The first cut (next entry) replayed the whole `winInfo` presentation, so the line and its stamped amount redrew on every pass — "this is wrong and was never asked, only the symbols animation should have looped". Reworked: `winLineCycle.ts` → **`winSymbolCycle.ts`**, which broadcasts NO `winLineShow`/`winLineHide` at all (whatever the round left on screen stays as it was) and awaits only `animateSymbols`. It also stopped stepping win-by-win: with no line to say WHICH win is on show, sequential groups just read as symbols blinking, so one pass lights the deduped UNION of every winning cell in the spin. The `winLineEnabledForWin` filter went with it — that gate is about the LINE (it excludes scatter wins and reads the line's on/off toggle), and a scatter win's symbols animate during the round like any other. Config moved OUT of the win-line style block to a doc-level **`winCycle: { enabled?, delay? }`** with its own `bakedWinCycleConfig()` + its own "Winning symbols after the spin" section in `/symbols`, because a replay that never draws the line must not be governed by the line's toggle (`winLine.loop`/`loopDelay` are gone; nothing had authored them yet). **Verified in the browser** (dev + mock RGS, two overlapping wins): the cue log shows only repeating `boardWithAnimateSymbols` over the deduped union `0:1,1:1,2:1,1:2,2:2` every ~450ms, ZERO `winLineShow`/`winLineHide`, and nothing after `stopWinCycle()`. `tsc --noEmit` over `apps/lines` 0 errors; both builds green.
- 2026-07-24 — **Win lines now CYCLE on the resting board until the next spin** (owner request: "they only play once and then go back to static"). `winInfo` presents each win exactly once, so the board sat static from the end of the round until the next bet. New `apps/lines/src/game/winLineCycle.ts` replays the LAST spin's wins on a loop: per pass it broadcasts `winLineShow` (points + amount/message from the shared `winLinePointsFor`/`winLineTextFor`), awaits `animateSymbols`, broadcasts `winLineHide`, waits `loopDelay` (default 0.4s, floored at 120ms so a subscriber-less cue can't busy-loop), and repeats — so a cycled pass is byte-identical to the round's own pass, including the scatter exclusion (`winLineEnabledForWin`) and the `/symbols` on/off toggle. Driven from `playBet` (`apps/lines/src/game/utils.ts`): `stopWinLineCycle()` FIRST, so a new bet never rolls the reels under a stale line, and `void startWinLineCycle()` in the `finally` after the slam token resets (so even a slammed round gets its full line back at rest). Wins are recorded in `dispatchBookEvent` — ahead of the coded/v1/v2 branch, since a flow-owned `winInfo` never reaches the handler map — with `reveal` clearing, so a free-spin feature cycles its LAST spin. A `generation` token makes start/stop idempotent and stops a stale pass wiping a live line. Skipped while `stateBetDerived.isContinuousBet()` (autoplay >1 / space hold), where the next spin is already on its way. Authored in `/symbols` → Win lines → Line as **Loop until next spin** + **Loop gap**, sparse (`winLine.loop` only persists when OFF), resolved by `bakedWinLineConfig()` (`loop` default true, `loopDelay` 0.4) and passed through the exporter verbatim. **Verified in the browser** (`apps/lines` dev + mock RGS): the cycle emitted show→animate→hide→~420ms gap repeatedly for as long as it was left running, and `stopWinLineCycle()` ended it with a single `winLineHide` and silence after. `tsc --noEmit` over `apps/lines` = 0 errors; `pnpm --filter lines build` + `pnpm --filter launcher-api build` green. Reaches online games via a runtime release (`runtime:lines`); standalone `bookofborut` needs the `src/game/{winLineCycle,utils}.ts` mirror.
- 2026-07-23 — **Free-spin OUTRO fix: a placed `freeSpinOutro` VISUAL now counts up + holds the round under a flow-driven game (the twin of the WIN-overlay fix, `5f581a0`).** Under a v2 flow that DRIVES screens (`flowV2DrivesScreens` true) the coded `<FreeSpinOutroGate>` is suppressed (`Game.svelte`, `{#if !flowV2DrivesScreens}` at `LAYER_BAND_TOP`), but it is the ONLY awaitable subscriber to the flow's `freeSpinOutroCountUp` action (`flowEffects.freeSpinOutroCountUp` → `awaitPresentation` → `broadcastAsync`): it owns the `WinCountUpProvider` count-up, writes `freeSpinOutroState` (which `FreeSpinOutroVisual` reads), and holds the round via `waitForResolve`. So the canonical DRIVEN SEED (`engine-flow-v2/reference/drivenSeed.ts`), which binds only `FreeSpinOutroVisual` in the shown `freeSpinOutro` container, got NO count-up, NO state and NO round-block — the outro drew nothing (the "unproven" leg the seed doc flagged). Book of Borut only worked because its `freeSpinOutro` scene binds the `FreeSpinOutro` COMPOSER (gate+visual) inside a shown container. **Fix (`apps/lines/src/components/Game.svelte`):** mount `FreeSpinOutroGate` ENGINE-OWNED under a driven flow — the count-up/state/round-block DRIVER — mirroring the WIN engine-mount. Placed at `freeSpinOutroZIndex` (`sceneLayerZIndex('freeSpinOutro')`, the outro container's OWN band) and BEFORE `<FlowV2Mount>`, so its dim scrim sits BEHIND the authored `FreeSpinOutroVisual` (same-z, insertion-order tiebreak) exactly as the WIN gate sits below the win visual — no dim burying the spine, no double dim. **Exactly one `freeSpinOutroCountUp` `waitForResolve` subscriber in every path** (two hang the round): a new `freeSpinOutroHasCodedGate` derived (`fsOutroScene.nodes.some(bind.component === 'FreeSpinOutro' | 'FreeSpinOutroGate')`, analog of `basegameOverlaysHasCodedWinGate`) skips the engine gate whenever the shown scene STILL binds the composer or a bare gate (Book of Borut ⇒ TRUE ⇒ engine gate stands down, live outro untouched); the driven seed binds only the visual ⇒ FALSE ⇒ engine gate is the sole driver. **Divergence from the WIN gate (deliberate, documented):** `FreeSpinOutroGate` does NOT self-suppress its press/dim under flow the way `WinGate` does — it has no `OnMount` self-resolve (its round-block releases ONLY on the tap, per `flowEffects`), and it is the SAME component Borut's composer mounts, so forcing `codedPressOwned`-style suppression would both HANG this round (nothing would release the hold) AND strip Borut's live outro of its scrim + tap. So it mounts INTACT: its `PressToContinue` is the SOLE tap surface the driven outro has (the seed authors no `tapToContinue`), matching Borut's proven tap-to-continue UX. **Parity:** the engine gate is gated on `flowV2DrivesScreens`, so a non-flow / book-events-only game NEVER mounts it — the coded gate at `LAYER_BAND_TOP` owns it there ⇒ byte-identical to today. **Verified:** `pnpm --filter engine-layout build` + `PUBLIC_RGS_TRANSPORT=play4fun pnpm --filter lines build` green; `svelte-check apps/lines` 0 errors (`typescript@5.8.3` + `svelte-check@4.3.0`). NOT verified in a browser (no baked driven-seed game to spin headlessly). **Reaches online Borut via a runtime release (`runtime:lines`).** Standalone desktop `bookofborut`'s `src/components/Game.svelte` is a PRE-flow-v2 divergent copy (no `flowV2DrivesScreens`, still carries the removed `Scene.gate` props, never got the WIN fix either) — the fix is NOT mechanically applicable there and was left untouched; that build's outro cannot hit this bug (it has no driven-screens path).
- 2026-07-23 — **Win-overlay coin fountain (`WinCoins`) is now AUTHORABLE — moved out of the coded gate into the placeable `win` VISUAL, with an on/off toggle + position/scale.** Follow-up to the `WIN_DEF` gate/visual split below. The coin fountain used to live in `WinGate.svelte` (the engine-owned DRIVER), so it hard-centred on the board via its own `MainContainer` + `boardLayout` and was neither toggleable nor movable. Now it renders as part of `WinVisual.svelte` (the positionable componentInstance VISUAL): it sits inside the SAME positioned container as the tier spine / count number, so dragging the `win` component moves the coins and its instance scale scales them. Three parts: (1) **emit bridge** — `WinCoins`'s old `emit={!countUpCompleted}` signal is now published to `winState` (`apps/lines/src/game/winState.svelte.ts`, new `coinsEmit: boolean`) by the gate via `WinStatePublisher` (extended: `<WinStatePublisher {countUpAmount} coinsEmit={!countUpCompleted} />`, a second `$effect` writes `winState.coinsEmit`), and `WinVisual` drives `<WinCoins emit={winState.coinsEmit} levelAlias={winLevelData?.alias} />` off it — coins start/stop at the exact same moments (while the count-up runs) as before, just via the reactive `winState` indirection instead of a snippet-scoped prop. (2) **`showCoins` param** — new `{ key: 'showCoins', kind: 'boolean', default: true }` on `WIN_DEF` (`packages/engine-layout/src/lib/builtinComponents.ts`), read by `WinVisual` via `getComponentParams()['showCoins']` (new `booleanParam` helper) with a `showCoinsProp = true` prop fallback (same pattern as `winSpine`/`slotName`); `false` ⇒ no fountain. Default true ⇒ parity, and an old baked def lacking the key falls back to the prop default. (3) **`WinCoins` de-centred** — `WinCoins.svelte` dropped its own `MainContainer` + `boardLayout` wrapper (and the now-unused `getContext`); it renders just the `<ParticleEmitter>` from its LOCAL origin, so it TRULY honours the enclosing transform (no hard-centring left). Rendered FIRST in `WinVisual`'s `content` snippet so it stays BEHIND the spine/number, matching the pre-move gate-then-visual z-order. **Parity:** on the OFF composer path (`Win.svelte` → `WinVisual boundToInstance={false}`) `WinVisual` wraps `content` in `MainContainer` + `Container(boardLayout)` — byte-identical to `WinCoins`'s old wrapper — so the default (showCoins true, non-flow coded path) renders the same coins, same timing, same board-centre. A game that doesn't place the `win` component is unaffected (its `WinVisual` never mounts). Exactly one `WinCoins` in every path (moved, not duplicated). **Verified:** `pnpm --filter engine-layout build` + `pnpm --filter lines build` green; `svelte-check apps/lines` **0 errors / 0 warnings** (pinned `typescript@5.8.3` + `svelte-check@4.3.0`). NOT verified in a browser. **Not committed / not published** — awaiting owner review; reaching online Borut needs a runtime release (`runtime:lines`), and because `WIN_DEF` gained a param the launcher must redeploy for `showCoins` to appear in the editor. Standalone `bookofborut` must MIRROR the per-game `src/components/{WinCoins,WinGate,WinVisual,WinStatePublisher}.svelte` + `src/game/winState.svelte.ts` edits (the `WIN_DEF` half rides an engine submodule bump).
- 2026-07-23 — **Win overlay (big-win presentation) fix: a placed `win` VISUAL now shows the big win under a flow-driven game.** Follow-up to the `WIN_DEF` gate/visual split below. On Book of Borut (a v2 flow that DRIVES screens, `flowV2DrivesScreens` true) the owner placed just the `win` componentInstance (`WinVisual`) and authored the `setWin` choreography in Invisible Flow (`FireCue winShow` → `winLevelSoundsPlay` → `winUpdate` → `winLevelSoundsStop` → `FireCue winHide`), removing the coded `bind:Win`/`bind:WinGate`. **The big win never showed:** `WinVisual` renders ONLY from `winState` (`winLevelData`/`amount`/`countUpAmount`), which is written EXCLUSIVELY by `WinGate` — and `WinGate` was mounted only via the doc's `basegameOverlays` `bind:Win` composer / `bind:WinGate` node, NOT engine-owned. With the coded bind removed, no `WinGate` existed, so the flow's `winUpdate` action (`flowEffects.winUpdate` → `awaitPresentation` → `broadcastAsync{winUpdate}`) had no awaitable subscriber: `winState` was never written, the `WinCountUpProvider` count-up never ran, `awaitPresentation` resolved instantly, and the visual drew nothing. Fix (`apps/lines/src/components/Game.svelte`): mount `WinGate` ENGINE-OWNED under a v2 driven flow — it is the count-up + `winState` + round-block DRIVER (distinct from a dim/press screen-gate), so unlike the free-spin gates it must RUN under flow, not be suppressed. Mounted at `basegameOverlaysZIndex` (the coded composer's own band) and BEFORE the `basegameOverlays` LayoutScene, so the big-win dim scrim sits BEHIND the placed `win` visual exactly as the coded composer stacks gate-then-visual (no double dim, right band). Its `PressToContinue` already self-suppresses under flow (`codedPressOwned = !flowV2DrivesScreens()`), so it adds no second tap surface; the count-up self-resolves via `OnMount startCountUp` (no tap needed). **Exactly one `winUpdate` `waitForResolve` subscriber in every path** (two would hang the round): a new `basegameOverlaysHasCodedWinGate` derived (`.nodes.some(bind.component === 'Win' | 'WinGate')`) skips the engine gate whenever the doc STILL carries a coded gate bind (the reference `WIN_INSTANCE` OFF/ON shapes, which mount their own gate), so the engine gate is the SOLE gate only when the doc has none (Borut). The `bigWin` component-signal handler (`Game.svelte:1046`) is a synchronous fire-and-forget `subscribe`, not an awaitable subscriber, so it never counts toward the round-block. **Parity:** the engine gate is gated on `flowV2DrivesScreens`, so a non-flow / book-events-only game NEVER mounts it — `WIN_INSTANCE` OFF (`bind:Win`) and ON (`bind:WinGate` + `winVisual` scene) render byte-identically to today. **FS-outro shares the SAME latent bug (NOT fixed here — WIN is the priority):** `freeSpinEnd` fires `freeSpinOutroCountUp`, whose only awaitable subscriber is `FreeSpinOutroGate`, which `Game.svelte:1785` suppresses under `flowV2DrivesScreens`. Borut's live FS outro counts up only because its `freeSpinOutro` scene binds the `FreeSpinOutro` COMPOSER (gate+visual) inside a SHOWN flow container; the CANONICAL driven seed binds only `FreeSpinOutroVisual`, so a fresh driven project's FS outro would NOT count up (already flagged unproven in `engine-flow-v2/reference/drivenSeed.ts`). The clean fix would mirror this change (engine-own `FreeSpinOutroGate` under flow, guarded against the composer double) — deferred to owner decision. **Verified:** `pnpm --filter engine-layout build` + `pnpm --filter lines build` green; `svelte-check apps/lines` **0 errors / 0 warnings** (run via `pnpm dlx --package=typescript@5.8.3 --package=svelte-check@4.3.0`); Prettier clean. NOT verified in a browser. **Not committed / not published** — awaiting owner review; reaching online Borut needs a runtime release (`runtime:lines`), and standalone `bookofborut` must MIRROR the `Game.svelte` edit (per-game file, not `packages/*`).
- 2026-07-23 — **Win overlay (big-win presentation) migrated to a shared, cross-project `WIN_DEF` built-in — gate/visual split mirroring the free-spin outro.** `apps/lines/src/components/Win.svelte` is split into (a) `winState.svelte.ts` — the gate→visual reactive bridge (`winLevelData`/`amount`/`countUpAmount`, the Win analog of `freeSpinOutroState`); (b) `WinGate.svelte` — the full-screen coded GATE (the `winShow/winHide/winUpdate` subscription, `WinCountUpProvider` + `OnMount startCountUp` count-up driver with the original auto-resolve `roundSkip.wait(300)` behaviour, the big-win-only `CanvasSizeRectangle` dim, `<WinCoins>`, the `codedPressOwned = !flowV2DrivesScreens()`-gated `<PressToContinue>`, and the round-blocking `waitForResolve`) publishing to `winState` via a new `WinStatePublisher.svelte`; (c) `WinVisual.svelte` — the positionable board-relative VISUAL (the tier spine via `WinAnimation` + count number in its slot, the small/medium plain-number `{:else}` path, and the `levelCaption` Invisible Win Text) reading `winState`, reading `winSpine`/`slotName` via `getComponentParams()` with prop fallbacks, and honouring `boundToInstance` (LOCAL origin when true, self-centre via `MainContainer` + `boardLayout()` when false). `Win.svelte` is now the OFF-path COMPOSER (`<WinGate/> <WinVisual boundToInstance={false} {...props}/>`), still registered as `Win` (the `bind:Win` anchor is byte-identical) and still exporting `EmitterEventWin`. `WinAnimation.svelte` gains configurable `key` (default `'bigwin'`) / `slotName` (default `'slot_win_count'`) props (state machine + tier `animationMap` unchanged). New shared `WIN_DEF` (`packages/engine-layout/src/lib/builtinComponents.ts`, id `'win'`, scope `'shared'`, category `'overlay'`, in `BUILTIN_COMPONENTS`): root binds `WinVisual` with `boundToInstance:true`; TWO convention-based params only — `winSpine` (kind `spine`, default `bigwin`) + `slotName` (kind `spineSlot`, `spineParam:'winSpine'`, default `slot_win_count`) — the 5 tiers stay convention-driven from `winLevelMap` (no 15 per-tier fields). Editor preview default `WinVisual` added to `boundComponentCatalog.ts` (`space:'game'`, `placement:'boardCentre'`, `bigwin` spine). New parity flag `WIN_INSTANCE` (default OFF) in `apps/lines/src/game/editorFlags.ts`, threaded via `editor-scenes.ts` → `defaultLayout('lines', { winInstance })` (`referenceLayouts/lines.ts`, mirroring the `transitionNode`/`freeSpinOverlays` split): OFF ⇒ the single `bind:Win` composer in `basegameOverlays` (unchanged); ON ⇒ a `canvas` `bind:WinGate` in `basegameOverlays` + a separate `game`-space `winVisual` scene holding `componentInstance(win)` at board centre. `Game.svelte` registers `WinGate`/`WinVisual` (bound) + `WIN_DEF` (`registerComponents`), reserves the `winVisual` scene id, and mounts the `winVisual` scene at `LAYER_BAND_WIN_PRESENTATION`. `engineSkeleton.ts` is left on the composer/OFF `bind:Win` shape (it has no flag mechanism and no runtime consumer — same as how the FS split left it). Book of Borut opts in purely by placing the `win` component in the editor (the shared def carries `boundToInstance:true`) — no game flag. **Verified:** `pnpm --filter engine-layout build` + `pnpm --filter lines build` green; `svelte-check apps/lines` 0 errors / 2 warnings (the two pre-existing `tsconfig.json` notices); an esbuild-bundled probe of `defaultLayout('lines')` confirms OFF `basegameOverlays` = `[bound-win→Win, bound-transition→Transition]` with NO `winVisual` scene (byte-identical parity) and ON = `[bound-win-gate→WinGate, …]` + a `game`-space `winVisual` scene with `componentInstance(win)` (`slotId:Win`, params `{winSpine:bigwin, slotName:slot_win_count}`); Prettier clean. NOT verified in a browser. **Shipped** — committed (`b80e4d6`) + published to `_runtime/lines` (runtime release run 29994685495, test server refreshed). **Follow-up same day — per-tier animation cues on `WIN_DEF`** (owner asked how to map a custom spine's animations, e.g. `R_WinScreen`): the def now also exposes a SHARED intro/idle/exit set (`introAnimation`/`idleAnimation`/`exitAnimation`, applies to every tier) PLUS optional per-tier override groups (big/super/mega/epic/max → `<prefix>Intro`/`Idle`/`Exit`), all `spineAnimation` dropdowns of `winSpine`'s real animations. `WinVisual` resolves per active tier as per-tier override ?? shared set ?? the `winLevelMap` convention default (`TIER_PREFIX` maps `superwin→super` etc.); every field empty ⇒ pure convention ⇒ parity. `WinAnimation`'s `animationMap` type relaxed from the fixed convention literals to plain strings. A missing/mis-named animation is NON-fatal — `SpineTrack` try/catches `setAnimation` and logs the spine's available names (`SpineTrack.svelte`), and the round still completes because the coded `WinGate` owns the count-up/round-await independent of the spine. `mergeBuiltinCodedParams` unions the new params onto the already-placed pinned-v1 instance at resolve time ⇒ no re-place, no version bump. Verified: `pnpm --filter engine-layout build` + `svelte-check apps/lines` 0 errors. Standalone `bookofborut` must mirror the per-game `src/components`/`src/game` files (the `packages/engine-layout` half rides an engine submodule bump).
- 2026-07-22 — **Boot loading screen (kills the online "long black screen").** The `?runtime=1` online build showed a bare black screen for the whole pre-mount window — `+layout.ts`'s blocking `load()` awaits the cross-origin `/api/editor/runtime` fetch (Railway, cold-startable) BEFORE the page component + Pixi canvas + the in-canvas `LoadingBar` mount, so nothing could paint, then the ~2.5s R2 editor-art/symbol/font asset wave followed. Fix is an HTML-shell splash in `apps/lines/src/app.html` (inline HTML/CSS + the inlined IW emblem, no JS deps) that paints on the first frame and is driven by a tiny global `window.__ieBoot` (`title`/`phase`/`progress`/`done`): animated IW emblem + game-name + uppercase phase text (`Fetching from R2…` → `Loading assets…`) + progress bar + %. `editor-scenes.ts`'s `prepareRuntimeBundle` calls `__ieBoot.phase('Fetching from R2…')` around the fetch and sets the title from a new `name` field the runtime endpoint now returns (`projectName`); `Game.svelte` feeds `stateApp.loadingProgress` → `__ieBoot.progress` and calls `done()` (fade + remove) on `stateApp.loaded`. Additive + optional-chained ⇒ inert offline/dev/Storybook (splash flashes + dismisses instantly; no `?runtime=1` ⇒ no R2 phase). A 30s safety timeout guarantees the splash can never pin over a live game. Verified live against the prod launcher: `Fetching from R2…` + pulsing emblem + indeterminate bar show during the exact fetch window, runtime fetch succeeded (no stale fallback), overlay auto-removed on load. **Needs a runtime publish** (`runtime:lines`) to reach online games; the launcher half (`/api/editor/runtime` `name`) ships via `main` auto-deploy. Standalone `bookofborut` must mirror `app.html` + the `Game.svelte`/`editor-scenes.ts` wiring (per-game files, not `packages/*`).
- 2026-07-21 — **Reveal-symbol rider ON a placed spine node** (branch `feature/spine-node-reveal-symbol-rider`, RUNTIME + game side only; /editor authoring UI is a follow-up). A placed spine (rig) node can now ride the chosen reveal symbol (`stateGame.specialSymbol`) on one of its OWN bones — WITHOUT spawning a second rig or a bound-component instance (the existing `freeSpinIntroSymbolReveal` component spawns its own `introSpine`; the owner wants the symbol on a bone of the rig they already placed, e.g. `R_Cage_Freespin`'s `Socket`). Reuses the same bone-hosting mechanism `EffectNode.hostSpineId` uses for effects. Six additive fields on `SpineNode` (`packages/engine-layout/src/lib/types.ts`): `revealSymbolBone` (unset ⇒ nothing mounts, byte-parity), `revealSymbolState` (default `'bookIdle'`), `revealSymbolScale` (1), `revealSymbolOffsetX/Y` (0), `revealSymbolFollowRotation`/`revealSymbolFollowScale` (true). `LayoutNodeView` mounts the game's registered `revealSymbolRider` bound component inside the rig's `<SpineProvider>` via `<SpineBoneAttach>` when `revealSymbolBone` is set (`packages/engine-layout/src/lib/LayoutNodeView.svelte`, beside the `attachedEffects` bone-hosting block). New tiny game component `apps/lines/src/components/RevealSymbolRider.svelte` (modeled on `ExpandingSymbol` — reads `stateGame.specialSymbol`, renders it via the shared `<Symbol>` state machine; position/scale/rotation come from the host bone), registered under key `revealSymbolRider` in `apps/lines/src/components/Game.svelte`. **No normalize/whitelist change needed:** the launcher's `normalizeNode` (`editorStorage.ts`) trusts kind+id and passes ALL node fields through untouched — only Scene-level fields are whitelisted (the `gotcha_scene_field_whitelist_normalizescene` gotcha is Scene-scoped), so the six new node fields survive save/load with no edit. Verified: `pnpm --filter engine-layout build` green (svelte-package type-checks), `svelte-check apps/lines` 0 errors / 2 warnings (the two pre-existing `tsconfig.json` option warnings). NOT verified in a browser (no live rig placed with `revealSymbolBone` yet). **Needs a runtime publish** to reach online games; the engine half is in `packages/*` (submodule bump), but the `RevealSymbolRider` component + its registration are per-game `src/components`/`src/game` wiring, so standalone `bookofborut` must MIRROR them. The /editor authoring UI to set the six fields is a separate task (owner-owned).
- 2026-07-20 — **The book-reveal rig (`FreeSpinIntroSymbolReveal.svelte`) played the first free-spin feature and froze on the second — a stray-`complete` RACE, distinct from the a3d16cb signal-cue bug.** Owner report: "the symbol Book reveal animation is not playing when I get a second free spin! the original rig is not resetting, I can see the delay I placed in that flow is working, but not the animation." This component drives `<SpineTrack animationName>` DIRECTLY (no replay token — `apps/lines/src/components/FreeSpinIntroSymbolReveal.svelte:134`), so a3d16cb doesn't cover it. Root cause: the reveal plays a one-shot `intro` then swaps `animationName` to a LOOPING `idle`, and Spine fires `complete` at the end of EVERY idle loop. The `complete` listener ran the intro→idle hand-off on any completion while `phase === 'intro'` (old `FreeSpinIntroSymbolReveal.svelte:139`). On a retrigger `play()` flips `phase` to 'intro' synchronously, but the `<SpineTrack>` `$effect` applies the intro a tick later; a stray idle-loop completion landing in that window ran the hand-off against an intro that never played — settling the reveal EARLY (round still unblocks — matches "the delay works") and pinning `animationName` back to `idle`, after which the value guard (`shouldApplySpineAnimation`) sees `idle === idle` and never re-applies. The rig sits frozen on idle. This is the PERSIST scenario: the live flow-v2 doc doesn't deliver `specialBookHide` to the mounted component (the coded `freeSpinEnd` that broadcasts it, `bookEventHandlerMap.ts:255`, is suppressed under flow ownership; the reference choreo authors it at `bookOfChoreo.ts:319` but the owner's doc need not), so `phase` never returns to 'hidden', `show` stays true, and **FadeContainer never fades the SpineProvider out** — `FadeContainer.svelte:30` unmounts children only at `alpha.current === 0`, and no `persistent` prop is passed, but the fade is never triggered. Failure mode (b): the animation never restarts on a rig that stays mounted; not (c). **Fix (minimal, 1 line of logic):** gate the `complete` handler on the completing animation's name — `phase === 'intro' && entry?.animation?.name === introAnimation` — so a stray idle-loop completion can't abort a fresh intro; each reveal is self-contained per `play()`. Deliberately did NOT also reset `animationName` in `hide()` (the coordinator's alternative): with the rig still mounted during a dismiss fade it would re-apply and REPLAY the intro while fading out — a visible regression — and it's unnecessary since `play()` always sets `animationName = introAnimation` and the unmount scenario resets on remount anyway. **Fixture-proven:** `packages/pixi-svelte/fixtures/introRevealRetrigger.fixture.ts` (`node --experimental-strip-types …`) models the real apply/complete lifecycle across two features, reusing the shipping `shouldApplySpineAnimation` value guard verbatim, and injects the stray idle complete in the retrigger race window. Pre-fix: `setAnimation` calls `['intro','idle']` (intro played once, ever) and feature 2 settled EARLY (`introAppliesAtSettle [true,false]`); post-fix: `['intro','idle','intro','idle']` (intro once per feature), rig ends on idle, one settle per feature, none early. Verified: `apps/lines` build green, `svelte-check` 0 errors / 2 warnings (the two pre-existing `tsconfig.json` option warnings), the a3d16cb fixture still green (no regression), and the guard confirmed REACHABLE in the built bundle (`m(L)==="intro"&&((we=me?.animation)?.name)===m(_)`). NOT verified in a browser. **Needs a runtime publish** to reach online games. **Book of Borut mirror required:** this is in a per-game component copy, so standalone `bookofborut`'s forked `src/components/FreeSpinIntroSymbolReveal.svelte` must get the same `complete`-guard edit (an engine submodule bump alone will NOT deliver it).
- 2026-07-20 — **An authored spine cue ("Plays on signal") played once per SESSION, not once per fire.** Owner report: "when I play the free spin for a second time in a row, my spine animation is not playing anymore! it works fine the first time". Root cause is engine-generic, not Book-of specific: a cue is an EVENT but is delivered to the spine as STATE. `<ComponentInstance>` wrote `signalAnims[nodeId] = { animation, loop }` on each fire (`packages/engine-layout/src/lib/ComponentInstance.svelte:398`), `<LayoutNodeView>` forwarded the name to `<SpineTrack>`, and `<SpineTrack>`'s apply guard is a VALUE comparison — `resolvedAnimationName !== track?.animation?.name` (`packages/pixi-svelte/src/lib/components/SpineTrack.svelte:46`). A second fire of the same cue writes the same animation name, so the guard read "already playing" and skipped the re-apply; the finished non-looping track stayed parked on its last frame for the rest of the session. Failure mode (b): the animation never restarts, not (a) or (c) — the component stays mounted and `signalAnims` is never cleared. Fixed by adding a monotonic per-instance FIRE token: `ComponentSignalAnim.fire` (`componentSignalContext.ts`), bumped by a shared `fireCue` helper on both the game-signal subscription and the self-fired `enter` cue, forwarded by `LayoutNodeView` as `<SpineTrack replay>` only while the cue is the ACTIVE override (a button `stateAnimations` spine is declarative and keeps the plain comparison), and consumed by an extracted pure decision `shouldApplySpineAnimation` (`packages/pixi-svelte/src/lib/spineTrackReplay.ts`). `replay: undefined` short-circuits to the original comparison ⇒ every uncued spine, and both coded book components (`SpecialBook`, `FreeSpinIntroSymbolReveal`, which already re-arm via their own phase machines), are byte-identical. **A fixture corrected the diagnosis mid-flight**: `packages/pixi-svelte/fixtures/spineTrackReplay.fixture.ts` (`node --experimental-strip-types …`) drives two free-spin features through the real decision function and showed a SECOND, worse pre-existing bug I had assumed away — when the cued node ALSO has a resting `defaultAnimation`, the one-shot→idle hand-off makes `props.then === track.animationName`, collapsing the guard so the cue never played AT ALL, not even the first time. The same token fixes it. Pre-fix: `['intro']` / `['idle']`; post-fix: `['intro','intro']` / `['idle','reveal','reveal']`; an uncued spine stays `['idle']`. This also pins the owner's node shape: it works once, so it is a **cue-only spine with no resting `defaultAnimation`** — most likely cued on `freeSpinStart` or `specialBookReveal`. Verified: `pixi-svelte` + `engine-layout` + `apps/lines` builds green, `svelte-check apps/lines` 0 errors / 2 warnings (both pre-existing `tsconfig.json` option warnings), and the guard confirmed REACHABLE in the built bundle (`apps/lines/build/_app/immutable/bundle.*.js` contains the minified `appliedReplay` comparison). NOT verified in a browser. **Needs a runtime publish** to reach online games; the change is entirely in `packages/*`, so standalone `bookofborut` needs only an engine submodule bump (no `src/game/*` or `src/components/*` mirror).
- 2026-07-20 — **The per-spin slam re-arm moved from `reveal` to `updateFreeSpin` (free-spin counter fix).** Owner report: "it seems like the free spin counter is not working anymore if I slam stop the reel during freespins". An ORDERING bug in the per-spin re-arm (`407e0c2`), not a render bug. A free spin's book events are `updateFreeSpin` → `reveal` → …, so the counter update LEADS the spin it labels, but `rearmSlamForSpin()` was called at the top of the `reveal` leaf. After a slam on spin N the token therefore stayed tripped across spin N+1's `updateFreeSpin`: its state write still landed (synchronous — which is why a state-level test passes and the free-spin COUNT was never wrong), but its PRESENTATION was processed under a tripped token — the counter's cues collapsed to 0ms and, under a screen-driving flow, a `showContainer{awaitComplete}` on that chain was auto-completed by the slam-aware mount (350b473) so the following `hideContainer` took the counter down. A player who slams every spin — the normal way slam is used — loses the counter presentation for the whole remainder of the feature. Fixed by keying the re-arm to the book event that STARTS a free spin: new `SPIN_REARM_BOOK_EVENTS` (`updateFreeSpin`) applied by `runBookEventPresentation` BEFORE the dispatch (`apps/lines/src/game/unskippablePresentation.ts`), so it covers the coded, v1-flow and v2-flow paths at one seam and is fresh ahead of the spin's first cue; the calls in the coded `reveal` handler and the `revealBoard` flow effect are removed (their `isBonusGame` guard STAYS — it still governs `stopButtonEnable` and `recordBookEvent`, which genuinely belong to the reveal). **Scoping verified against the data, not assumed**: across all 10,100 reference books `updateFreeSpin` never appears without a `freeSpinTrigger`, never before one, and exactly once per free spin (13 reveals = 1 base + 12 free spins, 12 updates) — so the base game never re-arms mid-round and keeps its round-scoped slam, and a RETRIGGER needs no special case because extra spins arrive as more `updateFreeSpin` events. The resume replay (`createBonusSnapshot`) re-arms once on an already-reset token ⇒ no-op. **No hang**: the change only ever un-trips the token, which can restore a wait but never strand one. Verified: `apps/lines` build green, `svelte-check` 0 errors / 2 warnings (identical to the stashed-main baseline), and a new PRESENTATION-level harness `tools/flow-spike/slamCounter.ts` (`pnpm --filter flow-spike run slamcounter`) driving `BOOK_OF_CHOREO` through `runFlowEvent` against the real `roundSkip`/`awaitCue`/`runBookEventPresentation` plus a copy of the slam-aware mount — 8/8 assertions, measuring token state at dispatch and observed cue DURATION rather than state. It reproduces the bug against pre-fix `main` (spin #3 collapsed at 0ms and its hold auto-completed; with `SLAM_EVERY=1`, spins #3–#11) and passes with the fix at every slam timing. NOT verified in a browser. **Needs a runtime publish** to reach online games + a mirror into standalone `bookofborut`.
  - Shipped alongside it: **`showWinInfoMessage` is now fault-isolated** (`apps/lines/src/game/flowEffects.ts`). This is a SEPARATE defect on its own merit, NOT the counter root cause. It runs inside the awaited book-event chain and touches data the game doesn't control end-to-end (`Intl` via the currency formatter, the baked win-text doc, an authored template); a throw propagated out of `sequence` and aborted `playBookEvents`, silently dropping every remaining book event — the counter frozen on the spin the round died on, `freeSpinEnd` never run, the game left in `freegame` with no outro. A presentation-only toast must never cost the player the rest of the round, so it now degrades to "showed nothing" and logs. Found while bisecting the counter report; no production trigger identified (a bad currency would break the unslammed `winLineTextFor` path too).
- 2026-07-20 — **A slam now shows a MINIMUM WIN DISPLAY instead of a blank fast-forward.** Owner report: "the slam stop seems to work correctly for the free spin, but I still would like the game to show the win symbols after the stop (if there is a win) … but also I would like to have the win info message to appear for each win". Collapsing every wait to zero ran the whole `winInfo` sequence inside a couple of frames: the symbols lit and the toast fired, but were overwritten before a frame painted, so a winning slammed spin read as "balance moved, nothing happened". Three edits, all in `apps/lines/src/game`: (a) **`unskippablePresentation.ts`** gains `SLAM_MINIMUM_DISPLAY_CUES` (`boardWithAnimateSymbols`), `SLAM_SYMBOL_HOLD_MS` = 200, `SLAM_MESSAGE_HOLD_MS` = 400 and `slamHold()`; `awaitCue` now holds for the symbol beat when the token is ALREADY tripped and the cue owns a minimum display, instead of collapsing. That cue is the single seam all three dispatch paths funnel their win-symbol animation through (coded `animateSymbols`, v1 flow `broadcastAsync`, v2 flow cue `broadcast`), so the lit symbols come back everywhere WITHOUT touching `Board.svelte` — its subscriber still runs detached and still lands on `postWinStatic` by itself, so nothing can stay lit forever. (b) **`flowEffects.ts`** — `winLineEnabledForWin` is false while slammed (the line and its stamped amount ARE the beat the press asked to skip), `hideWinLine` made unconditional so a press landing between a win's show and its hide can't strand a drawn line, and the `showMessage` text resolution lifted into a new exported `showWinInfoMessage()` shared with the coded handler; the effect holds `SLAM_MESSAGE_HOLD_MS` after a message when slammed, so a multi-win spin's messages read one after another rather than overwriting each other in one frame. (c) **`bookEventHandlerMap.ts`** — the coded `winInfo` handler shows that same per-win message on a slammed spin only (the coded path never had one; both reference choreographies author it as a `showMessage` node, and a flow that owns `winInfo` still owns it). **Pacing: 600ms per win** (200 symbols + 400 message) — a 3-win spin is ~1.8s against ~4.8s unslammed, and the message is readable. **Unslammed is byte-unchanged**: every new branch is behind `roundSkip.isSkipped()`, and the one unconditional change (`hideWinLine`) replaces a no-op with a no-op. **No hang**: both holds are bare `waitForTimeout` timers (deliberately NOT `roundSkip.wait`, which would collapse), so they settle on their own schedule with no dependency on a subscriber, a spine `complete` or a player press — i.e. on nothing the slam suppressed. **Money is untouched**: `setTotalWin`'s value write and `WinCountUpProvider`'s `onSkip` jump-to-final are unchanged. Verified: six app builds green, `svelte-check` 0 errors / 2 warnings (identical to the pre-change baseline), and the Node fixture — bundling the REAL `skipToken.ts`, `unskippablePresentation.ts` and `engine-layout/winText.ts` — extended to **23/23**, covering unslammed parity, slammed symbols marked + one message per win + staggered/readable + no count-up wait, a never-settling subscriber proving no hang, exact final totals, the per-spin re-arm and the unskippable carve-out. NOT verified in a browser. **Needs a runtime publish** to reach online games + a mirror into standalone `bookofborut` (it forks `src/game/*`).
- 2026-07-20 — **The slam press has its own sound (SOUND ONLY — no behaviour touched).** `ButtonBetProvider` broadcast `soundPressBet` BEFORE `runSpinOrSlamStop` decided bet-vs-slam, so a slam played the identical `sfx_btn_spin` whoosh and the player got no audible confirmation the press landed. New shared `getSpinPressSound({isIdle})` in `packages/utils-shared/spinStop.ts` — deliberately kept OUT of `runSpinOrSlamStop`, which stays sound-free per its existing contract (so an intent-invoked action can't double the sound the button already made) — drives all FOUR press sites off the SAME `isIdle` read the press body decides on: the coded provider, `Game.svelte`'s registered `spin` action, its replacement Space hotkey, and the flow-press sound replay (that last one hardcoded `soundPressBet`, so without it every flow-owned button kept the bug). New `soundPressStop` emitter event (`components-ui-pixi/src/types.ts`) + `sfx_btn_stop` in the `SoundEffectName` union, the audiosprite manifest and the `Sound.svelte` handler of all five audio apps (`number-picker` has no audio); `soundEnums.generated.ts` regenerated so the key appears in the flow inspector's sound dropdown.
  - ⚠️ **`sfx_btn_stop` is a STAND-IN region, not final audio** — see open item 5. No sprite offsets were invented: the region is byte-identical to `sfx_btn_general`'s, so it cannot land on silence or straddle a region boundary. New `sound.hasSound()` probe in `packages/utils-sound/src/createSound.svelte.ts` falls back to the spin cue where the region is absent (howler silently declines an unknown sprite key, so a missing region would otherwise be an inaudible non-failure).
  - **This is the sound half of the reverted `3a12e00` ONLY.** That commit also re-routed the press through a flow `stop` intent and broke the slam (reverted in `92253be`). Nothing routing-related was reused: the flow `stop` intent, `INTENT_COMMANDS`, `buttonStateImage.ts`, `skipToken.ts` and the five `Board.svelte` copies are untouched, and every press site still calls the same body with the same arguments. The only semantic delta is that `ButtonBetProvider` now reads `isIdle` once BEFORE the broadcast instead of after it — provably equivalent, since `broadcast` is synchronous and the only subscribers keyed on these event types are the apps' `Sound.svelte` handlers, which just play a howl sprite.
  - **Verified:** all six app builds green (`8 successful, 8 total`); `svelte-check` on `apps/lines` **0 errors, 2 warnings** — identical to the `main` baseline measured by stashing (both warnings are the pre-existing `tsconfig.json` `allowImportingTsExtensions`/`baseUrl` notices). `svelte-check` on the other five apps is byte-identical to baseline too (cluster 140 / price 153 / scatter 138 / ways 121 / number-picker 26 errors, all pre-existing and unchanged) — worth measuring because `vite build` does NOT type-check Svelte, so the five `Sound.svelte` edits would otherwise be unverified. Prettier: no new violations (the 21 files it flags across these paths are flagged identically on clean `main` — pre-existing CRLF/`endOfLine`). `pnpm lint` cannot run repo-wide (pre-existing: no `eslint.config.*`), and the launcher build fails on an unrelated in-progress owner change (a missing `hasBuiltinSpine` export in `editorSpine.client.ts`) — neither touched. **NOT verified in a browser** — the cue needs a live press.
  - **Not committed, not published** to `_runtime/lines`, and Book of Borut's engine submodule not bumped.
- 2026-07-20 — **Slam stop is now PER FREE SPIN, not per round (reverses the original owner choice).** Owner report: "during the free spin, if I click once more, the freespin sequence, play just the result one after another, instead I would like to only stop the one spin that is playing. and then the game should continue regular until I hit the spin again (to skip the anticipation)". `roundSkip` is sticky and was re-armed only in each app's `playBet`, so one press fast-forwarded the whole feature. New `rearmSlamForSpin()` (`apps/lines/src/game/unskippablePresentation.ts`) is called at the top of the `reveal` leaf — the coded handler AND the `revealBoard` flow effect, inside the existing `isBonusGame` branch — so each free spin is its own skippable unit. Chosen as a DELIBERATE call at the site that owns the spin lifecycle rather than restoring the old `stopButtonEnable` subscription, because that made the re-arm an invisible side effect of a UI-enable event which also fires from `playBet`'s `finally` (round end), where re-arming means nothing. A single-reveal base-game book never calls it ⇒ **base game unchanged**. Every consumer already reads the token at start-of-work (`createReelForSpinning`'s roll, `createEnhanceBoardSpin`'s anticipation arm, `WinCountUpProvider`'s duration), so a re-arm restores full pacing for free. **`spinStop.ts` needed no logic change**: `isSkipped()` now means "this spin is already slammed", so two presses within one spin still cancel autoplay — a cancel is always one extra press away, in whichever spin is playing (comment updated to say so). The unskippable carve-out is unaffected: it drops the race outright rather than reading the token, and the scope opens/closes inside a single serial book-event dispatch, so a `reveal` re-arm can never land inside the `setExpandingSymbol`/`freeSpinTrigger` scope. One deliberate consequence: a slam on spin 3 no longer pre-releases the free-spin OUTRO gate — that gate now needs a tap, or a press (which re-trips the token and releases it), and since the button is live again after a re-arm the 350b473 dead-end condition is actually harder to reach, not easier. Verified: all six app builds green, `svelte-check` 0 errors, Node fixture extended to 38/38 covering the re-arm, autoplay cancellability, scope isolation, gate pre-release/hang, retrigger, snapshot resume and base-game parity. NOT verified in a browser.
- 2026-07-20 — **Slam stop: the book reveal and the free-spin intro are now UNSKIPPABLE.** Owner report: "the free spin intro and book reveal can't be stopped! … they skip (but keep on playing) and the game starts while the irig for the book reveal still playing". Racing a wait against `roundSkip` ends the WAIT, not the ANIMATION: for a count-up or a win line that is fine (the skip lands the picture at its final value), but the book/expanding-symbol reveal and the free-spin intro are spine/`.irig` TIMELINES that nobody cancels, so the round chain walked on while the rig kept playing over the next free spin's reels. New `apps/lines/src/game/unskippablePresentation.ts`: `UNSKIPPABLE_BOOK_EVENTS` (`setExpandingSymbol`, `freeSpinTrigger`) + a depth-scoped `runBookEventPresentation`, opened in `playBookEvent` around the WHOLE dispatch so it covers the coded, v1-flow and v2-flow paths alike; `awaitCue` (every awaited `broadcastAsync` in the game now goes through it — `bookEventHandlerMap`, `flowEffects.awaitPresentation`, `flowRuntime`'s `broadcastAsync`, `flowV2Runtime`'s cue `broadcast`) and `waitPresentation` (both runtimes' `waitForTimeout`) drop the race inside that scope. Scoped by BOOK EVENT, not cue name, because `transition`/`uiHide`/`uiShow` punctuate the free-spin END too and that must keep fast-forwarding. Ordering falls out for free: book events are strictly serial and each handler fully awaited (`createPlayBookUtils`), so the first free-spin `reveal` cannot start its board work until the reveal cue's subscribers have settled. **THE HANG RULE** — `PLAYER_GATED_CUES` (`freeSpinIntroUpdate`, `bookRevealGateShow`, `freeSpinOutroCountUp`) keep racing even inside the scope, and flow-v2's `showContainer{awaitComplete}` keeps its `roundSkip.onSkip → complete` release (350b473 NOT regressed): each is held by a `PressToContinue`, and after a slam the spin button is inert and drawn above the overlay, so it swallows the very tap that would release it. The free-spin intro is therefore unskippable in everything except its tap-to-continue hold. Base-game slam, the reel snap and the rest of the win presentation are untouched. Verified: `apps/lines` build green, `svelte-check` 0 errors, and a Node fixture bundling the REAL `skipToken.ts` + `unskippablePresentation.ts` (16/16) proving each unskippable cue settles with the token already tripped, that it does not release before its subscribers, the reveal→next-event ordering, that every player-gated cue still releases, and that outside the scope the fast-forward is unchanged. NOT verified in a browser.
- 2026-07-20 — **Win presentation layers as HUD, not board furniture.** The win line and the amount it stamps rendered inside the board `<MainContainer>` at the implicit z 0, so every list-ordered screen in `LAYER_BAND_BASE` (the HUD, `basegameOverlays`, an author's above-reel art) painted over them. `<WinLine>` now mounts as its own board-space layer at the new fixed `LAYER_BAND_WIN_PRESENTATION` (8000, `engine-layout/layerOrder.ts`) — above that whole band, still below the pinned `LAYER_BAND_TAKEOVER` celebrations and the engine's `LAYER_BAND_TOP` round-blocking gates, so nothing an author layers can bury the line but a gate still covers it. It keeps its own `<MainContainer>` wrapper (it positions through `<BoardContainer>` in board-local coords and needs the game-space transform its old parent supplied); `<MainContainer>` is pure derived layout, so the second instance reproduces the identical transform and the line lands in the same PLACE — only higher in the stack. Still inside the base-game gate, so it unmounts with the board as before. Shipped to `_runtime/lines` (run 29734003384; verified live on `bookofborutremake` / `bundle.BWzPyPRC.js`) and **confirmed good in a live spin by the owner**. Standalone Book of Borut is NOT covered: it keeps its own `Game.svelte`, which still mounts `<WinLine>` inside the board `<MainContainer>` — the submodule delivers the constant but the markup edit must be mirrored there by hand.
- 2026-07-20 — **Slam stop: the free-spin outro hold now releases on a slam (hang fix).** Owner report: "the game breaks during free spin if I slam stop just before the free spin win". `showContainer{awaitComplete}` — the flow-v2 round-block hold, shipped as a KNOWN gap with af71840 — was the ONLY await left in the round chain that a slam could not release: every cue, delay and cut-short effect races `roundSkip`, but that hold is resolved solely by a tap on a `tapToContinue` overlay. In a flow-driven game the free-spin outro IS that tap gate, so a slam fast-forwarded the whole feature and then parked on it. Two things made it a dead end rather than a wait: `runSpinOrSlamStop` early-returns once the token is tripped, so the spin button the player presses next does nothing, and that button is drawn above the overlay, so it swallows the very tap that would have released the hold. The round then never completes ⇒ `playBet` never returns ⇒ the machine never reaches idle. `playBet`'s `try/finally` cannot save it: an un-settled await is not an exception, so the `finally` never runs. Fixed in `apps/lines/src/game/flowV2Runtime.svelte.ts` with a slam-aware mount wrapper — `awaitComplete` registers the hold, then subscribes `roundSkip.onSkip` to `mount.complete(id)` (subscribed AFTER registration, because `onSkip` fires synchronously on an already-tripped token). Completing is exactly what the tap does, so the same exec chain resumes show → hide → next and no beat, state write or win is dropped. The wrapper is applied unconditionally; the `?flowlog=1` trace wrapper now delegates through it (it previously wrapped `rawMount`, so a fix placed there would only have existed with tracing on). **Coded and v1-flow paths were never affected** — every hold in `bookEventHandlerMap`/`flowEffects`/`flowRuntime` is raced, and a Node harness driving the real svelte `Tween` + `interruptible` + `skipToken` on a fake raf clock confirmed `reel.spin()` settles for a slam at every point in the roll AND for a reel whose spin STARTS with the token already tripped (the next free-spin reveal), so the fully-awaited `enhancedBoard.spin()` cannot strand the round. Only apps/lines runs flow-v2 ⇒ no other app affected. **Needs a runtime publish to reach online games** (shared `_runtime/lines` bundle) + a submodule bump for standalone `bookofborut`.
- 2026-07-20 — **Slam stop implemented end-to-end (reels + win presentation).** It was ~60% built but gated to autoplay: `ButtonBetProvider.getKey()` returned `spin_disabled` for a rolling SINGLE bet ("a single bet's roll has nothing to stop"), and the identical logic was duplicated verbatim in `Game.svelte`. Four faults, all fixed:
  - **The button was inert.** The key/press decision now lives ONCE in `packages/utils-shared/spinStop.ts` (`getSpinButtonKey` / `runSpinOrSlamStop`), shared by `ButtonBetProvider.svelte`, `Game.svelte`'s registered `spin` action and its replacement Space hotkey. A rolling round shows a live STOP. The `stopDisabled` latch (which made a second press a no-op) is GONE — a second press has to be able to cancel autoplay. `SpinButtonKey` lost its `stop_disabled` member, which nothing could produce.
  - **Anticipating reels were un-slammable.** `createReelForSpinning`'s `noStop` branch awaited `slideDown()` OUTSIDE `interruptible.add`, so an anticipation reel could not be interrupted at all. It now always routes through the interrupt, with an explicit slam (`roundSkip.isSkipped()`) overriding `noStop` — which also covers the reel that has not STARTED rolling when the press lands, something `interruptible` cannot do (it only cuts an in-flight wait). `reel.stop()` clears `anticipating`, and `createEnhanceBoardSpin` no longer ARMS a new anticipation on a slammed round.
  - **The post-reveal phase was not skippable.** New primitive `packages/utils-shared/skipToken.ts` — `createSkipToken()` + the `roundSkip` singleton: `isSkipped/skip/reset/onSkip/wait/race`. STICKY (`onSkip` fires immediately for a late subscriber), which is what lets ONE press fast-forward everything that mounts after it. `runSpinOrSlamStop` trips it SYNCHRONOUSLY before broadcasting `stopButtonClick`, so no subscriber-ordering dependency exists. Re-armed per ROUND in `playBet` and nowhere else — a bonus book is one round, so one press covers the whole feature. `WinCountUpProvider` finishes on skip (and starts at duration 0 if already slammed) so count-ups land on the FINAL total; `WinLine` draws complete; the hard-coded waits and awaited broadcasts in `bookEventHandlerMap`/`flowEffects` are raced/`roundSkip.wait`-ed. Book events are still ALL run and each handler still fully awaited — racing a handler would let a detached `reveal` keep spinning the reels under the `winInfo` that follows.
  - **Flow-driven presentations ignored it.** Both flow hosts inject `waitForTimeout: roundSkip.wait` and race their awaited broadcasts. The flow EFFECT registry is invoked directly by the interpreter and bypasses that wrapper, so every awaited broadcast in `flowEffects.ts` now goes through a local `awaitPresentation` helper. The registry itself is deliberately NOT raced: on a whole-feature skip that would resolve `revealBoard` instantly for all ten spins and run ten reveals concurrently on the same reels. `FlowEmitter.broadcastAsync` was narrowed `Promise<unknown[]>` → `Promise<unknown>` (the results were never read) so the game can wrap it.
  - No XState change was needed: with the waits collapsed, `playGame` resolves fast and the machine drains to idle on its own.
  - **Turbo side effect checked (`ButtonTurbo`):** `stopButtonClick` still flips a NON-persistent turbo on and `stopButtonEnable` flips it off; `updateIsTurbo(_, {persistent:false})` is a no-op while `isTurboLocked`, so a player-set turbo is never corrupted. `playBet` now broadcasts `stopButtonEnable` from a `finally`, so a throwing handler can no longer leave turbo stuck on.
  - **One press, one action.** The spin button's Space hotkey now stands down while a press-to-continue overlay is mounted (`stateUi.continuePressCount` / `hasContinuePress()`, armed by each game's `PressToContinue`) — otherwise one keypress ran both the slam and the continue-press, with `soundPressBet` over the outro music. The pointer needs no such gate: the overlay's full-screen rect and the button are separate hit targets, so a click reaches exactly one, and both fast-forward the presentation.
  - **Blast radius (shared code).** `spinStop`/`ButtonBetProvider`, the `createEnhanceBoardSpin` anticipation guard, `WinCountUpProvider`'s skip hook and `PressToContinue`'s press claim are shared, so ALL six dev games get the live single-spin STOP and the two-press autoplay cancel. `createReelForSpinning` (the `noStop`-inside-`interruptible` change + `stop()` clearing `anticipating`) reaches only **lines** and **price** — `cluster`/`ways`/`scatter`/`number-picker` use `createReelForCascading`, which is untouched. Because `roundSkip` is a process-wide singleton, EVERY app's `playBet` had to be given the per-round `reset()`; without it one slam would have stayed tripped for the rest of the session (no anticipation would ever arm again, every count-up instant). The presentation fast-forward itself is still `apps/lines` only.
  - **Known gaps:** ~~a flow-v2 `showContainer{awaitComplete}` round-block hold (tap-to-continue) is NOT released by a slam~~ — this shipped as a HANG (the free-spin outro never released); fixed the same day, see the entry above. `UI_FEATURES_UK` is now an incomplete UK profile (UK prohibits player-led spin-stop but slam is unconditional); noted in `stateUi.svelte.ts`. The never-assigned `onCountUpComplete` dead prop was removed in `lines`' `Win`/`FreeSpinOutroGate` (and `WinCountUpProvider.oncomplete` made optional), but the same dead pattern remains in the other apps' copies.
  - **Not published** to `_runtime/lines`, and Book of Borut's engine submodule not bumped — an engine change reaches an online game only through the runtime-release chain.
- 2026-07-17 — **Book-of expanding special paid nothing unless it happened to form a left-aligned run from reel 1.** The book-of mock RGS (`scripts/mock-rgs-server-book.mjs` — which IS Borut's live RGS) expanded the free-spin special onto its reels but then evaluated the expanded board **strictly on the paylines**, so a special on non-adjacent reels, or on any reels not starting at reel 1, formed no run and paid zero (reported from a spin with the book/cactus on three reels that returned nothing). The special is the Book-of-Ra expanding symbol: it now pays **scatter-style on the COUNT OF REELS it covers** (adjacency-independent), at `PAY_TABLE_LINE[special][reelCount] × totalStake` — which equals `mult × betPerLine × NUM_LINES`, the symbol paying that N-of-a-kind on all ten lines at once. New `evaluateExpandingSpecial`; `expandSpecialBoard` still runs so the OTHER symbols pay their normal line wins on the expanded board (the special itself excluded from that pass, never double-paid). Gate is now **reels covered, not raw symbol count**: 3+ reels, except `PIC1` (the top symbol) which expands from 2 since it pays from a 2-of-a-kind. The client morph gate in `packages/rgs-translator-eagaming/src/engineFacade.ts` was changed in lockstep (same `PIC1`=2 / else=3 reel gate) so the reels that visibly morph are exactly the reels that pay. Validated deterministically against the reported board and the gate edges (PIC1@2, PIC2@2→null, ACE@5) — the live mock's reels are random so a browser spin can't reproduce the exact board. Standalone Borut carries its own `scripts/` copy, so this must be mirrored there before it ships.
- 2026-07-17 — **Four free-spin presentation faults, all silent by construction.** Reported as "the reveal symbol never appears, the counter says `10 OF` on spin one, the book symbol vanished, the outro plays nothing".
  - **`SpineBoneAttach` could not detect a missing bone.** `follow()` probed the result of `getBonePosition(name, outPos)` for truthiness, but spine-pixi returns the passed-in scratch point UNCHANGED on a miss (it only `console.error`s). So `pos` was always truthy, the documented "never silently vanish" fallback was **dead code**, and a bad/unset bone re-applied the world transform to the previous frame's leftover `bonePoint` every tick — compounding drift plus an error per frame. Now resolves the bone first and genuinely falls back. The bone is also resolved once per frame instead of twice.
  - **The free-spin counter had no writer on the trigger chain.** `freeSpinCounterCurrent` is only ever written by `updateFreeSpinCounter` (on `updateFreeSpin`), which does not arrive until the first spin has RESOLVED — so `freeSpinCounterShow` put the counter on screen reading a value nobody wrote (0 on a first session; the previous session's final count on a repeat, since `exitFreeSpinOutro` never reset it). `setFreeSpinCounterTotal`/`…TotalOnly` now seed `current = 1` and `exitFreeSpinOutro` resets it to 0.
  - **The `freeSpins` composed string bypassed the finite guard** its own siblings use (`freeSpinsCurrent`/`freeSpinsRemaining` → `freeSpinCounterValues.ts`), reading `stateUi` raw. It was the one readout that could still render a non-number at the player, and because the `gold` BMFont carries no letter glyphs, `NaN OF 10` dropped to a BLANK rather than visibly reading "NaN" — presenting as a layout fault instead of a wiring one. Now routed through the guard.
  - **The free-spin outro animation never re-armed.** `SpineTrack`'s `complete` parks `animationName` on `idleAnimation`, and the `$effect` that sets it only refires when `outroAnimation` CHANGES — which it never does. So the authored outro animation ran exactly once per page load and every later outro opened already parked on idle. `freeSpinOutroShow` now re-arms it.
  - **Not published** to `_runtime/lines` yet at time of writing — see the runtime-release note below; an engine fix reaches an online game only through that chain.
  - **Authoring note (not an engine fault):** placing any node in the `specialBook` scene flips `hasAuthoredBookReveal` ⇒ `Game.svelte` strips the coded `SpecialBook` anchor, which is why authoring a reveal rig "removes" the book symbol. Working as designed; the reveal is expected to carry the symbol itself (via `symbolBone`).
- 2026-07-17 — **Full-screen camera effects (shake / flash / zoom punch / chromatic wobble), authorable from Flow.** New `pixi-svelte` `cameraEffects` module driving the `Application.stage` + one `cameraEffect` vocab action with a `kind` enum dropdown. Verified against the LIVE `apps/lines` stage (`?flowV2=lines`) by pumping `app.ticker.update()` by hand — the Browser pane backgrounds the tab, so rAF never fires and the Pixi ticker sits frozen at 0ms; a naive `await` on any ticker-driven effect just times out there. All four kinds hit their reference strength and restore the stage to exactly `(0,0)`/scale 1 with the ticker listener released; all four compose simultaneously and drain independently; `intensity: 999` clamps; an unknown `kind` no-ops instead of throwing. The real registry was exercised through Vite's module cache (`import('/src/game/flowEffects.ts')` → same singleton as `window.__PIXI_APP__`), confirming `blocking:true` returns a Promise that resolves only after frames run and the default returns `undefined`. **Not yet published** to `_runtime/lines`, and no FlowDoc authors it yet.
  - **Gotcha found in review — `Container.filterArea` is LOCAL, not global.** `FilterSystem` does `bounds.addRect(filterArea)` then `bounds.applyMatrix(container.worldTransform)` (`filters/FilterSystem.mjs:63-66`). Setting `stage.filterArea = app.screen` therefore makes the filter bounds RIDE any stage transform — so a `chromaticWobble` composed with a `shake` clipped an unrendered strip off the screen edge, oscillating at the shake frequency (24px offset ⇒ 24px blank strip; measured). Chroma _alone_ is fine (identity transform), which is exactly why it survived the first live pass. The rect is now recomputed per frame as the inverse-transformed viewport, verified to 0px drift under a live shake with a control proving the check fails against the old code. Anything else that filters a transformed container faces the same trap.
- 2026-07-15 — **Idle-bound rig FX no longer clipped by the reel mask.** A rig-timeline effect (`RiggedEffect`) authored to fire on a symbol's IDLE animation was clipped to the reel window, because idle symbols render on the MASKED board layer and `RiggedEffect` parents its FX subtree on the host spine (a descendant of the mask). New generic pixi-svelte seam: `FxPortalProvider` mounts one UNMASKED sibling container + provides it via a new `getContextFxPortal()`/`setContextFxPortal()` context; `RiggedEffect`, when that context is present, parents its `fxParent` into the portal and per-frame mirrors the host spine's WORLD transform onto it (`setFromMatrix(portalWorld⁻¹ · spineWorld)`) so the effect is transform-identical to the spine-parented default but no longer clipped. `apps/lines/Board.svelte` wraps ONLY the masked (`animate={false}`) layer in `FxPortalProvider`; the win/animate layer is already unmasked and left unwrapped, so the win path + all non-portalled `RiggedEffect` usages (layout spines, tools) are byte-identical. Symbol ART stays masked. **Not yet published** to `_runtime/lines` (needs the runtime-release chain).
- 2026-07-14 — Free-spin intro: a screen-driving v2 flow is the sole presentation authority; the coded intro no longer half-fires when the flow drops the intro ([detail in history](../history.md)).
- 2026-07-14 — Win-line renderer ported into the shared engine (`806d6cf`) — every `runtime:lines` game now draws the authored payline + win amount ([detail in history](../history.md)).
- 2026-07-13 — Free-spin sequential reel stop shipped to `_runtime/lines` + Borut engine bump (`6004374`) ([detail in history](../history.md)).
- 2026-07-03 — Three-knob reel grid; shared `CatalogText` gives coded HUD text authored bitmap fonts ([detail in history](../history.md)).
