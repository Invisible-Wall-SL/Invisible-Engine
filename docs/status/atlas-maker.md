# Invisible Atlas Maker — status

> Design: [docs/design/atlas-per-user-session.md](../design/atlas-per-user-session.md) · Guide: [docs/tools/atlas-maker.md](../tools/atlas-maker.md) · Agent: `.claude/agents/atlas-python-tools.md`

**One-line state:** Live on Railway (`atlas-tool`). SDXL generate → slice → compose → deploy proven end-to-end; **FLUX (txt2img *and* the ref/ControlNet path) and gpt_image both proven on the RunPod backend** (owner-tested 2026-08-18) — the old "only SDXL ControlNets are installed on the local 4070" ceiling is gone.

## Current state
Works today on `main` / live:
- **From-scratch atlases (auto-pack)** — create a brand-new atlas in the tool (no Sheet Maker / `.atlas` needed) and grow it region-by-region. **`＋ New atlas`** asks for the name in a dialog first (sanitised the way `r2_slug` stores it; a name already in the manifest dropdown is flagged live and confirmed on Create, which then sends `overwrite:true`), writes an empty `pack`-layout manifest (`atlas.layout:"pack"`, no bound `.atlas`) and makes it active — a bare `/newatlas` call without the flag still switches to the existing atlas instead of overwriting; **`＋ Add region`** / per-card **🗑** append/remove name-only regions; each is generated from its prompt like any other. **Create Atlas then auto-packs**: `auto_pack_layout` (ui_server) measures each region's ALPHA-trimmed variant, runs the ported MaxRects packer (`pack.py`, a lockstep copy of `sheet-tool/packer.py` `pack()`), and stamps `x/y/w/h(/rotated)` + `atlas.width/height` back onto the manifest BEFORE the compose subprocess reads it. Region size is entirely emergent from the trimmed art (no per-region size to set); the page auto-sizes and re-packs every Create Atlas, so the atlas morphs as regions are added. Placement is 1:1 — the packed rect equals the trimmed bbox, so compose's default `contain` path scales by 1.0 (verified: packer no-overlap/in-bounds, `auto_pack_layout` stamping + ungenerated-skip, and `fit_to_region` exact-fill). **The geometry is derived output and only the packer owns it:** anything it did not just place is stripped of its rect, so a region can never carry a previous page's placement into `_deployatlas`'s manifest-regions fallback, which emits the TexturePacker `.json` from those same fields (see the 2026-09-16 entry — a stale rect there shipped a frame showing the wrong art). Both the pack note and the deploy note name every region left out of the frame map. After compose, `publish_pack_page` mirrors the composed page to `<C>/<P>/atlas/<stem>_new.webp` and repoints the manifest at it, so a `pack` atlas's declared page and its rects always come from the same producer (2026-09-16 in "Recent changes"). The page auto-crops to the layout's used bounding box on BOTH axes (`AUTO_PACK_MAX_WIDTH` = 2048 is only a cap), so a small atlas is a snug page, not a 2048-wide sheet of empty space — this is `pack.py`'s one intentional divergence from `sheet-tool/packer.py` `pack()` (which keeps its fixed sheet width); the `_MaxRects` core stays identical and compose parity (`_packer_compose_tile` ↔ `packer.compose`) is untouched. **Owner live-verify owed** (a real generate + Create Atlas + deploy through a from-scratch atlas on the GPU/tunnel).
- The local Python Atlas Maker (stdlib `http.server` + Pillow) **re-hosted on Railway** — `cloud_paths.py` staging mirrors the R2 prefix 1:1, `storage.py` write-through, ComfyUI reached over the Cloudflare tunnel (refs via `/upload/image`, outputs via `/view`).
- **SDXL** generate / slice / compose / deploy — the proven, default pipeline.
- Per-region card: prompt edit, seed lock/unlock, style/shape ref pick — **rembg (background-removal) toggle**, refs uploaded locally or **picked from R2** via `/fsbrowse`, one-click "lock the seed that made this".
- **Self-contained manifests** — Windows path/trailing-dot normalisation done in R2; `.atlas` + source-page upload (B10) repoints the manifest to staging-relative paths.
- **FLUX builder complete** — txt2img proven live (2026-05-30, ~105s on the 8GB 4070).
- **Blueprints** (an Atlas Maker feature, NOT a separate tool — [design](../design/invisible-blueprints.md)) — shareable ComfyUI workflows in a global `_shared/blueprints/` R2 library; pick one in the Atlas Maker to "generate with this network". **Code-complete (all 8 phases), owner live-verify owed.** Committed + publish-gated (`ATLAS_BLUEPRINT_SECRET`); Settings has the resolved-workflow (`/blueprintresolved`) export for debugging.
- **Sheet-derived FX cells auto-derive** on load/Process from their base region (not AI-generated), keyed on `shine.fx_layer_info` + `mode in FX_PRESETS` — no wasted ComfyUI credits.
- **Region Overlay Inspector** (`/atlasview`, the `🖼 View atlas` button — [guide](../tools/atlas-maker.md#-view-atlas--the-region-overlay-inspector)) — the composed page with each manifest rect outlined, plus **three separate readings** per region, kept apart because they know different amounts:
  1. **Sheet parity** (`MATCHES SHEET` / `DIFFERS` + delta / `NO SOURCE`) — the headline and the only *evidence* of how a region was placed. Server-side (`/parityscan`, on demand — it opens every source image): recompose the tile from the region's source art through the REAL `_packer_compose_tile` and diff it against the page's rect pixels. Directly predicts whether a re-Create-Atlas would move the region. Tolerance `max Δ 2`/channel is pure slack — the test is deterministic, so a real sheet-composed region matches **byte-identically** (`max Δ 0`); a placement difference reads `max Δ 255`. The comparison must replay compose's `canvas.paste(img, …, img)`, which is not a no-op (PIL applies the mask to every band: a semi-transparent pixel lands as `src * a`).
  2. **Placement mode** — which `fit_to_region` branch the next compose takes (`_placement_mode` mirrors that dispatch, incl. `spine_slot = "orig_w" in region and "orig_h" in region`). Answers *"will the sheet-parity fix do anything for this page?"*: only an EXPLICIT `fit_mode:"contain"` reaches it, and only `sheet-tool/atlas_writers.py` writes that field — a manifest imported from a `.atlas` (`_tp_frame_to_region`) has trim but no `fit_mode`, defaults to `fill`, and the parity fix is a **no-op** for it. Counted in the summary, which says so outright when no region carries it.
  3. **Ink coverage** (`FILLS` / `INSET n%` / `EMPTY`) — how much of the rect the art's alpha bbox covers. A **measurement, not a producer verdict**: full-bleed art FILLS under the sheet packer too (its scale clamps at 1.0), an FX halo FILLS while its base is INSET (`T_UI_Spin_Edge` 88% vs `T_UI_Spin_Edge_glow` 100% — same art, same placement), and a `contain` region reads INSET even when upscaled. The earlier "a mixed FILLS/INSET page proves two producers" claim was **unsound and has been removed**.

  Handles rotated `(h × w)` footprints and draws trimmed regions' untrimmed frame in the manifest's own Y-down convention. **Code-complete, owner live-verify owed.**
- **A Sheet-Maker cell composes byte-identically to its sheet** — the two composers used to disagree on what a rect MEANS: `sheet-tool/packer.py` `compose` scales against the **full art canvas** with a `min(…, 1.0)` clamp (**never upscales**) then centres the visible bbox, while `batch_atlas.fit_to_region` alpha-cropped to the **ink bbox** and scaled that to fill the rect with **no clamp** (upscales). Same `bounds:`, art up to ~2.5× bigger with its origin shifted from a centred inset to the rect's corner — and since the Rigger prefers the **deployed** page as soon as one exists (`editorRegions.ts` `findDeployedPage`), a Create-Atlas + deploy silently swapped a rig's pixels from the sheet's convention to the atlas's and offset every attachment. Now an **EXPLICIT** `fit_mode:"contain"` (only `sheet-tool/atlas_writers.py` writes it) routes to `_packer_compose_tile`, a verbatim replay of `packer.compose` — so republishing is a no-op for placement. **Explicit-only by design:** the default fallback also resolves to `contain` for legacy cell-grid regions, which keep their alpha-crop + letterbox. Proven byte-identical to the real `packer.compose` across 8 cases (rect bigger/smaller/equal, asymmetric padding, fully-transparent, rotated, rotated+downscale, non-square); 22/22 no-regression vs the pre-change function at `PADDING_PCT` 0.0 **and** 0.12. Keep `_packer_compose_tile` in lockstep with `packer.compose`. **Because it is explicit-only, it is only as good as whoever preserves the field** — the `Open in Atlas Maker` handoff dropped it (and forced the `fill` fallback) until 2026-09-01, so the parity path never ran for the route that carries sheets into this tool; see "Recent changes".
- **FX layers register with their base through compose** — `fit_to_region` alpha-crops each region independently, which mis-scaled every FX layer (a glow's halo makes its alpha bbox bigger than its base's, so the glyph shrank to ~54% and drifted up to ~15px when the halo clipped a canvas edge). `batch_atlas.fx_registration_crop` now crops an FX layer to its BASE's bbox grown by the two slots' size ratio — same scale, concentric, halo kept to whatever the packed FX slot can carry. Guarded: an FX image whose canvas ≠ its base's (hand-made art) falls back to self-cropping.

- **Flipbook video sessions run in THIS service** (`video_runner.py` + the `/video/*` routes in `ui_server.py`), because the blueprint library, the generic runner, the RunPod Serverless transport and the packer all already live here. The feature is the Flipbook's — its state lives in [flipbook status](flipbook.md), design in [invisible-flipbook-video.md](../design/invisible-flipbook-video.md). It deliberately does NOT touch the active-manifest globals, so it is unaffected by (and does not worsen) the per-user isolation gap in open item 4.

## Open items / next
1. **`_shadow` FX cells lose their drop offset under sheet-parity compose.** Fallout of the rect-convention fix (see "Works today"). `shine.make_shadow` (`shine.py:320`, `offset_y: int = 8`) is the ONLY asymmetric FX builder: its blob's bbox sits ~4px below the glyph in canvas space, and packer.compose centres the **visible bbox** — so parity re-centres the shadow and erases the deliberate drop. Everything else (glow/blur/zoom) measures exactly (0.0, 0.0) drift. The pre-fix behaviour was not a working alternative — it kept the offset only by clipping the shadow to its base's bbox and upscaling ~5×, i.e. the bug being fixed. **Next:** bake the offset into a symmetric canvas in `make_shadow` so the blob's bbox centre coincides with the glyph's, rather than relying on placement to preserve it.
2. **The re-pack/deploy frame guards are owed ONE live run (owner).** #684 + #686 are on `main` and everything about them is proved offline — real packer, real compose subprocess, real frame writer, 67 assertions, both guards proved against the behaviour they replace — but against a fake bucket and no GPU. **The run:** on a from-scratch atlas, generate two regions → Create Atlas → deploy; then delete one region's variant → Create Atlas → deploy again. **Pass =** the second deployed `.json` carries a frame for the surviving region ONLY, its `meta.size` equals the new page's real pixel size, and the deploy note names the dropped region. Distinct from item 3, which is the `.atlas`-BOUND compose/slice path — this one is a from-scratch `pack` atlas, and the guards only fire on that layout. It does, however, close the "Owner live-verify owed" note on the auto-pack bullet in "Current state".
3. **`.atlas` compose/slice (B10 / Part A) — live browser smoke-test owed** against live R2 (Part B = repoint the stale Windows-path `atlas.atlas_file`/`source_image` fields, owner + R2 creds).
4. ✅ ~~**FLUX ref/ControlNet path unproven**~~ — **DONE (owner-tested 2026-08-18).** shape_ref / ControlNet generates on the **RunPod** backend, which carries its own ControlNet models; it was only ever blocked on what the local 4070 had installed.
5. **Per-user session isolation — planned, unbuilt** (design `atlas-per-user-session.md`): the active-manifest resolver is process-global, so two users on one project clobber each other's open selection + see each other's render progress. Phases 1–3 (thread `user` id → per-user overlay → per-user render state).
6. **Access gate** — `ATLAS_TOOL_SECRET` unset → tool is open on its Railway URL; verify the launcher `?k=` flow when set.
7. **Blueprints: owner live-verify + model auto-download** — the 8 phases are code-complete but unverified live; the ComfyUI-Manager model auto-download (so a blueprint's models install without a manual drop) is the main remaining piece ([design](../design/invisible-blueprints.md) §7). **Advanced 2026-09-15, not closed:** `enrich_from_catalog` removes the blocker that made this unreachable for an uploaded blueprint (a derived declaration had no `url`/`base`, so nothing was ever installable) — but it is proved only against injected fixtures. **The live half is still owed: a real Manager, a real whitelist, a real download.**
8. ✅ ~~**The New-blueprint modal can't bind a graph whose knobs were "converted to input".**~~ — **DONE (2026-09-01)**, ported from the Flipbook's video twin the same day it was fixed there, so the two modals are back in step. See "Recent changes" below.

9. ✅ ~~**Seed the RunPod model catalog once (owner).**~~ — **DONE (2026-09-04)**: ⟳ discovers a running pod itself; just start one on /comfyui and press it. See "Recent changes".
10. **The real *My computer* render over the tunnel is owed (owner).** The `http` transport has been off the production path since 2026-08-18; the new setting puts it back on demand. Start ComfyUI + the desktop launcher's tunnel, pick *My computer* in ⚙ Global settings, render one region. **Attempted 2026-09-07 and it got further than expected:** the request crossed the tunnel and ComfyUI answered — refusing the graph for a missing node pack, not a transport fault. So what is still owed is a render against a local ComfyUI whose node set actually matches (`services/atlas-comfy-pod/tools/sync-local-nodes.py`, then a ComfyUI restart, then the PuLID weights), or the same blueprint on RunPod. **Half of that is now done:** `sync-local-nodes.py` put `PuLID_ComfyUI` on the local box at the pinned SHA and, after a restart, the five `Pulid*` classes register and the preflight passes on the graph that failed (1152 classes, no pack lost). What is left is purely WEIGHTS - the PuLID SDXL ip-adapter and InsightFace `antelopev2` - so the render is still owed. See "Recent changes".

11. ✅ ~~**A save made during a render's FX rebuild is still lost.**~~ — **DONE (2026-09-14).** The slow job no longer writes its own stale copy of the whole manifest; it re-applies only the fields it owns, onto a manifest re-read inside the lock. Same treatment for the two neighbouring cycles in `run_compose`. See "Recent changes".

## Blocked (owner / external)
_Nothing._ Both long-standing entries cleared on 2026-08-18 — see below.

## Recent changes
- 2026-09-16 — **Hydration was overwriting the page compose had just written, so a stale page shipped under fresh rects.** Owner report after a re-generate: the frames come out offset and off-centre, and 7 of 25 vanished from the deployed sheet. One cause, and it is the mirror #682 introduced coming back round.
  - **Measured (live R2, `invisible_wall/test6`, read-only).** `manifests/atlas_manifest_S_New_Squid_Idle.json` (12:43:37.353Z) is internally CORRECT: `atlas` 1934×1612, 25 regions on a uniform 320×320 grid, `source_image_path` → `atlas/S_New_Squid_Idle_new.webp` (the #682 pointer, working as designed). But the page it names is **2047×1173, 977590 bytes — byte-for-byte the 09:50 deploy page from before the re-generate**, re-published with a fresh mtime. Rects packed for a 1934×1612 page cropped out of a 2047×1173 one = every frame in the wrong place; the 7 rects at y=966 and y=1288 fall off the bottom = 18 frames deployed, not 25, so the clip plays short.
  - **The mechanism — NOT the `.webp`-shadows-`.png` story it looks like.** Compose is not best-effort about overwriting: it saves `<stem>_new.png` and then `canvas.save(<stem>_new.webp)` unconditionally, deleting the webp if the encode fails. A stale webp cannot survive a compose that reaches its save. What actually happened is upstream of all of that: `cloud_paths.hydrate()` pulls `atlas/` in a **background thread**, and `iw_common.storage.pull_prefix` re-downloaded any object whose byte count differed from the local file — **newer local file or not**. So a pull landing while (or just after) compose ran replaced the fresh page with the old one from the bucket and stamped it with a brand-new mtime. `publish_pack_page`'s freshness guard is an mtime check; it saw a brand-new file and published the old pixels. **No mtime-based guard can see this** — an mtime says when a file was last written, not what is in it.
  - **Fix 1 — measure the page, at both gates.** `publish_pack_page` now opens the page and refuses to publish (or repoint) one whose real pixel size contradicts `atlas.width/height`; the manifest keeps its previous pointer. `_deployatlas` now measures **before** it uploads anything and refuses the WHOLE deploy — page, `.json`, everything — leaving what is already deployed standing, rather than uploading the page and then quietly dropping the frames that fall off it. This is the structural check waved away early on as "not worth the extra request"; a page that disagrees with the manifest it ships under is never correct, whatever any mtime says. It can never fire on a page this tool composed against the manifest it is deploying: compose builds the canvas at exactly `atlas.width × atlas.height`.
  - **Fix 2 — the stale-page shadowing, closed at the source and at both readers.** Compose deletes any existing `<stem>_new.webp` immediately before writing the pair, so the choice cannot arise on the forward path (deliberately there and not at the top of compose: a compose that dies earlier must leave the previous page deployable). `_composed_page` and the deploy's page pick both drop a `.webp` older than the `.png` beside it — which catches what the size check cannot, a leftover of the *same* layout whose only fault is its pixels.
  - **Fix 3 — hydration never overwrites newer local work** (`iw_common.storage.pull_prefix`): a local file whose mtime is newer than the object's LastModified was produced after it, so the object has nothing to teach it. A genuinely re-uploaded object still re-downloads (its LastModified is newer than the copy we hold), so the "sheet editor shows old images" skip that the same-size branch exists for is untouched. **Chosen over moving the published page out of `atlas/`:** `atlas/` is the composed-page prefix, staging mirrors R2 1:1, and hydrating it is what lets a fresh container slice or deploy a page composed in a previous life without re-composing. Hiding the page somewhere unhydrated would have kept #682's guarantee while leaving the same clobber to hit `input/`, `sheets/` and the sheet tool.
  - **Verified:** `test_pack_page_pointer.py` — **57** offline assertions (was 31), extended with the reproduction: a stale `_new.webp` beside a fresh `_new.png` (both the wrong-size and the right-size-but-older case), a page whose pixel size contradicts the manifest, the deploy control that all 25 frames ship when page and rects agree, and `pull_prefix` driven against a fake client to prove a freshly composed page survives a hydrate. Suite green: `test_pack_trim` 39, `test_auto_pack_clear` 15, `test_stale_pack_rects` 67, plus every other atlas-tool suite and `sheet-tool/test_plist_import`; `test_config_defaults` and `test_shared_taxonomy` fail on `origin/main` already. **Not verified live: no re-generate has run through this** — and `S_New_Squid_Idle` in `test6` is still in the bad state until one does (a Create Atlas will now compose a correct page and publish it; the deploy would have refused the current one).
- 2026-09-16 — **A re-pack moves a frame; it no longer re-authors it. Plus: the alpha trim is now optional.** Owner report: after re-composing, a flipbook animation's character "moves up and down left and right in an unnatural way" — the frames no longer share a common centre.
  - **What was measured, and what it rules out.** `invisible_wall/test6`, sheet `S_New_Squid_Idle`, 25 regions. The manifest's regions carry **no trim in either spelling** and have **24 distinct packed sizes**, so `_deployatlas`'s `_pick(…, default=rw/rh)` wrote every frame a `spriteSourceSize` of `0,0` and a `sourceSize` of its own crop — each frame declaring itself its own tight crop, which is what makes PIXI anchor every one on a different centre.
    **The obvious explanation is wrong, and it was tested rather than assumed.** The Flipbook's own descriptor `sheets/S_New_Squid_Idle/S_New_Squid_Idle.json` survives (9116 B, all 25 frames) and records `320×320` packed, `sourceSize 320×320`, `spriteSourceSize 0,0` for **every** frame — i.e. `video_to_clip._extract` found nothing to trim, because these frames are fully opaque. Recomputing each cell's alpha bbox off the source page with the packer's own rule (`getchannel("A").getbbox()`) returns the full `320×320` at `0,0` for all 25: **0 of 25 match the manifest's rects**. So the `269×287`-class crops were NOT produced by alpha-cropping the sheet page, and the `export_prefix` auto-seed's page slices cannot be what the packer measured.
  - **The destruction path.** `auto_pack_layout` measures `override_image_path(r) or _pick_variant_png(batch_dir, r)` — a region's **committed variant / override art**, never the sheet page. So what it alpha-cropped is reprocessed art carrying a genuine transparent margin the page does not have; the `rembg` cutout that runs on every generated region (default ON — "Keep ON for game icons/symbols") is the obvious producer, though which step made it is inference, not measurement. That means the primary fault here is **trim applied and never recorded**, at the packer, on freshly generated art — the exact inverse of `video_to_clip`'s stated rule ("Trim is recorded, never just applied"). The packer cuts each frame down to its own ink and writes nothing down, and nothing downstream can tell that from art that was never trimmed.
  - **The inherited-trim loss is real, and is a SECOND defect rather than this one's cause.** `auto_pack_layout` clears `_REPACK_CLEARED_KEYS` off every region it places, trim included, on the reasoning that a rect it just re-derived makes the old trim stale. That is right about the RECT and **wrong about the CANVAS**: `origW/origH` does not describe the old placement, it describes what the source art was drawn on, and repacking does not change that. On this sheet the inherited record happened to be a no-op (`0,0,320,320`) describing art that had since been replaced, so preserving it blindly would have been a *different* wrong answer — which is why the carry below is gated on the art still being the crop the record was written for.
  - **Two commits made it live the same day, in a way neither one could see.** #682 stopped the Flipbook sheet declaring `layout:"pack"` — but only for sheets written after it; `S_New_Squid_Idle` already carried the declaration, so every Create Atlas still re-packed it. #683 then taught the clear the camelCase spelling `video_to_clip` actually emits, which is what turned "a survivor the tool ignored" into "no trim at all". Each change was correct on its own terms; the data loss is in their composition, on a manifest written before either.
  - **The fix.** `_carried_trim` re-derives the record against the new crop instead of dropping it: the bound art IS the crop the old record describes, so the original canvas carries over unchanged and the offset just moves further inside it — written back in **whichever spelling the manifest already uses** (converting is the same loss with extra steps; the launcher's `parseRegions` reads only camelCase). It **only preserves, never invents**: a region that recorded no trim still gets none, so every atlas packed before today re-packs byte-identically. And it is gated on the art still BEING that crop (file size == the rect the record was written for), so a regenerated region drops a canvas that now describes nothing, exactly as before.
  - **The option the owner asked for.** `atlas.pack_trim` — **Frame trim** in 🧩 Atlas settings, a two-choice dropdown, absent/unknown normalising to `alpha` = today's behaviour. `keep` packs each region at its FULL canvas, so frames authored on one canvas share one centre with no trim record needed. It also stamps `fit_mode:"contain"` on every region it places, which is load-bearing rather than cosmetic: without it `fit_to_region` takes its legacy path, alpha-crops the art and re-centres it in the rect — putting the per-frame centre straight back, at a uniform size. Explicit `contain` routes to `_packer_compose_tile`, and rect == image size makes that a pass-through. Conversely no `fit_mode` is stamped in `alpha` mode, where explicit `contain` would shrink the whole canvas into the bbox-sized rect.
  - **`video_to_clip` deliberately does NOT get the flag.** It already records its trim, so its output is correct and the loss was never there; untrimmed frames would multiply its page area (25 × 320² ≈ 2.6 MP of ink → 2.6 MP of canvas) and push clips past `PAGE_MAX` into multi-page splits; and the switch would have to be plumbed through the launcher's Flipbook video UI, a different service, to fix a bug that is not in this one.
  - **The deploy stops inventing a canvas in silence.** `_pick("orig_w","origW", default=rw)` is reasonable for genuinely untrimmed art and indistinguishable from data loss — so the two are told apart by the one signal the data still carries: genuinely untrimmed frames are **uniformly sized**, because nothing was cut off them. A `pack` manifest whose framed regions carry no trim at all **and** have more than one distinct size is now named in the deploy note, with the symptom and both remedies.
  - **`S_New_Squid_Idle` is NOT healable in place. Tested, not assumed — and no migration is available.** The one object that could have held the answer survives and does not hold it: `sheets/S_New_Squid_Idle/S_New_Squid_Idle.json` records an **untrimmed** `320×320` for all 25 frames, so it gives the canvas but says nothing about where the current `269×287`-class crop sits inside it. Copying `sourceSize`/`spriteSourceSize` across therefore heals nothing (and a same-crop gate correctly refuses all 25: the manifest says `269×287`, the descriptor says `320×320`). The offsets are not re-derivable from the page either — recomputing every cell's alpha bbox with the packer's own rule matched **0 of 25**, because the page cells are fully opaque. The art that WAS trimmed is each region's committed variant, and what was cut off it was never written down anywhere. **Re-running the Flipbook video session is the only route**, and it produces a sheet that no longer declares `pack` (#682) and whose trim would now survive a re-pack anyway.
  - **What the `keep` option can and cannot do, stated the same way.** It keeps whatever canvas each region's **committed art** is on. On the normal forward path that is the generated/cut-out variant, and one atlas rendered with one set of settings produces one canvas — so `keep` does deliver a shared centre, and preserves where the subject sits on that canvas (compose pastes verbatim). It does **not** give back a canvas the art was already cropped to, and it does not reconcile variants that disagree (a region re-rendered after a gen-size change, `gpt_image_size: match_ref`, a hand-uploaded image). The pack note now says so out loud when it packed more than one canvas size, rather than printing "one common centre" over frames that still do not share one.
  - **Verified:** `test_pack_trim.py` — **39** offline assertions (no R2/GPU/ComfyUI) driving the real `auto_pack_layout`, the real `_normalize_converted_region` and the real `batch_atlas.fit_to_region`. Pins the invariant (every region's recorded canvas identical before/after a re-pack, both spellings), that it reaches the deploy normalizer as ONE shared `sourceSize`, that the default invents nothing, that regenerated art still drops a stale canvas, the reported symptom reproduced as a control (3 frames → 3 distinct canvases) and removed by `keep` (3 frames → 1), the composed tile byte-identical to the source in both modes, and both halves of the mixed-canvas warning (named when `keep` packed two canvas sizes, silent when it did not). Whole suite green — `test_pack_page_pointer` 31, `test_stale_pack_rects` 67, `test_auto_pack_clear` 10, `test_render_slot` 73, `test_page_js` 20; `test_config_defaults` (ComfyUI unreachable) and `test_shared_taxonomy` (PyYAML) fail on `origin/main` already. **Not verified: no live re-pack or deploy has run through this.**
  - **One pre-existing red test resolved on the way.** `test_auto_pack_clear.py` still called `auto_pack_layout` as if it returned a bare note (#684 made it `(note, changed)`) and still asserted that an unplaced region keeps its trim, which #684 deliberately reversed. Both corrected; the unplaced assertion now follows `test_stale_pack_rects.py` rather than contradicting it.
- 2026-09-16 — **One key list for the re-pack's clear, because two concurrent fixes left half of it behind.** #683 and #684 landed the same day, both editing `auto_pack_layout`, and git merged them cleanly — every test green, nothing broken live. But they split one clear in two: a region the packer PLACED had `_REPACK_CLEARED_KEYS` popped (both spellings, after #683), while a region it did NOT place had `_PACK_GEOM_KEYS` popped (snake_case only, from #684), so an unplaced region kept `offX/offY/origW/origH`. Dormant — an unplaced region is neither composed nor framed, and it self-heals the moment it gets art and is re-packed — but it is precisely the trap #683 was written to close: a surviving `orig_*` is live INPUT, not a dead field, and its mere presence is what `fit_to_region` reads as `spine_slot`, flipping placement from `contain` to `fill`. `_PACK_GEOM_KEYS` is now **built from** `_REPACK_CLEARED_KEYS` (plus the rect and the legacy `rotate`), so the halves cannot drift again. **Worth recording as a merge hazard, not just a typo:** neither PR was wrong, neither conflicted, and CI passed on both — the defect existed only in their composition, which is the failure mode concurrent work on one function produces and no single review sees. Pinned by two assertions in `test_stale_pack_rects.py` (**67** now): the superset relation itself, so the guard survives the next key being added to either tuple, and the behavioural half — an unplaced region carrying the camelCase spelling `video_to_clip._build_manifest` really emits loses it. Both proved by restoring the divergence: 3 assertions fail.
- 2026-09-16 — **A re-packed atlas no longer deploys a frame for art it doesn't hold.** The other half of the fault in the entry below (#682): that one fixed the page a re-packed atlas DECLARES, this one fixes the frames it SHIPS. Deliberately kept separate — same day, same function, different remedy.
  - **The fault.** `auto_pack_layout` re-runs the MaxRects packer from scratch on every Create Atlas: it measures each region's committed art, packs it into a **freshly-sized** page, and stamps new `x/y/w/h` onto everything it placed. A region with nothing to measure — added but not generated yet, or its variant file gone — was left UNPLACED, which in practice meant *left holding the rect the previous packing gave it* while every other region moved and the page changed size underneath it.
  - **Why it shipped silently.** Nothing downstream could tell that rect from a live one. `_deployatlas`'s manifest-regions fallback emits a TexturePacker frame for **any** region with a complete rect, so the stale one travelled into the deployed `.json` as a well-formed frame addressing arbitrary pixels of the new page. The sheet looked right, the descriptor parsed, and the affected symbol just showed some other symbol's art. Verified end to end: a 2-region atlas packs to 204×160; take the big region's art away and it re-packs to **68×68**, where the old `(0,0,200×90)` rect is mostly off-page.
  - **The remedy, and why it is this one rather than a refusal.** On a `pack` layout the geometry is **derived output**, not authored data — so anything the packer did not just place is stripped of it (`_PACK_GEOM_KEYS`, which takes the trim keys too: `off_x`/`orig_w`/`fit_mode`/`bounds` describe the same dead placement and would let a later import read the region as pre-trimmed). The deploy then emits **no frame** for it, and the game fails to find one — loud, and traceable. Refusing the compose outright was rejected because composing before every region is generated is the documented way to grow a from-scratch atlas; placing them from source art is impossible, since the absence of art is the whole condition.
  - **Stripping a rect opened a worse hole than it closed, until compose was taught to skip.** `batch_atlas.region_box` answers `(0, 0, whole page)` for a region with no rect — deliberate, for a cell-grid atlas's single full-page image. But the strip and the compose subprocess are separated by a process launch, so a render committing that region's variant in the window left compose with **art and no rect**: it would paint one symbol across the entire sheet and destroy every other region's pixels. Compose now skips an unplaced region on a `pack` layout (only there — the fallback is authored intent everywhere else). `slice_atlas` got the same guard, where it would otherwise have bound a whole-page crop as the region's ref, and `_parity_of` now returns a verdict instead of raising `int(None)` into the inspector (`batch_atlas.ATLAS_META` is never populated in the ui_server process).
  - **But dropping a symbol is not allowed to be silent either.** The pack note now names every region that *lost* a rect (as distinct from one that never had one — that is the ordinary growth path and reporting it would cry wolf), and the deploy note names everything it left out of the frame map. The pre-existing `continue  # a region without a complete rect can't be a frame` was a silent drop; it now reports.
  - **Where the strip must NOT run — and the gate that had to change with it.** Measuring *nothing at all* is equally the signature of a **failed staging hydration** (`cloud_paths.ensure_lazy` swallows every R2 error and leaves `batch/` as it found it), so clearing every rect there — and mirroring that manifest back to R2 — would let a network blip rewrite the atlas. The strip runs only once at least one region has measured, which is the proof hydration worked; the other case leaves the geometry alone and says *do not deploy*. That page is caught at the far end instead: a composed page with **no ink anywhere** refuses to write a frame map at all. The caller's save gate was `not note.startswith("⚠")` — inferring "did it mutate?" from the shape of a human-readable note. `auto_pack_layout` returns `(note, changed)` and the caller saves on the signal.
  - **A deploy-side guard, because every manifest packed before today is already in the bad state.** The fallback refuses any frame whose rect leaves the **real, measured** page — ground truth, so a stale `atlas.width` can't condemn regions genuinely on the sheet — and says which ones and why. Rotated regions are checked at their true `(h × w)` page footprint: `write_texturepacker_json` never swaps w/h, so testing `(w × h)` would both pass rects that overrun the edge and fail rects that fit. A manifest whose declared page size disagrees with the page actually shipped is also now called out rather than baked into `meta.size` unremarked.
  - **The deployed `.json` is not the only consumer of these rects.** The launcher's `backfillMissingGeometry` ([editorRegions.ts](../../apps/launcher-api/src/lib/server/editorRegions.ts)) treats a manifest's `atlas.texturepacker_json` as authoritative and will re-add a rect to a region that has none. Stripping here does not reopen that: the deployed descriptor no longer carries the frame, and the entry below (#682, merged the same day) pops the stale `texturepacker_json` on every re-pack — so there is no descriptor left to hand a rect back from. The two are the same defect seen from opposite ends, and both are needed.
  - **And the partial version of the same hole, raised in review of this change.** The blank-page refusal above only catches a TOTAL failure; the common shape is *some* regions painted. That comes in by a door this change did not open and does not close: compose's own pre-existing `no generated variant found` skip drops a region that still holds a rect, when its art goes away between Create Atlas measuring it and the subprocess — leaving a hole under a live rect, i.e. a frame the game resolves to nothing. The deploy now tests each frame's rect against the page's actual alpha (`_rect_has_ink`, same `(h × w)` footprint rule) and drops it on a `pack` layout, where a rect is only ever stamped after measuring non-empty art so a blank one is definitionally a fault. A bound `.atlas` is authored elsewhere and may legitimately carry an empty slot, so there it is reported, not dropped. **This is deliberately checked at the pixels rather than by plumbing compose's skip list out of the subprocess:** it catches every cause, including a manifest that never re-composed.
  - **A dropped frame is attributable at both ends — checked, not assumed.** Raised in review: the Flipbook clips page does author-time dangling detection *precisely* because a shortened animation looks plausible, and a frame dropped at DEPLOY lands after that check has passed. The suggested remedy was for the deploy to cross-reference the launcher's clip docs by name. **Declined, because the gap is already covered at the far end:** `Flipbook.svelte` `console.error`s `Flipbook "<id>": N frame(s) missing from loadedAssets — the animation will play SHORT`, naming the clip and the frames, and its comment says it exists so "a game that shipped past that gate still says so". Cross-referencing would couple this Python tool to the launcher's clip format to bridge a gap both ends report. What was missing was the *connection*, so the deploy note now names the symptom ("a flipbook playing SHORT, or a symbol rendering nothing — the console names the clip"). Both halves are pinned in one test so neither is removed believing the other covers it.
  - **No alpha threshold in the ink test, deliberately.** Strict `> 0` — a single pixel at alpha 1 counts. Any tolerance would drop real art at exactly the frames that have least of it: the tail of a fade-out, a soft glow FX layer, a low-alpha shadow. Those measure non-empty at pack time (so they carry a rect) and would read as blank at deploy under a threshold. Pinned, along with the converse: the WebP page's coloured-but-fully-transparent gutter must not read as art.
  - **Verified — the whole record, in one place.** `test_stale_pack_rects.py`, **67** offline assertions, driving the real packer, the real frame writer and the **shipped** predicates (`_rect_on_page` / `_rect_has_ink` are module-level precisely so the tests cannot pass against a copy). Covers: the `rotated_regions` bucket (silently dropped by this same file before); the rotated `(h × w)` footprint for BOTH guards; degenerate rects; the hydration-failure branch; the compose skip and the disjointness that makes it safe (what compose skips is exactly what gets no frame, so it can never orphan a descriptor); the strict `> 0` ink test with no threshold (a fade-tail or soft-glow frame must not be dropped) and its converse (the WebP gutter must not read as art); the clear's two halves as a superset relation; and a non-pack manifest left completely alone.
    - **Every guard was proved against the behaviour it replaces,** not just asserted: neutering the strip fails 2, restoring the pre-merge key-list divergence fails 3, reinjecting the old bounds copy fails the rotation cases.
    - **Two end-to-end runs through the REAL compose subprocess:** a re-pack shrinking 204×160 → 68×68 drops the stale rect and deploys only the live frame; and the partial-hole case — art removed after its rect was stamped — composes `1/2`, catching the blank rect while the inked one ships.
    - **Re-run against MERGED `main`, not just the branch** — which is the only reason the #686 defect was found at all: three sessions edited `auto_pack_layout` in a few hours, every branch was green, and the fault existed only in the composition. All eight suites green on `main` at `aa87c04a` (stale-pack-rects 67, pack-page-pointer 31, fx-rebuild 23, page-JS 20, variant-pick 44, render-slot 73, image-delivery, sheet-handoff), plus an R2-less boot at 187 KB and both end-to-end runs repeated there.
    - **Shipped:** #684 (`e9041792`) and #686 (`aa87c04a`) are on `main`; Railway auto-deploys from it.
    - **NOT verified, and nothing above substitutes for it: no live deploy has run through any of this.** Every run is offline — real packer, real compose subprocess, real frame writer, but a fake bucket and no GPU. The check that is owed is concrete: on a from-scratch atlas, generate two regions, Create Atlas, deploy, then delete one region's variant, Create Atlas again and deploy again — the second deployed `.json` must have a frame for the surviving region ONLY, its `meta.size` must equal the new page's real pixel size, and the deploy note must name the dropped one. Until someone runs that, this is verified logic, not verified behaviour.
- 2026-09-16 — **A `pack` atlas no longer invalidates its own declared page: the re-pack now names the page it just composed, and mirrors it to R2.** Companion to the launcher-side fix in [editor status](editor.md) (same day) — that entry owns the guard's evidence, the live R2 numbers and the `isDeployedPageStale` rule; this one owns the producer side, which is where the mismatch was actually manufactured.
  - **The self-invalidating shape.** `atlas.layout:"pack"` means THIS tool owns the layout: `auto_pack_layout` re-measures every region's art and re-derives `x/y/w/h` + `atlas.width/height` on every **Create Atlas**. But `atlas.source_image_path` kept naming a page nobody re-packs — a Flipbook video sheet under `sheets/`, an import under `input/refs/atlas/`. **`sheets/` is read-only to this tool**: `video_to_clip` writes that page once and never again, so the first re-pack left the manifest declaring rects from one producer and a page from another. Nothing errors; the launcher crops the fresh rects out of a differently-sized page and `/editor`, `/symbols` and `/flipbook` all slice the wrong pixels.
  - **How a Flipbook sheet got re-packed at all:** activating one in the Session dropdown fires the `export_prefix`-gated ref auto-seed, which binds every region's slice of the page as an `output_override`. `auto_pack_layout` then has art to measure, so the next Create Atlas really does re-pack it.
  - **What changed (producer side).** After the compose subprocess, `publish_pack_page` (`ui_server.py`) mirrors the composed page to `<C>/<P>/atlas/<stem>_new.webp` (the `.png` only when the WEBP encode failed — WebP is what Deploy prefers) and only THEN rewrites `atlas.source_image` / `source_image_path` to that key. `auto_pack_layout` additionally drops `atlas.texturepacker_json`: it describes the page just superseded, and the launcher's `backfillMissingGeometry` treats it as authoritative — left in place it silently restores the OLD packing on top of the NEW page. `export_prefix` is deliberately kept (true provenance, and it gates the seed above). `batch_atlas.source_image_candidates` learned `atlas/` as a page location (by EXACT key, like `sheets/`): it had never been one, so the by-basename fallback — which scans only `refs/` and `sheets/` — would have failed to find the page Slice and the overlay inspector now ask for.
  - **Ordering, stated honestly.** The page does not exist when `auto_pack_layout` runs — it is what compose is about to make — and its extension is unknown until the WEBP encode succeeds or doesn't. So the fields are written in a post-hook, after the bytes are in R2, never before: **there is no moment where the manifest names a key that is not in the bucket.** A page older than the compose start is refused as a leftover rather than published under the new rects, and a failed PUT leaves the previous pointer standing with a `⚠` in the log. What remains is a window *during* the compose, where the manifest carries new rects and its previous pointer. That window is structural (compose must read the geometry it is composing, so the geometry is saved first) and is not a regression — before this, that mismatched state was the **permanent** one.
  - **The other half: `video_to_clip._build_manifest` stops declaring `layout:"pack"`.** It was the only manifest shape in the codebase that declared `pack` *and* pinned a fixed page. Of the two halves, `pack` was the wrong one: the layout is owned by the Flipbook's packer, the rects carry a real logical frame trim (`offX/offY/origW/origH`) a re-pack discards, and `texturepacker_json` is that packer's authoritative description of exactly that page. Dropping the declaration makes `auto_pack_layout` return `None` for these sheets, so page + rects + descriptor stay one consistent set — which is what the manifest's own `_comment` already promised ("re-running the video session replaces it"). **Cost:** a Flipbook sheet opened in the Atlas Maker can no longer be re-laid-out by Create Atlas (Create Atlas still recomposes it onto its declared page), and its cards lose the per-region 🗑 (gated on `layout=="pack"`). For a Flipbook sheet that is never opened here — the normal case — **nothing changes at all**; it keeps pointing at `sheets/…` exactly as before. One line in [flipbook status](flipbook.md).
  - **Launcher side, one caveat fixed:** `findDeployedPage` built its stem set from names that never match what Deploy WRITES. Deploy lands at `deploy/<path>/<out_base>.<ext>` where `out_base` is `deploy_basename` or the manifest stem minus `atlas_manifest_`; a pack atlas's page is `<stem>_new.webp` and its manifest key is `atlas_manifest_<stem>.json`. So a from-scratch atlas with no `deploy_basename` matched **nothing** under `deploy/` and could never show its deployed page. Both derived names are now added alongside the literal stems.
  - **Cost of the durability half:** one extra R2 PUT of the composed page per **Create Atlas** (the page was previously staging-only and lost on container swap), and a correspondingly slower cold hydrate for pack-heavy projects — `atlas/` is pulled in the background at startup, so it delays the first page render, not boot.
  - **Pinned by** `services/atlas-tool/test_pack_page_pointer.py` (31 assertions, offline — a fake bucket, a stand-in compose): the declared page's real pixel size equals the packed page size; the key it names IS in the bucket; the descriptor is dropped and `export_prefix` isn't; WebP preferred, PNG fallback, 0-byte WEBP refused; a failed mirror and a leftover page both leave the old pointer standing; a non-`pack` manifest comes out byte-for-byte unchanged; and the Flipbook shape no longer declares `pack`. `_sheet_manifest_for`'s page-basename join is unaffected (no Sheet-Maker manifest names a `_new.webp` page, and a re-pack pops `fit_mode` anyway). **Deployed 2026-09-16** — both services rolled out on `ed7bf11a` (which carries this commit): Railway's commit statuses read `Success - atlas-tool-production.up.railway.app` and `Success - app.invisiblewall.org`, the other three services correctly `No deployment needed`. ⏳ **Still not live-verified** — the deploys were the blocker, the *exercise* is what remains: a **Create Atlas on the same sheet**, then re-check that `/editor`, `/symbols` and `/flipbook` draw the new art. Deploying proves the code is running, not that the flow is fixed. Note the change does not retro-fix a manifest already in R2; the next Create Atlas heals it.
  - **Follow-up, same day: the re-pack's "drop the stale trim" now actually drops it.** `auto_pack_layout` pops the trim fields because the rect it stamps IS the frame, so an inherited `off_*`/`orig_*` describes a frame that no longer exists — but it popped only the snake_case spelling this tool writes, and camelCase (`offX/offY/origW/origH`) is a real producer spelling: `video_to_clip._build_manifest` emits it deliberately, because the launcher's `parseRegions` reads only camelCase. The pop list therefore never matched, and the re-pack kept fields it believed it had cleared. **Dropping `layout:"pack"` above made this unreachable from the one producer that could hit it** — that is exactly why it is fixed now rather than left: it went from a silent live no-op to a latent one, and the next producer to declare `pack` alongside camelCase trim would have paid for it with nothing to read. The clear now covers both spellings (`_REPACK_CLEARED_KEYS`). **Producers were deliberately not touched** — the `RawRegion` comment in `apps/launcher-api/src/lib/server/editorRegions.ts` records why teaching the launcher snake_case is a versioned migration, not a parser tweak (`off_y` needs a Y-axis conversion; emitting trim re-bases the space every authored `.irig` is frozen in — tried in 660c424, reverted). Note this also makes the sentence above literally true: before it, a re-pack did NOT discard a Flipbook sheet's `offX/offY/origW/origH`. A retained `orig_*` is live data rather than a dead field — both readers on this side (`_normalize_converted_region`, `_deployatlas`'s manifest-regions fallback) take either spelling, and its mere PRESENCE is what `fit_to_region` reads as `spine_slot`, flipping placement from `contain` to `fill`. **Shipped** as `ed7bf11a` (#683) and deployed the same day with the entry above — though nothing observable changed by construction, which is the point. **Pinned by** `test_auto_pack_clear.py` (10 assertions, offline — an alpha-trimmed PNG bound as `output_override`): the re-rect, both spellings cleared, the clear observed through the camel-tolerant reader, and a not-yet-generated region left untouched. Mutation-verified — pinning the old snake-only list leaves all four camelCase fields behind. `test_pack_page_pointer.py`'s "the camelCase frame trim a re-pack would have discarded survives" still holds: a Flipbook sheet no longer declares `pack`, so `auto_pack_layout` returns `None` for it and never reaches the clear.
- 2026-09-16 — **🏷 Taxonomy: one semantic taxonomy, edited here, injected into every render.** The ComfyUI pack gained a `taxonomy_yaml` input (the YAML itself, because the production target is a serverless worker with no durable filesystem — see [status/comfyui](comfyui.md)). That made a custom taxonomy possible in production but not *editable*: the text had to be baked into each blueprint as a param default, so a one-word vocabulary fix meant re-publishing every blueprint that used it, and with the `clip` analyzer the taxonomy's keywords ARE the candidate labels, so it is the whole quality knob.
  - **Stored once** at `_shared/semantic/taxonomy.yaml`, beside the blueprints it pairs with. Scoped shared rather than per-project (owner's call) because blueprints are already global — a per-project taxonomy would make one shared blueprint classify differently depending on which project ran it.
  - **Injected at render time** into any node declaring the input, after params so an explicit taxonomy (or a param the artist cleared) still wins. The class check reads `/object_info` via `comfy_specs`; on the serverless transport there is nothing live to ask, and the fallback is the right one rather than a weakness — fill the field only where the GRAPH itself names it, since an API export carries every widget the node had when it was exported, so the field's presence is the record of whether that pack knew about it. Never fails a render: a missing or unreadable taxonomy degrades to the bundled vocabulary.
  - **Save validates, because nothing downstream does.** The node falls back to a much smaller built-in vocabulary and carries on, so an unchecked save fails silently at render time. The editor refuses YAML that would not load, names the reason, and reports what it stored. Gated on `can_publish` like blueprint upload — this one file changes what every render classifies against.
  - **Compare-and-swap on save** (`if_match` with the ETag from the read): the second of two concurrent edits is refused, not silently applied over the first, and the modal says so without reloading over the editor's own text.
  - **PyYAML was NOT a dependency of this service** — caught on a local boot, where every save came back "PyYAML is unavailable, so the taxonomy cannot be checked". Added to `requirements.txt`. Without it the validator would have refused every save in production while passing its own tests, since the interpreter the tests run on happens to ship yaml.
  - **Verified:** 31 offline assertions (`test_shared_taxonomy.py`, R2 and the contract lookup both stubbed) covering validation, the load/save round trip, a refused invalid save, a stale compare-and-swap, and all six injection rules; plus an R2-less boot of the real server — the page renders at 187 KB with the modal, button and both endpoints present, the f-string braces unescape correctly (zero `{{` left), and both endpoints answer JSON rather than a 500 with no R2 creds at all. **Not verified:** no render has actually consumed an injected taxonomy.
- 2026-09-15 — **A derived blueprint's models can be adopted from ComfyUI-Manager's own catalog, and the page templates got a source-level escape guard.** Both were salvaged off `atlas-tool/refresh-stale-config-globals`, the branch behind PR #630 — opened 2026-09-09 11:09Z, closed 11:15Z, never merged, then left 54 commits behind `main`.
  - **Most of that branch was already here by another route, and finding out is the point.** Rebased commit-by-commit rather than copied: the unverified-model remedy dropped as *"patch contents already upstream"*; the authoring runbook was present line-for-line in `docs/tools/atlas-maker.md`; and the blueprint digest **plus** the modal-scroll/escape fix had both shipped in #620. With every conflict resolved in main's favour, `ui_server.py` came out **byte-identical to `main`**. Worth recording because the reflex on a stale branch is to copy the files across, and here that would have reverted ~500 lines of other people's `ui_server.py` (the 2026-09-14 FX-rebuild discipline among them) to win back nothing. What was actually missing was the **uncommitted working tree**, not the commits.
  - **`enrich_from_catalog` — a DERIVED declaration carries no `url`/`base`,** so every model on an uploaded blueprint reached `prepare_blueprint_models` un-installable and auto-install could never fire; the author got a manual checklist for files Manager already knows how to fetch. It now asks the target's own `/externalmodel/getlist?mode=cache` — the same JSON `check_whitelist_for_model` consults, so a row matched there is a row that gate accepts — in ONE read, and only when something actually needs it. Three refusals, all about where the bytes land: a row whose install dir carries a **subfolder** is rejected even on the right branch (`controlnet/SDXL` writes `models/controlnet/SDXL/x.safetensors`, which ComfyUI enumerates as `SDXL/x.safetensors`, so the graph's bare name still would not resolve and the download would only have moved the failure later); a row on a folder that is not ours nor one of ComfyUI's own aliases (`unet`/`diffusion_models`, `clip`/`text_encoders`, `controlnet`/`t2i_adapter`) is rejected; and candidates disagreeing on `(save_path, base, url)` adopt **nothing**, because 20 rows are named `diffusion_pytorch_model.safetensors`. `_install_save_path` keeps Manager's spelling for an adopted row (`catalog_save_path`) apart from ours — the whitelist compares Manager's, while `model_mirror.resolve` builds `comfyui-models/<save_path>/<filename>` out of ours and deliberately carries the legacy aliases, so collapsing them would break the mirror for exactly the models enrichment just made installable. `test_blueprint_models.py` 102 → **129** assertions, still fully offline.
  - **`test_page_js.py` absorbed the salvaged source-level guard — one suite, not two.** It now checks in three layers: a **source scan** reading `ui_server.py` as TEXT that fails on any odd-length backslash run Python would consume, naming `file:line` across all five module-level templates *before* anything renders (the only layer that reports the offending line); the **real render**, `PAGE.format()` exactly as the server calls it, through `node --check`; and **every other script-carrying template**. A missing `node` now FAILS loudly rather than skipping quietly, because a silent pass is the one outcome this suite exists to prevent. 7 → **20** assertions, and `test_page_template.py` is gone.
    - **Folding it in widened the coverage by accident, which is the argument for folding.** Reading the templates off the IMPORTED MODULE instead of via `ast` picked up **`SPLASH`** — assembled at import by `splash_html(...)`, so it is not a literal assignment and the `ast` scan could not see it at all. It had never been syntax-checked by anything.
    - **And it exposed a trap in the harness itself.** A literal `NAME = """…"""` template is a `.format()` source, so its `{slot}` must be filled and its `{{`/`}}` collapsed — but an already-assembled string is FINAL and its braces are real JavaScript. Substituting blindly rewrote `SPLASH`'s destructuring `const {…}=gen.next()` into `const null=gen.next()` and failed a page that was perfectly fine. The two kinds are now told apart by whether the name is a literal template in the source. Worth knowing before adding a sixth template: **a computed one must be parsed as-is.**
    - **Proved by reinjecting the original 2026-09-08 tooltip:** **4** assertions fail — the source scan, the pinned digest-tooltip line, `node --check` on `PAGE` (*"SyntaxError: Invalid or unexpected token"*), and the unterminated-assignment shape check — while `py_compile` still passes, which is exactly why this class shipped twice. The tooltip assertion pins the **property** (that line carries nothing Python would eat), not the remedy: as salvaged it demanded the escape be **doubled**, and so failed against the fix that actually shipped, which removed the backslash entirely.
  - Verified on an R2-less local boot against a fake ComfyUI: the real page (`/?fast=1` — bare `/` serves the CRT splash and proves nothing) renders at 183KB, all 4 inline `<script>` blocks parse, **1,155 functions defined on `window`** with `renderSel` among them (the one that vanished in the outage), zero console errors, and `⛭ Manage blueprints` opens with `.modalbody` at `overflow:auto; min-height:0`. Suites green: page JS 20, blueprint models 129, blueprint derive 82, blueprint publish, blueprint roles, atlas style gate, comfy catalog 102, fx rebuild window 23, image delivery, model mirror 126, push models 54, render slot 73, sheet handoff, variant pick 44, video runner, video to clip — and `test_config_defaults` **162 passed** once a reachable ComfyUI exists (it is the one suite that needs one; it fails `ComfyUnreachable` without).
- 2026-09-14 — **A save made while an FX rebuild was running came back undone.** `run_render`'s post-hook and `run_compose` both did `m = load_manifest()` → `rebuild_fx_layers(m, …)` — **seconds** of PIL per layer plus an R2 upload — → `save_manifest(m)`. That last write put back the WHOLE manifest as it looked before the rebuild, so anything saved meanwhile (prompt, seed, lock, variant pick) silently reverted. No error, no log line: the edit just reappeared as it had been. It stayed survivable only while saves were rare — committing a variant pick on click (same day, see below) made it frequent, which is what turned a latent race into a reported one.
  - **The fix is not a bigger lock.** Holding `_manifest_lock` across the rebuild would freeze every save in the UI for as long as the GPU art takes to post-process — its own outage. The slow work still runs on a detached copy with no lock held; then `persist_region_fields()` re-applies **only** the fields the job owns onto a manifest re-read **inside** the lock. Everything else on that region, and every other region, is whatever the last real save left. Four call sites go through it: the render post-hook, compose, the manual ⚙ FX build (`_FX_RESULT_KEYS` = `output_override`/`mode`/`fx`/`shine`), and the manifest-activation ref seed — both the Session dropdown and the deep-link twin — which owns `output_override` alone (`_SEED_RESULT_KEYS`) and had the identical unlocked read→slow→whole-snapshot-write shape.
  - **Whoever re-tuned a layer mid-rebuild wins, and is told so.** Each region carries a fingerprint of those same four fields taken before the build; if it changed under us the result in hand was computed from inputs the user has already replaced, so it is dropped and named in the render log (`Kept your own newer build of N FX layer(s)`). The manual build says it outright rather than reporting a success it did not write.
  - **The two neighbouring cycles in `run_compose` got the same discipline.** `repair_sheet_fit_mode` and `auto_pack_layout` now run read→mutate→write **inside** the lock — neither is instant (the first globs and parses every `atlas_manifest_*.json` in `MANIFEST_DIR`; the second opens every region's art) but both are bounded local-disk work on files compose is about to read anyway, and both MUST be atomic: they rewrite placement and geometry. `auto_pack_layout` no longer calls `save_manifest` from within — the CALLER saves, same contract as `rebuild_fx_layers`, which is what let its cycle be closed by the lock at all; its two ⚠ failure notes stamp nothing, so they no longer trigger a pointless R2 push. *(Superseded 2026-09-16: one of those notes CAN now stamp — sniffing the note for a ⚠ is no longer the save gate; see the top entry.)* **Note the lock is not network-free** — `_write_manifest_at` mirrors to R2 inside it, as `save_manifest` always did, which is precisely why the SLOW producers re-apply their fields instead of being wrapped.
  - **One manifest per job, pinned thread-locally.** Naming the path in each read/write was not enough: `all_regions()` resolves a manifest's bound `.atlas` through `manifest_path()`, so a dropdown change mid-job would merge ANOTHER atlas's geometry onto these regions — and region names (`H1`, `L1`, …) collide across atlases, so nothing looks wrong until the wrong art ships. `pinned_manifest(mp)` sets the active manifest for the calling thread only (the same `threading.local` idiom `cloud_paths` uses for (client, project), and for the same reason), and compose, the render post-hook and both activation-seed paths now run inside it. Never make that a module global — that is the race `cloud_paths.py` exists to have removed.
  - **The skip message says what is actually true.** A skipped layer keeps the MANIFEST the concurrent writer left; it does not undo the PNG both builds wrote to `refs/useroutput_<name>.png` and mirrored to R2, so the tile on disk is whichever finished last. The note names the layers and says to press ⚙ build again if one looks wrong. The manual build's warning now leads with `⚠` — `flashDiag` only promotes a message whose FIRST character is a severity glyph, and `fxBuild` reloads the page 800 ms later when it is not, which had been wiping the one message that mattered.
  - Proven by A/B on a live local boot (base region + a real `_glow` layer, save fired during the compose): **pre-fix** the manifest came back `{'name': 'H1', 'prompt': 'a cherry'}` — the prompt reverted and the pick was gone, after the UI had said `Saved (1 prompt change(s), 1 variant pick(s))`; **post-fix** `{'name': 'H1', 'prompt': 'a PLUM saved mid-compose', 'variant': '00002'}`, with the FX layer's `output_override` bound in the same pass. Guarded by `test_fx_rebuild_window.py` (20 assertions), which fails 7 of them against the pre-fix write — including the four that ARE the user-visible symptom.
- 2026-09-14 — **A picked variant was thrown away unless you also locked the slot, so Create Atlas composed the file you had just replaced.** Reported as "if I change my variation my changes do not get saved, and the atlas is generated with the old variation". Three things had grown into one field. `selectVariant()` only set `dataset.variant` on the card and left committing to the next `saveAll()`; `_save` then stored that id **only inside `if e["lock"] and (…)`**, so an unlocked pick — the ordinary case, since nothing in the UI says a pick needs a lock — was dropped on every save; `_pick_variant_png` therefore found no `variant` and fell through to "the most recent file" while the CARD went on showing the pick. Create Atlas composed art the page was not displaying, silently.
  - **The knot:** `variant` was doing double duty as the GPT lock key (gpt_image has no embedded seed, so a stored pick was the only way to say "locked"). Untying it needed an explicit **`lock` flag** on the region, or every saved pick would read back as a lock and stop its slot from ever rendering again. `batch_atlas.region_locked()` is now the single answer to "is this pinned", with the old seed-or-pick inference kept **only** as the fallback for manifests written before the flag existed; `already_generated` and the card's lock checkbox both go through it.
  - **A pick is now its own fact:** saved whether or not the slot is locked, and `Save` says so (`Saved (0 prompt change(s), 1 variant pick(s))` — "0 prompt change(s)" alone had read as "nothing happened", which is exactly what used to happen). Unticking lock keeps the pick and drops the seed.
  - **Card and atlas cannot drift apart again.** One `output_view()` now answers "which file does this region show" for BOTH the page build and `/cardsdata`, so a post-render refresh shows exactly what a reload would: the card used to keep a stale `?id=` after a render, and to caption a picked variant with the LATEST file's seed instead of the picked one's. A render that actually produced new art drops the now-superseded **unlocked** pin (`_drop_superseded_picks`, fed a before-snapshot so a stopped or failed render costs nobody their pick); deleting a picked file drops its pin too, because ids are handed out as "highest existing + 1" and a dangling pin would snap onto the next render's unrelated art.
  - **Three more instances the review caught, each the same loss by another route.** (a) The stub gate for `.atlas`-BOUND manifests listed prompt / seed+lock / negatives / unselected but not the PICK — and such a manifest holds only creative data, so a region driven by the global prefix/suffix alone has no entry while still getting a card from the geometry file. Picking a variant there created no stub and saved nothing. (b) `delVariants()` never refreshed the card, so `dataset.variant` kept the deleted id and the next `saveAll()` wrote the dangling pin straight back, undoing `_drop_dangling_pick`. (c) `saveAll()` treated a non-2xx as success — a refused save was printed in `#stat` as if it were the "Saved" note and flashed the button green. It now throws, and `savedOk()` gates Render / Create Atlas / mode-switch on it: running past a refused save means operating on stale server-side data, which is how a discarded edit becomes a wrong atlas.
  - **Also from the review:** a `lock` tick with nothing to pin (no seed, no pick) is no longer stored — it would have come back ticked beside the very "lock this pick" button `updateLock` draws, claiming a slot is pinned that re-renders every time. `_post` now writes back the manifest it RENDERED by path (the active one can be switched from another tab mid-render, and region names collide across atlases) and re-reads it after the FX rebuild before applying the pick cleanup. A new `_manifest_lock` serialises the fast load→mutate→save cycles, which matter now that a pick commits on click. **Still open:** the FX rebuild inside `_post` holds a manifest copy across seconds of PIL + R2 work and is deliberately NOT under that lock (it would stall every save in the UI) — a save landing in that window is still lost. Pre-existing; see open items.
  - Verified end-to-end on an R2-less local boot: pick variant 2 of 4 unlocked → manifest gets `variant:"00002", lock:false` → survives reload → `Create Atlas` logs `placed H1 <- H1_00002_.png` and the composed page's centre pixel is variant 2's colour (it was variant 4's before); then delete the picked file → the card falls back to the latest and the next save does NOT resurrect the pin; then tick `lock` with an empty seed → comes back unticked. Guarded by `test_variant_pick.py` (44 assertions: the pick round-trip, the atlas-bound stub, the empty lock, pick-is-not-a-lock for sdxl AND gpt_image, the lock still skipping generation, legacy manifests, supersede/stop/other-slot, and the card's output figure).
  - **`test_page_js.py` earned its keep mid-fix.** An editing tool ate the `
` in a new `alert(m+'…')` and wrote a REAL newline into that JS string literal — the same unterminated-literal outage as 2026-09-08, which takes EVERY function on the page with it. `py_compile` passed (the Python is valid); the browser console was the only other witness. The suite caught it by name and line. Nothing to change here — this is the guard working, recorded so the next person knows it fires on breakage that leaves no backslash in the source to grep for.
- 2026-09-09 — **Card thumbnails that never loaded, on art that was really there.** Owner on a ~60-region ship atlas: *"some images do not get loaded, and I am sure they are there and is not a broken link, its just that the page discard them too fast"*. Read literally that is exactly it — the page asked for all of them at once and kept whatever came back broken.
  - **The burst is the bug.** Every card paints TWO `<img>` (output thumb + reference), all eager, so a 60-region atlas fired ~120 image requests in one go. The launcher only *redirects* to the tool (`atlas/+page.server.ts` — no proxy hop), so that burst lands directly on the stdlib `ThreadingHTTPServer` behind Railway's HTTP/2 edge, where the browser has no 6-connection-per-host limit to throttle it. `request_queue_size` was the stdlib default **5**: past the fifth pending connection the OS refuses, and a refused connection IS the broken tile. Same storm the disk thumb cache was added for ("the 502-storm fix", `thumb_bytes`) — that removed the decode cost, not the fan-out.
  - **And a dropped image was permanent.** Nothing retried. An `<img>` that fails once keeps its broken tile for the life of the page, so the only cure was a full reload, which re-ran the same lottery.
  - **Four changes, layered — the first is the guarantee, the rest just make it rare.** The page carries a **retry loader in `<head>`**: capture-phase `error` listener (load/error do not bubble), backoff `400/1200/3000/7000`, and a throwaway `?retry=n` because a failed response can be negatively cached — re-pointing at the same URL is a no-op. It bails if `im.src` moved on while it waited, so a stale retry can never re-assert a previous render over a fresh one; after four refusals the tile gets `.imgfail` + a title saying so, rather than pretending. Cards then ask **lazily** (`loading="lazy" decoding="async"` — `.imgs img` already pins `height:160px`, so there is no layout shift to engineer around), cutting the burst to viewport size. `request_queue_size` **5 → 128**. And `do_GET`/`do_POST` route through **`_dispatch`**, which answers `500` when a handler raises — previously the exception reached socketserver, which closes the socket having written nothing, the one failure that looks identical to a slow network and that no reload fixes. It will not double-send over a reply already on the wire (`_responded`).
  - **Verified against a real browser, not asserted.** The loader was extracted from the REAL served page and run against a harness endpoint that 503s twice then succeeds: 3 requests, `naturalWidth 64` — the tile loads, and the retry counter clears so a later failure gets a fresh budget. A permanently-dead endpoint gives up after 5 attempts with the outline and the explaining title. Served page re-checked with `node --check` (4 inline blocks, all parse) and all 60 card `<img>` carry `loading="lazy"`. **Unproven:** the browser actually deferring the lazy ones — the preview pane would not paint that session (`innerHeight 0`), so lazy is asserted by attribute, not by observed deferral.
  - **New `test_image_delivery.py`** (13 assertions) pins all four, because each is invisible enough to delete by accident: both card thumbs lazy, the loader present *in `<head>`* with its cache-buster and give-up state, a raising handler answering 500 instead of dropping the socket, no double-send, and a backlog above the stdlib default.
- 2026-09-09 — **The Atlas style panel can no longer be silently unsaved across a render.** Reported as *"the general prompt is not getting processed — only the one on the card"*.
  - Not a prompt-plumbing bug: `_resolve_text` composes `prefix, region, suffix` into ONE string on the bound `positive` node, verified against the real `build_workflow_blueprint`. If the binding were wrong the CARD prompt would fail too, which is what ruled it out.
  - **The cause is that `📝 Atlas style` is the one settings panel `collect()` does not carry.** The big **💾 Save changes** sends the region cards; `style.positive_prefix/suffix/negative` persist only through the panel's own **Save atlas style** button and `/saveglobalstyle`. Type a prompt, press the big Save, render — and the render uses the SAVED style while your typing sits on screen looking applied. The panel is a collapsed `<details>`, so there was nothing to see either. Confirmed live: `collect()` output does not contain the style text.
  - Editing any of the three textareas now raises a **● unsaved — this render will NOT use it** badge **on the `<summary>`**, so it is visible while the panel is collapsed — the state worth seeing was the one that could not be seen. `renderSel` asks before starting and offers to save the style first; declining writes **nothing**, not even `saveAll()`, because the gate runs ahead of it. The baseline is re-taken only on a save that landed, and compared `.trim()`-ed because `_saveglobalstyle` strips — otherwise a trailing newline would leave the panel permanently, wrongly dirty.
  - **New `test_atlas_style_gate.py`.** Source-structure assertions, because the behaviour is browser JS with no DOM harness here and what rots silently is the ORDER. Proved against BOTH regression modes: moving the gate after `saveAll()` fails 2 assertions, and neutering it to `if(false)` fails 1 — the latter needed pinning the gate as the **first branch**, since a name-only check keeps passing on the dead body (the post-save re-check still mentions `gstyleDirty()`).
  - Verified live across all three paths: cancel → nothing posted, panel opened, still dirty; accept → `/saveglobalstyle` posted **before** `/render`, then clean; clean → no prompt at all. Suites green: page-JS, atlas-style gate, blueprint publish, blueprint roles, blueprint derive 82, blueprint models 102, comfy catalog 102, model mirror 126, push models 54, render slot 73, sheet handoff, video runner, video to clip.
- 2026-09-09 — **Three silent traps between publishing a blueprint and it actually running.** All three came out of one report: *"I published a background-removal blueprint and my image came back regenerated."* It had, and none of the three said a word.
  - **Publishing does not select.** It only adds to the shared library; the render dispatches on `region_pipeline()` → the atlas's `pipeline` config, whose untouched default is `sdxl` — the built-in TEXT-TO-IMAGE builder. So the blueprint never ran and the region was regenerated from its prompt. The modal now offers **"Use it for this atlas straight away"** (ticked by default) and the server acts on it, because the server is the only side that knows the slugged `bp_id` — the alternative was regexing it back out of the reply text, which stops working the first time someone rewords the message. Unticked, the reply now says the blueprint *will not run* until it is selected, instead of the old neutral "select it in the pipeline dropdown".
  - **The modal auto-created the ref collision its own runbook warns about.** On a single-`LoadImage` graph both `style_ref` and `shape_ref` matched, and the prefill bound *both*. At render time `shape_ref` is applied second and wins, so the node received `normalize_shape_ref`'s grayscale silhouette on a black canvas instead of the artwork. `buildBpBindings` now refuses to prefill a `(node,field)` another role already claimed and says which role it skipped and why. The rule is general, so a one-`CLIPTextEncode` graph no longer prefills `positive` and `negative` onto the same node either. A deliberate collision is still selectable by hand.
  - **`base` reads as a pipeline chooser and is inert.** Traced every read repo-wide: it is validated on publish, stored on the blueprint's `meta`, and **nothing dispatches on it** — yet it offers `sdxl / flux / gpt_image`, the built-in pipeline names, right where an author is deciding what will run. Relabelled **"Base model family (a label — it does NOT choose what runs)"** with a line pointing at ⚙ Settings → Pipeline.
  - **New `test_blueprint_publish.py`** drives the REAL `_uploadblueprint` offline (staging in a temp tree, `storage.put` stubbed — a `put` that raises is a hard publish failure, which is why an R2-less publish cannot be clicked through locally) and asserts the opt-in sets the pipeline, the opt-out leaves it alone, and `base` is not what dispatch reads. **Proved by reverting the `use_for_atlas` branch** — 4 assertions fail — not by construction.
  - Verified live on an R2-less boot with the reported graph shape (`LoadImage → Rembg → SaveImage`): `style_ref` binds alone, `shape_ref` stays `(not used)` with the explanation, the Base label reads as a label, and the opt-in is present and ticked. Suites green: page-JS, blueprint publish, blueprint roles, blueprint derive 82, blueprint models 102, comfy catalog 102, model mirror 126, push models 54, render slot 73, sheet handoff, video runner, video to clip.
- 2026-09-09 — **A processing blueprint can be published: `output` is the only required role.** Reported from the field — a graph with no seed anywhere could not be published, because the modal demanded a `seed` binding the graph had nothing to offer. There was no way forward at all.
  - The gate assumed every blueprint is a GENERATION graph. A **processing** one — upscale, relight, matting — has no sampler and no text encoder, so `seed` and `positive` have nothing to bind. `bpCandidates` correctly returned zero candidates for both, and the publish then refused for exactly that reason.
  - **Nothing downstream ever needed them.** Every role is injected through `batch_atlas._set_node_input`, which is a documented no-op for an absent binding, and `_seed_in_png` already returns `None` for a graph with no KSampler. So the refusal bought nothing and cost a whole class of blueprint. `blueprints.REQUIRED_ROLES` is now **`STRUCTURAL_ROLES = ("output",)`** — `output` names the node the bytes are read from, so it is the one thing a publish cannot do without; `OPTIONAL_ROLES` became `INJECTED_ROLES` and `KNOWN_ROLES` is unchanged in content.
  - **The useful half of the guard was kept, as advice rather than a refusal.** New `bpRoleWarn()` (and `unboundExpected` in the Flipbook twin) fires only when the graph HAS a candidate for a role the author left unbound, and names the cost: *"every region and every new variant comes out identical, because the graph keeps the seed your export baked in."* A role the graph cannot offer is never mentioned — there is nothing the author could do about it. The `*` on a role label follows the same rule, so a processing graph no longer shows a star it cannot satisfy.
  - **Both publishers moved together**, as their own comments ask: the Atlas Maker modal (`BP_ROLES` / `ui_server.py`) and the Flipbook video publisher (`PUBLISH_ROLES` / `VideoMode.svelte`, where `required` became `expected`). Both modal hints and both tool guides said "positive / seed / output are required" and now say what is actually true.
  - **New `test_blueprint_roles.py`** (26 assertions) pins the rule: a seedless, promptless binding set publishes; a missing `output` is still refused; a non-output role still needs its `field`. **Proved by reverting `STRUCTURAL_ROLES` to the old triple** — 8 assertions fail — not by construction.
  - Verified live on an R2-less boot driving the REAL importer with a real processing graph (LoadImage → UpscaleModelLoader → ImageUpscaleWithModel → SaveImage): `positive`/`seed` show 0 candidates, no star, no warning, and `output` auto-binds — the publish proceeds to the permission gate instead of the role gate. The opposite case too: a generation graph auto-binds `seed *`, and clearing it raises the warning while leaving publish enabled. svelte-check on the launcher is unchanged from pristine `main` (66 errors / 24 warnings, zero in `VideoMode.svelte`); eslint clean; page-JS, blueprint derive 82, blueprint models 102, comfy catalog 102, model mirror 126, push models 54, render slot 73, sheet handoff, video runner, video to clip all green. **The Flipbook publisher was type-checked and linted but not clicked through** — it needs the launcher DB + auth.
- 2026-09-09 — **Every modal can scroll, and Publish cannot be pushed off the bottom.** Reported from the field: exposed settings added to an imported blueprint ended up off-screen and unreachable.
  - `.modalbox` caps at `max-height:88vh` and is a flex column, but each body was a flex item at the default `min-height:auto` — which refuses to shrink below its content. So the body did not scroll, it spilled **past** its own box; and because `.modal` is `position:fixed`, nothing could bring the overflow back (page-scrolling moves it 0px). Measured on the live page with 14 exposed settings at 1280×800: **2370px of the form hung below the box, ending 2323px below the viewport.**
  - Shared `.modalbody` (`flex:1 1 auto; min-height:0; overflow:auto`) and `.modalfoot` (pinned actions); `.modalbox` gained `overflow:hidden`, `.modalhdr` `flex:none`. Applied to **all six** modals — New blueprint, Manage blueprints, Advanced, New atlas, Pick a file, Variants. The file picker's magic `max-height:55vh` became the same flex model, so it uses whatever the box actually has rather than a number that can exceed it on a short window. `min-height:0` is the load-bearing half; `overflow:auto` alone already implies it, but it is stated so the next edit does not drop it.
  - **Publish blueprint moved out of the scroll into a pinned footer**, because every `＋ Add` pushed it further down. The footer stays hidden until it has content (an empty action bar under a two-field dialog reads as a rendering bug), which is why every status write goes through `bpStat()` — an invalid-JSON error still shows with Publish still hidden. `＋ Add` scrolls its new row into view: **instant, not smooth**, because a smooth scroll is a silent no-op while the document is hidden (measured: smooth left `scrollTop` at 0, instant reached 2412/2430).
  - Verified on an R2-less local boot driving the REAL importer (a `File` on the input, `change` dispatched), rebased on this branch: 16 adds at 1280×800 — every new row visible, Publish on screen every time, box inside the viewport, 2681px scrollable; and `renderSel` still defined, so the #620 escape fix is intact. `test_page_js.py` (the guard #620 added) passes on the rendered page, plus blueprint derive 82, blueprint models 102, comfy catalog 102, model mirror 126, push models 54, render slot 73, sheet handoff, video runner, video to clip. `test_config_defaults` fails identically on pristine `main` — it needs a reachable ComfyUI.
- 2026-09-08 — **Two diagnostics that lied about which machine they were describing, and the blueprint runbook that came out of finding them.** All three came from one session authoring a FLUX img2img blueprint against a live local ComfyUI.
  - **`resolve_blueprint_workflow` answered from another era's config — and possibly another project's.** `CFG` and every `X = CFG["x"]` global are evaluated ONCE at import. That is right in the render subprocess (imports, runs one job, exits) and wrong in the ui_server worker, which imports once and lives for days — and `_config_paths()` resolves `atlas_config.json` under the ACTIVE (client, project) staging root, so the snapshot is pinned to that worker's boot era AND to whichever project it resolved then. `⤓ Resolved workflow` — whose entire purpose is to state what a real run sends — refused a live `characterdesignertest3` render as *"pipeline 'flux' is a built-in"* and reported the gen size as 1969x1222 while the config said 1024x1024. New `refresh_config_globals()` re-derives the `_SETTINGS_GLOBALS` set from the current config; the resolve calls it inside its EXISTING locked window, **before** `apply_manifest_settings` — refresh stands in for the subprocess's fresh import, then the manifest overlay goes on top; reversed, a per-atlas gen size would be erased by the shared default. Scoped to exactly the set the caller already snapshots and restores, so it cannot outlive its window. `_CONFIG_GLOBAL_NORMALIZERS` exists because `REMBG` is the one global that is not its raw config value (import coerces `"on"`/`"off"` to a bool, and the string `"off"` is TRUTHY). `CFG` is deliberately not rebound — all 41 reads of it are import-time derivations.
  - **`BLUEPRINT_MODEL_UNVERIFIED` handed a local render the RunPod remedy.** `explain` was transport-aware, `fix` was hardcoded, so the card said *"**your ComfyUI** has no inventory that could answer for them"* and then told the author to run `pull-models.py` onto a pod volume they were not using. The caller now derives `assurance` from the SAME `serverless` boolean that produces `where`. A text fix, but this advisory fires on EVERY run of any PuLID graph — `pulid_file` has no loader in `_MODEL_FIELD_NODES`, so it can never be verified on any target — and an advisory that confidently gives inapplicable instructions is how people learn to skim diagnostics.
  - **A trap in the plumbing, now pinned:** `iw_common.diagnostics._safe_format` falls back to the **raw template** on a missing key, so a catalog entry that grows a `{token}` its call site does not pass renders a literal `{assurance}` to the user and raises nowhere. Both targets are asserted token-free.
  - **`docs/tools/atlas-maker.md` gained "Blueprints: authoring one yourself, end to end"** — the runbook, not another description of the modal. Node ids ARE the top-level keys of `workflow.json`; every widget on a node in the ComfyUI canvas is a required input in the API JSON (a hand-edit that misses one is refused with `required_input_missing`, which names it); `shape_ref` bound alongside `style_ref` on a one-image graph is applied SECOND and wins, replacing the photo with a silhouette scaled to **Shape-ref fill %** of a black canvas — a single pixel at `0`; and most of ⚙ Settings (ControlNet strength / end %, Shape-ref fill %) is inert on a blueprint pipeline while still rendering as editable. Plus the step that would have saved the session: **verify with ⤓ Resolved workflow before spending a render**, because a publish reports PRESENCE, never CONTENT — `/video/library` shows a blueprint in `in_r2`/`on_disk`/`loaded` while the bytes are last week's, so re-uploading the wrong file is indistinguishable from success.
  - `test_config_defaults.py` 162 assertions (was 126). Six of the eleven new tests were proved by reverting each fix in memory and confirming they fail; the rest are labelled regression guards that pass either way by construction. Suites green: blueprint derive 71, blueprint models 102, model mirror 126, comfy catalog 102, push models 54, render slot 73 (the last needs `PYTHONIOENCODING=utf-8` on a cp1252 console — pre-existing).
  - **Open, not fixed: a publish cannot be told apart from a no-op.** `library_status` reports presence only. A `sha256` or `updated_at` per blueprint, surfaced in the blueprint list, would make "did my upload land?" answerable without reading the resolved graph. This cost most of a session.
- 2026-09-07 — **One bad `/status` read no longer fails a still region that is still rendering.** `_runpod_run_and_wait` — the poll loop every serverless SDXL/FLUX region goes through — raised on the FIRST unreadable read, so a single RunPod 500, or a 404 for a job it had not indexed yet, killed a region whose GPU job was still going and still billing, and reported it through `COMFY_NODE_FAILED`: *"ComfyUI failed while rendering '<region>' … check the node/model"*, pointing the author at a graph that was fine. The video path learned this twice (#540, #601); this is the same fix on the still transport, and it arrives with the fixtures that path had never had (`test_render_slot.py`, 73 assertions, was 63).
  - Reads are now tolerated for `STATUS_GRACE_SECONDS` (180) of CONTINUOUS failure. A run of 404s **on a job already read once** is an answer rather than a failure to answer — RunPod drops a finished job's record after ~30 min — so it gives up on `NOT_FOUND_GRACE_SECONDS` (15) and says *"RunPod no longer has a record of it"*; a 404 before any successful read is just a job not indexed yet and keeps the long grace. A run that stops being 404-only latches back to the long one.
  - **Cancels only what can be cancelled**, and reports a refusal: asking RunPod to stop a job it has no record of is a second 404, so that branch does not pretend to; the other branch calls `runpod_cancel` and folds a refusal into the message, the lesson `_cancel_job` already carries on the video side.
  - New diagnostic **`RUNPOD_STATUS_UNREADABLE`** — "Lost track of the GPU job for '<region>'", whose fix text sends the author to the endpoint's Requests tab rather than to their nodes and models. A status-API outage is the one failure where the graph is definitely not the cause.
  - **No rescue here, by construction:** this transport returns its image through RunPod's payload, so there is no hand-off slot to collect from and a genuinely lost job is a lost render — the grace IS the guard. The video path's R2 hand-off is what makes recovery possible there ([flipbook status](flipbook.md)).
- 2026-09-07 — **A pod render stopped consulting the artist's desktop, and a declared model now resolves against the R2 mirror.** Two live bugs first, because a change earlier the same day ARMED them: neither `preflight_models` nor `_prepare_blueprint_models_or_fail` referenced `COMFY_TRANSPORT`, and the latter binds `is_installed=_model_installed`, which probes `COMFY_BASE` — the artist's tunnel — whatever the render target. It was dormant only because it early-returns on `if not models: continue` and every blueprint's `models[]` was empty; deriving that list made it live. With a tunnel up, a RunPod render would Manager-install onto the desktop and `POST /manager/reboot` it mid-work; with the tunnel down, `ComfyUnreachable` short-circuited every declared model onto the checklist and killed a pod render with `SystemExit(2)` for files the volume had. Both are gated now, and the gate is SCOPED: on serverless `preflight_models` skips the inventory reads but still runs the PURE `wrong_family_vae` guard — an adversarial review caught a first cut that returned above it, which would have silently retired the 2026-09-04 fix for exactly the renders that cost the most. Measured on the real module: serverless + a good VAE runs with **0** desktop probes; serverless + `taesdxl` still exits 2 with **0** probes; http behaves as before with 8.
  - **`_model_installed` is three-valued** (`True` / `False` / `None`). It returned a flat `False` both when it read an enum and the name was absent and when it could not read anything — and **5 of the 14 fields a declaration can emit** (`clip_name3`, `gligen_name`, `hypernetwork_name`, `ipadapter_file`, `pulid_file`) have no entry in `_MODEL_FIELD_NODES`, so every one of them read as "missing". `None` now means no verdict: it never sets `ready=False`, it lands in the new `PrepareResult.advisories`, and a `None` on an installable model is still QUEUED (Manager's whitelist is the real gate, so declining to try would remove its only delivery path).
  - **`model_mirror.py`** (409 lines, imports only `storage` + stdlib) reads `comfyui-models/<save_path>/<filename>` and the launcher's manifest, and answers `hit` / `miss` / `ambiguous` / `unknown` / `indexed_but_gone` / `in_bucket_unindexed` — that last pair being exactly the state `push-models.py` has a `reindex` bucket for. `storage.head()` was added because `exists()` folds every exception into `False`, so a throttled HEAD would have read as "the mirror lost your file". A stored `r2_key` is a FALLBACK used only when the live index is unreadable, never an override of a live answer.
  - **The governing rule, and the reason this can't make things worse:** *the target's own inventory is the only thing allowed to refuse a render; the mirror is only ever allowed to explain how to fix one.* No mirror state — unreachable, absent, stale, ambiguous, hash-mismatched — can set `ready=False`. An independent audit traced all twelve writes of `ready` and confirmed none reads a mirror field.
  - **Stated plainly so nobody re-designs it:** a private or trained model CANNOT be auto-installed on either target. Manager's `install_model` whitelists on `(save_path, base, filename)` against its curated catalog, and the serverless worker holds no R2 credential and cannot restart ComfyUI mid-invocation. What this change buys is that the tool now SAYS so precisely, and distinguishes "in the mirror, here is how to sync it" from "nothing anywhere has this file".
  - `⟳ Rescan models` and blueprint upload now annotate `models[]` with `r2_key`/`sha256`/`size` when the mirror is readable, guarded three ways (index not ready, a bundled-author blueprint, any exception) so an R2 hiccup can never persist a stripped annotation. Two `diag_catalog` codes (`BLUEPRINT_MODEL_MISSING`, `BLUEPRINT_MODEL_UNVERIFIED`) put the verdict in the `#diags` strip instead of only in the collapsed log.
  - **Tests: 642 assertions across 7 suites, all green** — `test_model_mirror.py` 126 (new), `test_blueprint_models.py` 102 (was 45), `test_config_defaults.py` 126 (was 113), plus derive 71, comfy_catalog 100, render_slot 63, push_models 54. Several were proved by reverting the fix in an in-memory copy and confirming the test fails. **Unproven:** every live-R2 and live-tunnel path.
  - **Surfaced, not closed:** `GET /api/launcher/models-manifest` is admin-only, so a non-admin artist cannot Sync models; `push-models.py`'s 5 GB cap structurally excludes the large checkpoints a blueprint is most likely to pin; and `video_runner.py` has no prepare step at all, so the `kind: "video"` path declares models nothing reads.
- 2026-09-07 — **A blueprint now DECLARES the models it needs, read out of its own graph.** `ui_server`'s blueprint import wrote `"models": []` unconditionally — five lines after parsing the graph that names every model it loads. So no uploaded blueprint has ever declared anything, which quietly made three built things dead weight for them: `_validate_models`, the ComfyUI-Manager auto-install in `prepare_blueprint_models`, and every phase of [invisible-blueprints §10](../design/invisible-blueprints.md) (all of which assume a populated list). It also cost a real render: `characterdesignertest3` named `flux1-dev-fp8.safetensors` and `comic-style-lora-000002.safetensors`, both absent from the machine it ran on, with nothing anywhere recording that it needed them. `blueprints.derive_models_from_graph()` reads the requirements out of the graph — **the same rule the submit-time guard uses** (a string input ending in a model extension is a model; a prompt, a `filename_prefix`, a `sampler_name` and a wire are not), with `MODEL_FILE_EXTS` now defined once in `blueprints` and aliased by `batch_atlas`, so the declaration and the refusal can never drift. Verified against the real rejected payload: 6 models with correct folders (`unet`, `loras`, `pulid`, `vae`, `clip`×2) and every noise value left alone. **`save_path` is empty rather than guessed** when a field isn't mappable — `_validate_models` documents that as "not auto-installable, goes on the manual checklist", which is a correct outcome where a wrong folder is not; the one genuinely ambiguous input (`clip_name`, which is `clip_vision` on `CLIPVisionLoader` and `clip` on `CLIPLoader`) is resolved by CLASS, since a derived declaration always knows it. **Re-import merges rather than overwrites**: the graph decides WHICH models are needed, a person decides where one comes from, so an artist's `r2_key` / `sha256` / catalog `url` survives a graph tweak, and an entry the graph no longer references is dropped and NAMED rather than vanishing. A `⟳ Rescan models` button re-derives an existing blueprint (every one authored so far has an empty list) and converges — a second rescan writes nothing. `test_blueprint_derive.py` 71 assertions; `test_config_defaults.py` still 113. **Unproven:** the live R2 write on the rescan route (stubbed here, but it mirrors the import's persistence exactly) and the button in a real browser — though `PAGE.format` was verified to still resolve all 25 placeholders and render.
- 2026-09-07 — **A graph naming a model the target does not have is refused before the job, not after.** The third gate, and the one the other two left open. With every node type finally installed, the same blueprint still died on ComfyUI's `value_not_in_list`: it asked the artist's LOCAL ComfyUI for `flux1-dev-fp8.safetensors` and `comic-style-lora-000002.safetensors`, which are the R&D **pod's** files — a trained LoRA that exists nowhere else. Nothing checked them, and that is structural rather than a bug: `preflight_models` validates the names the SETTINGS panel produces for the three built-in pipelines, and a blueprint is covered **only** by what it declares in `models[]`, so a blueprint declaring nothing sails straight through. `assert_graph_models_present()` now reads each node's own `/object_info` enum and refuses a baked value that is not in it, naming the node, the field, the value and what the target does have. **It costs nothing to run:** the entries were already fetched by `assert_nodes_installed`, so the two guards now share one cached probe (`_class_info` keeps the payload where it used to keep a bool) and one readiness test (`_probe_ready` — the serverless/dead-tunnel/sentinel rules, in one place instead of two). **Scoped to values that look like FILES**, because this guard REFUSES and its whole risk is false positives: a class may override list validation with `VALIDATE_INPUTS` and `/object_info` never says which do, so a prompt, a `filename_prefix`, `sampler_name` or a mode switch is left to ComfyUI. Suggestions rank on the filename STEM — every candidate shares `.safetensors`, and that tail alone scored ~0.5, which had `gameIconInstitute3d_v10` reading as the closest thing to `comic-style-lora-000002`; an unrelated name now lists the folder instead of inventing a near miss. Honest about its size: unlike a missing node type, ComfyUI reports every bad name in ONE 400, so the win is one submitted job — which on the serverless path is a queued RunPod job and a cold start. **Verified live** against the real local ComfyUI with the rejected payload: both nodes reported with the same verdict ComfyUI gave, while `t5xxl_fp16` / `clip_l` / `ae.safetensors` / `pulid_flux_v0.9.1` (all present) and the non-file strings were left alone. `test_config_defaults.py` 113 assertions (was 98).
- 2026-09-07 — **A graph whose NODE TYPES the target lacks is refused before a job is spent.** The first real *My computer* render over the tunnel (open item 9) reached ComfyUI and was rejected with `missing_node_type` for `PulidModelLoader`: the blueprint was authored on the R&D pod, and the local ComfyUI has no PuLID pack. The transport worked; the node set did not. ComfyUI's validation **stops at the first unknown class**, so a three-class gap costs three failed submissions to enumerate by hand. `assert_nodes_installed()` — sibling of `assert_models_named`, called at the same gate one layer down (that one checks the graph NAMES a model; this one checks its nodes exist at all) — asks `/object_info/<class>` per distinct class and names the whole gap in one message, with the fixes ordered by cost: switch the target to RunPod, where the packs pinned in `services/atlas-comfy-pod/nodes.json` are already baked, or run `tools/sync-local-nodes.py` (shipped the same day, see [status/comfyui](comfyui.md)) and RESTART ComfyUI. Verdicts are cached per `(base, class)`, so only the first region pays. **It refuses to have an opinion when it cannot know**, because a guard that cries wolf gets ignored and then costs more than the failure it was added to catch: core `SaveImage` is probed first as a sentinel and the whole check falls silent if even that reads absent (a route that moved, an edge answering 404 for everything); a transport error is `None`, never "absent", so a flaky tunnel accuses no node; and the serverless transport is never probed at all, since there is no live ComfyUI to ask and that worker's node set is fixed in its own image. The post-rejection message was rewritten in the same pass — it used to send you to ComfyUI-Manager, whose installs land on the pack's **tip**, which is the drift those SHA pins exist to prevent. **Verified live against the artist's running ComfyUI**: the three PuLID classes reported with the graph nodes using each, while `CheckpointLoaderSimple`, `SaveImage` and `RMBG` — a custom node they DO have — were correctly left alone. `test_config_defaults.py` 98 assertions (was 84).
- 2026-09-04 — **A reload no longer hides a running render — Stop, the log and the diagnostics come back from the server.** Owner: *"in the atlas maker the stop button is gone, even if I am still rendering, that happens if I go away from that window or refresh… I would like for the all information to stay on screen, even the debug text at the bottom of the page."* The render lives on the SERVER (`_render_state`), but every trace of it in the page was client-only: `#log` and `#sbtn` are `display:none` until `renderSel()` reveals them, and `poll()` only ever started from a click in THAT tab. So a refresh mid-render showed a fully idle page — no Stop, no log, no diagnostic cards, the progress bar back at *▶ Render selected* — with the render still running behind it; the one surviving hint was the NEXT click being refused with *"a render is already running (2/2, started 2 minutes ago) — press Stop"*, naming a button that was no longer on the page. `adoptServerRender()` now runs on `DOMContentLoaded`: it reads `/progress` — which already returned the whole `_render_state`, log included, so no new endpoint was needed — and takes the server's answer as the truth, restoring the log and diagnostics whether or not something is running, and additionally re-arming Stop, the disabled render button and `poll()` when it is. The log/diagnostics half matters after a FAILED render too: `_run_cmd` clears both only when the NEXT render starts, so they are the entire record of what went wrong, and a reload used to be enough to lose it. **Verified locally** (R2-less boot, `_render_state` seeded by hand to skip the GPU): a cold load against a *running* render came up with Stop visible, `⏳ Rendering 2/2…`, the bar at 100%, the log restored and the diagnostic card back; a cold load against a *finished failed* render restored the log + error card while correctly leaving Stop hidden and the render button enabled. Note the state is still process-global (open item 4), so a reload also adopts a render another user started — pre-existing, and exactly what that item tracks.
- 2026-09-04 — **A wrong-FAMILY VAE is refused before the GPU runs, not 117s into it.** Owner hit `VAEDecode: Given groups=1, weight of size [64, 4, 3, 3], expected input[1, 16, 128, 128] to have 4 channels, but got 16 channels instead` after a FLUX render had already sampled for nearly two minutes on the local 4070. Reading the failed run back out of ComfyUI's `/history` named it exactly: `VAELoader.vae_name = "taesdxl"` — ComfyUI's tiny **SDXL** autoencoder — decoding a 16-channel FLUX latent from `flux1-dev` + `EmptySD3LatentImage`. Nothing was broken: **`preflight_models` checks that a model name EXISTS in the loader's `/object_info` enum, never that it belongs to the right family**, and `taesdxl` genuinely is in the VAELoader list on every install (the four `tae*` autoencoders ship with ComfyUI — there is no file to add), sitting one row from `taef1` in the dropdown. The preflight's own failure text already warned *"SDXL and FLUX models are not interchangeable"* — it simply had no way to detect it. So any wrong-family pick that happened to be installed sailed through and died at the LAST node of the graph, after the sampler had spent the full step count. `wrong_family_vae()` (pure, no ComfyUI call, offline-testable) now reads `_TAE_LATENT_CHANNELS` — the only VAE names whose family is knowable from the NAME alone — and the flux branch of `preflight_models` refuses a 4-channel pick, naming the setting (`FLUX VAE (flux_vae)`), the wrong value, the symptom it prevents, and both working replacements (`ae.safetensors`, or `taef1` for the tiny FLUX one). Deliberately narrow on both sides: an unrecognised filename is left to ComfyUI, because refusing every unknown name would block `flux2-vae.safetensors`, `ae.sft` and whatever a user names their own file; and the all-in-one `flux_checkpoint` path is exempt, since its VAE comes out of the checkpoint and has no name to get wrong. `test_config_defaults.py` 84 assertions (was 69).
- 2026-09-04 — **Stop now cancels the REMOTE job too, and clears ComfyUI's queue.** Owner: *"I pressed stop, and I expected all the jobs queued to be canceled."* `stop_render` killed the local subprocess and POSTed `/interrupt` — which aborts only what ComfyUI is executing *right now* — so two things survived: anything PENDING in ComfyUI's queue started next, and on the serverless path the **RunPod job carried on rendering and BILLING** while the UI reported it stopped. The worker was always built for this: `services/atlas-serverless/handler.py` polls RunPod every 5s (`_job_cancelled`) precisely so a cancelled job stops instead of running to completion, and its own docstring warns that without cancellation "a cancelled job renders to completion and bills for it while the UI reports it stopped" — nothing ever called `/cancel`. The job id lives in the render subprocess, so it now rides a marker line (`batch_atlas.RUNPOD_JOB_MARK`) that `_run_cmd` captures into `_render_state["runpodJob"]` and hides from the visible log; Stop calls `batch_atlas.runpod_cancel()`, then `/interrupt` **and** `POST /queue {clear:true}`. Every step is independent and best-effort — a failed cancel is reported in the Stop message rather than swallowed, and never blocks the rest. `test_render_slot.py` 63 assertions (was 51).
- 2026-09-04 — **The "a render is already running" dialog now says how far along and how long.** The #575 guard was working correctly but its message could not be acted on: it reads identically whether the GPU is 3 images into a 16-image batch or has been wedged since lunch, and the only remedy it offers (Stop) destroys work in flight. The owner hit exactly that and had to ask whether pressing Stop was safe — answering it needed a live query against their ComfyUI queue. `_render_state` now carries `started`, stamped when the slot is claimed, and the refusal reads *"A render is already running (3/16, started 9 minutes ago) — press Stop…"*; before the batch size is known it says "still starting up" rather than a meaningless `0/0`. `test_render_slot.py` 51 assertions (was 42).
- 2026-09-04 — **Two env-safety fixes found while checking whether changing `COMFY_URL` was safe (it is).** Owner rightly pushed back before touching a live variable, so the couplings got audited rather than asserted. Serverless generation reads `api.runpod.ai/v2/$RUNPOD_ENDPOINT_ID` and **never** `COMFY_URL` (`_runpod_endpoint_base`), and nothing in the launcher calls `ATLAS_BACKEND_URL` — so the production path is untouched by that variable. Two real problems surfaced anyway. **(1) The on-demand pod wake-up fired for every non-local render, including serverless ones**, where RunPod spins its own worker and a resumed GPU pod is billed for nothing; `runpod_control.targets_our_pod()` now ties the wake-up to the address the render is about to use, so it fires only for a render that really will talk to `RUNPOD_POD_ID` — which also makes changing `COMFY_URL` a no-op for it. **(2) `COMFY_CATALOG_URL` was live as `https://s3api-eu-ro-1.runpod.io s3://wvi855bwh8/`** — the Network Volume's S3 endpoint and bucket pasted together, space and all, so `urlparse` read the host as `s3api-eu-ro-1.runpod.io s3` and every probe failed silently; a value that cannot be an http(s) base is now ignored with a log line and pod discovery (#576) takes over, which is what works. `test_render_slot.py` 42 assertions (was 31).
- 2026-09-04 — **"My computer" was addressing a RunPod pod, and the panel described it as an unreachable tunnel.** Owner asked why a local render was "looking online": the strip read *My computer · Model lists unavailable — your ComfyUI has never answered at `https://pdn5pxpkrchofk-8188.proxy.runpod.net`*. Not a code bug — **`COMFY_URL` on Railway was set to a RunPod pod**. That var is what the `local` target resolves to (and `iw_common.comfy.comfy_url` documents it as "the full ComfyUI tunnel base URL"), so *My computer* pointed at a data centre while the message blamed the tunnel. `ui_server.local_target_misconfigured()` now recognises a RunPod host (`*.proxy.runpod.net`, `runpod.ai/io`) under the `local` target and says exactly that — in the strip **and** as a render refusal, **before** the reachability probe, because a RUNNING pod there would answer and render on the wrong machine, which is worse than not answering. `test_render_slot.py` 31 assertions (was 15). **Owner action:** set `COMFY_URL=https://comfy.invisiblewall.org` on the atlas-tool service (see the warning in `docs/INFRA.md`) — no code change can supply the right address.
- 2026-09-04 — **A blank REQUIRED model name is no longer stored, offered, or submitted — the other half of the blank-settings bug.** A day after #574 a FLUX render came back from RunPod as `/prompt rejected (400): vae_name: '' not in ['ae.safetensors', …]` on node `3` (`VAELoader`). #574 deliberately restricted its rule to NUMERIC keys so that a blank `flux_controlnet` / `flux_redux_style_model` could still disable an optional node — but that drew the line in the wrong place: `flux_vae` is TEXT **and** required. The right distinction is how the graph expresses "optional": by leaving the NODE OUT (`if FLUX_CHECKPOINT: … else: …`, the optional LoRA/ControlNet/Redux blocks), never by passing `""` to a node that runs — so any model name actually written into a graph is required. `batch_atlas.REQUIRED_MODEL_KEYS` now names those (checkpoint, rmbg_model, flux_unet/clip_t5/clip_l/vae/weight_dtype/sampler/scheduler/clip_vision, ipadapter_weight_type) and `blank_over_number` became `blank_shadows_default`, covering numeric ∪ required — so an existing poisoned config heals **on load**, with no re-save. The panel stops offering `(blank — none)` on a required field (it shows `(default: ae.safetensors)`), and `assert_models_named()` refuses to SUBMIT any graph carrying an empty model/enum input, naming the node, the ComfyUI input **and the setting** — scoped to enum inputs because plenty of string inputs are validly empty (FLUX's negative prompt is literally `"text": ""`). That verdict used to cost a queued RunPod job and a cold start. `test_config_defaults.py` 69 assertions (was 44). **Verified end-to-end**: with `flux_vae`/`flux_unet` blanked to the reported shape, `origin/main` built and SENT the graph, while the fixed tree healed both to their defaults and produced a complete one.
- 2026-09-04 — **⟳ finds a running RunPod pod by itself — no pod id to copy anywhere.** The RunPod side of the model dropdowns needed `COMFY_CATALOG_URL` set by hand to a `https://<pod-id>-8188.proxy.runpod.net`, which is exactly the kind of config that goes stale the moment the fleet changes (and the owner reasonably tried to point it at the Serverless endpoint, which cannot work: a worker has no address and exists only while a job runs). `runpod_control.running_pods()` now asks the RunPod API which pods are RUNNING — one GraphQL *query*, never `podResume`, so it cannot start or bill anything — and `comfy_catalog.pod_candidates()` derives each address from its id the way the launcher does, probing until one answers. Any running pod is valid: the fleet shares `Invisible_RunPod_Storage`, the same volume the serverless workers mount. `COMFY_CATALOG_URL` survives as an override, tried FIRST, for pinning a specific always-on reader. The messages changed with it — "start any pod on /comfyui, then ⟳ — nothing to configure" instead of naming an env var, and a pod that is booting is named rather than blamed on config. `test_comfy_catalog.py` 100 assertions (was 85), incl. that the first pod to ANSWER wins over the first listed, that a pinned URL outranks discovery, and that discovery never calls resume. Its harness also stopped dying on cp1252 when a FAILING assertion dumped panel HTML containing ⟳.
- 2026-09-04 — **A render could hang forever with no error; both halves of that are closed.** Reported as "my images are still processing and I am getting no error", with the local ComfyUI showing an empty queue AND an empty history — the job never arrived anywhere, yet the panel showed `Rendering 0/2...` indefinitely. Two defects had to line up. (1) **`batch_atlas.comfy_post` — the one call that submits the job (`/prompt`) — passed no `timeout`**, so `urlopen` fell back to `socket.getdefaulttimeout()` (None) and waited forever. A Cloudflare hostname whose tunnel connector is down still completes TCP+TLS and then never answers, so the read could not return: no exception, no end. Every other call in that module was already capped, and the comment above `_COMFY_HTTP_TIMEOUT` had made this exact argument for `comfy_get` — even naming `/prompt` as answering "in tens of ms"; `comfy_post` was simply missed. Now capped at `_COMFY_POST_TIMEOUT` (60 s), and `TimeoutError` joins the `except` so a mid-BODY timeout (which urllib does NOT wrap in `URLError`) reports "Cannot reach ComfyUI" instead of escaping as a traceback. (2) **`/render` answered `started` unconditionally**, even when it had not spawned a thread because `_render_state["running"]` was already set — and that flag only cleared in the render's own `finally`, which a hung subprocess never reaches. So after (1), every retry looked accepted and nothing ran, for the life of the container. `claim_render_slot()` now refuses out loud (the client shows it instead of discarding the response) and **self-heals** a flag left set with no live subprocess. `test_render_slot.py`: 15 offline assertions. **Verified against a TCP black hole** (accepts, never answers — the dead-connector shape): pre-fix still hanging when killed at 75 s, fixed exits at 63 s with the readable message.
- 2026-09-03 — **A blank numeric in Settings no longer shadows the engine default — it was killing every FLUX render.** Reported live: `ValueError: could not convert string to float: ''` from `build_workflow_flux`. Not a ComfyUI problem — a five-step chain in the settings round-trip: (1) `ui_server.load_config` reads `atlas_config.json` RAW (it does NOT merge `batch_atlas._DEFAULTS`), so any key a config predates rendered as an EMPTY box even though the render would have used 3.5; (2) `cfgData()` posts every `[data-cfg]` field, blanks included; (3) the save handler's GLOBAL branch stored that blank verbatim — the per-atlas, geometry and deploy branches each had an explicit blank rule, the global branch never did; (4) `batch_atlas.load_config` let `""` shadow the numeric default; (5) `float('')` raised. One Save on a project whose config predated a field was enough to kill generation for that project, with nothing in the UI showing why — and the recent Settings work (#571–#573) is what had people saving. **Fix, three layers:** `batch_atlas.blank_over_number` means an empty value never shadows a NUMERIC default (so every already-poisoned config in R2 heals on load, no migration) while TEXT keys keep blank-means-blank, because a blank `flux_controlnet` / `flux_redux_style_model` deliberately disables that optional node; `ui_server.apply_global_edit` DROPS a blank numeric instead of storing `""`, so the file self-heals on the next save; and the panel stops lying — an empty numeric box shows `default: 3.5` as its placeholder and a per-atlas field's `(inherit global: …)` names the value actually inherited (it read `(inherit global: )` while the render used 1024). `test_config_defaults.py`: 44 offline assertions incl. the verbatim traceback config. **Verified end-to-end**: the exact failure reproduced on the pre-fix tree, then the same poisoned config rendered past `build_workflow` on the fixed tree (failing only at the deliberately-dead ComfyUI port), and a live Save removed the four poisoned keys from the file.
- 2026-09-03 — **⚙ *Run generation on* — RunPod or my computer, per project; the model lists follow the choice.** New Global setting `run_on` (`""` = the service default from `COMFY_TRANSPORT`, `pod` = the RunPod serverless endpoint, `local` = your own ComfyUI over the tunnel). Applied as an env override on the render subprocess (`COMFY_TRANSPORT`) — the seam per-user routing already uses (`resolve_user_comfy_env`, which still supplies a registered user tunnel under `local`) — so there is no new transport code. An explicit *My computer* never calls `runpod_control.ensure_pod_ready` (a GPU the user just said not to use must not be woken) and preflights `<url>/system_stats` with the headers the render will use, so a machine that isn't answering fails in one readable sentence in the render panel instead of a stack of upload timeouts. **Two catalogs, never each other's:** `comfy_catalog` keys by target — `local` = live `COMFY_BASE` → `_shared/comfy/catalog.json`; `pod` = `COMFY_CATALOG_URL` only → `_shared/comfy/catalog-pod.json` — and the pod target never reads `COMFY_BASE`: it is the user's machine, and #571 as merged would have shown the tunnel's files for a render bound for RunPod whenever the owner's box happened to be up. The strip names the machine (*RunPod ·* / *My computer ·*) and, per tier, exactly what to start or set; ⟳ refreshes the selected machine's catalog. `test_comfy_catalog.py`: 85 assertions (was 56). **Verified locally** (R2-less boot + a fake ComfyUI as `COMFY_URL`): *My computer* → green, all 16 installed-file fields listed from the fake; *RunPod* → the fake untouched on render and on ⟳ (its request log did not move), red strip + ⟳ note both naming `COMFY_CATALOG_URL`; *My computer* with the fake down → `/render` ends at once with the one-line preflight. The real tunnel render is open item 9.
- 2026-09-03 — **The ＋ Blueprint importer and 🎛 Blueprint settings read the node's contract, in step with the Flipbook.** A setting's type, range and option list now come from ComfyUI `/object_info` (`comfy_specs.py`, `POST /video/nodespecs`) for the target ⚙ *Run generation on* selects, instead of a guess from the baked value — the same guess that let a `sensitivity` of 50 through on the Flipbook side; a bounded numeric is a slider + number box clamped on change; a dropdown re-reads its list each render (saved value kept, marked *(not installed)*). **⟳ Refresh model lists now also caches every published blueprint's select lists** for its target, so those dropdowns survive a stopped pod like the Settings ones do. Full story in [status/flipbook](flipbook.md).
- 2026-09-03 — **The dead "ComfyUI host:port" field is gone from Global settings.** In the cloud port `cloud_paths.resolve()` derives the ComfyUI address from the `COMFY_URL` env only (its own comment: a legacy field kept for compatibility) and `batch_atlas.COMFY_BASE` snapshots it at import — so editing the field changed nothing while its tooltip promised otherwise (found while verifying #571: pointing it at a fake ComfyUI did nothing until `COMFY_URL` was set). Removed the field, its `COMFY_HOST` proxy, the `DEFAULT_CONFIG` / `_DEFAULTS` seeds and the help text; an existing `atlas_config.json` keeps a harmless stale key. Where generation runs is a real choice, not an address, and that is the next Settings change.
- 2026-09-03 — **The Settings model/enum fields are dropdowns again — and they say where their values came from.** Root cause was NOT a missing feature: `ui_server` has always had `MODEL_FIELDS` + a `<select>` branch in `_control_html`, populated from `batch_atlas._available(node, field)` → ComfyUI `/object_info`. But `_available()` returns `None` the instant `_comfy_alive()` fails — a 3s GET to `{COMFY_BASE}/system_stats` — and production runs **`COMFY_TRANSPORT=serverless`** (the RunPod endpoint is a job queue, there is no long-lived ComfyUI HTTP server) with a blank `comfy_host`, so `COMFY_BASE` never answers. All 16 model fields fell back to a free-text `<input>` **with nothing on the page saying so**, which is what made it read as "the dropdowns were never built". Three changes:
  - **`ENUM_FIELDS`** (`ui_server.py`) — static seeds for the fields whose values are FIXED ComfyUI enums or our own options, so they need no network at all: `flux_weight_dtype`, `flux_sampler`, `flux_scheduler`, `ipadapter_weight_type`, the five `gpt_image_*` fields, and `atlas_format` (whose geometry-loop `<input>` now routes through `_control_html` like everything else). The seed is a **floor, never an override** — precedence is live `/object_info` > persisted catalog > seed.
  - **`comfy_catalog.py`** — caches the `/object_info` enum lists in R2 at the SHARED key `_shared/comfy/catalog.json` (what's installed is a property of the ComfyUI, not of a client/project), so the installed-file dropdowns keep working while nothing answers. `load()` fail-safes to `{}` on any R2/parse error and is parsed field-by-field (one junk value must not cost the whole catalog). `refresh(pairs)` probes `COMFY_BASE` when alive, else the new optional **`COMFY_CATALOG_URL`** (an always-on ComfyUI — e.g. the CPU volume pod the launcher already reads); it is GET-only so it never starts/resumes/bills a pod, and a probe that reaches nothing returns `ok:false` and **leaves the stored catalog alone** (an unreachable ComfyUI is not evidence the models are gone). A page render never PUTs; only ⟳ and one content-gated, rate-limited `commit_live()` do.
  - **The panel now explains itself** — a coloured status strip at the top of ⚙ Global settings (green *live from ComfyUI* / amber *cached <age> from <source>* / red *no ComfyUI has ever been reached … set `COMFY_CATALOG_URL` or start a pod*) plus a **⟳ Refresh model lists** button → `POST /refreshmodels` (`_bust_comfy_caches()` then `comfy_catalog.refresh()`), reporting source + list/value counts.
  Throughout, a configured value that no list carries is still offered and selected, marked `(not in ComfyUI)` for an installed-file list and `(custom)` for a static enum — saving the panel can never silently rewrite a setting. Covered offline by `test_comfy_catalog.py` (55 assertions: the three-tier precedence, value preservation on every tier, a failed refresh not overwriting a good catalog, malformed/absent/dead-R2 loads, and that a render is not an R2 write). **Owner live-verify owed** on Railway.
- 2026-09-02 — **`＋ New atlas` names the atlas in a dialog and confirms before replacing one.** The native `prompt()` gave no hint that the name was taken; the server then silently switched to the existing atlas with a ⚠ note, which read as "nothing happened". Now a modal (same `.modal`/`.modalbox` chrome as Variants/Advanced) shows the slug it will be stored under, warns live when that slug is already in the manifest dropdown, and Create on such a name asks for confirmation — OK sends `overwrite:true` (the `_newatlas` branch that already existed but nothing called) and replaces the manifest with an empty one; Cancel / Esc / backdrop / blank leave everything as it is. Sibling of the Sheet Maker's new-sheet dialog (#554). Esc closes it through the same global listener as the other modals. **Case:** manifests authored by the Sheet Maker keep their case (`atlas_manifest_F_Bottle_Exit.json`) while `r2_slug` lowercases, and on Linux those would be two files — so both the dialog's collision check and `_newatlas` match an existing manifest ignoring case and reuse its spelling (found on the first live pass: `F_Bottle_Exit` typed back was not flagged and would have made an `f_bottle_exit` twin). Verified locally (R2-less boot with two seeded manifests): warning, confirm/cancel, sanitising, Enter/Esc, a real create + reload. **Verified live on Railway** (2026-09-02, project `test6`, signed in via the launcher): the dialog opens, `f_bottle_exit` is flagged as the existing `F_Bottle_Exit`, and Esc leaves the active manifest untouched.
- 2026-09-01 — **Opening a Sheet-Maker sheet in the Atlas Maker stops stretching the art inside every
  region.** The rects stayed exactly right while the art in them was re-derived from its ink bounds
  and rescaled, so it read as a packing bug rather than a placement one — and it made the
  sheet-parity work above (`fit_mode:"contain"` → `_packer_compose_tile`) unreachable for the one
  route that needs it most.
  - **The handoff threw the contract away.** `ui_server._normalize_converted_region` rebuilt each
    region as a **closed geometry-only dict**, so `fit_mode` (and `prompt`/`shape_ref`/`seed`) never
    survived `import_sheet_to_manifest`. Only `sheet-tool/atlas_writers.py` writes
    `fit_mode:"contain"`, and only an EXPLICIT one routes to the parity path — dropping it dropped
    the sheet's placement.
  - **…and it synthesized the field that picks the worst fallback.** `orig_w`/`orig_h` defaulted to
    `w`/`h`, but `fit_to_region` reads their mere PRESENCE as `spine_slot`, which flips the default
    from `contain` to **`fill`** — alpha-crop, then stretch to the rect exactly. An untrimmed cell
    was thereby mistaken for a rig's authored footprint. Measured on a 493×501 cell holding 360×170
    of art: **×1.37 wide, ×2.95 tall, aspect 2.12 → 0.98.** Trim is now carried only when the source
    actually has it.
  - **Sliced-from-page art is marked `contain` at import.** The bound override IS the cell, cut at
    exactly `w × h`, so the faithful recompose is a verbatim paste. Scoped to the editor/seed-manifest
    branch — a raw TexturePacker/`.atlas` import keeps `fill`, where the rect is a rig's footprint and
    regenerated art must fill the slot the game already renders.
  - **The raw-TexturePacker route lands on `fill` too, and cannot be fixed at import.** The Sheet
    Maker's own `.json` declares `sourceSize` = the full cell (`trimmed:false`), so
    `_tp_frame_to_region` hands every cell an `orig_w`/`orig_h` and the same `spine_slot` default
    fires. That branch also carries genuine rig slots — including untrimmed ones, where `fill` is
    deliberate — so it cannot be blanket-switched.
  - **`repair_sheet_fit_mode` heals what is already damaged on disk.** The fixes above stop new
    damage; they cannot undo a manifest a pre-fix build already wrote, and re-clicking
    `Open in Atlas Maker` only re-activates that recipe. The repair restores `fit_mode` from the
    region of the same name in the **Sheet-Maker manifest that authored the page** (joined on the
    page BASENAME, which survives the `refs/atlas/` rewrite; identified by the `_comment` marker
    `build_manifest` stamps). Evidence, never inference: no authoring sheet manifest → nothing is
    touched, so a rig's `.atlas` keeps its `fill`; an explicit `fit_mode` is never overruled. Runs in
    the **compose pre-pass** (next to `rebuild_fx_layers` / `auto_pack_layout`, so no Create Atlas
    can run on a stripped manifest) and on **activation** via both routes — deep-link and Session
    dropdown — so `/atlasview`'s placement readout is truthful before anyone risks a compose. It
    covers BOTH damage routes because it sources the truth rather than guessing at import. Idempotent,
    and it reports what it repaired: a placement change nobody asked for should never be silent.
  - Regression fixtures: `services/atlas-tool/test_sheet_handoff.py` (26 checks) — pins the parity
    round-trip byte-identical, drives the repair through both damage routes, and asserts the failure
    mode still fails, so the checks can't pass for the wrong reason. Safety cases cover the untouched
    `.atlas`, the explicit-override, and the `rotated_regions` bucket.
- 2026-09-01 — **The New-blueprint modal binds whatever graph you import, not one shaped like the
  built-in three.** Ported from the Flipbook's video twin the same day it was fixed there
  ([status/flipbook](flipbook.md)), so the two hand-maintained modals are back in step.
  - **`bpCandidates` was a FILTER and `BP_FIELD` hardcoded the field per role**, which holds only
    while a graph's knobs are still widgets on the consuming node. Convert them to inputs — the
    prompt on a `PrimitiveString`, the seed on a `PrimitiveInt` — and the CLIPTextEncode the filter
    approves takes its `text` from a wire the role cannot write, while the primitive that actually
    holds the value is not offered at all. Both REQUIRED roles then have no usable option.
  - **`bpResolveKnob` follows the wire back to the widget**, through pass-through relays, and
    `bpCandidates` now RANKS (knob-through-a-wire, raw widget, wrong polarity) over a complete
    list — every `(node · input)` stays reachable under "All node inputs", wired ones marked.
    Bindings carry `node::field` from the select, so `BP_FIELD` is gone.
  - **Roles pre-fill only when the top rank is uniquely held**, and `(not used)` is now offered on
    required roles too — a required role used to default to whatever the filter approved FIRST, so
    a wrong binding could publish without the author ever looking at that row. `saveBlueprint`
    checks the required set before posting.
  - **A `text` param can declare `multiline`** and renders as a full-width textarea in the settings
    panel, auto-ticked when the baked value reads as prose. `bpInferType` reads the node CLASS
    first (a `PrimitiveFloat` holding `1` is a float, not an int).
  - **Two bugs the browser found that reading could not.** (a) The row prefill only ran
    `if(!key.value)`, and `addBpParam` ends by selecting the graph's FIRST node — so every row came
    out keyed after that node and defaulted to its value, and re-pointing the row changed nothing
    but the type. Now a field the author has not touched follows the node. (b) `bpSuggestKey`
    counted the row's OWN key when deduping, so each re-point appended a digit
    (`SaveAnimatedWEBP2`, `…3`).
  - Verified: the real JS lifted out of the `PAGE` template and run in node against the reported
    `WanLoopingVideo` export **and** a classic widget-shaped SDXL graph (the regression case —
    `width`/`height` still bind to `EmptyLatentImage`); then the tool booted locally and the actual
    modal driven in a browser, including a two-hop relay (`345.width → 380 → 355.value`), the
    settings-panel textarea, and the published payload through the real `validate_against_graph`.
- 2026-08-18 — **gpt_image and the FLUX ref/ControlNet path both work, on RunPod** (owner-tested). These were the two oldest blockers on this tool and they had the same root cause: they needed nodes/models installed on the **local** 4070 behind the tunnel (`Images to RGB` + `COMFY_ORG_API_KEY`/credits for gpt_image; FLUX ControlNets for shape_ref). Moving generation to the RunPod backend — whose worker image bakes the custom nodes and whose volume carries the models — retired the constraint rather than satisfying it. **The live Railway `atlas-tool` runs `COMFY_TRANSPORT=serverless`** (owner-confirmed 2026-08-18) — so the RunPod endpoint, not the local tunnel, is the production generation path, and the local 4070 + tunnel are no longer in the loop for Atlas Maker generation at all. Worth knowing when diagnosing: a generation failure in production is a RunPod/worker-image problem (cold start, missing baked node, endpoint quota), **not** "is Gualtiero's ComfyUI running".
- 2026-08-15 — **each serverless variant gets a unique filename** (`_next_variant_filename`). ComfyUI names saves with an incrementing per-output counter, but every serverless job lands on a FRESH worker whose counter restarts at `00001` — so each new variant of a region collided on `<region>_00001_.png` and overwrote the previous one. The transport now mirrors ComfyUI's scheme locally (highest existing id in the batch dir + 1, same `<region>_<NNNNN>_.png` shape the gallery globs). It also prefers the **`SaveImage`** output — the one whose worker filename is region-prefixed — over any preview/temp image the graph may also emit, so a preview never gets persisted as a variant.
- 2026-08-13 — **`batch_atlas` gained a second ComfyUI transport, selected by `COMFY_TRANSPORT`** ([design](../design/comfyui-serverless.md) §Pipeline changes, build-plan step 2). Unset / `http` (DEFAULT) = the unchanged live-ComfyUI path (`/upload/image` + `/prompt` + `/history` + `/view`); `serverless` = submit the SAME api-prompt graph to a RunPod Serverless endpoint (`RUNPOD_ENDPOINT_ID`/`RUNPOD_API_KEY`): `POST /run` with `{input:{workflow, images:[{name, image:<b64>}]}}`, poll `GET /status/{id}` (~2s, 30-min cap), decode the returned base64 image and persist it through the SAME `_persist_variant` path. The "which LoadImage refs, what filename" routing is factored into `_iter_workflow_refs` so both transports share it (`_upload_workflow_refs` for http, `_serverless_workflow_images` for base64). No behaviour change when `COMFY_TRANSPORT` is unset. _(The endpoint has since been created and generated for real — see the 08-15 and 08-18 entries above.)_
- 2026-08-11 — **the render subprocess now reads the SAME `atlas_config.json` the UI writes** (root-cause: selected blueprint / global settings never applied). `batch_atlas.load_config` read `SELF/atlas_config.json` (script dir — not shipped in the cloud image), while `ui_server` writes to `staging_root/atlas_config.json` (R2-backed, per client/project). So every GLOBAL-only setting — the **pipeline selector** above all — was stuck on its `_DEFAULTS` value (`sdxl`), silently generating built-in SDXL even when a blueprint was picked. `load_config` now prefers the staging copy (via `cloud_paths.resolve()['staging_root']`, resolvable in the subprocess from `IW_CLIENT_NAME/IW_PROJECT_NAME`), falling back to the script-dir file for the local tool. Diagnosed via the new `Pipeline: … (built-in|blueprint)` log line (#306) + the loud no-silent-SDXL guard (#305). PER_ATLAS settings were unaffected (they travel in the manifest via `apply_manifest_settings`).
- 2026-08-11 — **from-scratch atlases: generate/compose on a bare atlas block** (#301 follow-up) — a `pack` manifest's `atlas` block has no `source_image`/`width`/`height` until art is packed, so generation `KeyError`'d on `atlas["source_image"]` (it's only a built-in style-ref fallback; a blueprint ignores it) and composing before any art would `KeyError` on `atlas["width"]`. Generation now reads `atlas.get("source_image") or ""`; compose-before-any-art returns a readable "nothing to compose yet" line. Live-verified path: New atlas → single region → blueprint render.
- 2026-08-11 — **from-scratch atlases (auto-pack)** — `＋ New atlas` / `＋ Add region` / per-card 🗑 + a packed Create Atlas (`pack.py` + `auto_pack_layout`) let you build an atlas from prompts with no Sheet Maker, region sizes emergent from trimmed art. Shipped #301; page auto-crops to used bbox (2048 = cap). Owner live-verify continuing.
- 2026-07-16 — **a Sheet-Maker cell now composes byte-identically to its sheet** (explicit `fit_mode:"contain"` → `_packer_compose_tile`), so republishing an atlas stops re-offsetting rigged art. Needs an `atlas-tool` redeploy + a re-Create-Atlas to take effect on existing pages ([detail in history](../history.md)).
- 2026-07-16 — **Region Overlay Inspector: retracted the unsound producer inference, added sheet parity + placement mode.** The `FILLS`/`INSET` badge was being presented as a producer verdict ("one page, two producers") — it cannot know that (full-bleed art and FX halos FILL under either composer), so the summary/legend/docs now describe only what is measured. Replaced as the headline by the **recompose test** (`/parityscan`): re-run the region's source art through the real `_packer_compose_tile` and diff against the page. Offline-proven on a known-mix page, 7/7: sheet-composed → MATCHES SHEET `max Δ 0` (incl. rotated); atlas-fill-composed → DIFFERS `2.73x, origin (38,38)→(0,0), max Δ 255`; legacy cell-grid contain → DIFFERS (same); explicit cover → DIFFERS `2.14x×3.33x`; **full-bleed sheet-composed → FILLS *and* MATCHES SHEET** (the false positive that motivated the change); no variant → NO SOURCE. Parity is exact, so `PARITY_TOL = 2` is slack for a lossy round-trip, not a real need.
- 2026-07-16 — **`🖼 View atlas` now opens the Region Overlay Inspector** (`/atlasview`) instead of the bare page bytes, making the rect-convention difference visible + screenshottable. Same art + same 100×100 rect measures 46×46 under the packer vs 100×100 under `fit_to_region` (2.17×, origin 27,27 → 0,0).
- 2026-07-16 — fixed FX layers composing offset + shrunk against their base (`fx_registration_crop`); needs an `atlas-tool` redeploy + a re-Create-Atlas to take effect on existing pages.
- 2026-07-06 — sheet-derived FX cells now auto-derive on load/Process instead of being AI-generated ([detail in history](../history.md)).
- 2026-05-30 — FLUX proven live (~105s txt2img); B10 `.atlas`/source upload + R2 ref picking landed ([detail in history](../history.md)).
- 2026-05-29 — R2 seeded (1158 objects); tool hydrates + lists manifests ([detail in history](../history.md)).
