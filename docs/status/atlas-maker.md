# Invisible Atlas Maker — status

> Design: [docs/design/atlas-per-user-session.md](../design/atlas-per-user-session.md) · Guide: [docs/tools/atlas-maker.md](../tools/atlas-maker.md) · Agent: `.claude/agents/atlas-python-tools.md`

**One-line state:** Live on Railway (`atlas-tool`). SDXL generate → slice → compose → deploy proven end-to-end; **FLUX (txt2img *and* the ref/ControlNet path) and gpt_image both proven on the RunPod backend** (owner-tested 2026-08-18) — the old "only SDXL ControlNets are installed on the local 4070" ceiling is gone.

## Current state
Works today on `main` / live:
- **From-scratch atlases (auto-pack)** — create a brand-new atlas in the tool (no Sheet Maker / `.atlas` needed) and grow it region-by-region. **`＋ New atlas`** asks for the name in a dialog first (sanitised the way `r2_slug` stores it; a name already in the manifest dropdown is flagged live and confirmed on Create, which then sends `overwrite:true`), writes an empty `pack`-layout manifest (`atlas.layout:"pack"`, no bound `.atlas`) and makes it active — a bare `/newatlas` call without the flag still switches to the existing atlas instead of overwriting; **`＋ Add region`** / per-card **🗑** append/remove name-only regions; each is generated from its prompt like any other. **Create Atlas then auto-packs**: `auto_pack_layout` (ui_server) measures each region's ALPHA-trimmed variant, runs the ported MaxRects packer (`pack.py`, a lockstep copy of `sheet-tool/packer.py` `pack()`), and stamps `x/y/w/h(/rotated)` + `atlas.width/height` back onto the manifest BEFORE the compose subprocess reads it. Region size is entirely emergent from the trimmed art (no per-region size to set); the page auto-sizes and re-packs every Create Atlas, so the atlas morphs as regions are added. Placement is 1:1 — the packed rect equals the trimmed bbox, so compose's default `contain` path scales by 1.0 (verified: packer no-overlap/in-bounds, `auto_pack_layout` stamping + ungenerated-skip, and `fit_to_region` exact-fill). Deploy is unchanged: `_deployatlas`'s manifest-regions fallback already emits the TexturePacker `.json` from those same fields. The page auto-crops to the layout's used bounding box on BOTH axes (`AUTO_PACK_MAX_WIDTH` = 2048 is only a cap), so a small atlas is a snug page, not a 2048-wide sheet of empty space — this is `pack.py`'s one intentional divergence from `sheet-tool/packer.py` `pack()` (which keeps its fixed sheet width); the `_MaxRects` core stays identical and compose parity (`_packer_compose_tile` ↔ `packer.compose`) is untouched. **Owner live-verify owed** (a real generate + Create Atlas + deploy through a from-scratch atlas on the GPU/tunnel).
- The local Python Atlas Maker (stdlib `http.server` + Pillow) **re-hosted on Railway** — `cloud_paths.py` staging mirrors the R2 prefix 1:1, `storage.py` write-through, ComfyUI reached over the Cloudflare tunnel (refs via `/upload/image`, outputs via `/view`).
- **SDXL** generate / slice / compose / deploy — the proven, default pipeline.
- Per-region card: prompt edit, seed lock/unlock, style/shape ref pick — **rembg (background-removal) toggle**, refs uploaded locally or **picked from R2** via `/fsbrowse`, one-click "lock the seed that made this".
- **Self-contained manifests** — Windows path/trailing-dot normalisation done in R2; `.atlas` + source-page upload (B10) repoints the manifest to staging-relative paths.
- **FLUX builder complete** — txt2img proven live (2026-05-30, ~105s on the 8GB 4070).
- **Blueprints** (an Atlas Maker feature, NOT a separate tool — [design](../design/invisible-blueprints.md)) — shareable ComfyUI workflows in a global `_shared/blueprints/` R2 library; pick one in the Atlas Maker to "generate with this network". **Code-complete (all 8 phases), owner live-verify owed.** Committed + publish-gated (`ATLAS_BLUEPRINT_SECRET`); Settings has the resolved-workflow (`/blueprintresolved`) export for debugging.
- **Sheet-derived FX cells auto-derive** on load/Process from their base region (not AI-generated), keyed on `shine.fx_layer_info` + `mode in FX_PRESETS` — no wasted ComfyUI credits.
- **Region Overlay Inspector** (`/atlasview`, the `🖼 View atlas` button — [guide](../tools/atlas-maker.md#-view-atlas--the-region-overlay-inspector)) — the composed page with each manifest rect outlined, plus **three separate readings** per region, kept apart because they know different amounts:
  1. **Sheet parity** (`MATCHES SHEET` / `DIFFERS` + delta / `NO SOURCE`) — the headline and the only *evidence* of how a region was placed. Server-side (`/parityscan`, on demand — it opens every source image): recompose the tile from the region's source art through the REAL `_packer_compose_tile` and diff it against the page's rect pixels. Directly predicts whether a re-Create-Atlas would move the region. Tolerance `max Δ 2`/channel is pure slack — the test is deterministic, so a real sheet-composed region matches **byte-identically** (`max Δ 0`); a placement difference reads `max Δ 255`. The comparison must replay compose's `canvas.paste(img, …, img)`, which is not a no-op (PIL applies the mask to every band: a semi-transparent pixel lands as `src * a`).
  2. **Placement mode** — which `fit_to_region` branch the next compose takes (`_placement_mode` mirrors that dispatch, incl. `spine_slot = "orig_w" in region and "orig_h" in region`). Answers *"will the sheet-parity fix do anything for this page?"*: only an EXPLICIT `fit_mode:"contain"` reaches it, and only `sheet-tool/atlas_writers.py` writes that field — a manifest imported from a `.atlas` (`_tp_frame_to_region`) has trim but no `fit_mode`, defaults to `fill`, and the parity fix is a **no-op** for it. Counted in the summary, which says so outright when no region carries it.
  3. **Ink coverage** (`FILLS` / `INSET n%` / `EMPTY`) — how much of the rect the art's alpha bbox covers. A **measurement, not a producer verdict**: full-bleed art FILLS under the sheet packer too (its scale clamps at 1.0), an FX halo FILLS while its base is INSET (`T_UI_Spin_Edge` 88% vs `T_UI_Spin_Edge_glow` 100% — same art, same placement), and a `contain` region reads INSET even when upscaled. The earlier "a mixed FILLS/INSET page proves two producers" claim was **unsound and has been removed**.

  Handles rotated `(h × w)` footprints and draws trimmed regions' untrimmed frame in the manifest's own Y-down convention. **Code-complete, owner live-verify owed.**
- **A Sheet-Maker cell composes byte-identically to its sheet** — the two composers used to disagree on what a rect MEANS: `sheet-tool/packer.py` `compose` scales against the **full art canvas** with a `min(…, 1.0)` clamp (**never upscales**) then centres the visible bbox, while `batch_atlas.fit_to_region` alpha-cropped to the **ink bbox** and scaled that to fill the rect with **no clamp** (upscales). Same `bounds:`, art up to ~2.5× bigger with its origin shifted from a centred inset to the rect's corner — and since the Rigger prefers the **deployed** page as soon as one exists (`editorRegions.ts` `findDeployedPage`), a Create-Atlas + deploy silently swapped a rig's pixels from the sheet's convention to the atlas's and offset every attachment. Now an **EXPLICIT** `fit_mode:"contain"` (only `sheet-tool/atlas_writers.py` writes it) routes to `_packer_compose_tile`, a verbatim replay of `packer.compose` — so republishing is a no-op for placement. **Explicit-only by design:** the default fallback also resolves to `contain` for legacy cell-grid regions, which keep their alpha-crop + letterbox. Proven byte-identical to the real `packer.compose` across 8 cases (rect bigger/smaller/equal, asymmetric padding, fully-transparent, rotated, rotated+downscale, non-square); 22/22 no-regression vs the pre-change function at `PADDING_PCT` 0.0 **and** 0.12. Keep `_packer_compose_tile` in lockstep with `packer.compose`. **Because it is explicit-only, it is only as good as whoever preserves the field** — the `Open in Atlas Maker` handoff dropped it (and forced the `fill` fallback) until 2026-09-01, so the parity path never ran for the route that carries sheets into this tool; see "Recent changes".
- **FX layers register with their base through compose** — `fit_to_region` alpha-crops each region independently, which mis-scaled every FX layer (a glow's halo makes its alpha bbox bigger than its base's, so the glyph shrank to ~54% and drifted up to ~15px when the halo clipped a canvas edge). `batch_atlas.fx_registration_crop` now crops an FX layer to its BASE's bbox grown by the two slots' size ratio — same scale, concentric, halo kept to whatever the packed FX slot can carry. Guarded: an FX image whose canvas ≠ its base's (hand-made art) falls back to self-cropping.

- **Flipbook video sessions run in THIS service** (`video_runner.py` + the `/video/*` routes in `ui_server.py`), because the blueprint library, the generic runner, the RunPod Serverless transport and the packer all already live here. The feature is the Flipbook's — its state lives in [flipbook status](flipbook.md), design in [invisible-flipbook-video.md](../design/invisible-flipbook-video.md). It deliberately does NOT touch the active-manifest globals, so it is unaffected by (and does not worsen) the per-user isolation gap in open item 4.

## Open items / next
1. **`_shadow` FX cells lose their drop offset under sheet-parity compose.** Fallout of the rect-convention fix (see "Works today"). `shine.make_shadow` (`shine.py:320`, `offset_y: int = 8`) is the ONLY asymmetric FX builder: its blob's bbox sits ~4px below the glyph in canvas space, and packer.compose centres the **visible bbox** — so parity re-centres the shadow and erases the deliberate drop. Everything else (glow/blur/zoom) measures exactly (0.0, 0.0) drift. The pre-fix behaviour was not a working alternative — it kept the offset only by clipping the shadow to its base's bbox and upscaling ~5×, i.e. the bug being fixed. **Next:** bake the offset into a symmetric canvas in `make_shadow` so the blob's bbox centre coincides with the glyph's, rather than relying on placement to preserve it.
2. **`.atlas` compose/slice (B10 / Part A) — live browser smoke-test owed** against live R2 (Part B = repoint the stale Windows-path `atlas.atlas_file`/`source_image` fields, owner + R2 creds).
3. ✅ ~~**FLUX ref/ControlNet path unproven**~~ — **DONE (owner-tested 2026-08-18).** shape_ref / ControlNet generates on the **RunPod** backend, which carries its own ControlNet models; it was only ever blocked on what the local 4070 had installed.
4. **Per-user session isolation — planned, unbuilt** (design `atlas-per-user-session.md`): the active-manifest resolver is process-global, so two users on one project clobber each other's open selection + see each other's render progress. Phases 1–3 (thread `user` id → per-user overlay → per-user render state).
5. **Access gate** — `ATLAS_TOOL_SECRET` unset → tool is open on its Railway URL; verify the launcher `?k=` flow when set.
6. **Blueprints: owner live-verify + model auto-download** — the 8 phases are code-complete but unverified live; the ComfyUI-Manager model auto-download (so a blueprint's models install without a manual drop) is the main remaining piece ([design](../design/invisible-blueprints.md) §7).
7. ✅ ~~**The New-blueprint modal can't bind a graph whose knobs were "converted to input".**~~ — **DONE (2026-09-01)**, ported from the Flipbook's video twin the same day it was fixed there, so the two modals are back in step. See "Recent changes" below.

8. ✅ ~~**Seed the RunPod model catalog once (owner).**~~ — **DONE (2026-09-04)**: ⟳ discovers a running pod itself; just start one on /comfyui and press it. See "Recent changes".
9. **The real *My computer* render over the tunnel is owed (owner).** The `http` transport has been off the production path since 2026-08-18; the new setting puts it back on demand. Start ComfyUI + the desktop launcher's tunnel, pick *My computer* in ⚙ Global settings, render one region.

## Blocked (owner / external)
_Nothing._ Both long-standing entries cleared on 2026-08-18 — see below.

## Recent changes
- 2026-09-04 — **A wrong-FAMILY VAE is refused before the GPU runs, not 117s into it.** Owner hit `VAEDecode: Given groups=1, weight of size [64, 4, 3, 3], expected input[1, 16, 128, 128] to have 4 channels, but got 16 channels instead` after a FLUX render had already sampled for nearly two minutes on the local 4070. Reading the failed run back out of ComfyUI's `/history` named it exactly: `VAELoader.vae_name = "taesdxl"` — ComfyUI's tiny **SDXL** autoencoder — decoding a 16-channel FLUX latent from `flux1-dev` + `EmptySD3LatentImage`. Nothing was broken: **`preflight_models` checks that a model name EXISTS in the loader's `/object_info` enum, never that it belongs to the right family**, and `taesdxl` genuinely is in the VAELoader list on every install (the four `tae*` autoencoders ship with ComfyUI — there is no file to add), sitting one row from `taef1` in the dropdown. The preflight's own failure text already warned *"SDXL and FLUX models are not interchangeable"* — it simply had no way to detect it. So any wrong-family pick that happened to be installed sailed through and died at the LAST node of the graph, after the sampler had spent the full step count. `wrong_family_vae()` (pure, no ComfyUI call, offline-testable) now reads `_TAE_LATENT_CHANNELS` — the only VAE names whose family is knowable from the NAME alone — and the flux branch of `preflight_models` refuses a 4-channel pick, naming the setting (`FLUX VAE (flux_vae)`), the wrong value, the symptom it prevents, and both working replacements (`ae.safetensors`, or `taef1` for the tiny FLUX one). Deliberately narrow on both sides: an unrecognised filename is left to ComfyUI, because refusing every unknown name would block `flux2-vae.safetensors`, `ae.sft` and whatever a user names their own file; and the all-in-one `flux_checkpoint` path is exempt, since its VAE comes out of the checkpoint and has no name to get wrong. `test_config_defaults.py` 84 assertions (was 69).
- 2026-09-04 — **Stop now cancels the REMOTE job too, and clears ComfyUI's queue.** Owner: *"I pressed stop, and I expected all the jobs queued to be canceled."* `stop_render` killed the local subprocess and POSTed `/interrupt` — which aborts only what ComfyUI is executing *right now* — so two things survived: anything PENDING in ComfyUI's queue started next, and on the serverless path the **RunPod job carried on rendering and BILLING** while the UI reported it stopped. The worker was always built for this: `services/atlas-serverless/handler.py` polls RunPod every 5s (`_job_cancelled`) precisely so a cancelled job stops instead of running to completion, and its own docstring warns that without cancellation "a cancelled job renders to completion and bills for it while the UI reports it stopped" — nothing ever called `/cancel`. The job id lives in the render subprocess, so it now rides a marker line (`batch_atlas.RUNPOD_JOB_MARK`) that `_run_cmd` captures into `_render_state["runpodJob"]` and hides from the visible log; Stop calls `batch_atlas.runpod_cancel()`, then `/interrupt` **and** `POST /queue {clear:true}`. Every step is independent and best-effort — a failed cancel is reported in the Stop message rather than swallowed, and never blocks the rest. `test_render_slot.py` 63 assertions (was 51).
- 2026-09-04 — **The "a render is already running" dialog now says how far along and how long.** The #575 guard was working correctly but its message could not be acted on: it reads identically whether the GPU is 3 images into a 16-image batch or has been wedged since lunch, and the only remedy it offers (Stop) destroys work in flight. The owner hit exactly that and had to ask whether pressing Stop was safe — answering it needed a live query against their ComfyUI queue. `_render_state` now carries `started`, stamped when the slot is claimed, and the refusal reads *"A render is already running (3/16, started 9 minutes ago) — press Stop…"*; before the batch size is known it says "still starting up" rather than a meaningless `0/0`. `test_render_slot.py` 51 assertions (was 42).
- 2026-09-04 — **Two env-safety fixes found while checking whether changing `COMFY_URL` was safe (it is).** Owner rightly pushed back before touching a live variable, so the couplings got audited rather than asserted. Serverless generation reads `api.runpod.ai/v2/$RUNPOD_ENDPOINT_ID` and **never** `COMFY_URL` (`_runpod_endpoint_base`), and nothing in the launcher calls `ATLAS_BACKEND_URL` — so the production path is untouched by that variable. Two real problems surfaced anyway. **(1) The on-demand pod wake-up fired for every non-local render, including serverless ones**, where RunPod spins its own worker and a resumed GPU pod is billed for nothing; `runpod_control.targets_our_pod()` now ties the wake-up to the address the render is about to use, so it fires only for a render that really will talk to `RUNPOD_POD_ID` — which also makes changing `COMFY_URL` a no-op for it. **(2) `COMFY_CATALOG_URL` was live as `https://s3api-eu-ro-1.runpod.io s3://wvi855bwh8/`** — the Network Volume's S3 endpoint and bucket pasted together, space and all, so `urlparse` read the host as `s3api-eu-ro-1.runpod.io s3` and every probe failed silently; a value that cannot be an http(s) base is now ignored with a log line and pod discovery (#576) takes over, which is what works. `test_render_slot.py` 42 assertions (was 31).
- 2026-09-04 — **"My computer" was addressing a RunPod pod, and the panel described it as an unreachable tunnel.** Owner asked why a local render was "looking online": the strip read *My computer · Model lists unavailable — your ComfyUI has never answered at `https://pdn5pxpkrchofk-8188.proxy.runpod.net`*. Not a code bug — **`COMFY_URL` on Railway was set to a RunPod pod**. That var is what the `local` target resolves to (and `iw_common.comfy.comfy_url` documents it as "the full ComfyUI tunnel base URL"), so *My computer* pointed at a data centre while the message blamed the tunnel. `ui_server.local_target_misconfigured()` now recognises a RunPod host (`*.proxy.runpod.net`, `runpod.ai/io`) under the `local` target and says exactly that — in the strip **and** as a render refusal, **before** the reachability probe, because a RUNNING pod there would answer and render on the wrong machine, which is worse than not answering. `test_render_slot.py` 31 assertions (was 15). **Owner action:** set `COMFY_URL=https://comfy.invisiblewall.org` on the atlas-tool service (see the warning in `docs/INFRA.md`) — no code change can supply the right address.
- 2026-09-04 — **A blank REQUIRED model name is no longer stored, offered, or submitted — the other half of the blank-settings bug.** A day after #574 a FLUX render came back from RunPod as `/prompt rejected (400): vae_name: '' not in ['ae.safetensors', …]` on node `3` (`VAELoader`). #574 deliberately restricted its rule to NUMERIC keys so that a blank `flux_controlnet` / `flux_redux_style_model` could still disable an optional node — but that drew the line in the wrong place: `flux_vae` is TEXT **and** required. The right distinction is how the graph expresses "optional": by leaving the NODE OUT (`if FLUX_CHECKPOINT: … else: …`, the optional LoRA/ControlNet/Redux blocks), never by passing `""` to a node that runs — so any model name actually written into a graph is required. `batch_atlas.REQUIRED_MODEL_KEYS` now names those (checkpoint, rmbg_model, flux_unet/clip_t5/clip_l/vae/weight_dtype/sampler/scheduler/clip_vision, ipadapter_weight_type) and `blank_over_number` became `blank_shadows_default`, covering numeric ∪ required — so an existing poisoned config heals **on load**, with no re-save. The panel stops offering `(blank — none)` on a required field (it shows `(default: ae.safetensors)`), and `assert_models_named()` refuses to SUBMIT any graph carrying an empty model/enum input, naming the node, the ComfyUI input **and the setting** — scoped to enum inputs because plenty of string inputs are validly empty (FLUX's negative prompt is literally `"text": ""`). That verdict used to cost a queued RunPod job and a cold start. `test_config_defaults.py` 69 assertions (was 44). **Verified end-to-end**: with `flux_vae`/`flux_unet` blanked to the reported shape, `origin/main` built and SENT the graph, while the fixed tree healed both to their defaults and produced a complete one.
- 2026-09-04 — **⟳ finds a running RunPod pod by itself — no pod id to copy anywhere.** The RunPod side of the model dropdowns needed `COMFY_CATALOG_URL` set by hand to a `https://<pod-id>-8188.proxy.runpod.net`, which is exactly the kind of config that goes stale the moment the fleet changes (and the owner reasonably tried to point it at the Serverless endpoint, which cannot work: a worker has no address and exists only while a job runs). `runpod_control.running_pods()` now asks the RunPod API which pods are RUNNING — one GraphQL *query*, never `podResume`, so it cannot start or bill anything — and `comfy_catalog.pod_candidates()` derives each address from its id the way the launcher does, probing until one answers. Any running pod is valid: the fleet shares `Invisible_RunPod_Storage`, the same volume the serverless workers mount. `COMFY_CATALOG_URL` survives as an override, tried FIRST, for pinning a specific always-on reader. The messages changed with it — "start any pod on /comfyui, then ⟳ — nothing to configure" instead of naming an env var, and a pod that is booting is named rather than blamed on config. `test_comfy_catalog.py` 100 assertions (was 85), incl. that the first pod to ANSWER wins over the first listed, that a pinned URL outranks discovery, and that discovery never calls resume. Its harness also stopped dying on cp1252 when a FAILING assertion dumped panel HTML containing ⟳.
- 2026-09-04 — **A render could hang forever with no error; both halves of that are closed.** Reported as "my images are still processing and I am getting no error", with the local ComfyUI showing an empty queue AND an empty history — the job never arrived anywhere, yet the panel showed `Rendering 0/2...` indefinitely. Two defects had to line up. (1) **`batch_atlas.comfy_post` — the one call that submits the job (`/prompt`) — passed no `timeout`**, so `urlopen` fell back to `socket.getdefaulttimeout()` (None) and waited forever. A Cloudflare hostname whose tunnel connector is down still completes TCP+TLS and then never answers, so the read could not return: no exception, no end. Every other call in that module was already capped, and the comment above `_COMFY_HTTP_TIMEOUT` had made this exact argument for `comfy_get` — even naming `/prompt` as answering "in tens of ms"; `comfy_post` was simply missed. Now capped at `_COMFY_POST_TIMEOUT` (60 s), and `TimeoutError` joins the `except` so a mid-BODY timeout (which urllib does NOT wrap in `URLError`) reports "Cannot reach ComfyUI" instead of escaping as a traceback. (2) **`/render` answered `started` unconditionally**, even when it had not spawned a thread because `_render_state["running"]` was already set — and that flag only cleared in the render's own `finally`, which a hung subprocess never reaches. So after (1), every retry looked accepted and nothing ran, for the life of the container. `claim_render_slot()` now refuses out loud (the client shows it instead of discarding the response) and **self-heals** a flag left set with no live subprocess. `test_render_slot.py`: 15 offline assertions. **Verified against a TCP black hole** (accepts, never answers — the dead-connector shape): pre-fix still hanging when killed at 75 s, fixed exits at 63 s with the readable message.
- 2026-09-03 — **A blank numeric in Settings no longer shadows the engine default — it was killing every FLUX render.** Reported live: `ValueError: could not convert string to float: ''` from `build_workflow_flux`. Not a ComfyUI problem — a five-step chain in the settings round-trip: (1) `ui_server.load_config` reads `atlas_config.json` RAW (it does NOT merge `batch_atlas._DEFAULTS`), so any key a config predates rendered as an EMPTY box even though the render would have used 3.5; (2) `cfgData()` posts every `[data-cfg]` field, blanks included; (3) the save handler's GLOBAL branch stored that blank verbatim — the per-atlas, geometry and deploy branches each had an explicit blank rule, the global branch never did; (4) `batch_atlas.load_config` let `""` shadow the numeric default; (5) `float('')` raised. One Save on a project whose config predated a field was enough to kill generation for that project, with nothing in the UI showing why — and the recent Settings work (#571–#573) is what had people saving. **Fix, three layers:** `batch_atlas.blank_over_number` means an empty value never shadows a NUMERIC default (so every already-poisoned config in R2 heals on load, no migration) while TEXT keys keep blank-means-blank, because a blank `flux_controlnet` / `flux_redux_style_model` deliberately disables that optional node; `ui_server.apply_global_edit` DROPS a blank numeric instead of storing `""`, so the file self-heals on the next save; and the panel stops lying — an empty numeric box shows `default: 3.5` as its placeholder and a per-atlas field's `(inherit global: …)` names the value actually inherited (it read `(inherit global: )` while the render used 1024). `test_config_defaults.py`: 44 offline assertions incl. the verbatim traceback config. **Verified end-to-end**: the exact failure reproduced on the pre-fix tree, then the same poisoned config rendered past `build_workflow` on the fixed tree (failing only at the deliberately-dead ComfyUI port), and a live Save removed the four poisoned keys from the file.
- 2026-09-03 — **⚙ *Run generation on* — RunPod or my computer, per project; the model lists follow the choice.** New Global setting `run_on` (`""` = the service default from `COMFY_TRANSPORT`, `pod` = the RunPod serverless endpoint, `local` = your own ComfyUI over the tunnel). Applied as an env override on the render subprocess (`COMFY_TRANSPORT`) — the seam per-user routing already uses (`resolve_user_comfy_env`, which still supplies a registered user tunnel under `local`) — so there is no new transport code. An explicit *My computer* never calls `runpod_control.ensure_pod_ready` (a GPU the user just said not to use must not be woken) and preflights `<url>/system_stats` with the headers the render will use, so a machine that isn't answering fails in one readable sentence in the render panel instead of a stack of upload timeouts. **Two catalogs, never each other's:** `comfy_catalog` keys by target — `local` = live `COMFY_BASE` → `_shared/comfy/catalog.json`; `pod` = `COMFY_CATALOG_URL` only → `_shared/comfy/catalog-pod.json` — and the pod target never reads `COMFY_BASE`: it is the user's machine, and #571 as merged would have shown the tunnel's files for a render bound for RunPod whenever the owner's box happened to be up. The strip names the machine (*RunPod ·* / *My computer ·*) and, per tier, exactly what to start or set; ⟳ refreshes the selected machine's catalog. `test_comfy_catalog.py`: 85 assertions (was 56). **Verified locally** (R2-less boot + a fake ComfyUI as `COMFY_URL`): *My computer* → green, all 16 installed-file fields listed from the fake; *RunPod* → the fake untouched on render and on ⟳ (its request log did not move), red strip + ⟳ note both naming `COMFY_CATALOG_URL`; *My computer* with the fake down → `/render` ends at once with the one-line preflight. The real tunnel render is open item 9.
- 2026-09-03 — **The ＋ Blueprint importer and 🎛 Blueprint settings read the node's contract, in step with the Flipbook.** A setting's type, range and option list now come from ComfyUI `/object_info` (`comfy_specs.py`, `POST /video/nodespecs`) for the target ⚙ *Run generation on* selects, instead of a guess from the baked value — the same guess that let a `sensitivity` of 50 through on the Flipbook side; a bounded numeric is a slider + number box clamped on change; a dropdown re-reads its list each render (saved value kept, marked *(not installed)*). **⟳ Refresh model lists now also caches every published blueprint's select lists** for its target, so those dropdowns survive a stopped pod like the Settings ones do. Full story in [status/flipbook](flipbook.md).
- 2026-09-03 — **The dead "ComfyUI host:port" field is gone from Global settings.** In the cloud port `cloud_paths.resolve()` derives the ComfyUI address from the `COMFY_URL` env only (its own comment: a legacy field kept for compatibility) and `batch_atlas.COMFY_BASE` snapshots it at import — so editing the field changed nothing while its tooltip promised otherwise (found while verifying #571: pointing it at a fake ComfyUI did nothing until `COMFY_URL` was set). Removed the field, its `COMFY_HOST` proxy, the `DEFAULT_CONFIG` / `_DEFAULTS` seeds and the help text; an existing `atlas_config.json` keeps a harmless stale key. Where generation runs is a real choice, not an address, and that is the next Settings change.
- 2026-09-03 — **The Settings model/enum fields are dropdowns again — and they say where their values came from.** Root cause was NOT a missing feature: `ui_server` has always had `MODEL_FIELDS` + a `<select>` branch in `_control_html`, populated from `batch_atlas._available(node, field)` → ComfyUI `/object_info`. But `_available()` returns `None` the instant `_comfy_alive()` fails — a 3s GET to `{COMFY_BASE}/system_stats` — and production runs **`COMFY_TRANSPORT=serverless`** (the RunPod endpoint is a job queue, there is no long-lived ComfyUI HTTP server) with a blank `comfy_host`, so `COMFY_BASE` never answers. All 16 model fields fell back to a free-text `<input>` **with nothing on the page saying so**, which is what made it read as "the dropdowns were never built". Three changes:
  - **`ENUM_FIELDS`** (`ui_server.py`) — static seeds for the fields whose values are FIXED ComfyUI enums or our own options, so they need no network at all: `flux_weight_dtype`, `flux_sampler`, `flux_scheduler`, `ipadapter_weight_type`, the five `gpt_image_*` fields, and `atlas_format` (whose geometry-loop `<input>` now routes through `_control_html` like everything else). The seed is a **floor, never an override** — precedence is live `/object_info` > persisted catalog > seed.
  - **`comfy_catalog.py`** — caches the `/object_info` enum lists in R2 at the SHARED key `_shared/comfy/catalog.json` (what's installed is a property of the ComfyUI, not of a client/project), so the installed-file dropdowns keep working while nothing answers. `load()` fail-safes to `{}` on any R2/parse error and is parsed field-by-field (one junk value must not cost the whole catalog). `refresh(pairs)` probes `COMFY_BASE` when alive, else the new optional **`COMFY_CATALOG_URL`** (an always-on ComfyUI — e.g. the CPU volume pod the launcher already reads); it is GET-only so it never starts/resumes/bills a pod, and a probe that reaches nothing returns `ok:false` and **leaves the stored catalog alone** (an unreachable ComfyUI is not evidence the models are gone). A page render never PUTs; only ⟳ and one content-gated, rate-limited `commit_live()` do.
  - **The panel now explains itself** — a coloured status strip at the top of ⚙ Global settings (green *live from ComfyUI* / amber *cached <age> from <source>* / red *no ComfyUI has ever been reached … set `COMFY_CATALOG_URL` or start a pod*) plus a **⟳ Refresh model lists** button → `POST /refreshmodels` (`_bust_comfy_caches()` then `comfy_catalog.refresh()`), reporting source + list/value counts.
  Throughout, a configured value that no list carries is still offered and selected, marked `(not in ComfyUI)` for an installed-file list and `(custom)` for a static enum — saving the panel can never silently rewrite a setting. Covered offline by `test_comfy_catalog.py` (55 assertions: the three-tier precedence, value preservation on every tier, a failed refresh not overwriting a good catalog, malformed/absent/dead-R2 loads, and that a render is not an R2 write). **Owner live-verify owed** on Railway.
- 2026-09-02 — **`＋ New atlas` names the atlas in a dialog and confirms before replacing one.** The native `prompt()` gave no hint that the name was taken; the server then silently switched to the existing atlas with a ⚠ note, which read as "nothing happened". Now a modal (same `.modal`/`.modalbox` chrome as Variants/Advanced) shows the slug it will be stored under, warns live when that slug is already in the manifest dropdown, and Create on such a name asks for confirmation — OK sends `overwrite:true` (the `_newatlas` branch that already existed but nothing called) and replaces the manifest with an empty one; Cancel / Esc / backdrop / blank leave everything as it is. Sibling of the Sheet Maker's new-sheet dialog (#554). Esc closes it through the same global listener as the other modals. **Case:** manifests authored by the Sheet Maker keep their case (`atlas_manifest_F_Bottle_Exit.json`) while `r2_slug` lowercases, and on Linux those would be two files — so both the dialog's collision check and `_newatlas` match an existing manifest ignoring case and reuse its spelling (found on the first live pass: `F_Bottle_Exit` typed back was not flagged and would have made an `f_bottle_exit` twin). Verified locally (R2-less boot with two seeded manifests): warning, confirm/cancel, sanitising, Enter/Esc, a real create + reload. **Verified live on Railway** (2026-09-02, project `test6`, signed in via the launcher): the dialog opens, `f_bottle_exit` is flagged as the existing `F_Bottle_Exit`, and Esc leaves the active manifest untouched.
- 2026-09-01 — **Opening a Sheet-Maker sheet in the Atlas Maker stops stretching the art inside every
  region.** The rects stayed exactly right while the art in them was re-derived from its ink bounds
  and rescaled, so it read as a packing bug rather than a placement one — and it made the
  sheet-parity work above (`fit_mode:"contain"` → `_packer_compose_tile`) unreachable for the one
  route that needs it most.
  - **The handoff threw the contract away.** `ui_server._normalize_converted_region` rebuilt each
    region as a **closed geometry-only dict**, so `fit_mode` (and `prompt`/`shape_ref`/`seed`) never
    survived `import_sheet_to_manifest`. Only `sheet-tool/atlas_writers.py` writes
    `fit_mode:"contain"`, and only an EXPLICIT one routes to the parity path — dropping it dropped
    the sheet's placement.
  - **…and it synthesized the field that picks the worst fallback.** `orig_w`/`orig_h` defaulted to
    `w`/`h`, but `fit_to_region` reads their mere PRESENCE as `spine_slot`, which flips the default
    from `contain` to **`fill`** — alpha-crop, then stretch to the rect exactly. An untrimmed cell
    was thereby mistaken for a rig's authored footprint. Measured on a 493×501 cell holding 360×170
    of art: **×1.37 wide, ×2.95 tall, aspect 2.12 → 0.98.** Trim is now carried only when the source
    actually has it.
  - **Sliced-from-page art is marked `contain` at import.** The bound override IS the cell, cut at
    exactly `w × h`, so the faithful recompose is a verbatim paste. Scoped to the editor/seed-manifest
    branch — a raw TexturePacker/`.atlas` import keeps `fill`, where the rect is a rig's footprint and
    regenerated art must fill the slot the game already renders.
  - **The raw-TexturePacker route lands on `fill` too, and cannot be fixed at import.** The Sheet
    Maker's own `.json` declares `sourceSize` = the full cell (`trimmed:false`), so
    `_tp_frame_to_region` hands every cell an `orig_w`/`orig_h` and the same `spine_slot` default
    fires. That branch also carries genuine rig slots — including untrimmed ones, where `fill` is
    deliberate — so it cannot be blanket-switched.
  - **`repair_sheet_fit_mode` heals what is already damaged on disk.** The fixes above stop new
    damage; they cannot undo a manifest a pre-fix build already wrote, and re-clicking
    `Open in Atlas Maker` only re-activates that recipe. The repair restores `fit_mode` from the
    region of the same name in the **Sheet-Maker manifest that authored the page** (joined on the
    page BASENAME, which survives the `refs/atlas/` rewrite; identified by the `_comment` marker
    `build_manifest` stamps). Evidence, never inference: no authoring sheet manifest → nothing is
    touched, so a rig's `.atlas` keeps its `fill`; an explicit `fit_mode` is never overruled. Runs in
    the **compose pre-pass** (next to `rebuild_fx_layers` / `auto_pack_layout`, so no Create Atlas
    can run on a stripped manifest) and on **activation** via both routes — deep-link and Session
    dropdown — so `/atlasview`'s placement readout is truthful before anyone risks a compose. It
    covers BOTH damage routes because it sources the truth rather than guessing at import. Idempotent,
    and it reports what it repaired: a placement change nobody asked for should never be silent.
  - Regression fixtures: `services/atlas-tool/test_sheet_handoff.py` (26 checks) — pins the parity
    round-trip byte-identical, drives the repair through both damage routes, and asserts the failure
    mode still fails, so the checks can't pass for the wrong reason. Safety cases cover the untouched
    `.atlas`, the explicit-override, and the `rotated_regions` bucket.
- 2026-09-01 — **The New-blueprint modal binds whatever graph you import, not one shaped like the
  built-in three.** Ported from the Flipbook's video twin the same day it was fixed there
  ([status/flipbook](flipbook.md)), so the two hand-maintained modals are back in step.
  - **`bpCandidates` was a FILTER and `BP_FIELD` hardcoded the field per role**, which holds only
    while a graph's knobs are still widgets on the consuming node. Convert them to inputs — the
    prompt on a `PrimitiveString`, the seed on a `PrimitiveInt` — and the CLIPTextEncode the filter
    approves takes its `text` from a wire the role cannot write, while the primitive that actually
    holds the value is not offered at all. Both REQUIRED roles then have no usable option.
  - **`bpResolveKnob` follows the wire back to the widget**, through pass-through relays, and
    `bpCandidates` now RANKS (knob-through-a-wire, raw widget, wrong polarity) over a complete
    list — every `(node · input)` stays reachable under "All node inputs", wired ones marked.
    Bindings carry `node::field` from the select, so `BP_FIELD` is gone.
  - **Roles pre-fill only when the top rank is uniquely held**, and `(not used)` is now offered on
    required roles too — a required role used to default to whatever the filter approved FIRST, so
    a wrong binding could publish without the author ever looking at that row. `saveBlueprint`
    checks the required set before posting.
  - **A `text` param can declare `multiline`** and renders as a full-width textarea in the settings
    panel, auto-ticked when the baked value reads as prose. `bpInferType` reads the node CLASS
    first (a `PrimitiveFloat` holding `1` is a float, not an int).
  - **Two bugs the browser found that reading could not.** (a) The row prefill only ran
    `if(!key.value)`, and `addBpParam` ends by selecting the graph's FIRST node — so every row came
    out keyed after that node and defaulted to its value, and re-pointing the row changed nothing
    but the type. Now a field the author has not touched follows the node. (b) `bpSuggestKey`
    counted the row's OWN key when deduping, so each re-point appended a digit
    (`SaveAnimatedWEBP2`, `…3`).
  - Verified: the real JS lifted out of the `PAGE` template and run in node against the reported
    `WanLoopingVideo` export **and** a classic widget-shaped SDXL graph (the regression case —
    `width`/`height` still bind to `EmptyLatentImage`); then the tool booted locally and the actual
    modal driven in a browser, including a two-hop relay (`345.width → 380 → 355.value`), the
    settings-panel textarea, and the published payload through the real `validate_against_graph`.
- 2026-08-18 — **gpt_image and the FLUX ref/ControlNet path both work, on RunPod** (owner-tested). These were the two oldest blockers on this tool and they had the same root cause: they needed nodes/models installed on the **local** 4070 behind the tunnel (`Images to RGB` + `COMFY_ORG_API_KEY`/credits for gpt_image; FLUX ControlNets for shape_ref). Moving generation to the RunPod backend — whose worker image bakes the custom nodes and whose volume carries the models — retired the constraint rather than satisfying it. **The live Railway `atlas-tool` runs `COMFY_TRANSPORT=serverless`** (owner-confirmed 2026-08-18) — so the RunPod endpoint, not the local tunnel, is the production generation path, and the local 4070 + tunnel are no longer in the loop for Atlas Maker generation at all. Worth knowing when diagnosing: a generation failure in production is a RunPod/worker-image problem (cold start, missing baked node, endpoint quota), **not** "is Gualtiero's ComfyUI running".
- 2026-08-15 — **each serverless variant gets a unique filename** (`_next_variant_filename`). ComfyUI names saves with an incrementing per-output counter, but every serverless job lands on a FRESH worker whose counter restarts at `00001` — so each new variant of a region collided on `<region>_00001_.png` and overwrote the previous one. The transport now mirrors ComfyUI's scheme locally (highest existing id in the batch dir + 1, same `<region>_<NNNNN>_.png` shape the gallery globs). It also prefers the **`SaveImage`** output — the one whose worker filename is region-prefixed — over any preview/temp image the graph may also emit, so a preview never gets persisted as a variant.
- 2026-08-13 — **`batch_atlas` gained a second ComfyUI transport, selected by `COMFY_TRANSPORT`** ([design](../design/comfyui-serverless.md) §Pipeline changes, build-plan step 2). Unset / `http` (DEFAULT) = the unchanged live-ComfyUI path (`/upload/image` + `/prompt` + `/history` + `/view`); `serverless` = submit the SAME api-prompt graph to a RunPod Serverless endpoint (`RUNPOD_ENDPOINT_ID`/`RUNPOD_API_KEY`): `POST /run` with `{input:{workflow, images:[{name, image:<b64>}]}}`, poll `GET /status/{id}` (~2s, 30-min cap), decode the returned base64 image and persist it through the SAME `_persist_variant` path. The "which LoadImage refs, what filename" routing is factored into `_iter_workflow_refs` so both transports share it (`_upload_workflow_refs` for http, `_serverless_workflow_images` for base64). No behaviour change when `COMFY_TRANSPORT` is unset. _(The endpoint has since been created and generated for real — see the 08-15 and 08-18 entries above.)_
- 2026-08-11 — **the render subprocess now reads the SAME `atlas_config.json` the UI writes** (root-cause: selected blueprint / global settings never applied). `batch_atlas.load_config` read `SELF/atlas_config.json` (script dir — not shipped in the cloud image), while `ui_server` writes to `staging_root/atlas_config.json` (R2-backed, per client/project). So every GLOBAL-only setting — the **pipeline selector** above all — was stuck on its `_DEFAULTS` value (`sdxl`), silently generating built-in SDXL even when a blueprint was picked. `load_config` now prefers the staging copy (via `cloud_paths.resolve()['staging_root']`, resolvable in the subprocess from `IW_CLIENT_NAME/IW_PROJECT_NAME`), falling back to the script-dir file for the local tool. Diagnosed via the new `Pipeline: … (built-in|blueprint)` log line (#306) + the loud no-silent-SDXL guard (#305). PER_ATLAS settings were unaffected (they travel in the manifest via `apply_manifest_settings`).
- 2026-08-11 — **from-scratch atlases: generate/compose on a bare atlas block** (#301 follow-up) — a `pack` manifest's `atlas` block has no `source_image`/`width`/`height` until art is packed, so generation `KeyError`'d on `atlas["source_image"]` (it's only a built-in style-ref fallback; a blueprint ignores it) and composing before any art would `KeyError` on `atlas["width"]`. Generation now reads `atlas.get("source_image") or ""`; compose-before-any-art returns a readable "nothing to compose yet" line. Live-verified path: New atlas → single region → blueprint render.
- 2026-08-11 — **from-scratch atlases (auto-pack)** — `＋ New atlas` / `＋ Add region` / per-card 🗑 + a packed Create Atlas (`pack.py` + `auto_pack_layout`) let you build an atlas from prompts with no Sheet Maker, region sizes emergent from trimmed art. Shipped #301; page auto-crops to used bbox (2048 = cap). Owner live-verify continuing.
- 2026-07-16 — **a Sheet-Maker cell now composes byte-identically to its sheet** (explicit `fit_mode:"contain"` → `_packer_compose_tile`), so republishing an atlas stops re-offsetting rigged art. Needs an `atlas-tool` redeploy + a re-Create-Atlas to take effect on existing pages ([detail in history](../history.md)).
- 2026-07-16 — **Region Overlay Inspector: retracted the unsound producer inference, added sheet parity + placement mode.** The `FILLS`/`INSET` badge was being presented as a producer verdict ("one page, two producers") — it cannot know that (full-bleed art and FX halos FILL under either composer), so the summary/legend/docs now describe only what is measured. Replaced as the headline by the **recompose test** (`/parityscan`): re-run the region's source art through the real `_packer_compose_tile` and diff against the page. Offline-proven on a known-mix page, 7/7: sheet-composed → MATCHES SHEET `max Δ 0` (incl. rotated); atlas-fill-composed → DIFFERS `2.73x, origin (38,38)→(0,0), max Δ 255`; legacy cell-grid contain → DIFFERS (same); explicit cover → DIFFERS `2.14x×3.33x`; **full-bleed sheet-composed → FILLS *and* MATCHES SHEET** (the false positive that motivated the change); no variant → NO SOURCE. Parity is exact, so `PARITY_TOL = 2` is slack for a lossy round-trip, not a real need.
- 2026-07-16 — **`🖼 View atlas` now opens the Region Overlay Inspector** (`/atlasview`) instead of the bare page bytes, making the rect-convention difference visible + screenshottable. Same art + same 100×100 rect measures 46×46 under the packer vs 100×100 under `fit_to_region` (2.17×, origin 27,27 → 0,0).
- 2026-07-16 — fixed FX layers composing offset + shrunk against their base (`fx_registration_crop`); needs an `atlas-tool` redeploy + a re-Create-Atlas to take effect on existing pages.
- 2026-07-06 — sheet-derived FX cells now auto-derive on load/Process instead of being AI-generated ([detail in history](../history.md)).
- 2026-05-30 — FLUX proven live (~105s txt2img); B10 `.atlas`/source upload + R2 ref picking landed ([detail in history](../history.md)).
- 2026-05-29 — R2 seeded (1158 objects); tool hydrates + lists manifests ([detail in history](../history.md)).
