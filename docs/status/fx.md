# Invisible FX — status

> Design: [docs/design/invisible-fx.md](../design/invisible-fx.md) · Guide: [docs/tools/fx.md](../tools/fx.md) · Agent: `.claude/agents/invisible-fx.md`

**One-line state:** Shipped on `main` — all three particle tiers + the event trigger, a pro emitter inspector (now with per-particle **Min / Max** ranges, a colour overlay, a rotation lock and flipbook speed/loop), layer stack ops, a preset library, and rig-timeline direct binding are live; remaining work is the Tier-C perf ceiling live-verify, the Borut submodule bump, and an owner browser-pass over the reworked inspector.

## Current state
Live on `main`, owner-verified in the 2026-06-29 pass except the ⏳ items called out below.

- **All three tiers ship** (`/fx`, `fx` tool scope): **A** sprite/flipbook particles from project atlas regions, **B** an emitter pinned onto a bone of a playing Spine backdrop (`SpineBoneAttach`), and **C** whole Spine clips *as* the particles (pooled `SpineParticleBehavior`). The `skeletonKey` key-stability seam for Tier-C shipping is closed.
- **Event trigger end-to-end.** A `trigger.on:'event'` layer fires when a matching cue crosses the runtime event bus (a Flow v1 Broadcast / v2 `fireCue`), with an optional **Stop event** and **Duration**. The full author→ship→fire chain (export → `deploy/effects/` → bake → pull → `bakedEffects()` register) is wired.
- **Pro `/fx` inspector knobs:** emission **direction (centre °) + spread (±°)**, particle **spin min/max + accel**, and a **rotation lock** (`noRotation` — travel along the arc without facing it); a **movement** toggle (eased `moveSpeed` vs **gravity/acceleration** — up-direction + downward gravity = a fountain); **colour** start/end tint **plus a colour overlay with a per-particle intensity min/max**; **blend mode** (Normal/Add/Screen/Multiply); five **spawn kinds** — point / circle / ring / rectangle / **burst** (even fan). Alpha, Scale and the eased Speed each carry an **On** switch (a config with no such behavior can grow one instead of the section silently vanishing) and a **Min / Max** switch that splits each end into a per-particle range — four INDEPENDENT bounds (see the 2026-08-28 fix entry). All are live sliders + number boxes that re-tune the preview verbatim.
- **Layer stack ops** — ▲/▼ reorder (list order IS draw order; `FxStage` re-appends the emitter containers in doc order each rebuild so the preview restacks), ⧉ duplicate, and a `localStorage`-backed **⧉ Copy / 📋 Paste** that survives navigation, so a layer moves **between effects**.
- **Flipbook playback is authorable** — an `animated` layer chooses **Match particle lifetime** (`framerate: -1`, the long-standing default) or an explicit **fps + loop**, carried on `art.framerate`/`art.loop` through `bindArt` at all four render sites.
- **Preset library** — per-layer "Apply preset…" (Fountain, Fire, Smoke, Sparks, Explosion-burst, Rain, Snow, Confetti, Magic glow) preserving art/placement/trigger.
- **Rig-timeline direct binding** — on a Rigger event keyframe you pick the effect, its host bone, the SLOT it draws at, and per-binding opacity / size / delay / duration / speed, and a **Continuous** flag that stops a looping clip restarting an ambient burst every lap (`evtObj.fx`), baked to a `rigFx` manifest and played by `RiggedEffect` on the beat (`forceEmit`); the cue-broadcast path stays as the advanced mode. A *persistent* rig FX slot (an always-on, keyable emitter — the other half of invisible-cinematic §12.4a) is still NOT built.
- **Live particle previews everywhere** — the effect plays not just in `/fx` (`FxStage`) but in the **Rigger** and **Symbols state machine** via the shared `fxOverlay` on their raw-WebGL stages, all now riding the bone's **full transform** (position + rotation + per-axis scale) to match the game.
- **Effect atlases auto-ship** — `exportEditorArt` walks the project's effects and ships every layer's `art.assetKey`, so a placed/bound effect's particles have textures with no extra step.

⏳ **Still open (live-verify):** Tier-C spine-particle **GPU/perf ceiling** in a published game (expect tens, not hundreds); owner pixel-verify of the rig-bound FX size/rotation on live WebGL across `/symbols`, `/rigger`, and the game.

## Open items / next
1. **Owner browser-pass over the Min / Max rows.** Two passes have now happened and each found a real defect (see the two 2026-08-28 fix entries) — the model is heavily covered headlessly, but every symptom so far has been a CLICK, and `/fx` is auth + Postgres + R2 gated so those sliders still cannot be driven from a session. A third pass on the corrected rows is owed.
2. **Tier-C perf ceiling live-verify** — confirm a published game renders a spine-particle effect at 60fps and find the pool-size ceiling; flipbook-bake stays the per-effect fallback.
3. **Borut `engine` submodule bump** (owner) — once the Tier-C pixels are confirmed, Book of Borut bumps to ship the runtime FX (it has its own bundle; the shared `_runtime/lines` is already published).
4. Consolidation candidate: **three FX renderers** (engine `SpineBoneAttach`, `/fx` `FxStage`, tool `fxOverlay`) are kept in sync by hand — a future unify. The 2026-08-28 change added a FIFTH thing they must each do (`registerFxBehaviors`), mitigated by routing all three launcher stages through `effectEmitter.client.ts`.

## Blocked (owner / external)
- **Shipped-game submodule bumps** are owner-owned (Book of Borut) — see item 3.
- **Owner live-verify** of the WebGL pixels / Tier-C perf is the gate on marking those items done.

## Recent changes
- 2026-08-28 (fix 2) — **The Min / Max mins are stored ABSOLUTE, and all four thumbs share one axis.** Owner-reported after fix 1: "when I move the start and end MAX, the min also moves". Two causes, both in the presentation of an otherwise-correct model. (1) Each `min` slider was capped at ITS OWN `max`, so dragging the max **rescaled the min's track** — the min thumb slid from 45% to 25% of its groove while its value never changed. All four sliders now share the section's axis (`lo`..`hi`), and overshoot is clamped in `setCurveBound` where it belongs. (2) The floors were stored as RATIOS (`startMult`/`endMult`) and the absolute redisplayed as `max × ratio`, so every drag tick re-derived the min through a divide-then-multiply — invisible in the model tests, but a float smear in the number box and one more place for the two to couple. They are stored as the ABSOLUTE numbers the author typed now (`startMin`/`endMin`); the behavior derives its own ratio at init. Both legacy forms (`startMult`/`endMult` from fix 1, and the library's whole-curve `minMult` in the presets) are still read and migrate on first edit, so nothing already saved renders differently. Verified by simulating real drags tick-by-tick — an 86-tick max drag up and back leaves the min bit-identical. `tools/fx-spike/variation.ts` is at 121 assertions; the eleven other harnesses stay green. Still NOT browser-verified (auth-gated `/fx`).
- 2026-08-28 (fix) — **Min / Max: four bounds that are actually four bounds.** The first cut mapped the inspector's four boxes onto the library's THREE degrees of freedom (one whole-curve `minMult`) and derived the mode from the numbers. Owner-reported, both real: (1) on Scale, clicking a `min` made the sliders vanish and unticked the box — dragging a min to/above its max, or against a zero `End max`, pinned the ratio to 1, and `varied` was `minMult < 1`, so the mode switched ITSELF off; (2) on Movement, moving one end dragged the others, because `startMin`/`endMin` were both derived from that single shared ratio. Fixed at the root: `fxScale` + `fxSpeed` join `fxAlpha` in `engine-fx/src/behaviors.ts`, each mirroring its stock twin but carrying `startMult` **and** `endMult` — two independent floors, drawn per particle and interpolated across its life, so `t=0` lands in `[curve(0)·startMult, curve(0)]` and `t=1` in `[curve(1)·endMult, curve(1)]` independently. Multi-point curves keep their shape (the factor scales the whole list). `varied` is now keyed off the behavior TYPE, never the numbers, so only the toggle can turn the mode off; each `min` slider tops out at its own `max`; and dragging a `max` holds its own `min` at its ABSOLUTE value. A stock `minMult` (the presets, and anything authored before the split) is read as both floors and migrated on first edit, so nothing re-renders differently until it is touched. `fxSpeed` reproduces `SpeedBehavior`'s integration exactly, with one deliberate improvement: the launch direction is stored as a UNIT vector, so a curve starting at 0 cannot hit the stock `normalize()` divide-by-zero. Verified: `engine-fx` typecheck, launcher `tsc` clean over every FX module, `pixi-svelte` + launcher builds, and `tools/fx-spike/variation.ts` grown to ~110 assertions — including a regression block that reproduces BOTH reported bugs and closed-form checks of the new behaviors' particle output. The eleven other fx-spike harnesses stay green. Still NOT browser-verified (auth-gated `/fx`). **Engine change on `main` — reaches shipped games only via a runtime-bundle republish + `engine` submodule bump.**
- 2026-08-28 — **Per-particle Min / Max everywhere the library can express it, a colour overlay, spin accel + a rotation lock, flipbook speed/loop, and layer stack ops.** The inspector's biggest gap wasn't a missing knob, it was that a section VANISHED when its behavior was absent (`{#if alphaEnds}`) — so a layer without an alpha curve gave the author no way to add one. Every curve section now carries an **On** switch, and Alpha/Scale/Speed a **Min / Max** switch. The variation itself is the library's own model: `minMult` on `scale`/`moveSpeed` (a random multiplier in `[minMult, 1]` drawn once per particle and applied to the whole curve), so `min` writes the ratio and `max` writes the authored endpoint — and, unavoidably, **one ratio governs both ends of a curve**, which the UI says out loud rather than faking independent bands. `alpha` is the one curve the library gives no `minMult` and NOTHING randomises tint, so `engine-fx/src/behaviors.ts` adds exactly two custom behaviors — **`fxAlpha`** (the stock alpha config plus `minMult`; the two swap by TYPE alone, so variation-off configs stay 100% stock) and **`fxColorOverlay`** (a colour blended in at a per-particle intensity, running at order `Late` so it composes over `color`; it remembers the tint it last wrote, so with no colour behavior present it can't compound its own output into a runaway tint). They are import-free classes and `registerFxBehaviors(Emitter)` takes the class as an argument, keeping `engine-fx` PixiJS-free; the runtime registers in `<ParticleEmitter>` and all three launcher stages inherit it from the shared `effectEmitter.client.ts`. Also: `art.framerate`/`art.loop` (an EffectDoc schema addition — absent ⇒ the old match-life default, so every existing effect is byte-identical) threaded through `bindArt` at all four call sites; spin **accel** and a `noRotation` **angle lock** exposed; **▲▼ reorder / ⧉ duplicate / Copy-Paste-across-effects** in the Layers panel, with `FxStage` re-appending emitter containers in doc order each rebuild so a reorder actually restacks the preview (it only ever added them at creation). Verified: `engine-fx` typecheck, launcher `tsc` clean over every FX module, launcher build, and a new headless harness `tools/fx-spike/variation.ts` (`pnpm --filter fx-spike run variation`, 80 assertions) covering purity, the alpha↔fxAlpha swap, the ratio math, the two behaviors' actual particle output, `registerFxBehaviors`, flipbook binding, layer ops, and save→reopen through `normalizeEffectDoc`. All ten pre-existing fx-spike harnesses stay green. **NOT browser-verified** — `/fx` is auth + Postgres + R2 gated (open item 1). **Engine change on `main` — reaches shipped games only via a runtime-bundle republish + `engine` submodule bump.**
- 2026-08-27 — **A rig binding now carries draw depth + five modifiers, not just an effect and a bone.** `evtObj.fx` gained `slot` (draw at that slot's place in the skeleton draw order, via spine-pixi `addSlotObject`) plus `alpha` / `scale` / `delay` / `duration` / `speed`, all optional and all ABSENT by default so every rig baked before them is byte-identical. `readRigFxOverrides` in `engine-layout` is the single clamp the bake, the runtime registry and both live previews read through, and `rigFxExport.ts` now imports the binding type instead of hand-mirroring it. `duration` closes a real preview-vs-game divergence: `forceEmit` started emission and nothing ended it, so a continuous effect fired from a keyframe emitted forever in-game while the previews capped at ~1.5s. Guarded by `check:rig-fx-overrides` (mutation-verified). Detail + the owed in-game pixel check in [status/rigger](rigger.md).
- 2026-08-20 — **The Tier-B backdrop skeleton never loaded: `loadFxSpine` fetched its atlas pages with `Assets.load`.** Pixi's resolver picks a loader by the URL's apparent EXTENSION and drops the query string first, so `/spine/file?dir=…&name=….webp` reads as extension-less — the load resolves to `null` and reading `.source` off it throws, aborting the skeleton. Exactly the trap the emitters' own page loading was fixed for in July (`/api/editor/asset?key=…`), in the one place that still called `Assets.load`; it now goes through the same `loadPageSource` (fetch → `createImageBitmap` → `ImageSource`). Surfaced via the admin boot-mark preview, which reuses this helper — full story + the live verification in [launcher.md](launcher.md).
- 2026-08-03 — **Preview↔runtime FX speed mismatch fixed — the `/fx` editor now plays effects at the EXACT game speed.** The in-game runtime (`ParticleEmitter.svelte`) advanced the emitter by `deltaMS * 0.00234` (~2.34× real seconds), but the `/fx` preview (`FxStage.svelte`) used `deltaMS / 1000` (1× real seconds) — so an effect ran ~2.34× faster in-game than the editor showed, the drift behind mis-timed authored delays. Both now advance through ONE shared scalar: new `packages/engine-fx/src/playback.ts` — `DEFAULT_EMIT_SPEED = 0.00234`, `emitterDeltaSeconds(deltaMS, emitSpeed?)`, and `emitterSecondsToWallMs(emitterSeconds, emitSpeed?)` (emitter-lifetime → true wall-clock ms; `1/0.00234 ≈ 427ms` per emitter-second). All three emitter-driving renderers now call `emitterDeltaSeconds` — the runtime `ParticleEmitter.svelte`, the `/fx` `FxStage.svelte`, and the shared Rigger/Symbols `fxOverlay.client.ts`; each preview's backdrop SPINE stays on real seconds (a spine clip is wall-clock). **Effects now appear ~2.34× faster in the editor than before — that is the correct, game-matching speed, not a regression.** `emitterSecondsToWallMs` also backs Flow v2's new `showContainer.durationMs` pin (an FX node's contribution to a screen's duration). Verified: `engine-fx` typecheck clean + offline scalar math. NOT browser-verified (auth-gated `/fx`). **Engine change on `main` — reaches shipped games only via a runtime-bundle republish + `engine` submodule bump.**
- 2026-07-14 — `/symbols` + Rigger `fxOverlay` now apply the bone's full transform, matching the game ([detail in history](../history.md))
- 2026-07-14 — rig FX rides the bone's full transform (rotation + scale) via `followRotation`/`followScale`; published to `_runtime/lines` ([detail in history](../history.md))
- 2026-07-14 — fixed `SpineBoneAttach` bone-follow drift in a scaled game (map world→parent-local) ([detail in history](../history.md))
- 2026-07-13 — live runtime bundle now ships FX (effects + rigFx), parity with the offline bake ([detail in history](../history.md))
- 2026-07-13 — rig FX render fixes: cross-talk firing + effect-ignored-rig-transform in `RiggedEffect` ([detail in history](../history.md))
- 2026-07-10 — rig-timeline DIRECT binding (pick an effect on a Rigger keyframe) + live particle preview in the Rigger ([detail in history](../history.md))
