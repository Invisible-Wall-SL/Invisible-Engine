# Project status & roadmap

> Where things stand and what's left. Update this at the end of meaningful work.
> Last updated: 2026-05-31.

There are two tracks in this repo:
1. **Engine & games** — the Stake-Engine fork (pixi-svelte, Svelte 5, PixiJS 8) + the games.
2. **Studio / pipeline tools** — the launcher portal + the cloud-hosted asset tools (atlas) + ComfyUI pipeline.

---

## Track 1 — Engine & games

- **RGS translator (Play4Fun):** ✅ done. `packages/rgs-translator-eagaming` maps the Stake internal request shape to the Play4Fun `/rgs/engine` protocol (verified from real Hot Fruits captures). Stake-shaped facade lets `apps/lines` run unmodified against a local mock via `PUBLIC_RGS_TRANSPORT=play4fun`. See root `CLAUDE.md` for protocol details.
- **Games:** `apps/{lines,cluster,scatter,ways,number-picker,price}`. New game **Book of Borut** (evolution of Hot Fruits, Book-of slot type) lives in its own repo with the engine as a submodule (port 3002).
- **Rule:** engine changes go on `main` — never per-game engine branches. Don't dismantle the Turborepo/pnpm-workspace structure.

## Track 2 — Studio / pipeline tools

### Done
- **Invisible Sheet Maker (`services/sheet-tool`) — project-file workflow** ✅ code (2026-05-31). Added a project-file model to the Sheet Maker. The single editable **Sheet name** is now the project identity (removed the separate "Base filename" field) and drives both the output folder and the `atlas_manifest_<name>.json` filename. **Load existing** now lists only sheet projects (`atlas_manifest_*.json`) + folders; raw `.atlas`/TexturePacker/PNG moved behind an **Import…** toggle (`api_browse ?mode=import`). `api_load` now returns `source_path`/`source_dir`/`name`/`is_project` so the loaded file identity persists. UI shows an always-visible "Editing: …" line + a live "Saves to: …" destination readout. Replaced the single Export button with **Save** (overwrites the loaded manifest in place via `dest_dir`) and **Save As** (writes a new project under the current name) — editing the name before Save forks via Save As rather than clobbering the original. Manifest JSON schema + the exported file set are unchanged (still compatible with Atlas Maker / Spine / engine).
- **Invisible FTP Browser — project-scoped R2 file manager** ✅ code (2026-05-31). New online launcher tool (`ftpBrowser`, `/files`) — an in-launcher full-page UI (NOT a redirect) to browse + manage the R2 bucket: navigate folders, download, upload, overwrite, delete (single file + recursive folder), and move/rename (copy-then-delete; R2 has no native move). **Strictly scoped to the active client/project** — the same security model as the editor asset endpoint: a shared `gate()` (`src/lib/server/ftpScope.ts`) requires login + the `ftpBrowser` tool entitlement and resolves `(client, project)` from the **session-bound** active project (never a request param); `assertAllowed()` rejects `..`/leading-`/`/empty and requires every key to start with one of `allowedPrefixes(c,p)` = the project's OWN `atlas_maker|sheet_maker|localization|editor|spines/<client>/<project>/` prefixes (no `spines/_shared/`, no cross-project). Delete/move on a folder re-validate every server-enumerated key + computed destination (defense in depth); upload sanitizes each filename to a bare basename + caps the request at 200 MB. **Files:** `src/lib/server/ftpScope.ts`; `src/routes/api/files/{list,download,upload,delete,move}/+server.ts` (each self-gates); `src/routes/(app)/files/+page.{server.ts,svelte}` (client-driven via fetch); extended `src/lib/server/r2.ts` (`putObjectBytes`, `deleteObject`, `deleteObjects` ≤1000 batches, `copyObject`, `listAllKeys` recursive-paginated, `listFolder` with size+lastModified — `listObjects` left untouched). Registry: `ftpBrowser` added to `TOOLS`, to `developer` in `ROLE_TOOLS` (admin gets all), + doc slug. Doc: `docs/tools/ftp-browser.md`. Default roles admin + developer (configurable via the live role matrix). `pnpm --filter launcher-api build` GREEN. **NOT browser-tested against live R2:** verify binary upload, recursive delete/move + `Load more` pagination, and that R2 decodes the `%2F`-encoded `CopySource` (move/rename) correctly.
- **atlas-tool — `rembg` on/off toggle for full-bleed backgrounds** ✅ live (2026-05-31). SDXL + FLUX builders always ran the RMBG subject-cutout, so a full-image background got alpha-erased. New `rembg` setting (default ON = transparent icons, unchanged): OFF saves the raw VAE decode (node 14) and drops the RMBG node → opaque full-bleed render. Per-atlas overridable (on/off select, blank = inherit) + per-region. `REMBG` normalized to bool at load; both builder call sites coerce via `_truthy`. Runtime-verified (rembg=off → no node 16, save from 14). For a Ghibli background: pipeline=sdxl (not the unverified FLUX), rembg=off, LoRA off, ControlNet off unless a real composition sketch is supplied.
- **atlas-tool — SDXL style transfer is now opt-in (no hardcoded mockup)** ✅ live (2026-05-31). `mockup_image` defaulted to the Hot Fruits leftover `hotFruits_MockUp.png`, so every SDXL region without its own `style_ref` silently fed that file through the IPAdapter — and HTTP 400'd when the file wasn't in staging. Two commits: (1) `_locate_ref_in_staging()` resolves a bare/abs/`refs/`-relative LoadImage ref before giving up (replaces the `INPUT_DIR/<relpath>`-only check in `_upload_workflow_refs`); (2) emptied the `mockup_image` default + `build_workflow` now resolves `style_ref` like the FLUX builder (`region.style_ref` → `mockup_image` if set → none) and only adds the IPAdapter chain (nodes 3/5/6) when a style ref exists — KSampler reads its model straight from the LoRA loader otherwise. UI help updated (blank mockup = skip style transfer). py_compile clean; pushed to `main`.
- **Launcher** (`apps/launcher-api`) live at `app.invisiblewall.org`: invite-only email+password auth (scrypt), sessions in Postgres, roles + per-role tool manifest (`src/lib/roles.ts`), full-page tool pages.
- **`/admin` — admin-only management panel** ✅ code (2026-05-30). Role-gated (`requireAdmin` on the load + all 10 form actions). Manages users (create/role/enable-disable/delete), credentials (scrypt password reset), **login expiry** (`users.expires_at`, enforced at login AND session validation in `auth.ts` — expired = denied + session dropped), **per-user tool access** (`user_tool_access` table layered on `ROLE_TOOLS` via `effectiveToolIds`, enforced server-side on every tool gate), and **session management** (list/revoke). Lockout guards: can't demote/disable/expire/delete the last usable admin or your own account. Project-access section stubbed ("coming with B12"). Migration `drizzle/0001_foamy_firestar.sql`. **Schema applied to the `production` env DB + verified live 2026-05-30** (`/admin` 303-gated, login restored). A first admin must already exist (no self-service admin creation).
- **B12 — project-centric launcher** ✅ live (2026-05-30). The launcher is project-aware: `projects` registry + `user_project_access` + `sessions.active_project_key` (migration `0002`, applied to the production DB **before** the code deploy). Active-project selector in the launcher header (per session); accessible projects = all (admin) or grants + default `cloud`. `/atlas` redirect forwards `&project=<active>`. Admin panel gained project create/rename/delete (`cloud` protected) + per-user grant/revoke (replacing the B12 stub). **Tool side:** `atlas-tool` + `sheet-tool` `cloud_paths` switch project at runtime per request (`?project=` → cookie → env default), re-hydrating staging from the new R2 prefix under a lock (scoping model A). Verified: launcher build + tool py_compile clean; live health stable post-deploy (login 200, no 500). **Not yet:** browser test of an actual project switch; cross-project ref picking via `/fsbrowse` other prefixes.
- **Invisible Editor — build plan COMPLETE (steps 6–10 of 10)** ✅ committed (2026-05-30). Step 6: selection + transform handles + snap. Step 7: properties panel + outliner + per-`layoutType` override mode. Step 8: save action + debounced autosave + dirty/saved status. Step 9 (`24aa308`): the engine-side contract in `apps/lines` (`<LayoutScene>` from `engine-layout/svelte` + `registerBoundComponents({ Win, Transition })`). **Step 10 (`3b1f5b2`, final):** end-to-end contract proof. New `apps/lines/src/editor-scenes.ts` is a faithful checked-in stand-in for the editor's R2 doc (`editor/<client>/lines/scenes.json`; round-trips through `editorStorage.normalizeDoc`; `mainSizesMap` mirrors `stateLayout`). Two scenes — `basegame` ports `frame_bg.png`+`frame_edge.png` out of `BoardFrame.svelte` into `<LayoutScene>` with per-`layoutType` coords baked from the resolved `boardLayout()` (750×432, centre×1.01) + a layout-driven label; `basegameOverlays` mounts the coded `Win`+`Transition` via `bind` at App-root scope. `BoardFrame.svelte` keeps only the animated reelhouse glow; the step-9 `sample-layout.ts` fixture was deleted. **Verified live** (`pnpm --filter lines dev`, port 3001, Play4Fun transport): frame renders with parity, the layout label shows, zero console errors, `Win`/`Transition` mount via `bind`. (A winning spin wasn't exercised — the Play4Fun mock wallet was unfunded; balance is seeded by passing the amount as a string.) **Decision taken:** checked-in fixture for the smoke. **Runtime doc fetch — DONE (`6fd7839`):** new launcher `GET /api/editor/doc?project=&k=` (read-only, token-gated by `EDITOR_DOC_SECRET` — 503 when unset so it's never public; CORS-open since the token is the gate; resolves client from project → `editorStorage.loadDoc`). The launcher home appends `&k=<secret>` to game URLs (mirrors atlas/sheet `?k`). `apps/lines` `loadEditorScenes()` fetches the doc at boot (base `app.invisiblewall.org`, override `?editorDocBase=`; uses the `?project=`/`?k=` the launcher appends), with `editor-scenes.ts` as the offline fallback (`Game.svelte` seeds `$state` with the fallback, swaps to the fetched doc when it has a `basegame` scene). Verified: builds clean; live boot fires the fetch + degrades gracefully. **NEEDS OWNER:** set `EDITOR_DOC_SECRET` on the launcher Railway service (until then the endpoint 503s and games use the fallback). **Still pending:** seed a real `editor/<client>/lines/scenes.json` in R2 (via the editor UI) to see the non-fallback path render; port the engine consumer into the real shipping game (Book of Borut, engine as submodule — "step 9b/10b").
- **Invisible Editor — canvas + drag-spawn (step 5 of 10)** ✅ code (2026-05-30). New `EditorCanvas.svelte` mounts a DPR-scaled `<canvas>` 2D context (lightweight choice: launcher has no PixiJS dep; the Spine Viewer's vendored runtime is read-only). Reuses `engine-layout.resolveTransform`; renders the active scene's authoring frame (`mainSizesMap.desktop`) + nodes; sprites lazy-load via the new gated endpoint (cached `Image` map); spine/text/container kinds render placeholders/text/recurse. Zoom (wheel, cursor-anchored 0.05–8×) + pan (shift/middle/right drag) + Fit button. Asset library `<li>`s draggable (`data-asset-*` + `application/x-iw-asset` payload); canvas dragover/drop converts client→world coords and spawns a `LayoutNode` into the active scene. New `GET /api/editor/asset?key=…` (auth + 'editor' tool gated; key must start with one of the active project's R2 prefixes — `atlas_maker/<c>/<p>/`, `sheet_maker/<c>/<p>/`, `spines/<c>/<p>/` + `spines/_shared/`; rejects `..`/leading `/`; streams from R2 with extension-derived Content-Type). NOT yet: selection, transform handles, snap, properties wiring, autosave, real Spine playback, atlas-manifest region parsing, scene management, per-`layoutType` editing (steps 6–8 + future). Build clean locally; live deploy of the new endpoint pending Railway propagation.
- **Invisible Editor — route shell (step 4 of 10)** ✅ live (2026-05-30). `/editor` route gated by auth + role/user tool overrides (`editor` tool); load resolves `(clientKey, projectKey)` from session+DB (`'unassigned'`/`'cloud'` fallbacks) and fetches the project's `LayoutDoc` (`editorStorage.loadDoc`) + asset listing (`projectAssets.listProjectAssets`) in parallel. Svelte 5 shell with Emblem header, `Project: <c>/<p>`, scene/asset counters, 3-pane CSS grid (asset library / canvas placeholder / properties panel). No interactivity — canvas mount in step 5. Agent flagged for step 5+: assets need public URLs + manifest frame metadata + spine bundle file resolution before drag-in is wireable.
- **Invisible Editor — foundation (steps 1–3 of 10)** ✅ live (2026-05-30). Step 1: new `packages/engine-layout` with `LayoutDoc`/`Scene`/`LayoutNode` types + `<LayoutScene>` stub component (walks the tree → pixi-svelte primitives, resolves per-`layoutType` overrides) + per-game `registerBoundComponents`. Step 2+3: launcher gains `editorStorage.ts` (R2 load/save of `editor/<client>/<project>/scenes.json` with `normalizeDoc`; empty default matches the scaffold seed; `updatedAt` stamped only on save), `projectAssets.ts` (lists atlases — manifests + composed pages, spines per-project + `_shared/` fallback, sheet outputs; cap 500 per kind), and `GET /api/editor/assets` (auth + `editor` tool gate). New `editor` entry in the tool registry (online, url:`/editor`; default for developer + artist). NOT yet built: the `/editor` page route + canvas + drag/select/properties/autosave (steps 4–10).
- **Per-client R2 isolation + canonical project scaffold** ✅ Phase A + code deploy (2026-05-30). All tool prefixes are now `<tool>/<client>/<project>/...` (atlas_maker, sheet_maker, localization, editor, spines), with `unassigned` as the reserved default client. New `projectPaths.ts` (launcher single source of truth) + `projectScaffold.ts` (idempotent seed of atlas_config.json, sheet_config.json, strings.json, scenes.json + `.keep`s). Admin `createProject` takes a client + auto-scaffolds; new `rescaffoldProject` action; key (slug PK) renames forbidden. Tool sides: `cloud_paths` refactored to `set_context/switch_context(client, project)`; per-request resolution = `?client=&project=` → cookies `iw_client`/`iw_project` → env defaults; atlas-tool `ATLAS_OUTPUT_PREFIX` removed (locked to `<project>`); subprocess env handoff passes `IW_CLIENT_NAME` + `IW_PROJECT_NAME`. Migration script `scripts/migrate-r2-client-isolation.py` (boto3, `--phase {copy,delete,both}`, `--dry-run` default). **Phase A done live**: 1158 objects copied from `atlas_maker/cloud/cloud/` to `atlas_maker/unassigned/cloud/`. Code deployed; all three services verified healthy. **Phase B done (2026-05-30)**: 1158 old objects under `atlas_maker/cloud/cloud/` deleted after the new path was verified live in the atlas-tool UI (all 10 manifests + existing outputs loaded correctly). R2 migration to per-client isolation is fully complete.
- **Games registry — admin-managed** ✅ live (2026-05-30). Replaced the hardcoded `GAMES` list + single `GAMES_BASE_URL` env with a DB `games` table (migration `0005`, applied to prod first). Admin has a Games CRUD section in `/admin` (create/rename/edit URL/delete). The home grid reads from the DB; each game opens `<url>?project=<active>` (or `&project=` if the URL already has a query) in a new tab. Empty URL renders a disabled card with a "configure in Admin" hint. Future = a direct-deploy button per game when the dedicated game server exists.
- **gpt_image unblocked** ✅ (2026-05-30). `atlas-tool` `batch_atlas.py` gpt branch now skips the redundant RMBG cutout when `background='transparent'` (the gpt-image-1 default) — GPT already returns an alpha-cut RGBA, so the `Images to RGB` flatten + RMBG aren't needed and the missing custom node no longer blocks the default path. NEEDS OWNER: set `COMFY_ORG_API_KEY` + comfy.org credits to run gpt generation.
- **Games section (home)** ✅ live (2026-05-30). Quick test bridge: a grid of the engine games on the launcher home that opens `<GAMES_BASE_URL>/<gameId>/?project=<activeProject>` in a new tab. `GAMES_BASE_URL` env (default empty → muted hint). Not hosting games — future = a dedicated game server. **NEEDS OWNER:** set `GAMES_BASE_URL` to the current test server.
- **Invisible Localization tool** ✅ live (2026-05-30). (Fixes 2026-05-30: target-language input splits on commas; "+ Add row" no longer depends on `crypto.randomUUID`; fixed a black-screen from mutating state during render.) New online tool built into the launcher (`/localization`, project-scoped, role-gated). Table view (rows = game text fields, columns = source + target languages) + global settings (source lang, target langs, context/glossary). "Translate" batches strings to Claude (`claude-sonnet-4-6`, prompt-cached system) → results land **unreviewed** (amber) for human review → Save persists to R2 (`localization/<project>/strings.json`). Tool key `localization` (configurable via the role matrix; default for developer + artist). **NEEDS OWNER:** set `ANTHROPIC_API_KEY` on the launcher to enable translation (tool loads without it; translate returns a clear error) + pick default target languages per project.
- **Client layer above projects** ✅ live (2026-05-30). `clients` table + `projects.client_key` + `user_client_access` (migration `0004`, applied to prod first). Admin manages clients + assigns projects + per-user client access; project selector groups by client. Tools/R2 unchanged.
- **Editable role→tool matrix + configurable admin access** ✅ live (2026-05-30). New `role_tool_access` table (migration `0003`, applied to the production DB before deploy) overrides the hardcoded `ROLE_TOOLS` per role. Tool resolution is three layers: `ROLE_TOOLS` defaults → role overrides → user overrides. `/admin` has a "Roles" matrix (roles × tools + an `adminPanel` capability) with grant/revoke/default. Access to `/admin` itself is now the managed `adminPanel` capability (configurable per role) with a hard lockout-safety: the `admin` role can never lose it.
- **Railway consolidated into one project** ✅ (2026-05-30). All services (launcher, atlas-tool, atlas-backend, sheet-tool, Postgres) now live in ONE project / `production` environment with **shared variables** (R2/CF/COMFY/tool-secrets defined once). Clean domains `*-production.up.railway.app`; old separate projects retired. See `docs/INFRA.md`.
- **Sheet Maker → online tool + Test Server removed** ✅ (2026-05-30, owner directive). `sheetMaker` is now `kind:'online'` with a full-page `/sheet` route that redirects to `SHEET_TOOL_URL` with `?k=secret` + `&project=<active>` (project-centric, mirrors `/atlas`), behind the auth+role gate, with a "not configured" fallback. The `testServer` tool was removed from the registry + `ROLE_TOOLS` + docs (deleted `docs/tools/test-server.md`). Route verified live (303 unauth, like `/atlas`). **NEEDS OWNER:** create the Railway service from `services/sheet-tool/` (envs `R2_*`, optional `SHEET_PROJECT`/`SHEET_TOOL_SECRET`) and set `SHEET_TOOL_URL` (+ matching `SHEET_TOOL_SECRET`) on the launcher — until then `/sheet` shows the "not configured" page.
  - **⚠️ OUTAGE + LESSON (2026-05-30):** the admin code (which selects `users.expires_at` + queries `user_tool_access`/`tool_installs`) was deployed BEFORE the prod DB had those → every authed request 500'd. Compounded by **TWO Railway environments** (`production` + a stray empty `atlas`): the first `db:push` hit the wrong env's Postgres. Fix: applied the additive schema to the **production** env's Postgres (public proxy, port 58507). **Rules now:** (1) apply DB migrations to prod BEFORE pushing schema-dependent code; (2) migrations target the **production** environment's Postgres specifically. See `docs/INFRA.md`.
  - **B13 ✅ DONE (2026-05-30):** retired the stray `atlas` Railway environment (duplicate launcher + empty Postgres, 0 users — verified before deletion). Only the `production` environment remains; production verified healthy after deletion (login 200, /admin 303). Future migrations now have a single unambiguous DB target.
- **`/spine`** — Spine Viewer, full-page (redirect to static `view.html`).
- **`/atlas`** — full-page **redirect** to the cloud Atlas tool (NO iframe — see hard rules).
- **atlas-backend** (`services/atlas-backend`, FastAPI) — Railway → Cloudflare tunnel → local ComfyUI → R2, **verified end-to-end**: `/generate-test`, `/generate-region` (SDXL + LoRA + IPAdapter + ControlNet + RMBG), `/compose`, `/slice`.
- **atlas-tool** (`services/atlas-tool`) — the **local Python Atlas Maker re-hosted on Railway**. Drop-in `cloud_paths.py` (local staging mirrors an R2 prefix 1:1) + `storage.py` (R2). ComfyUI reached via tunnel; refs uploaded via `/upload/image`; variants fetched via `/view` → staging + R2. Config/manifests in R2 with write-through. Live and serving the UI.
- **B10 — atlas region cards "pick from R2" + `.atlas`/source upload** ✅ (2026-05-30). Region-card `style_ref`/`shape_ref` fields get a "Pick from R2" button reusing the existing `/fsbrowse` browser (writes INPUT_DIR-relative paths, the form `batch_atlas` resolves). New `/uploadatlas` POST writes an uploaded `.atlas` (+ optional source page) into `INPUT_DIR/refs/atlas/` (filenames stripped via `Path().name`), repoints `manifest.atlas.atlas_file`/`source_image` to staging-relative paths, `save_manifest` → R2. `atlas_file_path()` now resolves relative atlas files against staging. Code-complete + py_compile clean; **not browser-tested** (the FileReader→base64 upload + two-step picker untested live).
- **B11 — FLUX + gpt_image cloud pipelines reviewed** ✅ code, ⏳ live (2026-05-30). FLUX (`build_workflow_flux`) wiring is correct, no code change. gpt_image had a real defect: defaulted to `gpt-image-2` + a `Custom` size the shipped `OpenAIGPTImage1` node rejects (HTTP 400) → fixed to `gpt-image-1` with `_gpt_preset_for_ref()` picking a real preset by aspect (Custom path kept opt-in for a future gpt-image-2). API key env-first (no hardcode). **Live verify still owed by user** — see Remaining work item 4.
- **B1 — Invisible Sheet Maker ported to cloud** ✅ code (2026-05-30), Railway service not yet created. New `services/sheet-tool/` mirrors atlas-tool: `cloud_paths.py` (R2 prefix `sheet_maker/cloud/<proj>`), `storage.py`, R2 write-through, Dockerfile/requirements/.env. Pure Pillow (no ComfyUI). Exports handed off to the Atlas Maker's R2 `manifests/` prefix (needs an atlas-tool restart to appear, since hydrate runs at boot). py_compile clean; not run live (no local R2 creds). Env vars: `R2_*`, optional `SHEET_PROJECT`/`SHEET_STAGING`/`SHEET_TOOL_SECRET`/`PORT`.
- **B2 — local launcher trimmed to ComfyUI + cloudflared** ✅ (2026-05-30, file outside repo). `C:\Invisible Wall SL\ComfyUI\Invisible_Launcher.py` (`.bak` saved): removed the "Tools" tab so it no longer opens the local tool web UIs; added full Cloudflare Tunnel start/stop/status (tunnel `comfy-gualtiero`); ComfyUI/project management kept. User should run it once to confirm.
- **B3 — launcher/Postgres dedup: NOT a duplicate** ✅ (2026-05-30). Verified (probes, no dashboard): ONE launcher service + ONE Postgres; `app.invisiblewall.org` → the launcher's own `e6hl5cnp.up.railway.app`; both `invisible-engine-production…` and `invisible-engine-atlas-tool…` are two domains on the SAME service (the `…-atlas-tool` name is a historical artifact, NOT the python tool). Single `DATABASE_URL` everywhere → sessions can't split. Optional cleanup: remove the misleading `…-atlas-tool` domain in Railway (non-destructive). Steps in `docs/INFRA.md`.
- **B4 — Studio game-spec CLI** ✅ (2026-05-30). `packages/game-spec` already had the Zod schema + generators; added CLI wiring (`game-spec validate`/`generate`/`scaffold`), `bin` + build (`tsc`), `tsconfig.json`, JSON example `examples/lines.spec.json`, README. `generate` emits `<id>.normalized.json` + `game/paytable.ts` (`ServerPayEntry[]`) + `game/infoManifest.ts`. Build + validate + generate + failure-path all verified. Deferred: `config.ts` generation (carries math/reel data outside the frontend spec).
- **B5 — tool download links + per-user install paths** ✅ code (2026-05-30). New Drizzle `tool_installs` table (PK `(user_id, tool_key)`, FK→users cascade) + `lib/server/toolInstalls.ts` (get/set upsert); home `+page.server.ts` form action (role-gated) + UI showing download link + editable install path per local tool. `roles.ts`: `ToolDef.install` gains `download`+`steps`, added `sheetMaker` tool, helpers `localToolsForRole`/`toolDocPath`/`DOWNLOAD_TODO`. Real download links: ComfyUI, Spine Editor. **NEEDS OWNER:** real URLs for Invisible Test Server + Invisible Sheet Maker (currently `DOWNLOAD_TODO` = "coming soon"). **Migration:** `drizzle/0000_wise_whirlwind.sql` (first-ever; contains all 3 tables). No migrate-on-boot → apply to prod with `pnpm --filter launcher-api db:push` (diff-based, creates only `tool_installs`); `db:migrate` would fail on pre-existing tables. NOT run yet.
- **B6 — full onboarding session** ✅ (2026-05-30). `/onboarding` rewritten into a 4-step sectioned walkthrough (how it works / online tools / install local tools / help) with a table of contents, per-tool install steps + real download links + the B5 install-path form, and per-tool doc links to `/docs/tools/<slug>.md`. Svelte 5 runes + snippets.
- **B7 — per-tool docs** ✅ (2026-05-30). `docs/tools/` created: README index + `launcher.md`, `atlas-maker.md`, `spine-viewer.md`, `sheet-maker.md`, `comfyui.md`, `test-server.md`, each grounded in source. TODOs flagged: Invisible Test Server (only a `roles.ts` stub, no impl in repo — runtime/port/launch unknown); Sheet Maker (Railway service + `/sheet` route not yet created).
- **B8 — naming sweep** ✅ (2026-05-30). Branded user-facing strings to "Invisible …" in the atlas + spine pages (titles, h1, body, 403 messages) and `lib/server/spine.ts`. Third-party names (ComfyUI, Spine Editor) left as-is.
- **B9 — security cleanup** ✅ scrub + checklist (2026-05-30). Scrubbed the live `comfy_org_api_key` in `Invisible_Pipeline/.../atlas_config.json` (→ `""`; code reads env-first) + tightened that repo's `.gitignore`. Rotation checklist for all pasted secrets (R2 token `a6f88a7d…`, Postgres pw, CF Access service-token) added to `docs/INFRA.md`. **NEEDS OWNER:** actually rotate each secret + redeploy the services that use it (see INFRA.md "Security / secret rotation").

### 📋 Backlog — assigned to agents (added 2026-05-29)
All assigned items (B1–B13) are done — see the Done list above. Add new items here as they come up.

- **B25 — Reuse guard: `reuse-check` skill + UI inventory** ✅ DONE (2026-05-31, infra) / ⏳ extraction pending. To stop re-implementing surfaces we already have (≥4 file browsers, multiple tables, item UIs), added (1) **`docs/ui-inventory.md`** — a catalog of recurring patterns (file browser/R2 picker, tables, layouts, item/floating toolbar, region thumbnail, client/project selector, tool top bar, server gate/scope) with their canonical impl + reuse status, split into **two non-shareable domains**: A = launcher (Svelte → `packages/components-*`), B = Python tools (HTML → `iw_pipeline_common`); and (2) **`.claude/skills/reuse-check/SKILL.md`** — fires before building any of those surfaces: consult inventory → import shared / extract-then-reuse / build-new-as-shared. NOT a new subagent (the area agents own these surfaces; the skill fires inside whichever is active). **Remaining (the real payoff):** progressively **extract** the genuinely-shared patterns into `packages/components-*` (start: `<R2Browser>` from `ftpScope.ts`+`api/files`, `<DataTable>` for admin/localization) and the launcher gate/scope consolidation (health-eval #4) — until then the skill can only say "copy from the named reference." Keep `docs/ui-inventory.md` updated as patterns are extracted.
- **B24 — Restore back-and-forward navigation between tools (+ back to launcher)** ⏳ TODO. User: "the link to the Atlas Maker has been replaced with useless linked text — bring back the back/forward connection between tools." **Investigation:** the green **"Atlas Maker linked ✓"** in the Sheet Maker top bar was **never a link** — it's a *status pill* (`services/sheet-tool/ui.html:96` `<span id="amstatus">`, filled at `ui.html:201-202` from `STATE.atlas_maker_found`, which = `bool(atlas_manifest_prefix)` at `sheet_server.py:203` — i.e. "the Atlas Maker R2 handoff prefix is configured", not reachability). The real gaps: (a) **Sheet Maker** has a working `← Launcher` back-link (`ui.html:90`, repointed from `?home=` or `STATE.launcher_url`=`LAUNCHER_URL` env, `sheet_server.py:196`) but **no link to the Atlas Maker**; (b) **Atlas Maker** top bar (`services/atlas-tool/ui_server.py:1506-1510`) has **no launcher link and no sibling link at all** (no `LAUNCHER_URL` consumed). All plumbing to fix this already exists: the launcher knows both tool URLs (`ENV.ATLAS_TOOL_URL`/`ENV.SHEET_TOOL_URL`, `env.ts:53-67`) and already appends `?k=&client=&project=` on redirect (`(app)/atlas/+page.server.ts:26-30`, `(app)/sheet/+page.server.ts:26-30`); each tool reads+persists those (gate/context cookies are per-origin so a cross-domain jump must **re-append** the params). **Fix:** (1) launcher appends `sibling=<other tool URL>` + `home=<origin>` on each redirect; (2) Sheet Maker adds an "Atlas Maker →" anchor (carry current `client/project/k`), keep the status pill separate; (3) Atlas Maker adds `← Launcher` + "Sheet Maker →" anchors (needs `home`/`sibling` surfaced into `_index()` HTML + add `LAUNCHER_URL` to `atlas-tool/.env.example` for parity).
- **B23 — Launcher CLIENT/PROJECT selector is broken (nothing stays selected; selecting one reshuffles everything)** ⏳ TODO — **high priority** (affects every project-scoped tool). The B16 two-step selector lives in `(app)/+page.svelte` (NOT the layout). **Root causes (investigated):** (A) a re-seeding `$effect` (`+page.svelte:56-59`) overwrites the user's pick with the server value on every `invalidateAll` (the bare `use:enhance` at `:96`); (B) **CLIENT is not independent state — it WRITES the project**: `onClientChange` (`:73-86`) auto-submits `?/setProject` with the alphabetically-first project of the chosen client → "selecting one thing changes everything"; (C) the layout load **silently falls back to `cloud`** when the stored project isn't in `accessibleProjects` (`+layout.server.ts:36-38`), and `cloud` has `clientKey=null` so the CLIENT dropdown snaps to "Unassigned" → "nothing stays selected"; (D) one-way `value=` bindings (`:100,113`) mutated by both handlers and the `$effect`, papered over with `await tick()`. Note: only `sessions.active_project_key` is stored — **client is derived**, but the UI treats it as independent. **Fix direction (rebuild, more solid):** make CLIENT a pure local *filter* (never persists); persist only on explicit PROJECT change; seed selection once and don't clobber mid-edit (or `bind:value` + reconcile); stop the load silently rewriting the active project (offer only accessible projects, or surface inaccessible state); consider **storing client explicitly** alongside project so the CLIENT step has a real source of truth instead of reverse-deriving from `cloud`'s null client. Files: `(app)/+page.svelte`, `(app)/+page.server.ts` (`?/setProject` :42-56), `(app)/+layout.server.ts` (:36-38), `lib/server/auth.ts`, `lib/server/projects.ts` (`accessibleProjects` :41-63, `DEFAULT_PROJECT_KEY`), `lib/server/db/schema.ts` (:52, no client column).
- **B22 — Online launcher beauty pass (icons, sectioning, install-card state)** ✅ DONE (`d7c3d41`, 2026-05-31). All four parts shipped on the launcher home (`apps/launcher-api/src/routes/(app)/+page.svelte`), full-bleed (no centered card). (1) **Icons:** `icon?: string` on `ToolDef` + single-sourced `TOOL_ICONS` map in `src/lib/roles.ts` — 8 minimal stroke-based inline SVGs (`currentColor`, ~22px): grid / armature / node-graph / bone / packed-rects / globe / overlapping-shapes / bulleted-list; games render an inline play-triangle. Shown via `{@html tool.icon}` in an `.ico` holder tinted with the section accent (`/onboarding` can reuse `TOOL_ICONS` later — field added with that in mind, not wired there yet). (2) **Sectioning + colour:** each section is a card-wrapped panel (`.sec`, `#131318` bg + colour-coded left border + accent header) reusing the existing palette — Online=green `#7ee787`, Local=purple `#c8a3ff`, Games=teal `#7ee0c0` (owner choice: reuse-existing + card-wrapped). (3) **Install "configured" state:** `isConfigured(id)` from `data.installPaths` — when a `tool_installs` path is saved the local card flips: badge `install`→`configured` (green), saved path promoted to the primary line, download de-emphasized to "Re-download", field label → "Edit install path". (4) **Honest path helper:** section note explains the path is a *personal bookmark of where you installed it on your machine* — the launcher can't see your disk, doesn't verify or run it. `pnpm --filter launcher-api build` GREEN, no warnings. Not browser-tested live (authed page). Original spec retained below:
  1. **Per-tool icons.** Add a minimal, line-art / geometric icon to each tool card to distinguish them at a glance (match the IW minimal aesthetic; `currentColor` so they inherit theme). Cleanest place: add an `icon` field to `ToolDef` in `src/lib/roles.ts` (inline SVG string or a keyed icon-map component) so both the home grid and `/onboarding` can render it. Suggested motifs: **Atlas Maker** = square grid; **Spine Viewer** = bone/armature (or jointed-segment line); **Sheet Maker** = packed rectangles; **Localization** = globe or speech bubble with “A→文”; **Editor** = overlapping shapes + move handles; **FTP Browser** = a browse-list (stacked lines with leading bullets); **ComfyUI** = node-graph (two nodes + edge); **Spine Editor** = bone. Keep them ~20–24px, stroke-based, single color.
  2. **Section division + colour coding.** Give each section (Online Tools / Local Tools / Games) its own visual grouping — a subtle card background or border + a colour-coded accent per section (e.g. distinct hue on the section header underline / left border / icon tint). Must stay full-bleed and readable on the dark theme; use the existing palette, low-saturation.
  3. **Install-card "installed" state.** Today the local-tool card (ComfyUI / Spine Editor) keeps showing the `install` badge + "Download installer" even after the user saves an install path, which reads as "still not installed." When an install path is **saved** (`tool_installs` row exists — `lib/server/toolInstalls.ts`, surfaced via the home `+page.server.ts`), flip the card to a "configured/installed" state: change the `install` badge → e.g. `configured` (different accent), de-emphasize the download link, show the saved path as the primary line with an edit affordance. Note the inherent limit — the launcher **cannot truly detect** a local install (it's a cloud app with no access to the user's disk), so "path saved" is the best available proxy; word the UI honestly.
  4. **Clarify the local-path purpose.** The user was unsure why they can set a local path at all. Add a one-line helper explaining it's a personal bookmark of where *they* installed the tool on *this* machine (used for the download/onboarding flow + as a reminder), not something the launcher verifies or executes. (Cross-ref the install model in `roles.ts` `ToolDef.install` + B5/B6.)
- **B21 — Spine Viewer shows a fake hardcoded "HotFruits" project + unhelpful empty-state error** ✅ DONE (2026-05-31). Both fixes landed. (a) `static/spine/view.html`: removed the hardcoded `const st = { project: "HotFruits", projects: ["HotFruits"], … }` literal from `loadState()` (now only viewer prefs: bg/pma); added `setActiveProject(client, project)` which fills the header `#sub` + the read-only `#project` select from the server-resolved active project, called from `loadSkeletons()` after the fetch. The select reflects the launcher's real `(client, project)` (e.g. `invisible_wall / salmons`); switching is owned by the launcher, so the viewer's select stays display-only. (b) `(app)/spine/skeletons/+server.ts`: the endpoint now echoes the resolved `client`/`project` in every branch and the empty/missing message names the prefix — `No skeletons synced for spines/<client>/<project> yet.` — so "wrong project vs genuinely empty" is unambiguous. `pnpm --filter launcher-api build` GREEN. **Data note (unchanged from the investigation):** if a project is *meant* to have spines, run `node apps/launcher-api/scripts/r2-sync-spines.mjs <localSpinesDir> spines/<client-slug>/<project-slug>` — nothing has been synced for non-hotfruits projects yet.
  - *Superseded TODO (kept for history):* Reported when CLIENT=`Invisible_Wall`/PROJECT=`Salmons!!!` was selected but the Spine Viewer's "ACTIVE PROJECT" dropdown showed `HotFruits` and the list was empty with "No skeletons index in R2." **Investigation (root cause — two unrelated mechanisms, NOT a slug issue):** (1) **Cosmetic propagation bug.** The viewer's "Active project" `<select>` is built from a hardcoded inline literal `const st = { project: "HotFruits", projects: ["HotFruits"], … }` in `apps/launcher-api/static/spine/view.html:377` (`loadState()` 375–387) — a leftover from the local single-project era. `(app)/spine/+page.server.ts:15` redirects to `view.html` forwarding **no** active-project context, and the dropdown is purely cosmetic (changing it does nothing). So it always reads "HotFruits" regardless of the launcher selection. (2) **The empty list + error is CORRECT/honest, not a bug:** `(app)/spine/skeletons/+server.ts:11-18` resolves the **session-bound** active project from Postgres (`getActiveProjectKey`→`projectClientKey`), `lib/server/spine.ts:22-35` looks for `spines/<client>/<project>/skeletons.json` then `spines/_shared/skeletons.json`. It genuinely checked `spines/invisible_wall/salmons/` and found nothing — **no spines have ever been synced for that project** (`projectScaffold.ts` seeds atlas/sheet/localization/editor but nothing under `spines/`; `scripts/r2-sync-spines.mjs` defaults to `spines/hotfruits`). (3) Slugs are fine — `Salmons!!!`/`Invisible_Wall` are display names; R2 keys use the validated slug. **Fixes:** (a) make the dropdown reflect the launcher's real active project — drop the inline literal and feed it from the server (e.g. a small `GET /spine/state` returning the resolved `{client, project}` from the same `getActiveProjectKey`/`projectClientKey` calls), or remove the selector entirely (active project is owned by the launcher); (b) improve the message at `skeletons/+server.ts:16` to name the resolved prefix (e.g. `No skeletons index in R2 for spines/<client>/<project>.`) so "wrong project vs just empty" is unambiguous. **Data action (not code):** if Salmons is meant to have spines, run `node apps/launcher-api/scripts/r2-sync-spines.mjs <localSpinesDir> spines/<client-slug>/<project-slug>` (verify slugs against the `clients`/`projects` rows first).
- **B20 — Local launcher: strip project/game management, keep only the local pieces** ⏳ TODO (owner-run; file is outside the repo). The local `Invisible_Launcher.py` (`C:\Invisible Wall SL\ComfyUI\Invisible_Launcher.py`) still manages local "projects" — but these are **local ComfyUI workspaces** (disk folders w/ input/output/workflows + a `game` block that launches a local game test-server/frontend), a *different and divergent* concept from the **cloud projects** (the online launcher's `clients`/`projects` tables + R2 `<tool>/<client>/<project>/` prefixes, scaffolded via `/admin`). This is local↔cloud drift: two sources of truth for "project". **Alignment principle: the cloud is the single source of truth for projects/clients/tools; the only genuinely-local footprint is the GPU (ComfyUI) + the tunnel.** In the new architecture ComfyUI is a stateless GPU engine (cloud tools resolve client/project→R2 server-side and send full workflow JSON over the tunnel; results go to R2 via `/view`) — and the launcher already collapsed to **one shared ComfyUI workspace** (Phase 2), so per-project local folders drive nothing. Games are now the DB-backed Games registry on the online launcher → local game-launch belongs in dev (`pnpm dev`), not here. **Action:** reduce the local launcher to ComfyUI start/stop + cloudflared start/stop/status (+ status only); remove the Projects tab, `ProjectDialog`/`ProjectCard`, the projects config list, and `launch_game`/service-bat plumbing. **Consider:** vendor the launcher into the repo (e.g. `tools/local-launcher/`) so it's version-controlled instead of `.bak`-only.
- **B19 — Sheet Maker: rename a region + persist the custom name** ✅ DONE (2026-05-31). **Finding on investigation: the core round-trip was already fixed** by the project-file (Save/Save As) rework — code-traced end to end: the inspector "Name" input sets `r.name`; `runExport` sends `name:r.name`; `api_export` persists `safe_name(name)` into the AI manifest + libGDX `.atlas` + TexturePacker keys (all keyed off the edited name, NOT the src/filename); `api_load` returns the manifest name and `loadFile` shows it. So rename → Save → Load existing already shows the edited name, and the `.atlas`/TP/Atlas-Maker handoff already use it. (The original "reverts to image name" symptom predated that rework.) **What this task added** (the remaining acceptance-#4 gap — the B18 global-uniqueness constraint): in-editor guards in `services/sheet-tool/ui.html` so blanks/collisions are caught *before* a save round-trip instead of only as a hard server error. New `renameSel()` (replaces the generic `setField('name',…)` for the name field), `safeNm()` (mirrors the server's `safe_name` so the client check matches what's persisted), and `flagName()` (red border + tooltip on the inspector name input when the name is empty or duplicates another region in the sheet, live as you type + on selection via `syncInspector`). `runExport` now blocks save with a friendly message on a blank or duplicate region name (the server's existing `safe_name` dupe-check at `api_export` stays as the backstop). Verified: `ui.html` UTF‑8, no NUL bytes, braces balanced, `<script>` parses (`new Function`) clean. Static file (no build graph); not browser-tested live. **Note:** uniqueness is enforced *within a sheet*; the B18 cross-atlas global-uniqueness is a game-load concern outside this tool's scope.

- **B18 — Invisible Editor per-region drag + delete/lock + attached item UI** ✅ code (2026-05-30), ⏳ live-verify owed. Reworked so a sheet/atlas is no longer dropped as one (blank) sprite — instead each Library **sheet** and **atlas-manifest** row is now **expandable** into its individual regions (parsed from the manifest), each a draggable thumbnail (page image cropped to the region rect via `RegionThumb.svelte`). Dragging ONE region drops that sprite alone at its native region size. Each selected placed item gets a **Sheet-Maker-style floating toolbar attached to it** (`EditorItemOverlay.svelte`, tracks pan/zoom) with live W×H, name/region/atlas, **delete + lock**, 9-point anchor presets, scale nudge, z-order. **Locked** nodes are non-selectable / non-draggable on the canvas (amber dashed outline); Delete/Backspace removes the selected unlocked node (guarded while typing). Canvas crops regions via 9-arg `drawImage` honouring trim offsets; `nodeBox` sizes region sprites to the region.
  - **Schema (`packages/engine-layout`):** `SpriteNode.region?: string` (a frame within the `assetKey` atlas — render lookup is by `region`; verified accurate against pixi-svelte's `sprites` loader, which flattens atlas frames into `loadedAssets` keyed by frame name — ⚠️ so region/frame names must be **globally unique** across all atlases a game loads) + `BaseNode.locked?: boolean` (editor-only; engine ignores). `LayoutNodeView` renders `key={node.region ?? node.assetKey}`.
  - **Server:** new `GET /api/editor/regions?sheet=<key>` (+ `lib/server/editorRegions.ts`) — auth + `editor`-role gate + **session-bound** active-project scoping identical to `api/editor/asset`. Parses the sheet/atlas manifest, returns `{ assetKey, pageKey, pageWidth, pageHeight, regions[] }`; both the `sheet` arg AND the returned `pageKey` are constrained to the project's `allowedPrefixes` (code-reviewed: **no arbitrary-R2-key exposure**, no path traversal). Resolves stale local Windows page paths to the real R2 page (B14 `source_image_path`/`export_prefix`, `.png`/`.webp` tolerant).
  - **Node contract (saved doc stays clean):** a dropped region = `{ kind:'sprite', assetKey, region, label, width, height, anchor, scale }` where `assetKey` is the resolved manifest R2 key (the same value the regions endpoint accepts back) and `region` is the frame name. PageKeys/rects are **editor-side preview data only** (resolved live via the endpoint, cached by `assetKey`; reopening a saved doc lazily re-fetches) — never baked into the doc. The runtime asset-key namespacing (how a game's `assets.ts` maps `assetKey`/`region`) remains the design doc's **open decision**, intentionally out of scope for B18 (goal = make the editor usable). Cross-ref `docs/design/invisible-editor.md` §6 (extends Steps 5–7).
  - Build (`pnpm --filter launcher-api build`, the type-check) **green**; code-review clean (security gating verified, one unused-import + a thumbnail-cache nit fixed). **Limitations:** packed `rotated:true` frames preview upright (geometry-only, best-effort). **NOT browser-tested end-to-end** — needs an authed session + a project with real R2 sheets; the expand→thumbnail→drag→render→overlay→save flow against live R2 is unverified (gates verified: 401 unauth on the API, 303→login on `/editor`).

- **B17 — Admin panel UX: fullscreen, tabs, scalable Roles layout** ✅ done (2026-05-30). `/admin` (`apps/launcher-api/src/routes/(app)/admin/+page.svelte`) is now (a) **full-bleed** (matches B16 home: `width:100%; box-sizing:border-box; padding:32px clamp(24px,4vw,64px) 64px`, no centered max-width card); (b) split into a **WAI-ARIA tablist** (Users · Roles · Tools · Projects · Clients · Games · Sessions) — client-side `$state` tab + `hidden` panels, arrow/Home/End keyboard nav, `role=tab`/`tabpanel`/`aria-selected`. Server load + all **25 form actions untouched** (pure UI reorg). (c) New **Roles editor**: capabilities down the left grouped into collapsible sections (Admin capabilities · Online tools · Local tools), a search box that filters by name/key, roles across the top, tri-state `<select>` cell (Default/Grant/Revoke, colour-coded) reusing the existing `setRoleToolAccess` action — scales cleanly to 30+ rows. No data-model change; sits on `role_tool_access` + the three-layer resolution. Build (type-check) green.

- **B16 — Launcher UX: full-bleed home + colored/iconed sessions + Client→Project selector** ✅ done (`6565eed`, 2026-05-30). (a) Launcher home (`(app)/+page.svelte`) is now **full-bleed** (dropped the 960px centered card → `width:100%` + responsive clamp padding, matches the tool pages). (b) Sessions render only in `/admin`; gave each a **per-state badge** — active=green ●, expiring≤24h=yellow ◐, expired=amber ◷, revoked=red ⊘ — icon + text label (accessible). (c) Active-project selector is now a **two-step Client → Project hierarchy** (Client select → Project select filtered to that client), built on the EXISTING clients registry (`clients` + `user_client_access` already shipped in migration 0004 — no schema change); `&project=<key>` forwarding to tools/games unchanged. Build green. (Client forwarding to tools NOT added — project-key remains the sole tool contract; revoked-session styling is inert today since revoke hard-deletes.)

- **B15 — Atlas Maker "Slice source → refs" FileNotFoundError** ✅ FIXED (`ef5c8fe`, 2026-05-30). Root cause was the `name`-only-vs-staging-dir bug: `slice_atlas.py` + `batch_atlas.py` (compose) resolved the `--manifest` arg against `SELF` (`/app`) instead of `MANIFEST_DIR`. Fixed via a shared `batch_atlas.resolve_manifest_arg()` (absolute honoured; bare name → `MANIFEST_DIR`; Windows seps normalised) used by both; `ui_server::_sliceatlas` now passes the full `str(manifest_path())` + hands off `IW_CLIENT_NAME`/`IW_PROJECT_NAME` (it previously ran with no env). Live verify owed: reload → pick a manifest → ✂ → expect `✓ sliced` (or the new actionable "source not in R2" message). **Pushed → atlas-tool auto-deploys.**

- **B14 — Sheet Maker manifest self-contained** ✅ DONE (`0d2ccfa`, 2026-05-30). `sheet-tool/atlas_writers.build_manifest` now emits back-references as R2 keys (additive; legacy bare `atlas.source_image` kept): `export_prefix`, `atlas.source_image_path`, `atlas.atlas_file` (when libgdx exported), `atlas.texturepacker_json` (when TP exported), and each region's `shape_ref` defaults to its loose-trim R2 key. `sheet_server.api_export` computes those from values it already has (no-ops when `R2_PREFIX` unset). `atlas-tool/ui_server.load_manifest` runs `_ingest_self_contained` ONCE per manifest: a field that looks like a Sheet-Maker R2 key is fetched into `INPUT_DIR` (`refs/atlas/` for page + `.atlas`, `refs/` for shape refs), mirrored to the project's R2, and repointed to the INPUT_DIR-relative paths the `#3`/`B15` resolvers (`atlas_file_path`/`source_image_candidates`) already follow. **Old manifests untouched** (non-R2-key fields → no-op); ingest is best-effort (never fatal to a load). `py_compile` clean; pushed → both tools auto-deploy. **Live smoke-test owed:** export a sheet (manifest + libgdx) → **restart atlas-tool** (hydrate at boot) → select the manifest → sheet page + `.atlas` regions + shape-ref thumbnails auto-load aligned, no re-pick. (TexturePacker JSON is carried as metadata only — the `.atlas` is the geometry source. The Sheet Maker emits no separate trim PNGs, so `shape_ref` defaults to the loose uploaded sprite, which is the intended silhouette source.)

### ⚠️ Remaining work (the pipeline is NOT finished)

**atlas-tool (immediate):**
1. ✅ **Seed R2 — done (2026-05-29).** `seed_r2.py` uploaded 1158 objects to `atlas_maker/cloud/cloud/` (10 manifests, 1147 refs, config). Tool redeployed and hydrated — UI lists all manifests. Hydrate now pulls manifests/config synchronously + refs in a background thread (fast boot). ⚠️ The R2 creds used were pasted in chat → **rotate them** (security debt below). Open item: the loaded manifests reference `.atlas` geometry via local Windows paths (won't resolve in cloud) — fine for generate, needed for compose/slice (item 3).
2. **Access gate** — `ATLAS_TOOL_SECRET` is currently unset (tool is open on its URL). When set, the launcher appends `?k=<secret>`; verify the cookie flow.
3. **`.atlas` geometry + source image for compose/slice** — **Part A (code) ✅ DONE (`ef5c8fe`, 2026-05-30):** compose + slice now resolve the manifest against the R2-backed staging `MANIFEST_DIR` and the atlas source page via a shared `batch_atlas.source_image_candidates()` (normalises Windows seps, strips absolute local prefixes, also tries the bare name under `refs/atlas/` where `/uploadatlas` writes). Missing geometry/source → clear actionable messages instead of FileNotFoundError. **Part B (data) — PENDING owner + R2 creds:** the existing manifests still point `atlas.atlas_file`/`atlas.source_image` at local Windows paths; the real `.atlas` + page bitmaps live in `apps/<game>/static/assets/spines/<group>/` (note: pages are often `.webp`, while some manifests name `.png` — fix the mismatch on repoint). Migration = upload each `.atlas` + page to R2 `atlas_maker/unassigned/cloud/input/refs/atlas/<name>`, repoint the manifest fields to `refs/atlas/<name>.<ext>`, re-upload manifests, **restart atlas-tool** (hydrate runs at boot). Easiest path now that Part A is live: per region card, **Upload .atlas (B10)** auto-does the upload+repoint via the UI. (`fix_manifest_paths.py` could script it but still targets the STALE `atlas_maker/cloud/<proj>/` prefix — needs `atlas_maker/unassigned/cloud/`.) Needs `R2_*` env + a machine with the engine repo. **Live smoke-test owed** for B10 upload + compose + slice.
   - **File pickers now browse R2 (done):** `/fsbrowse` browses the project's R2 asset repo via the staging mirror and returns R2-relative paths, so the SETTINGS file pickers (atlas source image, .atlas geometry) pick from R2 instead of the local disk.
   - ✅ **Card "pick from R2" + `.atlas`/source upload done (B10, 2026-05-30)** — cards now have a "Pick from R2" button (reuses `/fsbrowse`) for `style_ref`/`shape_ref`, and `/uploadatlas` uploads a `.atlas` (+ source page) and repoints the manifest to staging-relative paths. Local upload (uploadRef/useMyImage) still works. Needs a live browser smoke-test.
   - **Outputs/variants intentionally NOT seeded.** The seed uploaded `input/` (refs + the 248 curated `useroutput_*` overrides) but NOT `output/` (the 577 raw batch variants — heavy, re-downloaded every boot, and regeneratable). The card prefers the override for display, so curated finals show once hydrate completes (verified 7/8 override regions render). Pure-prompt regions with no saved final correctly show "no output yet" until (re)generated. If raw prior variants are ever wanted in-cloud, seed `Shared/output/atlas_maker/<proj>/` → R2 `…/output/` (and ensure ATLAS_OUTPUT_PREFIX matches the local prefix dir, e.g. `HotFruits`).
4. **FLUX + gpt_image pipelines — code reviewed (B11), LIVE VERIFY still owed.** FLUX code correct; gpt_image default fixed to `gpt-image-1`. To verify, the user must: start local ComfyUI; for FLUX install the models the config names (`flux1-dev` + `t5xxl` + `clip_l` + `ae`, or an FP8 all-in-one) + Redux (`flux1-redux-dev` + `sigclip_vision`) — on the 8GB RTX 4070 use fp8/GGUF to avoid OOM; for gpt_image set `COMFY_ORG_API_KEY` (comfy.org credit) and confirm the `OpenAIGPTImage1` + `Images to RGB` nodes exist (`GET {comfy}/object_info`).
   - ✅ **VERIFIED LIVE (2026-05-30):** FLUX **proven** — all nodes + all models present (flux1-dev, t5xxl_fp16+clip_l, ae, Redux flux1-redux-dev+sigclip_vision); a live txt2img ran in ~105s on the 8GB GPU (fp8 + t5xxl offloaded to RAM). gpt_image **still blocked**: the `Images to RGB` node is NOT installed (the gpt rembg branch references it) + needs `COMFY_ORG_API_KEY`. Only SDXL ControlNets are installed, so FLUX+shape_ref/ControlNet won't work (FLUX base/Redux fine).
5. ✅ **Third tool — Invisible Sheet Maker is online (B1 + 2026-05-30)** — `services/sheet-tool/` deploy-ready; launcher `/sheet` route done (online, project-centric). Only remaining: **create the Railway service** + set `SHEET_TOOL_URL` on the launcher (owner action).

**Infra / cleanup:**
6. **Security** — rotate the secrets pasted during setup (R2 token, Postgres pw, CF Access service-token secret) and scrub the committed `comfy_org_api_key` in the `Invisible_Pipeline` repo. See docs/INFRA.md.
7. **cloudflared as a service** — install it so the tunnel survives reboots. (Partial: B2 added Start/Stop tunnel buttons to the local launcher GUI, but it's not yet a persistent Windows service via `cloudflared service install`.)
8. ✅ **Local launcher cleanup — done (2026-05-30).** Stripped all dead "local tools" code from `Invisible_Launcher.py` (the Tools tab was already hidden; this removed the leftover plumbing): the `tools`/`tools_install_dir`/`tools_server_url` config, all `*_tool` module functions, the `ToolCard`/`ToolDialog` classes, `_build_tools` + its handlers, and the Settings "Tools install folder / server URL" fields. Now manages only ComfyUI + cloudflared + projects. 2711 → 1929 lines (−782); `py_compile` clean; no residual refs. Backup at `Invisible_Launcher.py.bak-pre-toolcleanup`. (File is outside the repo — owner should run it once to confirm the GUI still opens.)

### 🔍 Pipeline health evaluation (2026-05-31)

Full read-only architecture/health review of the whole pipeline (Python tools + launcher + infra), run via the area subagents. The topology is sound (cloud-stateless Railway services + shared R2 system-of-record + only the GPU local); the issues are **drift, one concurrency bug, and security/process debt** — not architecture. Findings + ranked fixes (none applied yet — this is the to-do):

**Cross-cutting theme — duplication by copy-paste, not sharing (fragile, drifts silently):**
- `atlas-tool/storage.py` ≡ `sheet-tool/storage.py` (**byte-identical**); `iw_banner.py` identical too; `cloud_paths.py` ~85% identical (only env-prefix + hydrate plan differ).
- The ComfyUI client (UA `InvisibleAtlas/1.0` + CF-Access headers — the 403 gotcha) is re-implemented **three times**: `atlas-tool/cloud_paths.py`, `atlas-tool/batch_atlas.py`, `atlas-backend/comfy.py`.
- Launcher: the gate + "resolve active (client,project)" prelude is copy-pasted across ~9 routes (editor/localization redefine an identical `gate()` inline; only `/files` uses shared `ftpScope.ts`). **Two divergent `allowedPrefixes()`** exist (editor asset/regions allow `spines/_shared/`; ftpScope doesn't) — code even comments that they must be kept "in lockstep" by hand.
- Odd-one-out: **atlas-backend** has no client/project context (writes flat `atlas/test/...` instead of `<tool>/<client>/<project>/`) and is the only Python service with **no Dockerfile** (Procfile/Railpack → Python 3.13 vs the others' pinned 3.12).

**Real bug (not a nit) — cross-tenant race in atlas-tool + sheet-tool:** `ThreadingHTTPServer` + per-request mutation of **module-global** path state (`switch_context()` rewrites globals `INPUT_DIR`/`R2_PREFIX`/etc., `_project_lock` guards only the switch, not the handler body). Two concurrent requests for different projects interleave → one project's assets written into another's R2 prefix. Masked by single-user use; multi-user (the launcher's whole point) will hit it. Fix = request-local path resolution.

**Security (live exposure):**
- atlas-tool + sheet-tool are **anonymously reachable right now** — `ATLAS_TOOL_SECRET`/`SHEET_TOOL_SECRET` unset → code returns "allowed", so the public Railway URLs are an unauthed path to R2 read/write/delete + GPU/comfy.org spend.
- Three leaked secrets still owe rotation (R2 token = read+write whole bucket, shared by 4 services; Postgres pw; CF Access secret).

**Performance:**
- Boot hydrate re-downloads **everything** unconditionally (~1150 ref objects, no etag/size skip); no `railway.json` in repo confirming `/data` is a persistent volume (if not, every redeploy/sleep re-pays egress).
- atlas-tool re-uploads the **entire** INPUT_DIR serially (`push_dir`) after *any* ref edit (O(N)); `storage._client()` rebuilds a fresh boto3 client per op.
- Launcher per-request DB fan-out: ~4 sequential queries per tool load; layout + page **both** resolve overrides + active project (fetched twice/navigation); admin `…For` helpers do full-table scans.

**Ranked fixes (the working to-do):**
1. **Set `ATLAS_TOOL_SECRET` + `SHEET_TOOL_SECRET`, rotate R2 token** — closes live anonymous-access + bucket-leak. Owner-side (Railway/CF dashboards). ~30 min. **Do first.**
2. ✅ **DONE (2026-05-31) — threaded-server global-context race fixed.** atlas-tool + sheet-tool held the active (client, project) in **module-global** state, mutated per request on a `ThreadingHTTPServer` → concurrent different-project requests interleaved and wrote one tenant's assets into another's R2 prefix. Fix: context now lives in a `threading.local()` in both `cloud_paths.py` (thread-local == request-local under ThreadingHTTPServer); `resolve()` reads the calling thread's context; the local staging tree is keyed by `<client>/<project>`. `sheet_server.py` dropped its `_apply_project_paths`/module-global copy → resolves paths per-request via `_ctx()`. `ui_server.py`/`batch_atlas.py` bind the old path names (`INPUT_DIR`/`BATCH_DIR`/`R2_PREFIX`/…) to thin proxy objects that re-resolve per access, so the ~130 call sites are unchanged but thread-local; the per-request `_project_lock` is gone (only `_HYDRATED` is lock-guarded now). Subprocess handoff passes real `str` client/project via `IW_*` env. **Verified:** `py -m py_compile` green for all 8 files; adversarial code-review PASS on all points (proxy operator coverage, subprocess real-string handoff, hydrate lock, single-user unchanged). **Live smoke owed:** two concurrent `?project=` requests for different projects → each writes only its own R2 prefix.
3. ✅ **DONE — code (2026-05-31), ⚠️ needs Railway Root-Directory flip to deploy.** Extracted `services/_shared/iw_common/` (`storage.py` R2/boto3, `banner.py`, `comfy.py` = the single ComfyUI client with the `InvisibleAtlas/1.0` UA + CF-Access headers, `context.py` = the thread-local `ToolContext` base + slug validation + `prefix_for_tool`). atlas-tool + sheet-tool now carry **thin shims** (`storage.py`/`iw_banner.py` re-export from `iw_common`) and a **thin tool-specific `cloud_paths.py`** (own `TOOL_NAMESPACE`/`STAGING_BASE`/env names + `resolve()`/`hydrate()`, delegating context to a `ToolContext`). Public API + `resolve()` dict keys verified UNCHANGED per service; the thread-local race fix (#2) is preserved (the only `threading.local()` now lives in `iw_common/context.py`). **Dedup win ≈ −430 duplicated lines.** `py -m py_compile` green for all 15 files; runtime import of ui_server/batch_atlas/slice_atlas/sheet_server succeeds under `py`. **atlas-backend intentionally NOT included** (different stack — that's fix #7). **COUPLED DEPLOY:** the new Dockerfiles build from the **repo root** (`COPY services/_shared/iw_common` + `COPY services/<svc>/ .`, `PYTHONPATH=/app`), so each service's Railway **Root Directory must be set to the repo root** + **Dockerfile Path = `services/<svc>/Dockerfile`** BEFORE/with the deploy. Until the owner flips those two settings, atlas-tool + sheet-tool builds will fail (Railway keeps the last good deploy live → no outage, just a failed build). See `docs/INFRA.md` "atlas-tool + sheet-tool build from the REPO ROOT".
4. ✅ **DONE (2026-05-31) — one shared launcher scope gate.** New `apps/launcher-api/src/lib/server/toolScope.ts` is the SINGLE source for the gate + R2 prefix layout: `gate(locals, cookies, { tool, forbiddenMessage, includeSharedSpines? })` → `{ clientKey, projectKey, prefixes }` (the one auth sequence: not-logged-in→401 / `roleHasTool`→403 / session-bound active project→`(client,project)`→`allowedPrefixes`), `allowedPrefixes(client, project, { includeSharedSpines? })` (the sole definition of the namespace list `atlas_maker|sheet_maker|localization|editor|spines`, with the editor-only `spines/_shared/` now a single explicit flag), plus `isKeyAllowed`/`assertAllowed(key, prefixes)`. `ftpScope.ts` is now a thin wrapper (binds the shared gate to `ftpBrowser`, no shared spines; keeps the `(key,clientKey,projectKey)` call shape so the 5 `/api/files/*` endpoints + page are untouched). The editor endpoints `api/editor/{asset,regions}` were de-inlined to call `toolScope.gate(..., { tool:'editor', includeSharedSpines:true })` (they previously each carried their own copy of the gate + an `allowedPrefixes` with a "keep in lockstep" comment — that hand-sync is GONE). **Verified:** `pnpm --filter launcher-api build` GREEN (after a `svelte-kit sync` — the `$lib` alias needs regenerating in a fresh checkout); grep confirms the namespace allow-list literal + inline `allowedPrefixes`/`roleHasTool` gate exist only in toolScope.ts; `files/list` bidirectional check, `files/delete` recursive re-validate, `files/move` dual-validate, and the editor `spines/_shared/` inclusion all preserved. ~150 lines of dup removed.
5. ✅ **DONE — code (2026-05-31), ⚠️ one part is owner-side.** Three perf fixes in `services/_shared/iw_common/storage.py` (+ one sheet call site): **(a) cached boto3 client** — `_client()` was rebuilding a fresh boto3 client on every R2 op; now a lazy module-level singleton (double-checked lock; thread-safe for calls under ThreadingHTTPServer). **(b) incremental hydrate** — `pull_prefix` now skips the download when the local dest file already exists with a size matching the R2 object's (`list_keys` already returns size); still concurrent, still best-effort, return = files actually downloaded. Stops re-pulling ~1150 ref objects on every boot/switch (the real win once `/data` is a persistent volume). Size-only heuristic (these assets are write-once/replace-whole; a same-size different-content replace is effectively impossible). **(c) targeted push** — `sheet_server.api_load` replaced its whole-dir `_mirror_dir(up)` with a per-file `_mirror()` of exactly the regions it just sliced (it now collects `saved_files`). The atlas-tool `push_dir(INPUT_DIR, …/input)` post-hook was LEFT AS-IS (it runs generically after any ref-mutating route and those handlers don't report which file changed — a safe targeted set couldn't be proven; a real fix needs each handler to report its changed paths). **Verified:** `py -m py_compile` green for storage.py + sheet_server.py; diffs reviewed. **Owner action:** pin a Railway **`/data` persistent volume** on atlas-tool + sheet-tool so the incremental-hydrate skip actually persists across deploys (on ephemeral disk the first hydrate per boot still pulls everything — correct, just no speedup). Tiny known leftover: `_mirror_dir` in sheet_server.py is now unused (harmless; left in to avoid editing while the session's file-read display was glitching).
6. ✅ **DONE (2026-05-31) — launcher per-request DB fan-out collapsed.** Each tool page (`atlas`/`sheet`/`editor`/`localization`) re-ran `getRoleOverrides` + `getToolOverrides` (for its `roleHasTool` gate) + `getActiveProjectKey` + `projectClientKey` — duplicating work the `(app)` layout already did. Two provably-equivalent dedups: (a) the layout's `tools` array == `TOOLS.filter(roleHasTool(...))`, so a page's `roleHasTool(role, '<id>', ...)` gate ≡ `tools.includes('<id>')` — pages now read `const { tools } = await parent()` instead of refetching both override tables; (b) new `getActiveScope(token)` in `auth.ts` does the `sessions→projects` LEFT JOIN in ONE query (was `getActiveProjectKey` + `projectClientKey` = 2), with the fallback 2nd query only in the null/orphan case — verified identical for all four cases (active set / null / deleted-project / null clientKey). Net ≈4→≈1 page-level queries per tool nav, zero behavior change (same 403 messages, same project/client a tool opens with). `localization` actions keep the explicit gate (`await parent()` isn't available in actions). No circular import (projects.ts doesn't import auth.ts — verified). **Verified:** `pnpm --filter launcher-api build` GREEN (run directly); grep confirms the four override/project helpers are gone from the tool-page `load`s. Files: `auth.ts` (+`getActiveScope`), `(app)/{atlas,sheet,editor,localization}/+page.server.ts`.
7. ✅ **DONE (2026-05-31) — atlas-backend aligned + parked ready (self-contained).** New `services/atlas-backend/paths.py` (standalone, no `iw_common`): `project_prefix(client,project)` = `atlas_maker/<client>/<project>` (matches the launcher's `projectPrefix` + the tools' `prefix_for_tool`), `resolve_scope` (precedence `IW_CLIENT_NAME`/`IW_PROJECT_NAME` → request field → `ATLAS_CLIENT`/`ATLAS_PROJECT` env → `unassigned`/`cloud`, slug-validated), `safe_key` (reject empty/`..`/leading-`/`) + `ensure_atlas_namespace` (must start `atlas_maker/`). Endpoints now take `client`/`project` instead of a raw `prefix`: `/generate-region` writes `atlas_maker/<c>/<p>/batch/...` (was the un-isolated `atlas_maker/cloud`), `/generate-test` writes `atlas_maker/<c>/<p>/test/...` (was flat `atlas/test/`); `/compose` + `/slice` keep their caller-chosen `output_prefix` but it's now confined to the `atlas_maker/` namespace + all input keys `safe_key`-checked (so the service can't write/read arbitrary bucket paths). Added a **Dockerfile pinning `python:3.12-slim`**. `.env.example` gains `ATLAS_CLIENT`/`ATLAS_PROJECT`. `py -m py_compile` green. **Owner-review caught + fixed agent defects before commit:** the model classes had been reordered *after* their endpoints (FastAPI resolves type hints at decoration time → would `NameError` on boot — fixed; a leftover duplicate `class GenerateTest` from a stale manual edit was also removed) and the `/compose`+`/slice` validation the agent reported was missing — both corrected by hand.
   - **`iw_common` fold (full parity, done same day):** the ComfyUI client's submit/poll/upload/fetch logic was MOVED into `services/_shared/iw_common/comfy.py` (which previously held only `USER_AGENT`/`comfy_url`/`cf_headers` from #3) — now the single source for all of it. atlas-backend's local `comfy.py` + `r2.py` were DELETED; `app.py` imports `iw_common.comfy` + `iw_common.storage` (aliased `as r2` to keep call sites). The 3rd copy of the comfy client + the duplicate R2 helper are gone. atlas-tool's `from iw_common.comfy import USER_AGENT, cf_headers, comfy_url` still resolves (only additions to the module); `py -m py_compile` green for app/paths/workflows/compose + iw_common/{comfy,storage} + atlas-tool/{cloud_paths,batch_atlas,ui_server}. **Dockerfile is now REPO-ROOT context** (`COPY services/_shared/iw_common ./iw_common` + `COPY services/atlas-backend/ .`, `PYTHONPATH=/app`) — so, exactly like #3, **atlas-backend needs its Railway Root Directory set to the repo root + Dockerfile Path = `services/atlas-backend/Dockerfile`** before it'll build (until then its last good deploy stays live). Procfile left in place (harmless; the Dockerfile governs).
   - **NOTE for a future pass:** atlas-tool's `batch_atlas.py` still has its OWN inline ComfyUI submit/upload logic (it predates the shared client and wasn't touched here) — it could now also adopt `iw_common.comfy`, finishing the dedup. Out of scope for #7.
   - **⚠️ Coupled deploy (owner): set atlas-backend's Railway Root Directory → repo root + Dockerfile Path → `services/atlas-backend/Dockerfile`** (same flip as #3 for atlas-tool/sheet-tool). The push triggers a build that FAILS until that's done (old deploy stays live — no outage).
   - **Why atlas-backend is dormant (context for next session):** nothing calls it today. The launcher has an `ATLAS_BACKEND_URL` env var but no fetch; **atlas-tool runs ComfyUI generation itself** (`batch_atlas.py` talks to ComfyUI directly). atlas-backend was the ORIGINAL design — a clean stateless FastAPI generation microservice (submit workflow → poll → fetch → R2) meant to be the shared generation engine the launcher/tools call. The Atlas Maker was then ported wholesale (fix path) and brought its own generation code, so the two overlap. **Decision: align + park, don't wire up or delete.** It's now convention-compliant and deploy-ready; revisit if/when we want generation as a standalone service (e.g. to take it out of the per-request atlas-tool process, run it on its own schedule, or share it across tools). For now atlas-tool remains the live generation path.
8. **cloudflared as a Windows service** — removes the most likely generation outage (PC reboot drops tunnel). Low. ← next item (owner-side, mostly).

**Process notes (project-size hygiene, not new subagents — the 5 area agents map cleanly to the seams):**
- Big backlog of "✅ code … ⏳ live-verify owed / NOT browser-tested" — treat "code green" and "verified live" as two separate states; use the `/verify` skill as a standing discipline so the verify column doesn't grow unbounded.
- This STATUS.md is now an append-only changelog (~B1–B18 history) — split into a short *current state + open items* doc + an archived `docs/history.md` for the done-work narrative.
- For big sweeps (the shared-pkg refactor across 3 services; reconciling the gate across 9 routes) use multi-agent workflow orchestration rather than a new persistent agent.

### Key lessons from the 2026-05-29 session (don't repeat)
- **Always `git push`** after committing — a Railway service can't deploy commits that are only local. (We lost time because 5 commits were committed but not pushed.)
- **Railway staged vars** — adding a var only stages it; click "Apply changes / Deploy". A plain redeploy doesn't apply it. Prefer a **code default** for non-secret config (e.g. `ATLAS_TOOL_URL`) so deploys don't depend on the dashboard.
- **Cloudflare blocks `Python-urllib` UA with 403** — always send a custom User-Agent to ComfyUI.
- **Windows trailing-dot folders** — Windows silently strips a trailing `.` from directory names, so a folder named `symbols.` behaves as `symbols` locally but NOT on Linux/R2. Manifest ref paths like `refs/atlasslices/symbols./x.png` resolved locally but broke in the cloud ("no ref"). Fixed by normalizing path segments in the R2 manifests (`services/atlas-tool/fix_manifest_paths.py`). Watch for this on any Windows-sourced data.
- When something "isn't working" on a deployed service, **verify what the runtime actually sees** (a throwaway diagnostic) instead of guessing or re-checking config.
