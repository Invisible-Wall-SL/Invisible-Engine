# Project status & roadmap

> Where things stand and what's left. Update this at the end of meaningful work.
> Last updated: 2026-06-11.

There are two tracks in this repo:
1. **Engine & games** — the Stake-Engine fork (pixi-svelte, Svelte 5, PixiJS 8) + the games.
2. **Studio / pipeline tools** — the launcher portal + the cloud-hosted asset tools (atlas) + ComfyUI pipeline.

---

## Track 1 — Engine & games

- **RGS translator (Play4Fun):** ✅ done. `packages/rgs-translator-eagaming` maps the Stake internal request shape to the Play4Fun `/rgs/engine` protocol (verified from real Hot Fruits captures). Stake-shaped facade lets `apps/lines` run unmodified against a local mock via `PUBLIC_RGS_TRANSPORT=play4fun`. See root `CLAUDE.md` for protocol details.
- **Games:** `apps/{lines,cluster,scatter,ways,number-picker,price}` are **dev/reference** games (engine dev only). **Shipped games get their own repo** vendoring this engine as a submodule pinned to a `main` commit, deploying on their own cadence — e.g. **Book of Borut** (evolution of Hot Fruits, Book-of slot type; port 3002). Scaffold a new one with `node scripts/new-game.mjs --name "…"`. Full model: `docs/design/games-deploy.md`.
- **Repo strategy (decided 2026-06-03):** engine + tools + launcher stay in this ONE repo (shared `workspace:*` packages); games are the only split. The single `main` is kept filterable per area by **scoped commit subjects** (`commit-msg` hook), **squash-merge** (PR title = the one commit), and **CODEOWNERS** — not by re-splitting repos. See `docs/design/games-deploy.md`.
- **Rule:** engine changes go on `main` — never per-game engine branches. Don't dismantle the Turborepo/pnpm-workspace structure.
- **Invisible Editor — template/import engine contract (started 2026-06-02, branch `feat/editor-templates`):** design addendum in `docs/design/invisible-editor.md` §7 covers (1) per-game-type **templates** with named slots (`sprite`/`spine`/`text` = artist-owned, `mount` = engine-owned anchor), (2) **import** an existing game by deriving its layout from engine helpers, (3) in-editor template authoring, (4) the **symbol naming convention** (`docs/conventions/symbol-naming.md`). Engine-side landed so far: `packages/engine-layout` template types + `slotId` on nodes; `apps/lines/src/game/{template.ts,defaultLayout.ts}` (the `lines` template + the engine-truth generator that now produces `editor-scenes.ts`'s fallback doc — verified byte-for-byte parity with the prior fixture); `packages/game-spec/src/symbols.ts` (`classifySymbol`/`isKindConsistentWithId`, compatibility not equality since `S`=scatter|wildScatter); `engine-layout` template registry (`getTemplate`/`listTemplateGameTypes`) + `findUnfilledRequiredSlots` validation. **Launcher-side landed:** `projectScaffold` seeds the editor doc's scenes from the game type's template; the editor `save` action returns non-blocking unfilled-required-slot warnings; the editor UI (`EditorOutline` + header) shows each scene's slots with filled/empty status and an "N slots empty" pill. **§7.5 LANDED (PRs #10/#11, on `main`):** R2-data template override (`_shared/editor-templates/<gameType>.json`, built-in code fallback) via `templateStorage.loadTemplate`/`saveTemplate`; `POST /api/editor/template` authoring endpoint; in-editor **Template mode** (tag nodes as slots in Properties → "Save template" exports a `GameTemplate` to R2; required flags round-trip) + a **game-type selector** (lines/ways/cluster/scatter/bookOf) so you can author any game type's template online. **Editor-only — no game-rendering change.** Also: the type selector **loads + displays** the chosen template (built-in `lines`+`bookOf`, R2 override), and the canvas now renders **live spine preview** (real skeleton, static by default, per-node Play/Pause) via a transparent WebGL overlay reusing the vendored spine-webgl runtime (no new deps; new editor-gated `GET /api/editor/spine`). **Remaining:** (a) load an existing game scene + scene switcher — ✅ DONE (2026-06-03): the editor had no way to LOAD a populated layout and no visible scene list; owner's ask was simply "a Load game scene button + a scene list, template is a separate system." Delivered in `apps/launcher-api/src/routes/(app)/editor/*` + `packages/engine-layout`: (1) **scene-bar in the left panel** — a "Load a game scene…" picker (built-in PLACED layouts via `ref:<type>` + blank-from-template via `skel:<type>`) + a **Screens list** (click a screen → canvas switches; `selectScene`). (2) **Placed reference layouts** moved into `engine-layout/referenceLayouts/` (the `lines` `defaultLayout` relocated there; `apps/lines/src/game/defaultLayout.ts` now re-exports it, parity preserved) with `listReferenceLayouts()`/`getReferenceLayout()`, so the launcher can load a game's real placed+layered scenes without a cross-app import. `loadChosen`→`adoptScenes` swaps scenes + `mainSizesMap` (now a `$state`, canvas resizes) + `gameType`. (3) **Template authoring is now a fully SEPARATE, opt-in mode** — a single "Template editor" toggle (no more Fill|Template segmented control/banner); default view has zero template UI. The slot dropdown + type selector + Save-template + the Template tab only appear inside template mode. Drag-to-slot (canvas `fillRequest`→`fillSlotFromPayload`, panel/outline slot drop targets) is retained but secondary. (4) **game type persisted per-project** (no DB migration) — optional `gameType` on `LayoutDoc` (kept by `editorStorage.normalizeDoc`, written by autosave); `+page.server.ts` resolves the template from `doc.gameType ?? projectGameType()`; `seedScenesFromTemplate()` is shared by `projectScaffold`. `pnpm --filter launcher-api build` GREEN; svelte-check = the same 7 PRE-EXISTING baseline errors only (none in touched files). **Rotated/trimmed atlas frames now render upright** (2026-06-03): `drawRegionSprite` (`EditorCanvas.svelte`) + `RegionThumb.svelte` previously drew packed `rotated` frames sideways. Fixed to mirror the slicer (`services/atlas-tool/slice_atlas.py`): on-page packed rect is `(h × w)` for a rotated frame (swap), then draw under a +90° (canvas-clockwise, = PIL `rotate(-90)`) rotation into a swapped local box so it lands in the upright `(w × h)` content box; trim (`offX/offY/origW/origH`) math stays in upright space. Non-rotated path unchanged. So any project atlas (incl. HotFruits Spine atlases with rotated frames) draws correctly when a region is placed. **⚠️ Rendering caveat (still true):** the editor draws a sprite only if its art is in the PROJECT's R2 (`/api/editor/asset?key=`), so loading the `lines` reference into a different project shows correctly-PLACED placeholder boxes (+ the text node), not textures — its frames live in the `lines` game, not a launcher project. To see real art, place regions from the project's OWN atlas (renders from R2 today) or author+save a layout for a project whose assets are in R2. (b) `game_type` column on `projects` + admin field — SUPERSEDED for the editor by `doc.gameType`; DB column is a future cleanup (`projectGameType()` still stubbed `'lines'` as the no-`gameType` fallback); (c) **Reskin-layout rollout STARTED (2026-06-03)** — owner direction: the editor is the **reskin tool** — load each game's CURRENT layout placed, tweak sprite/spine sizes+positions per screen, save, and the game reads it as **new inputs at boot via `<LayoutScene>` + `loadEditorScenes()` without code changes** (parity fallback = the generator, so no-doc renders byte-for-byte as today). Rollout target = ALL games, one parity-gated element at a time (recipe + sequencing in `~/.claude/plans/prancy-roaming-cocke.md`). **Landed:** Phase 0 — documented the bound-component transform contract in `engine-layout/LayoutNodeView.svelte` (Container applies position; bound component renders at local origin using `transform.anchor/width/height` — avoids double-transform). Phase 1.1 — **Book of Borut** (`Invisible-Wall-SL/Book-of-Borut`, `33647ed`) now derives its editor fallback from a `src/game/defaultLayout.ts` generator (parity refactor of `editor-scenes.ts`; `pnpm build` GREEN). Engine (`3d76191`) added a **`bookOf` placed reference layout** so the editor's "Load a game scene" picker offers **"Book of Borut — base game"**. **Finding:** Borut's board frame is ALREADY editor-driven (`frame_bg/frame_edge` are frames in its `reelsFrame` atlas; `BoardFrame.svelte` is only the animated glow, like `lines`) — so remaining migration is **background, FS counter, loading, FS intro, logo** (Phase 1.2+). **Deliberately NOT done yet (SUPERSEDED — see Phase 4 below; submodule since bumped to `8ad8d0e` cleanly):** ~~Borut engine submodule stays at `24aa308` (a bump jumps it ~hundreds of commits — risky)~~ — STALE: it was actually at `5184d24` and the bump to current main was an 11-commit fast-forward, GREEN. **Owner-side to SEE real art / live edits:** sync Borut's spine+atlas assets to the project R2 (else placed placeholders) + set `EDITOR_DOC_SECRET` (else game uses the in-code generator, not live edits). **Phase 1.2 LANDED (Book-of-Borut `2cd618c`):** the **background** is now editor-repositionable — rendered via a `background` LayoutScene bind node (defaults to x:0/y:0/scale:1 = no-op, so the responsive `Background` keeps self-positioning; dragging/scaling the node offsets it). `Background.svelte` unchanged → render parity by construction; **background verified pixel-identical in a Chrome harness** (Borut dev :3002 + `mock-rgs-server-book` :7788/:7799 + click-through splash). **Verification-harness gotcha (DISCOVERED):** on REPEATED reloads the game stays on the press-to-continue splash / reaches only a heartbeat (`actions=[]`) to the mock — RGS **authenticate never completes**, so the populated basegame (board frame, reels, UI) + free-spin scenery don't render. This is **non-deterministic + independent of the layout changes** (an A/B with the background change disabled reproduced it; the very first fresh run DID show the full board). So verifying the remaining Phase 1.3 scenery (FS counter, FS intro = free-spin-only; logo/name = basegame) is **blocked until the local RGS-auth flow is made reliable** (or verified by the owner in-game). The Borut dev server + mocks were left running this session. **Phase 1.3 PARTIAL (Book-of-Borut `4642f46`):** **FS counter + FS intro + FS outro** now editor-repositionable via the same zero-risk no-op-bind-node pattern (components UNCHANGED → parity by construction; `pnpm build` GREEN). FS counter keeps its desktop/landscape gate; intro/outro are separate scenes to preserve tree order. Engine `bookof.ts` reference layout expanded to mirror all 6 scenes (background/basegame/overlays/fsCounter/fsIntro/fsOutro) so the editor's "Load a game scene → Book of Borut" lists them as positionable anchors. **⏳ Verify in a free-spin round** (counter panel + intro/outro overlays unchanged). **NOT migrated (deliberately, awkward fit):** **LoadingScreen** (has a required `onloaded` callback prop → doesn't fit a no-op bind node) and **logo / game-name** (live in the HUD `UI` snippet layer, not a MainContainer scene → different coordinate space). **✅ MILESTONE (2026-06-04): the editor now renders Book of Borut's real game art per screen, fully from R2** — Background (foregroundAnimation spine = the kitchen), Base game (board frame region sprites), Free-spin intro (fsIntro spine = the golden burst), etc. Reached via a chain of fixes, all live: (1) **`ssr=false`** on the editor route — it's a client-only canvas/WebGL app; SSR-rendering certain docs 500'd. (2) **TexturePacker read directly** — `editorRegions.loadRegionSet` normalizes a TP json-hash/array manifest into the Invisible region shape, so a game's own atlas (`reels_frame.json`) renders once its `.json`+page are in R2 (no convert step). (3) **camelCase spine bundles** — `projectPaths.BUNDLE_SEG_RE` was lowercase-only, so `assertBundle` threw on `foregroundAnimation`/`fsIntro`/… → spine endpoint 500; now allows `A-Z`. (4) spine endpoint wrapped → `{found:false}` not 500 on a bad bundle. (5) **bookOf removed from the "Load a game scene" picker** — its standalone-key layout couldn't render + autosaved over the seeded region-sprite doc (the picker-clobber behind the grey frame). (6) **`scripts/seed-game-editor.mjs`** converts+uploads the game atlas and writes a doc that **auto-places each screen's real art** (lists project spines from R2 + FS-counter region + label); editor-only (game uses its coded fallback). **Reskin workflow now: open a screen → real art shows → drag to reposition → autosave.** ⚠️ Cloud-native follow-up owed: the seed still reads the atlas from the local game repo + runs locally — with TP-reading, the future path is upload the atlas via `/files` (browser) + drag in the editor (no PowerShell). `EDITOR_DOC_SECRET` still unset, so the doc is an editor sandbox until wired. **Still pending:** Borut submodule bump (1.4), the cloud-native upload path, then Phase 2 (all other games). (d) spine preview's camera y-flip/positioning + node rotation are reasoned-not-eye-checked (fixes localized to `EditorSpineLayer.svelte`'s camera block). **Asset-loading feedback (2026-06-04):** opening a game/heavy template no longer shows a blank canvas while R2 assets stream in — `EditorCanvas` now tracks in-flight loads across all three sources (images + atlas/sheet regions locally, spine bundles reported up from `EditorSpineLayer` via a new `onLoadingChange` callback) with monotonic started/settled counters, and shows a centered "Loading assets… N / M" overlay with a spinner + progress bar. A per-burst high-water mark drives the fraction (resets to 0 once everything settles, so totals don't drift across doc opens) and a 250 ms defer keeps quick single drag-drops from flashing it. `pnpm --filter launcher-api build` GREEN. **Visual verification:** test online at `app.invisiblewall.org/editor` (no local run needed). **Editable game HUD (in progress 2026-06-04):** making the `<UI>` HUD layer (logo/game-name + balance/bet/win labels + the spin/menu/bet button cluster) editable in the editor. Plan: `~/.claude/plans/luminous-dazzling-metcalfe.md`. **Phase 1 (DONE, `c2d224f`):** contract — `Scene.space` (`game`/`standard`/`canvas`) + `BaseNode.screenAnchor` (window-edge pinning for HUD corners); `STANDARD_MAIN_SIZES_MAP` moved to `constants-shared` (one source, shared by `utils-layout` runtime + `engine-layout` editor) + re-exported; `LayoutNodeView` honors screenAnchor; editor draws labeled placeholders for bind nodes (were invisible), frames standard/canvas scenes from the standard box, drag writes back the edge offset, `editorStorage` preserves `space`. **Phase 3a (DONE, `9505862`):** `referenceLayouts/hud.ts` — the HUD as two reusable editor scenes (`hudBar` standard + `hudCorners` canvas) with bind nodes at the flattened absolute `LayoutDesktop` coords; wired into the `lines` reference layout, so editor "Load a game scene → Lines — base game" lists draggable HUD screens. **Phase 2 (DONE, `b87654e`):** opt-in render path — `<UI>`/`<UIDefault>` gain an optional `hud={bar,corners}` prop; when present a new `LayoutEditable` positions the game's HUD snippets from the scenes (bar in `<MainContainer standard>`, corners via screenAnchor), else the coded `Layout*` render unchanged (parity). Only placement is data-driven; menu drawer + dynamic values stay coded; `apps/lines` opts in. All builds GREEN. **Phase 3 (DONE, `0d8ab28`):** per-layoutType overrides for **landscape + tablet** added to the `hudBar` nodes (computed from the same constants as `LayoutLandscape`/`LayoutTablet`; tablet scale 1, others 0.8); `LayoutEditable` renders the matching per-layout menu drawer. **Portrait stays coded** (its bar is an animated fold-out drawer — behaviour, not static placement; `UIDefault` keeps portrait on `LayoutPortrait` even when opted in). So in the editor, switching the layoutType pills on the HUD screens shows the correct desktop/landscape/tablet arrangement. **Phase 4 (DONE, Borut `493b4a9`):** Borut's engine submodule was actually at `5184d24` (NOT the `24aa308` older notes claimed) — only **11 commits behind main + a clean fast-forward**, so the bump was low-risk (the "hundreds of commits/risky" warning was stale). Bumped to `8ad8d0e`, `pnpm build` GREEN (no breakage). `Game.svelte` now wires `<UI hud={{bar,corners}}>` reading `hudBar`/`hudCorners` from the LIVE editor doc only (no fallback) → coded HUD renders unchanged until the HUD scenes exist, then data-driven. **Enabler (`8ad8d0e`):** new non-destructive **"+ Add HUD layer"** editor action appends `hudScenes()` to a project's doc (vs the clobber-prone "Load a game scene"); `hudScenes` exported from `engine-layout`. Reference-load picker also hardened to non-destructive (confirm + preview, no autosave-clobber) — `6c567e3`. **Remaining OWNER step to light it up:** in the editor on `borut/book_of_borut` → "+ Add HUD layer" → Save (writes the HUD scenes into the live doc) → reload the game (`?k=`) → HUD renders from the doc and is editable (logo/name + balance/win/bet + button cluster; desktop/landscape/tablet). (`EDITOR_DOC_SECRET` is set, so the loop is live.) (Known v1 limits: portrait HUD not editor-driven; tablet in-bar FS-counter + dynamic values stay coded; HUD elements render as labeled placeholder boxes in the 2D editor.)

- **Invisible Editor — parametric Button `action` dropdown: ✅ LANDED (2026-06-09).** The `action` param (which selects a HUD button's BEHAVIOUR — spin / decrease / increase / autoSpin / turbo / menu / buyBonus — resolved at runtime via `getComponentAction` → the game's `registerComponentActions`) was a free-text box (silent typos). New `ENGINE_ACTION_CATALOG` (`componentCatalog.ts`) lists the canonical action keys; the editor now renders ANY param keyed `action` as a **dropdown** of the catalog in both the per-instance Params panel and the component Defaults panel (recognised by key, NOT by `options`, so it works on existing project Button defs without re-creation — and avoids tripping the `sourceParam` options-detection in the component-variables picker). A custom/out-of-catalog value is preserved as an extra option. Adding a NEW behaviour is still a game-side step (`registerComponentActions` in the shipped game's `Game.svelte`). **Decision (owner, 2026-06-09):** a shared `registerStandardHudActions(...)` helper to remove the per-game registration boilerplate was considered and **deliberately deferred** — if games end up diverging a lot, a shared standard-actions helper becomes a leaky abstraction; keep per-game registration explicit until a stable common core proves itself. (A full no-code custom-action system — visual scripting / composable primitives — is the bigger alternative, not planned.) `engine-layout` + `launcher-api` builds GREEN. ⏳ Owner-verify live.
- **Invisible Editor — resizable panels + persisted workspace layout: ✅ LANDED (2026-06-09).** The left + right (Properties) panels are now **drag-resizable** (handles on each inner edge; `beginResize` tracks via `window` pointer listeners so the drag follows even over the canvas, which gets `pointer-events: none` while dragging; widths clamped). The `.layout` grid columns are now driven by `leftWidth`/`rightWidth` state, and the existing canvas `ResizeObserver` re-fits the view automatically. **Persisted per-project** in `localStorage` (`iw-editor-ui:<projectKey>`, view-state only — never the doc): panel widths, the active left tab (library/outline), and **layer visibility** (`hiddenScenes` eye toggles) all restore on reopen. Loaded in `onMount`, re-serialised by a guarded `$effect`. **Collapsible state persisted too (follow-up, same day):** the Library atlas/sheet expansions (`libExpanded` in the same per-project store; restored-open atlases lazily re-hydrate their regions) AND the param-group `<details>` open/closed state (new `editor/groupCollapse.client.ts` helper, keyed `<componentId>:<group>`, used by both the instance Params panel and the component Defaults panel via controlled `open` + `ontoggle`). `launcher-api` build GREEN. ⏳ Owner-verify live.
- **Invisible Editor — "Expose text as params" (one-click, grouped per text node): ✅ LANDED (2026-06-09).** A text node in the Component Editor now has an **✨ Expose text as params** button that auto-creates + binds author params for its `text` / `font` / `size` / `colour` in one click (`exposeTextParams` in `components/+page.svelte` — keys namespaced `<slug><Field>` + deduped, defaults seeded from the node's current values, sets `node.paramBindings` for `text`/`style.fontFamily`/`style.fontSize`/`style.fill`; idempotent, skips already-bound fields). New `ComponentParam.group` + `label` (editor-only display hints, persisted by `normalizeParams`): the per-instance Params panel AND the component Defaults panel now render params **grouped into collapsible `<details>`** titled by the text node's name (plain field labels inside), so a component with several text objects edits each one independently. Both panels refactored onto a shared `{#snippet}` (`paramField` / `defRow`). Engine resolves these via the existing `LayoutNodeView` text/style bindings (+ `EditorCanvas` preview) — not cosmetic. Builds GREEN (`engine-layout` + `launcher-api`); code-reviewed (no blockers). ⏳ Owner-verify live.
- **Invisible Editor — `image` param kind (atlas-region picker) + cross-window component refresh: ✅ LANDED (2026-06-09).** Follow-ups to the custom-params work. **(a) Refresh bug:** a param added in the Component Editor didn't appear on the placed instance in the scene editor — the scene editor only re-pulled defs on `visibilitychange`, which never fires when the Component Editor is popped into its own WINDOW (both tabs stay `visible`). Fixed by also binding the refetch to window `focus` (`editor/+page.svelte`). **(b) Image picker:** new `ComponentParam.kind` `'image'` — a STRING value (atlas frame name) the editor renders with a **region picker** (thumbnails) instead of a free-text box, so authors pick art visually. New reusable `editor/RegionPicker.svelte` (lazy `fetchRegions` + `RegionThumb`); `image` option in "Your params"; `image` branch in the per-instance Params override (scene editor) AND the component Defaults panel (component editor); `image` added to the atlas-frame bind dropdown (`paramsForKinds(['string','image'])`); `pickSheets` (atlas-manifests + sheets) derived from `data.assets` in both pages; `normalizeParams` persists `'image'`. Engine needs NO branch — an image value resolves through the existing `region` sprite binding. **(c) Canvas preview (follow-up, same day):** the scene-editor canvas now reflects per-instance bindings. `EditorCanvas`'s `boundString`/`boundNumber`/`boundTextValue` read the OPEN-COMPONENT params (empty in the scene editor) and ignored the per-instance params `drawComponentInstance` already threads as `instanceParams` — so a placed instance's overridden image/text/fill didn't show on the canvas. Now they prefer `instanceParams ?? componentParams` (helpers take the params explicitly), so the canvas redraws the sprite with the picked frame (and text/fill overrides) live. Parity: both empty ⇒ static fallback. Builds GREEN; code-reviewed (no blockers). ⏳ Owner-verify live. **(c2) Cross-atlas preview (same day):** removed the "frame must be in the sprite's static atlas" limitation — when a BOUND region isn't in the sprite's own atlas, the canvas now consults the existing one-time `spriteRegionIndex` (frame name → atlas, the same lazy scan bind-anchor previews use) and `ensureRegionIndex()`, matching the game's by-name cross-atlas resolution. Scoped to `boundRegion && !boundAssetKey` on a MISS only, so normal sprites + same-atlas binds skip it entirely — no per-draw scan, no eager loading (the full scan fires once per session, only when a bound frame truly lives elsewhere). **(c3) Cross-atlas centralised in `findRegion` (same day):** the (c2) resolution only covered BOUND regions, so a recurring "image not showing — gray placeholder with the frame name" hit any sprite whose STATIC `assetKey` doesn't pack the frame (e.g. a widget background dragged from a different atlas, or a def whose sprite points at a stale atlas). Moved the cross-atlas fallback INTO `findRegion` (the single chokepoint used by every region draw + `naturalSize`): on a miss in the named atlas it resolves the frame by name via `spriteRegionIndex` (kicking `ensureRegionIndex()` if unbuilt). So bound OR static, the canvas resolves a frame exactly like the game does. Removed the now-redundant per-sprite-branch block. Still miss-only ⇒ same-atlas sprites pay nothing. **(d) Native color pickers (same day):** the remaining `#rrggbb` hash-string text inputs are now native `<input type="color">` pickers — the per-instance color-param override (NEW `color` branch in the `authorParams` loop, with a × to inherit the default), the bound-node color param (`editableParams`), and the static sprite **tint** + text **fill/stroke/dropShadow** fields. Stored values stay numeric (parsed via the existing `parseHex`/`setTintHex`/`hexFrom`). The component Defaults panel already used a native picker. **Data note for image params:** the picker only renders for params of kind `image` — a `string` param shows a text box. To convert, remove the string param in "Your params" and re-add it with the SAME key + kind `image` (the sprite's `region` binding is by key, so it survives).
- **Invisible Editor — custom (author-defined) component params: ✅ LANDED (2026-06-09).** The Component Editor's "Component variables" panel now has a **Your params** block (`EditorProperties.svelte`): an add form (key + kind string/number/color/boolean) + a removable list, wired through new `onAddParam`/`onRemoveParam` props to `addCustomParam`/`removeComponentParam` in `components/+page.svelte`. **Author flag:** added params carry `ComponentParam.author = true` (new optional field, persisted by `normalizeParams`) so the removable "Your params" list shows ONLY user-added params — a component's built-in/coded params (Button's `action`/`icon`/`variant`/`tint`…) can't be deleted by mistake. **UX cleanup (same day):** the node's "Bind to param" controls were cramped into one flex row (clipped labels); now stacked full-width (`.bind-grid`) with the common **Image — atlas frame** binding first, **whole texture** + **Tint** below, each with a one-line hint, and a 3-step explainer (add param → bind a node field → set per instance). Unlocks **per-instance graphic swaps**: declare e.g. `bgAtlas`/`bgFrame` (string), then on the component's background **sprite** node use Properties → "Bind to param" → *Texture ← param* (`assetKey`) / *Atlas frame ← param* (`region`) — the existing engine plumbing (`LayoutNodeView` sprite bindings + `ComponentInstance` param threading) already resolves it, so each placed instance gets a text field to swap its background with no per-button scripting. `pnpm --filter launcher-api build` green; not browser-verified (auth-gated tool).
- **Invisible Editor — complete + reconcile the scene set + "Add missing screens" button (2026-06-08):** owner report — opening Book of Borut in the editor, not all game screens appeared (the **logo/loading splash was missing**) and free-spin intro/outro looked like duplicates. **Root cause:** the editor renders `doc.scenes` from the saved R2 doc, and the FOUR scene-set sources had drifted — none defined a `loading`/logo scene (it lived only in coded `LoadingScreen.svelte`). **Fix (`docs/design/invisible-editor.md` §15, code-only, parity-safe, additive):** (1) `boundComponentCatalog.ts` — new `LoadingScreen` default (preview spine `loader` = the `title_screen` logo) so the editor draws the splash; (2) `templates/{bookof,lines}.ts` now enumerate the **full screen set** (loading/background/basegame/[freegame]/overlays/freeSpin intro+counter+outro), new mount slots NON-required (no validation noise; HUD stays the universal `hudScenes()` layer); (3) `referenceLayouts/bookof.ts` += loading scene + `...hudScenes()`; (4) `referenceLayouts/lines.ts` += inert loading + freeSpin scenes (deliberately NO `background` scene — keeps `apps/lines` on its §10.6 exact-cover default); (5) `seed-game-editor.mjs` `buildDoc()` += loading scene. Parity: every new scene is a `bind` anchor to an unregistered coded component + the game looks scenes up by id (never iterates all) → inert in-game. **No-console owner path:** new editor scene-bar button **"＋ Add missing screens (N)"** (next to "Add HUD layer") diffs the doc vs `engine-layout` `getFullSceneSet(gameType)` (covers lines+bookOf) and appends only ABSENT scenes by id, non-destructively (autosave persists) — mirrors `addHudLayer()`; shows only when something's missing. So picking up the logo scene is one click, no re-seed. `pnpm --filter {engine-layout,launcher-api} build` + `apps/lines` `vite build` GREEN. **Deliberately deferred/explained:** `freegame` is template-declared but given no placeholder content (no distinct art yet → would be a fake duplicate); intro/outro look identical because Borut ships no `fsOutro` spine so the preview falls back to `fsIntro` (faithful — the game reuses the frame too). **Move 2 LANDED (2026-06-08): single source via generated JSON + automation.** The `.mjs` seed and the TS `bookofReferenceLayout()` were two scene-set generators (the seed even inlined a ~180-line `hudScenes()` copy) that drifted. Now: `packages/engine-layout/scripts/gen-scene-sets.mjs` (uses **esbuild** — in-tree via Vite; no runtime TS loader exists — to bundle the pure-data reference graph, no `.svelte` in it) emits `packages/engine-layout/scenes/bookof.json` from `bookofReferenceLayout()`, deterministic, with a `--check` drift guard. First reconciled the reference's silent drift (`background`/`overlays`/`freeSpin*` were missing `space:'canvas'`; added slotIds) and verified its non-basegame scenes are byte-identical to the seed's prior literals. The seed now `import …'engine-layout/scenes/bookof.json' with {type:'json'}` (new `./scenes/*` export) and composes from it, overriding ONLY `basegame` (manifest region sprites) — deleted ~260 lines (inlined HUD + scene literals); HUD now comes from authoritative `referenceLayouts/hud.ts`. **Automation so it can't be forgotten:** `gen:scenes` runs in engine-layout's `build`, AND the pre-commit hook runs `--check` when a reference/template/seed/scenes file is staged (stale JSON → commit blocked with the fix command). Verified: stale→exit 1, fresh→pass; engine-layout + launcher builds GREEN; seed JSON import resolves from its dir. **Next (Move 3):** drive each screen's show/hide from the doc (route B, §8.7) so the editor OWNS, not just previews, every screen.
- **Symbol-debug overlay + atlas-manifest validator (2026-06-05):** shipped a reusable, game-agnostic **`SymbolDebugOverlay`** in `packages/components-pixi` — a symbols×states grid that magenta-marks any cell whose asset key isn't in the live `loadedAssets`, so a game can surface missing-symbol "holes" without per-game debug code. `IE_DEBUG`-gated + `d` to toggle; `apps/lines` is the reference consumer and **Book of Borut** consumes it via a thin wrapper. The whole grid sits under one transform root reserved for a **future zoom/pan + paylines view** (TODO). The Borut "missing symbols / holes" hunt's root cause was an **atlas-tool deploy bug** (fixed + shipped earlier same day): `_deployatlas` built the TexturePacker frame-map from `regions` only and **dropped every `rotated_regions` symbol** → 8 of 17 `symbolsStatic` frames had no frame entry → invisible cells. To stop that whole class of drift, added (`launcher`) **`scripts/derive-atlas-manifest.mjs`** (derive a project's atlas manifest from the game's committed sheet) + **`atlasManifestCheck.ts` / `GET /api/atlas/manifest-check`** (flags missing/extra/rotation-mismatched frame keys vs the game config). Also hardened the game's `getSymbolInfo` to return a silent fallback instead of throwing on an undefined symbol/state. **Deploy-path automation (same day):** the Atlas Maker's manual `deploy_path` was error-prone (empty → files dumped to the `deploy/` root, where the game can't find them; sticky field → wrong folder). Now `derive-atlas-manifest --all --game-root <repo>` scans the game's **git-tracked** `static/assets/` tree → publishes a per-project **`asset-map.json`** (stem → `deploy_path`/`deploy_basename` for every TexturePacker sheet + Spine page) to R2 + backfills empty `deploy_path` on existing manifests; the atlas-tool Deploy reads that map to **auto-resolve** an empty `deploy_path` (explicit field still overrides), and now **warns loudly** only on a genuine no-match root-dump. Validator flags `deployPathMissing`. Published for `borut/bookofborut` (23 assets indexed; `symbols/symbols2/symbols3/transition` backfilled). **Re-run `--all` when the game's asset layout changes.** Known leftovers: 3 manifests hold stale Windows-absolute `deploy_path`s (need `--force-deploy-path` or a manual fix); `mmBG` + scratch sheets are unmatched — `mmBG` is the **background Spine** (`mm_bg` → `spines/foregroundAnimation`), so a sprite-sheet deploy there only helps if it replaces the spine page. **Spine page-only deploy (same day):** the background reskin exposed that Deploy's TexturePacker `<base>.json` write **clobbers a Spine skeleton** (`mm_bg.json` is the 88 KB skeleton, not a frame-map). Fixed: asset-map entries now carry `kind` (`spine`|`sprite`), and `_deployatlas` does a **page-only deploy** (writes only `.png`/`.webp`, skips the `.json`/`.atlas`) when the target is a Spine — detected via the manifest `deploy_page_only` flag, asset-map `kind=="spine"`, or an existing skeleton at the target. So spine-page reskins (`mm_bg`) are now one-click safe. (Background reskin: confirmed page is shape-compatible 2028×1439; the stray TP `mm_bg.json` + `mmBG.*` leftovers were purged from R2 deploy/.)

- **Invisible Editor — fonts (edit text + see/ship real game fonts): 🚧 Phases 0+1+2 + Phase-4 HUD-text LANDED (2026-06-06), code-only/not deployed/not browser-verified.** Owner ask: from a text node's Properties panel, (1) see the same fonts in the editor the game renders, (2) edit the text → appears in-game, (3) change the font/bitmap → appearance changes in-game. **Render path DECIDED (owner, §9.5): WebGL/PIXI overlay** (pixel-exact PIXI parity over a 2D blitter). **Phase 4 HUD-text override (owner-chosen next step) done:** the coded HUD **logo / game-name** corners now take an editor-authored **font+size+fill+label** override (Book of Borut/all games have these as `container` bind anchors, not `kind:'text'` nodes — so the Phase 2 text picker didn't reach them). Contract: `engine-layout` `HudTextOverride` + `getHudTextOverride` (stored on `bind.props`, persisted by `normalizeNode` pass-through); engine `hudPositions.hudTextOverride()` → `<LayoutEditable>` hands it to the `gameName`/`logo` snippets (`Snippet<[HudTextOverride?]>`, ignore-arg = parity), forwarded through `<UI>`/`<UIDefault>`, merged in `<UiGameName>` + `apps/lines` snippets. Editor: a **"HUD text"** Properties section (font dropdown + size + fill/tint + label) for a `preview.style:'text'` bind anchor; the PIXI overlay renders the anchor in the chosen font (2D chip steps aside). `pnpm --filter launcher-api build` + `lines` svelte-check GREEN. **To SHIP in-game** a game bumps its engine submodule + applies the `gameName`/`logo` snippet merge (lines = reference) + rebuilds; the **editor preview needs no game change**. **Phase 2 (Properties: edit text + pick font) done:** `EditorProperties.svelte` text node — freeform `fontFamily` input → a `<select>` from `/api/editor/fonts` (`(game default)` + each catalog font tagged `[bitmap]`/`[web]`, off-catalog family kept as `(custom)`, empty-catalog hint); `text` textarea kept; for a bitmap font the `fill` field relabels to **tint**, a note explains tint+size-only, and Stroke + Drop-shadow are gated off (`{#if !isBitmapSelected}`). `pnpm --filter launcher-api build` GREEN. Tradeoff: arbitrary non-cataloged web families can no longer be typed (sync into the catalog first). **Only Phase 3 remains** (engine `LayoutNodeView` `<BitmapText>` + game boot-load + parity gate — the single engine-contract change; needed for the fonts to actually SHIP in-game). **Phase 1 (editor shows the real fonts) done:** `EditorTextLayer.svelte` — a transparent raw-PIXI `Application` over the 2D canvas (mirrors `EditorSpineLayer`) rendering top-level text nodes as `PIXI.BitmapText`/`PIXI.Text`, positioned via the 2D canvas's `nodeTransform` → `world*zoom+pan` (passed as `worldTransformOf`, one coord source). `fonts.client.ts` loads bitmap fonts via PIXI `Assets.load` (descriptor `<page>` refs rewritten to absolute editor-gated URLs by `/api/editor/asset?font=1` — the relativity gotcha) + web fonts via `FontFace`. Loads fold into the "Loading assets…" overlay; overlay reports owned node ids so the 2D `fillText` is skipped (no double-draw); "↻ Reload art" re-fetches the catalog. `pixi.js@8.8.1` added to the launcher. Built mostly by the engine-pixi-svelte agent; `pnpm --filter launcher-api build` GREEN. **Caveats:** top-level text only (nested-in-container → 2D fallback); matches the game's pixi anchor not the old `fillText` baseline (intended); a changed font PAGE needs a full reload. **Next: Phase 2** (Properties font dropdown + text edit) → Phase 3 (engine `LayoutNodeView` `<BitmapText>`, the only contract change). **Phase 0 (contract + fonts into R2, the spine analogue) done:** `packages/engine-layout/src/lib/fontCatalog.ts` — `FontEntry`/`FontCatalog` types (`kind: 'bitmap' | 'web'`, files relative to a `folder`) + `findFont`/`isBitmapFont`, exported from the barrel. Launcher: `SUB.fonts` + `fontCatalogKey`/`fontBundlePath`/`sharedFontsPrefix` (`projectPaths.ts`); `includeSharedFonts` scope opt (`toolScope.ts`) wired into `/api/editor/asset` (+ xml/fnt/woff2/woff/ttf/otf content types); `lib/server/fonts.ts` `resolveEditorFonts` (project `fonts.json` → `_shared/fonts/` fallback, each file → editor-gated `/api/editor/asset` URL); `GET /api/editor/fonts` (gated, `{ fonts: [] }` on no-catalog, never 500). Sync: `scripts/r2-sync-fonts.mjs` (clone of `r2-sync-spines.mjs`; parses BMFont `<info face>`+pages, web fonts by ext; `--dry-run` verified against the `lines` fonts → gold/silver/purple/goldblur). `pnpm --filter {launcher-api,engine-layout} build` GREEN. **Owner step to seed:** run `node scripts/r2-sync-fonts.mjs <fontsDir> <client> <project>` with R2 creds (defaults to the `lines` fonts → `borut/bookofborut/fonts/`). **Next: Phase 1** (2D BMFont blitter in `EditorCanvas` so the editor shows the real fonts) → Phase 2 (Properties font dropdown + text edit) → Phase 3 (engine `LayoutNodeView` `<BitmapText>`, the only contract change). Two gaps to close: the editor draws text with Canvas2D `fillText` (system-font fallback, loads none of the game's fonts) AND the engine's layout text path (`LayoutNodeView` `<Text>`) can't render a bitmap font even in-game — so the game's bitmap fonts (`gold`/`silver`/… from BMFont `.xml`+page, rendered via `<BitmapText>`) never show in a layout text node. Full plan: **`docs/design/invisible-editor.md` §9** — font catalog (`fonts.json` in R2, single source of truth) + `r2-sync-fonts.mjs` + `/api/editor/fonts` (Phase 0); editor 2D-canvas BMFont blitter for fidelity (Phase 1); Properties font dropdown + text editing (Phase 2); `LayoutNodeView` `<BitmapText>` + game boot-load + parity gate (Phase 3, the only engine-contract change). **Scope boundary:** static/semi-static layout text nodes only; dynamic coded values (FS counter, balance/win) stay coded. **Open decisions (§9.5):** editor render path (2D blitter recommended vs WebGL overlay), whether dynamic-text fonts are in v1, bitmap "change appearance" = tint/scale/swap not arbitrary recolor.

- **Invisible Editor → Atlas Maker deep-link: ✅ SHIPPED (2026-06-05).** A selected SPRITE node's Properties now has an **"🧩 Open in Atlas Maker"** button (`EditorProperties.svelte`). It links to `/atlas?atlas=<sheet>&region=<key>` (new tab); the launcher `/atlas` redirect forwards `atlas`+`region` to the tool; the atlas-tool (`ui_server.py`: `resolve_atlas_to_manifest`/`_manifest_deploy_stem`/`_handle_deeplink`) maps the deployed sheet → its **generation manifest** (direct name match, else by `deploy_basename`/`deploy_path` stem), sets it active, and scrolls+flashes the region card. **When no Atlas Maker recipe owns the sheet → AUTO-IMPORT (`88f9da5`, 2026-06-05, built in a worktree + code-reviewed):** the tool reads the existing TexturePacker/editor sheet from `manifests/<sheet>` (already hydrated), converts frames → snake_case regions (mirrors `tpRegions.mjs`), slices the page into per-region `output_override` art (mirrors `slice_atlas.py` rotation/trim; each slice `_mirror`ed to R2) so cards show the current art and Create Atlas re-packs it, sets `deploy_path`/`deploy_basename` from the project `asset-map.json`, activates the new `atlas_manifest_<stem>.json`, and opens it. **So "Open in Atlas Maker" now ALWAYS lands in the Atlas Maker** (silently creates the recipe the first time). The earlier Sheet Maker fallback redirect was REMOVED; an import failure shows the `ATLAS_IMPORT_FAILED` diag (never a 500). Guards an existing same-stem recipe from being clobbered (activates it instead). `import_sheet_to_manifest` + helpers in `ui_server.py`; new diag in `diag_catalog.py`. Self-contained in the atlas-tool (the editor button + launcher forward from `a8500ba` are unchanged; the sheet-tool `?import=` handler is now dead code for this flow but harmless). **End-user loop:** Open in Atlas Maker → replace a frame via "Choose file" override → Create Atlas → Deploy → next game deploy pulls the new sheet. ⏳ Not browser-tested live. **Provenance note:** the original resolver half landed in `d4e6795` (a concurrent session's `git add -A` swept it in); the launcher/editor/diag half is `a8500ba`; the auto-import is `88f9da5`. Deploy: atlas-tool auto-deploys from `main`. **Round-trip caveat (reviewer):** re-pack is re-fit from the sliced art (alpha-crop + LANCZOS via `fit_to_region` spine-slot path), not byte-identical for trimmed/transparent frames — visually identical for opaque art.

- **Editor — "↻ Reload art" + asset cache-bust: ✅ SHIPPED (`795ed90`, 2026-06-05).** The editor cached each atlas/spine page in memory by a stable URL (only re-pulled on a full reload), so replacing a PNG in R2 didn't show. Added a **"↻ Reload art"** button next to Fit (`EditorCanvas.svelte`) that clears the per-session image + region caches and bumps a cache-bust token; the spine layer (`EditorSpineLayer.svelte` + `editorSpine.client.ts`) drops its bundle cache on the forwarded token and the always-on RAF re-fetches; asset URLs now carry `?v=` (endpoint is already `no-store`). **Caveat:** re-fetches the SAME R2 key the doc references — doesn't help if the update wrote a different key (e.g. an Atlas Maker Deploy lands in `deploy/`, not the manifest's `source_image_path`).

- **Editor — visualize + position empty MOUNT slots: ✅ SHIPPED (`1d443e5`, 2026-06-05).** An empty `mount` slot (e.g. "Reel grid") had no node → invisible + un-placeable. Now the Slots list shows **"+ add"** on an empty mount; clicking materialises the engine anchor as a labeled, draggable box on the canvas (defaulted to the board frame's rect via slot `boardFrame`, else a centred box) and selects it. Universal across game types (driven by the active template's mount slot via the now-exported `mountAnchor`, with an optional position/size init). `ContainerNode` gained optional `width`/`height` and `resolveTransform` surfaces them for containers — editor draws/sizes a real box; a PLAIN container ignores them in-game, a `bind` container forwards them to the mounted component (LayoutNodeView contract). Existing docs unchanged (no container had a size) → zero game-render change. Files: `engine-layout/{types,resolveTransform,seedScenes}.ts`, `EditorOutline.svelte`, editor `+page.svelte`. **Note:** the anchor's position drives the in-game component only for games that consume it via the layout; bookOf's `ReelGrid` still self-positions in code (wiring it to read the anchor is a separate reskin step).

- **Invisible Editor — Components (prefabs) v1: 🚧 LANDED code-only (2026-06-06, branch `editor-component-maker`), not deployed/not browser-verified.** Builds §8 of `docs/design/invisible-editor.md` — the reusable composite tier between node and scene — scoped to **compose + reuse + declare engine variables, NO authored behavior/timeline** (the §8.5/§8.6 GSAP layer is deferred). Owner scope decisions: one `ComponentDef` + a `category` tag (`ui`/`overlay`/`scenery`; a button = `category:'ui'`); the "provided list" of engine-set variables is a **curated code-owned catalog** in `engine-layout`. **Landed (4 commits):** (1) **schema** — `ComponentDef`/`ComponentParam`/`ComponentSignal`/`ComponentInstanceNode` + `ComponentCategory` in `engine-layout/types.ts`; `componentInstance` added to the `LayoutNode` union + `editorStorage` normalize set; new `componentCatalog.ts` (`ENGINE_PARAM_CATALOG` = bet/win/balance/totalWin/playerName/projectName + `ENGINE_SIGNAL_CATALOG` = enter/exit/idle/win/bigWin). (2) **engine render** — `ComponentInstance.svelte` resolves a def via a new `registerComponents` registry (mirrors `registerBoundComponents`) and renders `def.root` through the existing `LayoutNodeView` walk (so a component composes identically to an inlined container); `LayoutNodeView` gains a `componentInstance` branch; cycle guard + 2-level depth cap (`MAX_COMPONENT_DEPTH`, shared) via Svelte context; parity Storybook story in `apps/lines`. (3) **launcher storage/API** — `componentStorage.ts` (mirrors `templateStorage`: project `editor/<p>/components/<id>.json` shadows shared `_shared/editor-components/<id>.json`; load/save/list + normalize) + `GET/POST /api/editor/component` + `GET /api/editor/components`, gated to the `editor` tool. (4) **editor UI** — a third **component mode** on the existing canvas (scene/template/component): `EditorComponentPanel` (list by category, create, open-to-edit, save, picker that drops a `componentInstance`); **"Edit as component"** dissects a selected container into a `ComponentDef` draft via a synthetic scene (reuses canvas/outline/properties); a **variable-declaration UI** picks params/signals from the catalog (metadata only); the editor canvas resolves+draws an instance from a `componentMap` prop (its own renderer). **Invariant (review-fixed):** a component `root` is normalised to LOCAL space (identity transform) on create / "edit as component" / authoritatively on save, so the engine (applies root transform) and editor canvas (skips it) agree and a dropped instance isn't offset. `pnpm --filter {engine-layout,launcher-api} build` GREEN; code-reviewed. **Remaining:** deploy + browser-verify the create→declare→save→place loop online; extract a first real static composite (logo+frame) as the first shipped component; THEN the deferred §8.5/§8.6 behavior layer (signals→GSAP timeline, `registerComponentSignals`, per-signal track editor) + migrating a coded overlay (`Transition`).
- **Component authoring extracted into its own tool — "Invisible Component Editor" at `/components`: 🚧 LANDED code-only (2026-06-06, branch `components-tool`), not deployed/not browser-verified.** Owner re-scope: component AUTHORING is no longer a mode inside `/editor` — it's a standalone full-page launcher tool. **New tool** `componentEditor` registered in `roles.ts` (`name:'Invisible Component Editor'`, `url:'/components'`, nested-squares icon; granted to the same roles as `editor`). **New route** `src/routes/(app)/components/` (`+page.server.ts` + `+page.svelte`, `ssr=false`): left = components sidebar (create by name+category, open) + asset library; centre = the REUSED `EditorCanvas` fed a synthetic single `Scene` from the open component's `root.children`; right = REUSED `EditorOutline` + `EditorProperties` (with `componentMode` variable-declaration UI). **Reuse approach:** cross-route imports of the editor's child components (`../editor/EditorCanvas.svelte`, `EditorOutline`, `EditorProperties`, `RegionThumb`, `editorRegions.client`) — NOT copied, NOT moved — so `/editor` keeps owning them unchanged. **Gate:** the page + the `/api/editor/component[s]` routes both gate on the **`editor`** tool (the `componentEditor` entry only controls the home-grid card; granted to identical roles so they never diverge). **Deep-link contract:** `/components?id=<id>[&project=<key>]` opens that component on mount (project resolved from session scope; the `project` param is accepted but unused). **`/editor` slimmed:** `mode` reverted to `'scene'|'template'` (component mode removed); `EditorComponentPanel` is now a lean PICKER (list + place a `componentInstance` + "Open Component Editor ↗"); **"Edit as component"** now MATERIALISES the selected container into a project `ComponentDef`, POSTs it, and opens `/components?id=…&project=…` in a **new tab** (Atlas-Maker deep-link pattern) — the scene is NOT mutated in v1. Editor canvas still resolves/draws placed instances from `componentMap`. `pnpm --filter launcher-api build` GREEN (svelte-check: 0 new errors, 1 pre-existing removed). **Remaining:** deploy + browser-verify (a) /components create→edit→declare→save→reload, (b) ?id= deep-link, (c) /editor place + "Edit as component" → new tab, (d) /editor scene+template still work.
- **Editor↔game rendering parity + background layout pipeline (2026-06-06/07, owner-verified live):** a run of fixes making the editor preview match the running game, ending in a fully pipeline-ized background. **(1) Rotated atlas frames** render upright in the editor (`EditorCanvas` `drawArtRegionSprite` matches the slicer's −90 pack). **(2) Editor edits reach the game** — `LayoutNodeView` folded `scale` into `width/height` so a pixi-svelte `width`-setter no longer clobbered the editor's scale (`engine` `016c3f8`); `/api/editor/doc` → `no-store`; `loadEditorScenes` logs loudly when it falls back to the bundled layout. **(3) Per-screen z-ordering** — the editor composites one DOM group per screen, z-ordered by screen order, so a background spine sits BELOW the base-game 2D (`e65b595`). **(4) Spine sizing from authored data** — `skeleton.getBounds()` returns 0 for animation-driven art at the setup pose, so cover-fit stranded at scale-1 (raw 2× art); now sizes from `skeleton.data.width/height` in BOTH the editor (`EditorSpineLayer`, `7f6277c`) and the game (pixi-svelte `BaseSpineProvider`/`spineSizeScale`, `a804bfa`). **(5) Background pipeline = `docs/design/invisible-editor.md` §10 (steps 1–5 DONE, owner-verified):** ONE shared `coverTransform` helper (`engine-layout`, true cover from art dims) used by editor + game; the editor composites EVERY scene against ONE fixed window per `layoutType` (`STANDARD_MAIN_SIZES_MAP`) — switching screens no longer rescales, background previews full-bleed (`0f9304a`, the big one); background cover **scale + fit** are doc-driven + editable in Properties (`backgroundCoverScale`/`backgroundFit` readers + `BaseNode.fit`, `605af7f`); the GAME runtime honors them via `LayoutNodeView` → a `cover={{scale,fit}}` prop on background bind anchors + pixi-svelte `SpineProvider fit:'cover'|'contain'` (`1e043dc`). **Book of Borut** `Background.svelte` reads the `cover` prop (`93b8291`, submodule → `1e043dc`); its background is now full-bleed and editor-cover-driven. All additive/parity-safe (no `cover`/`fit` = renders as before). **Gotcha surfaced:** game deploy is NOT automatic for engine code — the game's `pnpm build` runs `build:engine` (rebuilds engine dists) then publishes via `publish-game-bundle.mjs`; the desktop launcher's Build&Deploy needs the project's **Portal project key** set (`bookofborut`) or it won't publish. The shared `main` ↔ game-submodule loop: bump submodule → `pnpm build:engine` → rebuild → publish.

- **Invisible Editor — configurable grid primitive (`reelGrid`): 🚧 Phase 1 LANDED code-only (2026-06-08), not deployed/not browser-verified.** Owner re-scope of the Component Editor: stop authoring empty shells — instead **open the real game's parts and expose their layout PARAMETERS** (board position, rows, columns, cell size, padding) for visual editing, with **static placeholders** standing in for the dynamic symbols. This is route (B)/§8.7's "configurable grid primitive"; the coded spin/symbol logic stays code. Investigation: board layout is fully derived from per-game `constants.ts` (`SYMBOL_SIZE`/`BOARD_DIMENSIONS`/`REEL_PADDING`) via `boardLayout()`+`getSymbolX/Y`; the engine already declares a `reelGrid` mount slot but emits no node. **Phase 1 (editor-only, parity-safe in-game):** new `ReelGridNode` (`kind:'reelGrid'` + `reels`/`rows`/`cellSize`/`reelPadding`) in `engine-layout/types.ts` (added to `LayoutNode` union); the engine's `LayoutNodeView` `if/else` chain has no branch → **renders nothing in-game** (coded `Board.svelte` still draws symbols, byte-parity); `editorStorage.normalizeDoc` `NODE_KINDS += 'reelGrid'` (round-trips params); seeded with the REAL board values into `referenceLayouts/bookof.ts` "Base game" scene (centred + per-layoutType overrides); editor draws a `reels×rows` placeholder grid (`EditorCanvas.drawReelGrid` + `editorCanvas.helpers.nodeBox` footprint), a "Reel grid" Properties section (reels/rows/cell size/reel padding number fields), `EditorOutline` glyph `⊞`. `pnpm --filter {engine-layout,launcher-api} build` GREEN. Plan + caveats: `docs/design/invisible-editor.md` §11. **Owner verify:** `/editor` on Borut → Load the bookof game scene → select **Reel grid** → confirm it draws over the board, moves/scales, and the param fields reshape it. **Phase 2 LANDED for engine + `apps/lines` + Borut mirror (2026-06-08, code-only) — LAYOUT-ONLY (owner-chosen):** the board is built at module load + the RGS book is a fixed N×M, so `reels`/`rows` are data-coupled (out of scope, kept descriptive + warned); Phase 2 drives **position + cell size + reel padding** into the live board via the board-container transform (no per-symbol math change). Shared readers `engine-layout/reelGrid.ts` (`findReelGridNode`/`resolveReelGridFromNode`/`resolveReelGridLayout` + `ReelGridNode` export); `boardLayout()` applies position + `scale=cellSize/SYMBOL_SIZE` (on `BoardContainer` + coded `BoardFrame` glow) + reelPadding x-offset; `Game.svelte` sets the override from the loaded doc; parity = no node ⇒ identical to today. Save-time mismatch **warning** via `GameTemplate.board {reels,rows}` (5×3) + `reelGridWarnings`, folded into the editor's existing `warnings` channel. `pnpm --filter {engine-layout,launcher-api}` + `apps/lines` builds GREEN. **Borut mirror ✅ PUSHED (Book of Borut `e53012b` on origin/main, build-green; republish pending):** engine submodule `1e043dc→9a37cf0`, 4 game-file edits mirrored, `pnpm build:engine`+`vite build` GREEN; parity-safe (no `reelGrid` node in Borut's live doc yet). Only remaining owner step: republish (Build&Deploy w/ Portal key). Also: an editor **"⊞ Convert to parametric grid"** Properties button turns the old `container` reelGrid mount-anchor into a parametric `reelGrid` node (seeded 5×3@120 at board centre = parity), closing the "existing doc has no reelGrid node" gap. **Phase 3 (later):** reels/rows runtime-driving as a separate RGS/math effort; win-lines; per-layoutType grid overrides. **Also (same session):** Component Editor (`/components`) UX cleanup — opening a component now stays on the Outline (was dumping into the asset Library) and the desktop/tablet/landscape/portrait pills were removed (a component is one design; the scene editor owns responsive overrides).

- **Invisible Editor — universal bound-component params: 🚧 LANDED code-only (2026-06-08), not browser-verified.** Generalized the bespoke per-component appearance editing (reelGrid params + HUD logo/game-name text override) into ONE mechanism: a declarative param schema per coded component (`EditableParam` + `BOUND_COMPONENT_PARAMS` + `getEditableParams` in `engine-layout/boundComponentCatalog.ts`), auto-rendered by `EditorProperties` (font/number/colour/checkbox/text controls), persisted to `bind.props` (top-level or nested `style`), flowing to the component at runtime. **Finding:** placement (x/y/**scale**) + **visibility** were already universal for HUD anchors (LayoutEditable applies scale + `{#if visible}`), so only author-set appearance params were new. **Seeded for HUD:** logo/game-name (text+font/size/fill), balance/win/bet labels (font/size/fill; live value stays coded), buttons (tint). **Game consumption** (parity-gated) in shared `components-ui-pixi`: `hudPositions` `hudStyle`/`hudText`/`hudTint` readers; `LayoutEditable` forwards per-element props; `UiLabel` merges style; `UiButton`/`ButtonBet`/`ButtonBuyBonus` wrap in `<Container tint={tint ?? 0xffffff}>`. `EditorCanvas.drawHudChip` reflects tint/fill so edits show in-editor. Parity: no `bind.props` ⇒ byte-identical. Engine `3ae6d6d`+`3b20be6`; Borut submodule→`3b20be6` (`b958ba7`); lines+launcher+Borut builds GREEN. Plan: design `docs/design/invisible-editor.md` §12. **Owner step:** republish Borut to see label/button params live. **Extend to any component:** add a `BOUND_COMPONENT_PARAMS` entry + make the component read the prop.

- **Invisible Editor — background cover scale ↔ stretch decoupling + coded-Background goes doc-driven: ✅ LANDED (2026-06-08, engine + editor + `apps/lines`; owner-verified full-bleed live in `apps/lines`).** Two owner-reported bugs (`docs/design/invisible-editor.md` §10.3 step 6): (1) editing **cover scale** also moved the Transform **SCALE.X/SCALE.Y** to the same number — they were the SAME field (`backgroundCoverScale = node.scale.x`, `setCoverScale` wrote both axes), with no vertical-vs-horizontal stretch; (2) the in-game background was **half-size / wrong-ratio** because the coded `Background.svelte` still used `normalBackgroundLayout({ scale: 0.5 })` (ratio-driven, ignores the doc) while the editor used the true `coverTransform`. **Fix:** cover scale is now a dedicated uniform **`BaseNode.coverScale`** (default 1); stretch is the free per-axis `node.scale` (default `{1,1}`) via new reader **`backgroundCoverStretch`**; `coverTransform` returns `{scaleX,scaleY,x,y}` = `fit·coverScale·stretch`. All cover paths consume it — editor (`EditorCanvas` background + placedArt, `EditorSpineLayer`), engine `LayoutNodeView` (bound `cover` carries `stretch`; sprite/spine bg fold stretch onto width/height + `scale`), and pixi-svelte composes stretch with `fit` (`spine.scale.set(baseX·sizeScale.x, …)`). **Real in-game fix:** `apps/lines/src/components/Background.svelte` dropped the `0.5`/ratio path for a true full-bleed cover via pixi-svelte `fit`, taking a `cover={{scale,fit,stretch}}` prop (default = exact edge-to-edge); `Game.svelte` passes the canonical readers from the doc's `background` scene. `coverScale` persists verbatim through `editorStorage.normalizeNode`. Builds GREEN (`engine-layout`, `launcher-api`, `lines`); verified live in `apps/lines` (background fills the viewport edge-to-edge, was half-size; no distortion at default stretch). **Borut mirror PENDING (separate repo):** apply the same `Background.svelte`/`Game.svelte` edits + bump the engine submodule + rebuild engine dist + republish. Frame sprites left as-is this pass (owner-deferred).

- **Invisible Editor — B6: HUD button cluster as one parametric `Button` component (owner direction 2026-06-09, branch `feat/editor-button-component`):** the button analogue of B5 (the HudReadout split, `65a5917`). Owner decisions: build it as **separate coded parts** (each movable/restylable, matching B5), and cover the **Borut HUD cluster only** first (spin/bet, menu, buy-bonus, auto-spin, turbo, bet +/−). Full scope = `docs/design/invisible-editor.md` §16. **Phase B6.1 LANDED code-only (`bf21aff`, not deployed/not browser-verified):** one `BUTTON_DEF` (built-in beside `HUD_READOUT_DEF`, instanced per button by an `action` param) built from two coded `bind` parts in `components-ui-pixi` — `ButtonFrame` (the `UiSprite` tile + the hit area: variant bg / disabled-grey / active border / tint, `onpointerup`→the action `onpress`) and `ButtonLabel` (the localized icon/label `Text`, `UiButton`'s style merge). Both read params off the `<ComponentInstance>` param context (`getComponentParams`); registered in `apps/lines` `registerBoundComponents`; `button` added to `BUILTIN_COMPONENTS`. **Pure parity** — no HUD node references the def yet. **Phase B6.2 LANDED (`2977a79`):** `registerComponentActions` registry (sibling to `registerComponentValues`; `ActionSource = { onpress, disabled?: BoolSource, active?: BoolSource }`, keyed by action name) + `ComponentInstance.svelte` resolves the `action` param and threads `onpress`/`disabled`/`active` onto the param context (reactive getters, only when an action is registered → parity); `apps/lines` `boolSource()` helper + a `registerComponentActions({ menu, buyBonus, autoSpin, turbo, increase, decrease })` call (full) + `spin` (onpress + disabled; the dynamic bet↔stop LABEL flip, `stopDisabled` state machine, and `OnHotkey` Space DEFERRED to B6.4). **Phase B6.3 LANDED (`440cbfa`):** **finding** — the generic `componentInstance` mount loop `LayoutEditable` gained in B4.3 (the readout work) is component-agnostic, so a `button` instance ALREADY mounts through it (no new branch). B6.3 just (a) registered `BUTTON_DEF` in `registerComponents` and (b) extended the `mounted()` double-render guard to the 7 coded button slots (`hud-btn-*`) so a converted button won't double-render with its coded twin — parity-safe (no button instances in `hudBarScene()` → guard false → coded buttons render byte-identically); + a proof story `apps/lines/.../ButtonComponentInstance.stories.svelte` (def expands → frame+label → press fires `onpress` → disabled greys). All builds GREEN; pushed. **Phase B6.4 LANDED (`05f680b`) — the parity flip + finished `spin`, flag-gated:** mirrors the readout's B4.4 flip. Mechanism: `registerComponentActions` gained `TextSource` + optional `ActionSource.label` (dynamic caption); `ComponentInstance` threads the live label onto the param context (reactive getter, only when the action provides one → overrides the static `label`; `ButtonLabel` already reads `label` when `icon` is absent). `hud.ts` += `HudBarOptions.buttons` + `buttonInstanceNode()` builder + a `btn()` picker that emits `componentInstance(button)` when `buttons` is set, else the coded `UiButton*` `bind` node (default = parity); `spin` gets `{action:'spin'}` with NO icon (dynamic label), the other six `{action, icon}`. `spin` completed in `apps/lines/Game.svelte`: replicates `ButtonBetProvider` verbatim (`stopDisabled` latch via `subscribeOnMount`, `getKey`, `onpress`) → `disabled` (key∈spin_disabled/stop_disabled), `label` (`bet()`/`stop()`), + a replacement `<OnHotkey Space>` mounted ONLY when the flip is on (`textSource.ts` = string sibling of `boolSource`). **Flag DEFAULTS OFF** (`apps/lines/src/game/editorFlags.ts` `HUD_BUTTON_INSTANCES = false`), threaded via `defaultLayout('lines',{buttons})` into the fallback doc + the hotkey gate; editor picker + Borut get OFF (`scenes/bookof.json` unchanged). All builds GREEN. **⏳ VERIFICATION GATE (owner/browser):** flip `HUD_BUTTON_INSTANCES = true`, run `apps/lines`, confirm the 7-button cluster renders + positions identically AND spin behaves (bet↔stop label, disabled greying, Space hotkey, turbo/auto-bet stop nuances). Until verified the flag stays OFF. **Editor "Convert to parametric button" affordance LANDED (`872cfb5`):** the author-facing flip (mirrors reelGrid's "Convert to parametric grid"). New `engine-layout/buttonConvert.ts` = `HUD_BUTTON_ACTION_MAP` (single source for `bind.component`→`{action,icon?}`; `hud.ts` `buttonInstanceNode` now consumes it so seed + convert can't drift — `gen:scenes --check` confirms `bookof.json` byte-identical) + `isHudButtonBind()` + `buttonBindToInstance(node)` (preserves transform byte-for-byte). Editor: `onConvertToParametricButton` prop + `⊞ Convert to parametric button` Properties button (shown for a container whose `bind.component` is a known HUD button) + the `+page.svelte` in-place handler. This is how a live doc (Borut's) adopts parametric buttons without a re-seed. Builds GREEN. **Local in-game verification ATTEMPTED, blocked by harness (2026-06-09):** flipped `HUD_BUTTON_INSTANCES=true`, ran `apps/lines` — the componentInstance→HUD-bar mount path is PROVEN (the balance readout, same `<ComponentInstance>`+`LayoutEditable` path, mounts correctly — confirmed via PixiJS scene-graph introspection; `button` def resolves, no engine errors; the B6.3 story compiles + lists in Storybook). But the full button cluster + win/bet never initialize locally because they're gated on bet-config from RGS **authenticate**, which **stalls at a heartbeat** (the documented non-deterministic play4fun auth gotcha, mock on :7788 only ever saw `actions=[]`); and the **WebGPU** renderer makes preview screenshots/evals time out (also documented). So pixel-level verification needs a stable browser env or the owner in-game. **Next:** open PR `feat/editor-button-component`→`main` (carries B6.1–B6.4 + the convert affordance; flag default OFF); after merge → owner browser-verify (flip flag in `apps/lines`, or use the editor convert affordance on a deployed project) → make the flag default → **B6.5** (Borut: submodule bump + enable buttons + `build:engine` + republish + convert its live HUD doc). **Known gap (pre-existing, out of B6.4 scope):** the B6.2 `turbo` action doesn't replicate `ButtonTurbo`'s `subscribeOnMount` stop-toggle nuance. **PR [#32](https://github.com/Invisible-Wall-SL/Invisible-Engine/pull/32) MERGED to `main` (`5161827`, 2026-06-09):** the whole B6 batch is on `main` (launcher auto-redeploys → the editor's Convert button + the `button` component go live). Flag stays OFF in `apps/lines`. **B6.5 LANDED + PUSHED (Book of Borut `5f383c2`):** bumped Borut's engine submodule `65a5917`→`5161827`; mirrored the button wiring into Borut's `Game.svelte` (register `ButtonFrame`/`ButtonLabel` + `BUTTON_DEF` + `registerComponentActions` for all 7 actions; `spin` replicates `ButtonBetProvider` verbatim; new `src/game/{boolSource,textSource}.ts`); the spin Space hotkey is **doc-gated** via `spinIsInstance` (Borut reads its HUD from the LIVE doc, no static flag — true once `hud-btn-bet` is a `componentInstance`). `build:engine` + `vite build` GREEN; committed surgically (engine pointer + 3 `src/` files only — the owner's concurrent `static/assets/**` edits left untouched, NOT staged). **Parity-inert:** Borut's live doc still has coded `UiButton*` `bind` nodes, so the game renders byte-identically until converted. **Remaining OWNER steps for Borut:** (1) **republish** Borut (Build & Deploy w/ Portal key — NOT done here; the owner has in-progress `static/assets` changes, so republish is theirs); (2) in the editor on `borut/book_of_borut`, select each HUD button → **⊞ Convert to parametric button** → Save → the parametric cluster goes live; (3) browser-verify the flip (the local RGS/WebGPU harness couldn't).

- **Invisible Editor — Component Editor UX + reusable styling sweep (2026-06-09, on `main`, auto-deployed):** a run of editor changes building on B6, all shipped to `main` (commits `d447f9c`→`64e3ba6`); launcher-api + `apps/lines` builds GREEN throughout. **Layout:** the Component Editor (`/components`) now tabs the **Outline with the asset Library on the LEFT** (`Library | Outline`, reusing `EditorOutline`); the right panel shows the selected node's **Properties alone** (the right-side Outline/Properties tab toggle removed). **Defaults scope (owner decision):** the right-panel **"Defaults (all projects)"** now edits the component def's OWN param defaults (`def.params[i].default`, saved WITH the component via Save component) instead of a per-project shadow; engine-provided params hidden (fed at runtime). **`ComponentParam.options` (string enum, new field):** the editor renders a **dropdown** wherever a param is set (component default + placed-instance override). Used for the readout `source` (balance/win/bet/totalWin via `VALUE_SOURCE_CATALOG`, derived from the numeric `ENGINE_PARAM_CATALOG`) and the button `variant` (dark/light). For engine-fed components the inert literal engine-param checklist is replaced by a note pointing to the source binding. **Decomposed-readout double-text FIX:** `HUD_READOUT_DEF` Background → new `preview.style:'tile'` (tile-only chip, no caption / stray `0.00`); Caption/Value carry `preview.textParam` (`label`/`value`) so the preview reads "BALANCE"/a number, not the part names (`EditorTextLayer` + `EditorCanvas.drawHudChip` resolve textParam vs the open component's resolved params). **Reusable tile styling (`TILE_PARAMS`):** texture / tint / outline-colour / outline-width / corner-radius as ONE shared `EditableParam` group in `boundComponentCatalog`, attached to BOTH `HudTicker` (readout bg) AND `ButtonFrame` (button frame). Each coded component reads+forwards what it supports — `HudTicker`→`UiSprite` (key/tint/backgroundColor/border*), `ButtonFrame` layers them as the **resting** look UNDER its engine states (disabled grey + active border still WIN; bind.props tint overrides the instance tint). Editor `tile`/`button` chip previews reflect radius+border live. **This is the reuse proof the owner asked for: a 2nd consumer dropped in via one catalog line + reading the props — no new vocabulary, no duplicated UI/preview code.** **Blank Elements palette:** the Library in BOTH `/components` and `/editor` gains an **"Elements"** section with draggable **Text** + **Container** chips → `EditorCanvas.spawnNode` makes a default text node (proxima-nova/45/white) / empty container through the same drop pipeline (previously nodes could ONLY spawn from asset drags; a bare text node had no entry point). **Normalizer fix (`dc27bb7`):** `componentStorage.normalizeParams` now preserves `options` (was silently dropped on save/read). **⚠️ GOTCHA (cost the owner real confusion):** a SAVED R2 component (`_shared/editor-components/<id>.json` or a project shadow) **shadows the built-in def** (`loadComponent` precedence project ◁ shared ◁ built-in), so def/preview changes do NOT reach an already-saved copy — the stale copy must be **deleted (✕ in the Component Editor list)** to fall back to the updated built-in. Same for any baked node `preview` metadata. **Button-states finding (no code, owner asked):** the Stake button declares states `hovered`/`pressed`/`disabled`/`active`, BUT hover/pressed have NO visual in `UiButton` and disabled/active looks are HARDCODED constants — there are **no editable per-state style fields**; `disabled`/`active` are fed at runtime by `registerComponentActions` (game XState/bet state, keyed by the instance's `action`). Per-state styling = net-new fields + wiring hovered/pressed → DEFERRED per owner "nothing new".

- **Invisible Editor — B6 follow-up: parametric Button STATE IMAGES (owner request 2026-06-10): ✅ LANDED.** Un-defers the per-state styling above, scoped to image swaps: `BUTTON_DEF` gains five `kind: 'image'` instance params grouped under **State images** — `image` (resting), `imageHover`, `imagePressed`, `imageSelected` (engine `active` flag), `imageDisabled` (the **downstate**, engine `disabled` flag). The editor already renders `image` params with the RegionPicker (instance overrides + component defaults), so NO launcher change was needed. `ButtonFrame.svelte` now tracks `hovered`/`pressed` locally (mirroring the coded `Button.svelte`, incl. the disabled reset) and, when the current state's image is authored, renders a real `<Sprite key=…>` instead of the variant tile; cascade disabled → pressed (→hover→selected) → hovered (→selected) → selected → resting; empty-string params normalize to unset. **Parity:** with no image params the tile path is byte-identical (painted disabled-grey / active-border apply only where no image covers them); the active BORDER is tile-only, so an image-skinned button should author `imageSelected`. Side fix: a disabled `ButtonFrame` no longer fires `onpress` (matches the coded base button + `ComponentInstance`'s wrapper). **Verified live** in the lines Storybook — new story `state images swap the bg` mounts via `StoryPixiApp` + a `canvas`-space scene; all five swaps screenshot-proven. **Harness finding (pre-existing, open nit):** the older engine-layout proof stories (incl. B6.3's) mount NO `<App>` and `ButtonLabel` translates before `StoryLocale`'s mount-time `i18n.activate`, so they render blank in dev Storybook; the new story works around both (own `StoryPixiApp` + eager `i18n.activate` in the story script). ⚠️ Rollout caveat (same gotcha as above): an R2 `button.json` (shared or per-project) shadows the built-in def wholesale, hiding the new params until that saved copy is deleted (✕ in the Component Editor) or re-saved from the updated built-in.
- **Invisible Editor — state images for AUTHORED art buttons (2026-06-11, follow-up to the above after owner couldn't see the options):** root cause was TWO-fold: (1) bookofborut has a saved **project `button.json`** (plus the custom `Button_Square` `c_r3vqsa2q`) shadowing the built-in def — so the new params never listed; (2) those defs are **authored art** (bg sprite bound `region ← bg_image` + text, NO coded `ButtonFrame`), so the ButtonFrame-side state code never runs for them. Engine fix: new shared cascade `engine-layout/buttonStateImage.ts` (`resolveButtonStateImage` + `BUTTON_STATE_IMAGE_KEYS`, consumed by BOTH ButtonFrame and the authored path); `<ComponentInstance>`'s §18.4 interactive wrapper now tracks `hovered`/`pressed` (disabled-reset like the coded button) and — when the def declares state-image params — overrides the **`image` param with a reactive getter** resolving the cascade, so a bg sprite bound `region ← image` swaps per state. Parity: getter only defined for interactive instances of defs declaring the four state keys; everything else keeps the exact static param map. Proof story 3 (`authored art button…`) verified live (hover/pressed screenshot-proven; selected/downstate share the proven cascade + live flags). **Data migration RAN (owner-approved 2026-06-11) + verified:** the one-off `apps/launcher-api/migrate-button-states.mjs` (kept untracked beside its local backups `button.json`/`c_r3vqsa2q.json`/`scenes.backup.json` — delete all four once the owner confirms the editor looks right) renamed `bg_image`→`image` in BOTH saved defs (param + bg-sprite binding, the param now grouped under **State images** with the four new state params) and in the 6 placed button instances (autospin/spin/increase/decrease/turbo, incl. folding an orphaned `Image` key); hudReadout untouched; re-read from R2 confirmed clean (no stale `bg_image` on buttons). **Owner action: reload the editor tab** — an open tab still holds the pre-migration doc, and saving from it would resurrect `bg_image`. **Borut bumped + pushed (`2c990c1`):** engine submodule `2c9e6ac`→`32e6b6f` (carries both state-image commits), `build:engine` + `vite build` GREEN, committed surgically (pointer only — the owner's in-progress `static/assets` + baked-bundle edits left unstaged); **republish is the owner's** (Build & Deploy) — until then the deployed game keeps resting images only. **Follow-up (2026-06-11, RESOLVED): the swaps didn't ship — root cause was the TOKEN TRAP, not the engine.** Borut's turbo skin (`image=T_Button_Turbo_Off`, `imageSelected=imageDisabled=T_Button_Turbo`) + the migrated def + `Game.svelte`'s turbo `active`/`disabled` feed are all correct, but `pnpm build` ran `bake:doc`/`pull:assets` WITHOUT `EDITOR_DOC_SECRET` → shipped the `doc: null` placeholder → `registerBakedComponents()` + `bakedEditorArtAssets()` both no-op → the editor-art sheet (the ONLY source of the no-`.png` frame names the skin uses) never loaded. Fixed: Borut's `bake:doc`/`pull:assets` now run via `node --env-file-if-exists=.env …`, so `pnpm build` auto-loads the token from a gitignored `Borut/.env` (owner pastes it once; `.env.example` documents it). Owner confirmed working. See `project_component_art_to_game` memory (token-trap mitigation).
- **Invisible Editor — behavior layer (animated overlays `Win`/`Transition`/FS `intro`/`outro`): SCOPED, not started (owner direction 2026-06-09) → `docs/design/invisible-editor.md` §17.** Owner reframe: these overlays are *animation*, not logic, so the authoring surface is a **timeline/track editor, NOT a node/flow graph** (route A node-graph stays DEFERRED, re-confirmed). The §8.5 `BehaviorTrack`/`TweenStep` schema already exists; remaining = a shared **engine GSAP interpreter** in `<ComponentInstance>` + `registerComponentSignals` (book event → signal) + a **staged editor surface**. Altitude chosen: **Tier 0 (signal-preset dropdowns in Properties) → Tier 1 (full timeline panel)**, additive on one interpreter. Build order (parity-gated): interpreter → signal registry → Tier-0 UI → migrate `Transition` (proof) → `Win` + FS intros → Tier-1 timeline → Borut mirror. **v1 ceiling = tweens + spine playback + one count-up binding** (anything branching/stateful stays a coded `mount`) — the scope-creep line. **Parked behind the static-mount work (B6 + placement-only sweep); pick up at §17.4 step 1.**
- **Invisible Editor — spine pickers + signal-driven spine playback, wired end-to-end (2026-06-11, on `main`): PRs #44 + #45.** First real cut of the behavior layer, narrowed to the owner's "no spine-timeline editing" steer (tool split confirmed: scene editor = placement + properties; component editor = composition). **(1) Animation + skin pickers (#44):** a `kind:'spine'` node's animation is a dropdown (was free-text) + a new **skin** dropdown — both from the loaded skeleton's own lists, plumbed `EditorSpineLayer`→`EditorCanvas`→page→`EditorProperties` (`onSpineMetaChange`/`onSpineMeta`/`spineMeta`); engine honors via new `SpineNode.skin`→`<SpineProvider skin>` (`BaseSpineProvider.setSkinByName`, additive); live skin in the preview overlay. **(2) Signal→spine playback (#44, narrowed §8.5/§17, spine-only):** new `registerComponentSignals` registry (event sibling of the value/action feeds) + `componentSignalContext` + `SpineNode.cues:{signal,animation,loop}[]`; `<ComponentInstance>` walks its def for spine cues, subscribes each registered signal, and on fire writes the cue's animation into a reactive nodeId→anim map that `LayoutNodeView`'s spine block prefers over `defaultAnimation`. Editor: a **"Plays on signal"** cue editor in the spine Properties (component mode only). **(3) Game wiring (#45):** `utils-event-emitter` gained a lifecycle-free `subscribe` (the `onMount`-free sibling, needed because `<ComponentInstance>` subscribes from an `$effect`); `apps/lines` `signalSource.ts` `eventSignal()` + `Game.svelte` `registerComponentSignals({ win, bigWin })` — `win` fires on the `winShow` presentation event, `bigWin` on `winUpdate` gated to the `'big'` win-level tier. **All additive + parity-safe** (no skin/cues, unregistered signal, or scene-level spine = byte-identical). **End-to-end now works in `apps/lines`:** author a component with a spine cue → place it → it plays on the real win. **REMAINING:** Book of Borut activation (same `registerComponentSignals` call in its `Game.svelte` + submodule bump — its own repo, separate step); more signals (`idle`/`enter`/`exit`) are easy follow-ons on the same mechanism. See design §17.
- **Invisible Editor — Free-Spin Counter decomposed into an editable component (2026-06-11, on `main`): PRs #46/#47/#48/#49 + Book-of-Borut `2339e8c`.** Turned the opaque coded `FreeSpinCounter` (one `bind` anchor) into editor-visible/editable nodes, using the HUD-readout decomposition pattern. **(1) `FREE_SPIN_COUNTER_DEF` (#46):** built-in ComponentDef = Frame `sprite` + "FREE SPIN" `text` + dynamic "X OF Y" `text`, each a real node with `paramBindings` (text/font/size/fill editable); value engine-fed via a new `freeSpins` composed-string source (`stateUi.freeSpinCounterCurrent/Total`). Caption/value are plain `text` nodes (not reused `HudValue` — it's a numeric currency formatter). **(2) Bitmap text in the engine (#47, §9.4 Phase 3):** new `registerFontCatalog` registry; `LayoutNodeView` renders `<BitmapText>` when the catalog marks the family bitmap, else `<Text>` (parity). Fixes `gold` (and all bitmap fonts) for EVERY layout text node, not just this one. **(3) Component visibility feed (#48):** new `registerComponentVisibility` (`BoolSource`) registry; `<ComponentInstance>` reads a `visibleSource` param and gates the whole instance behind `<Container visible>` — but ONLY when a source resolved (render branches extracted to a snippet so the ungated path is byte-identical). This preserves the counter's free-spins-only gating. **(4) Live switch (#49):** both reference layouts' `freeSpinCounter` scene flipped from `bind: FreeSpinCounter` to a `componentInstance` (board-left placement per layoutType — desktop 87,220 — replicating the coded formula; params `source:'freeSpins'`, `visibleSource:'freeSpinCounterShow'`, `label:'FREE SPIN'`). Coded `FreeSpinCounter` stays registered as fallback (no double-mount). `apps/lines` (dev) now renders the decomposed counter. **(5) Borut (`2339e8c`):** engine submodule cf1a206→652a4fb + `Game.svelte` registers `freeSpins`/`freeSpinCounterShow`/`FREE_SPIN_COUNTER_DEF`/`registerFontCatalog`(gold/goldblur/silver/purple); `build:engine` GREEN, compiles. **OWNER STEPS to go live in Borut (order matters):** (a) **republish** the Borut bundle first (ships the registrations), then (b) **re-seed** `bookofborut`'s R2 doc so the live scene uses the componentInstance (the regenerated `scenes/bookof.json` seed emits it; needs R2 creds) — OR compose it in the editor. Until both, the live game keeps the coded anchor (safe). Couldn't browser-verify free spins here (needs funded RGS mock; svelte-check OOMs). See design §8/§9.4.

## Track 2 — Studio / pipeline tools

### Current live state
*(One line each; full done-work narrative in `docs/history.md`.)*
- **Launcher** at `app.invisiblewall.org` — invite-only email+password auth (scrypt) + Postgres sessions, three-layer role/user tool matrix, `/admin` panel (users/roles/tools/projects/clients/games/sessions), project + client layer (R2 isolated per **`<client>/<project>/`** — unified single-project repo, see below). Game cards on the home page are scoped to the active project (`games.project_key`; null = global). (history: see `docs/history.md`)
- **Desktop launcher — pull projects from the cloud** (2026-06-11, server live `17677fb`) — `C:\Invisible Wall SL\ComfyUI\Invisible_Launcher.py` now pulls the projects a signed-in account may access (scoped by the same `accessibleProjects` rules, grouped by a top **client selector**) instead of forcing a local-folder add. New `GET/POST /api/launcher/projects`: session-bearer GET returns accessible projects + their opaque `launcher_profile` + the distinct clients; admin-bearer POST publishes a project's profile (mirrors `register-game`). Schema: nullable `projects.launcher_profile` jsonb (migration `0008`; **column applied to prod** via idempotent `ADD COLUMN IF NOT EXISTS` — listProjects selects it, so it had to land before deploy). Desktop side: a **Projects root** setting derives each project folder as `<root>\<client>\<project>` (no stored absolute paths / game root; ComfyUI stays the shared service), **↻ Sync projects** git-clones the project repo from its profile `repoUrl` + runs `pull:assets` (R2 assets) and auto-syncs *definitions* after sign-in, **Load from folder** is now optional (reworded empty states), and owner **Publish setup** pushes a project's profile up. `pnpm --filter launcher-api build` + `py -m py_compile` GREEN. **⏳ Owner-verify:** release the desktop launcher (its own `invisible-launcher` repo + new exe per the B31 process — the `.py` edits are in the live working copy, not yet committed/released) and smoke-test on a fresh `comfyui_manager_config.json` (sign in → projects appear grouped by client, no manual Add → Sync clones a repo into `<Projects root>\<client>\<project>`).
- **Online tools** — `/atlas` (redirect to atlas-tool), `/spine` (Spine Viewer), `/sheet` (redirect to sheet-tool), `/localization` (in-launcher), `/editor` (Invisible Editor, in-launcher), `/files` (FTP Browser: project-scoped R2 manager for non-admins; **admins get a two-tab full-server view — whole R2 bucket + a read-only Railway/Postgres inspector** at `/api/db/*`, secrets redacted). (history: see `docs/history.md`)
- **Invisible Font Maker** (`/fonts`, NEW 2026-06-12 — Phase 0+1 landed, code-only/not browser-verified) — a launcher-native tool to **create, view, and save bitmap fonts** per client/project into the existing R2 fonts contract (`<client>/<project>/fonts/fonts.json` + `_shared/fonts/`), so games consume them by family name. Closes the authoring gap: until now fonts only entered the contract via the PowerShell `r2-sync-fonts.mjs` script. **Decided:** host = launcher Svelte page (no new service); baking = browser-side `opentype.js` + Canvas 2D (gradient/outline/shadow effects in v1); output = BMFont **XML** + page PNG (byte-compatible with what `apps/lines` loads). **Landed:** registered in `roles.ts` (`fontMaker` tool, granted to admin/developer/artist); route `(app)/fonts/` (`ssr=false`, `fontMaker`-gated); **View mode** lists + live-previews a project's fonts via a self-contained, `fontMaker`-gated `GET /api/fonts/catalog` + `GET /api/fonts/asset` (so the tool does NOT need the editor grant) — the byte-streamer + BMFont `?font=1` descriptor page-rewrite were **extracted to `lib/server/assetStream.ts`** and shared with `/api/editor/asset` (editor path parity-preserved); `resolveEditorFonts` parametrized with an `assetUrl` builder. **Phase 2 — Import + Save ✅ LANDED 2026-06-12** (code-only): upload a BMFont (`.xml`/`.fnt`/`.json` + page image[s]) → client parses + live-previews → save into the project's `fonts/`. Uploads use **presigned PUT** (page PNGs exceed adapter-node's 512 KB body limit): `POST /api/fonts/upload-urls` (presign descriptor+pages) → browser PUTs to R2 → `POST /api/fonts/save` is authoritative (re-reads the descriptor from R2, re-derives `name`/`pageFiles` via `lib/server/bmfont.ts`, verifies pages exist, upserts `fonts.json` by `id===folder`; per-project writes only). New client `FontImport.svelte` + `parseDescriptorClient`/`loadLocalBitmapFont`. **Phase 3 — Generate ✅ LANDED 2026-06-12** (code-only): `FontGenerate.svelte` + `fontBake.client.ts` bake a BMFont (XML + page PNG) from a TTF/OTF entirely in the browser — `opentype.js` for metrics/glyph geometry, Canvas 2D `Path2D` for rasterization + **effects** (solid/gradient fill, outline, drop-shadow, each toggleable), shelf packer → single page; charset presets in `charsets.client.ts`; live PIXI `BitmapText` preview (= the metrics self-check) + atlas thumbnail; saves via the **same** Phase-2 path (the upload→save sequence was extracted to `fonts.client.ts` `saveBitmapFont` and is now shared by Import + Generate). Deps added: `opentype.js` + `@types/opentype.js`. Deferred to Phase 4: kerning, multi-page atlases, shared-library save target. All three modes (View / Import / Generate) now functional. `pnpm --filter launcher-api build` GREEN. Design: `docs/design/invisible-font-maker.md`. **⏳ Owner-verify live**: View existing fonts; Import a BMFont (e.g. `lines` `mm_gold.xml`+`.webp`); Generate from a TTF with effects → confirm each appears in View + a game loads it by family name.
- **Invisible Storybook** (2026-06-11, built — nothing published yet) — `/storybook` lists + serves published `storybook build` outputs from R2 behind launcher auth: the engine reference at `_shared/storybook/engine/` (any user with the tool) and per-project builds at `<client>/<project>/storybook/` (project-access gated, same model as spines/files). Serving is path-shaped (`/storybook/view/engine/…`, `/storybook/view/p/<projectKey>/…`) so storybook's relative links resolve; html/json `no-store`, hashed `assets/` immutable. Publish with `apps/launcher-api/scripts/publish-storybook.mjs` (`--shared` or `--project <c>/<p>`; builds via `pnpm exec storybook build` unless `--dir` given; prunes stale hashed keys; `--dry-run`). `new-game.mjs` scaffolds `publish:storybook`; `apps/lines` gained `build-storybook`. Doc: `docs/tools/storybook.md`. TODO: publish the engine reference + Borut's storybook (needs R2 creds), then smoke-test live.
- **atlas-tool + sheet-tool** on Railway — Python Atlas/Sheet Makers re-hosted; now **repo-root Docker build** sharing `services/_shared/iw_common` + thread-local per-request context. Sheet Maker: Refresh-from-R2, destructive-Save confirm, shared-manifest browsing, **packs Atlas generation manifests** (loose sprites from `batch/`), and an **R2-backed Import browser** (open any packed `.atlas` anywhere in the project). Atlas Maker: deploy now lands in the project's R2 `deploy/` (the "deploy to C:/" bug is fixed). **Fixes 2026-06-05 (`1ffa20f`, shipped):** (1) user images loaded from disk (`_setoutput`/`_userefimg`) now `_mirror` to R2 (+ `_clearoutput` `_unmirror`s on revert) — they were local-staging-only, so a hydrate/refresh/redeploy reverted the card to the old generated output ("as if not saved"); same class as B42's FX-mirror fix. (2) the R2 folder picker's "✓ Use this folder" wrote the breadcrumb label (`R2 repo: <path>`) into the field — `_fsbrowse` now returns a clean `rel` and the client selects that (browsing was already project-root-confined). (history: see `docs/history.md`)
- **atlas-backend** (FastAPI) — aligned to `<client>/<project>/` + Dockerfile-pinned, **parked/dormant** (nothing calls it; atlas-tool runs generation itself). (history: see `docs/history.md`)
- **R2 unified single-project repo** ✅ (2026-06-02) — retired the per-`<tool>/` namespaces; R2 is now ONE repo per project: `<client>/<project>/{input,manifests,atlas,batch,sheets,sheet_src,deploy,spines,localization,editor,…}`, shared by all tools (`manifests/` is the shared surface — the Sheet Maker sees the Atlas Maker's manifests). A single canonical `r2_slug` (lowercase, non-alnum→`_`) in `iw_common.context` + launcher `r2Slug` fixes the old hyphen/underscore divergence (`book-of-borut`→`book_of_borut`). Migrated via `scripts/migrate-r2-unified-repo.py` (copy→verify→retire); cutover complete — old prefixes deleted (4902 objects), the stale `book_of_borut` dupe + `cloud/batch`/`cloudtest/batch` strays purged. Design: `docs/design/unified-project-repo.md`. R2 fully clean — all old per-tool namespaces are 0 objects (the last `localization/cloud/strings.json` orphan was deleted too).
- **ComfyUI tunnel** — local RTX 4070 ComfyUI reached over the Cloudflare named tunnel (`comfy.invisiblewall.org`); the only genuinely-local footprint. (history: see `docs/history.md`)

### Open items

**Owner-side (action required):**
1. ⚠️ **HIGHEST PRIORITY — live anonymous-access exposure:** set `ATLAS_TOOL_SECRET` + `SHEET_TOOL_SECRET` (unset → code returns "allowed", so the public Railway URLs are an unauthed path to R2 read/write/delete + GPU/comfy.org spend), and **rotate the R2 token** (read+write whole bucket, shared by 4 services) + Postgres pw + CF Access service-token secret. Owner-side (Railway/CF dashboards). ~30 min. **Do first.** See `docs/INFRA.md` "Security / secret rotation".
2. **Pin a Railway `/data` persistent volume** on atlas-tool + sheet-tool — payoff of perf fix #5 (incremental-hydrate skip only persists across deploys with a real volume; on ephemeral disk the first hydrate per boot still pulls everything). NOTE: doc has no `railway.json` confirming `/data` is persistent — verify whether already done. **Disk-growth now bounded (2026-06-02):** the `/data` staging tree was an uncapped, never-evicted write-through mirror of R2 (footprint = every project ever opened × full tree + every variant ever generated). Fixed with (a) **lazy hydrate** — the big variant subtrees (atlas `batch/`+`deploy/`, sheet `sheet_src/`) are dropped from the eager hydrate and pulled on-demand via `cloud_paths.ensure_lazy()` only when a project is actually opened, so cold projects never materialize them; and (b) a **"Clear local cache" button** (`POST /clearcache`, `POST /api/clearcache`) that rmtrees inactive project trees + the active project's regenerable subtrees and reports bytes freed (R2 is canonical, so local-only delete is always safe — never `_unmirror`s). clearcache serialized against the lazy/render locks (shared `iw_common.storage.dir_size`/`human_bytes`). **Not yet deployed** — code in working tree only.
3. **Flip atlas-tool + sheet-tool + atlas-backend Railway Root Directory → repo root + Dockerfile Path = `services/<svc>/Dockerfile`** (coupled deploy from fixes #3 + #7 — Dockerfiles now build from the repo root). Until flipped, builds fail (last good deploy stays live → no outage). See `docs/INFRA.md` "atlas-tool + sheet-tool build from the REPO ROOT".
4. **cloudflared as a Windows service** — install it so the tunnel survives reboots (`cloudflared service install`). Partial today: B2 added Start/Stop tunnel buttons to the local launcher GUI, but it's not yet a persistent service. (#8)
5. **B20 — local launcher strip** ✅ mostly DONE (2026-06-01, B31). The live `C:\Invisible Wall SL\ComfyUI\Invisible_Launcher.py` was stripped to ComfyUI + tunnel and **pushed to the `Invisible-Wall-SL/invisible-launcher` repo** (the tracked copy had been ~791 lines behind). Still open: vendoring it into `tools/local-launcher/` (it remains in its own repo, distributed as an `.exe` per B31).
6. **Atlas → game "live assets" (option C) — in-repo half DONE (2026-06-02), external wiring pending.** The Atlas Maker's Deploy writes the composed atlas to R2 `<client>/<project>/deploy/`, but a game build inlines committed art, so deployed art never reached the game. `apps/launcher-api/scripts/r2-sync-deploy.mjs` now pulls `deploy/` DOWN into a local game-assets dir (default groups a flat `<name>.<ext>` → `<stem>/<name>.<ext>` to match `static/assets/spines/<group>/`; `--map`/`--flat` overrides; dry-run verified against R2). **Owner step:** wire it into the desktop launcher's publish flow BEFORE `build_game` (it has the game-repo path + R2 creds), with a per-game name→path map if the defaults don't fit. Then: Deploy in Atlas Maker → build+publish runs sync→build→upload→refresh → reload the game. (NOT instant/live — it's automated-rebuild, the "C" option chosen over the larger runtime-CDN approach.)
   - **live-assets.md step 1 — producer emits game-loadable spritesheet ✅ DONE (2026-06-04).** `_deployatlas` (`services/atlas-tool/ui_server.py`) now also writes `<base>.json` in TexturePacker json-hash format alongside the existing `.png/.webp/.atlas`, derived from the BOUND `.atlas` regions + page dims (via `atlas_format.parse_atlas`). New writer `services/atlas-tool/atlas_writers.py::write_texturepacker_json` ported from the Sheet Maker's, with ONE deviation: frame.w/h are NOT swapped on rotation (the game + `seed-game-editor.mjs::tpToRegion` treat frame.w/h as the unrotated display size — verified against `symbolsStatic.json`). `meta.image` = the deployed page filename (prefers `.webp`). Deploy honors the manifest's `deploy_path` as a mirrored subpath (`deploy/sprites/<name>/…`) and falls back to flat `deploy/<base>.*` with a logged note when none is set. Static-verified (parse real `symbols.atlas` → TP JSON round-trips to the game shape). NOT yet run against live R2. **Watch-out:** for the `symbolsStatic` case the manifest must carry `deploy_path: sprites/symbolsStatic` (+ `deploy_basename: symbolsStatic`) or deploy lands flat — owner sets this in Atlas Maker Deploy.
   - **live-assets.md steps 2-3 — HTTP transport + build-time pull ✅ DONE (2026-06-04).** Owner chose **token-gated launcher endpoint** (not public R2 domain) + **build-time default with runtime override** + **naming-convention mapping** (R2 `deploy/<rel>` mirrors `static/assets/<rel>`). Built: (a) **`GET /api/deploy`** (`apps/launcher-api/src/routes/api/deploy/+server.ts`) — mirrors the `/api/editor/doc` posture (gated by `EDITOR_DOC_SECRET`, open CORS); listing mode returns `{files:[{rel,size}]}`, single-file mode (`&rel=`) streams bytes with inferred content-type, path-traversal guarded; added `listAllObjects(prefix)` to `r2.ts`. (b) **HTTP puller** `apps/launcher-api/scripts/pull-project-assets.mjs` (rewritten from S3→`fetch`, NO aws-sdk/R2 creds, so it runs in any game CI) — lists `/api/deploy`, mirrors each `rel` into `--dest`, `--optional` mode degrades to a loud warning + keeps checked-in assets (vs hard-fail) so it can't brick a build. (c) **Book of Borut prebuild hook** (separate repo): `prebuild: pnpm pull:assets --optional` → pulls `borut/book_of_borut` `deploy/` into `static/assets/` before `vite build`. `pnpm --filter launcher-api build` GREEN. **Step 4 (cache-bust) ✅ DONE (`6435c35`, 2026-06-06)** — solved at the SERVING layer, not per-file hashing: the test server now caches content-hashed `_app/immutable/` forever but sends `no-store, must-revalidate` on `index.html` + the game's stable-named `assets/…` (covers spine page images too, which an `.atlas`/JSON `?v=` query can't reach via relative resolution), so a re-publish always serves fresh. **Cleanup ✅ DONE (2026-06-06):** the superseded `r2-sync-deploy.mjs` (S3-based 2026-06-02 half) was deleted — the HTTP puller is the one path. **Step 5 (runtime override `PUBLIC_LIVE_ASSETS=1`) — NOT PURSUING (owner, 2026-06-06).** The feature goal (edit atlas → see new art in-game) is already met by producer + puller + dist-rebuild + fresh-serving (`6435c35`); the runtime override only saved a rebuild for previews, at the cost of the token-gated page-ref-resolution risk (a spine `.atlas`/sprite JSON fetched from `/api/deploy?...&rel=` can't resolve its sibling page — query is dropped on relative resolution — so it'd need per-type page-ref rewriting like the editor's loaders). Judged not worth it. **So live-assets is functionally COMPLETE.** Only step 6 remains = prove end-to-end live (`EDITOR_DOC_SECRET` is set on the launcher → needs a real Atlas Maker deploy with `deploy_path`, then republish Borut and confirm the new art shows).
   - **Dist-rebuild gap FIXED (2026-06-04).** Root-caused a class of "engine fix doesn't reach the game" bugs: standalone games consume engine packages (`pixi-svelte`, `engine-layout`) via their gitignored pre-built `dist/`, which bare `vite build` never rebuilt — so engine SOURCE changes silently never shipped. Fix: the game's `build` now runs `build:engine` (`pnpm --filter "<slug>^..." run build` — topological, only the ~2 packages that emit a dist) before `vite build`. Applied to Book of Borut (`build: pnpm build:engine && pnpm pull:assets --optional && vite build`, replacing the lifecycle `prebuild`) and baked into `scripts/new-game.mjs` so every future game gets it. Verified end-to-end: the SpineProvider spine-key fix now compiles into `build/_app/immutable`. Also fixed the live spine crash (editor stores spine assetKeys as R2 prefixes, games want plain bundle keys) two ways: engine-side `SpineProvider` fallback+fail-soft (`5184d24`) AND launcher-side `/api/editor/doc` rewrite via `resolveSpineKeysForGame` (`5196cc0`, fixes already-published games with no rebuild). Memo: `gotcha_game_build_stale_engine_dist`.

**NEEDS OWNER env vars (unset → consequence):**
- `EDITOR_DOC_SECRET` on the launcher — **SET (confirmed by owner 2026-06-04)**, so `GET /api/editor/doc` serves docs and a game launched with `?k=<secret>` fetches its live editor doc at boot (edits flow to the game). (Older notes above/below that say "unset / editor sandbox" are stale — disregard.)
- `ANTHROPIC_API_KEY` on the launcher — until set, the Localization tool loads but Translate returns a clear error.
- `GAMES_BASE_URL` — set to the current test server, else the home Games section shows a muted hint. (Now superseded by the admin-managed games DB, but the env still gates the legacy quick-bridge.)
- `COMFY_ORG_API_KEY` (+ comfy.org credits) — required to run gpt_image generation.
- `SHEET_TOOL_URL` (+ matching `SHEET_TOOL_SECRET`) on the launcher — until set, `/sheet` shows the "not configured" page. Also still owed: create the Railway service from `services/sheet-tool/` (envs `R2_*`, optional `SHEET_PROJECT`/`SHEET_TOOL_SECRET`).
- `ATLAS_BLUEPRINT_SECRET` — set the SAME value on the launcher + atlas-tool Railway services to enable blueprint PUBLISHING (upload to the shared `_shared/blueprints/` library). Unset → publishing stays OFF in the deployed tool (fail safe); reading/using blueprints is unaffected. The launcher only hands it to users with the `blueprintPublish` capability (admin by default).

**Live-verify owed (✅ code / ⏳ NOT browser-tested):**
- **atlas-tool compose/slice (B10 / `.atlas` Part A):** upload `.atlas` + compose + slice against live R2.
- **FLUX + gpt_image live:** FLUX proven live (2026-05-30, ~105s txt2img); **gpt_image still blocked** — `Images to RGB` node not installed + needs `COMFY_ORG_API_KEY`; only SDXL ControlNets installed (FLUX+shape_ref/ControlNet won't work). (The 3 are also now seeded as reference **blueprints** — see B43; live generation THROUGH a blueprint is owner-side verify-owed.)
- **FTP Browser (`/files`):** binary upload, recursive delete/move, `Load more` pagination, `%2F`-encoded `CopySource` move/rename — against live R2. **Admin full-server view (2026-06-02):** whole-bucket browse + Railway/Postgres tab (`/api/db/{tables,rows}`, secrets redacted) — code only, not yet browser-tested live.
- **Invisible Editor end-to-end:** expand→thumbnail→drag→render→overlay→save (B18) + non-fallback doc fetch (seed a real `editor/<client>/lines/scenes.json` in R2; port consumer into Book of Borut, "step 9b/10b").
- **Invisible Editor — layout contract (coord-space + text + validation)** 🚧 code done (2026-06-05, branch `feat/editor-layout-contract`). Closes the "saved positions render offset / editor doesn't emit everything the game needs" gap. **(a) Coordinate-space self-wrapping:** `<LayoutScene>` (`packages/engine-layout`) now reads `scene.space` (`game|standard|canvas|background`) and **self-wraps** in the right container (`<MainContainer>` / `<MainContainer standard align…>` / none / cover-fit) so a scene renders identically regardless of mount site — fixes the bug where a `game`-space scene mounted outside `MainContainer` drew main-coords as raw canvas pixels (offset background). New `Scene.align` + `'background'` space; `LayoutNodeView` cover-fits bg sprites/spine via `normalBackgroundLayout`. `apps/lines/Game.svelte` migrated to drop its manual `<MainContainer>` wrappers; existing scenes tagged with their space (`basegameOverlays`→`canvas`, `hudBar`→`standard`+`align bottom`) to keep `lines` byte-identical. HUD path unaffected (it consumes scenes as position data via `hudPositions.ts`, not `<LayoutScene>`). **(b) Rich text:** `TextNode.style` → full `TextStyle` (wordWrap/wordWrapWidth/breakWords/align/lineHeight/letterSpacing/fontStyle/stroke/dropShadow); editor Properties panel exposes them. **(c) Persistence:** launcher `normalizeDoc` now preserves `space`/`align`/the new style fields on R2 round-trip (was silently dropping `background`+`align`). **(d) Validation:** live client-side content warnings in the editor (sprite/spine referencing a missing/unassigned asset) surfaced next to slot warnings; scene space/align controls added. `pnpm --filter engine-layout build` + `pnpm --filter launcher-api build` GREEN; code-reviewed (no blockers). **⏳ Owner verify online:** `lines` renders unchanged; a background placed in the editor is no longer offset; text wrap/stroke/shadow render. **Deferred:** font-registry validation (no project font list yet — TODO marked in client); background **spine** cover-fit in the editor's WebGL preview (2D sprite path is correct); responsive per-layoutType text style overrides.
  - **SHIPPED to `main` (2026-06-05):** PR #21 (self-wrap+text+validation, `b34d371`), PR #23 cross-type clobber guard (`3b2bad0`), PR #24 seed bind-anchors (`47c801d`).
  - **Cross-type clobber guard (PR #23):** loading a "Placed game layout"/blank scenes whose game type ≠ the project's own (e.g. Lines into a bookOf project) now warns specifically AND **suppresses autosave** until you explicitly Save (converts) or Discard (reload). Same-type loads keep the non-destructive preview. This was the bug that overwrote `borut/bookofborut`'s doc with Lines content.
  - **Seed bind-anchor contract (PR #24) — IMPORTANT, applies to every game seed (`seed-game-editor.mjs` + each game's `defaultLayout.ts`):** dynamic / full-bleed / event-gated scenery (background, free-spin counter/intro/outro, Win/Transition) MUST be **bind anchors in `space: 'canvas'`** so the registered coded component owns rendering + layout + show/hide; `canvas` space means `<LayoutScene>` adds no `MainContainer` (the component supplies its own — avoids double-wrap). The board frame is the one genuinely static piece → real sprite in `game` space. The OLD seed emitted free-spin/bg as STATIC sprite+text → they showed during base game (no gating) + rendered serif (a static Text node can't use the `gold` BITMAP font) + the bg drew at main-box coords (offset). Re-seeding with the fixed seed corrects a live game **without a rebuild** (the game renders the R2 doc; bind anchors render right on the deployed engine too — `space` is just ignored by the old engine).
  - **CONSUMER MIGRATION required when a game bumps to this engine:** the self-wrapping `<LayoutScene>` means a game's `Game.svelte` must DROP its manual `<MainContainer>` wrapper around a `game`-space `<LayoutScene>` (else double-scale). Done for `apps/lines` AND **Book of Borut** (its repo, commit `1a2eaf2`: submodule→`47c801d`, Game.svelte de-wrapped, `defaultLayout.ts` scenes tagged `space:'canvas'`, `I18nTest` gated behind `import.meta.env.DEV`). **The game on `games.invisiblewall.org` is a separately-published bundle — it does NOT auto-update on merge; needs rebuild (`build:engine`) + `publish-game-bundle.mjs` republish.** So: re-seed = fixes doc-driven bugs live now; rebuild+republish = drops `I18nTest` + adopts the new engine.
  - **Editor "see the game composed" — COMPLETE (PRs #25–31, on `main` 2026-06-05).** Coded `bind` overlays (Background/Win/Transition/FreeSpin*) are now shown, placed, movable + stable in the editor — the game still mounts the real component (it ignores all of this). **(1) Art preview (#25/#26):** editor-only `BaseNode.preview.art` (`{kind:'spine'|'sprite', assetKey, region?, fit:'cover'|'contain'}`) draws a stand-in for a coded anchor (2D region + WebGL spine). **(2) Catalog-driven, not per-item (#27):** `packages/engine-layout/src/lib/boundComponentCatalog.ts` — `BOUND_COMPONENT_DEFAULTS` maps a coded component NAME → its preview by CONVENTION asset name; `resolveAnchorPreviewArt(node,assets,regionIndex)` resolves explicit override → catalog → the project's own assets. So any game gets previews with zero per-item config (convention published at `docs/conventions/coded-component-assets.md`). The seed emits plain bind anchors — no baked preview. **(3) Board-relative placement / Level 1 (#28):** catalog `placement` (`cover|centre|boardCentre|boardLeft`) + `computeOverlayPlacement(placement,{main,board,art})`; the editor reads the board rect from the doc's `boardFrame` node (game-agnostic) and maps MAIN→screen like `<MainContainer>`, so previews land at (≈) the real in-game spot. **(4) Drag to reposition / Level 2 (#29):** overlay anchors are draggable; the drag writes `node.x/y`, which the game ALREADY applies via `LayoutNodeView`'s wrapping `<Container>` for a canvas-space bind — so it moves in-game with NO engine/component/seed change. Background (cover) stays fixed. **(5) Spine overlay zoom-lock (#30 no-op, #31 real fix):** the WebGL spine preview slid under zoom because the vendored `OrthoCamera` `up=(0,-1,0)` (cancels WebGL's y-flip) makes `lookAt`'s x-axis `(-1,0,0)` → mirrors X. Fixed in `EditorSpineLayer` per-skeleton bake: `skeleton.x = clientW - (world*zoom+panX)`, negate `scaleX`; Y untouched. **Deferred:** resolution-independent (fractional) drag offset; true per-anchor in-game positioning for components that self-position (currently the bind offset is additive). Owner-confirmed working online.
- **Component Editor (`/components`) — UX batch A SHIPPED to `main` (2026-06-08, `editor:` commit).** Owner found the standalone Component Editor confusing (a polluted "library" of duplicate empty shells, clicking shows nothing, unclear buttons, no way back to the editor). Fixed: **(a) single-component feel** — the component picker shows ONLY in the home state; opening a component switches the left panel to the Asset library, so the tool visibly edits one component at a time. **(b) Delete** — new `deleteComponent` storage fn + `DELETE /api/editor/component` + per-row trash control (confirm), so accumulated shells can be purged (there was NO delete before). **(c) No more duplicate generation** — the editor's "Edit as component" (`editContainerAsComponent`) is now idempotent by name (overwrites the existing project component instead of minting a new id each click — the root cause of the duplicate Reel grid/Balance/Win rows). **(d) Navigation** — a `← Editor` back-link in the Component Editor topbar + the editor refreshes its component list when its tab regains focus. Also documented the required `R2_*` vars in `apps/launcher-api/.env.example` (a local launcher 500s without them). ⏳ Owner-verify live. **NEXT — Batch B ✅ SCOPED (owner-chosen 2026-06-08, NOT yet built) → see `invisible-editor.md` §13.** Owner chose the ambitious reading: **value-as-param** — HUD Balance/Win/Bet become ONE `HudReadout` component instanced 3× (the data source is a param, not coded identity), with **per-project** author-set defaults set in the Component Editor + per-placement overrides in the scene Editor. Key finding: this is **build step 6 of §8.8** (the param/value-binding layer — the explicit `ComponentInstance.svelte:53` "STATIC ONLY" seam); steps 1–5 (static componentInstance expansion + storage + reuse) are DONE and §12 already proves the appearance half on the bound HUD path, so Batch B = value feed + per-project defaults store + Component-Editor param surface + reconciling the two component systems (the Balance/Win shells in `/components` are dead artifacts). Phased B1 → B2 → B3 → B4. **B1+B2 ✅ SHIPPED to `main` 2026-06-08 (`b26bc3b`, `engine:`):** `BaseNode.paramBindings` + Svelte-free `resolveComponentParams`/`resolveBoundValue` (`componentParams.ts`) + `componentParamsContext.ts`; `ComponentInstance` threads resolved params + subscribes to a registered value SOURCE (`registerComponentValues.ts`, keyed by `balance`/`win`/`bet` — any component with a `source` param binds), exposing a reactive `value`; `ParamReadoutText.svelte` formats + counts up via `svelte/motion` `Tween` (no GSAP); `LayoutNodeView` text branch routes a numeric bind through it. Proof = `apps/lines` story renders ONE `HudReadout` def as TWO instances (balance tracks, win counts up). PARITY: no `paramBindings` + no registered source ⇒ byte-identical. **B3 ✅ SHIPPED 2026-06-08 (`editor:`):** per-project defaults store (`editor/<project>/component-defaults/<id>.json`, `componentDefaultsStorage.ts` + `GET/POST /api/editor/component-defaults`), Component-Editor "Defaults (this project)" controls (typed per param kind, debounced persist), and a **non-empty canvas preview** — `EditorCanvas`'s 2D text draw is now param-aware (reuses `resolveBoundValue`; parity when `componentParams` empty). Precedence wired: `def.param.default ◁ projectDefault` (instance override is B4). **⏳ Owner verify live.** **Gap to demonstrate B3/B4:** the existing `/components` entries are EMPTY shells (no params/text node) → preview still blank + "declares no params" hint; needs a real parametric `HudReadout` def seeded (the Component-Editor UI can't yet author `paramBindings` on a node) — natural B4 prep. **B4 ✅ SCOPED (owner-chose HYBRID 2026-06-08) → see `invisible-editor.md` §14.** Architecture finding: the HUD bottom bar is a **bespoke `LayoutEditable` renderer** decoupled from the engine path (3 separate coded `Label{Balance,Win,Bet}` snippets hardcoded to `stateBet.*`; not componentInstances, no value registry). HYBRID = `LayoutEditable` keeps its positioning but **mounts a `<ComponentInstance>`** of ONE shared `HudReadout` def for the 3 readouts (fed by `registerComponentValues({balance,win,bet})`), so B1/B2/B3 become the HUD's foundation + the Component-Editor-owns-it model holds (rejected: full engine rewrite = too risky; §12 `source`-prop = orphans B1/B2/B3). Phased B4.1 (def + registration — also makes B3 demonstrable) → B4.2 (value sources) → B4.3 (LayoutEditable mounts ComponentInstance) → B4.4 (convert 3 nodes + parity flip, retire coded `Label*`) → B4.5 (deliver per-project defaults to the standalone game — lean: launcher bakes defaults into served def `param.default`) → B4.6 (Borut mirror + republish). Parity-gated. **B4.1+B4.2 ✅ SHIPPED (`11f09df`):** `HUD_READOUT_DEF` built-in (`builtinComponents.ts`, surfaced to the Component Editor via a `componentStorage` built-in fallback → makes B3 demonstrable) + `apps/lines` registers it & the `balance`/`win`/`bet` value sources (`valueSource` runes adapter). **B4.3 ✅ SHIPPED (`8dc1476`):** `LayoutEditable` mounts a `componentInstance` HUD node via `<ComponentInstance space="standard">` at the same `hudPos()` Container — engine layout context confirmed live in the HUD tree. **B4.4 ✅ SHIPPED (`5143fd1`, owner-chose MOUNT path §14.3):** new coded `HudReadout.svelte` reuses `UiLabel` (keeps localized caption via the same `i18nDerived` selectors, font, per-source CURRENCY formatting, `svelte/motion` count-up, `LabelBet` tap-to-menu); `HUD_READOUT_DEF.root` is now a single `bind: HudReadout`; `hudBarScene({ readouts: true })` (opt-in, `apps/lines` only) emits `componentInstance(hudReadout)` for balance/win/bet; `LayoutEditable` `mounted()` gate stops the coded-snippet double-render; `Label*`/`UiLabel` KEPT for revert. **Reversible** (drop `{ readouts: true }`). **Borut untouched** (zero scene-set diff, gen-scene-sets check passed). **✅ VERIFIED LIVE (Claude, lines dev server 2026-06-08):** the `apps/lines` base-game HUD renders "Balance / $0.00" through the `HudReadout`→`UiLabel` mount — correct caption/font/currency, no console errors, no Svelte warnings (fixed one `state_referenced_locally` in `HudReadout`, `afa45b5`). (Win count-up needs a winning spin — owner play-through.) **B4.5 ✅ SHIPPED (`cf5cb97`):** `registerComponentDefaults.ts` registry (sibling to `registerComponents`); `ComponentInstance` passes `getComponentDefaults(componentId)` as the projectDefaults layer of `resolveComponentParams`; `GET /api/editor/doc` now returns `componentDefaults` (`listComponentDefaults(projectKey)`); `apps/lines` `loadEditorScenes` calls `registerComponentDefaults` at boot. (Chose the registry+doc-field delivery over "bake into served def" since the def is a code built-in.) PARITY: nothing registered ⇒ def defaults apply (verified — lines dev falls back, HUD unchanged). **B4.6 CODE ✅ SHIPPED (engine `a989b16` + Book-of-Borut `7130695`):** engine `referenceLayouts/bookof.ts` now passes `hudScenes({ readouts: true })` + regenerated `scenes/bookof.json` (3 `hudReadout` instances); Borut repo bumped its engine submodule `fd6d64e → a989b16`, mirrored `Game.svelte` (register `HudReadout` + `registerComponents`/`registerComponentValues` + `valueSource`) + `editor-scenes.ts` (`registerComponentDefaults`), added `valueSource.ts`, and approved `esbuild`/`svelte-preprocess` build scripts in `package.json` (the bumped engine's `gen-scene-sets` prestep needs esbuild — a fresh install otherwise fails). Borut `pnpm build` GREEN. **⏳ OWNER DEPLOY (order matters — bundle BEFORE doc):** (1) **republish the Borut bundle** (Build&Deploy / `publish-game-bundle.mjs`) — still renders coded labels against the old R2 doc (parity); (2) **then flip the HUD doc to readouts.** PREFERRED = the editor's **"↻ Refresh HUD layer"** button (`addHudLayer`, now emits `hudScenes({ readouts: true })` after `6246b0a`) — open `borut/bookofborut` in `/editor` → Refresh HUD layer → Save. **Non-destructive** (only the `hudBar`/`hudCorners` scenes refresh; all other scenes + edits stay — but HUD POSITION edits reset to defaults). Alternative = the console re-seed (`seed-game-editor.mjs …`), which CLOBBERS the WHOLE doc. Either MUST follow the republish (else the live bundle can't render the new readout nodes → blank HUD). **Then cleanup** (delete the now-unused `Label{Balance,Win,Bet}` once owner confirms Borut parity live, incl. win count-up). **Concurrent caveat:** Borut's `static/assets/*` churn from the build's `pull:assets` was left uncommitted (not part of B4.6). **componentInstance editor UX ✅ SHIPPED (`a7c038d`):** 5 owner fixes — `nodeBox` frames the def's real content (selection box matches the chip); the chip caption shows the resolved `label` param (params threaded through `drawComponentInstance`→`drawNode`); a "◇ Edit in Component Editor" button (Properties → opens `/components?id=`); the Properties header shows the COMPONENT name (label demoted to a `· role` tag); "Base (desktop)" → "Base values · `<layout>` view" + tooltip. **B5 — decompose HudReadout into editable parts ✅ SHIPPED (engine `65a5917` + Book-of-Borut `00ead3e`; owner-chose "separate coded parts" over full authored text nodes, because the readout's caption is localized + the BET caption is dynamic (active bet mode) + BET has tap-to-menu — plain text nodes can't reproduce those, §8.5 keeps dynamic behavior coded).** `HUD_READOUT_DEF.root` now holds THREE labelled `bind` mounts — **Background** (`HudTicker`), **Caption** (`HudCaption`: localized/dynamic per source + `label` override + style), **Value** (`HudValue`: per-source CURRENCY format + count-up + bet tap) — split from the single `HudReadout.svelte` (kept, unused, for revert). Each part reads the same engine param context; def id/params unchanged so existing instances + the Borut doc keep resolving. So the Component Editor now lists 3 named editable nodes (was one opaque container) + the author can add nodes; **no format contract needed** (parts stay coded). `apps/lines` + Borut register the 3 parts. VERIFIED LIVE (lines): HUD renders the SAME stacked "Balance / $0.00". **Borut: same single republish** (`☁ Build & publish` button) now covers B4.6 **and** B5; submodule already bumped to `65a5917` in `00ead3e`. **Concurrent (owner, in parallel this session):** §15 "complete/reconcile the scene set" + Move 2 generator unification landed independently (`3878ad8`, `5ab88c1`) — disjoint files from the B4 commits.
- **Layout-doc bake (freeze doc + custom component defs at build): ✅ CAPABILITY BUILT (2026-06-09).** Closes the deploy gap behind the per-instance-graphics feature. (Background: a game's first load is NOT slowed by the data-driven layout — Svelte compiles at BUILD time; runtime just walks the ~12 KB JSON doc mounting already-compiled components, assets dominate first-paint, and component-library size is O(0) for games that don't reference a def. The gaps were deployment, not rendering: custom defs lived only in R2 / weren't registered at game runtime, and the doc was fetched live.) **Built (design: `docs/design/live-assets.md` → "Layout-doc bake"):** (1) `engine-layout` `collectComponentIds(nodes)` (pure walker, exported). (2) `GET /api/editor/doc?…&components=1` now also returns `componentDefs` = the transitive closure of referenced defs, resolved via `loadComponent` (built-in→shared→project precedence, so an edited `button` shadows the coded one) — opt-in so the runtime boot fetch stays lean. (3) `apps/launcher-api/scripts/bake-editor-doc.mjs` (sibling to `pull-project-assets.mjs`, fetch-only, `--optional`/`--dry-run`) writes `{ doc, componentDefaults, componentDefs }` to a game's `src/baked-editor-bundle.json`. (4) `apps/lines` wired as reference: `editor-scenes.ts` imports the bundle; a non-null `doc` flips to the **baked path** — `registerBakedComponents()` (after the built-in `registerComponents`, so baked defs shadow built-ins) + `loadEditorScenes()` returns the baked doc, skipping the live fetch. Self-describing (no env flag); checked-in placeholder has `doc: null` → un-baked repos keep fetching live, byte-identical parity. (5) `new-game.mjs` scaffolds a `bake:doc` script. **Builds GREEN:** `engine-layout` + `launcher-api` + `apps/lines` (`vite build`). **⏳ Remaining (owner / per-game):** wire `bake:doc` into a real shipped game's `build` (Book of Borut — its own repo, copy the `apps/lines` `editor-scenes.ts`/`Game.svelte` wiring + the placeholder JSON), run a live bake (needs `EDITOR_DOC_SECRET` + an authored doc), and verify a customized-Button background renders in the shipped bundle. Not browser-verified (endpoint is auth/token-gated). Same family as `gotcha_game_build_stale_engine_dist`.
- **Editor-art export — doc-referenced art reaches the build automatically ✅ CODE BUILT (2026-06-10).** Root cause of "sprite placed in the editor doesn't show in the published game": the editor renders sprites straight from R2 atlas MANIFESTS (`SpriteNode.assetKey` = manifest key), but a game resolves textures only from `assets.ts`-declared sheets — nothing exported doc-referenced atlases to `deploy/`, so they rendered as `Texture.EMPTY`. Built (design: `docs/design/live-assets.md` → "Editor-art export"): (1) `$lib/server/editorArtExport.ts` — walks doc + referenced defs (sprite manifest keys, image-param region names located among project atlases, standalone page images), writes per-atlas TexturePacker json-hash sheets (frames keyed by the editor REGION NAMES) + page copies + `index.json` under `deploy/editor-art/`, prunes stale, idempotent. (2) `POST /api/editor/export-art` (same `EDITOR_DOC_SECRET` gate as the doc endpoint). (3) `bake-editor-doc.mjs` triggers the export + embeds the index as `bundle.editorArt` (dry-run skips — it writes to R2). (4) `apps/lines`: `editor-scenes.ts#bakedEditorArtAssets()` → asset entries (`sprites` per sheet; `sprite` keyed by full assetKey for page drops); `stateApp.ts` spreads into `createApp`. Builds GREEN (launcher-api + lines). ⚠ Build-order per game: `bake:doc` BEFORE `pull:assets` (export writes deploy/, pull mirrors it). ⏳ Owner verify: republish Book of Borut from the desktop launcher → editor-placed sprites + instance image params render.
- **Shared panel chrome for the editor-family tools ✅ (2026-06-10).** Extracted the Scene Editor's resizable sidebars + collapsible sections into SHARED components — `editor/PanelResizers.svelte` (drag handles, clamping, localStorage width persistence per tool) and `editor/PanelSection.svelte` (chevron `<details>` section persisted via sectionCollapse) — consumed by BOTH the Scene Editor and the Component Editor, so a styling/behaviour change in one file propagates to every tool (owner rule: the tool UIs inherit from each other). Component Editor gains resizable sidebars + collapsible sections everywhere (home: New component/Library; open state: Elements/Atlases/Spines/Sheets); its New-component form is now two lines (name row, then type/category/Create). Also renamed the tool: **Invisible Editor → Invisible Scene Editor** (topbar brand, page title, `roles.ts` tool name; route stays `/editor`). NOTE: editor panel widths moved to their own localStorage key (`iw-editor-panels:<project>`) — previously-saved widths in the old ui blob are ignored once (one-time reset to defaults).
- **From-scratch buttons in the Component Editor ✅ (2026-06-10).** The New-component form gains a TYPE select (Blank / Button). Button = same blank root (author drops their own art) pre-declared with the `action` param (`ENGINE_ACTION_CATALOG` options) + category `ui`; a placed instance picks its action from the dropdown and the §18.4 engine hit surface makes the whole art clickable — no coded part, no manual param wiring. Owner-chosen direction: buttons stay COMPONENTS (not a scene-Library element / new node kind), reusing instancing, action feed, editor-art export and the bake.
- **§18.7 editor↔game HUD text fidelity ✅ FIXED (2026-06-10), launcher-only.** HUD readout caption/value font + vertical alignment differed between editor and game. Editor-side fixes: (1) added the games' Typekit kit (`use.typekit.net/aba0ebl.css`) to launcher `app.html` so the editor previews proxima-nova instead of a sans-serif fallback (+ `document.fonts.ready` redraw in EditorCanvas); (2) `drawHudChip`'s `style:'text'` chip now matches the coded `HudCaption`/`HudValue` — resolved fontFamily/fontSize, normal weight, top-anchored at the node origin (was hardcoded `600 30px sans-serif`, vertically centred). Coded parts remain an editor approximation; true WYSIWYG = author caption/value as textBox/text nodes (§18). No game republish. Design §18.7.
- **§18.6 ghost coded HUD buttons after replacing with custom components ✅ FIXED (2026-06-10).** Custom button components placed on the HUD bar left the old coded spin/turbo/± buttons rendering as duplicates: `LayoutEditable`'s `mounted(id)` guard only suppressed the coded snippet when an instance had the RESERVED id, but from-scratch/placed instances get random ids. Now also suppressed when a placed instance covers the snippet's ACTION (buttons) or SOURCE (readouts), id-independent. Parity-safe (seeds carry reserved ids → same result). Design §18.6.
- **§18.5 free author art on HUD scenes ✅ FIXED (2026-06-10).** A plain sprite/text/container dropped on a HUD scene (hudBar/hudCorners) showed in the editor but not the game: HUD scenes render through the bespoke `LayoutEditable` (not `<LayoutScene>`), which only positioned coded snippets by id + rendered `componentInstance` nodes — any other node was dropped. `LayoutEditable` now also renders every "free" node (non-reserved-id, non-componentInstance) via the engine `<LayoutNodeView>` in the scene's space (bar = standard inside MainContainer, corners = canvas), before the coded snippets. Parity-safe (live seed has none). No special component needed. Design §18.5.
- **§18.4 default hit surface for art-only components ✅ FIXED + VERIFIED (2026-06-10, engine `d87bb13`, Borut `2e50713`).** The owner's custom button (Component-Editor def: sprite + text, NO coded `ButtonFrame` part) published as a static image — all button interactivity lived in the coded part. `<ComponentInstance>` now wraps an action-bound, bind-less def in an interactive Container (whole art = hit area, pointer/not-allowed cursor, press → onpress unless disabled); defs WITH bind parts keep the coded part as sole press owner (no double-fire, parity). Verified in Borut dev against the real baked doc: cursors + bet +/− fires. ⏳ Owner: republish Borut from the desktop launcher to ship it.
- **§18 Text Box + universal text localization ✅ CODE BUILT (2026-06-10), engine+lines verified in browser.** ONE reusable `textBox` built-in (text param = literal OR localization key; optional `source` param feeds live engine values — strings render as text, numbers via the count-up readout path), engine text resolver (`registerTextResolver` + `LayoutNodeView` hook, parity when unregistered), `ValueSource` widened to `number|string`, and the Localization tool finally DELIVERS to games: `GET /api/localization/strings` (token-gated, reviewed-only) → baked into the bundle → merged into the Lingui catalog at boot. See `docs/design/invisible-editor.md` §18. ⏳ Remaining: Borut mirror, HUD-corner migration to textBox (needs `gameName`/`clock` string sources), editor key-picker.
- **Sheet Maker ↔ Atlas Maker round-trip — UNIFIED ✅ CODE BUILT (2026-06-10, `626d799`).** Symptom: a sheet packed in the Sheet Maker loaded in the Atlas Maker but "didn't match". Root cause: the Sheet Maker writes packed output to `<client>/<project>/sheets/<sheet>/…` (page PNG, `.atlas`, TP json) + loose inputs to `…/sheet_src/<sheet>/…`, but the Atlas Maker only ever read `input/…`, bridged by a fragile copy `_ingest_self_contained()` (sheets/ → input/refs/) that silently failed (empty `r2_prefix` at export, unticked atlas/TP boxes, already-rewritten paths, once-per-process cache after a container cache-clear). **Fix (Atlas-Maker-side only, no sheet-tool change):** resolve/hydrate/browse `sheets/` + `sheet_src/` natively and DELETE the copy. `cloud_paths.py`: both are lazy subtrees (pulled per-manifest, not eagerly — `sheet_src/` can be large). `batch_atlas.py`: `STAGING_ROOT` proxy + `_staging_rel()` (strips the `<client>/<project>/` R2 prefix — STAGING_ROOT is already project-rooted, so EVERY `STAGING_ROOT / x` must pass through it or you double the prefix); `_hydrate_from_r2_by_name` gains an exact-`r2_key` fetch + a basename scan over sheets/sheet_src; `atlas_file_path`/`source_image_candidates` add STAGING_ROOT candidates + read `source_image_path`. `ui_server.py`: `_resolve_region_ref` routes sheet refs through by-key hydrate; `_fsbrowse` re-rooted at STAGING_ROOT with an allowed-roots whitelist (input/sheets/sheet_src/atlas; legacy `refs/…` field values mapped to `input/…` so the picker opens at the field's folder instead of snapping to root); removed `_ingest_self_contained`, `_fetch_r2_into_input`, `_ingested_manifests` + the `load_manifest` guard. Three manifest shapes still resolve: (a) self-contained `source_image_path`=`sheets/…`, (b) legacy bare-basename, (c) already-ingested `refs/atlas/…`. Closes the same family as B27. `py_compile` clean, code-reviewed (no blockers). ⏳ Owner: deploy auto-runs from `main` (hydration change needs a container restart); smoke-test on Railway — pack a sheet with `.atlas` on → ↻ Refresh from R2 in the Atlas Maker → open the manifest → page + region boxes align; watch logs for `[atlas] lazy-pull sheets/ …`. (HTTP 403s behind the auth gate → verify via Railway logs / in-app, not curl.)
- **Atlas Maker — bulk "Refs → generated" seed ✅ CODE BUILT (2026-06-10, `6dc30da`).** The per-card "USE THIS IMAGE" action (`/userefimg`, bind a region's `shape_ref`/`style_ref` as its atlas tile via `output_override`, verbatim/no-AI) now has a bulk form so you can build/preview an atlas straight from the refs placed in the Sheet editor without running generation. (1) Toolbar button **"⤵ Refs → generated"** (`/userefall` → `_seed_refs_into_outputs`) fills every EMPTY region slot from its ref. (2) **Automatic** on activating a sheet-derived manifest (`export_prefix` truthy) — hooked in `_saveconfig` (the dropdown-activation handler) + the `_handle_deeplink` re-activate branch, so the dropdown-load round-trip seeds too, not just the "Open in Atlas Maker" deeplink (which already seeds from the page slice on fresh import). **Only-empty for both paths** (owner rule): `_region_has_output` skips a region that already has a committed override or a generated variant — and it `ensure_lazy("batch/")`-hydrates the variant pile first so a just-switched project's variants aren't misread as absent and clobbered. Sizing is safe (compose runs every override through `fit_to_region`). Reuses `output_override`/`_mirror`/`_drop_fx_snapshot`; one `save_manifest` per bulk op. `py_compile` clean, code-reviewed (hydration overwrite-risk caught + fixed, no blockers). ⏳ Owner: after redeploy, smoke-test — load a sheet-derived manifest from the Settings dropdown → empty cells auto-fill from refs; the toolbar button fills any remaining empties; existing generations are untouched.
- **Scene Editor mis-cropped re-packed atlases — stale page pick ✅ FIXED (2026-06-10, `7fd7cda`).** Symptom: after Sheet Maker re-pack → Atlas Maker compose+deploy, the editor cut every region wrong ("as if the .atlas is still the old one"). Root cause (verified against live R2, borut/bookofborut S_StaticElements): `findDeployedPage` stem-matched ANY image under `deploy/` — **including the editor's own `deploy/editor-art/` bake output** — ranked ".webp then largest", so a stale 855KB editor-art webp (baked at 10:22, before the re-pack) beat the fresh 840KB deployed page (14:22) by 14KB; new manifest rects then sliced old pixels. The editor never reads the `.atlas` (geometry = manifest `regions[]` only) and the Atlas Maker's compose only reaches R2 via Deploy — so the page pick is the whole game. Fix: shared `$lib/server/deployedPage.ts::pickDeployedPage()` — excludes `deploy/editor-art/` (circular as a display source), ranks lastModified desc → newest ~10s batch → prefer .webp → size desc; used by BOTH `editorRegions.findDeployedPage` and `spine.resolveSpinePageKeys` (had the identical bug). `listAllObjects` now returns `{key,size,lastModified}`. Also fixed: `RegionThumb`'s module-level page cache ignored "↻ Reload art" (sidebar thumbnails stayed stale all session) — now version-busted + cleared by `refreshAssets`. Build GREEN, reviewed (no blockers). ⏳ Owner: after Railway redeploys the launcher, Reload art / refresh the editor — regions should align. Note for later: a re-pack that changes geometry needs a manifest whose `regions[]` match the deployed page (true today: Sheet Maker writes both together, Atlas Maker composes per the same `.atlas`).
- **Editor: bad image-param bindings + advisory sprite slots ✅ (2026-06-10, `79526b4` + `3be2f94`).** (1) The borut `hudreadout` def bound BOTH sprite image fields (`assetKey` + `region`) to one `bg_image` param, shoving a region NAME into `assetKey` → the canvas fetched `/api/editor/regions?sheet=T_UI_Widget_Big` (403 from the scope guard) and placeholder'd forever. Def fixed directly in R2 (`editor/bookofborut/components/hudreadout.json`, stray `assetKey` binding removed); canvas hardened (non-key assetKeys skip the fetch; failed/missing sets fall through to the cross-atlas name index; Reload art clears the failed-key memo); the two image bind dropdowns in EditorProperties now clear each other when pointed at the same param. (2) Owner decision: **graphical (sprite) template slots are advisory, never `required`** — `boardFrame`/`boardFrameEdge` de-required in BOTH `lines` + `bookof` templates (an unfilled sprite slot renders nothing in-game; authors may replace slot art with free nodes). Mount slots (reelGrid etc.) untouched — they anchor engine-owned components and the in-scene anchors must not be deleted. No authored R2 template overrides exist, so the built-ins are live.
- **Atlas Maker CRT splash phrases restored ✅ (2026-06-10, `c023d7d`).** The MIN_SPLASH_MS refactor (removing the double-render probe) made the WORK phrase pool unreachable and froze the splash at a blinking cursor while `location.replace` waited on the cold `_index()` render. Now the splash background-fetches the real UI while the typewriter loops phrases, then document-swaps it in (`history.replaceState` keeps the `fast=1` reload contract; single server render kept; fetch failure → FATAL screen). Verified: py_compile + `node --check` on the exact served script. **Splash SHARED (2026-06-11, `32e6b6f`):** extracted to `iw_common.splash.splash_html(tool_name, phrases)`; Sheet Maker now serves it for `/` BEFORE `_resolve_context()` (cold visits previously blanked the tab while the eager `sheets/` hydration ran) with a packing-flavored pool; atlas behavior unchanged. Audit of other surfaces: Spine Viewer (static R2) + launcher pages (SvelteKit incremental) don't need it.
- **Atlas Maker stale-staging on manifest activation ✅ FIXED (2026-06-11, `091711f`).** Symptom: a Sheet Maker re-export (new sprite added; Save = full export to fixed R2 keys) never showed in the Atlas Maker. Three layers: the manifest dropdown lists/loads once-per-process staging copies (hydrate guard); `atlas_file_path` returned the stale staged `.atlas` and `merge_atlas_regions` (.atlas-authoritative) silently dropped the manifest-only region; and the activation auto-seed loaded the STALE staging manifest + mirrored it back to R2 — overwriting the fresh export (data loss; introduced with the refs→generated auto-seed). Fix: `_refresh_manifest_from_r2()` exact-key re-pulls manifest + bound sheets/ `.atlas` on every real manifest switch (dropdown + deeplink); `_saveconfig` saves the previous manifest only when an edit touched a manifest-backed field (`manifest_dirty`); `all_regions` banners+logs manifest regions missing from the bound `.atlas` instead of vanishing. ⚠ Owner: after the redeploy, re-Save the affected sheet once in the Sheet Maker (in case a stale manifest was already pushed back), then re-pick the manifest in the Atlas Maker.
- **Sheet Maker exports mis-routed to manifests/ ✅ FIXED (2026-06-11, `4551617`).** Follow-up to the stale-staging fix: the persistent "1 manifest region(s) missing from .atlas" banner was REAL — opening a sheet via its `atlas_manifest_*.json` set `loadedDir` = the manifest's parent dir (`source_dir`), Save reused it as `dest_dir`, and `_dest_output_dir` accepted anything under the staging root → the fresh page/.atlas/TP json landed at `manifests/<sheet>.*` in R2 while the manifest's hardcoded back-refs pointed at the stale `sheets/<sheet>/` files (verified live: 24-region `.atlas` at `manifests/S_StaticElements.atlas`, 23-region at the canonical key). Fix: `_dest_output_dir` confines custom destinations to the `sheets/` subtree; `api_export` derives `export_prefix`/back-refs from the ACTUAL output dir. ⚠ NOTE: commit `4551617` also unintentionally carried the owner's concurrent in-flight session-persistence work (`api_session`/`load_session` + api_state hook — additive, compiles, harmless if unused; owner aware). Strays `manifests/S_StaticElements.{png,atlas,json}` left in R2 — delete when convenient. Owner: after redeploy, re-Save the sheet once → re-pick the manifest in the Atlas Maker.
- **"Clear local cache" → TRUE reset of the active project (2026-06-11, CODE BUILT, not yet committed).** Symptom: a file deleted from R2 (launcher FTP browser / a separate container) lingered in a tool and kept showing/reading. Root cause: tools mirror R2 into a per-(client,project) staging subtree, but `pull_prefix` only adds/overwrites (never prunes) and listings come from the local staging glob — so deletions never propagate; the old "Clear local cache" only rmtree'd the ACTIVE project's regenerable subtrees (atlas `batch/`+`deploy/`, sheet `sheet_src/`), KEEPING manifests/atlas/input (atlas) and manifests/sheets/config (sheet), so it could never drop a deleted manifest/atlas/sheet for the project you were in. Fix: the active project now rmtrees the WHOLE per-project subtree (same whole-tree delete the inactive branch already did) then `hydrate(..., force=True)` to rebuild from R2 — manifests/config (+ sheet_session.json, sheets/) pull synchronously, input/atlas (atlas) background, batch/deploy/sheet_src lazily; lazy guards cleared. Net: active staging EXACTLY matches R2; anything deleted from R2 is gone. Guarded so rmtree only ever hits `STAGING_BASE/<client>/<project>`, never STAGING_BASE or a sibling. Sheet WIP `sheet_session.json` lives under the project subtree + mirrors to R2, so it drops then re-pulls from R2 (source of truth) — WIP intact. UI relabeled **↺ Reset from R2** with confirm "Reset this project from the cloud? Re-downloads everything from R2 and DROPS any local files that were deleted from the cloud. Unsaved local work is lost." Files: `services/atlas-tool/ui_server.py::_clearcache` + the embedded clearCache JS/button; `services/sheet-tool/sheet_server.py::api_clearcache` + `services/sheet-tool/ui.html`. `py_compile` clean. ⏳ Owner: redeploy (push to main) then smoke-test — delete a manifest from R2 via the launcher FTP browser → Reset from R2 in the tool → the dropdown no longer lists it.
- **Invisible Storybook engine reference PUBLISHED (2026-06-11):** 207 files / 73.7 MB at `_shared/storybook/engine/` via `publish-storybook.mjs --shared` — `/storybook` now has its first entry; Borut's storybook still to publish (needs `.storybook/` config or build-elsewhere + `--dir`).
- **sheet-tool project-file + B19:** rename/Save/Save As + blank/dup name guards, browser-tested live.
- **B12 project switch:** browser test an actual project switch + cross-project ref picking via `/fsbrowse`.
- **Spine Viewer (B21):** active-project propagation + named empty-state message live (data: run `r2-sync-spines.mjs` for projects meant to have spines — none synced for non-hotfruits yet).
- **Launcher beauty pass (B22):** authed home page not browser-tested.
- **Threaded-race concurrent smoke (fix #2):** two concurrent `?project=` requests for different projects → each writes only its own R2 prefix.
- **iw_common deploys (fixes #3/#7):** verify atlas-tool/sheet-tool/atlas-backend build + run after the Root-Directory flip.

**Invisible Launcher — canonical release + fresh-machine checklist (B31–B37):**
1. **Release the desktop launcher:** bump `LAUNCHER_VERSION` in `C:\Invisible Wall SL\ComfyUI\Invisible_Launcher.py`, then `py scripts/build-launcher-exe.py --upload` (publishes the exe + `latest.json` to R2; running launchers self-update). ⚠️ **Smoke-test the FROZEN exe, not source** — boto3-in-onefile can fail only when frozen (`DataNotFoundError`); after a build, run the exe and click a **Publish** button once.
2. **Seed the cloud (owner, main PC, once / when assets change):** in the launcher → **Publish to cloud** → enter **R2 credentials…** once → **Publish tunnel creds** + **Publish models** + **Publish nodes**. (CLI equivalents for the first two: `node apps/launcher-api/scripts/seed-tunnel-bundle.mjs`, `py scripts/seed-comfyui-models.py`, both with `R2_*` env.)
3. **Fresh machine (hands-off):** Install/Update ComfyUI → **Sync models** + **Sync custom nodes** (sign in → downloads + pip-installs node deps) → **Start tunnel** (auto-installs cloudflared, sign in → fetches creds) → **restart ComfyUI** → generate.

**Open backlog (B-items):**
- **B23 — launcher CLIENT/PROJECT selector broken** ✅ DONE (2026-05-31). Rebuilt the two-step selector in `(app)/+page.svelte` (no schema change — client stays derivable). The four root causes are gone: (A) the re-seeding `$effect` that clobbered the user's pick on every `invalidateAll` now re-seeds ONLY when the server's active project actually changes (gated by a `syncedActive` sentinel) — never mid-edit; (B) **CLIENT is now a pure local filter** — `onClientChange` only re-filters the PROJECT list (keeps the project selected if it belongs to the chosen client, else blanks it), it no longer auto-submits `?/setProject`; (C) committing happens ONLY on an explicit PROJECT change (`onProjectChange` submits on a real change); a blank state shows a disabled "— choose a project —" placeholder so a cross-client browse forces an explicit pick instead of silently snapping; (D) dropped the `await tick()` papering (and the now-unused `tick` import + `activeProject` derived) since the options no longer change out from under the submit. Server `?/setProject` (validates `canAccessProject`) + the layout's accessible-list fallback to `cloud` are unchanged — that fallback is now benign (client=Unassigned, project=cloud, stable) rather than triggering a reshuffle. `pnpm --filter launcher-api build` GREEN. ⏳ Not browser-tested live (authed page) — verify: pick a project → it sticks across reloads; switch CLIENT → only the project list refilters (active unchanged until you pick); pick a project under the new client → commits + sticks.
- **B24 — restore back/forward navigation between tools (+ back to launcher)** ✅ DONE (2026-05-31). Both launcher redirects (`(app)/atlas/+page.server.ts`, `(app)/sheet/+page.server.ts`) now append `home=<ENV.ORIGIN>` + a `sibling=<full other-tool URL>` (the sibling carries its OWN `k`+`client`+`project`, URL-encoded) — **but only when the user actually has the sibling tool** (`tools.some(t => t.id === …)`), so the link can't bypass the role gate (the tools' own gate is just the shared secret). Sheet Maker (`services/sheet-tool/ui.html`): added an "Atlas Maker →" anchor (`#siblinglink`, hidden until `init()` reads `?sibling=`); the `← Launcher` back-link already read `?home=`. Atlas Maker (`services/atlas-tool/ui_server.py` `_index` header): added `← Launcher` (`#homelink`) + "Sheet Maker →" (`#siblinglink`) anchors (hidden by default) + a small JS reader of `?home=`/`?sibling=` (back-link falls back to `LAUNCHER_URL` env, default `app.invisiblewall.org`); `LAUNCHER_URL` added to `atlas-tool/.env.example`. The "Atlas Maker linked ✓" status pill is kept separate (it's honest config status, not a link). **Verified:** `pnpm --filter launcher-api build` GREEN; atlas `py -m py_compile` OK **and** a real `_index()` render-smoke (44 KB HTML, no `.format()` brace error) confirms both links + the JS readers are present; sheet `ui.html` UTF-8/no-NUL. ⏳ Not browser-tested live (needs the launcher→tool redirect + cross-origin hop). NOTE: the sibling link's client/project are baked at redirect time — switching project *inside* a tool makes the sibling link point at the prior project until you return via the launcher (acceptable; the launcher is the source of truth for the active project).
- **B25 extraction (the real payoff)** ⏳ pending: progressively extract genuinely-shared UI into `packages/components-*` (start `<R2Browser>` from `ftpScope.ts`+`api/files`, `<DataTable>` for admin/localization) + the launcher gate/scope consolidation. Keep `docs/ui-inventory.md` updated. (`reuse-check` skill + `docs/ui-inventory.md` infra is ✅ DONE.)
- **B26 — tool output/staging not reachable from the FTP Browser** ⏳ TODO (needs investigation): sheet-tool output (e.g. `/data/sheet-tool/invisible_wall/test1/output`) + atlas-tool outputs aren't visible in `/files`. **Key distinction:** `/data/<tool>/...` is the tool's **container-local staging**; the FTP Browser only browses **R2** (`<tool>/<client>/<project>/` via `ftpScope.allowedPrefixes`). So an output is FTP-visible only if **mirrored to R2** under `sheet_maker|atlas_maker/<client>/<project>/output/`. Likely root: outputs aren't (fully) written through to R2 (atlas-tool's raw `output/` is intentionally not mirrored; sheet-tool may only `_mirror` some paths) → they live only on container disk. **Decision:** (1) write outputs through to R2 so the existing FTP browser shows them (preferred, no new UI) vs (2) add a "browse tool staging" mode for the `/data` disk (heavier, ephemeral). Confirm what's actually missing from R2 first. Files: `lib/server/ftpScope.ts`, `api/files/list`, `services/{atlas-tool,sheet-tool}` mirror/`push_dir` logic.
- **B27 — Atlas Maker can't generate: Sheet-authored manifest's `atlas.atlas_file` is a raw Windows path, not an R2 key** ✅ DONE (`f04fb26`, 2026-05-31). **Root cause = the geometry resolver, fixed.** `batch_atlas.atlas_file_path()` treated a Windows path (`C:\…\symbols.atlas`) as relative — `Path(ref).is_absolute()` is `False` on Linux — and joined it onto `INPUT_DIR` verbatim → `/data/.../input/C:/.../symbols.atlas` (the exact error); its `p.name` fallbacks were dead because POSIX `Path.name` of a backslash string is the whole string. Fix: normalise separators, derive the true basename (`Path(ref.replace('\\','/').lstrip('/')).name`), try `INPUT_DIR/refs/atlas/<name>` (where B10 Upload .atlas + the B14 ingest write) + bare-name candidates — the same robustness `source_image_candidates()` already had for the page image; a genuine container-absolute path that exists still wins; the not-found fallback now reports `refs/atlas/<name>` so the message names a real, fixable location. Also tightened `ui_server._ingest_self_contained._looks_like_r2_key` to reject Windows drive paths (`^[A-Za-z]:/`) so ingest no longer wastes an R2 fetch on a local path. **Verified:** `py -m py_compile` clean (both files) + a pathlib unit-check proving the Windows path now resolves to `refs/atlas/symbols.atlas` instead of `input/C:/…`. **Scope note (honest):** part (a) "sheet side always emit `atlas_file` as an R2 key" was NOT needed — B14 already emits the R2 key on cloud libGDX export; the broken manifests were authored OFFLINE (no R2 prefix, so no key derivable), and the resolver fix (b) is exactly what rescues those. ⏳ Not live-tested: load such a manifest → Upload .atlas (or rely on ingest) → generate. The `unassigned/cloud` vs real-project context point (cf. B23) is separate and unchanged.
- **B29 — seed Borut/HotFruits into a real launcher project** 🚧 in progress (2026-06-01). Created the **Borut** client + **hotfruits** + **book_of_borut** projects via `/admin` (underscore key for Book of Borut to dodge the Python `_safe_proj_name` hyphen-rewrite — see note below); the launcher now hands the atlas tool `?client=borut&project=hotfruits` (verified live: the tool's SESSION bar reads `hotfruits`, not `unassigned/cloud`). To populate the empty project, **fixed `services/atlas-tool/seed_r2.py`** (it predated `<client>/<project>` isolation — hardcoded the wrong `atlas_maker/cloud/<proj>` prefix). It now: (1) targets `atlas_maker/<ATLAS_CLIENT>/<ATLAS_PROJECT>` (defaults `borut`/`hotfruits`), keeping a valid slug key VERBATIM; (2) **localizes atlas-bound manifests** — a manifest authored offline carries a Windows path in `atlas.atlas_file`/`source_image` (the B27 error case); the seed now copies the referenced `.atlas` + its page image from the game repo into `input/refs/atlas/` and rewrites the manifest to the relative `refs/atlas/<name>` key the cloud resolver expects; (3) also drops each tool-folder `.atlas` into `input/refs/atlas/` so manifests with a bare relative `atlas_file` (symbols2/3/transition → `symbols2.atlas`) resolve too. **Verified:** `py -m py_compile` clean + a DRY-RUN against the real local manifests proving symbols/anticipation/buy_button/loader/mmBG get their Windows paths rewritten and the real `.atlas`+PNG bytes read off disk (e.g. `symbols.atlas` 2937 B + `symbols.png` 1.06 MB), while symbolsStatic/symbols2/3/transition are correctly left as-is. ⏳ NOT yet run against live R2 / not Create-Atlas-tested: run `py seed_r2.py` (R2 creds in env) on the box with the local tool, restart atlas-tool (or ↻ Refresh from R2), then Create Atlas on `symbols`. **Slug bug noted (not yet fixed):** the launcher allows hyphens in keys but the Python tools run keys through `_safe_proj_name` (→ `_`), so a hyphenated launcher key (`book-of-borut`) would diverge from the tool's path (`book_of_borut`); using underscore keys sidesteps it. Worth a follow-up fix in `services/_shared/iw_common/context.py` + both `cloud_paths.py`.

- **B30 — launcher ComfyUI card pointed at the wrong ComfyUI** ✅ DONE (2026-06-01). The `comfyui` local-tool card (`apps/launcher-api/src/lib/roles.ts`) linked download → `comfy.org/download` (ComfyUI **Desktop**), a different product from the `ComfyUI_windows_portable` build the pipeline actually uses (tunnel `comfy.invisiblewall.org → localhost:8188`, started by the desktop `Invisible_Launcher.py` which hardcodes `…\ComfyUI_windows_portable\ComfyUI`). Repointed download → `github.com/comfyanonymous/ComfyUI/releases/latest` (the portable `.7z`) + reworded the steps (portable not Desktop; extract with 7-Zip; save the extracted ComfyUI folder path). The install-path **recording** already works (saved per-user to the `toolInstalls` table via `?/saveInstallPath`, retrievable with `getInstallPaths(userId)`) — no new code; it's available to surface as an env value later. `pnpm --filter launcher-api build` GREEN. **Open follow-up — auto-install:** the **web** launcher can't install to a user's disk (browser sandbox); the natural place for an automated download+extract+register-path is the **desktop `Invisible_Launcher.py`** (already manages the portable ComfyUI). Today new users still install manually. Also worth wiring: have the desktop launcher read the recorded per-user path instead of hardcoding it.

- **B31 — ComfyUI auto-install + Invisible Launcher as the distributed desktop app** 🚧 mostly done (2026-06-01, follow-up to B30). Replaces the manual ComfyUI install with a one-click flow and turns the desktop launcher into a downloadable `.exe`. **(a) Desktop `Invisible_Launcher.py`** (outside the repo, at `C:\Invisible Wall SL\ComfyUI\`): added `install_comfyui()` + `latest_comfyui_asset()`/`_select_portable_asset()` — resolves the latest **Windows portable** build from the GitHub releases API, downloads it (urllib, stdlib, with % progress + reuses an already-downloaded archive), extracts with the **real 7-Zip engine** (`_ensure_7z()` prefers an installed `7z.exe`, else auto-downloads the standalone `7zr.exe` from 7-zip.org), backs up any existing install to `…_portable.bak`, and writes `comfyui_dir`/`python_exe` into config. **NOTE:** py7zr was tried first but **cannot decode the BCJ2 filter** the ComfyUI portable `.7z` uses ("BCJ2 filter is not supported by py7zr") — hence the 7-Zip CLI. Added a **ComfyUI control card** on the Launcher tab (status + **Install / Update ComfyUI** + **Start**, both on worker threads via `_ui_after`). Made `LOGO_FILE` frozen-aware (`_MEIPASS`) so the bundled exe finds the emblem. **Verified:** `py -m py_compile` clean + live `latest_comfyui_asset()` resolves `ComfyUI_windows_portable_nvidia.7z` (2.01 GB); full download/extract NOT run. **(b) Web launcher** (`apps/launcher-api`, via launcher-studio agent): removed the `comfyui` tool entirely; added a `invisibleLauncher` **local** tool (rocket icon, `download: '/download/launcher'`); new auth-gated route `src/routes/(app)/download/launcher/+server.ts` **streams** the exe from R2 key `tools/invisible-launcher/Invisible_Launcher.exe` (presign skipped — `@aws-sdk/s3-request-presigner` isn't a dep; streams via existing `getObjectBytes`, 404 until uploaded). `TOOL_DOC_SLUG` updated (`invisibleLauncher → invisible-launcher`). `pnpm --filter launcher-api build` GREEN. **(c) Build pipeline:** `scripts/build-launcher-exe.py` — PyInstaller `--onefile --noconsole`, renders an `.ico` from the emblem, `--collect-all customtkinter` + `--collect-submodules py7zr`, optional `--upload` to R2 (boto3). NOTE: drop the `#!/usr/bin/env python` shebang in repo Python scripts — the Windows `py` launcher honors it and delegates to the broken Store `python` stub. **(d) Docs:** new `docs/tools/invisible-launcher.md`; `docs/tools/comfyui.md` updated to the auto-install flow. The live launcher was committed + pushed to `Invisible-Wall-SL/invisible-launcher` (`5d8a1e4`). **⏳ OPEN (owner):** (1) **upload the `.exe` to R2** — owner runs `py scripts/build-launcher-exe.py --upload` with `R2_*` env set; until then `/download/launcher` 404s; (2) **live-test** the exe on a clean machine: download from the portal → Install / Update ComfyUI → Start → Start tunnel; (3) consider streaming vs presigned for the ~big exe (Node loads it fully into memory per download — fine for a few internal users); (4) optional: have the desktop launcher read the per-user recorded install path (B30) instead of the fixed `C:\Invisible Wall SL\ComfyUI` location.

- **B32 — Invisible Launcher self-update** ✅ DONE (2026-06-01, follow-up to B31). The desktop launcher can now update itself in place. **(a) Web (`apps/launcher-api`, launcher-studio agent):** two **open** routes (outside the `(app)` auth gate, since the desktop app has no portal session) — `GET /api/launcher/latest` (returns the R2 manifest `tools/invisible-launcher/latest.json`) and `GET /api/launcher/download` (streams the exe). The portal card's `download` was repointed to `/api/launcher/download` and the old authed `(app)/download/launcher` route deleted (one path now). Build GREEN. **(b) Desktop launcher:** `LAUNCHER_VERSION` constant + a **Check for updates** button in the header. `check_launcher_update()` polls `/api/launcher/latest`; `is_newer()` compares dotted versions via tuple (so 1.2.10 > 1.2.9). If newer + frozen, `download_launcher_update()` pulls the new exe to `%TEMP%` and verifies sha256, then the **swap-on-restart** dance: `apply_update_and_restart()` relaunches the new exe with `--apply-update <oldpath>` and `os._exit(0)`s to release the lock; the new exe's `_run_apply_update()` copies itself over the old path (retrying while locked) and relaunches. From source (not frozen) it tells the user to `git pull`. **(c) Build script** `scripts/build-launcher-exe.py`: reads `LAUNCHER_VERSION` from source, and on `--upload` publishes `latest.json` (`{version, size, sha256, notes}`) alongside the exe; added `--notes`. **Verified:** both .py compile clean; version-compare unit-checked (1.0.1>1.0.0 True, 1.0.0>1.0.0 False, v0.9>1.0.0 False, v1.2.10→(1,2,10)); exe rebuilt 31 MB; web build GREEN. **Release flow:** bump `LAUNCHER_VERSION` → `py scripts/build-launcher-exe.py --upload`. ✅ Live-tested end-to-end (owner, 2026-06-01): version bump → build/upload → in-place self-update swap.

- **B33 — fresh-ComfyUI-install hardening + install progress bar** ✅ DONE (2026-06-01, follow-up to B31). After the BCJ2 fix, a fresh portable install couldn't START: (1) the launcher starts ComfyUI with `--base-directory …\Shared`, which makes ComfyUI resolve `custom_nodes` under `Shared\custom_nodes` — the prestartup `os.listdir()` crashed with FileNotFoundError because the seed list omitted it. Fixed `_build_comfyui_bat` to also seed `custom_nodes` + `models`. (2) `--enable-manager` needs ComfyUI-Manager's deps; `install_comfyui` now runs the portable python to `pip install -r ComfyUI/manager_requirements.txt` post-extract (non-fatal). **Progress UX:** the install `progress` callback now passes `(msg, frac)`; the ComfyUI card shows a `CTkProgressBar` (determinate % during download, indeterminate while extracting/installing), shown during install and hidden after. **Verified:** `py -m py_compile` clean + introspection confirms the seeds, the bar helpers, and the `(msg, frac)` signature; exe rebuilt. ✅ Live-tested: fresh install → Start on a clean machine (owner, 2026-06-01). **Overwrite/reinstall fix (same day):** renaming `ComfyUI_windows_portable` → `.bak` failed with WinError 5 (Access denied) because a leftover ComfyUI console holds the folder as its cwd. Dropped the rename entirely — `install_comfyui` now stops ComfyUI (`_free_port(8188)`) and extracts **over the folder in place** (`7z x … -y -aoa`), which sidesteps the directory-lock; the error message now tells the user to close ComfyUI windows + retry.

- **B34 — Atlas geometry "not found in R2" (B27 follow-up: R2 auto-hydrate + thread-context fix)** ✅ DONE (2026-06-01). The B27 resolver (`f04fb26`) correctly collapsed a Windows `atlas.atlas_file` to its basename but only probed **local staging** — so a legacy/offline-authored manifest whose `.atlas` lives only in R2 still errored ("Atlas geometry not found in R2: C:\…\symbols.atlas"). Two root causes fixed: **(1) reader never looked in R2** and **(2) the run resolved under `unassigned/cloud`, not `borut/hotfruits`** (the context point B27 flagged as "separate and unchanged"). Changes: **(a)** `batch_atlas.py` — new `_hydrate_from_r2_by_name()` pulls geometry/page files from the active project's R2 tree by basename (the two `seed_r2.py` locations: `{prefix}/input/refs/atlas/<name>` + `{prefix}/manifests/<name>`) into staging before the not-found fallback; wired into `atlas_file_path()` + `source_image_candidates()` (list-return contract preserved); module-level `import storage`. `slice_atlas.py` inherits it via the shared helpers. **(b)** `ui_server.py` — `/render` + `/createatlas` spawn **worker threads**, and (client, project) context is **thread-local**, so workers fell back to env defaults; now ctx is captured on the request thread and re-applied via `project_paths.set_context(*ctx)` inside `run_render`/`run_compose` (slice/upload already ran synchronously, so were fine). **(c)** `atlas_writers.build_manifest` — `atlas.source_image` is reduced to basename so a local OS/Windows path can never enter a new manifest (`source_image_path` R2 key stays the resolvable field). **Verified:** `py -m py_compile` clean (5 files) + code-review clean (no blocking/should-fix). The manual **Upload .atlas** (B10) flow is untouched as a fallback; auto-hydrate only fires when nothing is on disk. ✅ Live-tested (owner, 2026-06-01): manifest under a real project → Create Atlas / slice with no manual upload. **Deploy note:** restart the Railway service after deploy — staging only hydrates net-new R2 objects on demand via this path.

- **B35 — automate ComfyUI-tunnel setup on a fresh machine** ✅ DONE (2026-06-01, live-tested by owner). On a new box the tunnel couldn't start because (1) `cloudflared.exe` wasn't installed and (2) `~/.cloudflared/{config.yml,<id>.json,cert.pem}` (the tunnel credentials) were absent. The desktop **Invisible Launcher** now closes both gaps; the credentials are fetched from the portal (owner login), never bundled in the exe. **(a) Web (`apps/launcher-api`, launcher-studio agent):** `POST /api/launcher/login` (OPEN — desktop has no session; JSON `{email,password}` → **short-lived 10-min** session bearer token; reuses `verifyCredentials`+`createSession`; generic 401, `no-store`) and `GET /api/launcher/tunnel-bundle` (`Authorization: Bearer`, `validateSession` + **role `admin` only**, returns the bundle JSON from R2 key `tools/invisible-launcher/cloudflared-bundle.json`; 401/403/404; `no-store`, never logs secrets). Seed script `scripts/seed-tunnel-bundle.mjs` (owner runs once with `R2_*` env — reads the 3 local files, prints only byte counts, PUTs `{tunnel_id,config_yml,credentials_json,cert_pem}`). Build GREEN. **(b) Desktop `Invisible_Launcher.py`** (outside repo, `C:\Invisible Wall SL\ComfyUI\`): `cloudflared_exe()` resolver (system → PATH → launcher-managed `_tools/cloudflared.exe`); `install_cloudflared()` downloads the official standalone exe (no admin/MSI) with % progress; `portal_login()`/`fetch_tunnel_bundle()`/`write_tunnel_bundle()` (writes the 3 files, **repoints `credentials-file:` to the local path** — injects one if the seeded config lacks it — so it can't inherit the authoring box's home dir); refactored `start_cloudflared()`+`_build_cloudflared_bat(exe)`; `_start_tunnel()` now does ensure-install → login modal (`_prompt_portal_login`) → fetch → write → start on a worker thread (Tk-safe via `_ui_after`). The login token is used immediately + discarded (not persisted). **Code-reviewed:** safe-to-ship verdict; the two should-fixes (short-TTL token, credentials-file no-op) are applied. `py -m py_compile` clean. **✅ OWNER STEPS done (2026-06-01):** bundle seeded, exe shipped, and live-tested on a clean machine (Start tunnel → installs cloudflared → sign-in → fetches creds → tunnel up). **Follow-up (security, ✅ DONE in B38):** the open `/api/launcher/login` had no rate-limiting (at parity with the existing web `/login`, which also had none) — a shared per-IP/email throttle now covers BOTH.

- **B36 — automate ComfyUI model-file provisioning on a fresh machine** ✅ DONE (2026-06-01, live-tested by owner). Completes the fresh-machine story (B31–B33 ComfyUI binary, B35 tunnel creds): a new box's `Shared\Models` is empty → ComfyUI sees no checkpoints/LoRAs → generation preflight fails ("(none installed)"). Models are now mirrored to R2 and pulled by the launcher. **(a) Web (`apps/launcher-api`, launcher-studio agent):** `GET /api/launcher/models-manifest` (`Authorization: Bearer` + role `admin`; reads R2 `tools/invisible-launcher/models-manifest.json`; for each model validates `key` is under `base_prefix/` then adds a **6-h presigned R2 GET url**; returns manifest + `expires_in`; 401/403/404; `no-store`, never logs). Added `presignGet(key, ttl)` to `r2.ts` + dep `@aws-sdk/s3-request-presigner@3.1057.0`. **(b) Seed script `scripts/seed-comfyui-models.py`** (owner, boto3, `R2_*` env): walks `Shared\Models`, uploads each model to R2 `comfyui-models/<relpath>` (multipart; **idempotent** — skips files already in R2 at same size, reuses sha from prior manifest to avoid re-hashing tens of GB), writes the manifest. **(c) Desktop `Invisible_Launcher.py`** (outside repo, v**1.0.2**): `fetch_models_manifest()` + `sync_models()` + `_download_resumable()` (HTTP-Range resume to `.part`, restart on 200-not-206, sha256 verify, atomic replace, **legible message on expired presigned URL** — `.part` kept so re-click resumes); a "ComfyUI Models" card with **Sync models** button + progress bar; reuses the B35 login modal. Downloads go **R2→client directly** (presigned) so the multi-GB data never transits Railway. **Code-reviewed:** safe-to-ship; should-fix #1 (legible expiry error) applied. `py -m py_compile` clean (launcher + seed); `pnpm --filter launcher-api build` GREEN. **✅ OWNER STEPS done (2026-06-01):** models seeded, exe shipped, and Sync verified on a fresh machine (Sync models → downloads → restart ComfyUI → generate). Note: `pnpm-lock.yaml` in this commit also carries incidental dedupe churn beyond the one dep add.

- **B37 — "Publish to cloud" button (owner seeds R2 from the launcher UI)** ✅ DONE (2026-06-01, follow-up to B35/B36; live-tested by owner). The fetch side (download from R2) was automated; publishing (uploading THIS machine's local assets INTO R2) still meant running the two `scripts/seed-*` CLIs. Now the desktop launcher does it from the UI. **(a) Desktop `Invisible_Launcher.py`** (outside repo, v**1.0.3**): a "Publish to cloud · owner only" card with **R2 credentials…** (modal → saved to local `comfyui_manager_config.json`, secret fields masked; endpoint+bucket pre-filled non-secret defaults), **Publish tunnel creds**, **Publish models** buttons + status/progress. New fns `r2_creds()`/`_r2_client()` (boto3), `publish_tunnel_bundle()` (mirrors `seed-tunnel-bundle.mjs`), `publish_models()` (mirrors `seed-comfyui-models.py` — walk `Shared\Models`, skip-by-size, sha-reuse, multipart `upload_file`, write manifest). R2 **write** creds are entered by the owner (env-or-config), never bundled in the exe. **(b) `scripts/build-launcher-exe.py`** (in repo): bundles boto3 into the frozen exe (`--collect-all boto3 botocore` — the data files frozen boto3 needs, not just submodules). **Code-reviewed:** the one blocking bug (dead `_set_r2_credentials` → `_prompt_r2_credentials`) + should-fixes (boto3 collect flag, stale 404 text now points to the Publish card, progress final tick) all applied. `py -m py_compile` clean (launcher + build script). **✅ Frozen-exe smoke-tested (owner, 2026-06-01)** — boto3 works frozen; v1.0.3 shipped, R2 creds entered, and Publish tunnel creds + Publish models run from the main box (replaces the CLI seed steps). **Drift risk (reviewer, STILL OPEN):** the manifest/bundle JSON shapes now have THREE definitions — launcher `publish_*`, the `scripts/seed-*`, and the TS endpoint — with no shared schema; keep them in lockstep if `version`/fields change.

- **B38 — brute-force throttle on the password-login surfaces** ✅ DONE (2026-06-01, follow-up to B35). Both password-login surfaces — the open `POST /api/launcher/login` (desktop tunnel-setup, B35) and the web `/login` form action — accepted unlimited email+password guesses. Added a **shared** throttle used by both so behavior is identical. **(a) `src/lib/server/loginThrottle.ts`** — counts failures independently per **client IP** and per **target email**; a lockout on EITHER scope blocks the request. Policy is **asymmetric** by design: **IP** = 10 free attempts, then exponential backoff (1 min doubling, capped at **1 h**), 1 h decay; **email** = 5 free attempts, backoff (30 s doubling) capped at **15 min**, 30 min decay. The short email cap deliberately bounds the **account-lockout DoS** (a remote attacker who knows an email — possibly the owner's — but not the password could otherwise freeze that login): worst case is a brief, self-healing delay, not a sustained denial. Residual accepted risk: repeated short delays on a known email can't be fully eliminated without CAPTCHA/PoW (out of scope). A scope decays to zero when quiet; a successful login clears both. `checkLoginThrottle()` runs BEFORE the scrypt verify (returns `{blocked, retryAfterSeconds}`); `recordLoginFailure()` after a bad verify; `recordLoginSuccess()` after a good one. Every DB call **fails open** (logs + allows) so a DB hiccup can't lock the whole team out. **(b) Storage = Postgres `login_attempts` table** (`key` PK = `ip:<addr>`|`email:<addr>`, `failures`, `locked_until`, `updated_at`) via Drizzle — migration `drizzle/0006_blushing_snowbird.sql`. Chosen over an in-memory map because the launcher restarts on every Railway deploy and may scale horizontally (in-memory would silently reset/shard the counter). Tradeoffs (both acceptable at this scale): a read-modify-write under high concurrency can lose an increment (only ever *under*-counts, never over-locks); and rows are deleted only on success, so never-succeeding scanner IPs accumulate slowly — the decay logic ignores their stale counts, so it's housekeeping, not correctness (prune rows older than the decay window if the table grows). **(c) Both endpoints** now take `getClientAddress()` and reject with **429** + generic message (`Retry-After` header on the JSON endpoint). The JSON login still returns `{token, expiresAt, role}` on success — desktop contract unchanged. **⚠️ Requires Railway env `ADDRESS_HEADER=x-forwarded-for` + `XFF_DEPTH=1`** so `getClientAddress()` (adapter-node) returns the real client IP behind Railway's proxy — without them the per-IP bucket collapses to one global counter (per-email bucket still works). See `docs/INFRA.md`. **Build GREEN** (`pnpm --filter launcher-api build`). **✅ SHIPPED + LIVE-VERIFIED (2026-06-01):** committed `ab9c9df`, deployed; owner set `ADDRESS_HEADER`/`XFF_DEPTH`; the `login_attempts` table is created in prod with the correct shape and all existing tables/PKs verified intact. Probed live `POST /api/launcher/login` with a throwaway email — 5 free attempts return 401, then 429 + a counting-down `Retry-After`, exactly as designed. **⚠️ Migration gotcha (for next time):** `db:push` against prod errors part-way with `42P16 column "id" is in a primary key` — there's a **pre-existing schema drift** (the live DB differs from `schema.ts` such that push wants to drop/recreate a PK constraint). It created `login_attempts` first (additive, succeeded) then refused the destructive ALTER, so prod is intact — but **don't re-run `db:push`** (it retries that destructive op every time). Hand-apply the generated SQL or use `db:migrate` for future schema changes until the drift is investigated and fixed.

- **B38 — custom-node provisioning on a fresh machine (file-sync)** 🚧 code done (2026-06-01, follow-up to B36/B37). A fresh ComfyUI has no custom nodes → workflows die on a missing node (`RMBG`, `CannyEdgePreprocessor`, IP-Adapter…). The needed packs are **Comfy-registry installs, not git repos** (folders w/ `pyproject` version, no remote/commit), and **cm-cli is fragile headless** (needs `COMFYUI_PATH`+network, was unresponsive in test, confused by the `--base-directory Shared` split) — so a Manager-snapshot restore was rejected in favour of **file-sync** (the proven models pattern). **(a) Web (`apps/launcher-api`, launcher-studio agent):** `GET /api/launcher/comfyui-nodes` (Bearer + role `admin`; reads R2 `tools/invisible-launcher/comfyui-nodes.json`; presigns each node's `zip_key` (6 h); 401/403/404; `no-store`). Factored a shared `presignManifestEntries(entries, keyField, basePrefix, ttl)` helper in `r2.ts` now used by BOTH this and models-manifest (models contract unchanged). **(b) Desktop `Invisible_Launcher.py`** (outside repo, v**1.0.4**): `publish_custom_nodes()` zips each pack under `Shared\Custom_Nodes` (excl `ComfyUI-Manager`/`.git`/`__pycache__`/`.github`/`ckpts`) → R2 `comfyui-nodes/<name>.zip` + manifest; `sync_custom_nodes()` downloads (presigned, resumable) → `extractall` → pip-installs each `requirements.txt` with the portable Python (**surfaces nodes whose deps fail** so a half-installed pack isn't silently reported OK); "ComfyUI Custom Nodes" card (**Sync custom nodes**) + **Publish nodes** button on the publish card. **Code-reviewed:** safe-to-ship; both should-fixes (surface pip failures; exclude `ckpts` weight cache from zips) + the zip-slip trust note applied. Compiles under Python 3.9; `pnpm --filter launcher-api build` GREEN. **⏳ OWNER:** ship v1.0.4; on the main box **Publish nodes**; on a fresh machine **Sync custom nodes** → restart ComfyUI. **Note:** node-internal weight caches (`ckpts`) are excluded — nodes re-download those on first use (or seed them via Models if they live under `Shared\Models`).

- **B39 — Invisible Test Server (run the games from any machine)** 🚧 code done + locally verified (2026-06-01). The launcher's Games section only ever *linked* to a game URL; nothing hosted the games, so they couldn't be launched. Built `services/test-server/` — one small Node service that (1) serves each game's built bundle under `GET /<gameKey>/` and (2) hosts a per-game **mock Play4Fun RGS** under `/api/<gameKey>/rgs/engine`, so a game has a backend to spin against (fake money, no real spend). **One server serves ALL games.** Bundles + a `test_server/games.json` manifest (gameKey → `lines`|`book` protocol + name) live in **R2** (`test_server/<gameKey>/…`); the service **hydrates from R2 on boot** (and on secret-gated `POST /refresh`) — same R2-is-source-of-truth model as the other tools. A local-dev mode (`TEST_SERVER_LOCAL=<dir>`) hydrates from disk (no R2 creds) — **not** used on Railway. **Reused, not duplicated:** the two existing mock servers (`scripts/mock-rgs-server.mjs`, `…-book.mjs`) were refactored into a `createMockRgs(opts)` factory (per-instance session store + RNG; path-suffix routing so the handler works mounted under `/api/<gameKey>`); their standalone CLIs are preserved behind an `isMainModule` guard. Publish flow: `apps/launcher-api/scripts/publish-game-bundle.mjs <gameKey> <buildDir> [--protocol] [--name]` (R2 write creds) uploads the whole `build/` tree + updates the manifest. Games are registered in `/admin → Games` with a launch URL `https://games.invisiblewall.org/<gameKey>/?sessionID=demo&rgs_url=games.invisiblewall.org/api/<gameKey>&lang=en&currency=USD&device=desktop` (launcher appends `&project=`). Key build fact: the games' static build emits **relative** asset paths (SvelteKit runtime base = `new URL('.')`), so subpath serving needs no rebuild. **Verified locally (owner machine, 2026-06-01):** built `lines` (play4fun), served via the test server in local mode — `/healthz` OK, bundle + nested `_app/assets` files serve (200), full mock round (heartbeat→bet→play→collect, balance accounting) correct via curl, and **the game booted + rendered in a real browser tab and its auth heartbeat reached the mock through `/api/hotfruits/rgs/engine` with zero console errors**. ⏳ NOT yet done: get a visible spin to render on the production static bundle (the stale `lines` demo showed boot+connect but the reels didn't appear past the intro — likely a game-build nuance, re-check with a fresh build of the actual target game). **⏳ OWNER:** (1) create the Railway service (Root Dir = repo root, Dockerfile Path = `services/test-server/Dockerfile`; envs `R2_*`, optional `TEST_SERVER_SECRET`); (2) DNS `games.invisiblewall.org` → the service; (3) build + `publish-game-bundle.mjs` for Hot Fruits (and Book of Borut from its own repo, `--protocol book`); (4) add the `/admin → Games` rows. Docs: `docs/tools/test-server.md`.

- **B40 — "Build & publish game" one-click in the desktop launcher** 🚧 code done (2026-06-01, follow-up to B39). Turns the manual 3-step publish (build → `publish-game-bundle.mjs` → add `/admin` row) into a single button. **(a) Web (`apps/launcher-api`):** new open route `POST /api/launcher/register-game` (Bearer → `validateSession` + role `admin`; body `{key,name,url}`; validates key via `isValidGameKey` + `url` is https; **upserts** the `games` row via `gameExists`/`setGameUrl`/`renameGame`/`createGame`; `no-store`, never logs) — same bearer pattern as `comfyui-nodes`. No schema change. `pnpm --filter launcher-api build` GREEN. **(b) Desktop `Invisible_Launcher.py`** (external repo, v**1.0.5**): extended a project's `game` with an optional `publish` block (`key`, `name`, `protocol` lines|book, `build_cwd`, `build_cmd`, `build_out`, `env`); added module fns `build_game` (runs `pnpm` via subprocess, captures output, raises with tail on failure / missing pnpm), `publish_game` (uploads `build/` → R2 `test_server/<key>/` + MERGES `games.json`), `refresh_test_server` (best-effort `POST /refresh`), `register_game` (calls the web endpoint with a portal token), reusing `r2_creds`/`_r2_client`/`_resolve_service_cwd`/`portal_login`/`_prompt_portal_login`. UI: a "Cloud publish" section in `ProjectDialog` (the editable registry) + a **☁ Build & publish** button on the Projects toolbar that runs build→publish→refresh→register on a worker thread behind a progress modal. `py -3.9 -m py_compile` clean. **(c) Manifest contract** documented once in `docs/tools/test-server.md` (shape `{games:{<key>:{protocol,name,updatedAt}}}`, merge-not-clobber) + referenced from both producers. **⏳ OWNER:** (1) merge + deploy the web endpoint to `main`; (2) ship launcher v1.0.5 (`py scripts/build-launcher-exe.py --upload`, smoke-test the FROZEN exe — boto3 frozen quirk); (3) fill a project's Cloud-publish fields + click Build & publish; verify the game lands on the test server AND a row appears in `/admin → Games`. Note: the build half needs Node+pnpm+the game repo (owner/dev boxes only).

- **B41 — structured diagnostics ("what / why / how to fix") for the Atlas Maker** 🚧 code done + validated (2026-06-02). Replaces the unstructured feedback (raw subprocess stdout dumped into `<pre id="log">`; bare one-line toast strings) with explain-only diagnostic cards. Motivated by the recurring confusion where Render does "nothing" — e.g. selecting only `output_override` regions (clover/clover_glow/clover_shadow) prints "Nothing to generate (all selected regions use a user image)" with no guidance. **(a) Shared framework `services/_shared/iw_common/diagnostics.py`** (stdlib-only): `diag(code, catalog, **ctx)` → `{code,severity,title,explain,fix}` (never raises — unknown code / missing placeholder → safe generic dict); `canonical(d)` (uniform `glyph title / why / → Fix:` string); `emit(d)` prints the canonical line **plus** a machine-readable `@@DIAG@@<json>` line; `parse_diag_line()`. Importable via the same `iw_common` PYTHONPATH shim as `storage`/`cloud_paths`. **(b) Atlas catalog `services/atlas-tool/diag_catalog.py`** — owner-facing copy for 24 codes across missing-file / wrong-setting / external-service (ComfyUI auth+payment hint text folded in from the old batch_atlas inline strings). **(c) `batch_atlas.py`** — converted the failure/skip prints + ComfyUI raises (auth/payment/node/timeout — emit then raise, non-zero exit preserved) to `emit(diag(...))`. **Latent bug fixed:** the generation override-skip partition counted a region as "uses my image" on `output_override` key truthiness ALONE, never checking the file exists — so a stale key (file deleted) blocked a region from ever generating. Now `override_image_path(region) is not None` gates the skip; a missing-file override emits `OUTPUT_OVERRIDE_FILE_MISSING` (warn) and falls through to normal generation. `ALL_REGIONS_OVERRIDDEN` is emitted exactly once at the partition (the final no-jobs guard no longer re-emits it after the locked filter — that was a false-positive for the mixed override+locked case). **(d) `ui_server.py`** — `_render_state` gains a `diagnostics` list (reset per run); `_run_cmd` pulls `@@DIAG@@` lines out of the stream into it (stripped from the visible log; `/progress` serializes it for free); sync-handler failure strings routed through `_diag()` (catalog wording) — no dispatch-contract change. UI: a `#diags` panel + `renderDiagnostics`/`flashDiag` JS (card text via `textContent` — XSS-safe) + `.diag-error/.warn/.info` CSS, all inside the `PAGE.format()` string with **braces doubled**. **Validated:** `py -m py_compile` clean (4 files); real `PAGE.format()` render-smoke (balanced braces, `#diags`/`renderDiagnostics`/`flashDiag` present); unit checks (unknown code + missing placeholder don't raise, `parse_diag_line` round-trips); **code-reviewed** (no blockers; both should-fix/nit applied). Scope decision: framework is shared; only the Atlas Maker is wired this pass — **Sheet Maker (`services/sheet-tool`) still uses bare-string feedback and can adopt the same framework + its own catalog later.** **FX-aware extension (same session):** the generic "all regions use your own image" dead-end was the owner hitting an FX-layer misunderstanding — `<base>_glow`/`_shadow`/`_shine` (also `_blur`/`_zoom`) regions are **derived locally from their base** (PIL, no AI, no ref) and carry an `output_override` once built, so the AI pass skips them *by design*. Added a canonical, dependency-free classifier `shine.fx_layer_info(name)` (+ `FX_SUFFIX_MODE` map) — the **shared FX-naming primitive** intended to also back future *automated FX* (e.g. auto-rebuild FX layers when their base regenerates). `batch_atlas.py` now splits the skipped/overridden set into **FX layers vs genuine user images**: FX layers emit a new `FX_LAYERS_SKIPPED` (info) card explaining they're base-derived and giving two paths (regenerate base → ⚙ build the FX → Create Atlas; OR switch the layer to AI mode → Render); `ALL_REGIONS_OVERRIDDEN` now fires only when *every* skipped region is a genuine user image. Catalog is now 25 codes. Validated: `py -m py_compile` clean (5 files); `fx_layer_info`/`diag` unit checks + a 3-bucket partition trace (all-FX / mixed / all-user-image) pass. ⏳ NOT yet committed/deployed or browser-tested live.

- **B42 — automated FX rebuild** 🚧 code done + validated (2026-06-02), `services/atlas-tool/ui_server.py` only. Builds on B41's `shine.fx_layer_info`: when a base region's image changes, its FX layers (`<base>_glow`/`_shadow`/`_shine`/`_blur`/`_zoom`) auto-rebuild so they stay in sync — **(a)** after a Render completes, for layers whose base was just rendered; **(b)** before Create Atlas, for ALL FX layers — instead of the user clicking ⚙ per layer. **One shared code path (house rule):** extracted the FX build core out of `Handler._fxbuild` into three module-level fns — `fx_source(m, name)` (resolution order unchanged; `Handler._fx_source` is now a thin delegate), `build_fx_region(m, name, mode=None, payload=None) -> (ok, msg)` (snapshot-freeze + param-merge/clamp + make_* dispatch + save PNG + set region fields, verbatim from the old `_fxbuild`; differences: operates on the passed `m`, defaults `mode` to the region's stored mode, **now `_mirror`s the useroutput PNG to R2** (was a persistence bug — manual builds never mirrored either), and does NOT `save_manifest` — caller saves), and `rebuild_fx_layers(m, base_names=None) -> list[str]` (batch driver). `Handler._fxbuild` is now a thin wrapper (validate mode → `build_fx_region` → `save_manifest` if ok → return msg) — **manual build behaviour is byte-for-byte preserved** (same success/failure `_diag` strings) and **always runs regardless of the toggle**. **Selection/order/toggle:** `rebuild_fx_layers` reads `config['auto_fx_rebuild']` via `batch_atlas._truthy(..., default=True)` so a MISSING key = ON; candidates = regions where `fx_layer_info(name)` truthy AND base region exists AND stored `mode` in `shine.FX_PRESETS` (a *configured* FX layer), filtered to `base_names` when given (render) or all (compose); ordered ascending by FX-suffix depth so `clover_glow` rebuilds before a nested `clover_glow_shadow`; each build wrapped in try/except (one failure never aborts the rest). **Hooks:** `_run_cmd` gained `post_hook=`/`pre_note=` — `run_render` passes a `post_hook` that runs `rebuild_fx_layers(m, base_names=set(names))` + saves + surfaces "Auto-rebuilt N FX layer(s)…" in the render log (client `refreshCards()` then shows updated tiles); `run_compose` runs `rebuild_fx_layers(m, base_names=None)` BEFORE building the cmd (best-effort try/except, never blocks compose) and passes the summary as `pre_note`. **Global setting:** added `("auto_fx_rebuild", "Auto-rebuild FX layers on render/compose", "text")` to `CONFIG_FIELDS` + `"auto_fx_rebuild": "on"` to `DEFAULT_CONFIG`; `_control_html` renders it as an on/off `<select>` (missing/blank → ON). `colour` mode has no suffix (recolour-in-place) so `fx_layer_info` never matches it — correctly excluded from auto-rebuild. **Validated:** `py -m py_compile ui_server.py batch_atlas.py shine.py` clean; offline render-smoke of the real `_index()`→`PAGE.format()` path (102 KB HTML, new Settings label + `data-cfg="auto_fx_rebuild"` present, no brace error); unit harness proving candidate selection picks `{clover_glow, clover_shadow}` for `base_names={'clover'}` (excludes `apple`, orphan-base layers, and non-preset-mode layers), returns `[]` when toggle off, defaults ON on missing key, and orders nested layers by depth. ⏳ NOT yet committed/deployed or browser-tested live.

- **B43 — Invisible Blueprints (shareable ComfyUI workflows)** 🚧 **backbone DONE (phases 2-3) + Atlas Maker UI loop DONE (phases 5-6 + the publish-gating half of 7), 2026-06-06**; designed 2026-06-04. Upload/share ComfyUI "blueprints" (a saved generation network) in the pipeline so everyone can see + use them, with the models a blueprint needs **auto-downloaded into the local ComfyUI** (via ComfyUI-Manager's install API over the existing tunnel), and a **blueprint picker in the Atlas Maker** so "generate" runs the chosen network. Full plan in **`docs/design/invisible-blueprints.md`**. Locked decisions: model install = ComfyUI-Manager API; authoring = export ComfyUI **API-format** JSON + a **binding step** (map roles→node IDs, since today's graphs are hardcoded Python with fixed node IDs); scope = one **global `_shared/blueprints/`** library visible to everyone (mirrors `_shared/spines/`). 7-phase build plan; **phase 1 is a spike** to pin the ComfyUI-Manager download/reboot endpoints (the one external unknown + the only cloud→local-filesystem boundary crossing).
- **B44 — games no longer land "global" on publish (project scope required)** ✅ DONE + shipped (2026-06-06). Bug: the GAMES section showed games from EVERY project under any client/project selection (e.g. both Book of Borut and Hotfruits appeared under Borut/HotFruits). Root cause was DATA, not the filter — `listGamesForProject` correctly returns `projectKey = NULL` (global) OR `= active`, but the auto-publish path (`POST /api/launcher/register-game`, called by the desktop launcher's ☁ Build & publish) treated a missing `project` as global, so every published game landed `NULL`. Nothing is hardcoded for `book_of_borut`. **Fix — three parts, all code done:** **(a) Web (`apps/launcher-api/src/routes/api/launcher/register-game/+server.ts`):** `project` is now REQUIRED — missing → 400 `Missing project`, unknown → 400 `Unknown project`; a global game is only creatable deliberately via `/admin → Games`. **(b) `/admin → Games` Create form (`admin/+page.svelte`):** the project `<select>` now defaults to the currently-active project (`data.activeProjectKey`) instead of "Global", so manual creation can't silently drift global either; the explicit "Global (all projects)" option is still selectable on purpose. `pnpm --filter launcher-api build` GREEN. **(c) Desktop `Invisible_Launcher.py` (external repo, bumped v1.0.5 → v1.0.6):** added a "Portal project key" field to a project's Cloud-publish block (`pub_project_var` + UI row + saved as `publish.project`); `register_game` now sends `{key,name,url,project}` and refuses to publish with a clear message if the field is empty; the HTTP error handler surfaces the endpoint's 400 reason (e.g. unknown project → hints at the underscore/hyphen slug gotcha). `py -m py_compile` clean. **CLOSED OUT (2026-06-06):** (1) ✅ web change committed + pushed to `main` (`8b5fb6f`), auto-deployed by Railway; (2) ✅ owner scoped the live `hotfruits`/`bookofborut` rows via `/admin → Games → Save scope` — symptom cleared; (3) ✅ launcher **v1.0.6** built + uploaded to R2 (`tools/invisible-launcher/Invisible_Launcher.exe` + `latest.json`, sha256 `8eeea28e15d5…`) and committed to the `invisible-launcher` repo (`3e0e12d`). Remaining per-game step is routine: fill the new "Portal project key" on each game's Cloud-publish settings before the next ☁ Build & publish.

  - **Backbone shipped (phases 2-3, BUILT-IN pipelines only) — `services/atlas-tool/`, code in working tree (NOT committed):** the data-driven pipeline path is in place and a 4th pipeline is now **two dropped files, zero Python change**. **(a) Loader `blueprints.py`** — hydrates the global `_shared/blueprints/` R2 prefix into staging (once per process, incremental, best-effort like the rest of cloud_paths), parses + validates `blueprint.json` (required roles `positive`/`seed`/`output` mapped), exposes `list_blueprints()` / `get_blueprint(id) -> {graph, bindings, meta}` (+ `validate_against_graph` for uploads). Self-contained, no secrets. **(b) Generic runner `batch_atlas.build_workflow_blueprint(region, style, blueprint) -> (wf, output_node_id)`** — deep-copies the API-format graph and applies the bindings (§2): positive/negative/seed via the SAME `_resolve_text` the sdxl branch uses, width/height, the `style_ref`/`shape_ref` LoadImage `image` fields (so the existing `_upload_workflow_refs` uploads them unchanged + `shape_ref` is normalized), and the output SaveImage `filename_prefix` = the project prefix. **(c) Dispatch + output generalization in `run_region`** — when `region_pipeline(region)` ∉ `{sdxl,flux,gpt_image}` AND a blueprint with that id exists, route to the generic runner; the hardcoded SaveImage node read `"17"` is generalized to `bindings.output.node` (defaults `"17"` for the built-in Python builders). The proven sdxl/flux/gpt path stays the default and behaves identically when no blueprint is selected — ZERO regression. **(d) Seeded 3 built-ins** as reference blueprints + templates under `blueprints_src/{sdxl,flux,gpt_image}/` (tracked source; API-format `workflow.json` + `blueprint.json` with correct bindings, derived faithfully from the builders / the old `flux_pipeline.blueprint.json`); `seed_blueprints.py` mirrors them to R2 `_shared/blueprints/`. The 3 stray UI-format `*.workflow.json` exports were folded into the dirs as `workflow.ui.json` (human authoring source, not uploaded); the tracked `flux_pipeline.blueprint.json` was deleted (folded into `blueprints_src/flux/`). **(e) README** `blueprints_src/README.md` documents the "drop `<id>/workflow.json` (API format) + `<id>/blueprint.json` (bindings)" flow + the bindings schema. **Validated:** `py -m py_compile *.py` clean; a throwaway self-test loaded the seeded `sdxl` blueprint, ran `build_workflow_blueprint` with a sample region, and asserted the prompt landed in node 10, seed in node 13, the style_ref filename on the LoadImage, output node = `"17"`, save_prefix = `<C>/<P>/batch/h1` (prefix resolved LIVE via the thread-local context). **Owner-side verify owed:** live ComfyUI generation through a blueprint (needs the local GPU/tunnel; gpt_image also needs `COMFY_ORG_API_KEY` + the `Images to RGB` node) — NOT testable from here. **Owner steps to activate:** `py seed_blueprints.py` (R2 creds in env) then RESTART atlas-tool so it hydrates. **Deferred at backbone time, NOW DONE (UI loop, 2026-06-06):** phase 5/6 (pick + upload UI) and the tool half of phase 7 (publish gating) — see the "UI loop" note below. **Still deferred:** only phase 1/4 = ComfyUI-Manager model auto-download spike (the `models[]` in `blueprint.json` is read-only metadata today; an uploaded blueprint's models must already be installed in the local ComfyUI). The phase 5/6 UI loop + the phase-7 publish gating (tool + launcher) all landed 2026-06-06 — see the "UI loop" + "Publish-gating" notes below.

  - **UI loop shipped (`services/atlas-tool/`, committed `f00ffd5`; launcher side `bc98bb9`+`9c9ef0f`) — 2026-06-06:** **(1) Pipeline picker lists blueprints** — `_pipeline_options_html(current)` renders the `pipeline` `<select>` as two `<optgroup>`s ("Built-in" = sdxl/flux/gpt_image, "Blueprints" = every shared blueprint whose id ISN'T one of the three keywords, so the built-in reference blueprints aren't double-listed and a built-in keyword keeps the proven Python path per `run_region` dispatch). Current value always kept selectable even if its blueprint vanished. Saves through the existing `data-cfg="pipeline"` → `/saveconfig` path (no save change). **(2) `/refresh` re-hydrates blueprints** — `_refresh()` now also calls `blueprints.hydrate(force=True)` (best-effort) so a seeded/uploaded blueprint appears after the existing "↻ Refresh from R2" button, no restart. **(3) In-browser upload + bind (`/uploadblueprint`)** — a "🧩 Blueprints" settings panel lists the library + (when allowed) a "＋ New blueprint" button opening a modal: pick an API-format `workflow.json`, parse in-browser, candidate-filter nodes per role by `class_type`/inputs (CLIPTextEncode→positive/negative; `seed`/`noise_seed` input→seed; width/height inputs→size; LoadImage→refs; SaveImage→output), enter name/description/base, POST. Server `Handler._uploadblueprint(payload)` slugs the name via `project_paths.r2_slug` → `<id>`, refuses built-in ids (sdxl/flux/gpt_image), warns on overwrite without `overwrite:true`, validates the graph is API-format JSON with required roles (positive/seed/output) bound to existing nodes (`blueprints.validate_against_graph`), writes `blueprint.json` (version/id/name/description/author/createdAt/base/bindings/`models:[]`; author = `IW_USER_NAME` env else "uploaded") + `workflow.json` to BOTH `BLUEPRINTS_STAGING/<id>/` AND R2 `_shared/blueprints/<id>/` directly via `storage.put` (NOT the per-route project push — blueprints are outside the project tree, like `seed_blueprints.py`), then `blueprints.hydrate(force=True)`. Returns a readable string on every path (never a 500). **(4) Per-blueprint options** — when the selected pipeline is a blueprint, `pipeVisible(g,p)` hides the built-in model `<select>`s (the sdxl/flux/gpt_image data-pipe groups) while keeping the universal + generic "both" generation fields (size/padding) editable, and the per-region Advanced ref fields (`style_ref`/`shape_ref`) show only when the active blueprint binds that role (`BP_BOUND_ROLES` map injected from `list_blueprints`+`get_blueprint`). **Publish-gating (phase-7 tool+launcher half) DONE + HARDENED (2026-06-06, post-review):** gated by KNOWLEDGE OF A SECRET, not a forgeable flag. The first review build used a bare `bp=1`, but every atlas user already holds the read secret `?k=`, so a constant flag gated nothing — anyone could self-publish/overwrite shared blueprints. Now a shared **`ATLAS_BLUEPRINT_SECRET`** (env on BOTH launcher + atlas-tool): the launcher (`atlas/+page.server.ts`) appends **`bp=<ATLAS_BLUEPRINT_SECRET>`** to the `/atlas` redirect ONLY when the user holds `blueprintPublish` AND the secret is set; the tool's `_resolve_publish()` requires param/cookie to EQUAL the secret (sets cookie `atlas_bp=<secret>`, same attrs as `atlas_tool`). When the secret is UNSET it falls back to dev-only (`not ATLAS_TOOL_SECRET` = local-open, or the explicit `ATLAS_BLUEPRINT_PUBLISH` env) — **fail safe: publishing is OFF in a deployed tool until the secret is set on both services.** `/uploadblueprint` also caps the body (~4 MB) + returns a readable error (never 500) on a malformed envelope. Refuses with a clear message when `can_publish` is false; the "＋ New blueprint" UI is hidden when false. **OWNER STEP ✅ DONE (2026-06-06):** `ATLAS_BLUEPRINT_SECRET` set as a Railway shared variable on BOTH launcher-api + atlas-tool, so publishing is live in prod (grant `blueprintPublish` per role in `/admin → Roles` to authorize publishers). **Launcher side also landed (phase 7):** `roles.ts` `blueprintPublish` capability (admin ON, else OFF; grant/revoke in `/admin` Roles matrix), `toolScope.includeBlueprints` read-allow for `_shared/blueprints/`. **Validated:** `py -m py_compile *.py` clean; `PAGE.format()` render-smoke (template parses, `BP_BOUND_ROLES`/"New blueprint" present, no brace error); a self-test of `_uploadblueprint` (offline, `storage.put` stubbed) asserting a valid graph+bindings with `can_publish=True` writes both files + `get_blueprint(id)` then loads it, `can_publish=False` refuses, built-in id refused, duplicate-without-overwrite warns, bad-node binding rejected with the node id, `overwrite=True` succeeds; plus `_pipeline_options_html` dedupe of built-ins from the Blueprints optgroup. **Owner-side verify owed:** the rendered HTML in a real browser + live ComfyUI generation through an uploaded blueprint (local GPU/tunnel) — not testable here.

**`.atlas` geometry — Part B (data migration), PENDING owner + R2 creds:** existing manifests still point `atlas.atlas_file`/`atlas.source_image` at local Windows paths; the real `.atlas` + page bitmaps live in `apps/<game>/static/assets/spines/<group>/` (pages often `.webp` while some manifests name `.png` — fix the mismatch on repoint). Migration = upload each `.atlas` + page to R2 `atlas_maker/unassigned/cloud/input/refs/atlas/<name>`, repoint manifest fields to `refs/atlas/<name>.<ext>`, re-upload manifests, restart atlas-tool. Easiest path: per region card, **Upload .atlas (B10)** auto-does it. (`fix_manifest_paths.py` still targets the STALE `atlas_maker/cloud/<proj>/` prefix — needs `atlas_maker/unassigned/cloud/`.) Needs `R2_*` env + the engine repo. (Part A code ✅ done.)

### Key lessons
Full list (+ the 2026-05-29 session lessons) in `docs/history.md#key-lessons`. The operationally-critical few:
- **Always `git push`** after committing — Railway can't deploy local-only commits.
- **Railway staged vars** — adding a var only stages it; click "Apply changes / Deploy". Prefer a **code default** for non-secret config so deploys don't depend on the dashboard.
- **Cloudflare blocks `Python-urllib` UA with 403** — always send a custom User-Agent to ComfyUI.
- **Windows trailing-dot folders** — Windows silently strips a trailing `.` (`symbols.` → `symbols` locally but NOT on Linux/R2); normalize path segments in R2 manifests. Watch on any Windows-sourced data.
- **The desktop `Invisible_Launcher.py` runs from source under Python 3.9** (VS `Python39_64`) — so **no PEP 604 `X | None`** or other 3.10+ runtime syntax in annotations (the built exe uses a newer Python and hides it). The file now carries `from __future__ import annotations` (lazy annotations) to neutralize this; keep it.

---

Full done-work changelog: `docs/history.md`.
